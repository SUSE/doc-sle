<?xml version="1.0" encoding="UTF-8"?>
<sect1 xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="deployment_prep_zseries_prep_secure_boot.xml" version="5.0" xml:id="sec-zseries-prep-secure-boot">
 <title>Boot sicuro</title>

 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>

 <para>
  Per poter utilizzare la funzionalità di boot sicuro in un sistema IBM Z, è necessario soddisfare le seguenti condizioni.
 </para>

 <itemizedlist>
  <listitem>
   <para>
    Il computer deve essere z15 T01, z15 T02, LinuxONE III LT1, LinuxONE III LT2 o un modello successivo.
   </para>
  </listitem>
  <listitem>
   <para>
    È necessario utilizzare una partizione LPAR. Il boot sicuro non è supportato in z/VM e KVM.
   </para>
  </listitem>
  <listitem>
   <para>
    Nella partizione LPAR deve essere abilitato il boot sicuro.
   </para>
  </listitem>
  <listitem>
   <para>
    È necessario utilizzare dischi SCSI (FCP). Il boot sicuro non è supportato in DASD.
   </para>
  </listitem>
 </itemizedlist>

 <note>
  <title>migrazione hardware</title>
  <para>
   In caso di migrazione a un altro computer (ad esempio, da z13 a z15), verificare che la partizione LPAR sul computer di destinazione abbia lo stato di boot sicuro del sistema nel disco.
  </para>
 </note>

 <para>
  È necessario modificare lo stato di boot sicuro secondo la procedura riportata di seguito.
 </para>

 <procedure>
  <title>Modifica dello stato di boot sicuro</title>
  <step>
   <para>
    Abilitare il boot sicuro in YaST e scrivere il nuovo boot loader.
   </para>
  </step>
  <step>
   <para>
    Arrestare il sistema.
   </para>
  </step>
  <step>
   <para>
    Modificare la configurazione della partizione LPAR (abilitare o disabilitare il boot sicuro).
   </para>
  </step>
  <step>
   <para>
    Avviare il sistema.
   </para>
  </step>
 </procedure>

 <note>
  <title>boot sicuro nella console HMC</title>
  <para>
   Il sistema sul disco configurato con il parametro <literal>secure=1</literal> può essere avviato in una console HMC z15 a condizione che il firmware supporti il nuovo formato su disco (condizione sempre valida nei sistemi z15).
  </para>
 </note>
</sect1>
