<?xml version="1.0" encoding="UTF-8"?>
<sect1 xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="deployment_prep_zseries_prep_io_auto_config.xml" version="5.0" xml:id="sec-zseries-prep-io-auto-config">
 <title>IBM Z 系统上的 I/O 设备自动配置</title>

 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>

 <para>
  I/O 设备自动配置机制可让用户指定应在 Linux 上自动启用的 I/O 设备的 ID 和设置。这些信息通过在 DPM（动态分区管理器）模式下运行的 HMC 针对 LPAR 指定。
 </para>

 <note>
  <para>
   运行 DPM 的系统上可以使用 I/O 设备自动配置功能。DPM 在 LinuxONE 计算机上默认即会运行。对于 IBM Z，则必须订购此功能。
  </para>
 </note>

 <para>
  可以使用 <option>rd.zdev=no-auto</option> 内核参数来禁用自动配置。
 </para>

 <para>
  要使用 启用 I/O 自动配置，请运行 <command>yast2
  system_settings</command>yast 命令，转到<guimenu>内核设置</guimenu>部分，然后启用<guimenu>自动配置</guimenu>选项。
 </para>

 <para>
  要在 AutoYaST 配置文件中禁用 I/O 自动配置，请在引导选项后追加 <option>rd.zdev=no-auto</option>。例如：
 </para>

<screen>
&lt;bootloader&gt;
  &lt;global&gt;
    &lt;append&gt;rd.zdev=no-auto&lt;/append&gt;
  &lt;/global&gt;
&lt;/bootloader&gt;</screen>

 <para>
  安装期间，自动配置设置的状态会显示在<guimenu>安装设置</guimenu>屏幕的<guimenu>设备设置</guimenu>部分。
 </para>
</sect1>
