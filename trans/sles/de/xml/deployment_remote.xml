<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter PUBLIC "-//Novell//DTD NovDoc XML V1.0//EN" "novdocx.dtd">
<!--
*********************************
Please see LICENSE.txt for this document's license.
*********************************
-->
<chapter xml:base="deployment_remote.xml" role="General" id="cha.deployment.remoteinst">
 <title>Installation mit entferntem Zugriff</title>
 <para>
  Für die Installation von <phrase role="productname"><phrase os="sles">SUSE® Linux Enterprise Server</phrase></phrase> stehen zahlreiche Verfahren zur Auswahl. Abgesehen von der normalen Medieninstallation, die in <xref linkend="cha.inst"/> beschrieben wird, können Sie aus mehreren netzwerkbasierten Ansätzen auswählen oder eine vollautomatische Installation von <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> ausführen.
 </para>
 <para>
  Die einzelnen Methoden werden über zwei kurze Checklisten eingeführt: In einer werden die Voraussetzungen für diese Methoden aufgeführt, in der anderen die grundlegenden Verfahren dargestellt. Anschließend werden alle in diesen Installationsszenarien verwendeten Techniken ausführlicher erläutert.
 </para>
 <note>
  <para>
   In den folgenden Abschnitten wird das System, auf dem die neue <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>-Installation ausgeführt wird, als <emphasis>Zielsystem</emphasis> oder <emphasis>Installationsziel</emphasis> bezeichnet. Der Begriff <emphasis>Repository</emphasis> (früher <quote>Installationsquelle</quote> genannt) wird für alle Quellen der Installationsdaten verwendet. Dazu gehören physische Medien, z. B. CD und DVD, sowie Netzwerkserver, die die Installationsdaten im Netzwerk verteilen.
  </para>
 </note>
 <sect1 id="sec.deployment.remoteinst.scenario">
  <title>Installationsszenarien für die Installation auf entfernten Systemen</title>

  <para>
   In diesem Abschnitt werden die gängigsten Installationsszenarien für Installationen auf entfernten Systemen beschrieben. Prüfen Sie für jedes Szenario die Liste der Voraussetzungen und befolgen Sie das für dieses Szenario beschriebene Verfahren. Falls Sie für einen bestimmten Schritt ausführliche Anweisungen benötigen, folgen Sie den entsprechenden Links.
  </para>

  <sect2 id="sec.deployment.remoteinst.scenario.vncstat">
   <title>Einfache Installation mit entferntem Zugriff über VNC – Statische Netzwerkkonfiguration</title>
   <para>
    Diese Art der Installation erfordert physischen Zugriff auf das Zielsystem, um dieses für die Installation zu booten. Die Installation selbst wird vollständig von einer entfernten Arbeitsstation gesteuert, die mit dem Installationsprogramm über VNC verbunden ist. Das Eingreifen des Benutzers ist wie bei der manuellen Installation erforderlich (siehe <xref linkend="cha.inst"/>).
   </para>
   <para>
    Stellen Sie bei dieser Art der Installation sicher, dass die folgenden Anforderungen erfüllt sind:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Entferntes Repository: NFS, HTTP, FTP oder SMB mit einer funktionierenden Netzwerkverbindung.
     </para>
    </listitem>
    <listitem>
     <para>
      Zielsystem mit funktionierender Netzwerkverbindung.
     </para>
    </listitem>
    <listitem>
     <para>
      Steuersystem mit funktionierender Netzwerkverbindung und VNC-Viewer-Software oder Java-fähiger Browser (Firefox, Chromium, Internet Explorer, Opera usw.).
     </para>
    </listitem>
    <listitem>
     <para>
      Physisches Bootmedium (CD, DVD oder USB-Flash-Laufwerk) zum Booten des Zielsystems.
     </para>
    </listitem>
    <listitem>
     <para>
      Gültige statische IP-Adressen, die dem Repository und dem Steuersystem bereits zugewiesen sind.
     </para>
    </listitem>
    <listitem>
     <para>
      Gültige statische IP-Adresse, die dem Zielsystem zugewiesen wird.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.vncstat">
    <para>
     Gehen Sie wie folgt vor, um diese Art der Installation durchzuführen:
    </para>
    <step performance="required">
     <para>
      Richten Sie das Repository wie in <xref linkend="sec.deployment.remoteinst.instserver"/> beschrieben ein. Wählen Sie einen NFS-, HTTP- oder FTP-Netzwerkserver. Informationen zu einem SMB-Repository finden Sie unter <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Booten Sie das Zielsystem mit DVD1 aus dem <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>-Medienkit.
     </para>
    </step>
    <step performance="required">
     <para>
      Wenn der Bootbildschirm des Zielsystems erscheint, legen Sie mithilfe der Eingabeaufforderung für die Boot-Optionen die entsprechenden VNC-Optionen und die Adresse des Repositorys fest. Dies wird ausführlich in <xref linkend="sec.deployment.remoteinst.bootinst"/> beschrieben.
     </para>
     <para>
      Das Zielsystem bootet in eine textbasierte Umgebung und gibt die Netzwerkadresse und Anzeigenummer an, unter der die grafische Installationsumgebung über eine VNC-Viewer-Anwendung oder einen Browser erreichbar ist. VNC-Installationen werden über OpenSLP angekündigt, sofern dies in den Firewall-Einstellungen zulässig ist. Sie finden sie mit <command>slptool</command>, wie in <xref linkend="proc.deployment.remoteinst.monitor.vnc.prep"/> beschrieben.
     </para>
    </step>
    <step performance="required">
     <para>
      Öffnen Sie auf der steuernden Arbeitsstation eine VNC-Viewer-Anwendung oder einen Webbrowser und stellen Sie wie in <xref linkend="sec.deployment.remoteinst.monitor.vnc"/> beschrieben eine Verbindung zum Zielsystem her.
     </para>
    </step>
    <step performance="required">
     <para>
      Führen Sie die Installation wie in <xref linkend="cha.inst"/> beschrieben aus. Stellen Sie die Verbindung zum Zielsystem wieder her, nachdem dieses neu gebootet wurde.
     </para>
    </step>
    <step performance="required">
     <para>
      Schließen Sie die Installation ab.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.vncdyn">
   <title>Einfache Installation mit entferntem Zugriff über VNC – Dynamische Netzwerkkonfiguration</title>
   <para>
    Diese Art der Installation erfordert physischen Zugriff auf das Zielsystem, um dieses für die Installation zu booten. Die Netzwerkkonfiguration erfolgt über DHCP. Die Installation selbst wird vollständig über eine entfernte Arbeitsstation ausgeführt, die über VNC mit dem Installationsprogramm verbunden ist. Für die eigentliche Konfiguration ist jedoch das Eingreifen des Benutzers erforderlich.
   </para>
   <para>
    Stellen Sie bei dieser Art der Installation sicher, dass die folgenden Anforderungen erfüllt sind:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Entferntes Repository: NFS, HTTP, FTP oder SMB mit einer funktionierenden Netzwerkverbindung.
     </para>
    </listitem>
    <listitem>
     <para>
      Zielsystem mit funktionierender Netzwerkverbindung.
     </para>
    </listitem>
    <listitem>
     <para>
      Steuersystem mit funktionierender Netzwerkverbindung und VNC-Viewer-Software oder Java-fähiger Browser (Firefox, Chromium, Internet Explorer oder Opera).
     </para>
    </listitem>
    <listitem>
     <para>
      Booten Sie das Zielsystem mit DVD1 aus dem <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>-Medienkit.
     </para>
    </listitem>
    <listitem>
     <para>
      Laufender DHCP-Server, der IP-Adressen zur Verfügung stellt.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.vncdyn">
    <para>
     Gehen Sie wie folgt vor, um diese Art der Installation durchzuführen:
    </para>
    <step performance="required">
     <para>
      Richten Sie das Repository wie in <xref linkend="sec.deployment.remoteinst.instserver"/> beschrieben ein. Wählen Sie einen NFS-, HTTP- oder FTP-Netzwerkserver. Informationen zu einem SMB-Repository finden Sie unter <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Booten Sie das Zielsystem mit DVD1 aus dem <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>-Medienkit.
     </para>
    </step>
    <step performance="required">
     <para>
      Wenn der Bootbildschirm des Zielsystems erscheint, legen Sie mithilfe der Eingabeaufforderung für die Boot-Optionen die entsprechenden VNC-Optionen und die Adresse des Repositorys fest. Dies wird ausführlich in <xref linkend="sec.deployment.remoteinst.bootinst"/> beschrieben.
     </para>
     <para>
      Das Zielsystem bootet in eine textbasierte Umgebung und gibt die Netzwerkadresse und Anzeigenummer an, unter der die grafische Installationsumgebung über eine VNC-Viewer-Anwendung oder einen Browser erreichbar ist. VNC-Installationen werden über OpenSLP angekündigt, sofern dies in den Firewall-Einstellungen zulässig ist. Sie finden sie mit <command>slptool</command>, wie in <xref linkend="proc.deployment.remoteinst.monitor.vnc.prep"/> beschrieben.
     </para>
    </step>
    <step performance="required">
     <para>
      Öffnen Sie auf der steuernden Arbeitsstation eine VNC-Viewer-Anwendung oder einen Webbrowser und stellen Sie wie in <xref linkend="sec.deployment.remoteinst.monitor.vnc"/> beschrieben eine Verbindung zum Zielsystem her.
     </para>
    </step>
    <step performance="required">
     <para>
      Führen Sie die Installation wie in <xref linkend="cha.inst"/> beschrieben aus. Stellen Sie die Verbindung zum Zielsystem wieder her, nachdem dieses neu gebootet wurde.
     </para>
    </step>
    <step performance="required">
     <para>
      Schließen Sie die Installation ab.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.vncpxe">
   <title>Installation auf entfernten Systemen über VNC – PXE-Boot und Wake-on-LAN</title>
   <para>
    Diese Art der Installation wird vollständig automatisch durchgeführt. Der Zielcomputer wird über den entfernten Zugriff gestartet und gebootet. Das Eingreifen des Benutzers ist lediglich für die eigentliche Installation erforderlich. Dieser Ansatz ist für standortübergreifende Implementierungen geeignet.
   </para>
   <para>
    Stellen Sie bei dieser Art der Installation sicher, dass die folgenden Anforderungen erfüllt sind:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Entferntes Repository: NFS, HTTP, FTP oder SMB mit einer funktionierenden Netzwerkverbindung.
     </para>
    </listitem>
    <listitem>
     <para>
      TFTP-Server.
     </para>
    </listitem>
    <listitem>
     <para>
      Laufender DHCP-Server für Ihr Netzwerk.
     </para>
    </listitem>
    <listitem>
     <para>
      Zielsystem, das PXE-Boot-, Netzwerk- und Wake-on-LAN-fähig, angeschlossen und mit dem Netzwerk verbunden ist.
     </para>
    </listitem>
    <listitem>
     <para>
      Steuersystem mit funktionierender Netzwerkverbindung und VNC-Viewer-Software oder Java-fähiger Browser (Firefox, Chromium, Internet Explorer oder Opera).
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.vncpxe">
    <para>
     Gehen Sie wie folgt vor, um diese Art der Installation auszuführen:
    </para>
    <step performance="required">
     <para>
      Richten Sie das Repository wie in <xref linkend="sec.deployment.remoteinst.instserver"/> beschrieben ein. Wählen Sie einen NFS-, HTTP- oder FTP-Netzwerkserver aus oder konfigurieren Sie ein SMB-Repository wie in <xref linkend="sec.deployment.remoteinst.instserver.smb"/> beschrieben.
     </para>
    </step>
    <step performance="required">
     <para>
      Richten Sie einen TFTP-Server ein, auf dem das Boot-Image gespeichert wird, das vom Zielsystem abgerufen werden kann. Die Konfiguration eines solchen Servers wird in <xref linkend="sec.deployment.remoteinst.boot.tftp"/> beschrieben.
     </para>
    </step>
    <step performance="required">
     <para>
      Richten Sie einen DHCP-Server ein, der IP-Adressen für alle Computer bereitstellt und dem Zielsystem den Speicherort des TFTP-Servers bekannt gibt. Die Konfiguration eines solchen Servers wird in <xref linkend="sec.deployment.remoteinst.boot.dhcp"/> beschrieben.
     </para>
    </step>
    <step performance="required">
     <para>
      Bereiten Sie das Zielsystem für PXE-Boot vor. Dies wird ausführlich in <xref linkend="sec.deployment.remoteinst.boot.pxeprep"/> beschrieben.
     </para>
    </step>
    <step performance="required">
     <para>
      Initiieren Sie den Bootvorgang des Zielsystems mithilfe von Wake-on-LAN. Die Konfiguration eines solchen Servers wird in <xref linkend="sec.deployment.remoteinst.boot.wol"/> beschrieben.
     </para>
    </step>
    <step performance="required">
     <para>
      Öffnen Sie auf der steuernden Arbeitsstation eine VNC-Viewer-Anwendung oder einen Webbrowser und stellen Sie wie in <xref linkend="sec.deployment.remoteinst.monitor.vnc"/> beschrieben eine Verbindung zum Zielsystem her.
     </para>
    </step>
    <step performance="required">
     <para>
      Führen Sie die Installation wie in <xref linkend="cha.inst"/> beschrieben aus. Stellen Sie die Verbindung zum Zielsystem wieder her, nachdem dieses neu gebootet wurde.
     </para>
    </step>
    <step performance="required">
     <para>
      Schließen Sie die Installation ab.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.sshstat">
   <title>Einfache Installation mit entferntem Zugriff über SSH – Statische Netzwerkkonfiguration</title>
   <para>
    Diese Art der Installation erfordert physischen Zugriff auf das Zielsystem, um dieses für die Installation zu booten und um die IP-Adresse des Installationsziels zu ermitteln. Die Installation selbst wird vollständig von einer entfernten Arbeitsstation gesteuert, die mit dem Installationsprogramm über SSH verbunden ist. Das Eingreifen des Benutzers ist wie bei der regulären Installation erforderlich (siehe <xref linkend="cha.inst"/>).
   </para>
   <para>
    Stellen Sie bei dieser Art der Installation sicher, dass die folgenden Anforderungen erfüllt sind:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Entferntes Repository: NFS, HTTP, FTP oder SMB mit einer funktionierenden Netzwerkverbindung.
     </para>
    </listitem>
    <listitem>
     <para>
      Zielsystem mit funktionierender Netzwerkverbindung.
     </para>
    </listitem>
    <listitem>
     <para>
      Steuersystem mit funktionierender Netzwerkverbindung und funktionierender SSH-Client-Software.
     </para>
    </listitem>
    <listitem>
     <para>
      Booten Sie das Zielsystem mit DVD1 aus dem <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>-Medienkit.
     </para>
    </listitem>
    <listitem>
     <para>
      Gültige statische IP-Adressen, die dem Repository und dem Steuersystem bereits zugewiesen sind.
     </para>
    </listitem>
    <listitem>
     <para>
      Gültige statische IP-Adresse, die dem Zielsystem zugewiesen wird.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.sshstat">
    <para>
     Gehen Sie wie folgt vor, um diese Art der Installation durchzuführen:
    </para>
    <step performance="required">
     <para>
      Richten Sie das Repository wie in <xref linkend="sec.deployment.remoteinst.instserver"/> beschrieben ein. Wählen Sie einen NFS-, HTTP- oder FTP-Netzwerkserver. Informationen zu einem SMB-Repository finden Sie unter <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Booten Sie das Zielsystem mit DVD1 aus dem <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>-Medienkit.
     </para>
    </step>
    <step performance="required">
     <para>
      Wenn der Bootbildschirm des Zielsystems erscheint, legen Sie mithilfe der Eingabeaufforderung für die Boot-Optionen die entsprechenden VNC-Optionen und die Adresse des Repositorys fest. Dies wird ausführlich in <xref linkend="sec.deployment.remoteinst.bootinst.custom"/> beschrieben.
     </para>
     <para>
      Das Zielsystem bootet in eine textbasierte Umgebung und gibt die Netzwerkadresse an, unter der die grafische Installationsumgebung von einen beliebigen SSH-Client adressiert werden kann.
     </para>
    </step>
    <step performance="required">
     <para>
      Öffnen Sie auf der steuernden Arbeitsstation ein Terminalfenster und stellen Sie wie in <xref linkend="sec.deployment.remoteinst.monitor.ssh.connect"/> beschrieben eine Verbindung zum Zielsystem her.
     </para>
    </step>
    <step performance="required">
     <para>
      Führen Sie die Installation wie in <xref linkend="cha.inst"/> beschrieben aus. Stellen Sie die Verbindung zum Zielsystem wieder her, nachdem dieses neu gebootet wurde.
     </para>
    </step>
    <step performance="required">
     <para>
      Schließen Sie die Installation ab.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.sshdyn">
   <title>Einfache entfernte Installation über SSH – Dynamische Netzwerkkonfiguration</title>
   <para>
    Diese Art der Installation erfordert physischen Zugriff auf das Zielsystem, um dieses für die Installation zu booten und um die IP-Adresse des Installationsziels zu ermitteln. Die Installation selbst wird vollständig über eine entfernte Arbeitsstation ausgeführt, die über SSH mit dem Installationsprogramm verbunden ist. Für die eigentliche Konfiguration ist jedoch das Eingreifen des Benutzers erforderlich.
   </para>
   <note>
    <title>Vermeiden eines Verbindungsverlusts nach Schritt 2 (Installation)</title>
    <para>
     Aktivieren Sie im Dialogfeld der Netzwerkeinstellungen die Option <guimenu>Traditionelle Methode mit ifup</guimenu>, und vermeiden Sie NetworkManager. Ansonsten geht Ihre SSH-Verbindung während der Installation verloren. Sobald die Installation beendet ist, setzen Sie die Einstellungen wieder auf <guimenu>Benutzergesteuert mithilfe von NetworkManager</guimenu> zurück.
    </para>
   </note>
   <para>
    Stellen Sie bei dieser Art der Installation sicher, dass die folgenden Anforderungen erfüllt sind:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Entferntes Repository: NFS, HTTP, FTP oder SMB mit einer funktionierenden Netzwerkverbindung.
     </para>
    </listitem>
    <listitem>
     <para>
      Zielsystem mit funktionierender Netzwerkverbindung.
     </para>
    </listitem>
    <listitem>
     <para>
      Steuersystem mit funktionierender Netzwerkverbindung und funktionierender SSH-Client-Software.
     </para>
    </listitem>
    <listitem>
     <para>
      Physisches Bootmedium (CD, DVD oder USB-Flash-Laufwerk) zum Booten des Zielsystems.
     </para>
    </listitem>
    <listitem>
     <para>
      Laufender DHCP-Server, der IP-Adressen zur Verfügung stellt.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.sshdyn">
    <para>
     Gehen Sie wie folgt vor, um diese Art der Installation durchzuführen:
    </para>
    <step performance="required">
     <para>
      Richten Sie das Repository wie in <xref linkend="sec.deployment.remoteinst.instserver"/> beschrieben ein. Wählen Sie einen NFS-, HTTP- oder FTP-Netzwerkserver. Informationen zu einem SMB-Repository finden Sie unter <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Booten Sie das Zielsystem mit DVD1 aus dem <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>-Medienkit.
     </para>
    </step>
    <step performance="required">
     <para>
      Wenn der Bootbildschirm des Zielsystems erscheint, legen Sie mithilfe der Eingabeaufforderung für die Boot-Optionen die entsprechenden Parameter für die Netzwerkverbindung, den Speicherort der Installationsquelle und die SSH-Aktivierung fest. Weitere Informationen sowie ausführliche Anweisungen zur Verwendung dieser Parameter finden Sie in <xref linkend="sec.deployment.remoteinst.bootinst.custom"/>.
     </para>
     <para>
      Das Zielsystem bootet in eine textbasierte Umgebung und gibt die Netzwerkadresse an, unter der die grafische Installationsumgebung über einen beliebigen SSH-Client erreichbar ist.
     </para>
    </step>
    <step performance="required">
     <para>
      Öffnen Sie auf der steuernden Arbeitsstation ein Terminalfenster und stellen Sie wie in <xref linkend="sec.deployment.remoteinst.monitor.ssh.connect"/> beschrieben eine Verbindung zum Zielsystem her.
     </para>
    </step>
    <step performance="required">
     <para>
      Führen Sie die Installation wie in <xref linkend="cha.inst"/> beschrieben aus. Stellen Sie die Verbindung zum Zielsystem wieder her, nachdem dieses neu gebootet wurde.
     </para>
    </step>
    <step performance="required">
     <para>
      Schließen Sie die Installation ab.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.sshpxe">
   <title>Installation auf entfernten Systemen über SSH – PXE-Boot und Wake-on-LAN</title>
   <para>
    Diese Art der Installation wird vollständig automatisch durchgeführt. Der Zielcomputer wird über den entfernten Zugriff gestartet und gebootet.
   </para>
   <para>
    Stellen Sie bei dieser Art der Installation sicher, dass die folgenden Anforderungen erfüllt sind:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Entferntes Repository: NFS, HTTP, FTP oder SMB mit einer funktionierenden Netzwerkverbindung.
     </para>
    </listitem>
    <listitem>
     <para>
      TFTP-Server.
     </para>
    </listitem>
    <listitem>
     <para>
      Laufender DHCP-Server für Ihr Netzwerk, der dem zu installierenden Host eine statische IP-Adresse zuweist.
     </para>
    </listitem>
    <listitem>
     <para>
      Zielsystem, das PXE-Boot-, Netzwerk- und Wake-on-LAN-fähig, angeschlossen und mit dem Netzwerk verbunden ist.
     </para>
    </listitem>
    <listitem>
     <para>
      Steuersystem mit funktionierender Netzwerkverbindung und SSH-Client-Software.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.sshpxe">
    <para>
     Gehen Sie wie folgt vor, um diese Art der Installation auszuführen:
    </para>
    <step performance="required">
     <para>
      Richten Sie das Repository wie in <xref linkend="sec.deployment.remoteinst.instserver"/> beschrieben ein. Wählen Sie einen NFS-, HTTP- oder FTP-Netzwerkserver. Weitere Informationen zur Konfiguration eines SMB-Repositorys finden Sie in <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Richten Sie einen TFTP-Server ein, auf dem das Boot-Image gespeichert wird, das vom Zielsystem abgerufen werden kann. Die Konfiguration eines solchen Servers wird in <xref linkend="sec.deployment.remoteinst.boot.tftp"/> beschrieben.
     </para>
    </step>
    <step performance="required">
     <para>
      Richten Sie einen DHCP-Server ein, der IP-Adressen für alle Computer bereitstellt und dem Zielsystem den Speicherort des TFTP-Servers bekannt gibt. Die Konfiguration eines solchen Servers wird in <xref linkend="sec.deployment.remoteinst.boot.dhcp"/> beschrieben.
     </para>
    </step>
    <step performance="required">
     <para>
      Bereiten Sie das Zielsystem für PXE-Boot vor. Dies wird ausführlich in <xref linkend="sec.deployment.remoteinst.boot.pxeprep"/> beschrieben.
     </para>
    </step>
    <step performance="required">
     <para>
      Initiieren Sie den Bootvorgang des Zielsystems mithilfe von Wake-on-LAN. Die Konfiguration eines solchen Servers wird in <xref linkend="sec.deployment.remoteinst.boot.wol"/> beschrieben.
     </para>
    </step>
    <step performance="required">
     <para>
      Starten Sie auf der steuernden Arbeitsstation einen SSH-Client und stellen Sie wie in <xref linkend="sec.deployment.remoteinst.monitor.ssh"/> beschrieben eine Verbindung zum Zielsystem her.
     </para>
    </step>
    <step performance="required">
     <para>
      Führen Sie die Installation wie in <xref linkend="cha.inst"/> beschrieben aus. Stellen Sie die Verbindung zum Zielsystem wieder her, nachdem dieses neu gebootet wurde.
     </para>
    </step>
    <step performance="required">
     <para>
      Schließen Sie die Installation ab.
     </para>
    </step>
   </procedure>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.instserver">
  <title>Einrichten des Servers, auf dem sich die Installationsquellen befinden</title>

  <para>
   Je nachdem, welches Betriebssystem auf dem Computer ausgeführt wird, der als Netzwerk-Installationsquelle für <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> verwendet werden soll, stehen für die Serverkonfiguration mehrere Möglichkeiten zur Verfügung. Am einfachsten lässt sich ein Installationsserver mit YaST auf Enterprise Server 11/opensuse; 11.1 oder höher einrichten.
  </para>

  <tip>
   <para>
    Für die Linux-Implementierung kann auch ein Microsoft Windows-Computer als Installationsserver verwendet werden. Weitere Informationen finden Sie in <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
   </para>
  </tip>

  <sect2 id="sec.deployment.remoteinst.instserver.sles9">
   <title>Einrichten eines Installationsservers mithilfe von YaST</title>
   <para>
    YaST bietet ein grafisches Werkzeug zum Erstellen von Repositorys. Es unterstützt HTTP-, FTP- und NFS-Netzwerk-Installationsserver.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.sles9">
    <step performance="required">
     <para>
      Melden Sie sich bei dem Computer, der als Installationsserver verwendet werden soll, als <systemitem class="username">root</systemitem> an.
     </para>
    </step>
    <step performance="required">
     <para>
      Starten Sie <menuchoice><guimenu>YaST</guimenu><guimenu>Verschiedenes</guimenu><guimenu>Installationsserver</guimenu></menuchoice>.
     </para>
    </step>
    <step performance="required">
     <para>
      Wählen Sie den gewünschten Repositorytyp (HTTP, FTP oder NFS). Der ausgewählte Dienst wird bei jedem Systemstart automatisch gestartet. Wenn ein Dienst des ausgewählten Typs auf dem System bereits ausgeführt wird und Sie diesen Dienst für den Server manuell konfigurieren möchten, deaktivieren Sie die automatische Konfiguration des Serverdiensts, indem Sie <guimenu>Keine Netzwerkdienste konfigurieren</guimenu> wählen.  Geben Sie in beiden Fällen das Verzeichnis an, in dem die Installationsdaten auf dem Server zur Verfügung gestellt werden sollen.
     </para>
    </step>
    <step performance="required">
     <para>
      Konfigurieren Sie den erforderlichen Repositorytyp. Dieser Schritt bezieht sich auf die automatische Konfiguration der Serverdienste. Wenn die automatische Konfiguration deaktiviert ist, wird dieser Schritt übersprungen.
     </para>
     <para>

      Legen Sie einen Aliasnamen für das root-Verzeichnis auf dem FTP- oder HTTP-Server fest, in dem die Installationsdaten gespeichert werden sollen. Das Repository befindet sich später unter <literal>ftp://<replaceable>Server-IP</replaceable>/<replaceable>Alias</replaceable>/<replaceable>Name</replaceable></literal> (FTP) oder unter <literal>http://<replaceable>Server-IP</replaceable>/<replaceable>Alias</replaceable>/<replaceable>Name</replaceable></literal> (HTTP). <replaceable>Name </replaceable>steht für den Namen des Repositorys, das im folgenden Schritt definiert wird. Wenn Sie im vorherigen Schritt NFS ausgewählt haben, legen Sie Platzhalter und Exportoptionen fest. Der Zugriff auf den NFS-Server erfolgt über <literal>nfs://<replaceable>Server-IP</replaceable>/<replaceable>Name</replaceable></literal>. Informationen zu NFS und Exportvorgängen finden Sie in <xref linkend="cha.nfs"/>.
     </para>
     <tip>
      <title>Firewall-Einstellungen</title>
      <para>
       Stellen Sie sicher, dass die Firewall-Einstellungen Ihres Server-Systems Datenverkehr an den entsprechenden Ports für HTTP, NFS und FTP erlauben.  Ist dies nicht der Fall, aktivieren Sie zuvor <guimenu>Firewall-Port öffnen</guimenu> oder <guimenu>Firewall-Details</guimenu>.
      </para>
     </tip>
    </step>
    <step performance="required">
     <para>
      Konfigurieren Sie das Repository. Bevor die Installationsmedien in ihr Zielverzeichnis kopiert werden, müssen Sie den Namen des Repositorys angeben (dies sollte im Idealfall eine leicht zu merkende Abkürzung des Produkts und der Version sein). YaST ermöglicht das Bereitstellen von ISO-Images der Medien an Stelle von Kopien der Installations-DVDs. Wenn Sie diese Funktion verwenden möchten, aktivieren Sie das entsprechende Kontrollkästchen und geben Sie den Verzeichnispfad an, in dem sich die ISO-Dateien lokal befinden. Je nachdem, welches Produkt mithilfe dieses Installationsservers verteilt werden soll, können mehrere Add-on-CDs oder Service-Pack-CDs erforderlich sein. Sie müssen als zusätzliche Repositorys hinzugefügt werden.  Um den Installationsserver über OpenSLP im Netzwerk bekannt zu geben, aktivieren Sie die entsprechende Option.
     </para>
     <tip>
      <para>
       Wenn Ihr Netzwerk diese Option unterstützt, sollten Sie Ihr Repository auf jeden Fall über OpenSLP bekannt machen. Dadurch ersparen Sie sich die Eingabe des Netzwerk-Installationspfads auf den einzelnen Zielcomputern. Die Zielsysteme werden unter Verwendung der SLP-Boot-Option gebootet und finden das Netzwerk-Repository ohne weitere Konfigurationsschritte. Weitere Informationen zu dieser Option finden Sie in <xref linkend="sec.deployment.remoteinst.bootinst"/>.
      </para>
     </tip>
    </step>
    <step performance="required">
     <para>
      Laden Sie die Installationsdaten hoch. Der die meiste Zeit in Anspruch nehmende Schritt bei der Konfiguration eines Installationsservers ist das Kopieren der eigentlichen Installations-DVDs. Legen Sie die Medien in der von YaST angegebenen Reihenfolge ein und warten Sie, bis der Kopiervorgang abgeschlossen ist. Wenn alle Quellen erfolgreich kopiert wurden, kehren Sie zur Übersicht der vorhandenen Repositorys zurück und schließen Sie die Konfiguration, indem Sie <guimenu>Verlassen</guimenu> wählen.
     </para>
     <para>
      Der Installationsserver ist jetzt vollständig konfiguriert und betriebsbereit. Er wird bei jedem Systemstart automatisch gestartet. Es sind keine weiteren Aktionen erforderlich. Sie müssen diesen Dienst lediglich ordnungsgemäß manuell konfigurieren und starten, wenn die automatische Konfiguration der ausgewählten Netzwerkdienste mit YaST anfänglich deaktiviert wurde.
     </para>
    </step>
   </procedure>
   <para>
    Wählen Sie zum Deaktivieren eines Repositorys das zu entfernende Repository aus und wählen Sie dann <guimenu>Löschen</guimenu>. Die Installationsdaten werden vom System entfernt. Um den Netzwerkdienst zu deaktivieren, verwenden Sie das entsprechende YaST-Modul.
   </para>
   <para>
    Wenn der Installationsserver die Installationsdaten für mehrere Produkte einer Produktversion zur Verfügung stellen soll, starten Sie das YaST -Installationservermodul, und wählen Sie in der Übersicht der vorhandenen Repositorys die Option <guimenu>Hinzufügen</guimenu>, um das neue Repository zu konfigurieren.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.nfs">
   <title>Manuelles Einrichten eines NFS-Repositorys</title>
   
   <para>
    Das Einrichten einer NFS-Quelle für die Installation erfolgt in zwei Hauptschritten. Im ersten Schritt erstellen Sie die Verzeichnisstruktur für die Installationsdaten und kopieren diese in die Struktur. Im zweiten Schritt exportieren Sie das Verzeichnis mit den Installationsdaten in das Netzwerk.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.cdir">
    <para>
     Gehen Sie wie folgt vor, um ein Verzeichnis für die Installationsdaten zu erstellen:
    </para>
    <step performance="required">
     <para>
      Melden Sie sich als <systemitem class="username">root</systemitem>-Benutzer an.
     </para>
    </step>
    <step performance="required">
     <para>
      Erstellen Sie ein Verzeichnis, in dem die Installationsdaten gespeichert werden sollen, und wechseln Sie in dieses Verzeichnis. Beispiel:
     </para>
<screen>mkdir install/<replaceable>product</replaceable>/<replaceable>productversion</replaceable>
cd install/<replaceable>product</replaceable>/<replaceable>productversion</replaceable></screen>
     <para>
      Ersetzen Sie <replaceable>Produkt</replaceable> durch eine Abkürzung des Produktnamens und <replaceable>Produktversion</replaceable> durch eine Zeichenkette, die den Produktnamen und die Version enthält.
     </para>
    </step>
    <step performance="required">
     <para>
      Führen Sie für die einzelnen im Medienkit enthaltenen DVDs die folgenden Kommandos aus:
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        Kopieren Sie den gesamten Inhalt der Installations-DVD in das Server-Installationsverzeichnis:
       </para>
<screen>cp -a /media/<replaceable>path_to_your_DVD_drive .</replaceable></screen>
       <para>
        Ersetzen Sie <replaceable>path_to_your_DVD_drive </replaceable> durch den tatsächlichen Pfad, in dem sich das DVD-Laufwerk befindet. Dies kann je nach Laufwerktyp, der auf dem System verwendet wird, <filename>cdrom</filename>, <filename>cdrecorder</filename>, <filename>dvd</filename> oder <filename>dvdrecorder</filename> sein.
       </para>
      </step>
      <step performance="required">
       <para>
        Benennen Sie das Verzeichnis in die DVD-Nummer um:
       </para>
<screen>mv <replaceable>path_to_your_DVD_drive</replaceable> DVD<replaceable>x</replaceable></screen>
       <para>
        Ersetzen Sie <replaceable>x</replaceable> durch die Nummer der DVD.
       </para>
      </step>
     </substeps>
    </step>
   </procedure>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.export">
    <para>
     In <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> können Sie das Repository mit NFS unter Verwendung von YaST exportieren. Führen Sie dazu die folgenden Schritte aus:
    </para>
    <step performance="required">
     <para>
      Melden Sie sich als <systemitem class="username">root</systemitem>-Benutzer an.
     </para>
    </step>
    <step performance="required">
     <para>
      Starten Sie <menuchoice><guimenu>YaST</guimenu><guimenu>Netzwerkdienste</guimenu><guimenu>NFS-Server</guimenu></menuchoice>.
     </para>
    </step>
    <step performance="required">
     <para>
      Wählen Sie <guimenu>Starten</guimenu> und <guimenu>Firewall-Port öffnen</guimenu> und klicken Sie auf <guimenu>Weiter</guimenu>.
     </para>
    </step>
    <step performance="required">
     <para>
      Wählen Sie <guimenu>Verzeichnis hinzufügen</guimenu> und navigieren Sie zum Verzeichnis mit den Installationsquellen, in diesem Fall <filename><replaceable>Produktversion</replaceable></filename>.
     </para>
    </step>
    <step performance="required">
     <para>
      Wählen Sie <guimenu>Host hinzufügen</guimenu>, und geben Sie die Hostnamen der Computer ein, auf die die Installationsdaten exportiert werden sollen. An Stelle der Hostnamen können Sie hier auch Platzhalter, Netzwerkadressbereiche oder den Domänennamen Ihres Netzwerks eingeben. Geben Sie die gewünschten Exportoptionen an oder übernehmen Sie die Vorgabe, die für die meisten Konfigurationen ausreichend ist. Weitere Informationen dazu, welche Syntax beim Exportieren von NFS-Freigaben verwendet wird, finden Sie auf der man-Seite zu <systemitem>exports</systemitem>.
     </para>
    </step>
    <step performance="required">
     <para>
      Klicken Sie auf <guimenu>Verlassen</guimenu>. Der NFS-Server, auf dem sich das <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>-Repository befindet, wird automatisch gestartet und in den Bootvorgang integriert.
     </para>
    </step>
   </procedure>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.exportman">
    <para>
     Wenn Sie die Repositorys nicht mit dem YaST-NFS-Servermodul, sondern manuell exportieren möchten, gehen Sie wie folgt vor:
    </para>
    <step performance="required">
     <para>
      Melden Sie sich als <systemitem class="username">root</systemitem>-Benutzer an.
     </para>
    </step>
    <step performance="required">
     <para>
      Öffnen Sie die Datei <filename>/etc/exports</filename> und geben Sie die folgende Zeile ein:
     </para>
<screen>/<replaceable>productversion</replaceable> *(ro,root_squash,sync)</screen>
     <para>
      Dadurch wird das Verzeichnis <filename>/<replaceable>Produktversion</replaceable></filename> auf alle Hosts exportiert, die Teil dieses Netzwerks sind oder eine Verbindung zu diesem Server herstellen können. Um den Zugriff auf diesen Server zu beschränken, geben Sie an Stelle des allgemeinen Platzhalters <literal>*</literal> Netzmasken oder Domänennamen an. Weitere Informationen hierzu finden Sie auf der man-Seite für den Befehl <systemitem>export</systemitem>. Speichern und schließen Sie diese Konfigurationsdatei.
     </para>
    </step>
    <step performance="required">
     <para>
      Um den NFS-Dienst zu der beim Booten des System generierten Liste der Server hinzuzufügen, führen Sie die folgenden Befehle aus:
     </para>
<screen>systemctl enable nfsserver.service</screen>
    </step>
    <step performance="required">
     <para>
      Starten Sie den NFS-Server mit <command>systemctl start nfsserver.service</command>. Wenn Sie die Konfiguration des NFS-Servers zu einem späteren Zeitpunkt ändern müssen, ändern Sie die Konfigurationsdatei wie erforderlich und starten die den NFS-Daemon neu, indem Sie <command>systemctl restart nfsserver.service</command> eingeben.
     </para>
    </step>
   </procedure>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.slpann">
    <para>
     Die Bekanntgabe des NFS-Servers über OpenSLP stellt dessen Adresse allen Clients im Netzwerk zur Verfügung.
    </para>
    <step performance="required">
     <para>
      Melden Sie sich als <systemitem class="username">root</systemitem>-Benutzer an.
     </para>
    </step>
    <step performance="required">
     <para>
      Erstellen Sie die Konfigurationsdatei <filename>/etc/slp.reg.d/install.suse.nfs.reg</filename> mit folgenden Zeilen:
     </para>
<screen>
# Register the NFS Installation Server
service:install.suse:nfs://$HOSTNAME/<replaceable>path_to_repository</replaceable>/DVD1,en,65535 
description=NFS Repository</screen>
     <para>
      Ersetzen Sie  <replaceable>path_to_repository</replaceable> durch den eigentlichen Pfad der Installationsquelle auf dem Server.
     </para>
    </step>
    <step performance="required">
     <para>
      Starten Sie den OpenSLP-Daemon mit <command>systemctl start slpd.service</command>.
     </para>
    </step>
   </procedure>
   <para os="sles">
    Weitere Informationen zu OpenSLP finden Sie in der Paket-Dokumentation im Verzeichnis <filename>/usr/share/doc/packages/openslp/</filename> oder in <xref linkend="cha.slp"/>. Weitere Informationen zu NFS finden Sie unter <xref linkend="cha.nfs"/>.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.ftp">
   <title>Manuelles Einrichten eines FTP-Repositorys</title>
   <para>
    Das Erstellen eines FTP-Repositorys ist dem Erstellen eines NFS-Repositorys sehr ähnlich. Ein FTP-Repository kann ebenfalls mit OpenSLP im Netzwerk bekannt gegeben werden.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.ftp">
    <step performance="required">
     <para>
      Erstellen Sie wie in <xref linkend="sec.deployment.remoteinst.instserver.nfs"/> beschrieben ein Verzeichnis für die Installationsquellen.
     </para>
    </step>
    <step performance="required">
     <para>
      Konfigurieren Sie den FTP-Server für die Verteilung des Inhalts des Installationsverzeichnisses:
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        Melden Sie sich als <systemitem class="username">root</systemitem> an, und installieren Sie mithilfe der YaST-Softwareverwaltung das Paket <systemitem>vsftpd</systemitem>.
       </para>
      </step>
      <step performance="required">
       <para>
        Wechseln Sie in das root-Verzeichnis des FTP-Servers:
       </para>
<screen>cd <option>/srv/ftp</option></screen>
      </step>
      <step performance="required">
       <para>
        Erstellen Sie im root-Verzeichnis des FTP-Servers ein Unterverzeichnis für die Installationsquellen:
       </para>
<screen>mkdir <replaceable>repository</replaceable></screen>
       <para>
        Ersetzen Sie <replaceable>repository</replaceable> durch den Produktnamen.
       </para>
      </step>
      <step performance="required">
       <para>
        Hängen Sie den Inhalt des Installations-Repository in der change-root-Umgebung des FTP-Servers ein:
       </para>
<screen>mount --bind <replaceable>path_to_repository</replaceable> /srv/ftp/<replaceable>repository</replaceable></screen>
       <para>
        Ersetzen Sie <replaceable>path_to_repository</replaceable> und <replaceable>Repository</replaceable> durch die entsprechenden Werte für Ihre Konfiguration. Wenn diese Einstellungen dauerhaft übernommen werden sollen, fügen Sie sie zu <filename>/etc/fstab</filename> hinzu.
       </para>
      </step>
      <step performance="required">
       <para>
        Starten Sie vsftpd mit <command>vsftpd</command>.
       </para>
      </step>
     </substeps>
    </step>
    <step performance="required">
     <para>
      Geben Sie das Repository über OpenSLP bekannt, sofern dies von Ihrer Netzwerkkonfiguration unterstützt wird:
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        Erstellen Sie die Konfigurationsdatei <filename>/etc/slp.reg.d/install.suse.ftp.reg</filename> mit folgenden Zeilen:
       </para>
<screen>
# Register the FTP Installation Server
service:install.suse:ftp://$HOSTNAME/<replaceable>repository</replaceable>/DVD1,en,65535 
description=FTP Repository</screen>
       <para>
        Ersetzen Sie <replaceable>repository</replaceable> durch den Namen des Repository-Verzeichnisses auf Ihrem Server. Die Zeile <literal>Dienst:</literal> sollte als eine fortlaufende Zeile eingegeben werden.
       </para>
      </step>
      <step performance="required">
       <para>
        Starten Sie den OpenSLP-Daemon mit <command>systemctl start slpd.service</command>.
       </para>
      </step>
     </substeps>
    </step>
   </procedure>
   <tip os="sles">
    <title>Konfigurieren eines FTP-Servers mit YaST</title>
    <para>
     Wenn Sie lieber YaST verwenden, anstatt den FTP-Installationsserver manuell zu konfigurieren, finden Sie unter <xref linkend="cha.ftp"/> weitere Informationen zum Verwenden des YaST-FTP-Servermoduls.
    </para>
   </tip>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.http">
   <title>Manuelles Einrichten eines HTTP-Repositorys</title>
   <para>
    Das Erstellen eines HTTP-Repositorys ist dem Erstellen eines NFS-Repositorys sehr ähnlich. Ein HTTP-Repository kann ebenfalls mit OpenSLP im Netzwerk bekannt gegeben werden.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.http">
    <step performance="required">
     <para>
      Erstellen Sie wie in <xref linkend="sec.deployment.remoteinst.instserver.nfs"/> beschrieben ein Verzeichnis für die Installationsquellen.
     </para>
    </step>
    <step performance="required">
     <para>
      Konfigurieren Sie den HTTP-Server für die Verteilung des Inhalts des Installationsverzeichnisses:
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        <phrase os="sles">Installieren Sie den Webserver Apache wie in <xref linkend="sec.apache2.quickstart.installation"/></phrase> beschrieben.
       </para>
      </step>
      <step performance="required">
       <para>
        Wechseln Sie in das root-Verzeichnis des HTTP-Servers (<filename>/srv/www/htdocs</filename>) und erstellen Sie ein Unterverzeichnis für die Installationsquellen:
       </para>
<screen>mkdir <replaceable>repository</replaceable></screen>
       <para>
        Ersetzen Sie <replaceable>repository</replaceable> durch den Produktnamen.
       </para>
      </step>
      <step performance="required">
       <para>
        Erstellen Sie einen symbolischen Link vom Speicherort der Installationsquellen zum root-Verzeichnis des Webservers (<filename>/srv/www/htdocs</filename>):
       </para>
<screen>ln -s <replaceable>/path_to_repository </replaceable>/srv/www/htdocs/<replaceable>repository</replaceable></screen>
      </step>
      <step performance="required">
       <para>
        Ändern Sie die Konfigurationsdatei des HTTP-Servers (<filename>/etc/apache2/default-server.conf</filename>) so, dass sie symbolischen Links folgt. Ersetzen Sie die folgende Zeile:
       </para>
<screen>Options None</screen>
       <para>
        mit
       </para>
<screen>Options Indexes FollowSymLinks</screen>
      </step>
      <step performance="required">
       <para>
        Laden Sie die HTTP-Server-Konfiguration mit <command>systemctl reload apache2.service</command> neu.
       </para>
      </step>
     </substeps>
    </step>
    <step performance="required">
     <para>
      Geben Sie das Repository über OpenSLP bekannt, sofern dies von Ihrer Netzwerkkonfiguration unterstützt wird:
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        Erstellen Sie die Konfigurationsdatei <filename>/etc/slp.reg.d/install.suse.http.reg</filename> mit folgenden Zeilen:
       </para>
<screen>
# Register the HTTP Installation Server
service:install.suse:http://$HOSTNAME/<replaceable>repository</replaceable>/DVD1/,en,65535 
description=HTTP Repository</screen>
       <para>
        Ersetzen Sie <replaceable>repository</replaceable> durch den eigentlichen Pfad des Repositorys auf dem Server.  Die Zeile <literal>Dienst:</literal> sollte als eine fortlaufende Zeile eingegeben werden.
       </para>
      </step>
      <step performance="required">
       <para>
        Starten Sie den OpenSLP-Daemon, indem Sie <command>systemctl start slpd.service</command> verwenden.
       </para>
      </step>
     </substeps>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.smb">
   <title>Verwalten eines SMB-Repositorys</title>
   <para>
    Mithilfe von SMB können Sie die Installationsquellen von einem Microsoft Windows-Server importieren und die Linux-Implementierung starten, ohne dass ein Linux-Computer vorhanden sein muss.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.smb.export">
    <para>
     Gehen Sie wie folgt vor, um eine exportierte Windows Share mit dem<phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>-Repository einzurichten:
    </para>
    <step performance="required">
     <para>
      Melden Sie sich auf dem Windows-Computer an.
     </para>
    </step>
    <step performance="required">
     <para>
      Erstellen Sie ein neues Verzeichnis, das die gesamte Baumstruktur der Installation aufnehmen soll, und nennen Sie es beispielsweise <filename>INSTALL</filename>.
     </para>
    </step>
    <step performance="required">
     <para>
      Geben Sie diesen Ordner wie in der Windows-Dokumentation beschrieben im Netzwerk frei.
     </para>
    </step>
    <step performance="required">
     <para>
      Wechseln Sie in das freigegebene Verzeichnis, und erstellen Sie ein Unterverzeichnis mit dem Namen <filename><replaceable>Produkt</replaceable></filename>. Ersetzen Sie <replaceable>Produkt</replaceable> durch den tatsächlichen Produktnamen.
     </para>
    </step>
    <step performance="required">
     <para>
      Wechseln Sie in das Verzeichnis <filename>INSTALL/<replaceable>produkt</replaceable></filename>, und kopieren Sie jede DVD in einen separaten Ordner, z. B. <literal>DVD1</literal> und <literal>DVD2</literal>.
     </para>
    </step>
   </procedure>
   <para>
    Um eine SMB-eingehängte Freigabe als Repository zu verwenden, gehen Sie wie folgt vor:
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.smb.use">
    <step performance="required">
     <para>
      Booten Sie das Installationsziel.
     </para>
    </step>
    <step performance="required">
     <para>
      Wählen Sie <guimenu>Installation</guimenu>.
     </para>
    </step>
    <step performance="required">
     <para>
      Drücken Sie <keycap>F4</keycap>, um eine Auswahl der Repositorys anzuzeigen.
     </para>
    </step>
    <step performance="required">
     <para>
      Wählen Sie „SMB“, und geben Sie den Namen oder die IP-Adresse des Windows-Computers, den Freigabenamen (in diesem Beispiel <filename>INSTALL/<replaceable>produkt</replaceable>/DVD1</filename>), den Benutzernamen und das Passwort ein. Beispiel für die Syntax:
     </para>
<screen>smb://workdomain;user:password@server/INSTALL/DVD1</screen>
     <para>
      Wenn Sie die <keycap function="enter">Eingabetaste</keycap> drücken, wird YaST gestartet und Sie können die Installation ausführen.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.iso">
   <title>Verwenden von ISO-Images der Installationsmedien auf dem Server</title>
   <para>
    Statt physische Medien manuell in Ihr Serververzeichnis zu kopieren, können Sie auch die ISO-Images der Installationsmedien in Ihrem Installationsserver einhängen und als Repository verwenden.  Gehen Sie wie folgt vor, um einen HTTP-, NFS- oder FTP-Server einzurichten, der ISO-Images anstelle von Medienkopien verwendet:
   </para>
   <procedure>
    <step performance="required">
     <para>
      Laden Sie die ISO-Images herunter und speichern Sie sie auf dem Rechner, den Sie als Installationsserver verwenden möchten.
     </para>
    </step>
    <step performance="required">
     <para>
      Melden Sie sich als <systemitem class="username">root</systemitem>-Benutzer an.
     </para>
    </step>
    <step performance="required">
     <para>
      Wählen und erstellen Sie einen geeigneten Speicherort für die Installationsdaten. Siehe dazu <xref linkend="sec.deployment.remoteinst.instserver.nfs"/>, <xref linkend="sec.deployment.remoteinst.instserver.ftp"/> oder <xref linkend="sec.deployment.remoteinst.instserver.http"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Erstellen Sie für jede DVD ein Unterverzeichnis.
     </para>
    </step>
    <step performance="required">
     <para>
      Erteilen Sie folgenden Befehl, um jedes ISO-Image an der endgültigen Position einzuhängen und zu entpacken:
     </para>
<screen>mount -o loop <replaceable>path_to_iso</replaceable><replaceable>path_to_repository</replaceable>/<replaceable>product</replaceable>/<replaceable>mediumx</replaceable></screen>
     <para>
      Ersetzen Sie <replaceable>path_to_iso</replaceable> durch den Pfad zu Ihrer lokalen Kopie des ISO-Images, <replaceable>path_to_repository</replaceable> durch das Quellverzeichnis Ihres Servers, <replaceable>product</replaceable> durch den Produktnamen und <replaceable>mediumx</replaceable> durch Typ (CD oder DVD) und Anzahl der verwendeten Medien.
     </para>
    </step>
    <step performance="required">
     <para>
      Wiederholen Sie die vorherigen Schritte, um alle erforderlichen ISO-Images für Ihr Produkt einzuhängen.
     </para>
    </step>
    <step performance="required">
     <para>
      Starten Sie den Installationsserver wie gewohnt wie unter <xref linkend="sec.deployment.remoteinst.instserver.nfs"/>, <xref linkend="sec.deployment.remoteinst.instserver.ftp"/> oder <xref linkend="sec.deployment.remoteinst.instserver.http"/> beschrieben.
     </para>
    </step>
   </procedure>
   <para>
    Um ISO-Images beim Systemstart automatisch einzuhängen, fügen Sie die entsprechenden Einhänge-Einträge <filename>/etc/fstab</filename> hinzu. Ein Eintrag würde dann gemäß dem vorherigen Beispiel wie folgt aussehen:
   </para>
<screen><replaceable>path_to_iso</replaceable> <replaceable>path_to_repository</replaceable>/<replaceable>product</replaceable><replaceable>medium</replaceable> auto loop</screen>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.boot">
  <title>Vorbereitung des Bootvorgangs für das Zielsystem</title>

  <para>
   In diesem Abschnitt werden die für komplexe Boot-Szenarien erforderlichen Konfigurationsschritte beschrieben. Er enthält zudem Konfigurationsbeispiele für DHCP, PXE-Boot, TFTP und Wake-on-LAN.
  </para>

  <sect2 id="sec.deployment.remoteinst.boot.dhcp">
   <title>Einrichten eines DHCP-Servers</title>
   <para>
    Es gibt zwei Möglichkeiten zum Einrichten eines DHCP-Servers. Für <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> bietet YaST eine grafische Schnittstelle für den Prozess. Benutzer können die Konfigurationsdateien auch manuell bearbeiten.<phrase os="sles"> Für weitere Informationen über DHCP-Server siehe auch <xref linkend="cha.dhcp"/>.</phrase>
   </para>
   <sect3 id="sec.deployment.remoteinst.boot.dhcp.yast">
    <title>Einrichten eines DHCP-Servers mit YaST</title>
    <para>
     Fügen Sie Ihrer DHCP-Serverkonfiguration zwei Deklarationen hinzu, um den Netzwerk-Clients den Standort des TFTP-Servers mitzuteilen und die Boot-Image-Datei für das Installationsziel anzugeben.
    </para>
    <procedure id="proc.deployment.remoteinst.boot.dhcp.yast">
     <step performance="required">
      <para>
       Melden Sie sich als <systemitem class="username">root</systemitem> auf dem Computer an, der den DHCP-Server bereitstellt.
      </para>
     </step>
     <step performance="required">
      <para>
       Starten Sie <menuchoice><guimenu>YaST</guimenu><guimenu>Netzwerkdienste</guimenu><guimenu>DHCP-Server</guimenu></menuchoice>.
      </para>
     </step>
     <step performance="required">
      <para>
       Schließen Sie den Installationsassistenten für die Einrichtung des grundlegenden DHCP-Server ab.
      </para>
     </step>
     <step id="st.deployment.remoteinst.boot.dhcp.yast.expert" performance="required">
      <para>
       Wenn Sie eine Warnmeldung zum Verlassen des Start-Dialogfelds erhalten, wählen Sie <guimenu>Einstellungen für Experten</guimenu> und <guimenu>Ja</guimenu>.
      </para>
     </step>
     <step performance="required">
      <para>
       Im Dialogfeld <guimenu>Konfigurierte Deklarationen</guimenu> wählen Sie das Subnetz aus, indem sich das neue System befinden soll und klicken Sie auf <guimenu>Bearbeiten</guimenu>.
      </para>
     </step>
     <step performance="required">
      <para>
       Im Dialogfeld <guimenu>Konfiguration des Subnetzes</guimenu> wählen Sie <guimenu>Hinzufügen</guimenu>, um eine neue Option zur Subnetz-Konfiguration hinzuzufügen.
      </para>
     </step>
     <step performance="required">
      <para>
       Wählen Sie <option>Dateiname</option> und geben Sie <literal>pxelinux.0</literal> als Wert ein.
      </para>
     </step>
     <step performance="required">
      <para>
       Fügen Sie eine andere Option (<option>next-server</option>) hinzu und setzen Sie deren Wert auf die Adresse des TFTP-Servers.
      </para>
     </step>
     <step performance="required">
      <para>
       Wählen Sie <guimenu>OK</guimenu> und <guimenu>Verlassen</guimenu>, um die DHCP-Serverkonfiguration abzuschließen.
      </para>
     </step>
    </procedure>
    <para>
     Wenn Sie DHCP zum Angeben einer statischen IP-Adresse für einen bestimmten Host konfigurieren möchten, fügen Sie unter <guimenu>Einstellungen für Experten</guimenu> im DHCP-Serverkonfigurationsmodul (<xref linkend="st.deployment.remoteinst.boot.dhcp.yast.expert" xrefstyle="StepOnPage"/>) eine neue Deklaration für den Hosttyp hinzu. Fügen Sie dieser Hostdeklaration die Optionen <option>hardware</option> und <option>fixed-address</option> hinzu und bieten Sie die entsprechenden Werte an.
    </para>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.boot.dhcp.man">
    <title>Manuelles Einrichten eines DHCP-Servers</title>
    <para>
     Die einzige Aufgabe des DHCP-Servers ist neben der Bereitstellung der automatischen Adresszuweisung für die Netzwerk-Clients die Bekanntgabe der IP-Adresse des TFTP-Servers und der Datei, die von den Installationsroutinen auf dem Zielcomputer abgerufen werden muss.
    </para>
    <procedure id="proc.deployment.remoteinst.boot.dhcp.man">
     <step performance="required">
      <para>
       Melden Sie sich als <systemitem class="username">root</systemitem> auf dem Computer an, der den DHCP-Server bereitstellt.
      </para>
     </step>
     <step performance="required">
      <para>
       Fügen Sie einer Subnetzkonfiguration in der Konfigurationsdatei des DHCP-Servers, die sich unter <filename>/etc/dhcpd.conf</filename> befindet, folgende Zeilen hinzu:
      </para>
<screen>subnet 192.168.1.0 netmask 255.255.255.0 { 
  range dynamic-bootp 192.168.1.200 192.168.1.228;
  # PXE related settings
  #
  # "next-server" defines the TFTP server that will be used
  next-server <replaceable>ip_tftp_server</replaceable>;
  #
  # "filename" specifies the pxelinux image on the TFTP server
  # the server runs in chroot under /srv/tftpboot
  filename  "pxelinux.0";
}</screen>
      <para>
       Ersetzen Sie <replaceable>ip_tftp_server</replaceable> durch die IP-Adresse des TFTP-Servers. Weitere Informationen zu den in <filename>dhcpd.conf</filename> verfügbaren Optionen finden Sie auf der man-Seite <systemitem>dhcpd.conf</systemitem>.
      </para>
     </step>
     <step performance="required">
      <para>
       Starten Sie den DHCP-Server neu, indem Sie <command>systemctl restart dhcpd.service</command> ausführen.
      </para>
     </step>
    </procedure>
    <para>
     Wenn Sie SSH für die Fernsteuerung einer PXE- und Wake-on-LAN-Installation verwenden möchten, müssen Sie die IP-Adresse, die der DHCP-Server dem Installationsziel zur Verfügung stellen soll, explizit angeben. Ändern Sie hierzu die oben erwähnte DHCP-Konfiguration gemäß dem folgenden Beispiel:
    </para>
<screen>group {
  # PXE related settings
  # 
  # "next-server" defines the TFTP server that will be used 
  next-server <replaceable>ip_tftp_server</replaceable>:
  # 
  # "filename" specifies the pxelinux image on the TFTP server 
  # the server runs in chroot under /srv/tftpboot
  filename "pxelinux.0";
  host test {
    hardware ethernet <replaceable>mac_address</replaceable>; 
    fixed-address <replaceable>some_ip_address</replaceable>;
    }
}
</screen>
    <para>
     Die Host-Anweisung gibt den Hostnamen des Installationsziels an. Um den Hostnamen und die IP-Adresse an einen bestimmten Host zu binden, müssen Sie die Hardware-Adresse (MAC) des Systems kennen und angeben. Ersetzen Sie alle in diesem Beispiel verwendeten Variablen durch die in Ihrer Umgebung verwendeten Werte.
    </para>
    <para>
     Nach dem Neustart weist der DHCP-Server dem angegebenen Host eine statische IP-Adresse zu, damit Sie über SSH eine Verbindung zum System herstellen können.
    </para>
   </sect3>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.tftp">
   <title>Einrichten eines TFTP-Servers</title>
   <para>
    Während einer SUSE-basierten Installation können Sie zur Einrichtung eines TFTP-Servers YaST verwenden. Alternativ können Sie diesen Server aber auch manuell einrichten. Der TFTP-Server übergibt das Boot-Image an das Zielsystem, sobald dieses gebootet ist und eine entsprechende Anforderung sendet.
   </para>
   <sect3 id="sec.deployment.remoteinst.boot.tftp.yast">
    <title>Einrichten eines TFTP-Servers mit YaST</title>
    <procedure id="proc.deployment.remoteinst.boot.tftp.yast">
     <step performance="required">
      <para>
       Melden Sie sich als <systemitem class="username">root</systemitem>-Benutzer an.
      </para>
     </step>
     <step performance="required">
      <para>
       Starten Sie <menuchoice><guimenu>YaST</guimenu><guimenu>Netzwerkdienste</guimenu><guimenu>TFTP-Server</guimenu></menuchoice> und installieren Sie das erforderliche Paket.
      </para>
     </step>
     <step performance="required">
      <para>
       Klicken Sie auf <guimenu>Aktivieren</guimenu>, um sicherzustellen, dass der Server gestartet und in die Boot-Routine aufgenommen wird. Ihrerseits sind hierbei keine weiteren Aktionen erforderlich. tftpd wird zur Boot-Zeit von xinetd gestartet.
      </para>
     </step>
     <step performance="required">
      <para>
       Klicken Sie auf <guimenu>Firewall-Port öffnen</guimenu>, um den entsprechenden Port in der Firewall zu öffnen, die auf dem Computer aktiv ist. Diese Option ist nur verfügbar, wenn auf dem Server eine Firewall installiert ist.
      </para>
     </step>
     <step performance="required">
      <para>
       Klicken Sie auf <guimenu>Durchsuchen</guimenu>, um nach dem Verzeichnis mit dem Boot-Image zu suchen. Das Standardverzeichnis <filename>/tftpboot</filename> wird erstellt und automatisch ausgewählt.
      </para>
     </step>
     <step performance="required">
      <para>
       Klicken Sie auf <guimenu>Verlassen</guimenu>, um die Einstellungen zu übernehmen und den Server zu starten.
      </para>
     </step>
    </procedure>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.boot.tftp.man">
    <title>Manuelles Einrichten eines TFTP-Servers</title>
    <procedure id="proc.deployment.remoteinst.boot.tftp.man">
     <step performance="required">
      <para>
       Melden Sie sich als <systemitem class="username">root</systemitem> an und installieren Sie die Pakete <systemitem>tftp</systemitem> und <systemitem>xinetd</systemitem>.
      </para>
     </step>
     <step performance="required">
      <para>
       Erstellen Sie die Verzeichnisse <filename>/srv/tftpboot</filename> und <filename>/srv/tftpboot/pxelinux.cfg</filename>, sofern sie noch nicht vorhanden sind.
      </para>
     </step>
     <step performance="required">
      <para>
       Fügen Sie wie in <xref linkend="sec.deployment.remoteinst.boot.pxe"/> beschrieben die für das Boot-Image erforderlichen Dateien hinzu.
      </para>
     </step>
     <step performance="required">
      <para>
       Ändern Sie die Konfiguration von xinetd, die sich unter <filename>/etc/xinetd.d/</filename> befindet, um sicherzustellen, dass der TFTP-Server beim Booten gestartet wird:
      </para>
      <substeps performance="required">
       <step performance="required">
        <para>
         Erstellen Sie, sofern noch nicht vorhanden, in diesem Verzeichnis eine Datei namens <filename>tftp</filename>, indem Sie <command>touch tftp</command> eingeben.  Führen Sie anschließend folgenden Befehl aus: <command>chmod 755 tftp</command>.
        </para>
       </step>
       <step performance="required">
        <para>
         Öffnen Sie die Datei <filename>tftp</filename> und fügen Sie die folgenden Zeilen hinzu:
        </para>
<screen>service tftp 
{ 
        socket_type            = dgram
        protocol               = udp 
        wait                   = yes 
        user                   = root 
        server                 = /usr/sbin/in.tftpd 
        server_args            = -s /srv/tftpboot 
        disable                = no 
}
</screen>
       </step>
       <step performance="required">
        <para>
         Speichern Sie die Datei und starten Sie xinetd mit <command>rcxinetd restart</command> neu.

        </para>
       </step>
      </substeps>
     </step>
    </procedure>
   </sect3>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.pxe">
   <title>Verwenden von PXE Boot</title>
   <para>
    Einige technische Hintergrundinformationen sowie die vollständigen PXE-Spezifikationen finden Sie in der PXE-(Preboot Execution Environment-)Spezifikation (<ulink url="http://www.pix.net/software/pxeboot/archive/pxespec.pdf"/>).
   </para>
   <procedure id="proc.deployment.remoteinst.boot.pxe">
    <step performance="required">
     <para>
      Wechseln Sie in das Verzeichnis <filename>boot/&lt;architecture&gt;/loader</filename> des Installations-Repositorys und kopieren Sie die Dateien <filename>linux</filename>, <filename>initrd</filename>, <filename>message</filename>, <filename>biostest</filename> und <filename>memtest</filename> in das Verzeichnis <filename>/srv/tftpboot</filename>, indem Sie folgendes Kommando eingeben:
     </para>
<screen>cp -a linux initrd message biostest memtest /srv/tftpboot</screen>
    </step>
    <step performance="required">
     <para>
      Installieren Sie das Paket <systemitem>syslinux</systemitem> mithilfe von YaST direkt von den Installations-DVDs.
     </para>
    </step>
    <step performance="required">
     <para>
      Kopieren Sie die Datei <filename>/usr/share/syslinux/pxelinux.0</filename> in das Verzeichnis <filename>/srv/tftpboot</filename>, indem Sie folgenden Befehl eingeben:
     </para>
<screen>cp -a /usr/share/syslinux/pxelinux.0 /srv/tftpboot
     </screen>
    </step>
    <step performance="required">
     <para>
      Wechseln Sie in das Verzeichnis des Installations-Repositorys und kopieren Sie die Datei <filename>isolinux.cfg</filename> in das Verzeichnis <filename>/srv/tftpboot/pxelinux.cfg/default</filename>, indem Sie folgenden Befehl eingeben:
     </para>
<screen>cp -a boot/&lt;architecture&gt;/loader/isolinux.cfg /srv/tftpboot/pxelinux.cfg/default
     </screen>
    </step>
    <step performance="required">
     <para>
      Bearbeiten Sie die Datei <filename>/srv/tftpboot/pxelinux.cfg/default </filename> und entfernen Sie die Zeilen, die mit <literal>readinfo</literal> und <literal>framebuffer</literal> beginnen.
     </para>
    </step>
    <step performance="required">
     <para>

      Fügen Sie die folgenden Einträge in die append-Zeilen der standardmäßigen Kennungen <option>failsafe</option> und <option>apic</option> ein:
     </para>
     <variablelist>
      <varlistentry>
       <term><option>insmod=<replaceable>kernel module</replaceable></option>
       </term>
       <listitem>
        <para>
         Durch diesen Eintrag geben Sie das Netzwerk-Kernelmodul an, das zur Unterstützung der Netzwerkinstallation auf dem PXE-Client erforderlich ist. Ersetzen Sie <replaceable>kernel module</replaceable> durch den entsprechenden Modulnamen Ihres Netzwerkgeräts.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term><option>netdevice=<replaceable>interface</replaceable></option>
       </term>
       <listitem>
        <para>
         Dieser Eintrag definiert die Schnittstelle des Client-Netzwerks, die für die Netzwerkinstallation verwendet werden muss. Dieser Eintrag ist jedoch nur erforderlich und muss entsprechend angepasst werden, wenn der Client mit mehreren Netzwerkkarten ausgestattet ist. Falls nur eine Netzwerkkarte verwendet wird, kann dieser Eintrag ausgelassen werden.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term>
<option>install=nfs://<replaceable>ip_instserver</replaceable>/<replaceable>path_to_repository</replaceable>/DVD1</option>
       </term>
       <listitem>
        <para>
         Dieser Eintrag gibt den NFS-Server und das Repository für die Client-Installation an. Ersetzen Sie <replaceable>ip_instserver</replaceable> durch die tatsächliche IP-Adresse Ihres Installationsservers. <replaceable>path_to_repository</replaceable> muss durch den tatsächlichen Pfad des Repositorys ersetzt werden. HTTP-, FTP- oder SMB-Repositorys werden auf ähnliche Weise adressiert. Eine Ausnahme ist das Protokollpräfix, das wie folgt lauten sollte: <literal>http</literal>, <literal>ftp</literal> oder <literal>smb</literal>.
        </para>
        <important>
         <title>Hinzufügen von Bootoptionen</title>
         <para>
          Wenn den Installationsroutinen weitere Boot-Optionen, z. B. SSH- oder VNC-Boot-Parameter, übergeben werden sollen, hängen Sie sie an den Eintrag<option> install</option> an. Einen Überblick über die Parameter sowie einige Beispiele finden Sie in <xref linkend="sec.deployment.remoteinst.bootinst"/>.
         </para>
        </important>
       </listitem>
      </varlistentry>
     </variablelist>
     <tip>
      <title>Ändern der Kernel- und initrd-Dateinamen</title>
      <para>
       Es ist möglich, unterschiedliche Dateinamen für Kernel- und initrd-Images zu verwenden. Dies ist nützlich, wenn Sie am selben Bootserver unterschiedliche Betriebssysteme bereitstellen möchten. Sie sollten sich jedoch dessen bewusst sein, dass in den Dateinamen, die von TFTP für den PXE-Boot angegeben werden, nur ein Punkt erlaubt ist.
      </para>
     </tip>
     <para>
      Im Folgenden finden Sie die Beispieldatei <option>/srv/tftpboot/pxelinux.cfg/default.</option> Passen Sie das Protokollpräfix für das Repository gemäß der Netzwerkkonfiguration an und geben Sie die bevorzugte Methode an, mit der die Verbindung zum Installationsprogramm hergestellt werden soll, indem Sie die Optionen <literal>vnc</literal> und <literal>vncpassword</literal> oder <literal>usessh</literal> und <literal>sshpassword</literal> zum Eintrag <literal>install</literal> hinzufügen. Die durch <literal>\</literal> getrennten Zeilen müssen als fortlaufenden Zeile ohne Zeilenumbruch und ohne den <literal>\</literal> eingegeben werden.
     </para>
<screen>default harddisk 
        
# default
label linux 
  kernel linux 
  append initrd=initrd ramdisk_size=65536 \
     install=nfs://<replaceable>ip_instserver</replaceable>/<replaceable>path_to_repository</replaceable>/<replaceable>product</replaceable>/DVD1 
	    
# repair
label repair
  kernel linux
  append initrd=initrd splash=silent repair=1 showopts

# rescue 
label rescue 
  kernel linux 
  append initrd=initrd ramdisk_size=65536 rescue=1 

# bios test
label firmware
  kernel linux
  append initrd=biostest,initrd splash=silent install=exec:/bin/run_biostest showopts

#  memory test 
label memtest 
  kernel memtest 

#  hard disk 
label harddisk
  localboot 0

implicit     0 
display      message
prompt       1 
timeout      100</screen>
    </step>
    <step performance="required">
     <para>
      Ersetzen Sie <replaceable>ip_instserver</replaceable> und <replaceable>path_to_repository</replaceable> durch die in Ihrer Konfiguration verwendeten Werte.
     </para>
     <para>
      Der folgende Abschnitt dient als Kurzreferenz für die in dieser Konfiguration verwendeten PXELINUX-Optionen. Weitere Informationen zu den verfügbaren Optionen finden Sie in der Dokumentation des Pakets <systemitem>syslinux</systemitem>, die sich im Verzeichnis <filename>/usr/share/doc/packages/syslinux/</filename> befindet.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.pxeopts">
   <title>PXELINUX-Konfigurationsoptionen</title>
   <para>
    Die hier aufgeführten Optionen sind eine Teilmenge der für die PXELINUX-Konfigurationsdatei verfügbaren Optionen.
   </para>
   <variablelist>
    <varlistentry>
     <term><option>APPEND <replaceable>Optionen... </replaceable></option>
     </term>
     <listitem>
      <para>
       Fügt der Kernel-Kommandozeile eine oder mehrere Optionen hinzu. Diese werden sowohl bei automatischen als auch bei manuellen Bootvorgängen hinzugefügt. Die Optionen werden an den Beginn der Kernel-Kommandozeile gesetzt und ermöglichen, dass explizit eingegebene Kernel-Optionen sie überschreiben können.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>APPEND -</option>
     </term>
     <listitem>
      <para>
       Es wird nichts angehängt. <option>APPEND</option> mit einem Bindestrich als Argument in einem <option>LABEL</option>-Abschnitt kann zum Überschreiben einer globalen <option>APPEND</option>-Option verwendet werden.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>DEFAULT <replaceable>Kernel Optionen</replaceable>...</option>
     </term>
     <listitem>
      <para>
       Legt die standardmäßige Kernel-Kommandozeile fest. Wenn PXELINUX automatisch gebootet wird, agiert es, als wären die Einträge nach DEFAULT in der Booteingabeaufforderung eingegeben worden, außer, dass die Option für das automatische Booten (boot) automatisch hinzugefügt wird.
      </para>
      <para>
       Wenn keine Konfigurationsdatei vorhanden oder der DEFAULT-Eintrag in der Konfigurationsdatei nicht vorhanden ist, ist die Vorgabe der Kernel-Name <quote>linux</quote> ohne Optionen.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>

     <term><option>IFAPPEND <replaceable>FLAG</replaceable></option>
     </term>
     <listitem>
      <para>
       Fügt eine bestimmte Option in die Kernel-Kommandozeile ein, abhängig vom Wert für <replaceable>FLAG</replaceable>. Die Option <option>IFAPPEND</option> ist nur unter PXELINUX verfügbar. Für <replaceable>FLAG</replaceable> ist ein Wert erforderlich, siehe <xref linkend="tab.deployment.boot.pxeopts.ifappend"/>:
      </para>
      <table id="tab.deployment.boot.pxeopts.ifappend">
       <title>Generierte und hinzugefügte Optionen für Kernel-Kommandozeilen von <option>IFAPPEND</option></title>
       <tgroup cols="2">
        <colspec colwidth="1*"/>
        <colspec colwidth="4*"/>
        <thead>
         <row>
          <entry valign="middle">
           <para>
            Argument
           </para>
          </entry>
          <entry>
           <para>
            Generierte Kernel-Kommandozeile/Beschreibung
           </para>
          </entry>
         </row>
        </thead>
        <tbody>
         <row>
          <entry>
           <para>
            <option>1</option>
           </para>
          </entry>
          <entry>
<screen>ip=<replaceable>CLIENT_IP</replaceable>:<replaceable>BOOT_SERVER_IP</replaceable>:<replaceable>GW_IP</replaceable>:<replaceable>NETMASK</replaceable></screen>
           <para>
            Die Platzhalter werden auf der Grundlage der Eingaben vom DHCP/BOOTP- oder PXE-Boot-Server ersetzt.
           </para>
           <para>
            Diese Option ist kein Ersatz für das Ausführen eines DHCP-Clients im gebooteten System. Ohne regelmäßige Verlängerung läuft die vom PXE BIOS erworbene Lease ab, sodass die IP-Adresse zur erneuten Verwendung durch den DHCP-Server verfügbar wird.
           </para>
          </entry>
         </row>
         <row>
          <entry>
           <para>
            <option>2</option>
           </para>
          </entry>
          <entry>
<screen>BOOTIF=<replaceable>MAC_ADDRESS_OF_BOOT_INTERFACE</replaceable></screen>
           <para>
            Mit dieser Option lässt sich eine Zeitüberschreitung vermeiden, wenn der Installationsserver die LAN-Schnittstellen einzeln nacheinander abfragt, bis er eine Antwort von einem DHCP-Server erhält. Ein initrd-Programm kann dabei ermitteln, von welcher Schnittstelle das System gebootet wurde. linuxrc liest diese Option aus und verwendet die erkannte Netzwerkschnittstelle.
           </para>
          </entry>
         </row>
         <row>
          <entry>
           <para>
            <option>4</option>
           </para>
          </entry>
          <entry>
<screen>SYSUUID=<replaceable>SYSTEM_UUID</replaceable></screen>
           <para>
            Fügt UUIDs im Hexadezimalformat mit Kleinbuchstaben hinzu, siehe <filename>/usr/share/doc/packages/syslinux/pxelinux.txt</filename>
           </para>
          </entry>
         </row>
        </tbody>
       </tgroup>
      </table>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>LABEL <replaceable>Kennung</replaceable> KERNEL <replaceable>Image</replaceable> APPEND <replaceable>Optionen</replaceable>...</option>
     </term>
     <listitem>
      <para>
       Gibt an, dass, wenn <replaceable>Kennung</replaceable> als zu bootender Kernel eingegeben wird, PXELINUX stattdessen <replaceable>Image</replaceable> booten soll und die angegebenen <option>APPEND</option>-Optionen an Stelle der im globalen Abschnitt der Datei (vor dem ersten <option>LABEL</option>-Kommando) angegebenen Optionen verwendet werden sollen. Die Vorgabe für <replaceable>Image</replaceable> ist dieselbe wie für <replaceable>Kennung</replaceable> und wenn keine <option>APPEND</option>-Optionen angegeben sind, wird standardmäßig der globale Eintrag verwendet (sofern vorhanden). Es sind bis zu 128 <option>LABEL</option>-Einträge zulässig.
      </para>

      <para>
       PXELINUX verwendet die folgende Syntax:
      </para>
<screen>label <replaceable>mylabel</replaceable> 
  kernel <replaceable>mykernel</replaceable> 
  append <replaceable>myoptions</replaceable></screen>
      <para>
       Kennungen werden wie Dateinamen umgesetzt und müssen nach der Umsetzung (sogenanntes Mangling) eindeutig sein. Die beiden Kennungen <quote>v2.6.30</quote> und <quote>v2.6.31</quote> wären beispielsweise unter PXELINUX nicht unterscheidbar, da beide auf denselben DOS-Dateinamen umgesetzt würden.
      </para>
      <para>
       Der Kernel muss kein Linux-Kernel, sondern kann ein Bootsektor oder eine COMBOOT-Datei sein.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>LOCALBOOT <replaceable>Typ</replaceable></option>
     </term>
     <listitem>
      <para>
       Wenn Sie unter PXELINUX <option>LOCALBOOT 0</option> an Stelle einer <option>KERNEL</option>-Option angeben, bedeutet dies, dass diese bestimmte Kennung aufgerufen und die lokale Festplatte an Stelle eines Kernels gebootet wird.
      </para>
      <informaltable frame="topbot" rowsep="0">
       <tgroup cols="2">

        <thead>
         <row>
          <entry colname="1">
           <para>
            Argument
           </para>
          </entry>
          <entry colname="2">
           <para>
            Beschreibung
           </para>
          </entry>
         </row>
        </thead>
        <tbody>
         <row>
          <entry colname="1">
           <para>
            <literal>0</literal>
           </para>
          </entry>
          <entry colname="2">
           <para>
            Führt einen normalen Bootvorgang aus
           </para>
          </entry>
         </row>
         <row>
          <entry colname="1">
           <para>
            <literal>4</literal>
           </para>
          </entry>
          <entry colname="2">
           <para>
            Führt einen lokalen Bootvorgang mit dem noch im Arbeitsspeicher vorhandenen UNDI-Treiber (Universal Network Driver Interface) aus
           </para>
          </entry>
         </row>
         <row>
          <entry colname="1">
           <para>
            <literal>5</literal>
           </para>
          </entry>
          <entry colname="2">
           <para>
            Führt einen lokalen Bootvorgang mit dem gesamten PXE-Stack, einschließlich des UNDI-Treibers aus, der sich im Arbeitsspeicher befindet
           </para>
          </entry>
         </row>
        </tbody>
       </tgroup>
      </informaltable>
      <para>
       Alle anderen Werte sind nicht definiert. Wenn Sie die Werte für die UNDI- oder PXE-Stacks nicht wissen, geben Sie <literal>0</literal> an.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>TIMEOUT <replaceable>Zeitlimit</replaceable></option>
     </term>
     <listitem>
      <para>
       Gibt in Einheiten von 1/10 Sekunde an, wie lange die Booteingabeaufforderung angezeigt werden soll, bevor der Bootvorgang automatisch gestartet wird. Das Zeitlimit wird aufgehoben, sobald der Benutzer eine Eingabe über die Tastatur vornimmt, da angenommen wird, dass der Benutzer die Befehlseingabe abschließt. Mit einem Zeitlimit von Null wird das Zeitüberschreitungsoption deaktiviert (dies ist die Vorgabe). Der größtmögliche Wert für das Zeitlimit ist 35996 (etwas weniger als eine Stunde).
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>PROMPT<replaceable> flag_val</replaceable></option>
     </term>
     <listitem>
      <para>
       Wenn <option>flag_val</option> 0 ist, wird die Booteingabeaufforderung nur angezeigt, wenn die Taste <keycap function="shift">Umschalt</keycap> oder <keycap function="alt">Alt</keycap> gedrückt wird oder die <keycap>Feststelltaste </keycap>oder die Taste <keycap>Rollen </keycap> gesetzt ist (dies ist die Vorgabe). Wenn <option>flag_val</option> 1 ist, wird die Booteingabeaufforderung immer angezeigt.
      </para>
<screen>F2  <replaceable>filename</replaceable>
F1  <replaceable>filename</replaceable> 
..etc... 
F9  <replaceable>filename</replaceable>
F10 <replaceable>filename</replaceable></screen>
      <para>
       Zeigt die angegebene Datei auf dem Bildschirm an, wenn an der Booteingabeaufforderung eine Funktionstaste gedrückt wird. Mithilfe dieser Option kann auch die Preboot-Online-Hilfe implementiert werden (für die Kernel-Kommandozeilenoptionen). Aus Gründen der Kompabilität mit früheren Versionen kann <keycap>F10</keycap> auch als <keycap>F0</keycap> verwendet werden.  Beachten Sie, dass derzeit keine Möglichkeit besteht, Dateinamen an <keycap>F11</keycap> und <keycap>F12</keycap> zu binden.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.pxeprep">
   <title>Vorbereiten des Zielsystems für PXE-Boot</title>
   <para>
    Bereiten Sie das System-BIOS für PXE-Boot vor, indem Sie die PXE-Option in die BIOS-Boot-Reihenfolge aufnehmen.
   </para>
   <warning>
    <title>BIOS-Bootreihenfolge</title>
    <para>
     Die PXE-Option darf im BIOS nicht vor der Boot-Option für die Festplatte stehen. Andernfalls würde dieses System versuchen, sich selbst bei jedem Booten neu zu installieren.
    </para>
   </warning>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.wolprep">
   <title>Vorbereiten des Zielsystems für Wake-on-LAN</title>
   <para>
    Wake-on-LAN (WOL) erfordert, dass die entsprechende BIOS-Option vor der Installation aktiviert wird. Außerdem müssen Sie sich die MAC-Adresse des Zielsystems notieren. Diese Daten sind für das Initiieren von Wake-on-LAN erforderlich.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.wol">
   <title>Wake-on-LAN</title>
   <para>
    Mit Wake-on-LAN kann ein Computer über ein spezielles Netzwerkpaket, das die MAC-Adresse des Computers enthält, gestartet werden. Da jeder Computer einen eindeutigen MAC-Bezeichner hat, ist es nicht möglich, dass versehentlich ein falscher Computer gestartet wird.
   </para>
   <important>
    <title>Wake-on-LAN über verschiedene Netzwerksegmente</title>
    <para>
     Wenn sich der Steuercomputer nicht im selben Netzwerksegment wie das zu startende Installationsziel befindet, konfigurieren Sie die WOL-Anforderungen entweder so, dass sie als Multicasts verteilt werden, oder steuern Sie einen Computer in diesem Netzwerksegment per entferntem Zugriff so, dass er als Absender dieser Anforderungen agiert.
    </para>
   </important>
   <para>
    Benutzer von SUSE Linux Enterprise können zur einfachen Konfiguration von Wake-on-LAN ein YaST-Modul namens WOL verwenden. Die Benutzer anderer Betriebssysteme mit der Basis SUSE Linux können ein Kommandozeilenwerkzeug verwenden.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.wolyast">
   <title>Wake-on-LAN mit YaST</title>
   <procedure id="proc.deployment.remoteinst.boot.wolyast">
    <step performance="required">
     <para>
      Melden Sie sich als <systemitem class="username">root</systemitem>-Benutzer an.
     </para>
    </step>
    <step performance="required">
     <para>
      Starten Sie <menuchoice><guimenu>YaST</guimenu><guimenu>Netzwerkdienste</guimenu><guimenu>WOL</guimenu></menuchoice>.
     </para>
    </step>
    <step performance="required">
     <para>
      Klicken Sie auf <guimenu>Hinzufügen </guimenu>, und geben Sie den Hostnamen und die MAC-Adresse des Zielsystems ein.
     </para>
    </step>
    <step performance="required">
     <para>
      Wählen Sie zum Einschalten dieser Maschine den entsprechenden Eintrag und klicken Sie auf <guimenu>Wake up</guimenu> (Aufwachen).
     </para>
    </step>
   </procedure>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.bootinst">
  <title>Booten des Zielsystems für die Installation</title>

  <para>
   Abgesehen von der in <xref linkend="sec.deployment.remoteinst.boot.wol"/> und <xref linkend="sec.deployment.remoteinst.boot.pxe"/> beschriebenen Vorgehensweise gibt es zwei unterschiedliche Möglichkeiten, den Bootvorgang für die Installation anzupassen. Sie können entweder die standardmäßigen Boot-Optionen und Funktionstasten oder die Eingabeaufforderung für die Boot-Optionen im Bootbildschirm für die Installation verwenden, um die Boot-Optionen anzugeben, die der Installations-Kernel für die entsprechende Hardware benötigt.
  </para>

  <sect2 id="sec.deployment.remoteinst.bootinst.opts">
   <title>Standardmäßige Boot-Optionen</title>
   <para>
    Die Boot-Optionen werden unter <xref linkend="cha.inst"/> genauer erläutert. In der Regel wird durch die Auswahl von <guimenu>Installation</guimenu> der Bootvorgang für die Installation gestartet.
   </para>
   <para>
    Verwenden Sie bei Problemen <guimenu>Installation - ACPI deaktiviert</guimenu> bzw. <guimenu>Installation - Sichere Einstellungen</guimenu>. Weitere Informationen zu Fehlerbehebung beim Installationsvorgang finden Sie in <xref linkend="sec.trouble.install"/>.
   </para>
   <para>
    Die Menüleiste unten im Bildschirm enthält einige erweiterte Funktionen, die bei einigen Setups erforderlich sind. Mithilfe der F-Tasten können Sie zusätzliche Optionen angeben, die an die Installationsroutinen weitergegeben werden, ohne dass Sie die detaillierte Syntax dieser Parameter kennen müssen (siehe <xref linkend="sec.deployment.remoteinst.bootinst.custom"/>). Eine detaillierte Beschreibung der verfügbaren Funktionstasten erhalten Sie unter <xref linkend="sec.i.yast2.bootscreen"/>.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.bootinst.custom">
   <title>Benutzerdefinierte Boot-Optionen</title>
   <para>
    Mithilfe geeigneter Boot-Optionen können Sie den Installationsvorgang vereinfachen. Viele Parameter können mit den linuxrc-Routinen auch zu einem späteren Zeitpunkt konfiguriert werden, das Verwenden der Boot-Optionen ist jedoch viel einfacher. In einigen automatisierten Setups können die Boot-Optionen über die Datei <filename>initrd</filename> oder eine <filename>info</filename>-Datei bereit gestellt werden.
   </para>
   <para>
    In der folgenden Tabelle sind alle in diesem Kapitel erwähnten Installationsszenarien mit den erforderlichen Parametern für das Booten sowie die entsprechenden Boot-Optionen aufgeführt. Um eine Boot-Zeichenkette zu erhalten, die an die Installationsroutinen übergeben wird, hängen Sie alle Optionen in der Reihenfolge an, in der sie in dieser Tabelle angezeigt werden. Beispiel (alle in einer Zeile):
   </para>
<screen>install=<replaceable>xxx</replaceable> netdevice<replaceable>=xxx</replaceable> hostip=<replaceable>xxx</replaceable> netmask=<replaceable>xxx</replaceable> vnc=<replaceable>xxx</replaceable> vncpassword=<replaceable>xxx</replaceable></screen>

   <para>
    Ersetzen Sie alle <replaceable>xxx</replaceable>-Werte in dieser Zeichenkette durch die für Ihre Konfiguration geeigneten Werte.
   </para>
   <variablelist id="tab.deployment.remoteinst.bootinst.custom">
    <varlistentry>
     <term><xref linkend="cha.inst"/>
     </term>
     <listitem>
      <formalpara>
       <title>Für den Bootvorgang erforderliche Parameter</title>
       <para>
        Keine
       </para>
      </formalpara>
      <formalpara>
       <title>Bootoptionen</title>
       <para>
        Nicht erforderlich
       </para>
      </formalpara>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.vncstat"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Für den Bootvorgang erforderliche Parameter</title>
       <listitem>
        <para>
         Adresse des Installationsservers
        </para>
       </listitem>
       <listitem>
        <para>
         Netzwerkgerät
        </para>
       </listitem>
       <listitem>
        <para>
         IP-Adresse
        </para>
       </listitem>
       <listitem>
        <para>
         Netzmaske
        </para>
       </listitem>
       <listitem>
        <para>
         Gateway
        </para>
       </listitem>
       <listitem>
        <para>
         VNC-Aktivierung
        </para>
       </listitem>
       <listitem>
        <para>
         VNC-Passwort
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Bootoptionen</title>
       <listitem>
        <para>
         <filename>install=(nfs,http,?ftp,smb)://<replaceable>path_to_instmedia</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netdevice=<replaceable>some_netdevice</replaceable></filename> (nur erforderlich, wenn mehrere Netzwerkgeräte verfügbar sind)
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>hostip=<replaceable>some_ip</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netmask=<replaceable>some_netmask</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>gateway=<replaceable>ip_gateway</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>vnc=1</filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>vncpassword=<replaceable>some_password</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.vncdyn"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Für den Bootvorgang erforderliche Parameter</title>
       <listitem>
        <para>
         Adresse des Installationsservers
        </para>
       </listitem>
       <listitem>
        <para>
         VNC-Aktivierung
        </para>
       </listitem>
       <listitem>
        <para>
         VNC-Passwort
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Bootoptionen</title>
       <listitem>
        <para>
         <filename>install=(nfs,http,?ftp,smb)://<replaceable>path_to_instmedia</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <literal>vnc=1</literal>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>vncpassword=<replaceable>some_password</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.vncpxe"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Für den Bootvorgang erforderliche Parameter</title>
       <listitem>
        <para>
         Adresse des Installationsservers
        </para>
       </listitem>
       <listitem>
        <para>
         Adresse des TFTP-Servers
        </para>
       </listitem>
       <listitem>
        <para>
         VNC-Aktivierung
        </para>
       </listitem>
       <listitem>
        <para>
         VNC-Passwort
        </para>
       </listitem>
      </itemizedlist>
      <formalpara>
       <title>Bootoptionen</title>
       <para>
        Nicht zutreffend; Prozess wird über PXE und DHCP verwaltet
       </para>
      </formalpara>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.sshstat"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Für den Bootvorgang erforderliche Parameter</title>
       <listitem>
        <para>
         Adresse des Installationsservers
        </para>
       </listitem>
       <listitem>
        <para>
         Netzwerkgerät
        </para>
       </listitem>
       <listitem>
        <para>
         IP-Adresse
        </para>
       </listitem>
       <listitem>
        <para>
         Netzmaske
        </para>
       </listitem>
       <listitem>
        <para>
         Gateway
        </para>
       </listitem>
       <listitem>
        <para>
         SSH-Aktivierung
        </para>
       </listitem>
       <listitem>
        <para>
         SSH-Passwort
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Bootoptionen</title>
       <listitem>
        <para>
         <filename>install=(nfs,http,?ftp,smb)://<replaceable>path_to_instmedia</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netdevice=<replaceable>some_netdevice</replaceable></filename> (nur erforderlich, wenn mehrere Netzwerkgeräte verfügbar sind)
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>hostip=<replaceable>some_ip</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netmask=<replaceable>some_netmask</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>gateway=<replaceable>ip_gateway</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <literal>usessh=1</literal>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>sshpassword=<replaceable>some_password</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.sshdyn"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Für den Bootvorgang erforderliche Parameter</title>
       <listitem>
        <para>
         Adresse des Installationsservers
        </para>
       </listitem>
       <listitem>
        <para>
         SSH-Aktivierung
        </para>
       </listitem>
       <listitem>
        <para>
         SSH-Passwort
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Bootoptionen</title>
       <listitem>
        <para>
         <filename>install=(nfs,http,?ftp,smb)://<replaceable>path_to_instmedia</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>usessh=1</filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>sshpassword=<replaceable>some_password</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.sshpxe"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <listitem>
        <para>
         Adresse des Installationsservers
        </para>
       </listitem>
       <listitem>
        <para>
         Adresse des TFTP-Servers
        </para>
       </listitem>
       <listitem>
        <para>
         SSH-Aktivierung
        </para>
       </listitem>
       <listitem>
        <para>
         SSH-Passwort
        </para>
       </listitem>
      </itemizedlist>
      <formalpara>
       <title>Bootoptionen</title>
       <para>
        Nicht zutreffend; Prozess wird über PXE und DHCP verwaltet
       </para>
      </formalpara>
     </listitem>
    </varlistentry>
   </variablelist>
   <tip>
    <title>Weitere Informationen zu den linuxrc-Boot-Optionen</title>
    <para>
     Weitere Informationen zu den linuxrc-Boot-Optionen für das Booten eines Linux-Systems finden Sie in <ulink url="http://en.opensuse.org/SDB:Linuxrc"/>.
    </para>
   </tip>
   <sect3 id="sec.deployment.remoteinst.bootinst.custom.addon">
    <title>Installieren von Add-on-Produkten und Treiberaktualisierungen</title>
    <para>
     <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> unterstützt die Installation von Add-on-Produkten mit Erweiterung (z. B. die SUSE Linux Enterprise High Availability Extension), von Produkten und Treibern von Drittanbietern sowie von zusätzlicher Software. Geben Sie den Parameter <literal>addon=<replaceable>REPOSITORY</replaceable></literal> an, damit ein Add-on-Produkt automatisch installiert werden kann, wenn <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> im Fernverfahren bereitgestellt wird.
    </para>
    <para>
     <replaceable>REPOSITORY</replaceable> muss ein gehostetes Repository sein, das von YaST (YaST 2 oder YUM (rpm-md)) ausgelesen werden kann. ISO-Images werden zurzeit nicht unterstützt.
    </para>
    <tip>
     <title>Treiberaktualisierungen</title>
     <para>
      Aktualisierungen für Treiber finden Sie unter <ulink url="http://drivers.suse.com/"/>. Nicht alle Treiberaktualisierungen werden als Repositorys bereitgestellt; einige Aktualisierungen sind nur als ISO-Images verfügbar und können daher nicht mit dem Parameter <literal>addon</literal> installiert werden. Weitere Anweisungen zum Installieren von Treiberaktualisierungen mithife von ISO-Images finden Sie unter <ulink url="http://drivers.suse.com/doc/SolidDriver/Driver_Kits.html"/>.
     </para>
    </tip>
   </sect3>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.monitor">
  <title>Überwachen des Installationsvorgangs</title>

  <para>
   Es gibt mehrere Möglichkeiten der entfernten Überwachung des Installationsvorgangs. Wenn beim Booten für die Installation die richtigen Boot-Optionen angegeben wurden, kann die Installation und Systemkonfiguration mit VNC oder SSH von einer entfernten Arbeitsstation aus überwacht werden.
  </para>

  <sect2 id="sec.deployment.remoteinst.monitor.vnc">
   <title>VNC-Installation</title>
   <para>
    Mithilfe einer beliebigen VNC-Viewer-Software können Sie die Installation von <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> von praktisch jedem Betriebssystem aus überwachen. In diesem Abschnitt wird das Setup mithilfe einer VNC-Viewer-Anwendung oder eines Webbrowsers beschrieben.
   </para>
   <sect3 id="sec.deployment.remoteinst.monitor.vnc.prep">
    <title>Vorbereiten der VNC-Installation</title>
    <para>
     Um das Installationsziel für eine VNC-Installation vorzubereiten, müssen Sie lediglich die entsprechenden Boot-Optionen beim anfänglichen Bootvorgang für die Installation angeben (siehe <xref linkend="sec.deployment.remoteinst.bootinst.custom"/>). Das Zielsystem bootet in eine textbasierte Umgebung und wartet darauf, dass ein VNC-Client eine Verbindung zum Installationsprogramm herstellt.
    </para>
    <para>
     Das Installationsprogramm gibt die IP-Adresse bekannt und zeigt die für die Verbindung zum Installationsprogramm erforderliche Nummer an. Wenn Sie physischen Zugriff auf das Zielsystem haben, werden diese Informationen sofort nach dem Booten des Systems für die Installation zur Verfügung gestellt. Geben Sie diese Daten ein, wenn Sie von der VNC-Client-Software dazu aufgefordert werden, und geben Sie Ihr Passwort ein.
    </para>
    <para>
     Da sich das Installationsziel über OpenSLP selbst bekannt gibt, können Sie die Adressinformationen des Installationsziels über einen SLP-Browser abrufen, ohne dass Sie physischen Zugriff auf die Installation selbst haben müssen, vorausgesetzt, OpenSLP wird von der Netzwerkkonfiguration und von allen Computern unterstützt:
    </para>
    <procedure id="proc.deployment.remoteinst.monitor.vnc.prep">
     <title>Auffinden von VNC-Installationen über OpenSLP</title>
     <step performance="required">
      <para>
       Mit <command>slptool findsrvtypes | grep vnc</command> erhalten Sie eine Liste aller Dienste, die VNC anbieten. Die VNC-Installationsziele müssen unter einem Dienst mit dem Namen <literal>YaST.installation.suse</literal> bereitstehen.
      </para>
     </step>
     <step performance="required">
      <para>
       Mit <command>slptool findsrvs <replaceable>YaST.installation.suse</replaceable></command> erhalten Sie eine Liste der verfügbaren Installationen. Geben Sie die IP-Adresse und den Port (in der Regel <systemitem>5901</systemitem>) an; diese Angaben sind dem VNC-Viewer beigelegt.
      </para>
     </step>
    </procedure>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.monitor.vnc.connect">
    <title>Herstellen der Verbindung mit dem Installationsprogramm</title>
    <para>
     Es gibt zwei Möglichkeiten, eine Verbindung zu einem VNC-Server (dem Installationsziel in diesem Fall) herzustellen. Sie können entweder eine unabhängige VNC-Viewer-Anwendung unter einem beliebigen Betriebssystem starten oder die Verbindung über einen Java-fähigen Webbrowser herstellen.
    </para>
    <para>
     Mit VNC können Sie die Installation eines Linux-Systems von jedem Betriebssystem, einschließlich anderer Linux-, Windows- oder Mac OS-Betriebssysteme, aus steuern.
    </para>
    <para>
     Stellen Sie auf einem Linux-Computer sicher, dass das Paket <systemitem>tightvnc</systemitem> installiert ist. Installieren Sie auf einem Windows-Computer den Windows-Port dieser Anwendung, der über die Homepage von TightVNC (<ulink url="http://www.tightvnc.com/download.html"/>) erhältlich ist.
    </para>
    <procedure id="proc.deployment.remoteinst.monitor.vnc.connect.app">
     <para>
      Gehen Sie wie folgt vor, um eine Verbindung zu dem auf dem Zielcomputer ausgeführten Installationsprogramm herzustellen:
     </para>
     <step performance="required">
      <para>
       Starten Sie den VNC-Viewer.
      </para>
     </step>
     <step performance="required">
      <para>
       Geben Sie die IP-Adresse und die Anzeigenummer des Installationsziels wie vom SLP-Browser oder dem Installationsprogramm selbst zur Verfügung gestellt ein:

      </para>
<screen><replaceable>ip_address</replaceable>:<replaceable>display_number</replaceable></screen>
      <para>
       Auf dem Desktop wird ein Fenster geöffnet, in dem die YaST-Bildschirme wie bei einer normalen lokalen Installation angezeigt werden.
      </para>
     </step>
    </procedure>
    <para>
     Wenn Sie die Verbindung zum Installationsprogramm mithilfe eines Webbrowsers herstellen, sind Sie von der VNC-Software bzw. dem zu Grunde liegenden Betriebssystem vollkommen unabhängig. Sie können die Installation des Linux-Systems in einem beliebigen Browser (Firefox, Internet Explorer, Chromium, Opera usw.) ausführen, solange dieser Java unterstützt.)
    </para>
    <para>
     Gehen Sie wie folgt vor, um eine VNC-Installation auszuführen:
    </para>
    <procedure id="proc.deployment.remoteinst.monitor.vnc.connect.brow">
     <step performance="required">
      <para>
       Starten Sie Ihren bevorzugten Webbrowser.
      </para>
     </step>
     <step performance="required">
      <para>
       Geben Sie in der Adressleiste Folgendes ein:

      </para>
<screen>http://<replaceable>ip_address_of_target</replaceable>:5801</screen>
     </step>
     <step performance="required">
      <para>
       Geben Sie Ihr VNC-Passwort ein, wenn Sie dazu aufgefordert werden. Die YaST-Bildschirme werden im Browserfenster wie bei einer normalen lokalen Installation angezeigt.
      </para>
     </step>
    </procedure>
   </sect3>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.monitor.ssh">
   <title>SSH-Installation</title>
   <para>
    Mithilfe von SSH können Sie die Installation des Linux-Computers unter Verwendung einer beliebigen SSH-Client-Software von einem entfernten Standort aus überwachen.
   </para>
   <sect3 id="sec.deployment.remoteinst.monitor.ssh.prep">
    <title>Vorbereiten der SSH-Installation</title>
    <para>
     Zusätzlich zum Installieren der entsprechenden Softwarepakete (OpenSSH für Linux und PuTTY für Windows) müssen Sie die entsprechenden Boot-Optionen übergeben, um SSH für die Installation zu aktivieren. Weitere Informationen finden Sie in <xref linkend="sec.deployment.remoteinst.bootinst.custom"/>. OpenSSH wird auf allen SUSE Linux-basierten Betriebssystemen standardmäßig installiert.
    </para>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.monitor.ssh.connect">
    <title>Herstellen der Verbindung mit dem Installationsprogramm</title>
    <procedure id="proc.deployment.remoteinst.monitor.ssh.connect">
     <step performance="required">
      <para>
       Rufen Sie die IP-Adresse des Installationsziels ab. Wenn Sie physischen Zugriff auf den Zielcomputer haben, verwenden Sie die IP-Adresse, die von der Installationsroutine nach dem anfänglichen Bootvorgang auf der Konsole angezeigt wird. Verwenden Sie andernfalls die IP-Adresse, die diesem Host in der DHCP-Serverkonfiguration zugewiesen wurde.
      </para>
     </step>
     <step performance="required">
      <para>
       Geben Sie an der Kommandozeile den folgenden Befehl ein:
      </para>
<screen>ssh -X root@<replaceable>
ip_address_of_target</replaceable></screen>
      <para>
       Ersetzen Sie <replaceable>ip_address_of_target</replaceable> durch die IP-Adresse des Installationsziels.
      </para>
     </step>
     <step performance="required">
      <para>
       Wenn Sie zur Eingabe eines Benutzernamens aufgefordert werden, geben Sie <literal>root</literal> ein.
      </para>
     </step>
     <step performance="required">
      <para>
       Wenn Sie zur Eingabe eines Passworts aufgefordert werden, geben Sie das Passwort ein, das mit der SSH-Boot-Option festgelegt wurde. Wenn Sie sich erfolgreich authentifiziert haben, wird eine Kommandozeilenaufforderung für das Installationsziel angezeigt.
      </para>
     </step>
     <step performance="required">
      <para>
       Geben Sie <command>yast</command> ein, um das Installationsprogramm zu starten. Es wird ein Fenster geöffnet, in dem die üblichen YaST-Bildschirme wie in <xref linkend="cha.inst"/> beschrieben angezeigt werden.
      </para>
     </step>
    </procedure>
   </sect3>
  </sect2>
 </sect1>
</chapter>
