<?xml version="1.0"?>
<!DOCTYPE chapter [
  <!ENTITY % entities SYSTEM "generic-entities.ent">
  %entities;
]>

<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.0" xml:id="cha-selfinstal-procedure">
 <title>Deploying selfinstall images</title>
 <info xmlns:d="http://docbook.org/ns/docbook">
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
  <abstract>
    <para>
      The chapter describes deployment of &slema; from selfinstall pre-built ISO images.
    </para>
  </abstract>
 <revhistory xml:id="rh-cha-selfinstal-procedure">
   <revision>
     <date>2022-10-03</date>
     <revdescription>
       <para>Initial version 2022-10-03</para>
     </revdescription>
   </revision>
 </revhistory>
</info>
<para>
  &productname; provides selfinstall ISO images that enable you to deploy &slema; to your machine (either a virtual machine or a bare metal) and configure the system on the first boot.
</para>
<para>
  To prepare the setup, you need the following:  
  </para>
  <itemizedlist>
    <listitem>
      <para>
        a disk (either a physical or a virtual) where &slema; will run
      </para>
    </listitem>
    <listitem>
      <para>
        a bootable device with the selfinstall ISO (for example a USB disk)
      </para>
    </listitem>
    <listitem>
      <para>
        a device that serves as a configuration medium. To prepare the configuration device, follow the steps in <xref linkend="pro-selfinstall-configuration-device"/>.
      </para>
      <note>
        <title>The configuration device must be plugged in during the first boot.</title>
        <para>
          Bear in mind that the configuration device must be plugged in throughout the whole configuration process on the first boot. It is recommended to plug in the device before starting the installation process. However, if your firmware does not support having attached two or more USB disks on boot, you can exchange the USB device before you start the configuration process.
        </para>
      </note>
    </listitem>
  </itemizedlist>

<para>
  The following procedure describes how to prepare the configuration device: 
  </para>
<procedure xml:id="pro-selfinstall-configuration-device">
  <title>Preparing the configuration device.</title>
  <step>
   <para>
    Format the disk to any file system supported by SLE Micro: Ext3, Ext4, etc.:
   </para>
<screen>
<prompt>&gt; </prompt>sudo mkfs.ext4 /dev/sd<replaceable>Y</replaceable>
</screen>
  </step>
  <step>
   <para>
    Set the device label to either <literal>ignition</literal> (when either
    Ignition or Combustion is used) or <literal>combustion</literal> (when only
    Combustion is used). If needed, you can use uppercase letters for the labels, too. To label the device, run:
   </para>
<screen><prompt>&gt; </prompt>sudo e2label /dev/sd<replaceable>Y</replaceable> ignition</screen>
   <para>
    You can use any type of configuration storage media that your
    virtualization system or your hardware supports: ISO image, a USB
    flash disk, etc.
   </para>
  </step>
  <step>
   <para>
    Mount the device:
   </para>
<screen>
<prompt>&gt; </prompt>sudo mount /dev/sdY /mnt
</screen>
  </step>
  <step>
   <para>
    Create the directory structure as mentioned in
    <xref linkend="cha-images-ignition"/> or
    <xref linkend="cha-images-combustion"/>, depending on the configuration
    tool used:
   </para>
<screen>
<prompt>&gt; </prompt>sudo mkdir -p /mnt/ignition/
</screen>
   <para>
    or:
   </para>
<screen>
<prompt>&gt; </prompt>sudo mkdir -p /mnt/combustion/
</screen>
  </step>
  <step>
   <para>
    Prior to booting for the first time, prepare all elements of the
    configuration that will be used by
    <xref linkend="cha-images-ignition" xrefstyle="template:Ignition"/> or
    <xref linkend="cha-images-combustion" xrefstyle="template:Combustion"/>. To
    log in to your system, you need to provide a password for <systemitem class="username">root</systemitem> or set
    up passwordless authentication, otherwise the system will not be accessible
    after the first boot.
   </para>
  </step>
</procedure>

<para>
  After you have prepared the configuration device, you can begin the installation process as described below.
  </para>

  <procedure>
    <title>&slema; installation</title>
    <step>
      <para>
        Boot your machine with the selfinstall ISO attached. 
      </para>
    </step>
    <step>
      <para>
        Select <guimenu>Install SLE Micro</guimenu> to start the installation process.
      </para>
    </step>
    <step>
      <para>
        Select the disk where &slema; will be installed and confirm that you want to delete data on the disk. A &slema; image is then copied to the disk.
      </para>
    </step>
    <step>
      <para>
        Using <command>kexec</command>, your system reboots and is then prepared for the configuration process.
      </para>
    </step>
    <step>
      <para>
        Start the configuration process by selecting <guimenu>SLE Micro</guimenu>. &slema; is configured according to the instructions provided on the configuration device.
      </para>
    </step>
    <step>
      <important>
        <title>Installation using the selfinstall ISO image does not create a boot EFI entry</title>
        <para>
          During the deployment of the selfinstall ISO, the image of the system is just copied to the selected disk, therefore, an EFI boot entry is not created (like it normally would if the system is deployed using an installer). You might need to manually boot your system using the EFI shell by selecting the &slema; boot loader. After the first boot, you can use <command>efibootmgr</command> to create the boot entry. <command>efibootmgr</command> is available by default in the deployed image.
        </para>
      </important>
      <para>
        After the configuration process is complete, you can log in to your system.
      </para>
    </step>
  </procedure>

<para>
  After the first boot, you can register your &slem; instance by using the
   <command>transactional-update</command> command. For details, refer to
  <xref linkend="sec-images-registration"/>.
 </para>
 <para>
  &slema; has available extension for live patching. To use this extension, you
  need to add the extension to your subscription from the installed system. For
  details, refer to <xref linkend="sec-adding-extension"/>.
 </para> 
</chapter>
