<?xml version="1.0"?>
<!DOCTYPE chapter PUBLIC "-//Novell//DTD NovDoc XML V1.0//EN" "novdocx.dtd"
[
  <!ENTITY % NOVDOC.DEACTIVATE.IDREF "INCLUDE">
  <!ENTITY % entities SYSTEM "entity-decl.ent">
  %entities;
]>
<chapter id="cha.containers.intro">
 <title>Introduction to Linux Containers (&lxc;)</title>
  <para>
   &lxc; is a lightweight virtualization method to run multiple virtual units
   (containers, similar to the chroot environment) simultaneously on a single
   host. Containers are isolated with kernel Control Groups (cgroups) and kernel
   Namespaces.
  </para>
  <para>
   &lxc; provides an operating system level virtualization where the
   <emphasis>kernel</emphasis> controls the isolated containers. With other full
   virtualization solutions like &xen; or &kvm; the
   <emphasis>processor</emphasis> simulates a complete hardware environment and
   controls its virtual machines.
  </para>
 <sect1 id="sec.containers.intro">
  <title>Basic Components</title>
  <para>
  </para>
 </sect1>
</chapter>
