<?xml version="1.0" encoding="UTF-8"?>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="fuse.xml" version="5.0" xml:id="cha.fuse">

 <title>Zugriff auf Dateisysteme mit FUSE</title>
 <info>
      <abstract>
        <para>
    FUSE ist das Akronym für <emphasis>File System in Userspace</emphasis> (Dateisystem im Benutzerraum). Das bedeutet, Sie können ein Dateisystem als nicht privilegierter Benutzer konfigurieren und einhängen. Normalerweise müssen Sie für diese Aufgabe als <systemitem class="username">root</systemitem> angemeldet sein. FUSE alleine ist ein Kernel-Modul. In Kombination mit Plug-Ins kann FUSE auf nahezu alle Dateisysteme wie SSH-Fernverbindungen, ISO-Images und mehr erweitert werden.
   </para>
      </abstract>
      <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
        <dm:bugtracker>
        </dm:bugtracker>
        <dm:translation>Ja</dm:translation>
      </dm:docmanager>
    </info>
    <indexterm> <primary>FUSE</primary> </indexterm>

 <sect1 xml:id="sec.fuse.config">
  <title>Konfigurieren von FUSE</title><indexterm> <primary>FUSE</primary> <secondary>Konfigurieren</secondary> </indexterm>

  <para>
   Bevor Sie FUSE installieren können, müssen Sie das Paket <systemitem class="resource">fuse</systemitem> installieren. Abhängig vom gewünschten Dateisystem benötigen Sie zusätzliche Plugins, die in verschiedenen Paketen verfügbar sind. 
  </para>

  <para>
   Im Allgemeinen müssen Sie FUSE nicht konfigurieren, Sie können es einfach verwenden. Jedoch empfiehlt es sich, ein Verzeichnis anzulegen, in dem Sie alle Ihre Einhängepunkte speichern. Sie können beispielsweise das Verzeichnis <filename>~/mounts</filename> anlegen und dort Ihre Unterverzeichnisse für die verschiedenen Dateisysteme einfügen.
  </para>
 </sect1>
 <sect1 xml:id="sec.fuse.ntfs">
  <title>Einhängen einer NTFS-Partition</title><indexterm> <primary>FUSE</primary> <secondary>ntfs-3g</secondary> </indexterm> <indexterm> <primary>ntfs-3g</primary> </indexterm>

  <para>
   NTFS (<emphasis>New Technology File System</emphasis>) ist das Standard-Dateisystem von Windows. Gehen Sie zum Einhängen einer Windows-Partition als gewöhnlicher Benutzer wie folgt vor:
  </para>

  <procedure>
   <step>
    <para>
     Melden Sie sich als <systemitem class="username">root</systemitem> an und installieren Sie das Paket <systemitem class="resource">ntfs-3g</systemitem>.
    </para>
   </step>
   <step>
    <para>
     Erstellen Sie ein Verzeichnis, das als Einhängepunkt genutzt werden soll, z. B. <filename>~/mounts/windows</filename>.
    </para>
   </step>
   <step>
    <para>
     Finden Sie heraus, welche Windows-Partition Sie brauchen. Starten Sie das Partitionierungsmodul von YaST und ermitteln Sie die Partition, die zu Windows gehört; nehmen Sie jedoch keine Änderungen vor. Alternativ können Sie sich als <systemitem class="username">root</systemitem> anmelden und <command>/sbin/fdisk </command><option>-l</option> ausführen. Suchen Sie Partitionen mit dem Partitionstyp <literal>HPFS/NTFS</literal>.
    </para>
   </step>
   <step>
    <para>
     Hängen Sie die Partition im Schreib-Lese-Modus ein. Ersetzen Sie den Platzhalter <replaceable>DEVICE</replaceable> durch Ihre entsprechende Windows-Partition:
    </para>
<screen>ntfs-3g /dev/<replaceable>DEVICE</replaceable> <replaceable>MOUNT POINT</replaceable></screen>
    <para>
     Wenn die Windows-Partition im schreibgeschützten Modus verwendet werden soll, hängen Sie <option>-o ro</option> an:
    </para>
<screen>ntfs-3g /dev/<replaceable>DEVICE</replaceable> <replaceable>MOUNT POINT</replaceable> -o ro</screen>
    <para>
     Der Befehl <command>ntfs-3g</command> hängt das angegebene Gerät mit der aktuellen Benutzer- (UID) und Gruppen-ID (GID) ein. Sollen die Schreibberechtigungen auf einen anderen Benutzer eingestellt werden, rufen Sie mit dem Befehl <command>id</command> <option>USER</option> die Ausgabe der UID- und GID-Werte ab. Legen Sie ihn fest mit:
    </para>
<screen>id tux
uid=1000(tux) gid=100(users) groups=100(users),16(dialout),33(video)
ntfs-3g /dev/<replaceable>DEVICE</replaceable> <replaceable>MOUNT POINT</replaceable> -o uid=1000,gid=100</screen>
    <para>
     Weitere Optionen finden Sie auf der man-Seite.
    </para>
   </step>
  </procedure>

  <para>
   Zum Aushängen der Ressource starten Sie <command>fusermount -u </command><replaceable>MOUNT POINT</replaceable>.
  </para>
 </sect1>
 
 
 
 <sect1 xml:id="sec.fuse.moreinfo">
  <title>Weiterführende Informationen</title>

  <para>
   Weitere Informationen finden Sie auf der Homepage <link xlink:href="http://fuse.sourceforge.net"/> von FUSE.
  </para>
 </sect1>
</chapter>
