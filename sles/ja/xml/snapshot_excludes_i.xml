<?xml version="1.0" encoding="UTF-8"?>
<variablelist xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="snapshot_excludes_i.xml" version="5.0">
 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>
 <varlistentry>
  <term><filename>/boot/grub2/i386-pc</filename>、<filename>/boot/grub2/x86_64-efi</filename>、<filename>/boot/grub2/powerpc-ieee1275</filename>、<filename>/boot/grub2/s390x-emu</filename>
  </term>
  <listitem>
   <para>
    ブートローダ設定のロールバックはサポートされていません。これらのディレクトリは、アーキテクチャ固有です。最初の2つのディレクトリはAMD64/Intel 64マシン上に存在し、その後の2つのディレクトリはそれぞれIBM POWERとIBM z Systems上に存在します。
   </para>
  </listitem>
 </varlistentry>
 <varlistentry>
  <term><filename>/home</filename>
  </term>
  <listitem>
   <para>
    <filename>/home</filename>が独立したパーティションに存在していない場合、ロールバック時にデータが失われのを避けるために除外されます。
   </para>
  </listitem>
 </varlistentry>
 <varlistentry>
  <term><filename>/opt</filename>、<filename>/var/opt</filename>
  </term>
  <listitem>
   <para>
    サードパーティ製品は通常、<filename>/opt</filename>にインストールされます。ロールバック時にこれらのアプリケーションがアンインストールされるのを避けるために除外されます。
   </para>
  </listitem>
 </varlistentry>
 <varlistentry>
  <term><filename>/srv</filename>
  </term>
  <listitem>
   <para>
    WebおよびFTPサーバ用のデータが含まれています。ロールバック時にデータが失われるのを避けるために除外されます。
   </para>
  </listitem>
 </varlistentry>
 <varlistentry>
  <term><filename>/tmp</filename>、<filename>/var/tmp</filename>、<filename>/var/cache</filename>、<filename>/var/crash</filename>
  </term>
  <listitem>
   <para>
    スナップショットから除外される一時ファイルとキャッシュを含むすべてのディレクトリ。
   </para>
  </listitem>
 </varlistentry>
 <varlistentry>
  <term><filename>/usr/local</filename>
  </term>
  <listitem>
   <para>
    このディレクトリは、ソフトウェアの手動インストール時に使用します。ロールバック時にこれらのインストール済みソフトウェアがアンインストールされるのを避けるために除外されます。
   </para>
  </listitem>
 </varlistentry>
 <varlistentry>
  <term><filename>/var/lib/libvirt/images</filename>
  </term>
  <listitem>
   <para>
    libvirtで管理される仮想マシンイメージのデフォルトの場所。ロールバック時に仮想マシンイメージが古いバージョンに置き換えられないようにするために除外されます。デフォルトでは、このサブボリュームは、<literal>no copy on write</literal>オプションを使用して作成されます。
   </para>
  </listitem>
 </varlistentry>
 <varlistentry>
  <term><filename>/var/lib/mailman</filename>、<filename>/var/spool</filename>
  </term>
  <listitem>
   <para>
    電子メールまたは電子メールキューを含むディレクトリは、ロールバック後に電子メールが失われるのを避けるために除外されます。
   </para>
  </listitem>
 </varlistentry>
 <varlistentry>
  <term><filename>/var/lib/named</filename>
  </term>
  <listitem>
   <para>
    DNSサーバ用のゾーンデータが含まれます。ネームサーバがロールバック後に確実に動作できるように、スナップショットから除外されます。
   </para>
  </listitem>
 </varlistentry>
 <varlistentry>
  <term><filename>/var/lib/mariadb</filename>、<filename>/var/lib/mysql</filename>、<filename>/var/lib/pgqsl</filename>
  </term>
  <listitem>
   <para>
    これらのディレクトリにはデータベースのデータが格納されます。デフォルトでは、これれのサブボリュームは、<literal>no copy on write</literal>オプションを使用して作成されます。
   </para>
  </listitem>
 </varlistentry>
 <varlistentry>
  <term><filename>/var/log</filename>
  </term>
  <listitem>
   <para>
    ログファイルの場所。壊れたシステムのロールバック後にログファイルを分析できるようにスナップショットから除外されます。
   </para>
  </listitem>
 </varlistentry>
</variablelist>
