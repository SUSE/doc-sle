<?xml version="1.0" encoding="UTF-8"?>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="deployment_boot_parameters.xml" version="5.0" xml:id="cha-boot-parameters">
 <title>Parâmetros de boot</title>
 <info>
  <abstract>
   <para>
    O <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> permite definir vários parâmetros durante o boot, por exemplo, escolhendo a fonte dos dados de instalação ou definindo a configuração de rede.
   </para>
  </abstract>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>
 <para>
  O uso do conjunto apropriado de parâmetros de boot simplifica o procedimento de instalação. Muitos parâmetros também podem ser configurados posteriormente usando as rotinas linuxrc, mas o uso dos parâmetros de boot é mais fácil. Em algumas configurações automatizadas, os parâmetros de boot podem ser fornecidos com o <filename>initrd</filename> ou em um arquivo <filename>info</filename>.
 </para>
 <para>
  O modo como o sistema é iniciado para instalação depende da arquitetura. A inicialização do sistema é diferente para PC (AMD64/Intel 64) ou mainframe, por exemplo. Se você instalar o <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> como Convidado da VM em um hipervisor KVM ou Xen, siga as instruções para a arquitetura AMD64/Intel 64.
 </para>
 <note>
  <title>Opções e parâmetros de boot</title>
  <para>
   Em geral, os termos <emphasis>Parâmetros de Boot</emphasis> e <emphasis>Opções de Boot</emphasis> são utilizados alternadamente. Nesta documentação, usamos mais o termo <emphasis>Parâmetros de Boot</emphasis>.
  </para>
 </note>
 <sect1 xml:id="sec-boot-parameters-default">
  <title>Usando os parâmetros de boot padrão</title>

  <para>
   <phrase os="sles">Os parâmetros de boot estão descritos em detalhes no <xref linkend="cha-install"/>&#x002E;</phrase> Em geral, basta selecionar <guimenu>Instalação</guimenu> para iniciar o processo de boot da instalação.
  </para>

  <para>
   Se problemas ocorrerem, use <guimenu>Instalação—ACPI Desabilitado</guimenu> ou <guimenu>Instalação—Configurações Seguras</guimenu>. Para obter mais informações sobre solução de problemas no processo de instalação, consulte o <xref linkend="cha-installation-troubleshooting"/>.
  </para>

  <para>
   A barra de menus na tela inferior oferece algumas funcionalidades avançadas necessárias em algumas configurações. Usando as teclas de função (<keycap>F1</keycap> ... <keycap>F12</keycap>), você pode especificar opções adicionais para passar para as rotinas de instalação sem a necessidade de saber a sintaxe detalhada desses parâmetros (consulte o <xref linkend="cha-boot-parameters"/>). Há uma descrição detalhada das teclas de função disponíveis na <xref linkend="sec-boot-parameters-screen"/>.
  </para>
 </sect1>
 <sect1 xml:id="sec-boot-parameters-start-x86-64" arch="x86_64">
  <title>PC (AMD64/Intel 64/Arm AArch64)</title>

  <para>
   Esta seção descreve como mudar os parâmetros de boot para AMD64, Intel 64 e Arm AArch64.
  </para>

  <sect2 xml:id="sec-boot-parameters-screen">
   <title>Tela de boot em máquinas equipadas com BIOS tradicional</title>
   <para>
    A tela de boot exibe várias opções para o procedimento de instalação. <guimenu>Inicializar do Disco Rígido</guimenu> inicializa o sistema instalado e está marcada por padrão, pois o CD é geralmente deixado na unidade. Selecione uma das outras opções com as teclas de seta e pressione <keycap function="enter"/> para inicializá-la. As opções relevantes são:
   </para>
   <variablelist>
    <varlistentry>
     <term><guimenu>Instalação</guimenu></term>
     <listitem>
      <para>
       O modo de instalação normal. Todas as funções de hardware modernas estão habilitadas. Em caso de falha na instalação, consulte <xref linkend="vle-boot-parameters-screen-kernel"/> para obter os parâmetros de boot que desabilitam funções possivelmente problemáticas.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry os="sles;sled">
     <term><guimenu>Upgrade</guimenu></term>
     <listitem>
      <para>
       Faça upgrade do sistema. Para obter mais informações, consulte <xref os="sles;sled" linkend="cha-upgrade-paths"/>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry os="sles;sled">
     <term><menuchoice><guimenu>Mais</guimenu> <guimenu> Sistema de Recuperação</guimenu></menuchoice></term>
     <listitem>
      <para>
       Inicia um sistema Linux mínimo sem interface gráfica do usuário. Para obter mais informações, consulte
       
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><menuchoice><guimenu>Mais</guimenu> <guimenu> Inicializar Sistema Linux</guimenu></menuchoice></term>
     <listitem>
      <para>
       Inicialize um sistema Linux que já esteja instalado. Você deverá informar a partição da qual inicializar o sistema.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><menuchoice><guimenu>Mais</guimenu> <guimenu> Verificar Mídia de Instalação</guimenu></menuchoice></term>
     <listitem>
      <para>
       Essa opção só está disponível quando você instala de mídias criadas das ISOs descarregadas. Nesse caso, é recomendável verificar a integridade da mídia de instalação. Essa opção inicia o sistema de instalação antes de verificar a mídia automaticamente. No caso da verificação bem-sucedida, é iniciada a rotina de instalação normal. Se for detectada uma mídia corrompida, a rotina de instalação será interrompida. Substitua a mídia danificada e reinicie o processo de instalação.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><menuchoice><guimenu>Mais</guimenu> <guimenu> Teste de Memória</guimenu></menuchoice></term>
     <listitem>
      <para>
       Testa a RAM do sistema por meio de ciclos repetidos de leitura e gravação. Termine o teste reinicializando. Para obter mais informações, consulte a <xref linkend="sec-installation-troubleshooting-noboot"/>. 
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
   <figure>
    <title>Tela de boot em máquinas com BIOS tradicional</title>
    <mediaobject>
     <textobject role="description"><phrase>The Boot Screen on Machines with a Traditional BIOS</phrase>
     </textobject>
     <imageobject os="sles;sled">
      <imagedata fileref="install_boot.png" width="75%"/>
     </imageobject>
     
     
    </mediaobject>
   </figure>
   <para>
    Use as teclas de função indicadas na parte inferior da tela para mudar idioma, resolução da tela, fonte de instalação ou adicionar um driver extra do fornecedor de hardware:
   </para>
   <variablelist>
    <varlistentry>
     <term><keycap>F1</keycap><guimenu>Ajuda</guimenu></term>
     <listitem>
      <para>
       Obtenha ajuda sensível ao contexto referente ao elemento ativo da tela de boot. Use as teclas de seta para navegar, <keycap function="enter"/> para seguir um link, e <keycap function="escape"/> para sair da tela de ajuda.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><keycap>F2</keycap><guimenu>Idioma</guimenu></term>
     <listitem>
      <para>
       Selecione o idioma de exibição e um layout de teclado correspondente para a instalação. O idioma padrão é o inglês (EUA).
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><keycap>F3</keycap><guimenu>Modo de Vídeo</guimenu></term>
     <listitem>
      <para>
       Selecione vários modos de exibição gráficos para a instalação. Por <guimenu>Padrão</guimenu>, a resolução de vídeo é automaticamente determinada usando o KMS (<quote>Kernel Mode Setting</quote>). Se essa configuração não funcionar em seu sistema, escolha <guimenu>No KMS</guimenu> (Sem KMS) e, opcionalmente, especifique <literal>vga=ask</literal> na linha de comando de boot para receber um prompt da resolução de vídeo. Escolha <guimenu>Modo de Texto</guimenu> se a instalação gráfica causar problemas.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><keycap>F4</keycap><guimenu>Fonte</guimenu></term>
     <listitem>
      <para>
       Normalmente, a instalação é realizada pela mídia de instalação inserida. Aqui, selecione outras origens, como os servidores FTP ou NFS. Se a instalação for implantada em uma rede com servidor SLP, selecione a fonte de instalação disponível no servidor com essa opção. <phrase os="sles;sled">Há informações disponíveis sobre a configuração de um servidor de instalação com SLP no <xref linkend="cha-deployment-instserver"/>&#x002E;</phrase>
      </para>
     </listitem>
    </varlistentry>
    <varlistentry xml:id="vle-boot-parameters-screen-kernel">
     <term><keycap>F5</keycap><guimenu>Kernel</guimenu></term>
     <listitem>
      <para>
       Se encontrar problemas com a instalação regular, use esse menu para desabilitar algumas funções que podem ser problemáticas. Se seu hardware não oferecer suporte a ACPI (advanced configuration and power interface — interface de energia e configuração avançada), selecione <guimenu>Sem ACPI</guimenu> para instalar sem suporte a ACPI. A opção <guimenu>Sem APIC local</guimenu> desabilita o suporte a APIC (Advanced Programmable Interrupt Controllers — Controladores de Interrupção Programáveis Avançados) que poderá causar problemas com alguns itens de hardware. A opção <guimenu>Configurações Seguras</guimenu> inicializa o sistema com o modo DMA (para unidades de CD/DVD-ROM) e as funções de gerenciamento de energia desabilitadas.
      </para>
      <para>
       Se você não tiver certeza, tente primeiro as seguintes opções: <guimenu>Instalação—ACPI Desabilitada</guimenu> ou <guimenu>Instalação—Configurações Seguras</guimenu>. Os especialistas também podem usar a linha de comando (<guimenu>Opções de Boot</guimenu>) para digitar ou mudar os parâmetros de kernel.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><keycap>F6</keycap><guimenu>Driver</guimenu></term>
     <listitem>
      <para>
       Pressione esta tecla para notificar ao sistema de que há uma atualização de driver opcional para o <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>. Carregue drivers diretamente antes do início da instalação, usando <guimenu>Arquivo</guimenu> ou <guimenu>URL. </guimenu> Se selecionar <guimenu>Sim</guimenu>, você será solicitado a inserir o disco de atualização no ponto apropriado no processo de instalação.
      </para>
      <tip os="sles;sled">
       <title>Obtendo discos de atualização de driver</title>
       <para>
        As atualizações de driver para o SUSE Linux Enterprise estão disponíveis em <link xlink:href="http://drivers.suse.com/"/>. Esses drivers foram criados pelo SUSE SolidDriver Program.
       </para>
      </tip>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>

  <sect2 xml:id="sec-boot-parameters-uefi">
   <title>Tela de boot em máquinas equipadas com UEFI</title>
   <para>
    UEFI (Unified Extensible Firmware Interface) é um novo padrão da indústria que substitui e estende o BIOS tradicional. As mais recentes implementações da UEFI incluem a extensão de <quote>Boot Seguro</quote>, que impede a inicialização de código malicioso, permitindo apenas que carregadores de boot assinados sejam executados.<phrase os="sles;sled"> Consulte o <xref linkend="cha-uefi"/> para obter mais informações.</phrase>
   </para>
   <para>
    O gerenciador de boot GRUB 2, usado para inicializar máquinas com BIOS tradicional, não suporta UEFI; portanto, o GRUB 2 foi substituído pelo GRUB 2 para EFI. Se o Boot Seguro estiver habilitado, o YaST selecionará automaticamente o GRUB 2 para EFI para instalação. Da perspectiva do administrador e do usuário, as duas implementações do gerenciador de boot têm o mesmo comportamento e são chamadas de <literal>GRUB 2</literal> a seguir.
   </para>
   <tip>
    <title>Usando drivers adicionais com boot seguro</title>
    <para>
     Durante a instalação com o Boot Seguro habilitado, você não pode carregar drivers que não estão incluídos no <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>. Isso também se aplica aos drivers enviados via SolidDriver, porque a chave de assinatura deles por padrão não é confiável.
    </para>
    <para>
     Para carregar drivers que não acompanham o <phrase role="productname"><phrase os="sles"> SUSE Linux Enterprise Server</phrase></phrase>, execute um dos seguintes procedimentos:
    </para>
    <itemizedlist>
     <listitem>
      <para>
       Antes da instalação, adicione as chaves necessárias ao banco de dados do firmware usando as ferramentas de gerenciamento de firmware/sistema.
      </para>
     </listitem>
     <listitem>
      <para>
       Use uma ISO inicializável que registrará as chaves necessárias na lista MOK no primeiro boot.
      </para>
     </listitem>
    </itemizedlist>
    <para os="sles;sled">
     Para obter mais informações, consulte o <xref linkend="sec-uefi-secboot"/>. 
    </para>
   </tip>
   <para>
    A tela de boot exibe várias opções para o procedimento de instalação. Mude a opção selecionada com as teclas de setas e pressione <keycap function="enter"/> para inicializar. As opções relevantes são:
   </para>
   <variablelist>
    <varlistentry>
     <term><guimenu>Instalação</guimenu></term>
     <listitem>
      <para>
       O modo de instalação normal. Todas as funções de hardware modernas estão habilitadas. Em caso de falha na instalação, consulte <xref linkend="vle-boot-parameters-screen-kernel"/> para obter os parâmetros de boot que desabilitam funções possivelmente problemáticas.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry os="sles;sled;osuse">
     <term><guimenu>Upgrade</guimenu></term>
     <listitem>
      <para>
       Faça upgrade do sistema. <phrase os="sles;sled">Para obter mais informações, consulte o <xref os="sles;sled" linkend="cha-upgrade-paths"/>&#x002E;</phrase>
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><menuchoice><guimenu>Mais</guimenu> <guimenu> Sistema de Recuperação</guimenu></menuchoice></term>
     <listitem>
      <para>
       Inicia um sistema Linux mínimo sem interface gráfica do usuário. Para obter mais informações, consulte
       
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><menuchoice><guimenu>Mais</guimenu> <guimenu> Inicializar Sistema Linux</guimenu></menuchoice></term>
     <listitem>
      <para>
       Inicialize um sistema Linux que já esteja instalado. Você deverá informar a partição da qual inicializar o sistema.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><menuchoice><guimenu>Mais</guimenu> <guimenu> Verificar Mídia de Instalação</guimenu></menuchoice></term>
     <listitem>
      <para>
       Essa opção só está disponível quando você instala de mídias criadas das ISOs descarregadas. Nesse caso, é recomendável verificar a integridade da mídia de instalação. Essa opção inicia o sistema de instalação antes de verificar a mídia automaticamente. No caso da verificação bem-sucedida, é iniciada a rotina de instalação normal. Se for detectada uma mídia corrompida, a rotina de instalação será interrompida.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
   <figure>
    <title>Tela de boot em máquinas com UEFI</title>
    <mediaobject>
     <textobject role="description"><phrase>The Boot Screen on Machines with UEFI</phrase>
     </textobject>
     <imageobject os="sles;sled">
      <imagedata fileref="install_boot_uefi.png" width="75%"/>
     </imageobject>
     
    </mediaobject>
   </figure>
   <para>
    O GRUB 2 para EFI no <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> não suporta prompt de boot nem teclas de função para adicionar parâmetros de boot. Por padrão, a instalação será iniciada com o inglês americano e a mídia de boot como a fonte de instalação. Uma pesquisa DHCP é executada para configurar a rede. Para mudar esses padrões ou adicionar parâmetros de boot, você precisa editar a respectiva entrada de boot. Realce-a usando as teclas de setas e pressione <keycap>E</keycap>. Consulte a ajuda na tela para obter dicas sobre edição (observe que apenas o teclado em inglês está disponível no momento). A entrada de <guimenu>Instalação</guimenu> será parecida com a seguinte:
   </para>
<screen>setparams 'Installation'

   set gfxpayload=keep
   echo 'Loading kernel ...'
   linuxefi /boot/x86_64/loader/linux splash=silent
   echo 'Loading initial ramdisk ...'
   initrdefi /boot/x86_64/loader/initrd</screen>
   <para>
    Adicione parâmetros separados por espaço ao fim da linha que começa com <literal>linuxefi</literal>. Para inicializar a entrada editada, pressione <keycap>F10</keycap>. Se você acessar a máquina por um console serial, pressione <keycombo><keycap function="escape"/> <keycap>0</keycap></keycombo> . Há uma lista completa de parâmetros disponível em <link xlink:href="https://en.opensuse.org/Linuxrc"/>. 
   </para>
  </sect2>
 </sect1>
 <sect1 xml:id="sec-boot-parameters-list">
  <title>Lista de parâmetros de boot importantes</title>

  <para>
   Esta seção contém uma seleção de parâmetros de boot importantes.
  </para>

  <sect2 xml:id="sec-boot-parameters-list-general">
   <title>Parâmetros de boot gerais</title>
   <para/>
   <variablelist>
    <varlistentry>
     <term><option>autoyast=</option><replaceable>URL</replaceable></term>
     <listitem>
      <para>
       O parâmetro <envar>autoyast</envar> especifica o local do arquivo de controle <filename>autoinst.xml</filename> para instalação automática.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>manual=&lt;0|1&gt;</option></term>
     <listitem>
      <para>
       O parâmetro <envar>manual</envar> controla se os outros parâmetros são apenas valores padrão que ainda devem ser confirmados pelo usuário. Defina esse parâmetro como <literal>0</literal> caso todos os valores devam ser aceitos e sem perguntas. A definição do <envar>autoyast</envar> implica a definição de <envar>manual</envar> como <literal>0</literal>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>Info=</option><replaceable>URL</replaceable></term>
     <listitem>
      <para>
       Especifica o local de um arquivo de onde ler as opções adicionais.
      </para>
      <para arch="zseries" os="sles">
       Ajuda a superar as limitações de 10 linhas (e 80 caracteres por linha do z/VM) para o arquivo de parâmetros. <phrase os="sles;sled">Há mais documentação sobre o arquivo Info disponível em <xref linkend="invoking-autoinst-linuxrc"/>&#x002E;</phrase> Como o arquivo Info geralmente só pode ser acessado por meio da rede no IBM Z, você não pode usá-lo para especificar as opções necessárias para configurar a rede (essas opções estão descritas na <xref linkend="sec-boot-parameters-list-network"/>). Além disso, outras opções específicas do linuxrc, como para depuração, devem ser especificadas no parmfile para serem efetivadas.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>upgrade=&lt;0|1&gt;</option></term>
     <listitem>
      <para>
       Para fazer upgrade do <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>, especifique <option>Upgrade=1</option>.
      </para>
      <para os="sles" arch="zseries">
       Um parmfile personalizado é necessário para fazer upgrade de uma instalação existente do SUSE Linux Enterprise. Sem esse parâmetro, a instalação não oferece nenhuma opção de upgrade.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>dud=</option><replaceable>URL</replaceable></term>
     <listitem>
      <para>
       Carregue as atualizações de driver do <replaceable>URL</replaceable>.
      </para>
      <para>
       Defina <literal>dud=ftp://ftp.example.com/CAMINHO_PARA_DRIVER</literal> ou <literal>dud=http://www.example.com/CAMINHO_PARA_DRIVER</literal> como os drivers de carregamento de um URL. Quando <literal>dud=1</literal>, será solicitado que você insira o URL durante o boot.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>language=</option><replaceable>IDIOMA</replaceable></term>
     <listitem>
      <para>
       Defina o idioma da instalação. Alguns dos valores suportados são <literal>cs_CZ</literal>, <literal>de_DE</literal>, <literal>es_ES</literal>, <literal>fr_FR</literal>, <literal>ja_JP</literal>, <literal>pt_BR</literal>, <literal>pt_PT</literal>, <literal>ru_RU</literal>, <literal>zh_CN</literal> e <literal>zh_TW</literal>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>acpi=off</option></term>
     <listitem>
      <para>
       Desabilite o suporte à ACPI.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>noapic</option></term>
     <listitem>
      <para>
       Sem APIC lógico.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>nomodeset</option></term>
     <listitem>
      <para>
       Desabilite o KMS.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>textmode=1</option></term>
     <listitem>
      <para>
       Inicie o instalador no modo de texto.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>console=</option><replaceable>DISPOSITIVO_SERIAL</replaceable>[,<replaceable>MODO</replaceable>]</term>
     <listitem>
      <para>
       <replaceable>DISPOSITIVO_SERIAL</replaceable> pode ser um dispositivo serial ou paralelo real (por exemplo, <literal>ttyS0</literal>) ou um terminal virtual (por exemplo, <literal>tty1</literal>). <replaceable>MODO</replaceable> é a taxa de transmissão, a paridade e o bit de fim (por exemplo, <literal>9600n8</literal>). O padrão para essa configuração é definir pelo firmware da placa-mãe. Se a saída não aparecer no seu monitor, tente definir <literal>console= tty1</literal>. É possível definir vários dispositivos.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>

  <sect2 xml:id="sec-boot-parameters-list-network">
   <title>Configurando a interface de rede</title>
   <important>
    <title>Configurando a interface de rede</title>
    <para>
     As configurações abordadas nesta seção aplicam-se à interface de rede usada durante a instalação.<phrase os="sles"> Configure interfaces de rede adicionais no sistema instalado seguindo as instruções descritas no <xref linkend="sec-network-manconf"/>.</phrase>
    </para>
   </important>
   <para>
    A rede será configurada apenas se for necessária durante a instalação. Para forçar a configuração da rede, use os parâmetros <literal>netsetup</literal> ou <literal>ifcfg</literal>.
   </para>
   <variablelist>
    <varlistentry>
     <term><option>netsetup=<replaceable>VALOR</replaceable></option></term>
     <listitem>
      <para>
       <literal>netsetup=dhcp</literal> força a configuração por meio do DHCP. Defina <literal>netsetup=-dhcp</literal> ao configurar a rede com os parâmetros de boot <literal>hostip</literal>, <literal>gateway</literal> e <literal>nameserver</literal>. Com a opção <literal>netsetup=hostip,netmask,gateway,nameserver</literal>, o instalador solicita as configurações de rede durante o boot.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option> ifcfg=<replaceable>INTERFACE</replaceable>[.<replaceable>VLAN</replaceable>]=[.try,]<replaceable>SETTINGS</replaceable> </option></term>
     <listitem>
      <para>
       <replaceable>INTERFACE</replaceable> pode ser <literal>*</literal> para corresponder a todas as interfaces ou, por exemplo, <literal>eth*</literal> para corresponder a todas as interfaces que começam com <literal>eth</literal>. Também é possível usar os endereços MAC como valores.
      </para>
      <para>
       É possível definir uma VLAN com o nome da interface, separado por um ponto.
      </para>
      <para>
       Se <replaceable>CONFIGURAÇÕES</replaceable> for <literal>dhcp</literal>, todas as interfaces correspondentes serão configuradas com DHCP. Se você adicionar a opção <literal>try</literal>, a configuração será interrompida quando o repositório de instalação puder ser acessado por meio de uma das interfaces configuradas.
      </para>
      <para>
       Se preferir, use a configuração estática. Com os parâmetros estáticos, apenas a primeira interface correspondente será configurada, a menos que você adicione a opção <literal>try</literal>. Isso configurará todas as interfaces até que o repositório possa ser acessado.
      </para>
      <para>
       A sintaxe para a configuração estática é:
      </para>
<screen>ifcfg=*="<replaceable>IPS_NETMASK</replaceable>,<replaceable>GATEWAYS</replaceable>,<replaceable>NAMESERVERS</replaceable>,<replaceable>DOMAINS</replaceable>"</screen>
      <para>
       Cada valor separado por vírgula pode, por sua vez, conter uma lista de valores separados por caractere de espaço. <replaceable>IPS_NETMASK</replaceable> é a <emphasis>notação CIDR</emphasis>. Por exemplo, <literal>10.0.0.1/24</literal>. As aspas são necessárias apenas ao usar listas separadas por caractere de espaço. Exemplo com dois servidores de nomes:
      </para>
<screen>ifcfg=*="<replaceable>10.0.0.10/24</replaceable>,<replaceable>10.0.0.1</replaceable>,<replaceable>10.0.0.1 10.0.0.2</replaceable>,<replaceable>example.com</replaceable>"</screen>
      <tip>
       <title>Outros parâmetros de projeto de rede</title>
       <para>
        O parâmetro de boot <literal>ifcfg</literal> é muito avançado e permite definir quase todos os parâmetros de rede. Além dos parâmetros mencionados anteriormente, você pode definir valores para todas as opções de configuração (separadas por vírgula) de <filename>/etc/sysconfig/network/ifcfg.template</filename> e <filename>/etc/sysconfig/network/config</filename>. O exemplo a seguir define um tamanho de MTU personalizado em uma interface diferente da que foi configurada por DHCP:
       </para>
<screen>ifcfg=eth0=dhcp,MTU=1500</screen>
      </tip>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>hostname=host.example.com</option></term>
     <listitem>
      <para>
       Digite o nome completo do host.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>domain=example.com</option></term>
     <listitem>
      <para>
       Caminho de pesquisa de domínio para o DNS. Permite que você use nomes curtos de host em vez de nomes completos de host.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>hostip=192.168.1.2[/24]</option></term>
     <listitem>
      <para>
       Digite o endereço IP da interface a ser configurada. O IP pode conter a máscara de sub-rede. Por exemplo, <literal>hostip=192.168.1.2/24</literal>. Essa configuração será avaliada apenas se a rede for necessária durante a instalação.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>gateway=192.168.1.3</option></term>
     <listitem>
      <para>
       Especifique o gateway a ser usado. Essa configuração será avaliada apenas se a rede for necessária durante a instalação.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>nameserver=192.168.1.4</option></term>
     <listitem>
      <para>
       Especifique o servidor DNS ativo. Essa configuração será avaliada apenas se a rede for necessária durante a instalação.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>domain=example.com</option></term>
     <listitem>
      <para>
       Caminho de pesquisa de domínio. Essa configuração será avaliada apenas se a rede for necessária durante a instalação.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>

  <sect2 xml:id="sec-boot-parameters-list-install-source">
   <title>Especificando a fonte de instalação</title>
   <para>
    Se você não usa um DVD ou uma unidade flash USB para instalação, especifique uma fonte de instalação alternativa.
   </para>
   <variablelist>
    <varlistentry>
     <term><option>install=SOURCE</option></term>
     <listitem>
      <para>
       Especifique o local da fonte de instalação a ser usada. Os protocolos possíveis <literal>cd</literal>, <literal>hd</literal>, <literal>slp</literal>, <literal>nfs</literal>, <literal>smb</literal> (Samba/CIFS), <literal>ftp</literal>, <literal>tftp</literal>
       <literal> http</literal> e <literal>https</literal>. Nem todos os tipos de fonte estão disponíveis em todas as plataformas.<phrase os="sles"> Por exemplo, IBM Z não suporta <literal>cd</literal> e <literal>hd</literal>&#x002E;</phrase>
      </para>
      <para>
       A opção padrão é <literal>cd</literal>.
      </para>
      <para>
       Se um URL de <literal>ftp</literal>, <literal>tftp</literal> ou <literal>smb</literal> for informado, especifique o nome de usuário e a senha com o URL. Esses parâmetros são opcionais, e um login anônimo ou convidado será considerado se eles não forem fornecidos. Exemplo:
      </para>
<screen>install=ftp://<replaceable>USER</replaceable>:<replaceable>PASSWORD</replaceable>@<replaceable>SERVER</replaceable>/<replaceable>DIRECTORY</replaceable>/DVD1/</screen>
      <para>
       Para instalação em uma conexão criptografada, use um URL <literal>https</literal>. Se o certificado não puder ser verificado, use o parâmetro de boot <literal>sslcerts=0</literal> para desabilitar a verificação de certificado.
      </para>
      <para>
       No caso de uma instalação do Samba ou do CIFS, também é possível especificar o domínio a ser usado:
      </para>
<screen>install=smb://<replaceable>WORKDOMAIN</replaceable>;<replaceable>USER</replaceable>:<replaceable>PASSWORD</replaceable>@<replaceable>SERVER</replaceable>/<replaceable>DIRECTORY</replaceable>/DVD1/</screen>
      <para>
       Para usar <literal>cd</literal>, <literal>hd</literal> ou <literal>slp</literal>, defina-os como no exemplo a seguir:
      </para>
<screen>install=cd:/
install=hd:/?device=<replaceable>sda/PATH_TO_ISO</replaceable>
install=slp:/</screen>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>

  <sect2 xml:id="sec-boot-parameters-list-remote-access">
   <title>Especificando o acesso remoto</title>
   <para>
    Apenas um dos diferentes métodos de controle remoto deve ser especificado de cada vez. Os diferentes métodos são: SSH, VNC, servidor X remoto. <phrase os="sles;sled;slemicro">Para obter informações sobre como usar os parâmetros listados nesta seção, consulte o <xref linkend="cha-remote-installation"/>&#x002E;</phrase>
   </para>
   <variablelist>
    <varlistentry>
     <term><option>display_ip=</option><replaceable>ENDEREÇO_IP</replaceable></term>
     <listitem>
      <para>
       <envar>Display_IP</envar> faz com que o sistema de instalação tente se conectar a um servidor X usando um determinado endereço.
      </para>
      <important>
       <title>Mecanismo de autenticação X</title>
       <para>
        A instalação direta com o Sistema X Window utiliza um mecanismo de autenticação primitivo baseado em nomes de host. Este mecanismo está desabilitado nas versões atuais do <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server. </phrase></phrase> É preferível a instalação com SSH ou VNC.
       </para>
      </important>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>vnc=1</option></term>
     <listitem>
      <para>
       Habilita um servidor VNC durante a instalação.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>vncpassword=</option><replaceable>SENHA</replaceable></term>
     <listitem>
      <para>
       Define a senha para o servidor VNC.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>ssh=1</option></term>
     <listitem>
      <para>
       <envar>ssh</envar> habilita a instalação com SSH.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>ssh.password=</option><replaceable>SENHA</replaceable></term>
     <listitem>
      <para>
       Especifica uma senha SSH para o usuário root durante a instalação.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>
 </sect1>
 <sect1 xml:id="sec-boot-parameters-advanced">
  <title>Configurações avançadas</title>

  <para>
   Para configurar o acesso a uma RMT local ou servidor <command>supportconfig</command> para instalação, você pode especificar parâmetros de boot para configurar esses serviços durante a instalação. Faça o mesmo se você precisar de suporte a IPv6 durante a instalação.
  </para>

  <sect2 xml:id="sec-boot-parameters-advanced-smt" os="sles;sled">
   <title>Fornecendo dados para acessar um servidor RMT</title>
   <para>
    Por padrão, as atualizações para o <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> são fornecidas pelo SUSE Customer Center. Se a sua rede disponibilizar um servidor RMT para fornecer uma fonte de atualização local, você precisará enviar ao cliente o URL do servidor. O cliente e o servidor se comunicarão somente via protocolo HTTPS, portanto, você também precisará digitar um caminho para o certificado do servidor se o certificado não tiver sido emitido por uma autoridade de certificação. 
   </para>
   <note>
    <title>Apenas instalação não interativa</title>
    <para>
     Só é necessário especificar parâmetros para acessar um servidor RMT em instalações não interativas. Na instalação interativa, é possível inserir os dados durante a instalação (consulte a <xref linkend="sec-yast-install-scc-registration"/> para obter detalhes).
    </para>
   </note>
   <variablelist>
    <varlistentry>
     <term><option>regurl</option></term>
     <listitem>
      <para>
       URL do servidor RMT. Esse URL tem o formato fixo <literal>https://<replaceable>FQN</replaceable>/center/regsvc/</literal>. <replaceable>FQN</replaceable> deve ser o nome completo do host do servidor RMT. Exemplo:
      </para>
<screen>regurl=https://smt.example.com/center/regsvc/</screen>
      <para>
       Verifique se os valores digitados estão corretos. Se <literal>regurl</literal> não tiver sido especificado corretamente, o registro da fonte de atualização falhará.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>regcert</option></term>
     <listitem>
      <para>
       Local do certificado do servidor RMT. Especifique um dos seguintes locais:
      </para>
      <variablelist>
       <varlistentry>
        <term>URL</term>
        <listitem>
         <para>
          Local remoto (HTTP, HTTPS ou FTP) do qual é possível fazer download do certificado. Se o regcert não for especificado, será assumido o padrão <literal>http://<replaceable>FQN</replaceable>/smt.crt</literal> com <literal>FQN</literal> como nome do servidor RMT. Exemplo:
         </para>
<screen>regcert=http://rmt.example.com/smt-ca.crt</screen>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term>Caminho local</term>
        <listitem>
         <para>
          Caminho absoluto do certificado na máquina local. Exemplo:
         </para>
<screen>regcert=/data/inst/smt/smt-ca.cert</screen>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term>Interativo</term>
        <listitem>
         <para>
          Use <literal>ask</literal> para abrir um menu popup durante a instalação que permite especificar o caminho do certificado. Não use esta opção com AutoYaST. Exemplo
         </para>
<screen>regcert=ask</screen>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term>Desativar a instalação do certificado</term>
        <listitem>
         <para>
          Use <literal>done</literal> se o certificado for instalado por um produto complementar, ou se você usar um certificado emitido por uma autoridade de certificação oficial. Por exemplo:
         </para>
<screen>regcert=done</screen>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>

  <sect2 xml:id="sec-boot-parameters-advanced-supportconfigurl" os="sles;sled">
   <title>Configurando um servidor de dados alternativo para <command>supportconfig</command></title>
   <para>
    Os dados coletados pelo supportconfig (consulte o <xref linkend="cha-adm-support"/> para obter mais informações) são enviados ao SUSE Customer Center por padrão. Também é possível configurar um servidor local para coletar esses dados. Se esse servidor estiver disponível na rede, será preciso definir o URL do servidor no cliente. Essa informação deve ser digitada no prompt de boot.
   </para>
   <formalpara>
    <title><literal>supporturl</literal></title>
    <para>
     URL do servidor. O URL tem o formato <literal>http://<replaceable>FQN</replaceable>/<replaceable>Caminho</replaceable>/</literal>, em que <replaceable>FQN</replaceable> é o nome completo do host do servidor e <replaceable>Caminho</replaceable> é o local no servidor. Por exemplo:
    </para>
   </formalpara>
<screen>supporturl=http://support.example.com/supportconfig/data/</screen>
  </sect2>

  <sect2 xml:id="sec-boot-parameters-advanced-ipv6">
   <title>Usando IPv6 para instalação</title>
   <para>
    Por padrão, só é possível atribuir endereços de rede IPv4 à sua máquina. Para habilitar o IPv6 durante a instalação, digite um dos seguintes parâmetros no prompt de boot:
   </para>
   <variablelist>
    <varlistentry>
     <term>Aceitar IPv4 e IPv6</term>
     <listitem>
<screen>ipv6=1</screen>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Aceitar apenas IPv6</term>
     <listitem>
<screen>ipv6only=1</screen>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>

  <sect2 xml:id="sec-boot-parameters-advanced-proxy">
   <title>Usando proxy para instalação</title>
   <para>
    Em redes que impõem o uso de um servidor proxy para acesso a sites na Web remotos, o registro durante a instalação é possível apenas ao configurar um servidor proxy.
   </para>
   <para>
    Para usar um proxy durante a instalação, pressione <keycap>F4</keycap> na tela de boot e defina os parâmetros necessários na caixa de diálogo <guimenu>Proxy HTTP.</guimenu>
   </para>
   <para>
    Se preferir, insira o parâmetro de kernel <literal>proxy</literal> no prompt de boot:
   </para>
   <procedure>
    <step>
     <para>
      Na tela de instalação, pressione <keycap>E</keycap> para abrir o editor de parâmetros de boot.
     </para>
    </step>
    <step>
     <para>
      Adicione uma nova linha com a seguinte sintaxe:
     </para>
<screen>proxy=http://<replaceable>USER</replaceable>:<replaceable>PASSWORD</replaceable>@proxy.example.com:<replaceable>PORT</replaceable></screen>
     <para>
      Especificar <replaceable>USER</replaceable> e <replaceable>PASSWORD</replaceable> é opcional. Se o servidor permitir acesso anônimo, os seguintes dados serão suficientes:
     </para>
<screen>http://proxy.example.com:<replaceable>PORT</replaceable></screen>
     <para>
      O resultado final terá aparência similar à seguinte:
     </para>
     <figure>
      <title>Editor de opções do GRUB</title>
      <mediaobject>
       <imageobject role="fo">
        <imagedata fileref="grub_cmdline_proxy.png" width="40%"/>
       </imageobject>
       <imageobject role="html">
        <imagedata fileref="grub_cmdline_proxy.png" width="40%"/>
       </imageobject>
      </mediaobject>
     </figure>
    </step>
    <step>
     <para>
      Pressione <keycap>F10</keycap> para inicializar com a nova configuração de proxy.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 xml:id="sec-boot-parameters-advanced-selinux">
   <title>Habilitando o suporte a SELinux</title>
   <para>
    A habilitação do SELinux na inicialização da instalação permitirá configurá-lo após o término da instalação, sem ter que reinicializar. Use os seguintes parâmetros:
   </para>
<screen>security=selinux selinux=1</screen>
  </sect2>

  <sect2 os="sles;sled;osuse" xml:id="sec-boot-parameters-advanced-self-update">
   <title>Habilitando a atualização automática do instalador</title>
   <para>
    Durante a instalação e o upgrade, o YaST pode se atualizar, conforme descrito na <xref linkend="sec-yast-install-self-update"/>, para resolver possíveis bugs descobertos após o lançamento. O parâmetro <literal>self_update</literal> pode ser usado para modificar o comportamento desse recurso.
   </para>
   <para>
    Para habilitar a autoatualização do instalador, defina o parâmetro como <literal>1</literal>:
   </para>
<screen>self_update=1</screen>
   <para>
    Para usar um repositório definido pelo usuário, especifique um URL:
   </para>
<screen>self_update=https://updates.example.com/</screen>
  </sect2>

  <sect2 xml:id="sec-boot-parameters-advanced-high-dpi">
   <title>Dimensionar interface do usuário para alto DPI</title>
   <para>
    Se a sua tela usa um DPI muito alto, aplique o parâmetro de boot <literal>QT_AUTO_SCREEN_SCALE_FACTOR</literal>. Esse procedimento dimensiona os elementos de fonte e da interface do usuário de acordo com o DPI da tela.
   </para>
<screen>QT_AUTO_SCREEN_SCALE_FACTOR=1</screen>
  </sect2>

  <sect2 xml:id="sec-boot-parameters-advanced-cpu-mitigations">
   <title>Usando mitigações de CPU</title>
   <para>
    O parâmetro de boot <literal>mitigations</literal> permite controlar as opções de mitigação para ataques de temporização nas CPUs afetadas. Os valores possíveis desse parâmetro são:
   </para>
   <formalpara>
    <title><literal>auto</literal></title> <para>
   Habilita todas as mitigações necessárias para o modelo de CPU, mas não protege contra ataques de thread entre CPUs. Essa configuração pode afetar o desempenho a um certo nível, dependendo da carga de trabalho.
  </para>
   </formalpara>
   <formalpara>
    <title><literal>nosmt</literal></title> <para>
    Fornece o conjunto completo de mitigações de segurança disponíveis. Habilita todas as mitigações necessárias para o modelo de CPU. Além disso, ela desabilita o Multithreading Simultâneo (SMT, Simultaneous Multithreading) para evitar ataques de temporização em vários threads de CPU. Essa configuração pode prejudicar o desempenho, dependendo da carga de trabalho.
    </para>
   </formalpara>
   <formalpara>
    <title><literal>off</literal></title> <para>
    Desabilita todas as mitigações. Os ataques de temporização contra a CPU são possíveis, dependendo do modelo dela. Essa configuração não afeta o desempenho.
   </para>
   </formalpara>
   <para>
    Cada valor inclui um conjunto de parâmetros específicos, dependendo da arquitetura da CPU, da versão do kernel e das vulnerabilidades que precisam ser mitigadas. Consulte a documentação do kernel para obter detalhes.
   </para>
  </sect2>
 </sect1>
 <sect1 xml:id="sec-boot-parameters-zseries" os="sles">
  <title>IBM Z</title>

  <para>
   No caso das plataformas IBM Z, o sistema será inicializado (IPL, Initial Program Load — Carga Inicial de Programa) conforme descrito na <xref linkend="sec-prep-ipling"/>. O <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> não mostra uma splash screen nesses sistemas. Durante a instalação, carregue o kernel, o initrd e o parmfile manualmente. O YaST é iniciado com a tela de instalação quando uma conexão é estabelecida com o sistema de instalação via VNC, X ou SSH. Por não haver splash screen, os parâmetros de kernel ou de boot não poderão ser digitados na tela, mas deverão ser especificados em um parmfile (consulte a <xref linkend="sec-appdendix-parm"/>).
  </para>

  <variablelist>
   <varlistentry>
    <term><option>InstNetDev=osa</option></term>
    <listitem>
     <para>
      Digite o tipo de interface a ser configurado. Os valores possíveis são <literal>osa</literal>, <literal>hsi</literal>, <literal>ctc</literal>, <literal>escon</literal> e <literal>iucv</literal> (CTC, ESCON e IUCV não são mais oficialmente suportadas).
     </para>
     <para>
      Para as interfaces do tipo <literal>hsi</literal> e <literal>osa</literal>, especifique uma máscara de rede adequada e um endereço de broadcast adicional:
     </para>
<screen>Netmask=255.255.255.0
Broadcast=192.168.255.255</screen>
     <para>
      Para as interfaces do tipo <literal>ctc</literal>, <literal>escon</literal> e <literal>iucv</literal> (CTC, ESCON e IUCV não são mais oficialmente suportadas), digite o endereço IP do peer:
     </para>
<screen>Pointopoint=192.168.55.20</screen>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><option>OsaInterface=&lt;lcs|qdio&gt;</option></term>
    <listitem>
     <para>
      Para os dispositivos de rede <literal>osa</literal>, especifique a interface do host (<literal>qdio</literal> ou <literal>lcs</literal>).
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><option>Layer2=&lt;0|1&gt;</option></term>
    <listitem>
     <para>
      Para os dispositivos Ethernet QDIO <literal>osa</literal> e <literal>hsi</literal>, especifique se é para habilitar (<literal>1</literal>) ou desabilitar (<literal>0</literal>) o suporte da Camada 2 de OSI.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><option>OSAHWAddr=02:00:65:00:01:09</option></term>
    <listitem>
     <para>
      Para dispositivos Ethernet QDIO <literal>osa</literal> habilitados para Layer 2, especifique um endereço MAC manualmente ou indique <literal>OSAHWADDR= </literal> (com espaço em branco à direita) para o sistema padrão.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><option>PortNo=&lt;0|1&gt;</option></term>
    <listitem>
     <para>
      Para dispositivos de rede <literal>osa</literal>, especifique o número de porta (desde que o dispositivo suporte esse recurso). O valor padrão é 0.
     </para>
    </listitem>
   </varlistentry>
  </variablelist>

  <para>
   Cada uma das interfaces requer certas opções de configuração:
  </para>

  <itemizedlist>
   <listitem>
    <para>
     As interfaces <literal>ctc</literal> e <literal>escon</literal> (CTC e ESCON não são mais oficialmente suportadas):
    </para>
<screen>ReadChannel=0.0.0600
WriteChannel=0.0.0601</screen>
    <para>
     <envar>ReadChannel</envar> especifica o canal READ a ser usado. <envar>WriteChannel</envar> especifica o canal WRITE.
    </para>
   </listitem>
   <listitem>
    <para>
     Para a interface <literal>ctc</literal> (não mais oficialmente suportada), especifique o protocolo a ser usado:
    </para>
<screen>CTCProtocol=&lt;0/1/2&gt;</screen>
    <para>
     Estas são as entradas válidas:
    </para>
    <informaltable>
     <tgroup cols="2">
      <tbody>
       <row>
        <entry>
         <para>
          <literal>0</literal>
         </para>
        </entry>
        <entry>
         <para>
          Modo de compatibilidade, também para peers não Linux diferentes de OS/390 e z/OS (este é o modo padrão)
         </para>
        </entry>
       </row>
       <row>
        <entry>
         <para>
          <literal>1</literal>
         </para>
        </entry>
        <entry>
         <para>
          Modo estendido
         </para>
        </entry>
       </row>
       <row>
        <entry>
         <para>
          <literal>2</literal>
         </para>
        </entry>
        <entry>
         <para>
          O modo de compatibilidade com OS/390 e z/OS
         </para>
        </entry>
       </row>
      </tbody>
     </tgroup>
    </informaltable>
   </listitem>
   <listitem>
    <para>
     O tipo de dispositivo de rede <literal>osa</literal> com a interface <literal>lcs</literal>:
    </para>
<screen>ReadChannel=0.0.0124</screen>
    <para>
     <envar>ReadChannel</envar> representa o número de canal usado nesta configuração. Um segundo número de porta poderá ser derivado desse número se você adicionar um a <envar>ReadChannel</envar>. <envar>Portnumber</envar> é usado para especificar a porta relativa.
    </para>
   </listitem>
   <listitem>
    <para>
     Interface <literal>iucv</literal>:
    </para>
<screen>IUCVPeer=PEER</screen>
    <para>
     Digite o nome da máquina peer.
    </para>
   </listitem>
   <listitem>
    <para>
     O tipo de dispositivo de rede <literal>osa</literal> com a interface <literal>qdio</literal> para OSA-Express Gigabit Ethernet:
    </para>
<screen>ReadChannel=0.0.0700
WriteChannel=0.0.0701
DataChannel=0.0.0702</screen>
    <para>
     Para <envar>ReadChannel</envar>, digite o número do canal READ. Para <envar>WriteChannel</envar>, digite o número do canal WRITE. <envar>DataChannel</envar> especifica o canal DATA. Verifique se o canal READ tem um número de dispositivo par.
    </para>
   </listitem>
   <listitem>
    <para>
     Interface <literal>hsi</literal> para LANs HiperSockets e VM convidadas:
    </para>
<screen>ReadChannel=0.0.0800
WriteChannel=0.0.0801
DataChannel=0.0.0802</screen>
    <para>
     Para <envar>ReadChannel</envar>, digite o número adequado para o canal READ. Para <envar>WriteChannel</envar> e <envar>DataChannel</envar>, digite os números de canal WRITE e DATA.
    </para>
   </listitem>
  </itemizedlist>
 </sect1>
 <sect1 xml:id="sec-boot-parameters-more-information">
  <title>Mais informações</title>

  <para>
   Você pode encontrar mais informações sobre os parâmetros de boot no wiki do openSUSE em <link xlink:href="https://en.opensuse.org/SDB:Linuxrc#Parameter_Reference"/>.
  </para>
 </sect1>
</chapter>
