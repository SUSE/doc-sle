<?xml version="1.0" encoding="UTF-8"?>
<sect1 xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="deployment_prep_x86_media.xml" version="5.0" xml:id="sec-x86-media">
 <title>Booting the Installation System</title>

 <info>
      <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
        <dm:bugtracker/>
        <dm:translation>no</dm:translation>
      </dm:docmanager>
    </info>
    

 <para>
  This section gives an overview of the steps required for the
  complete installation of <phrase role="productname"><phrase os="sled">SUSE® Linux Enterprise Desktop</phrase></phrase>.
 </para>
 <para>
  Unlike previous SLE products, the entire SLE <phrase role="productnumber"><phrase os="sles;sled">15 SP2</phrase></phrase> product
  line can be installed using the Unified Installer. 
 </para>
 <para>
  For a full description of how to install and configure the system with
  YaST, refer to <xref linkend="part-deployment"/>.
 </para>

 

 <procedure>
  <step>
   <para>
    Prepare the installation media.
   </para>
   <variablelist>
    <varlistentry>
     <term>USB Flash Drive</term>
     <listitem>
      <para>
       This is the simplest way to start the installation. To create a bootable
       flash disk, you need to copy a DVD image to the device using the <command>dd</command>
       command. The flash disk must not be mounted, and all data on the device
       will be erased.
      </para>
      <screen><prompt role="root">root # </prompt><command>dd</command> if=<replaceable>PATH_TO_ISO_IMAGE</replaceable> of=<replaceable>USB_STORAGE_DEVICE</replaceable> bs=4M</screen>
     </listitem>
    </varlistentry>
    
    <varlistentry>
     <term>Network Booting</term>
     <listitem>
      <para>
       If the target computer's firmware supports it, you can boot the computer
       from the network and install from a server. This booting method
       requires a boot server that provides the needed boot images over the
       network. The exact protocol depends on your hardware. Commonly you need
       several services, such as TFTP and DHCP or PXE boot. For details, read
       <xref linkend="cha-deployment-prep-pxe"/>.
      </para>
      <para>It is possible to install from many common network protocols, such
       as NFS, HTTP, FTP, or SMB. For more information on how to perform such an
       installation, refer to <xref linkend="cha-remote-installation"/>.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </step>
  <step>
   <para>
    Configure the target system firmware to boot the medium you chose. Refer to the
    documentation of your hardware vendor about how to configure the correct
    boot order.
   </para>
  </step>
  <step>
   <para>
    Set the boot parameters required for your installation method. An overview of
    the different methods is provided in <xref linkend="sec-x86-prep-installation-methods"/>.
    A list of boot parameters is available in
    <xref linkend="cha-boot-parameters"/>.
   </para>
  </step>
  <step>
   <para>
    Perform the installation as described in <xref linkend="cha-install"/>.
    The system needs to restart after the installation is finished.
   </para>
  </step>
  <step>
   <para>
    Optional: Change the boot order of the system to directly boot from the
    medium to which <phrase role="productname"><phrase os="sled">SUSE Linux Enterprise Desktop</phrase></phrase> has been installed. If the system boots
    from the installation medium, the first boot parameter will be to boot the
    installed system.
   </para>
  </step>
  <step>
   <para>
    Perform the initial system configuration as described in
    <xref linkend="part-sysconfig"/>.
   </para>
  </step>
 </procedure>
</sect1>
