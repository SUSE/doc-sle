sudo: required
language: bash
services:
  - docker

before_install:
  - docker pull svenseeberg/doc_ci
  # list the installed packages (just for easier debugging)
  #- docker run --rm -it sle-doc-image rpm -qa | sort

script:
  - docker run --rm -it svenseeberg/doc_ci find . -maxdepth 1 -name 'DC-*' -exec echo "Validating {}..." \; -exec daps -d \{\} validate --remarks \;
