<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter
[
  <!ENTITY % entities SYSTEM "entity-decl.ent">
    %entities;
]>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.0" xml:id="cha.gnome.settings">
 <title>Customizing Your Settings</title>
 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker>
   </dm:bugtracker>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>
 <para>
  You can change the way the &gnome; desktop looks and behaves to suit your
  own personal tastes and needs.
  Some possible changes settings are:
 </para>
 <itemizedlist mark="bullet" spacing="normal">
  <listitem>
   <para>
    Keyboard and mouse configuration, as described in
    <xref linkend="sec.gnome.settings.hardware.keyboard"/> and
    <xref linkend="sec.gnome.settings.hardware.mouse"/>
   </para>
  </listitem>
  <listitem>
   <para>
    Desktop background, as described in
    <xref linkend="sec.gnome.settings.look.deskback"/>
   </para>
  </listitem>
  <listitem>
   <para>
    Sounds, as described in
    <xref linkend="sec.gnome.settings.hardware.audio"/>
   </para>
  </listitem>
 </itemizedlist>
 <para>
  These settings and others can be changed in the <guimenu>All
  Settings</guimenu> dialog.
 </para>
 <sect1 xml:id="sec.gnome.settings.cc">
  <title>The &gnome; Settings Dialog</title>

  <para>
   While &yast; is a desktop-independent system-wide tool to configure
   most aspects of your &productname; installation, such as hardware
   settings, network devices and services, software management or
   virtualization, the settings dialog is a &gnome; configuration tool and
   focuses more on the look and feel, personal settings and preferences of
   your &gnome; desktop.
  </para>

  <para>
   To access the settings dialog, click <menuchoice>
   <guimenu>Applications</guimenu> <guimenu>System Tools</guimenu>
   <guimenu>Settings</guimenu> </menuchoice>. The dialog is divided into the
   following three categories:
  </para>

  <variablelist>
   <varlistentry>
    <term><guimenu>Personal</guimenu></term>
    <listitem>
     <para>
      Go here to change your login password, language settings and
      keyboard layout.
      You can also modify the desktop background or set up accounts for e-mail,
      chat and cloud providers.
      For more information, see <xref linkend="sec.gnome.settings.personal"/>.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><guimenu>Hardware</guimenu></term>
    <listitem>
     <para>
      Allows you to configure hardware components such as monitors, printers,
      mouses/touchpads, network adapters and sound devices.
      You can also change key combination settings and set up power-saving
      features.
      For more information, see <xref linkend="sec.gnome.settings.hardware"/>.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><guimenu>System</guimenu></term>
    <listitem>
     <para>
      Lets you configure system settings such as date and time, whether to start
      software when inserting USB drives or whether you want to share your
      screen with others.
      You can also set up user accounts.
      If you want, you can also start &yast; from this screen, though it is
      also available separately from within the menu.
      For more information, see <xref linkend="sec.gnome.settings.system"/>.
     </para>
    </listitem>
   </varlistentry>
  </variablelist>

  <figure>
   <title>&gnome; Settings Dialog</title>
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="controlcenter.png" width="90%" format="PNG"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="controlcenter.png" width="75%" format="PNG"/>
    </imageobject>
   </mediaobject>
  </figure>

  <para>
   In order to change some system-wide settings, the control center will
   prompt you for the <systemitem>root</systemitem> password and start
   &yast;. This is mostly the case for administrator settings (including
   most of the hardware, the graphical user interface, Internet access,
   security settings, user administration, software installation and system
   updates and information). Follow the instructions in &yast; to
   configure these settings. <phrase os="sled">For information about using
   &yast;, refer to the integrated &yast; help texts or to the
   <xref linkend="book.sle.deployment"/>.</phrase>
  </para>

  <para>
   This chapter focuses on individual settings you can change directly in
   the &gnome; settings dialog, without having to use &yast;.
  </para>
 </sect1>

 <sect1 xml:id="sec.gnome.settings.personal">
  <title>Personal</title>

  <para>
   The following sections introduce examples of how to configure some
   personal aspects of your &gnome; desktop, like your languages used or
   desktop backgrounds.
  </para>

  <sect2 xml:id="sec.gnome.settings.look.deskback">
   <title>Changing the Desktop Background and Lock Screen Appearance</title>
   <para>
    The desktop background is the image or color that is applied to your
    desktop. You can also customize the image shown when the screen is
    locked.
   </para>
   <para>
    To change the desktop background or the lock screen:
   </para>
   <procedure>
    <step>
     <para>
      Click <menuchoice> <guimenu>Applications</guimenu> <guimenu>System
      Tools</guimenu> <guimenu>Settings</guimenu>
      <guimenu>Background</guimenu> </menuchoice>.
     </para>
<!--<informalfigure>
      <mediaobject>
       <imageobject role="fo">
        <imagedata fileref="preferences_background.png" width="45%"
         format="PNG"/>
       </imageobject>
       <imageobject role="html">
        <imagedata fileref="preferences_background.png" width="45%"
         format="PNG"/>
       </imageobject>
      </mediaobject>
     </informalfigure>-->
    </step>
    <step>
     <para>
      Click <guimenu>Background</guimenu> or <guimenu>Lock Screen</guimenu>.
     </para>
    </step>
    <step>
     <para>
      Click <guimenu>Wallpapers</guimenu>, <guimenu>Pictures</guimenu>, or
      <guimenu>Colors</guimenu>. Wallpapers are preconfigured images
      distributed with your system. Pictures are your own images from your
      <filename>Pictures</filename> directory
      (<filename>~/Pictures</filename>). Colors are predefined colors chosen
      by &gnome; developers.
     </para>
    </step>
    <step>
     <para>
      Choose an option from the list.
     </para>
    </step>
    <step>
     <para>
      When you are satisfied with your choices, click
      <guimenu>Select</guimenu>.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.lang">
   <title>Configuring Language Settings</title>
   <para>
    &productname; can be configured to use any of a number of languages.
    The language setting determines the language of dialogs and menus and
    can also determine the keyboard and clock layout.
   </para>
   <para>
    To configure your language settings click <menuchoice>
    <guimenu>Applications</guimenu> <guimenu>System Tools</guimenu>
    <guimenu>Settings</guimenu> <guimenu>Region and Language</guimenu>
    </menuchoice>. Here you can choose the interface language, number
    formats and input sources (keyboard layouts).
   </para>
  </sect2>
 </sect1>

 <sect1 xml:id="sec.gnome.settings.hardware">
  <title>Hardware</title>

  <para>
   In the following sections you will find examples of how to configure some
   hardware aspects of your &gnome; desktop, including keyboard or mouse
   preferences, handling of removable drives (and other media) or screen
   resolution.
  </para>

  <sect2 xml:id="sec.gnome.settings.hardware.bluetooth">
   <title>Configuring Bluetooth Settings</title>
   <para>
    The Bluetooth module lets you set the visibility of your machine over
    Bluetooth and to connect to available Bluetooth devices. To configure
    Bluetooth connectivity, follow these steps:
   </para>
   <procedure>
    <step>
     <para>
      Click <menuchoice> <guimenu>Applications</guimenu> <guimenu>System
      Tools</guimenu> <guimenu>Settings</guimenu>
      <guimenu>Bluetooth</guimenu> </menuchoice> to open the Bluetooth
      settings module.
     </para>
    </step>
    <step>
     <para>
      To use Bluetooth, turn the <guimenu>Bluetoth</guimenu> switch on.
     </para>
    </step>
    <step>
     <para>
      To make your computer visible over Bluetooth, turn the
      <guimenu>Visibility</guimenu> switch on. This visibility is only
      temporary. You do not need this option to be turned on to connect to a
      Bluetooth device.
     </para>
    </step>
    <step>
     <para>
      The <guimenu>Devices</guimenu> list contains all known Bluetooth
      devices. In the beginning, it may be empty.
     </para>
     <para>
      To add a device to the list, click the plus icon in the lower left
      corner. On the device to be connected, turn the Bluetooth visibility
      on. In <guimenu>Device Type</guimenu>, choose the type of the device
      you want to connect, for example an input device, a phone, a computer
      or a camera. To see all available devices, set <guimenu>All
      Types</guimenu>. Select the desired device from the list. In
      <guimenu>PIN Options</guimenu> select the appropriate option and click
      <guimenu>Continue</guimenu>. If a PIN was used, confirm the PIN match.
     </para>
     <para>
      To remove a device from the list, select the device and click the
      minus icon.
     </para>
    </step>
    <step>
     <para>
      To connect to a Bluetooth device, select the device in the list and
      turn the <guimenu>Connection</guimenu> switch on. You can send files
      to the connected device using the <guimenu>Send Files</guimenu>
      button. If you are connected to a device such as a mobile phone, you
      can use it as a network device by activating the appropriate option.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.hardware.power">
   <title>Configuring Power Settings</title>
   <para/>
   <procedure>
    <step>
     <para>
      Click <menuchoice> <guimenu>Applications</guimenu> <guimenu>System
      Tools</guimenu> <guimenu>Settings</guimenu> <guimenu>Power</guimenu>
      </menuchoice> to open the Power settings module.
     </para>
    </step>
    <step>
     <para>
      In the upper part of the dialog, you can see the current state of the
      battery.
     </para>
    </step>
    <step>
     <para>
      In the <guimenu>Power Saving</guimenu> section of the dialog, set the
      <guimenu>Screen Brightness</guimenu> to conserve power. You can also
      set, whether to dim the screen after a period of inactivity and set
      the time interval. You can also set, whether to turn off wireless
      networking after the period of inactivity.
     </para>
    </step>
    <step>
     <para>
      In the <guimenu>Suspend and Power Off</guimenu> section of the dialog,
      set the <guimenu>Automatic Suspend</guimenu>. When you click it, a
      separate dialog opens, where you can turn the automatic suspending on
      and associated time intervals separately for computer running on
      battery power or plugged in. You can also set the action performed
      when the battery power is critical (hibernation to the disk or
      powering off).
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.hardware.keyboard">
   <title>Modifying Keyboard Settings</title>
   <para>
    To modify some keyboard settings (such as autorepeat preferences or
    typing break sessions) click <menuchoice>
    <guimenu>Applications</guimenu> <guimenu>System Tools</guimenu>
    <guimenu>Settings</guimenu> <guimenu>Keyboard</guimenu> </menuchoice>.
   </para>
   <figure xml:id="fig.gnome.settings.hardware.keyboard">
    <title>Keyboard Settings Dialog</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="preferences_keyboard.png" width="50%" format="PNG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="preferences_keyboard.png" width="55%" format="PNG"/>
     </imageobject>
    </mediaobject>
   </figure>
   <procedure>
    <step>
     <para>
      On the <guimenu>Typing</guimenu> tab you can set some general keyboard
      preferences, such as enabling keyboard repeat with individual delay
      and speed options or enabling or disabling the blinking of the cursor
      and defining the speed.
     </para>
    </step>
    <step>
     <para>
      On the <guimenu>Shortcuts</guimenu> tab you can set the key
      combinations. To edit a key combination, click the row and hold down
      the new keys or press backspace to clear the key combination.
     </para>
    </step>
    <step>
     <para>
      If all options are set according to your wishes, close the dialog.
     </para>
    </step>
   </procedure>
   <para>
    For configuration of keyboard accessibility options refer to
    <xref linkend="sec.general.accessibility.mobility"/>.
   </para>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.hardware.mouse">
   <title>Configuring the Mouse and Touchpad</title>
   <para>
    To modify some mouse options click <menuchoice>
    <guimenu>Applications</guimenu> <guimenu>System Tools</guimenu>
    <guimenu>Settings</guimenu> <guimenu>Mouse and Touchpad</guimenu>
    </menuchoice>.
   </para>
   <figure xml:id="fig.gnome.settings.hardware.mouse">
    <title>Mouse and Touchpad Settings Dialog</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="preferences_mouse.png" width="55%" format="PNG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="preferences_mouse.png" width="45%" format="PNG"/>
     </imageobject>
    </mediaobject>
   </figure>
   <procedure>
    <step>
     <para>
      In the <guimenu>General</guimenu> section of the dialog, set the
      <guimenu>Primary button</guimenu> orientation (left or right) and the
      speed of the double-click.
     </para>
    </step>
    <step>
     <para>
      In the <guimenu>Mouse</guimenu> section of the dialog, use
      <guimenu>Pointer Speed</guimenu> to adjust the sensitivity of the
      mouse pointer.
     </para>
    </step>
    <step>
     <para>
      In the <guimenu>Touchpad</guimenu> section of the dialog, turn the
      touchpad on and off and use <guimenu>Pointer Speed</guimenu> to adjust
      the sensitivity of the touchpad pointer. You can also disable the
      touchpad while typing and enable clicks by taping the touchpad.
     </para>
    </step>
    <step>
     <para>
      To test your new settings, click <guimenu>Test Your Settings</guimenu>
      and try the pointing device. If all options are set according to your
      wishes, close the dialog.
     </para>
    </step>
   </procedure>
   <para>
    For configuration of mouse accessibility options refer to the
    <xref linkend="sec.general.accessibility.mobility"/>.
   </para>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.hardware.printer">
   <title>Installing and Configuring Printers</title>
   <para>
    The <guimenu>Printers</guimenu> module lets you connect to any available
    local or remote CUPS server and configure printers.
   </para>
   <para>
    To start the Printers module, click <menuchoice>
    <guimenu>Applications</guimenu> <guimenu>System Tools</guimenu>
    <guimenu>Settings</guimenu> <guimenu>Printers</guimenu> </menuchoice>.
    For detailed information refer to <xref linkend="cha.gnome.print"/>.
   </para>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.hardware.screen">
   <title>Configuring Screens</title>
   <para>
    To specify the resolution, refresh rate and orientation for your screen
    or to configure multiple screens, click <menuchoice>
    <guimenu>Applications</guimenu> <guimenu>System Tools</guimenu>
    <guimenu>Settings</guimenu> <guimenu>Displays</guimenu> </menuchoice>
    and modify the options.
   </para>
   <procedure>
    <step>
     <para>
      To set options for any monitor, click the monitor's icon and set its
      resolution and orientation (using the arrows).
     </para>
    </step>
    <step>
     <para>
      If you use multiple monitors, you can set their respective positions
      by dragging their icons to the appropriate location.
     </para>
    </step>
   </procedure>
   <figure xml:id="fig.gnome.settings.hardware.screen">
    <title>Monitor Resolution Settings Dialog</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="preferences_screen.png" width="45%" format="PNG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="preferences_screen.png" width="45%" format="PNG"/>
     </imageobject>
    </mediaobject>
   </figure>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.hardware.audio">
   <title>Configuring Sound Settings</title>
   <para>
    The <guimenu>Sound</guimenu> tool lets you manage sound devices and set
    the sound effects. In the top part of the dialog, you can select the
    general output volume or turn the sound off completely.
   </para>
   <para>
    Click <menuchoice> <guimenu>Applications</guimenu> <guimenu>System
    Tools</guimenu> <guimenu>Settings</guimenu> <guimenu>Sound</guimenu>
    </menuchoice> to open the sound settings.
   </para>
   <figure>
    <title>Configuring Sound Settings</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="sound_devices.png" width="65%" format="PNG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="sound_devices.png" width="65%" format="PNG"/>
     </imageobject>
    </mediaobject>
   </figure>
   <sect3 xml:id="sec.gnome.settings.hardware.audio.dev">
    <title>Configuring Sound Devices</title>
    <para>
     Use the <guimenu>Output</guimenu> tab to select the device for sound
     output. Below the list, choose the sound device setting you prefer, for
     example balance.
    </para>
    <para>
     Use the <guimenu>Input</guimenu> tab to set the input device volume or
     to mute the input temporarily. If you have more than one sound device,
     you can also select a default device for audio input in the
     <guimenu>Choose a device for sound input</guimenu> list.
    </para>
   </sect3>
   <sect3 xml:id="sec.gnome.settings.hardware.audio.effects">
    <title>Configuring Sound Effects</title>
    <para>
     Use the <guimenu>Sound Effects</guimenu> tab to configure sound event
     functions.
    </para>
    <para>
     Specify the volume at which the sound effects will be played under
     <guimenu>Alert volume</guimenu>. You can also turn the effects on and
     off.
    </para>
    <para>
     Select the <guimenu>Alert Sound</guimenu> to use.
    </para>
   </sect3>
  </sect2>

<!--<sect2 xml:id="sec.gnome.settings.system.proxy">
   <title>Configuring Network Proxies</title>
   <para>
    The Network Proxy Configuration tool lets you configure how your system
    connects to the Internet. You can configure the desktop to connect to a
    proxy server and specify the details of the server. A proxy server is a
    server that intercepts requests to another server and fulfills the
    request itself, if it can. You can specify the Domain Name Service (DNS)
    name or the Internet Protocol (IP) address of the proxy server. A DNS
    name is a unique alphabetic identifier for a computer on a network. An
    IP address is a unique numeric identifier for a computer on a network.
   </para>
   <para>
    Click <menuchoice> <guimenu>Computer</guimenu> <guimenu>Control
    Center</guimenu> <guimenu>System</guimenu> <guimenu>Network
    Proxy</guimenu> </menuchoice>.
   </para>
   <figure id="fig.gnome.settings.system.proxy">
    <title>Network Proxy Configuration Dialog</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="preferences_network.png" width="65%" format="PNG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="preferences_network.png" width="65%" format="PNG"/>
     </imageobject>
    </mediaobject>
   </figure>
   <para>
    For more information on the individual options, click
    <guimenu>Help</guimenu>.
   </para>
  </sect2>-->

 </sect1>

 <sect1 xml:id="sec.gnome.settings.system">
  <title>System</title>

  <para>
   In the following sections you will find examples of how to configure some
   system aspects of your &gnome; desktop, like language settings, power
   management, preferred applications, session (and session sharing)
   preferences and audio preferences. For configuration of assistive
   technologies refer to <xref linkend="cha.gnome.accessibility"/>.
  </para>

<!-- <sect2 xml:id="sec.gnome.settings.personal.password">
   <title>Changing Your Password</title>
   <para>
    For security reasons, it is a good idea to change your login password
    from time to time. To change your password:
   </para>
   <procedure>
    <step>
     <para>
      Click <menuchoice> <guimenu>Computer</guimenu> <guimenu>Control
      Center</guimenu> <guimenu>Change Password</guimenu></menuchoice>.
     </para>
    </step>
    <step>
     <para>
      In the upper right corner, click <guimenu>Change
      Password...</guimenu>.
     </para>
    </step>
    <step>
     <para>
      Type your old (current) password.
     </para>
    </step>
    <step>
     <para>
      Type your new password.
     </para>
    </step>
    <step>
     <para>
      Confirm your new password by typing it again, then click
      <guimenu>Change Password</guimenu>.
     </para>
    </step>
   </procedure>
  </sect2>-->

  <sect2 xml:id="sec.gnome.settings.prefapps">
   <title>Setting Preferred Applications</title>
   <para>
    The Preferred Applications module allows you to change the default
    application for various common tasks such as browsing the Internet,
    sending mails or playing multimedia files.
   </para>
   <figure>
    <title>Preferred Applications</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="preferences_preferred_apps.png" width="65%" format="PNG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="preferences_preferred_apps.png" width="65%" format="PNG"/>
     </imageobject>
    </mediaobject>
   </figure>
   <procedure>
    <step>
     <para>
      Click <menuchoice> <guimenu>Applications</guimenu> <guimenu>System
      Tools</guimenu> <guimenu>Settings</guimenu><guimenu>Details</guimenu>
      </menuchoice>.
     </para>
    </step>
    <step>
     <para>
      Click <guimenu>Default Applications</guimenu>.
     </para>
    </step>
    <step>
     <para>
      Select one of the available applications from the drop-down box. You
      can choose an application to handle Web, mail, calendar, music, videos
      or photographs.
     </para>
    </step>
   </procedure>
   <para>
    The changes take effect immediately.
   </para>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.share">
   <title>Setting Session Sharing Preferences</title>
   <para>
    The <guimenu>Remote Desktop Preference</guimenu> dialog lets you share a
    &gnome; desktop session between multiple users and set session-sharing
    preferences.
   </para>
   <important>
    <title>Sharing Desktop Sessions Affects System Security</title>
    <para>
     Be aware that sharing desktop sessions can be a security risk. Use the
     restriction options available. If you need to adjust the options to a
     lower security level, do not forget to switch back to a higher security
     level as soon as possible.
    </para>
   </important>
   <procedure>
    <step>
     <para>
      Click <menuchoice> <guimenu>Applications</guimenu> <guimenu>System
      Tools</guimenu> <guimenu>Settings</guimenu> <guimenu>Sharing</guimenu>
      </menuchoice>.
     </para>
     <informalfigure>
      <mediaobject>
       <textobject role="description"><phrase>Sharing Preferences dialog box</phrase>
       </textobject>
       <imageobject role="fo">
        <imagedata fileref="remote_desktop_a.png" width="48%" format="PNG"/>
       </imageobject>
       <imageobject role="html">
        <imagedata fileref="remote_desktop_a.png" width="48%" format="PNG"/>
       </imageobject>
      </mediaobject>
     </informalfigure>
    </step>
    <step>
     <para>
      Turn on the switch in the upper part of the dialog.
     </para>
    </step>
    <step>
     <para>
      To share your public directory over the network, click
      <guimenu>Personal File Sharing</guimenu> and turn on <guimenu>Share
      Public Folder On This Network</guimenu>. You can also set a password.
     </para>
    </step>
    <step>
     <para>
      To share your desktop session with other users click <guimenu>Screen
      Sharing</guimenu> and activate <guimenu>Remote View</guimenu>. To
      allow other users to control your screen, activate also
      <guimenu>Remote Control</guimenu>. You can also set a password.
     </para>
    </step>
    <step>
     <para>
      Click the address in the selected text to send the system address by
      e-mail to a remote user.
     </para>
    </step>
   </procedure>
  </sect2>

<!--<sect2 xml:id="sec.gnome.settings.system.updates">
   <title>Setting Software Updates</title>
   <para>
    The <guimenu>Software Updates</guimenu> tool lets you configure the
    frequency of update checking, automatic updates and update
    notifications. Click <menuchoice> <guimenu>Computer</guimenu>
    <guimenu>Control Center</guimenu> <guimenu>System</guimenu>
    <guimenu>Software Updates</guimenu> </menuchoice> to open the
    <guimenu>Software Updates Preferences</guimenu> tool. Refer to
    <xref
     linkend="sec.updater.gnome.config"/> for more information.
   </para>
  </sect2>-->

  <sect2 xml:id="sec.gnome.settings.admin">
   <title>Configuring Administrative Settings with &yast;</title>
   <para>
    For your convenience, &yast; is available from the Control Panel as
    well as the Applications menu. <phrase os="sled">For information about
    using &yast;, refer to
    <xref linkend="book.sle.deployment"/>.</phrase>
   </para>
  </sect2>
 </sect1>
</chapter>
