<?xml version="1.0" encoding="UTF-8"?>
<sect1 xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="security_ldap_firewall.xml" version="5.0" xml:id="sec-security-ldap-firewall">
 <info>
  <title>防火墙配置</title>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>
  <para>
      389 Directory Server 的默认 TCP 端口为 389 和 636。TCP 端口 389 用于建立未加密连接，以及用于 STARTTLS。端口 636 用于通过 TLS 建立加密连接。
  </para>
  <para>
      <systemitem class="daemon">firewalld</systemitem> 是 SUSE Linux Enterprise 的默认防火墙管理器。以下规则会激活 <literal>ldap</literal> 和 <literal>ldaps</literal> 防火墙服务：
  </para>
  <screen><prompt>&gt; </prompt><command>sudo</command> <command>firewall-cmd --add-service=ldap --zone=<replaceable>internal</replaceable></command>
<prompt>&gt; </prompt><command>sudo</command> <command>firewall-cmd --add-service=ldaps --zone=<replaceable>internal</replaceable></command>
<prompt>&gt; </prompt><command>sudo</command> <command>firewall-cmd --runtime-to-permanent</command>
</screen>
  <para>
      请将 zone 替换为您服务器的相应区域。有关使用 TLS 保护连接的信息，请参见<xref linkend="sec-security-ldap-server-ca"/>；有关 <systemitem class="daemon">firewalld</systemitem> 的信息，请参见<xref linkend="sec-security-firewall-fw"/>。
  </para>
</sect1>
