<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter
[
  <!ENTITY % entities SYSTEM "generic-entities.ent">
    %entities;
  <!ENTITY hamburger-icon "<inlinemediaobject xmlns='http://docbook.org/ns/docbook'><textobject role='description'><phrase>Three-lines button</phrase></textobject><imageobject role='fo'><imagedata fileref='hamburger_button.svg' width='0.8em' format='SVG'/></imageobject><imageobject role='html'><imagedata fileref='hamburger_button-fallback.png' width='1em' format='PNG'/></imageobject></inlinemediaobject>">
]>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.0" xml:id="cha-firefox">
 <title><productname>Firefox</productname>: browsing the Web</title>
 <info>
  <abstract>
   <para>
    The <productname>Mozilla Firefox</productname> Web browser is included
    with &productnamereg;. With features like tabbed browsing, pop-up window
    blocking and download management, <productname>Firefox</productname>
    combines the latest browsing and security technologies with an easy-to-use
    interface. It gives you easy access to different search engines to help you
    find the information you need.
   </para>
  </abstract>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker></dm:bugtracker>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>
 <sect1 xml:id="sec-firefox-start">
  <title>Starting <productname>Firefox</productname></title>

  <para>
   Open the <guimenu>Activities</guimenu> overview by pressing <keycap
   function="meta"/> and search for <literal>fire</literal>.
  </para>
 </sect1>
 <sect1 xml:id="sec-firefox-navigating">
  <title>Navigating Web sites</title>

  <para>
   The look and feel of <productname>Firefox</productname> is similar to that
   of other browsers. It is shown in <xref linkend="fig-firefox-main"/>. At the
   top of the window, you find the address bar for a Web address, and the
   search bar. Bookmarks are also available for quick access from the bookmarks
   toolbar. For more information about the various
   <productname>Firefox</productname> features, use the
   <guimenu>Help</guimenu> menu in the menu bar.
  </para>

  <note>
   <title>Using the menu bar</title>
   <para>
    While most functions of <productname>Firefox</productname> are available
    through the three-lines button (&hamburger-icon;), some are only available
    from the menu bar.
   </para>
   <para>
    The menu bar of <productname>Firefox</productname> is hidden by default.
    To temporarily show it, press <keycap function="alt"/>. It will then be
    displayed until you click elsewhere in the browser window.
   </para>
   <para>
    To permanently enable the menu bar, first press
    <keycap function="alt"/>, then choose
    <menuchoice><guimenu>View</guimenu><guimenu>Toolbars</guimenu></menuchoice>
    and activate the <guimenu>Menu Bar</guimenu> check box.
   </para>
  </note>

  <figure xml:id="fig-firefox-main">
   <title>The browser window of <productname>Firefox</productname></title>
   <mediaobject>
    <imageobject role="html">
     <imagedata fileref="firefox_main.png" width="100%"/>
    </imageobject>
    <imageobject role="fo">
     <imagedata fileref="firefox_main.png" width="100%"/>
    </imageobject>
   </mediaobject>
  </figure>

  <sect2 xml:id="sec-firefox-locationbar">
   <title>The address bar</title>
   <para>
    When typing into the address bar, an auto-completion drop-down box opens.
    It shows all previous location addresses and bookmarks containing the
    characters you type. The matching phrase is highlighted in bold. Entries
    visited most frequently and recently are listed first.
   </para>
   <para>
    List entries from the bookmark list are marked with a star. Bookmarks with
    tags are marked with an additional label followed by the tag names. List
    entries from the browsing history are not marked. To search in your
    bookmarks only, type <keycap>*</keycap> as the first character of your
    search.
   </para>
   <para>
    Use <keycap function="up"/> and <keycap function="down"/> or the mouse
    wheel to navigate the list. Press <keycap function="enter"/> or click an
    entry to go to the selected page. <keycap function="delete"/> removes an
    entry from the list if it is an entry from the history. Bookmarked entries
    can only be removed by deleting the associated bookmark.
   </para>
  </sect2>

  <sect2 xml:id="sec-firefox-zoom">
   <title>Zooming</title>
   <para>
    Firefox offers two zooming options: page zoom, the default, and text zoom.
    Page zoom enlarges the entire page as is, with all elements of a page,
    including graphics, expanding equally while text zoom only changes the text
    size.
   </para>
   <para>
    To toggle between page and text zoom, from the menu bar, choose
    <menuchoice> <guimenu>View</guimenu> <guimenu>Zoom</guimenu> <guimenu>Zoom
    Text Only</guimenu></menuchoice>. To zoom in or out either use the mouse
    wheel while holding the <keycap function="control"/> key, or use <keycombo>
    <keycap function="control"/> <keycap>+</keycap> </keycombo> and <keycombo>
    <keycap function="control"/> <keycap>-</keycap></keycombo>. Reset the zoom
    factor with <keycombo> <keycap function="control"/> <keycap>0</keycap>
    </keycombo>.
   </para>
  </sect2>

  <sect2 xml:id="sec-firefox-tabbedbrowsing">
   <title>Tabbed browsing</title>
   <para>
    Tabbed browsing allows you to load multiple Web sites in a single window.
    To switch between pages in use, use the tabs at the top of the window. If
    you often use more than one Web page at a time, tabbed browsing makes it
    easier to switch between pages.
   </para>
   <variablelist>
    <varlistentry>
     <term>Opening tabs</term>
     <listitem>
      <para>
       To open a new tab, from the menu bar, select <menuchoice>
       <guimenu>File</guimenu><guimenu>New Tab</guimenu> </menuchoice> or press
       <keycombo> <keycap function="control"/> <keycap>T</keycap> </keycombo>.
       This opens an empty tab in the <productname>Firefox</productname> window.
       To open a link on a Web page or a bookmark in a tab, middle-click it.
       Alternatively, right-click a link and select
       <guimenu>Open Link in New Tab</guimenu>. You may also
       open an address in the address bar in a new tab with a middle-click or
       by pressing <keycombo> <keycap function="control"/>
       <keycap function="enter"/> </keycombo>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Closing tabs</term>
     <listitem>
      <para>
       Right-click a tab to open a context menu, giving you access to tab
       managing options such as closing, reloading, or bookmarking. To close a
       tab, you may also use <keycombo> <keycap function="control"/>
       <keycap>W</keycap> </keycombo> or click the close button. Any closed tab
       can be restored by choosing from the menu bar, <menuchoice>
       <guimenu>History</guimenu> <guimenu>Recently Closed Tabs</guimenu>
       </menuchoice>. To reopen the last closed tab, either choose
       <guimenu>Undo Close Tab</guimenu> from the context menu or press
       <keycombo> <keycap function="control"/> <keycap function="shift"/>
       <keycap>T</keycap></keycombo>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Sorting tabs</term>
     <listitem>
      <para>
       By default, tabs are sorted in the order you opened them. Rearrange the
       tab order by dragging and dropping a tab to the new position. If you
       have opened a lot of tabs, they cannot all be displayed in the
       tab bar at the same time. Use the arrows at the ends of the bar to move
       left or right-click the down arrow at the right end of the tab bar to
       get a list of all tabs.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Dragging and dropping</term>
     <listitem>
      <para>
       Drag and drop also works with tabs. Drag a link onto an existing tab to
       open it in that tab or drag and drop a link on an empty space in the tab
       bar to open a new tab. Drag and drop a tab outside of the tab bar to
       open it in a new browser window.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>

  <sect2 xml:id="sec-firefox-sidebar">
   <title>Using the sidebar</title>
   <para>
    Use the left side of your browser window for viewing bookmarks or browsing
    history. Extensions may add new ways to use the sidebar as well. To display
    the sidebar, from the menu bar, select <menuchoice> <guimenu>View</guimenu>
    <guimenu>Sidebar</guimenu> </menuchoice> and select the contents.
   </para>
  </sect2>
 </sect1>
 <sect1 xml:id="sec-firefox-findinfo">
  <title>Finding information</title>

  <para>
   There are two ways to find information in <productname>Firefox</productname>.
   To search the Internet with a search engine, use the search bar. To search
   the page currently displayed, use the find bar.
  </para>

  <sect2 xml:id="sec-firefox-searchengine">
   <title>Finding information on the Web</title>
   <para>
    <productname>Firefox</productname> has a search bar that can access
    different engines like Google, Yahoo, or Amazon. For example, if you want to
    find information about &suse; using the current engine, click in the search
    bar, type <literal>&suse;</literal>, and press <keycap function="enter"/>.
    The results appear in your window.
   </para>
   <para>
    To choose a different search engine, type your search term, then click one
    of the search provider icons at the bottom of the appearing pop-up.
   </para>
   <sect3 xml:id="sec-firefox-searchengine-add">
    <title>Customizing the search bar</title>
    <para>
     If you want to change the order, add, or delete a search engine, proceed
     as follows.
    </para>
    <procedure>
     <step>
      <para>
       Click the hamburger icon to the right of the search bar.
      </para>
     </step>
     <step>
      <para>
       From the list, select <guimenu>Settings</guimenu>. 
      </para>
     </step>
     <step>
      <para>
       In the <guimenu>Settings</guimenu> window, go to
       <guimenu>Search</guimenu>.
      </para>
     </step>
     <step>
      <para>
       The <guimenu>Default Search Engine</guimenu> section shows the engine that
       is currently set as the default search engine.
      </para>
     </step>
     <step>
      <para>
       To select another search engine, click the drop-down menu and select an
       engine from the list.
      </para>
<!--  2022-04-08 jufa: can't find these features
       <para>
       To add a search engine, click <guimenu>Add More Search
       Engines</guimenu>. Firefox displays a Web page with available search
       plug-ins.
       You can choose from Wikipedia, IMDB, Flickr, and numerous
       others.
       To install a search plug-in, select it and click <guimenu>Add to
       Firefox</guimenu>.
      </para>-->
     </step>
    </procedure>
    <figure xml:id="fig-firefox-manage-search">
     <title>Firefox&mdash;manage search engines</title>
     <mediaobject>
      <imageobject role="html">
       <imagedata fileref="firefox_manage_searchengines.png" width="70%"/>
      </imageobject>
      <imageobject role="fo">
       <imagedata fileref="firefox_manage_searchengines.png" width="70%"/>
      </imageobject>
     </mediaobject>
    </figure>
    <para>
     Some Web sites offer search engines that you can add directly to the
     search bar. Whenever you are visiting such a Web site, the icon to the
     left of the search bar gains a <literal>+</literal> sign. Click the icon
     and select <guimenu>Add</guimenu>.
    </para>
   </sect3>
   <sect3 xml:id="sec-firefox-searchengine-smartkeywords">
    <title>Adding keywords to your online searches</title>
    <para>
     <productname>Firefox</productname> lets you define own
     <emphasis>keywords</emphasis>: abbreviations to use as a URL shortcut for
     a particular search engine. If you have defined <literal>ws</literal> as a
     keyword for the Wikipedia search, for example, you can type <literal>ws
     <replaceable>SEARCHTERM</replaceable></literal> into the address bar to
     search Wikipedia for <replaceable>SEARCHTERM</replaceable>.
    </para>
    <para>
     To assign a shortcut for a search engine from the search bar, click the
     hamburger icon to the right of the search bar and select <menuchoice>
     <guimenu>Settings</guimenu><guimenu>Search</guimenu></menuchoice>. 
     In the <guimenu>Search Shortcuts</guimenu> section, select a search engine,
     double-click its <guimenu>Keyword</guimenu> column, enter a keyword and
     press <keycap function="enter"></keycap>.
    </para>
    <para>
     It is also possible to define a keyword for any search field on a Web
     site. Proceed as follows:
    </para>
    <procedure>
     <step>
      <para>
       Right-click the search field and choose <guimenu>Add a Keyword for this
       Search</guimenu> from the menu that opens. The <guimenu>Add
       bookmark</guimenu> dialog appears.
      </para>
     </step>
     <step>
      <para>
       In <guimenu>Name</guimenu>, enter a descriptive name for this keyword.
      </para>
     </step>
     <step>
      <para>
       Enter your <guimenu>Keyword</guimenu> for this search.
      </para>
     </step>
     <step>
      <para>
       <guimenu>Save</guimenu> this keyword.
      </para>
     </step>
    </procedure>
    <tip>
     <title>Keywords for regular Web sites</title>
     <para>
      Using keywords is not restricted to search engines. You can also add a
      keyword to a bookmark (via the bookmark's properties). For example, if
      you assign <literal>suse</literal> to the &suse; home page bookmark, you
      can open it by typing <literal>suse</literal> into the address bar.
     </para>
    </tip>
   </sect3>
  </sect2>

  <sect2 xml:id="sec-firefox-findinfo-page">
   <title>Searching in the current page</title>
   <para>
    To search inside a Web page, in the menu bar, click <menuchoice>
    <guimenu>Edit</guimenu> <guimenu>Find in Page</guimenu> </menuchoice> or
    press <keycombo> <keycap function="control"/> <keycap>F</keycap> </keycombo>.
    The find bar opens at the bottom of the window. Type your query in the text
    box. <productname>Firefox</productname> finds the first occurrence of this
    phrase as you type. You can find other occurrences of the phrase by
    pressing <keycap>F3</keycap> or the <guimenu>Next</guimenu> button in the
    find bar. Clicking the <guimenu>Highlight All</guimenu> button will
    highlight all occurrences of the phrase. Checking the <guimenu>Match
    Case</guimenu> option makes the query case-sensitive.
   </para>
   <para>
    <productname>Firefox</productname> also offers two quick-find options.
    Click anywhere you like to start a search on a Web page, type the key
    <keycap>/</keycap> followed by the search term. The first occurrence of the
    search term will be highlighted as you type. Use <keycap>F3</keycap> to find
    the next occurrence. It is also possible to limit quick-find to links only.
    This search option is available by typing the key <keycap>'</keycap>.
   </para>
  </sect2>
 </sect1>
 <sect1 xml:id="sec-firefox-bookmarks">
  <title>Managing bookmarks</title>

  <para>
   Bookmarks offer a convenient way of saving links to your favorite Web sites.
   <productname>Firefox</productname> not only makes it very easy to add new
   bookmarks with just one mouse click, it also offers multiple ways to manage
   large bookmark collections.
   You can sort bookmarks into folders, classify them with tags, or filter them
   with smart bookmark folders.
  </para>

  <para>
   Add a bookmark by clicking the star in the address bar. The star will turn
   blue to indicate the page was bookmarked. The bookmark will be saved in the
   <guimenu>Bookmarks Toolbar</guimenu> folder under the page title. To change
   the name and folder of your bookmark or add tags, after bookmarking, click
   the star again. This will open a pop-up where you can make your changes.
  </para>

  <para>
   To bookmark all open tabs, right-click in a tab and choose <guimenu>Select
   All Tabs</guimenu>. Right-click on any tab again and select
   <guimenu>Bookmark Tabs</guimenu>. Firefox asks you to create a new folder for
   the tab links.
  </para>


  <para>
   To remove a bookmark, open the bookmarked location. Then, click the star and
   click <guimenu>Remove Bookmark</guimenu>.
  </para>

  <sect2 xml:id="sec-firefox-bookmarks-organise">
   <title>Organizing bookmarks</title>
   <para>
    The <guimenu>Library</guimenu> can be used to manage the properties (name
    and address location) for each bookmark and organize the bookmarks into
    folders and sections. It resembles <xref linkend="fig-firefox-library"/>.
   </para>
   <figure xml:id="fig-firefox-library">
    <title>The <productname>Firefox</productname> bookmark library</title>
    <mediaobject>
     <imageobject role="html">
      <imagedata fileref="firefox_library.png" width="85%"/>
     </imageobject>
     <imageobject role="fo">
      <imagedata fileref="firefox_library.png" width="85%"/>
     </imageobject>
    </mediaobject>
   </figure>
   <para>
    To open the <guimenu>Library</guimenu>, in the menu bar, click <menuchoice>
    <guimenu>Bookmarks</guimenu> <guimenu>Manage Bookmarks</guimenu>
    </menuchoice>. The library window is split into two parts: the left pane
    shows the folder tree view, the right pane the subfolders and bookmarks of
    the selected folder. Use <guimenu>Views</guimenu> to customize the right
    pane. The left pane contains three main folders:
   </para>
   <variablelist>
    <varlistentry>
     <term>History</term>
     <listitem>
      <para>
       Contains your complete browsing history. You cannot alter this list
       other than by deleting entries from it.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Tags</term>
     <listitem>
      <para>
       Lists bookmarks for each tag you have specified. See
       <xref linkend="sec-firefox-bookmarks-tags"/> for more information on
       tags.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>All Bookmarks</term>
     <listitem>
      <para>
       This category contains three main bookmark folders:
      </para>
      <variablelist>
       <varlistentry>
        <term>Bookmarks Toolbar</term>
        <listitem>
         <para>
          Contains the bookmarks and folders displayed beneath the location
          bar. See <xref linkend="sec-firefox-bookmarks-toolbar"/> for more
          information.
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term>Bookmarks Menu</term>
        <listitem>
         <para>
          Holds the bookmarks and folder accessible via the
          <guimenu>Bookmarks</guimenu> entry in the main menu or the bookmarks
          side menu.
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term>Other Bookmarks</term>
        <listitem>
         <para>
          Contains all bookmarks created with a single click on the star in the
          address bar. This folder is only visible in the library and the
          bookmarks sidebar.
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
   </variablelist>
   <para>
    Organize your bookmarks using the right pane. Choose actions for folders or
    bookmarks either from the context menu that opens when you right-click an
    item or from the <guimenu>Organize</guimenu> menu. The properties of a
    chosen folder or bookmark can be edited in the bottom part of the right
    pane. By default, only <guimenu>Name</guimenu>,
    <guimenu>URL</guimenu>, and <guimenu>Tags</guimenu> are displayed for
    a bookmark.
   </para>
   <para>
    To rearrange your bookmarks, use the mouse to drag them. You can use this
    to move a bookmark or a folder to a different folder, or to change the
    order of bookmarks in a folder.
   </para>
  </sect2>

  <sect2 xml:id="sec-firefox-bookmarks-tags">
   <title>Tags</title>
   <para>
    Tags offer a convenient way to file a bookmark under several categories.
    You can tag a bookmark with as many terms as you want. For example, to
    access all sites tagged with <literal>suse</literal>, enter
    <literal>suse</literal> into the address bar. For each tag, an item is
    automatically created in the <systemitem>Tags</systemitem> folder of
    the library. Drag and drop an item for a tag onto the bookmark toolbar to
    easily access it.
   </para>
   <para>
    To add tags to a bookmark, open the bookmark in
    <productname>Firefox</productname> and click the blue star in the address
    bar. The <guimenu>Edit bookmark</guimenu> dialog opens where you can add a
    comma-separated list of tags. It is also possible to add tags via the
    bookmark properties dialog which you can open in the library, or by
    right-clicking a bookmark in the menu or the toolbar.
   </para>
  </sect2>

  <sect2 xml:id="sec-firefox-bookmarks-imexport">
   <title>Importing and exporting bookmarks</title>
<!-- There is an import assistant for importing from other installed
   browsers. It is pretty useless though, since a) only Firefox is included with
   SLE, b) Epiphany and Chromium are not supported. (Though you can import from
   Chrome, IE, and Safari.) - sknorr, 2015-09-23 -->
   <para>
    To import bookmarks from another browser or from a file in HTML format,
    open the library by choosing from the menu bar, <menuchoice>
    <guimenu>Bookmarks</guimenu> <guimenu>Manage Bookmarks</guimenu>
    </menuchoice>. To start the Import Wizard, click <menuchoice>
    <guimenu>Import and Backup</guimenu> <guimenu>Import Bookmarks from
    HTML</guimenu> </menuchoice> and choose an import location. Start the
    import by clicking <guimenu>Next</guimenu>. Imports from an HTML file are
    imported as is.
   </para>
   <para>
    Exporting bookmarks is also done via <guimenu>Import and Backup</guimenu>
    in the library window. To save your bookmarks as an HTML file, choose
    <guimenu>Export Bookmarks to HTML</guimenu>. To create a backup of your
    bookmarks, choose <guimenu>Backup</guimenu>.
    <productname>Firefox</productname> uses a JavaScript Object Notation file
    format (<filename>.json</filename>) for backups.
   </para>
   <para>
    To restore a bookmark backup, click <menuchoice><guimenu>Import and
    Backup</guimenu> <guimenu>Restore</guimenu></menuchoice>. Then locate the
    backup you want to restore from.
   </para>
  </sect2>

  <sect2 xml:id="sec-firefox-bookmarks-toolbar">
   <title>The bookmarks toolbar</title>
   <para>
    The <guimenu>Bookmarks Toolbar</guimenu> is displayed beneath the address
    bar and lets you quickly access bookmarks. You can also add, organize, and
    edit bookmarks directly. To activate the <guimenu>Bookmarks
    Toolbar</guimenu>, select <menuchoice><guimenu>View </guimenu>
    <guimenu>Toolbars</guimenu> <guimenu>Bookmarks Toolbar </guimenu>
    <guimenu>Always Show</guimenu></menuchoice>.
   </para>
   <para>
    To edit the <literal>Bookmarks Toolbar</literal>, you can use the library
    as described in <xref linkend="sec-firefox-bookmarks-organise"/>. Its
    content is located in the folder <guimenu>Bookmarks Toolbar</guimenu>. It
    is also possible to manage the toolbar directly. To add a folder, bookmark,
    or separator, right-click an empty space in the toolbar and select the
    appropriate entry from the pop-up menu. To add the current page to the bar,
    click the icon of the Web page in the address bar and drag it to the
    final position on the bookmarks toolbar.
   </para>
   <para>
    To edit a certain folder or bookmark, right-click it. A menu opens where
    you can <guimenu>Remove Folder</guimenu> or change other settings. To move
    or copy an entry, choose <guimenu>Cut</guimenu> or <guimenu>Copy</guimenu>
    and <guimenu>Paste</guimenu> it to the final position.
   </para>
  </sect2>
 </sect1>
 <sect1 xml:id="sec-firefox-downloadmanager">
  <title>Using the download manager</title>

  <para>
   Keep track of your current and past downloads with the download manager. To
   start the download manager, in the menu bar, click <menuchoice>
   <guimenu>Tools</guimenu> <guimenu>Downloads</guimenu> </menuchoice>. While
   downloading a file, a progress bar indicates the download status. If
   necessary, pause the download and resume it later. To open a downloaded file
   with the associated application, click <guimenu>Open</guimenu>. To open the
   location to which the file was saved, choose <guimenu>Open Containing
   Folder</guimenu>. <guimenu>Remove From History</guimenu> only deletes the
   entry from the download manager, however, it does not delete the file from
   the hard disk.
  </para>

  <para>
   By default, all files are downloaded to <filename>~/Downloads</filename>. To
   change this behavior, in the menu bar, click <menuchoice>
   <guimenu>Edit</guimenu> <guimenu>Settings</guimenu> </menuchoice>. Go to
   <guimenu>General</guimenu>. Under <guimenu>Downloads</guimenu>, either
   choose another location or <guimenu>Always ask you where to save
   files</guimenu>.
  </para>

  <tip>
   <title>Resuming downloads</title>
   <para>
    If your browser crashes or is closed while downloading, all pending
    downloads will automatically be resumed in the background when starting
    <productname>Firefox</productname> the next time. A download that was
    paused before the browser was closed can be resumed manually via the
    download manager.
   </para>
  </tip>
 </sect1>
 <sect1 xml:id="sec-firefox-security">
  <title>Security</title>

<note>
 <title>FTP support stopped</title>
 <para>
 For security reasons <productname>Mozilla</productname> has removed the default
  support for clicking on FTP links in <productname>Firefox</productname>. You
  can enable this support by adding
  <command>network.gio.supported-protocols=ftp:</command> via the
  <command>about:config</command> settings.
  This will enable desktop support via the &gnome; Virtual File System (GVFS).
  See the GVFS documentation for all supported protocols.</para>
</note>

  <para>
   Since browsing the Internet has become more risky,
   <productname>Firefox</productname> offers various
   measures to make browsing safer. It automatically checks whether you are
   trying to access a site known to contain harmful software (malware) or a
   site known to steal sensitive data (phishing) and stops you from entering
   these sites. The Instant Web Site ID lets you easily check a site's
   legitimacy, and a password manager and the pop-up blocker offer additional
   security. With Private Browsing, you can surf the Internet without 
   <productname>Firefox</productname> recording data on your computer.
  </para>

  <sect2 xml:id="sec-firefox-security-instant-id">
   <title>Instant Web site ID</title>
   <para>
    <productname>Firefox</productname> allows you to check the identity of a
    Web page with a single glance. The icon in the address bar next to the
    address indicates which identity information is available and whether
    communication is encrypted:
   </para>
   <variablelist>
    <varlistentry>
     <term>Gray globe</term>
     <listitem>
      <para>
       The site does not provide any identity information and communication
       between Web server and browser is not encrypted. Do not exchange
       sensitive information with such sites.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Gray triangle</term>
     <listitem>
      <para>
       This site is from a domain that has been verified by a certificate, so
       you can be sure that you are really connected to the very site it claims
       to be. However, the site tried to load additional elements, such as
       images or scripts over an insecure connection. Firefox has blocked these
       items. Therefore, the page can look broken.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Gray padlock</term>
     <listitem>
      <para>
       This site is from a domain that has been verified by a certificate, so
       you can be sure that you are really connected to the very site it claims
       to be. Communication with a <quote>gray-padlock</quote> site is always
       encrypted.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Green padlock</term>
     <listitem>
      <para>
       This site completely identifies itself by a certificate that ensures a
       site is owned by the person or organization it claims to be. This is
       especially important when exchanging very sensitive data (for example
       when doing money transactions over the Internet). In this case you can
       be sure to be on the Web site of your bank when it sends complete
       identity information. Communication with a <quote>green-padlock</quote>
       server is always encrypted.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
   <para>
    To view detailed identity information, click the icon of the Web site in
    the address bar. In the opening pop-up, click <guimenu>More
    Information</guimenu> to open the <guimenu>Page Info</guimenu> window.
    Here, you can view the site's certificate, the encryption level, and
    information about stored passwords and cookies.
   </para>
   <para>
    On the <guimenu>Permissions</guimenu> tab, you can set per-site
    permissions for image loading, pop-ups, cookies and installation
    permissions. The <guimenu>Media</guimenu> tab lists all images, background
    graphics and embedded objects from a site and displays further information
    on each item together with a preview. It also lets you save individual
    items.
   </para>
   <figure xml:id="fig-firefox-pageinfo">
    <title>The <productname>Firefox</productname> page info window</title>
    <mediaobject>
     <imageobject role="html">
      <imagedata fileref="firefox_page_info.png" width="90%"/>
     </imageobject>
     <imageobject role="fo">
      <imagedata fileref="firefox_page_info.png" width="90%"/>
     </imageobject>
    </mediaobject>
   </figure>
  </sect2>

  <sect2 xml:id="sec-firefox-security-instant-id-cert-import">
   <title>Importing certificates</title>
   <para>
    <productname>Firefox</productname> comes with a certificate store for
    identifying certificate authorities (CA). Using these certificates enables
    the browser to automatically verify certificates issued by Web sites. If a
    Web site issues a certificate that has not been signed by one of the CAs
    from the certificate store, it is not trusted. This ensures that no spoofed
    certificates are accepted.
   </para>
   <para>
    Large organizations usually use their own certificate authorities in-house
    and distribute the respective certificates via the system-wide certificate
    store located at <filename>/etc/pki/nssdb</filename>. To configure Firefox
    (and other Mozilla tools, such as Thunderbird) to use this system-wide CA
    store in addition to its own, export the <envar>NSS_USE_SHARED_DB</envar>
    variable. For example, you can add the following line to
    <filename>~/.bashrc</filename>:
   </para>
<screen>export NSS_USE_SHARED_DB=1</screen>
   <para>
    Alternatively, or additionally, you can import certificates manually. To do
    so, in the menu bar, open the <guimenu>Preferences</guimenu> dialog by
    clicking <menuchoice><guimenu>Edit</guimenu><guimenu>Settings</guimenu>
    </menuchoice>. Select <menuchoice><guimenu>Privacy &amp; Security</guimenu>
    <guimenu>Certificates</guimenu><guimenu>View Certificates</guimenu>
    <guimenu>Your Certificates</guimenu><guimenu>Import</guimenu></menuchoice>
    and select the certificate to import. Only import certificates you
    absolutely trust!
   </para>
  </sect2>

  <sect2 xml:id="sec-firefox-security-passwords">
   <title>Password management</title>
   <para>
    Each time you enter a user name and a password on a Web site,
    <productname>Firefox</productname> offers to store this data. A pop-up at
    the top of the page opens, asking you whether you want
    <productname>Firefox</productname> to save the password. If you accept
    by clicking <guimenu>Save</guimenu>, the password will be stored on your
    hard disk in an encrypted format. The next time you access this site,
    <productname>Firefox</productname> will automatically fill in the login data.
   </para>
   <para>
    To review or manage your passwords, open the password manager by clicking
    <menuchoice> <guimenu>Edit</guimenu> <guimenu>Settings</guimenu>
    <guimenu>Privacy &amp; Security</guimenu> <guimenu>Logins and Passwords
    </guimenu> <guimenu>Saved Logins</guimenu></menuchoice> in the menu bar.
    The password manager opens with a list of sites and their corresponding
    user names. By default, the passwords are not
    displayed. You can click the eye icon to display them.
    To delete single or all entries from the list, click
    <guimenu>Remove</guimenu> or <guimenu>Remove All</guimenu>, respectively.
   </para>
   <para>
    To protect your passwords from unauthorized access, you can set a master
    password that is required when managing or adding passwords. In the menu
    bar, click <menuchoice><guimenu>Edit</guimenu>
    <guimenu>Settings</guimenu></menuchoice>, choose the category
    <guimenu>Privacy &amp; Security</guimenu> and activate
    <guimenu>Use a Primary Password</guimenu>.
   </para>
  </sect2>

  <sect2 xml:id="sec-firefox-security-private-browsing">
   <title>Private browsing</title>
   <para>
    By default, <productname>Firefox</productname> keeps track of your browsing
    history by storing content and links of visited Web sites, cookies,
    downloads, passwords, search terms, and formula data. Collecting and storing
    this data makes browsing faster and more convenient. However, when you use
    a public terminal or a friend's computer, for example, you could turn this
    behavior off. In Private Browsing mode, <productname>Firefox</productname>
    will not keep track of your browsing history nor will
    it cache the content of pages you have visited.
   </para>
   <para>
    To enable the Private Browsing mode, in the menu bar, click
    <menuchoice><guimenu>File</guimenu> <guimenu>New Private
    Window</guimenu></menuchoice>. The current Web site and all open tabs will
    be replaced by the Private Browsing information screen. As long as you will
    browse in private mode, the string <literal>Private Browsing</literal>
    will be displayed in the titlebar of the window.
   </para>
   <para>
    Disable Private Browsing by closing the private window.
   </para>
   <para>
    To make Private Browsing the default mode, open the
    <guimenu>Privacy &amp; Security</guimenu> tab in the
    <guimenu>Settings</guimenu> window as described in
    <xref linkend="sec-firefox-preferences"/>. Go to the
    <guimenu>History</guimenu> section. Choose <guimenu>Use custom settings for
    History</guimenu> from the drop-down menu and check the <guimenu>Always
     use private browsing mode</guimenu>.
   </para>
   <note>
    <title>Bookmarks and downloads</title>
    <para>
     Downloads and bookmarks you made during Private Browsing mode will be
     kept.
    </para>
   </note>
  </sect2>
 </sect1>
 <sect1 xml:id="sec-firefox-customizing">
  <title>Customizing <productname>Firefox</productname></title>

  <para>
   <productname>Firefox</productname> can be customized extensively.
  </para>

  <itemizedlist>
   <listitem>
    <para>
     Change the way <productname>Firefox</productname> behaves by altering the
     preferences.
    </para>
   </listitem>
   <listitem>
    <para>
     Add functionality by installing extensions.
    </para>
   </listitem>
   <listitem>
    <para>
     Change the look and feel by installing themes.
    </para>
   </listitem>
  </itemizedlist>

  <para>
   To manage extensions, themes and plug-ins, <productname>Firefox</productname>
   has an add-ons manager.
  </para>

  <sect2 xml:id="sec-firefox-preferences">
   <title>Preferences</title>
   <para>
    <productname>Firefox</productname> offers a wide range of configuration
    options. These are available by choosing <menuchoice> <guimenu>Edit</guimenu>
    <guimenu>Settings</guimenu> </menuchoice> in the menu bar. Each option
    is described in detail in the online help, which can be accessed by
    clicking the question mark icon in the dialog.
   </para>
   <figure xml:id="fig-firefox-preferences">
    <title>The preferences window</title>
    <mediaobject>
     <imageobject role="html">
      <imagedata fileref="firefox_preferences.png" width="90%"/>
     </imageobject>
     <imageobject role="fo">
      <imagedata fileref="firefox_preferences.png" width="100%"/>
     </imageobject>
    </mediaobject>
   </figure>
   <sect3 xml:id="sec-firefox-preferences-sessions">
    <title>Session management</title>
    <para>
     By default, <productname>Firefox</productname> automatically restores your
     session&mdash;windows and tabs&mdash;only after it has crashed, or after a
     restart because of an extension. However, it can be configured to restore
     a session every time it is started: open the Preferences dialog as
     described in
     <xref linkend="sec-firefox-preferences"/> and go to the category
     <guimenu>General</guimenu>. In the <guimenu>Startup</guimenu> section, set
     the option <guimenu>Restore previous session</guimenu>.
    </para>
    <para>
     When you have multiple windows open they will only be restored the next
     time when you close all of them at once with <menuchoice>
     <guimenu>File</guimenu> <guimenu>Quit</guimenu> </menuchoice> (from the
     menu bar) or with <keycombo> <keycap function="control"/>
     <keycap>Q</keycap> </keycombo>. If you close the windows one by one, only
     the last window will be restored.
    </para>
   </sect3>
   <sect3 xml:id="sec-firefox-preferences-language">
    <title>Language preferences for Web sites</title>
    <para>
     When sending a request to a Web server, the browser always sends the
     information about which language is preferred by the user. Web sites that
     are available in more than one language (and are configured to evaluate
     this language parameter) will display their pages in the language the
     browser requests. On &productname;, the preferred language is
     preconfigured to use the same language as the desktop. To change this
     setting, open the <guimenu>Preferences</guimenu> window as described in
     <xref linkend="sec-firefox-preferences"/>, go to the category
     <guimenu>General</guimenu> and choose your preferred language in the
     <guimenu>Language and Appearance</guimenu> section.
    </para>
   </sect3>
   <sect3 xml:id="sec-firefox-preferences-spelling">
    <title>Spell-checking</title>
    <para>
     By default, <productname>Firefox</productname> spell-checks what you type
     when typing into multiple-line text boxes. Misspelled words are underlined
     in red. To correct a word, right-click it and select the correct spelling
     from the context menu. You may also add the word to the dictionary, if it is
     correct.
    </para>
    <para>
     To change or add a dictionary, right-click anywhere in a multi-line text
     box and select the appropriate option from the context menu. Here you may
     also disable spell-checking for this text box. If you want to globally
     disable spell-checking, open the <guimenu>Preferences</guimenu> window as
     described in <xref linkend="sec-firefox-preferences"/> and go to the
     category <guimenu>General</guimenu>. Deactivate <guimenu>Check your
     spelling as you type</guimenu> in the <guimenu>Language and
      Appearance</guimenu> section.
    </para>
   </sect3>
  </sect2>

  <sect2 xml:id="sec-firefox-extensions">
   <title>Add-ons</title>
   <para>
    Extensions let you personalize <productname>Firefox</productname> to fit
    your needs. With extensions, you can change the look and feel of
    <guimenu>Firefox</guimenu>, enhance existing functionality, and add
    functions. For example, extensions can enhance the
    download manager, show the weather, or control Web music players. Other
    extensions assist Web developers or increase security by blocking content
    such as ads or scripts.
   </para>
   <para>
    There are thousands of extensions available for
    <productname>Firefox</productname>. With the add-ons
    manager, you can install, enable, disable, update, and remove extensions.
   </para>
   <para>
    If you do not like the standard look and feel of
    <productname>Firefox</productname>, install a new
    <emphasis>theme</emphasis>. Themes do not change the functionality, only
    the appearance of the browser.
   </para>
   <sect3 xml:id="sec-firefox-extensions-install">
    <title>Installing add-ons</title>
    <para>
     To add an extension or theme, start the add-ons manager with <menuchoice>
     <guimenu>Tools</guimenu> <guimenu>Add-Ons and Themes
     </guimenu> </menuchoice> from the menu bar. It opens the
     <guimenu>Add-ons Manager</guimenu> either displaying a choice of
     recommended add-ons or the results of your last search.
    </para>
    <para>
     Use the <guimenu>Find more add-ons</guimenu> field to search for specific
     add-ons. Click an entry in the list to view a short description. Install
     the add-on by clicking <guimenu>Add to Firefox</guimenu>.
    </para>
    <figure xml:id="fig-firefox-extensions">
     <title>Installing <productname>Firefox</productname> extensions</title>
     <mediaobject>
      <imageobject role="html">
       <imagedata fileref="firefox_addon_manager.png" width="80%"/>
      </imageobject>
      <imageobject role="fo">
       <imagedata fileref="firefox_addon_manager.png" width="100%"/>
      </imageobject>
     </mediaobject>
    </figure>
    <para>
     To activate freshly installed extensions or themes,
     <productname>Firefox</productname> sometimes
     needs to be restarted by clicking <guimenu>Restart now</guimenu> in the
     add-ons manager. Restart this way to make sure that your browsing session
     will be restored.
    </para>
   </sect3>
   <sect3 xml:id="sec-firefox-extensions-manage">
    <title>Managing add-ons</title>
    <para>
     The Add-ons Manager also offers a convenient interface to manage
     extensions, themes, and plug-ins. <guimenu>Extensions</guimenu> can be
     enabled, disabled or uninstalled. If an extension is configurable, its
     configuration options can be accessed via the
     <guimenu>Preferences</guimenu> button. In the
     <guimenu>Themes</guimenu> tab, you may <guimenu>Uninstall</guimenu> a
     theme, or activate a different theme by clicking
     <guimenu>Enable</guimenu>. Pending extension and theme installations are
     also listed. Select <guimenu>Cancel</guimenu> to stop the installation.
     Although you cannot install <guimenu>Plugins</guimenu> as a user, you may
     disable or enable them with the Add-ons Manager.
    </para>
    <para>
     Some add-ons require you to restart the browser when you uninstall or
     disable them. In such cases, after clicking either of these actions, a
     <guimenu>Restart now</guimenu> link appears in the add-ons manager.
    </para>
   </sect3>
  </sect2>

<!--  <sect2 id="sec-firefox-lockdown"
   os="sled">
   <title>Disabling features</title>
   <para>
    <remark condition="generic"> 2008-02-29 - fs: Fate #302023 and #300976 </remark>
    For special-use cases (for example when using &productname; as an
    Internet terminal) it is recommended to disable (lockdown) certain
    features, such as saving or printing a page, viewing the page source or
    disabling the cache. This can be achieved by using the GConf system. See
    <xref linkend="sec-gnome-gconf-lockdown"/> for detailed information.
   </para>
  </sect2>-->
 </sect1>
 <sect1 xml:id="sec-firefox-printing">
  <title>Printing from <productname>Firefox</productname></title>

  <para>
   Before you actually print a Web page, a print preview is shown. From the
   menu bar, choose <menuchoice> <guimenu>File</guimenu> <guimenu>Print</guimenu>
   </menuchoice> or press
   <keycombo><keycap function="control"/><keycap>P</keycap></keycombo>. The
   <guimenu>Printer</guimenu> dialog opens. Configure paper size and
   orientation per printer.
  </para>
 </sect1>
<!--
   We used to have section here that explained how to open/create MHTML files.
   However, the firefox-mhtml package is not maintained anymore. Should we still
   need this functionality, there is this add-on:
   https://addons.mozilla.org/en-GB/firefox/addon/mozilla-archive-format
   Relevant old FATE: FATE#302866. - sknorr, 2015-09-23
 -->
<!--taroth 2016-06-16: the following does no longer seem to be valid - could
      not find an #Adone Flash Player' entry in the main menu-->
<!--
  <sect1 xml:id="sec-firefox-flashplayer-props">
  <title>Controlling adobe flash player settings</title>

  <para>
   Adobe Flash Player is a software that extends browsers with the ability to
   display certain animations and applications.
  </para>
  <para>
   <remark condition="generic">taroth 2013-02-20 - Fate #313848</remark>
   You can control the settings of the Adobe Flash Player plug-in with a
   settings manager. To start the Adobe Flash Player Settings, choose
   <menuchoice><guimenu>Applications</guimenu><guimenu>Internet</guimenu>
   <guimenu>Adobe Flash Player</guimenu></menuchoice>.
   The dialog allows you to define your preferences with regard to
   <guimenu>Storage</guimenu>, <guimenu>Camera and Mic</guimenu>,
   <guimenu>Playback</guimenu> and to change some advanced settings. For
   more information, refer to
   <link xlink:href="http://www.macromedia.com/support/documentation/en/flashplayer/help/settings_manager.html"/>.
  </para>
  </sect1>-->
 <sect1 xml:id="sec-firefox-moreinfo">
  <title>More information</title>

  <para>
   To get more information about <productname>Firefox</productname>, see the
   following links:
  </para>

  <simplelist>
   <member><emphasis>Mozilla forums</emphasis>: <link xlink:href="https://www.mozilla.org/about/forums/"/>
   </member>
   <member><emphasis>Main Menu reference</emphasis>: <link xlink:href="http://support.mozilla.org/kb/Menu+reference"/>
   </member>
   <member><emphasis>Preferences reference</emphasis>: <link xlink:href="http://support.mozilla.org/kb/Options+window"/>
   </member>
   <member><emphasis>Key combinations</emphasis>: <link xlink:href="http://support.mozilla.org/kb/Keyboard+shortcuts"/>
   </member>
  </simplelist>
 </sect1>
</chapter>
