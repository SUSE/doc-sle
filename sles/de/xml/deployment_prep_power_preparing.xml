<?xml version="1.0" encoding="UTF-8"?>
<sect1 xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="deployment_prep_power_preparing.xml" version="5.0" xml:id="cha.power.prep">
 <title>Vorbereitung</title>

 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>Ja</dm:translation>
  </dm:docmanager>
 </info>

 <para>
  In diesem Abschnitt werden die Schritte beschrieben, die zur Vorbereitung vor der eigentlichen Installation von SUSE Linux Enterprise Server anfallen. Das Installationsverfahren richtet sich nach dem verwendeten System. Folgende Methoden werden unterstützt:
 </para>

 <itemizedlist>
  <listitem>
   <para>
    <xref linkend="sec.prep.powerlinux.powerkvm.kimchi" xrefstyle="select:title nopage"/>
   </para>
  </listitem>
  <listitem>
   <para>
    <xref linkend="sec.prep.powerlinux.powerkvm.virt-install" xrefstyle="select:title nopage"/>
   </para>
  </listitem>
  <listitem>
   <para>
    <xref linkend="sec.prep.powerlinux.powerkvm.ivm" xrefstyle="select:title nopage"/>
   </para>
  </listitem>
  <listitem>
   <para>
    <xref linkend="sec.prep.powerlinux.powerkvm.bare_metal" xrefstyle="select:title nopage"/>
   </para>
  </listitem>
 </itemizedlist>

 <para>
  Wenn SUSE Linux Enterprise Server auf mehreren Systemen oder Partitionen installiert werden soll, empfiehlt sich die Erstellung einer Netzwerkinstallationsquelle. Die Installationsquelle kann für die gleichzeitige Installation auf mehreren Partitionen oder Systemen verwendet werden. Informationen zur Konfiguration einer Netzwerkinstallationsquelle finden Sie in <xref linkend="sec.deployment.instserver.sles9"/>.
 </para>

 <sect2 version="5.0" xml:id="sec.prep.powerlinux.powerkvm.kimchi">
  <title>Installation auf IBM PowerKVM-Servern mithilfe von Kimchi</title>
  <para>
   Dieser Abschnitt befasst sich mit der Vorbereitung der Installation auf IBM PowerLinux-Systemen mit PowerKVM. Hier wird die Installation von einem ISO-Image über die Kimchi-Web-Schnittstelle erläutert. Kimchi ist ein Werkzeug für die Administration von IBM PowerKVM.
  </para>
  <para>
   In diesem Abschnitt wird vorausgesetzt, dass PowerKVM auf dem IBM PowerLinux-Server ausgeführt wird. Falls PowerKVM nicht vorinstalliert ist, befolgen Sie die Anweisungen zum Installieren und Einrichten von PowerKVM im Abschnitt <quote>Configuring IBM PowerKVM on Power Systems</quote> (Konfigurieren von IBM PowerKVM auf Power-Systemen) unter <link xlink:href="http://www.ibm.com/support/knowledgecenter/linuxonibm/liabp/liabpkickoff.htm"/>.
  </para>
  <sect3 xml:id="sec.prep.powerlinux.powerkvm.template">
   <title>Erstellen einer SUSE Linux Enterprise Server-Schablone mit Kimchi</title>
   <para>
    Schablonen fungieren als Installationsquelle für PowerKVM-Gäste. Sie können eine Schablone erstellen, eine bereits vorhandene Schablone bearbeiten oder eine Schablone klonen. Wenn Sie eine Schablone auf Basis eines bestehenden Gasts klonen möchten, muss der betreffende Gast deaktiviert werden.
   </para>
   <procedure>
    <title>Erstellen einer Schablone mit Kimchi</title>
    <step>
     <para>
      Geben Sie im Webbrowser die URL des PowerLinux-Servers ein, auf dem PowerKVM ausgeführt wird, beispielsweise <literal>https://<replaceable>POWERLINUX_IP</replaceable>:8001</literal> (ersetzen Sie <replaceable>POWERLINUX_IP</replaceable> durch die IP-Adresse Ihres Systems).
     </para>
    </step>
    <step>
     <para>
      Klicken Sie auf den Karteireiter <guimenu>Templates</guimenu> (Schablonen). Die Seite <guimenu>Templates</guimenu> (Schablonen) wird geöffnet.
     </para>
    </step>
    <step>
     <para>
      Zum Erstellen der SUSE Linux Enterprise Server-Schablone klicken Sie auf das grüne Pluszeichen (<guimenu>+</guimenu>).
     </para>
    </step>
    <step>
     <para>
      Wählen Sie im Dialogfeld <guimenu>Add Template</guimenu> (Schablone hinzufügen) eine der folgenden Optionen aus:
     </para>
     <variablelist>
      <varlistentry>
       <term><guimenu>Lokales ISO-Image</guimenu>
       </term>
       <listitem>
        <para>
         Wählen Sie diese Option aus, wenn Sie Speicherpools nach auf dem System verfügbaren ISO-Installationsimages durchsuchen möchten.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term><guimenu>Local Image File</guimenu> (Lokale Imagedatei)
       </term>
       <listitem>
        <para>
         Wählen Sie diese Option aus, wenn Sie einen Pfad zu einer lokalen Imagedatei angeben möchten.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term><guimenu>Remote ISO file</guimenu> (Remote-ISO-Datei)
       </term>
       <listitem>
        <para>
         Wählen Sie diese Option aus, wenn Sie einen entfernten Ort für ein ISO-Installationsimage angeben möchten.
        </para>
       </listitem>
      </varlistentry>
     </variablelist>
    </step>
    <step>
     <para>
      Wählen Sie die gewünschte ISO-Datei für die Erstellung eines Gasts aus und klicken Sie auf <guimenu>Create Templates from Selected ISO</guimenu> (Schablonen aus ausgewählter ISO erstellen).
     </para>
    </step>
    <step>
     <para>
      Zum Konfigurieren der soeben erstellten Schablone klicken Sie auf <menuchoice><guimenu>Actions (Aktionen)</guimenu>
      <guimenu>Edit (Bearbeiten)</guimenu></menuchoice>, und ändern Sie die Werte gemäß Ihren Anforderungen.
     </para>
     <informalfigure>
      <mediaobject>
       <imageobject role="fo">
        <imagedata fileref="kimchi_create_template.png" width="50%"/>
       </imageobject>
       <imageobject role="html">
        <imagedata fileref="kimchi_create_template.png" width="50%"/>
       </imageobject>
      </mediaobject>
     </informalfigure>
    </step>
   </procedure>
   <para>
    Weitere Informationen finden Sie im Abschnitt <quote>Setting up a template using Kimchi</quote> (Einrichten einer Schablone mit Kimchi) unter <link xlink:href="http://www.ibm.com/support/knowledgecenter/linuxonibm/liabp/liabpkimchitemplate.htm"/>.
   </para>
  </sect3>
  <sect3 xml:id="sec.prep.powerlinux.powerkvm.install">
   <title>Installieren von SUSE Linux Enterprise Server als Gast mit Kimchi</title>
   <para/>
   <procedure>
    <step>
     <para>
      Geben Sie im Webbrowser die URL des PowerLinux-Servers ein, auf dem PowerKVM ausgeführt wird, beispielsweise <literal>https://<replaceable>POWERLINUX_IP</replaceable>:8001</literal> (ersetzen Sie <replaceable>POWERLINUX_IP</replaceable> durch die IP-Adresse Ihres Systems).
     </para>
    </step>
    <step>
     <para>
      Klicken Sie auf den Karteireiter <guimenu>Guests</guimenu> (Gäste). Die Seite <guimenu>Guests</guimenu> (Gäste) wird geöffnet.
     </para>
    </step>
    <step>
     <para>
      Zum Erstellen des SUSE Linux Enterprise Server-Gastes klicken Sie auf das grüne Pluszeichen (<guimenu>+</guimenu>).
     </para>
    </step>
    <step>
     <para>
      Geben Sie unter <guimenu>Virtual Machine Name</guimenu> (Name des virtuellen Computers) einen Namen für den SUSE Linux Enterprise Server-Gast ein.
     </para>
     <para>
      Wählen Sie die SUSE Linux Enterprise Server-Schablone aus, die Sie in <xref linkend="sec.prep.powerlinux.powerkvm.template"/> erstellt haben, und klicken Sie auf <guimenu>Erstellen</guimenu>.
     </para>
     <informalfigure>
      <mediaobject>
       <imageobject role="fo">
        <imagedata fileref="kimchi_create_guest.png" width="70%"/>
       </imageobject>
       <imageobject role="html">
        <imagedata fileref="kimchi_create_guest.png" width="70%"/>
       </imageobject>
      </mediaobject>
     </informalfigure>
    </step>
    <step>
     <para>
      Nach seiner Erstellung kann der Gast gestartet werden. Zum Starten des SUSE Linux Enterprise Server-Gastes klicken Sie auf den roten Ein-/Aus-Schalter. Sie können jedoch auch <menuchoice><guimenu>Actions</guimenu>
      <guimenu>Start</guimenu></menuchoice> (Aktionen &gt; Starten) auswählen.
     </para>
    </step>
    <step>
     <para>
      Klicken Sie auf <menuchoice><guimenu>Actions (Aktionen)</guimenu>
      <guimenu>Connect (Verbinden)</guimenu></menuchoice>, und verbinden Sie den VNC-Viewer mit dem Installationsprogramm (siehe <xref linkend="sec.deployment.remoteinst.monitor.vnc.connect"/>).
     </para>
    </step>
   </procedure>
   <tip>
    <title>Erstellen mehrerer Gäste</title>
    <para>
     Wenn Sie mehrere Gäste eines ähnlichen Typs erstellen möchten, wählen Sie <guimenu>Clone</guimenu> (Klonen) im Menü <guimenu>Actions</guimenu> (Aktionen) eines bestehenden Gasts aus.
    </para>
   </tip>
   <para>
    Nun können Sie die Standardinstallation über VNC wie in <xref linkend="sec.i.yast2.steps"/> beschrieben fortsetzen.
   </para>
  </sect3>
 </sect2>

 <sect2 xml:id="sec.prep.powerlinux.powerkvm.virt-install">
  <title>
   Installation auf IBM PowerKVM-Servern mit <command>virt-install</command>
  </title>
  <para>
   Alternativ zu einer Installation mit Kimchi können Sie auch das Kommandozeilenwerkzeug <command>virt-install</command> für eine Installation auf Servern mit IBM PowerKVM verwenden. Dies ist vor allem dann sinnvoll, wenn Sie mehrere virtuelle Maschinen auf IBM PowerLinux Server-Systemen installieren müssen. <command>virt-install</command> unterstützt zahlreiche Installationsszenarien. Im Folgenden wird die Installation im Fernverfahren mit VNC und PXE-Boot erläutert. Weitere Informationen zu <command>virt-install</command> finden Sie in <xref linkend="sec.libvirt.inst.virt-install"/>.
  </para>
  <para>
   Bereiten Sie ein Repository mit den Installationsquellen und einem PXE-Boot-fähigen Zielsystem vor (siehe <xref linkend="sec.deployment.remoteinst.scenario.vncpxe"/>).
  </para>
  <para>
   Geben Sie Folgendes in die Kommandozeile ein (passen Sie dabei die Option gemäß Ihren Anforderungen und der Hardware an):
  </para>

<screen>virt-install --name server_sle12 --memory 4096 --vcpus=2 --pxe \
--graphics vnc --os-variant sles11 \
--disk pool=default,size=3000,format=qcow2,allocation=1G,bus=virtio \
-w mac=<replaceable>mac_address</replaceable>,model=spapr-vlan</screen>
  <para>
   Hierbei wird die grafische VNC-Oberfläche verwendet, und der grafische Client wird automatisch gestartet. Führen Sie die Installation wie in <xref linkend="sec.i.yast2.steps"/> beschrieben aus.
  </para>
 </sect2>

 <sect2 xml:id="sec.prep.powerlinux.powerkvm.ivm">
  <title>
   Installation in einer Partition mit IVM
  </title>
  <para>
   Diese Anleitung enthält hilfreiche Informationen für die Installation von <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> in einer Power Systems-Serverpartition mit der Weboberfläche Integrated Virtualization Manager (IVM). Vor Installationsbeginn müssen folgende Voraussetzungen erfüllt sein:
  </para>
  <itemizedlist>
   <listitem>
    <para>
     Das Linux on Power-System wurde eingeschaltet.
    </para>
   </listitem>
   <listitem>
    <para>
     Der virtuelle E/A-Server (Virtual I/O Server, VIOS) wurde installiert.
    </para>
   </listitem>
   <listitem>
    <para>
     Die IVM-Erstkonfiguration wurde durchgeführt.
    </para>
   </listitem>
  </itemizedlist>
  <procedure>
   <title>An der IVM-Weboberfläche anmelden</title>
   <step>
    <para>
     Öffnen Sie ein Webbrowserfenster und stellen Sie mit dem HTTP- oder HTTPS-Protokoll eine Verbindung zu der IP-Adresse her, die dem IVM während des Installationsprozesses zugewiesen wurde (zum Beispiel https://<replaceable>IP-ADRESSE</replaceable>). Das Begrüßungsfenster wird angezeigt.
    </para>
   </step>
   <step>
    <para>
     Melden Sie sich als Benutzer <systemitem class="username">padmin</systemitem> an und geben Sie das Passwort ein, das Sie während des Installationsprozesses definiert haben. Die IVM-Oberfläche wird angezeigt.
    </para>
   </step>
   <step>
    <para>
     Wählen Sie <guimenu>View/Modify Virtual Ethernet</guimenu> (Virtuelles Ethernet anzeigen/ändern) aus.
    </para>
   </step>
   <step>
    <para>
     Klicken Sie auf <guimenu>Initialize Virtual Ethernet</guimenu> (Virtuelles Ethernet initialisieren), um eine Ethernet-Konnektivität zwischen den Partitionen bereitzustellen.
    </para>
   </step>
   <step>
    <para>
     Sobald das virtuelle Ethernet initialisiert wurde, klicken Sie auf „Apply“ (Anwenden).
    </para>
   </step>
   <step>
    <para>
     Wenn Ihre Installation externes Networking erfordert, erstellen Sie eine virtuelle Ethernet-Bridge:
    </para>
    <substeps>
     <step>
      <para>
       Wählen Sie den Karteireiter <guimenu>Virtual Ethernet Bridge</guimenu> (Virtuelle Ethernet-Bridge) aus.
      </para>
     </step>
     <step>
      <para>
       Wählen Sie den physischen Adapter für die Bridge aus und fahren Sie mit der Option <guimenu>Apply</guimenu> (Anwenden) fort.
      </para>
     </step>
    </substeps>
   </step>
  </procedure>
  <para>Erstellen Sie jetzt mit folgenden Schritten eine Partition:</para>
  <procedure>
   <title>Partition erstellen</title>
   <step>
    <para>
     Klicken Sie in der IVM-Weboberfläche auf <menuchoice><guimenu>View/Modify Partition</guimenu> <guimenu>Create Partition</guimenu></menuchoice> (Partition änzeigen/ändern &gt; Partition erstellen).
    </para>
   </step>
   <step>
    <para>
     Geben Sie einen Namen für die Partition ein. Um mit dem nächsten Schritt fortzufahren, klicken Sie in diesem und in den folgenden Schritten jeweils auf <guimenu>Next</guimenu> (Weiter).
    </para>
   </step>
   <step>
    <para>
     Geben Sie den Arbeitsspeicher für Ihre Partition an. Falls Sie einen gemeinsam genutzten Arbeitsspeicherpool erstellt haben, können Ihre Partitionen Arbeitsspeicher gemeinsam nutzen. Wählen Sie andernfalls <guimenu>Dedicated</guimenu> (Dediziert) aus.
    </para>
   </step>
   <step>
    <para>
     Geben Sie die Anzahl der Prozessoren und den Verarbeitungsmodus für die Partition an.
    </para>
   </step>
   <step>
    <para>
     Geben Sie ein virtuelles Ethernet für die Partition an. Wenn Sie keinen Adapter konfigurieren möchten, wählen Sie <guimenu>none</guimenu> (Keiner) für das virtuelle Ethernet aus.
    </para>
   </step>
   <step>
    <para>
     Erstellen Sie einen neuen virtuellen Datenträger oder weisen Sie bereits vorhandene virtuelle Datenträger und physische Volumes zu, die zurzeit keiner Partition zugewiesen sind.
    </para>
   </step>
   <step>
    <para>
     Überprüfen Sie die Angaben in <guimenu>Virtual disk name</guimenu> (Name des virtuellen Datenträgers) und <guimenu>Storage pool name</guimenu> (Name des Speicherpools) für Ihre Platte und geben Sie in <guimenu>Virtual disk size</guimenu> (Größe des virtuellen Datenträgers) die gewünschte Größe an.
    </para>
   </step>
   <step>
    <para>
     Konfigurieren Sie optische Einheiten für Ihre Partition, indem Sie die Einträge <guimenu>Physical Optical Devices</guimenu> (Physische optische Einheiten) und <guimenu>Virtual Optical Devices</guimenu> (Virtuelle optische Einheiten) einblenden und die Einheit(en) auswählen, die Sie der Partition zuweisen möchten.
    </para>
   </step>
   <step>
    <para>
     Überprüfen Sie die Konfigurationseinstellungen für Ihre Partition und klicken Sie auf <guimenu>Finish</guimenu> (Beenden). Die Partition wird erstellt und ist danach in der Liste <guimenu>View/Modify Partitions</guimenu> (Partitionen anzeigen/ändern) verfügbar.
    </para>
   </step>
  </procedure>
  <para>Aktivieren Sie nun die soeben erstellte Partition:</para>
  <procedure>
   <title>Partition aktivieren</title>
   <step>
    <para>
     Klicken Sie in der IVM-Weboberfläche auf <guimenu>View/Modify Partition</guimenu> (Partition anzeigen/ändern) und wählen Sie das Feld neben der Partition aus, die Sie aktivieren möchten.
    </para>
   </step>
   <step>
    <para>
     Wählen Sie <guimenu>More Tasks</guimenu> (Weitere Aufgaben) aus.
    </para>
   </step>
   <step>
    <para>
     Wählen Sie <guimenu>Open a terminal window</guimenu> (Terminalfenster öffnen) aus.
    </para>
   </step>
   <step>
    <para>
     Klicken Sie neben der Partition auf <guimenu>Activate</guimenu> (Aktivieren).
    </para>
   </step>
   <step>
    <para>
     Geben Sie im Terminalfenster <keycap>1</keycap> ein, um die System Management Services (SMS) zu starten.
    </para>
   </step>
  </procedure>
  <para>
   Der Rechner ist jetzt eingerichtet und Sie können in die Installation booten:
  </para>
  <procedure>
   <title>Linux-Installation booten</title>
   <step>
    <para>
     Geben Sie im Fenster <guimenu>Boot selection</guimenu> (Bootauswahl) <keycap>1</keycap> ein, um das <guimenu>SMS-Menü</guimenu> auszuwählen. Geben Sie <keycap>1</keycap> ein, bevor der Bildschirm für das Booten der Firmware vollständig in der Anzeige zu sehen ist, da der Bildschirm ausgeblendet wird, sobald er vollständig ist. Falls Sie den Bildschirm versäumt haben, booten Sie das System neu.
    </para>
   </step>
   <step>
    <para>
     Sie können jetzt den Mediendatenträger für den virtuellen E/A-Server (Virtual I/O Server, VIOS) in das Laufwerk einlegen.
    </para>
   </step>
   <step>
    <para>
     Geben Sie <keycap>2</keycap> ein, um im Menü <guimenu>Language selection</guimenu> (Sprachauswahl) mit der Eingabe des Passworts fortzufahren. Geben Sie das Passwort für die Admin-ID ein.
    </para>
   </step>
   <step>
    <para>
     Geben Sie im SMS-Hauptmenü <keycap>5</keycap> ein, um <guimenu>Select Boot Options</guimenu> (Bootoptionen auswählen) zu wählen.
    </para>
   </step>
   <step>
    <para>
     Geben Sie <keycap>1</keycap> ein, um <guimenu>Install/Boot Device</guimenu> (Installations-/Bootgerät) auszuwählen.
    </para>
   </step>
   <step>
    <para>
     Geben Sie <keycap>7</keycap> ein, um alle verfügbaren Bootgeräte anzuzeigen.
    </para>
   </step>
   <step>
    <para>
     Geben Sie die Nummer des gewünschten Geräts ein. Falls Ihr Gerät nicht aufgelistet wird, geben Sie <keycap>N</keycap> ein, damit weitere Geräte angezeigt werden.
    </para>
   </step>
   <step>
    <para>
     Geben Sie <keycap>2</keycap> ein, damit das Booten mit der Option <guimenu>Normal Mode Boot</guimenu> (Booten im normalen Modus) erfolgt.
    </para>
   </step>
   <step>
    <para>
     Geben Sie <keycap>1</keycap>, um das SMS-Menü zu verlassen und den Bootvorgang zu starten.
    </para>
   </step>
   <step>
    <para>
     Geben Sie am Bootprompt des Installationsprogramms Folgendes ein:
    </para>
    <screen>install vnc=1
vncpassword=<replaceable>VNC_PASSWORD</replaceable></screen>
    <para>
     Ersetzen Sie <replaceable>VNC_PASSWORD</replaceable> durch ein Passwort Ihrer Wahl (die Mindestlänge beträgt acht Zeichen) und drücken Sie <keycap function="enter"/>, um die Installation von <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> zu starten. Das Laden des Kernels beginnt.
    </para>
   </step>
  </procedure>
  <para>
   Sobald das Laden des Kernels begonnen hat, benötigt das Installationsprogramm einige Informationen vom System, um eine VNC-Sitzung einrichten zu können. Damit VNC genutzt werden kann, wird ein gültiger TCP/IP-Stack benötigt. Verwenden Sie entweder DHCP oder definieren Sie Ihre Informationen zum Networking manuell mithilfe der Anweisungen des Installationsprogramms.
  </para>
  <procedure>
   <title>VNC-Sitzung starten</title>
   <step>
    <para>
     Wählen Sie im Fenster <guimenu>Network device</guimenu> (Netzwerkgerät) <guimenu>eth0</guimenu> als Netzwerkgerät aus. Wählen Sie <guimenu>OK</guimenu> aus und drücken Sie <keycap function="enter"/>.
    </para>
   </step>
   <step>
    <para>
     Testen Sie die Installationsmedien. Wenn Sie den Vorgang ohne Test fortsetzen möchten, wählen Sie <guimenu>Skip</guimenu> (Überspringen) aus.
    </para>
   </step>
   <step>
    <para>
     Sobald das System den VNC-Server gestartet hat, wird eine Nachricht bezüglich der Verbindung Ihres VNC-Clients gefolgt von einer IP-Adresse angezeigt. Notieren Sie sich diese IP-Adresse.
    </para>
   </step>
   <step>
    <para>
     Starten Sie einen VNC-Client auf Ihrem Notebook oder PC. Geben Sie die IP-Adresse aus dem vorherigen Schritt und danach <literal>:1</literal> ein. Beispiel: 192.168.2.103:1.
    </para>
   </step>
   <step>
    <para>
     Führen Sie die Installation wie in <xref linkend="sec.i.yast2.steps"/> beschrieben aus.
    </para>
   </step>
  </procedure>
 </sect2>
 <sect2 xml:id="sec.prep.powerlinux.powerkvm.bare_metal">
  <title>
   Installieren auf Servern ohne Open Power Abstraction Layer
  </title>
  <para>
   Mithilfe der folgenden Informationen können Sie Linux über eine serielle Konsole oder mit einem Monitor und einer Tastatur auf einem Power Systems-Server installieren. Bei dieser Installation wird ein nicht verwaltetes (eigenständiges) System vorausgesetzt, das umgehend gebootet werden kann.
  </para>
  <procedure>
   <step>
    <para>
     Schalten Sie Ihr System ein, indem Sie <guimenu>Power On</guimenu> (Einschalten) im Menü <guimenu>Power On/Off System</guimenu> (System ein-/ausschalten) auswählen. Wenn Sie gefragt werden, ob Sie mit der Konsole fortfahren möchten, geben Sie <keycap>0</keycap> ein. In diesem Fall wird die Konsole genutzt.
    </para>
   </step>
   <step>
    <para>
     Legen Sie das Installationsmedium von <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> in das Laufwerk ein.
    </para>
   </step>
   <step>
    <para>
     Geben Sie im Fenster <guimenu>Select Language</guimenu> (Sprache auswählen) <keycap>2</keycap> ein, um mit dem Bootvorgang fortzufahren.
    </para>
   </step>
   <step>
    <para>
     Geben Sie <keycap>1</keycap> ein, um die Lizenzvereinbarung zu akzeptieren.
    </para>
   </step>   <step>
    <para>
     Geben Sie im Fenster <guimenu>Boot selection</guimenu> (Bootauswahl) <keycap>1</keycap> ein, um das <guimenu>SMS-Menü</guimenu> auszuwählen. Geben Sie <keycap>1</keycap> ein, bevor der Bildschirm für das Booten der Firmware vollständig in der Anzeige zu sehen ist, da der Bildschirm ausgeblendet wird, sobald er vollständig ist. Falls Sie den Bildschirm versäumt haben, booten Sie das System neu.
    </para>
   </step>
   <step>
    <para>
     Geben Sie <keycap>2</keycap> ein, um im Menü <guimenu>Language selection</guimenu> (Sprachauswahl) mit der Eingabe des Passworts fortzufahren. Geben Sie das Passwort für die Admin-ID ein.
    </para>
   </step>
   <step>
    <para>
     Geben Sie im SMS-Hauptmenü <keycap>5</keycap> ein, um <guimenu>Select Boot Options</guimenu> (Bootoptionen auswählen) zu wählen.
    </para>
   </step>
   <step>
    <para>
     Geben Sie <keycap>7</keycap> ein, um alle verfügbaren Bootgeräte anzuzeigen.
    </para>
   </step>
   <step>
    <para>
     Geben Sie die Nummer des gewünschten Geräts ein. Falls Ihr Gerät nicht aufgelistet wird, geben Sie <keycap>N</keycap> ein, damit weitere Geräte angezeigt werden.
    </para>
   </step>
   <step>
    <para>
     Geben Sie <keycap>2</keycap> ein, damit das Booten mit der Option <guimenu>Normal Mode Boot</guimenu> (Booten im normalen Modus) erfolgt.
    </para>
   </step>   <step>
    <para>
     Geben Sie <keycap>1</keycap>, um das SMS-Menü zu verlassen und den Bootvorgang zu starten.
    </para>
   </step>
   <step>
    <para>
     Geben Sie am Bootprompt des Installationsprogramms Folgendes ein:
    </para>
    <screen>install vnc=1
vncpassword=<replaceable>VNC_PASSWORD</replaceable></screen>
    <para>
     Ersetzen Sie <replaceable>VNC_PASSWORD</replaceable> durch ein Passwort Ihrer Wahl (die Mindestlänge beträgt acht Zeichen) und drücken Sie <keycap function="enter"/>, um die Installation von <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> zu starten. Das Laden des Kernels beginnt.
    </para>
   </step>
  </procedure>

  <para>
   Sobald das Laden des Kernels begonnen hat, benötigt das Installationsprogramm einige Informationen vom System, um eine VNC-Sitzung einrichten zu können. Damit VNC genutzt werden kann, wird ein gültiger TCP/IP-Stack benötigt. Verwenden Sie entweder DHCP oder definieren Sie Ihre Informationen zum Networking manuell mithilfe der Anweisungen des Installationsprogramms.
  </para>
  <procedure>
   <title>VNC-Sitzung starten</title>
   <step>
    <para>
     Wählen Sie im Fenster <guimenu>Network device</guimenu> (Netzwerkgerät) <guimenu>eth0</guimenu> als Netzwerkgerät aus. Wählen Sie <guimenu>OK</guimenu> aus und drücken Sie <keycap function="enter"/>.
    </para>
   </step>
   <step>
    <para>
     Testen Sie die Installationsmedien. Wenn Sie den Vorgang ohne Test fortsetzen möchten, wählen Sie <guimenu>Skip</guimenu> (Überspringen) aus.
    </para>
   </step>
   <step>
    <para>
     Sobald das System den VNC-Server gestartet hat, wird eine Nachricht bezüglich der Verbindung Ihres VNC-Clients gefolgt von einer IP-Adresse angezeigt. Notieren Sie sich diese IP-Adresse.
    </para>
   </step>
   <step>
    <para>
     Starten Sie einen VNC-Client auf Ihrem Notebook oder PC. Geben Sie die IP-Adresse aus dem vorherigen Schritt und danach <literal>:1</literal> ein. Beispiel: 192.168.2.103:1.
    </para>
   </step>
   <step>
    <para>
     Führen Sie die Installation wie in <xref linkend="sec.i.yast2.steps"/> beschrieben aus.
    </para>
   </step>
  </procedure>
 </sect2>

</sect1>
