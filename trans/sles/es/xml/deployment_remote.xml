<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter PUBLIC "-//Novell//DTD NovDoc XML V1.0//EN" "novdocx.dtd">
<!--
*********************************
Please see LICENSE.txt for this document's license.
*********************************
-->
<chapter xml:base="deployment_remote.xml" role="General" id="cha.deployment.remoteinst">
 <title>Instalación remota</title>
 <para>
  <phrase role="productname"><phrase os="sles">SUSE® Linux Enterprise Server</phrase></phrase> se puede instalar de varias formas. Además de la instalación habitual a partir de un medio, descrita en el <xref linkend="cha.inst"/>, es posible seleccionar diversos métodos basados en red, o incluso utilizar un método sin intervención física alguna para instalar <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>.
 </para>
 <para>
  Cada método se presenta mediante dos breves listas de verificación: una que muestra los requisitos previos necesarios para el método y otra que describe el procedimiento básico. A continuación se incluyen más detalles de las técnicas utilizadas en cada situación de instalación.
 </para>
 <note>
  <para>
   En las próximas secciones, el sistema que almacenará la nueva instalación de <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> se menciona como <emphasis>sistema de destino</emphasis> o <emphasis>destino de la instalación</emphasis>. El término <emphasis>repositorio</emphasis> (anteriormente denominado <quote>origen de instalación</quote> se utiliza para todos los orígenes de datos de instalación. Esto incluye medios físicos, tales como CD y DVD, y los servidores de red que distribuyan los datos de instalación en la red.
  </para>
 </note>
 <sect1 id="sec.deployment.remoteinst.scenario">
  <title>Situaciones de instalación para la instalación remota</title>

  <para>
   En esta sección se describen las situaciones de instalación más habituales para la instalación remota. Para cada situación, compruebe detenidamente los requisitos previos y siga el procedimiento indicado. Si necesita instrucciones detalladas para un paso concreto, siga los enlaces que aparecen en cada uno de ellos.
  </para>

  <sect2 id="sec.deployment.remoteinst.scenario.vncstat">
   <title>Instalación remota sencilla mediante VNC: configuración de red estática</title>
   <para>
    Para este tipo de instalación, es necesario un cierto grado de acceso físico al sistema de destino con el fin de arrancarlo para la instalación. Una estación de trabajo remota controla completamente la instalación propiamente dicha y usa VNC para conectarse al programa de instalación. Es necesaria la misma intervención del usuario que en la instalación manual descrita en el <xref linkend="cha.inst"/>.
   </para>
   <para>
    Para este tipo de instalación, asegúrese de que se cumplen los siguientes requisitos:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Repositorio remoto: NFS, HTTP, FTP o SMB con una conexión de red en funcionamiento.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema de destino con una conexión de red en funcionamiento.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema de control con una conexión de red en funcionamiento y con software de visor VNC o un navegador compatible con Java (Firefox, Chromium, Internet Explorer, Opera, etc.).
     </para>
    </listitem>
    <listitem>
     <para>
      Medio de arranque físico (CD, DVD o memoria USB) para arrancar el sistema de destino.
     </para>
    </listitem>
    <listitem>
     <para>
      Direcciones IP estáticas válidas ya asignadas al repositorio y al sistema de control.
     </para>
    </listitem>
    <listitem>
     <para>
      Dirección IP estática válida para asignar al sistema de destino.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.vncstat">
    <para>
     Para realizar este tipo de instalación, siga estos pasos:
    </para>
    <step performance="required">
     <para>
      Configure el repositorio tal y como se describe en la <xref linkend="sec.deployment.remoteinst.instserver"/>. Escoja un servidor de red NFS, HTTP o FTP. Para un repositorio SMB, consulte la <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Arranque el sistema de destino con el DVD1 del kit de medios de <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>.
     </para>
    </step>
    <step performance="required">
     <para>
      Cuando aparezca la pantalla de arranque del sistema de destino, utilice el menú de opciones de arranque para establecer las opciones de VNC apropiadas y la dirección del repositorio. Esto se describe con más detalle en la <xref linkend="sec.deployment.remoteinst.bootinst"/>.
     </para>
     <para>
      El sistema de destino inicia un entorno basado en texto y ofrece la dirección de red y el número de pantalla mediante los cuales las aplicaciones para la visualización de VNC o navegadores pueden dirigirse al entorno de instalación gráfica. Las instalaciones de VNC se anuncian a sí mismas mediante OpenSLP si la configuración del cortafuegos lo permite. Se pueden localizar mediante <command>slptool</command>, como se describe en <xref linkend="proc.deployment.remoteinst.monitor.vnc.prep"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      En la estación de trabajo de control, abra una aplicación de visualización VNC o un navegador Web y conéctese al sistema de destino como se describe en la <xref linkend="sec.deployment.remoteinst.monitor.vnc"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Realice la instalación como se describe en el <xref linkend="cha.inst"/>. Es necesario volver a conectarse al sistema de destino después de reiniciarlo para la parte final de la instalación.
     </para>
    </step>
    <step performance="required">
     <para>
      Complete la instalación.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.vncdyn">
   <title>Instalación remota sencilla mediante VNC: configuración de red dinámica</title>
   <para>
    Para este tipo de instalación, es necesario un cierto grado de acceso físico al sistema de destino con el fin de arrancarlo para la instalación. La configuración de la red se realiza mediante DHCP. Una estación de trabajo remota controla completamente la instalación propiamente dicha y usa VNC para conectarse al programa de instalación, pero es necesaria la intervención del usuario para realizar la configuración.
   </para>
   <para>
    Para este tipo de instalación, asegúrese de que se cumplen los siguientes requisitos:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Repositorio remoto: NFS, HTTP, FTP o SMB con una conexión de red en funcionamiento.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema de destino con una conexión de red en funcionamiento.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema de control con una conexión de red en funcionamiento y con software de visor de VNC o un navegador compatible con Java (Firefox, Chromium, Internet Explorer u Opera).
     </para>
    </listitem>
    <listitem>
     <para>
      Arranque el sistema de destino con el DVD1 del kit de medios de <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>.
     </para>
    </listitem>
    <listitem>
     <para>
      Servidor DHCP en funcionamiento que suministre las direcciones IP.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.vncdyn">
    <para>
     Para realizar este tipo de instalación, siga estos pasos:
    </para>
    <step performance="required">
     <para>
      Configure el repositorio tal y como se describe en la <xref linkend="sec.deployment.remoteinst.instserver"/>. Escoja un servidor de red NFS, HTTP o FTP. Para un repositorio SMB, consulte la <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Arranque el sistema de destino con el DVD1 del kit de medios de <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>.
     </para>
    </step>
    <step performance="required">
     <para>
      Cuando aparezca la pantalla de arranque del sistema de destino, utilice el menú de opciones de arranque para establecer las opciones de VNC apropiadas y la dirección del repositorio. Esto se describe con más detalle en la <xref linkend="sec.deployment.remoteinst.bootinst"/>.
     </para>
     <para>
      El sistema de destino inicia un entorno basado en texto y ofrece la dirección de red y el número de pantalla mediante los cuales las aplicaciones para la visualización de VNC o navegadores pueden dirigirse al entorno de instalación gráfica. Las instalaciones de VNC se anuncian a sí mismas mediante OpenSLP si la configuración del cortafuegos lo permite. Se pueden localizar mediante <command>slptool</command>, como se describe en <xref linkend="proc.deployment.remoteinst.monitor.vnc.prep"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      En la estación de trabajo de control, abra una aplicación de visualización VNC o un navegador Web y conéctese al sistema de destino como se describe en la <xref linkend="sec.deployment.remoteinst.monitor.vnc"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Realice la instalación como se describe en el <xref linkend="cha.inst"/>. Es necesario volver a conectarse al sistema de destino después de reiniciarlo para la parte final de la instalación.
     </para>
    </step>
    <step performance="required">
     <para>
      Complete la instalación.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.vncpxe">
   <title>Instalación remota mediante VNC: arranque en PXE y Wake on LAN</title>
   <para>
    Este tipo de instalación no requiere intervención física alguna. La máquina de destino se inicia y arranca de manera remota. Solo es necesaria la intervención del usuario para la instalación propiamente dicha. Este método es adecuado para instalaciones en múltiples ubicaciones.
   </para>
   <para>
    Para este tipo de instalación, asegúrese de que se cumplen los siguientes requisitos:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Repositorio remoto: NFS, HTTP, FTP o SMB con una conexión de red en funcionamiento.
     </para>
    </listitem>
    <listitem>
     <para>
      Servidor TFTP.
     </para>
    </listitem>
    <listitem>
     <para>
      Servidor DHCP en funcionamiento para su red.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema de destino compatible con arranque en PXE, funcionamiento en red y Wake on LAN, enchufado y conectado a la red.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema de control con una conexión de red en funcionamiento y con software de visor de VNC o un navegador compatible con Java (Firefox, Chromium, Internet Explorer u Opera).
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.vncpxe">
    <para>
     Para realizar este tipo de instalación, siga los pasos siguientes:
    </para>
    <step performance="required">
     <para>
      Configure el repositorio tal y como se describe en la <xref linkend="sec.deployment.remoteinst.instserver"/>. Escoja un servidor de red NFS, HTTP o FTP o configure un repositorio SMB como se describe en la <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Configure un servidor TFTP para que almacene una imagen de arranque que pueda ser utilizada por el sistema de destino. Este aspecto se describe en la <xref linkend="sec.deployment.remoteinst.boot.tftp"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Configure un servidor DHCP para que suministre direcciones IP a todas las máquinas e indique la ubicación del servidor TFTP al sistema de destino. Este aspecto se describe en la <xref linkend="sec.deployment.remoteinst.boot.dhcp"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Prepare el sistema de destino para arranque en PXE. Esto se describe con más detalle en la <xref linkend="sec.deployment.remoteinst.boot.pxeprep"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Comience el proceso de arranque del sistema de destino mediante Wake on LAN. Este aspecto se describe en la <xref linkend="sec.deployment.remoteinst.boot.wol"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      En la estación de trabajo de control, abra una aplicación de visualización VNC o un navegador Web y conéctese al sistema de destino como se describe en la <xref linkend="sec.deployment.remoteinst.monitor.vnc"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Realice la instalación como se describe en el <xref linkend="cha.inst"/>. Es necesario volver a conectarse al sistema de destino después de reiniciarlo para la parte final de la instalación.
     </para>
    </step>
    <step performance="required">
     <para>
      Complete la instalación.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.sshstat">
   <title>Instalación remota sencilla mediante SSH: configuración de red estática</title>
   <para>
    Para este tipo de instalación es necesario un cierto grado de acceso físico al sistema de destino con el fin de arrancarlo para la instalación y de determinar la dirección IP del destino de la instalación. Una estación de trabajo remota controla completamente la instalación propiamente dicha y usa SSH para conectarse al programa de instalación. Es necesaria la misma intervención del usuario que en la instalación manual descrita en el <xref linkend="cha.inst"/>.
   </para>
   <para>
    Para este tipo de instalación, asegúrese de que se cumplen los siguientes requisitos:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Repositorio remoto: NFS, HTTP, FTP o SMB con una conexión de red en funcionamiento.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema de destino con una conexión de red en funcionamiento.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema de control con una conexión de red y software cliente para SSH en funcionamiento
     </para>
    </listitem>
    <listitem>
     <para>
      Arranque el sistema de destino con el DVD1 del kit de medios de <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>.
     </para>
    </listitem>
    <listitem>
     <para>
      Direcciones IP estáticas válidas ya asignadas al repositorio y al sistema de control.
     </para>
    </listitem>
    <listitem>
     <para>
      Dirección IP estática válida para asignar al sistema de destino.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.sshstat">
    <para>
     Para realizar este tipo de instalación, siga estos pasos:
    </para>
    <step performance="required">
     <para>
      Configure el repositorio tal y como se describe en la <xref linkend="sec.deployment.remoteinst.instserver"/>. Escoja un servidor de red NFS, HTTP o FTP. Para un repositorio SMB, consulte la <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Arranque el sistema de destino con el DVD1 del kit de medios de <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>.
     </para>
    </step>
    <step performance="required">
     <para>
      Cuando aparezca la pantalla de arranque del sistema de destino, utilice el menú de opciones de arranque para establecer los parámetros apropiados de la conexión de red, la dirección del repositorio y la habilitación de SSH. Esto se describe con más detalle en la <xref linkend="sec.deployment.remoteinst.bootinst.custom"/>.
     </para>
     <para>
      El sistema de destino inicia un entorno basado en texto y ofrece la dirección de red que los clientes SSH pueden utilizar para acceder al entorno de instalación gráfica.
     </para>
    </step>
    <step performance="required">
     <para>
      En la estación de trabajo de control, abra una ventana de terminal y conéctese al sistema de destino como se describe en la <xref linkend="sec.deployment.remoteinst.monitor.ssh.connect"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Realice la instalación como se describe en el <xref linkend="cha.inst"/>. Es necesario volver a conectarse al sistema de destino después de reiniciarlo para la parte final de la instalación.
     </para>
    </step>
    <step performance="required">
     <para>
      Complete la instalación.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.sshdyn">
   <title>Instalación remota sencilla mediante SSH: configuración de red dinámica</title>
   <para>
    Para este tipo de instalación es necesario un cierto grado de acceso físico al sistema de destino con el fin de arrancarlo para la instalación y de determinar la dirección IP del destino de la instalación. Una estación de trabajo remota controla completamente la instalación propiamente dicha y usa SSH para conectarse al programa de instalación, pero es necesaria la intervención del usuario para realizar la configuración.
   </para>
   <note>
    <title>cómo evitar las conexiones perdidas después del segundo paso (instalación)</title>
    <para>
     En el recuadro de diálogo de configuración de red, marque la opción de <guimenu>método tradicional con ifup</guimenu> y evite NetworkManager. De lo contrario, la conexión SSH se perderá durante la instalación. Restablezca la configuración a <guimenu>controlado por el usuario mediante NetworkManager</guimenu> una vez finalizada la instalación.
    </para>
   </note>
   <para>
    Para este tipo de instalación, asegúrese de que se cumplen los siguientes requisitos:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Repositorio remoto: NFS, HTTP, FTP o SMB con una conexión de red en funcionamiento.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema de destino con una conexión de red en funcionamiento.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema de control con una conexión de red y software cliente para SSH en funcionamiento
     </para>
    </listitem>
    <listitem>
     <para>
      Medio de arranque físico (CD, DVD o memoria USB) para arrancar el sistema de destino.
     </para>
    </listitem>
    <listitem>
     <para>
      Servidor DHCP en funcionamiento que suministre las direcciones IP.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.sshdyn">
    <para>
     Para realizar este tipo de instalación, siga estos pasos:
    </para>
    <step performance="required">
     <para>
      Configure el repositorio tal y como se describe en la <xref linkend="sec.deployment.remoteinst.instserver"/>. Escoja un servidor de red NFS, HTTP o FTP. Para un repositorio SMB, consulte la <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Arranque el sistema de destino con el DVD1 del kit de medios de <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>.
     </para>
    </step>
    <step performance="required">
     <para>
      Cuando aparezca la pantalla de arranque en el sistema de destino, utilice el menú de opciones de arranque para establecer los parámetros apropiados de la conexión de red, la dirección del origen de la instalación y la habilitación de SSH. Para obtener instrucciones detalladas sobre el uso de estos parámetros, consulte la <xref linkend="sec.deployment.remoteinst.bootinst.custom"/>.
     </para>
     <para>
      El sistema de destino inicia un entorno basado en texto y ofrece la dirección de red mediante la cual los clientes SSH pueden dirigirse al entorno de instalación gráfica.
     </para>
    </step>
    <step performance="required">
     <para>
      En la estación de trabajo de control, abra una ventana de terminal y conéctese al sistema de destino como se describe en la <xref linkend="sec.deployment.remoteinst.monitor.ssh.connect"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Realice la instalación como se describe en el <xref linkend="cha.inst"/>. Es necesario volver a conectarse al sistema de destino después de reiniciarlo para la parte final de la instalación.
     </para>
    </step>
    <step performance="required">
     <para>
      Complete la instalación.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.sshpxe">
   <title>Instalación remota mediante SSH: arranque en PXE y Wake on LAN</title>
   <para>
    Este tipo de instalación no requiere intervención física alguna. La máquina de destino se inicia y arranca de manera remota.
   </para>
   <para>
    Para este tipo de instalación, asegúrese de que se cumplen los siguientes requisitos:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Repositorio remoto: NFS, HTTP, FTP o SMB con una conexión de red en funcionamiento.
     </para>
    </listitem>
    <listitem>
     <para>
      Servidor TFTP.
     </para>
    </listitem>
    <listitem>
     <para>
      Servidor DHCP en funcionamiento en la red que proporcione una dirección IP estática para el host que se vaya a instalar.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema de destino compatible con arranque en PXE, funcionamiento en red y Wake on LAN, enchufado y conectado a la red.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema de control con una conexión de red en funcionamiento y software cliente para SSH.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.sshpxe">
    <para>
     Para realizar este tipo de instalación, siga los pasos siguientes:
    </para>
    <step performance="required">
     <para>
      Configure el repositorio tal y como se describe en la <xref linkend="sec.deployment.remoteinst.instserver"/>. Escoja un servidor de red NFS, HTTP o FTP. Para configurar un repositorio SMB, consulte la <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Configure un servidor TFTP para que almacene una imagen de arranque que pueda ser utilizada por el sistema de destino. Este aspecto se describe en la <xref linkend="sec.deployment.remoteinst.boot.tftp"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Configure un servidor DHCP para que suministre direcciones IP a todas las máquinas e indique la ubicación del servidor TFTP al sistema de destino. Este aspecto se describe en la <xref linkend="sec.deployment.remoteinst.boot.dhcp"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Prepare el sistema de destino para arranque en PXE. Esto se describe con más detalle en la <xref linkend="sec.deployment.remoteinst.boot.pxeprep"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Comience el proceso de arranque del sistema de destino mediante Wake on LAN. Este aspecto se describe en la <xref linkend="sec.deployment.remoteinst.boot.wol"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      En la estación de trabajo de control, abra un cliente SSH y conéctese al sistema de destino como se describe en la <xref linkend="sec.deployment.remoteinst.monitor.ssh"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Realice la instalación como se describe en el <xref linkend="cha.inst"/>. Es necesario volver a conectarse al sistema de destino después de reiniciarlo para la parte final de la instalación.
     </para>
    </step>
    <step performance="required">
     <para>
      Complete la instalación.
     </para>
    </step>
   </procedure>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.instserver">
  <title>Configuración del servidor que almacena los orígenes de la instalación</title>

  <para>
   En función del sistema operativo que se ejecute en el equipo que se utilizará como origen de la instalación para <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>, existen varias opciones para configurar el servidor. La manera más sencilla de configurar un servidor de instalación es utilizar YaST en SUSE Linux Enterprise Server 11/opensuse; 11.1 o superior.
  </para>

  <tip>
   <para>
    Es posible incluso utilizar un equipo con Microsoft Windows como servidor de la instalación para la distribución de Linux. Consulte el <xref linkend="sec.deployment.remoteinst.instserver.smb"/> para obtener más información.
   </para>
  </tip>

  <sect2 id="sec.deployment.remoteinst.instserver.sles9">
   <title>Configuración de un servidor de instalación mediante YaST</title>
   <para>
    YaST ofrece una herramienta gráfica para crear repositorios en red. Admite servidores de instalación en red HTTP, FTP y NFS.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.sles9">
    <step performance="required">
     <para>
      Inicie sesión como usuario <systemitem class="username">Root</systemitem> en la máquina que actuará como servidor de la instalación.
     </para>
    </step>
    <step performance="required">
     <para>
      Inicie <menuchoice><guimenu>YaST</guimenu><guimenu>Otros</guimenu><guimenu>Servidor de instalación</guimenu></menuchoice>.
     </para>
    </step>
    <step performance="required">
     <para>
      Seleccione el tipo de repositorio (HTTP, FTP o NFS). El servicio seleccionado se ejecuta automáticamente cada vez que se inicia el sistema. Si ya se encuentra en funcionamiento en el sistema un servicio del tipo seleccionado y desea configurarlo manualmente para el servidor, desactive la configuración automática del servicio del servidor mediante <guimenu>No configurar ningún servicio de red</guimenu>. En ambos casos, defina el directorio en el que los datos de la instalación estarán disponibles en el servidor.
     </para>
    </step>
    <step performance="required">
     <para>
      Configure el tipo de repositorio requerido. Este paso está relacionado con la configuración automática de servicios de servidor. Se omite cuando la configuración automática está desactivada.
     </para>
     <para>

      Defina un alias para el directorio raíz del servidor FTP o HTTP en el que se encontrarán los datos de la instalación. El repositorio se ubicará más adelante en <literal>ftp://<replaceable>IP_del_servidor</replaceable>/<replaceable>alias</replaceable>/<replaceable>nombre</replaceable></literal> (FTP) o en <literal>http://<replaceable>IP_del_servidor</replaceable>/<replaceable>alias</replaceable>/<replaceable>nombre</replaceable></literal> (HTTP). <replaceable>nombre</replaceable> representa el nombre del repositorio, que se define en el siguiente paso. Si ha seleccionado NFS en el paso anterior, defina los comodines y las opciones de exportación. Podrá acceder al servidor NFS en <literal>nfs://<replaceable>IP_del_servidor</replaceable>/<replaceable>nombre</replaceable></literal>. Se pueden encontrar más detalles sobre NFS y las exportaciones en el <xref linkend="cha.nfs"/>.
     </para>
     <tip>
      <title>configuración del cortafuegos</title>
      <para>
       Asegúrese de que la configuración del cortafuegos del sistema del servidor permita el tráfico en los puertos HTTP, NFS y FTP. Si no es así, active <guimenu>Puerto abierto en el cortafuegos</guimenu> o marque <guimenu>Detalles del cortafuegos</guimenu> antes.
      </para>
     </tip>
    </step>
    <step performance="required">
     <para>
      Configure el repositorio. Antes de que los medios de instalación se copien en el destino, defina el nombre del repositorio (lo ideal sería una abreviatura fácil de recordar del producto y la versión). YaST permite ofrecer imágenes ISO de los medios, en lugar de copias de los DVD de instalación. Si desea hacerlo así, active la casilla de verificación correspondiente y especifique la vía del directorio en el que se ubican localmente los archivos ISO. En función del producto que se vaya a distribuir por medio de este servidor de instalación, puede que sean necesarios otros CD de productos adicionales o de paquetes de servicios, que se deberán añadir como repositorios extra. Para anunciar en la red el servidor de instalación mediante OpenSLP, active la opción correspondiente.
     </para>
     <tip>
      <para>
       Considere la opción de anunciar el repositorio mediante OpenSLP si la red lo admite. Esto le evita el tener que introducir la vía de instalación en red en cada máquina de destino. Los sistemas de destino se arrancan con la opción de arranque en SLP y encontrarán el repositorio en red sin necesidad de configuración adicional. Para obtener más detalles sobre esta opción, consulte la <xref linkend="sec.deployment.remoteinst.bootinst"/>.
      </para>
     </tip>
    </step>
    <step performance="required">
     <para>
      Cargue los datos de la instalación. El paso que más tiempo ocupa durante la configuración de un servidor de instalación es la copia de los medios de instalación en sí. Introduzca los medios en el orden que YaST solicite y espere a que termine el proceso de copiado. Cuando los orígenes se hayan copiado completamente, vuelva al resumen de los repositorios existentes y cierre la configuración seleccionando <guimenu>Finalizar.</guimenu>
     </para>
     <para>
      El servidor de instalación quedará completamente configurado y listo para usarse. Se ejecutará automáticamente cada vez que se inicie el sistema. No es necesario intervenir de ninguna otra manera. Solo es necesario configurar e iniciar correctamente este servicio a mano si se desactiva la configuración automática del servicio de red seleccionado con YaST en el paso inicial.
     </para>
    </step>
   </procedure>
   <para>
    Para desactivar un repositorio, seleccione el que desea eliminar y haga clic en <guimenu>Suprimir.</guimenu> Los datos de instalación se eliminan del sistema. Para desactivar el servicio de red, utilice el módulo de YaST correspondiente.
   </para>
   <para>
    Si el servidor de instalación debe ofrecer datos de instalación para más de un producto o versión, inicie el módulo del servidor de instalación de YaST y seleccione <guimenu>Añadir</guimenu> en la descripción general de los repositorios existentes para configurar el nuevo repositorio.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.nfs">
   <title>Configuración manual de un repositorio NFS</title>
   
   <para>
    La configuración de un origen de instalación NFS se lleva a cabo en dos pasos principales. En primer lugar, cree la estructura de directorios en la que se almacenarán los datos de la instalación y copie los medios de instalación en dicha estructura. A continuación, exporte a la red el directorio que contiene los datos de la instalación.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.cdir">
    <para>
     Para crear un directorio en el que se almacenen los datos de la instalación, siga estos pasos:
    </para>
    <step performance="required">
     <para>
      Entre a la sesión como usuario <systemitem class="username">root</systemitem>.
     </para>
    </step>
    <step performance="required">
     <para>
      Cree un directorio en el que más adelante se almacenarán los datos de la instalación y cambie a dicho directorio. Por ejemplo:
     </para>
<screen>mkdir install/<replaceable>product</replaceable>/<replaceable>productversion</replaceable>
cd install/<replaceable>product</replaceable>/<replaceable>productversion</replaceable></screen>
     <para>
      Sustituya <replaceable>product</replaceable> por una abreviación del nombre del producto y <replaceable>productversion</replaceable> por una cadena que contenga el nombre del producto y la versión.
     </para>
    </step>
    <step performance="required">
     <para>
      Ejecute los siguientes comandos para cada DVD contenido en el kit de medios:
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        Copie el contenido completo del DVD de instalación en el directorio del servidor de instalación:
       </para>
<screen>cp -a /media/<replaceable>path_to_your_DVD_drive .</replaceable></screen>
       <para>
        Sustituya <replaceable>path_to_your_DVD_drive</replaceable> por la vía real por la que se accede a la unidad de DVD. En función del tipo de unidad utilizado en el sistema, la vía puede ser <filename>cdrom</filename>, <filename>cdrecorder</filename>, <filename>dvd</filename> o <filename>dvdrecorder</filename>.
       </para>
      </step>
      <step performance="required">
       <para>
        Cambie el nombre del directorio al número del DVD:
       </para>
<screen>mv <replaceable>path_to_your_DVD_drive</replaceable> DVD<replaceable>x</replaceable></screen>
       <para>
        Sustituya <replaceable>x</replaceable> por el número real del DVD.
       </para>
      </step>
     </substeps>
    </step>
   </procedure>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.export">
    <para>
     En <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>, puede exportar el repositorio con NFS mediante YaST. Proceda de la siguiente manera:
    </para>
    <step performance="required">
     <para>
      Entre a la sesión como usuario <systemitem class="username">root</systemitem>.
     </para>
    </step>
    <step performance="required">
     <para>
      Inicie <menuchoice><guimenu>YaST</guimenu><guimenu>Servicios de red</guimenu><guimenu>Servidor NFS</guimenu></menuchoice>.
     </para>
    </step>
    <step performance="required">
     <para>
      Seleccione <guimenu>Iniciar</guimenu> y <guimenu>Puerto abierto en el cortafuegos</guimenu> y haga clic en <guimenu>Siguiente.</guimenu>
     </para>
    </step>
    <step performance="required">
     <para>
      Seleccione <guimenu>Añadir directorio</guimenu> y busque el directorio que contiene los orígenes de instalación. En este caso, <filename><replaceable>productversion</replaceable></filename>.
     </para>
    </step>
    <step performance="required">
     <para>
      Seleccione <guimenu>Añadir host</guimenu> e introduzca los nombres de host de los equipos a los que se exportarán los datos de la instalación. En lugar de especificar aquí los nombres de host, es posible usar comodines, rangos de direcciones de red o, simplemente, el nombre de dominio de la red. Introduzca las opciones de exportación apropiadas o mantenga las que se ofrecen por defecto, las cuales funcionan correctamente en la mayoría de las configuraciones. Para obtener más información sobre la sintaxis utilizada en la exportación de recursos compartidos NFS, lea la página Man de <systemitem>exports</systemitem>.
     </para>
    </step>
    <step performance="required">
     <para>
      Haga clic en <guimenu>Finalizar</guimenu>. El servidor NFS en el que se almacena el repositorio de <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> se iniciará automáticamente y se integrará en el proceso de arranque.
     </para>
    </step>
   </procedure>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.exportman">
    <para>
     Si prefiere exportar el repositorio mediante NFS de manera manual, en lugar de utilizar el módulo Servidor NFS de YaST, siga estos pasos:
    </para>
    <step performance="required">
     <para>
      Entre a la sesión como usuario <systemitem class="username">root</systemitem>.
     </para>
    </step>
    <step performance="required">
     <para>
      Abra el archivo <filename>/etc/exports</filename> e introduzca la siguiente línea:
     </para>
<screen>/<replaceable>productversion</replaceable> *(ro,root_squash,sync)</screen>
     <para>
      De esta forma se exporta el directorio <filename>/<replaceable>productversion</replaceable></filename> a cualquier host que forme parte de la red o a cualquier host que pueda conectar a este servidor. Para limitar el acceso al servidor, utilice máscaras de red o nombres de dominio en lugar del comodín general <literal>*</literal>. Consulte la página Man de <systemitem>export</systemitem> para obtener más detalles. Guarde y salga del archivo de configuración.
     </para>
    </step>
    <step performance="required">
     <para>
      Para añadir el servicio NFS a la lista de servidores que se inicia durante el arranque del sistema, ejecute los siguientes comandos:
     </para>
<screen>systemctl enable nfsserver.service</screen>
    </step>
    <step performance="required">
     <para>
      Inicie el servidor NFS mediante el comando <command>systemctl start nfsserver.service</command>. Si más adelante necesita cambiar la configuración del servidor NFS, modifique el archivo de configuración y reinicie el daemon NFS mediante el comando <command>systemctl restart nfsserver.service</command>.
     </para>
    </step>
   </procedure>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.slpann">
    <para>
     El anuncio del servidor NFS mediante OpenSLP hace que todos los clientes de la red conozcan su dirección.
    </para>
    <step performance="required">
     <para>
      Entre a la sesión como usuario <systemitem class="username">root</systemitem>.
     </para>
    </step>
    <step performance="required">
     <para>
      Cree el archivo de configuración <filename>/etc/slp.reg.d/install.suse.nfs.reg</filename> con las líneas siguientes:
     </para>
<screen><?dbsuse-fo font-size="7pt"?>

# Register the NFS Installation Server
service:install.suse:nfs://$HOSTNAME/<replaceable>path_to_repository</replaceable>/DVD1,en,65535 
description=NFS Repository</screen>
     <para>
      Sustituya <replaceable>path_to_repository</replaceable> por la vía real al repositorio del servidor.
     </para>
    </step>
    <step performance="required">
     <para>
      Inicie el daemon de OpenSLP mediante el comando <command>systemctl start slpd.service</command>.
     </para>
    </step>
   </procedure>
   <para os="sles">
    Para obtener más información sobre OpenSLP, consulte el paquete de documentación que se encuentra en <filename>/usr/share/doc/packages/openslp/</filename> y también el <xref linkend="cha.slp"/>. Para obtener más información sobre NFS, consulte el <xref linkend="cha.nfs"/>.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.ftp">
   <title>Configuración manual de un repositorio FTP</title>
   <para>
    La creación de un repositorio FTP es muy similar a la de repositorios NFS. Los repositorios FTP también se pueden anunciar en la red mediante OpenSLP.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.ftp">
    <step performance="required">
     <para>
      Cree un directorio en el que se almacenarán los orígenes de la instalación como se describe en la <xref linkend="sec.deployment.remoteinst.instserver.nfs"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Configure el servidor FTP para que distribuya los contenidos del directorio de instalación:
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        Entre como usuario <systemitem class="username">Root</systemitem> e instale el paquete <systemitem>vsftpd</systemitem> con el gestor de software de YaST.
       </para>
      </step>
      <step performance="required">
       <para>
        Entre en el directorio raíz del servidor FTP:
       </para>
<screen>cd <option>/srv/ftp</option></screen>
      </step>
      <step performance="required">
       <para>
        Cree un subdirectorio en el que se almacenarán los orígenes de la instalación en el directorio raíz FTP:
       </para>
<screen>mkdir <replaceable>repository</replaceable></screen>
       <para>
        Sustituya <replaceable>repository</replaceable> por el nombre del producto.
       </para>
      </step>
      <step performance="required">
       <para>
        Monte el contenido del repositorio de instalación en el entorno chroot del servidor FTP:
       </para>
<screen>mount --bind <replaceable>path_to_repository</replaceable> /srv/ftp/<replaceable>repository</replaceable></screen>
       <para>
        Sustituya <replaceable>path_to_repository</replaceable> y <replaceable>repository</replaceable> por los valores correspondientes a su configuración. Si necesita que sea permanente, añádalo a <filename>/etc/fstab</filename>.
       </para>
      </step>
      <step performance="required">
       <para>
        Inicie vsftpd con <command>vsftpd</command>.
       </para>
      </step>
     </substeps>
    </step>
    <step performance="required">
     <para>
      Anuncie el repositorio mediante OpenSLP si la configuración de la red lo admite:
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        Cree el archivo de configuración <filename>/etc/slp.reg.d/install.suse.ftp.reg</filename> con las líneas siguientes:
       </para>
<screen><?dbsuse-fo font-size="7pt"?>

# Register the FTP Installation Server
service:install.suse:ftp://$HOSTNAME/<replaceable>repository</replaceable>/DVD1,en,65535 
description=FTP Repository</screen>
       <para>
        Sustituya <replaceable>repository</replaceable> por el nombre real del repositorio en el servidor. La línea <literal>service:</literal> se debe introducir como una sola línea continua.
       </para>
      </step>
      <step performance="required">
       <para>
        Inicie el daemon de OpenSLP mediante el comando <command>systemctl start slpd.service</command>.
       </para>
      </step>
     </substeps>
    </step>
   </procedure>
   <tip os="sles">
    <title>Configuración de un servidor FTP con YaST</title>
    <para>
     Si prefiere utilizar YaST en lugar de configurar manualmente el servidor de instalación FTP, consulte el <xref linkend="cha.ftp"/> para obtener más información acerca del uso del módulo del servidor FTP de YaST.
    </para>
   </tip>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.http">
   <title>Configuración manual de un repositorio HTTP</title>
   <para>
    La creación de un repositorio HTTP es muy similar a la de repositorios NFS. Los repositorios HTTP también se pueden anunciar en la red mediante OpenSLP.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.http">
    <step performance="required">
     <para>
      Cree un directorio en el que se almacenarán los orígenes de la instalación como se describe en la <xref linkend="sec.deployment.remoteinst.instserver.nfs"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Configure el servidor HTTP para que distribuya los contenidos del directorio de instalación:
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        <phrase os="sles">Instale el servidor Web Apache como se describe en la <xref linkend="sec.apache2.quickstart.installation"/></phrase>.
       </para>
      </step>
      <step performance="required">
       <para>
        Entre en el directorio raíz del servidor HTTP (<filename>/srv/www/htdocs</filename>) y cree un subdirectorio en el que se almacenarán los repositorios:
       </para>
<screen>mkdir <replaceable>repository</replaceable></screen>
       <para>
        Sustituya <replaceable>repository</replaceable> por el nombre del producto.
       </para>
      </step>
      <step performance="required">
       <para>
        Cree un enlace simbólico entre la ubicación de los orígenes de la instalación y el directorio raíz del servidor Web (<filename>/srv/www/htdocs</filename>):
       </para>
<screen>ln -s <replaceable>/path_to_repository </replaceable>/srv/www/htdocs/<replaceable>repository</replaceable></screen>
      </step>
      <step performance="required">
       <para>
        Modifique el archivo de configuración del servidor HTTP (<filename>/etc/apache2/default-server.conf</filename>) para que siga enlaces simbólicos. Sustituya la siguiente línea:
       </para>
<screen>Options None</screen>
       <para>
        con
       </para>
<screen>Options Indexes FollowSymLinks</screen>
      </step>
      <step performance="required">
       <para>
        Vuelva a cargar la configuración del servidor HTTP mediante el comando <command>systemctl reload apache2.service</command>.
       </para>
      </step>
     </substeps>
    </step>
    <step performance="required">
     <para>
      Anuncie el repositorio mediante OpenSLP si la configuración de la red lo admite:
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        Cree el archivo de configuración <filename>/etc/slp.reg.d/install.suse.http.reg</filename> con las líneas siguientes:
       </para>
<screen><?dbsuse-fo font-size="6.5pt"?>

# Register the HTTP Installation Server
service:install.suse:http://$HOSTNAME/<replaceable>repository</replaceable>/DVD1/,en,65535 
description=HTTP Repository</screen>
       <para>
        Sustituya <replaceable>repository</replaceable> por la vía real del repositorio del servidor. La línea <literal>service:</literal> se debe introducir como una sola línea continua.
       </para>
      </step>
      <step performance="required">
       <para>
        Inicie el daemon de OpenSLP mediante el comando <command>systemctl start slpd.service</command>.
       </para>
      </step>
     </substeps>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.smb">
   <title>Gestión de un repositorio SMB</title>
   <para>
    Mediante SMB es posible importar los orígenes de la instalación desde un servidor Microsoft Windows e iniciar la distribución de Linux incluso sin que haya ningún sistema Linux.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.smb.export">
    <para>
     Para configurar un recurso compartido de Windows en el que se almacenará el repositorio de <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>, siga estos pasos:
    </para>
    <step performance="required">
     <para>
      Inicie sesión en la máquina que tenga instalado Windows.
     </para>
    </step>
    <step performance="required">
     <para>
      Cree un directorio nuevo en el que se almacenará el árbol de la instalación completo y asígnele un nombre, por ejemplo, <filename>INSTALL</filename>.
     </para>
    </step>
    <step performance="required">
     <para>
      Exporte este recurso compartido mediante el procedimiento descrito en la documentación de Windows.
     </para>
    </step>
    <step performance="required">
     <para>
      Introduzca este recurso compartido y cree un subdirectorio llamado <filename><replaceable>producto</replaceable></filename>. Sustituya <replaceable>producto</replaceable> por el nombre real del producto.
     </para>
    </step>
    <step performance="required">
     <para>
      Acceda al directorio <filename>INSTALL/<replaceable>producto</replaceable></filename> y copie cada DVD en una carpeta independiente, como <literal>DVD1</literal> y <literal>DVD2</literal>.
     </para>
    </step>
   </procedure>
   <para>
    Para utilizar un recurso compartido SMB montado como repositorio, siga estos pasos:
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.smb.use">
    <step performance="required">
     <para>
      Arranque el destino de la instalación.
     </para>
    </step>
    <step performance="required">
     <para>
      Seleccione <guimenu>Instalación</guimenu>.
     </para>
    </step>
    <step performance="required">
     <para>
      Pulse <keycap>F4</keycap> para ver una selección del repositorio.
     </para>
    </step>
    <step performance="required">
     <para>
      Seleccione SMB e introduzca el nombre o la dirección IP del equipo Windows, el nombre del recurso compartido (en este ejemplo, <filename>INSTALL/<replaceable>producto</replaceable>/DVD1</filename>), el nombre de usuario y la contraseña. La sintaxis será similar a esta:
     </para>
<screen>smb://workdomain;user:password@server/INSTALL/DVD1</screen>
     <para>
      Cuando pulse <keycap function="enter">Entrar</keycap>, YaST se iniciará y podrá realizar la instalación.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.iso">
   <title>Uso de imágenes ISO de los medios de instalación en el servidor</title>
   <para>
    En lugar de copiar los medios físicos en el directorio del servidor manualmente, puede montar las imágenes ISO de los medios en el servidor de instalación para usarlas como repositorios. Para configurar un servidor HTTP, NFS o FTP que utilice imágenes ISO en lugar de copias de los medios, siga estos pasos:
   </para>
   <procedure>
    <step performance="required">
     <para>
      Descargue las imágenes ISO y guárdelas en el equipo que vaya a utilizar como servidor de instalación.
     </para>
    </step>
    <step performance="required">
     <para>
      Entre a la sesión como usuario <systemitem class="username">root</systemitem>.
     </para>
    </step>
    <step performance="required">
     <para>
      Seleccione y cree una ubicación adecuada para los datos de instalación, como se describe en la <xref linkend="sec.deployment.remoteinst.instserver.nfs"/>, la <xref linkend="sec.deployment.remoteinst.instserver.ftp"/> o la <xref linkend="sec.deployment.remoteinst.instserver.http"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Cree subdirectorios para cada DVD.
     </para>
    </step>
    <step performance="required">
     <para>
      Para montar y desempaquetar cada imagen ISO a la ubicación final, emita el siguiente comando:
     </para>
<screen>mount -o loop <replaceable>path_to_iso</replaceable><replaceable>path_to_repository</replaceable>/<replaceable>product</replaceable>/<replaceable>mediumx</replaceable></screen>
     <para>
      Sustituya <replaceable>path_to_iso</replaceable> por la vía a la copia local de la imagen ISO, <replaceable>path_to_repository</replaceable> por el directorio del repositorio, <replaceable>product</replaceable> por el nombre del producto y <replaceable>mediumx</replaceable> por el tipo (CD o DVD) y el número de los medios que esté utilizando.
     </para>
    </step>
    <step performance="required">
     <para>
      Repita el paso anterior para montar todas las imágenes ISO necesarias para el producto.
     </para>
    </step>
    <step performance="required">
     <para>
      Inicie el servidor de instalación de la forma habitual, como se describe en la <xref linkend="sec.deployment.remoteinst.instserver.nfs"/>, la <xref linkend="sec.deployment.remoteinst.instserver.ftp"/>, o la <xref linkend="sec.deployment.remoteinst.instserver.http"/>.
     </para>
    </step>
   </procedure>
   <para>
    Para montar automáticamente las imágenes ISO en el momento del arranque, añada las entradas correspondientes al archivo <filename>/etc/fstab</filename>. Una entrada relativa al ejemplo anterior podría tener el aspecto siguiente: 
   </para>
<screen><replaceable>path_to_iso</replaceable> <replaceable>path_to_repository</replaceable>/<replaceable>product</replaceable><replaceable>medium</replaceable> auto loop</screen>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.boot">
  <title>Preparación del arranque del sistema de destino</title>

  <para>
   En esta sección se describen las tareas de configuración necesarias en entornos de arranque complejos. Contiene ejemplos de configuración listos para usar para DHCP, arranque en PXE, TFTP y Wake on LAN.
  </para>

  <sect2 id="sec.deployment.remoteinst.boot.dhcp">
   <title>Configuración de un servidor DHCP</title>
   <para>
    Existen dos formas de configurar un servidor DHCP. Para <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>, YaST proporciona una interfaz gráfica para el proceso. Los usuarios tienen también la posibilidad de editar los archivos de configuración.<phrase os="sles"> Para obtener más información acerca de los servidores DHCP, consulte también el <xref linkend="cha.dhcp"/>.</phrase>
   </para>
   <sect3 id="sec.deployment.remoteinst.boot.dhcp.yast">
    <title>Configuración de un servidor DHCP con YaST</title>
    <para>
     Para indicar la ubicación del servidor TFTP a los clientes de la red y especificar el archivo de imagen de arranque que debe usar el destino de instalación, añada dos declaraciones a la configuración del servidor DHCP.
    </para>
    <procedure id="proc.deployment.remoteinst.boot.dhcp.yast">
     <step performance="required">
      <para>
       Inicie sesión como usuario <systemitem class="username">Root</systemitem> en la máquina que aloje el servidor DHCP.
      </para>
     </step>
     <step performance="required">
      <para>
       Inicie <menuchoice><guimenu>YaST</guimenu><guimenu>Servicios de red</guimenu><guimenu>Servidor DHCP</guimenu></menuchoice>.
      </para>
     </step>
     <step performance="required">
      <para>
       Siga los pasos del asistente para realizar una configuración básica del servidor DHCP.
      </para>
     </step>
     <step id="st.deployment.remoteinst.boot.dhcp.yast.expert" performance="required">
      <para>
       Seleccione <guimenu>Configuración avanzada</guimenu> y pulse <guimenu>Sí</guimenu> cuando se le indique que va a salir del recuadro de diálogo de inicio.
      </para>
     </step>
     <step performance="required">
      <para>
       En el recuadro de diálogo <guimenu>Declaraciones configuradas,</guimenu> seleccione la subred en la que se debe ubicar el nuevo sistema y haga clic en <guimenu>Editar.</guimenu>
      </para>
     </step>
     <step performance="required">
      <para>
       En el recuadro de diálogo <guimenu>Configuración de subred,</guimenu> seleccione <guimenu>Añadir</guimenu> para añadir una opción nueva a la configuración de la subred.
      </para>
     </step>
     <step performance="required">
      <para>
       Seleccione <option>filename</option> y escriba <literal>pxelinux.0</literal> como nombre de archivo.
      </para>
     </step>
     <step performance="required">
      <para>
       Indique cuál debe ser el siguiente servidor (<option>next-server</option>) y establezca como valor la dirección del servidor TFTP.
      </para>
     </step>
     <step performance="required">
      <para>
       Haga clic en <guimenu>Aceptar</guimenu> y en <guimenu>Finalizar</guimenu> para completar la configuración del servidor DHCP.
      </para>
     </step>
    </procedure>
    <para>
     Para configurar DHCP con objeto de proporcionar una dirección IP estática para un host específico, indique la <guimenu>Configuración avanzada</guimenu> del módulo de configuración del servidor DHCP (<xref linkend="st.deployment.remoteinst.boot.dhcp.yast.expert" xrefstyle="StepOnPage"/>) y añada una declaración nueva del tipo de host. Añada las opciones <option>hardware</option> y <option>fixed-address</option> a la declaración de este host y proporcione los valores oportunos para el hardware y la dirección fija, respectivamente.
    </para>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.boot.dhcp.man">
    <title>Configuración manual de un servidor DHCP</title>
    <para>
     Todo lo que necesita hacer el servidor DHCP, además de ofrecer asignación de direcciones automática a los clientes de la red, es anunciar la dirección IP del servidor TFTP y el archivo que las rutinas de instalación del equipo de destino deben obtener.
    </para>
    <procedure id="proc.deployment.remoteinst.boot.dhcp.man">
     <step performance="required">
      <para>
       Inicie sesión como usuario <systemitem class="username">Root</systemitem> en la máquina que aloje el servidor DHCP.
      </para>
     </step>
     <step performance="required">
      <para>
       Añada las siguientes líneas a una configuración de subred del archivo de configuración del servidor DHCP que se encuentra en <filename>/etc/dhcp.conf</filename>:
      </para>
<screen>subnet 192.168.1.0 netmask 255.255.255.0 { 
  range dynamic-bootp 192.168.1.200 192.168.1.228;
  # PXE related settings
  #
  # "next-server" defines the TFTP server that will be used
  next-server <replaceable>ip_tftp_server</replaceable>;
  #
  # "filename" specifies the pxelinux image on the TFTP server
  # the server runs in chroot under /srv/tftpboot
  filename  "pxelinux.0";
}</screen>
      <para>
       Sustituya <replaceable>ip_tftp_server</replaceable> con la dirección IP real del servidor TFTP. Para obtener más información acerca las opciones disponibles en <filename>dhcpd.conf</filename>, consulte la página de Man de <systemitem>dhcpd.conf</systemitem>.
      </para>
     </step>
     <step performance="required">
      <para>
       Para reiniciar el servidor DHCP, ejecute el comando <command>systemctl restart dhcpd.service</command>.
      </para>
     </step>
    </procedure>
    <para>
     Si tiene previsto utilizar SSH para controlar remotamente una instalación PXE y Wake on LAN, especifique explícitamente la dirección IP que DHCP debe suministrar al destino de la instalación. Para ello, modifique la configuración DHCP antes mencionada de acuerdo con el siguiente ejemplo:
    </para>
<screen>group {
  # PXE related settings
  # 
  # "next-server" defines the TFTP server that will be used 
  next-server <replaceable>ip_tftp_server</replaceable>:
  # 
  # "filename" specifies the pxelinux image on the TFTP server 
  # the server runs in chroot under /srv/tftpboot
  filename "pxelinux.0";
  host test {
    hardware ethernet <replaceable>mac_address</replaceable>; 
    fixed-address <replaceable>some_ip_address</replaceable>;
    }
}
</screen>
    <para>
     La declaración del host incluye el nombre del host del destino de la instalación. Para vincular el nombre de host y la dirección IP con un host determinado, es necesario conocer y especificar la dirección de hardware del sistema (MAC). Sustituya todas las variables utilizadas en este ejemplo por los valores reales correspondientes a su entorno.
    </para>
    <para>
     Una vez reiniciado el servidor DHCP, ofrecerá una dirección IP estática al host especificado, lo que permitirá conectarse al sistema mediante SSH.
    </para>
   </sect3>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.tftp">
   <title>Configuración de un servidor TFTP</title>
   <para>
    Si usa una instalación basada en SUSE, puede usar YaST para configurar un servidor TFTP. También puede configurarlo manualmente. El servidor TFTP proporciona la imagen de arranque al sistema de destino después de que este arranque y envía una solicitud para ello.
   </para>
   <sect3 id="sec.deployment.remoteinst.boot.tftp.yast">
    <title>Configuración de un servidor TFTP mediante YaST</title>
    <procedure id="proc.deployment.remoteinst.boot.tftp.yast">
     <step performance="required">
      <para>
       Entre a la sesión como usuario <systemitem class="username">root</systemitem>.
      </para>
     </step>
     <step performance="required">
      <para>
       Inicie <menuchoice><guimenu>YaST</guimenu><guimenu>Servicios de red</guimenu><guimenu>Servidor TFTP</guimenu></menuchoice> e instale el paquete necesario.
      </para>
     </step>
     <step performance="required">
      <para>
       Haga clic en <guimenu>Habilitar</guimenu> para asegurarse de que el servidor se inicie y se incluya en las rutinas de arranque. No es necesaria ninguna otra intervención por su parte para ello. xinetd inicia tftpd en el momento del arranque.
      </para>
     </step>
     <step performance="required">
      <para>
       Haga clic en <guimenu>Puerto abierto en el cortafuegos</guimenu> para abrir el puerto correspondiente en el cortafuegos que se esté ejecutando en la máquina. Si no hay ningún cortafuegos en ejecución en el servidor, está opción no estará disponible.
      </para>
     </step>
     <step performance="required">
      <para>
       Haga clic en <guimenu>Examinar</guimenu> para explorar el directorio de la imagen de arranque. Se creará y se seleccionará automáticamente el directorio por defecto <filename>/tftpboot</filename>.
      </para>
     </step>
     <step performance="required">
      <para>
       Haga clic en <guimenu>Finalizar</guimenu> para aplicar la configuración e iniciar el servidor.
      </para>
     </step>
    </procedure>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.boot.tftp.man">
    <title>Configuración manual de un servidor TFTP</title>
    <procedure id="proc.deployment.remoteinst.boot.tftp.man">
     <step performance="required">
      <para>
       Inicie sesión como usuario <systemitem class="username">Root</systemitem> e instale los paquetes <systemitem>tftp</systemitem> y <systemitem>xinetd</systemitem>.
      </para>
     </step>
     <step performance="required">
      <para>
       Si no están disponibles, cree los directorios <filename>/srv/tftpboot</filename> y <filename>/srv/tftpboot/pxelinux.cfg</filename>.
      </para>
     </step>
     <step performance="required">
      <para>
       Añada los archivos correspondientes necesarios para la imagen de arranque como se describe en la <xref linkend="sec.deployment.remoteinst.boot.pxe"/>.
      </para>
     </step>
     <step performance="required">
      <para>
       Modifique la configuración de xinetd, que se encuentra en <filename>/etc/xinetd.d/</filename>, para asegurarse de que el servidor TFTP se inicie durante el arranque:
      </para>
      <substeps performance="required">
       <step performance="required">
        <para>
         Si no existe, cree un archivo de nombre <filename>tftp</filename> en este directorio mediante <command>touch tftp</command>. A continuación, ejecute <command>chmod 755 tftp</command>.
        </para>
       </step>
       <step performance="required">
        <para>
         Abra el archivo <filename>tftp</filename> y añada las siguientes líneas:
        </para>
<screen>service tftp 
{ 
        socket_type            = dgram
        protocol               = udp 
        wait                   = yes 
        user                   = root 
        server                 = /usr/sbin/in.tftpd 
        server_args            = -s /srv/tftpboot 
        disable                = no 
}
</screen>
       </step>
       <step performance="required">
        <para>
         Guarde el archivo y reinicie xinetd con <command>rcxinetd restart</command>.

        </para>
       </step>
      </substeps>
     </step>
    </procedure>
   </sect3>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.pxe">
   <title>Uso del arranque PXE</title>
   <para>
    En las especificaciones de Preboot Execution Environment (PXE), disponibles en<ulink url="http://www.pix.net/software/pxeboot/archive/pxespec.pdf"/>, se incluye información técnica básica, así como las especificaciones completas de PXE.
   </para>
   <procedure id="proc.deployment.remoteinst.boot.pxe">
    <step performance="required">
     <para>
      Cambie al directorio <filename>boot/&lt;arquitectura&gt;/loader</filename> del repositorio de instalación y copie los archivos <filename>linux</filename>, <filename>initrd</filename>, <filename>message</filename>, <filename>biostest</filename> y <filename>memtest</filename> en el directorio <filename>/srv/tftpboot</filename> introduciendo lo siguiente:
     </para>
<screen>cp -a linux initrd message biostest memtest /srv/tftpboot</screen>
    </step>
    <step performance="required">
     <para>
      Instale el paquete <systemitem>syslinux</systemitem> directamente desde los DVD de instalación con YaST.
     </para>
    </step>
    <step performance="required">
     <para>
      Copie el archivo <filename>/usr/share/syslinux/pxelinux.0</filename> en el directorio <filename>/srv/tftpboot</filename> introduciendo lo siguiente:
     </para>
<screen>cp -a /usr/share/syslinux/pxelinux.0 /srv/tftpboot
     </screen>
    </step>
    <step performance="required">
     <para>
      Cambie al directorio del repositorio de la instalación y copie el archivo <filename>isolinux.cfg</filename> en <filename>/srv/tftpboot/pxelinux.cfg/default</filename> introduciendo lo siguiente:
     </para>
<screen>cp -a boot/&lt;architecture&gt;/loader/isolinux.cfg /srv/tftpboot/pxelinux.cfg/default
     </screen>
    </step>
    <step performance="required">
     <para>
      Edite el archivo <filename>/srv/tftpboot/pxelinux.cfg/default</filename> y elimine las líneas que empiecen por <literal>readinfo</literal> y <literal>framebuffer</literal>.
     </para>
    </step>
    <step performance="required">
     <para>

      Añada las siguientes entradas en las líneas append de las etiquetas por defecto <option>failsafe</option> y <option>apic</option>:
     </para>
     <variablelist>
      <varlistentry>
       <term><option>insmod=<replaceable>módulo del núcleo</replaceable></option>
       </term>
       <listitem>
        <para>
         Mediante esta entrada, especifique el módulo del núcleo de red necesario para realizar la instalación de red en el cliente PXE. Sustituya <replaceable>módulo del núcleo</replaceable> por el nombre del módulo adecuado para su dispositivo de red.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term><option>netdevice=<replaceable>interfaz</replaceable></option>
       </term>
       <listitem>
        <para>
         Esta entrada define la interfaz de red del cliente que debe utilizarse para la instalación en red. Solo es necesaria si el cliente dispone de varias tarjetas de red y debe adaptarse en consecuencia. En el caso de que solo se disponga de una tarjeta de red, esta entrada debe omitirse.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term>
<option>install=nfs://<replaceable>ip_instserver</replaceable>/<replaceable>path_to_repository</replaceable>/DVD1</option>
       </term>
       <listitem>
        <para>
         Esta entrada define el servidor NFS y el repositorio para la instalación del cliente. Sustituya <replaceable>ip_instserver</replaceable> por la dirección IP real del servidor de instalación. <replaceable>path_to_repository</replaceable> se debe sustituir por la vía real al repositorio. Las direcciones de los repositorios HTTP, FTP y SMB son similares, excepto en el prefijo del protocolo, que debe ser <literal>http</literal>, <literal>ftp</literal> o <literal>smb</literal>.
        </para>
        <important>
         <title>adición de opciones de arranque</title>
         <para>
          Si necesita pasar otras opciones de arranque a las rutinas de instalación, tales como parámetros de inicio de VNC o SSH, añádalas a la entrada <option>install</option>. En la <xref linkend="sec.deployment.remoteinst.bootinst"/> se ofrece un resumen de los parámetros y algunos ejemplos.
         </para>
        </important>
       </listitem>
      </varlistentry>
     </variablelist>
     <tip>
      <title>cambio del nombre del núcleo y del archivo initrd</title>
      <para>
       Se pueden utilizar nombres de archivo distintos para las imágenes del núcleo y de initrd. Esto resulta útil si se desea proporcionar distintos sistemas operativos desde el mismo servidor de arranque. Sin embargo, debe ser consciente de que solo se admite un punto en el nombre de los archivos que proporciona TFTP para el arranque PXE.
      </para>
     </tip>
     <para>
      A continuación se incluye un ejemplo de archivo <option>/srv/tftpboot/pxelinux.cfg/default. </option> Ajuste el prefijo del protocolo del repositorio para que se corresponda con la configuración de la red y especifique el método que prefiera para conectarse al instalador añadiendo las opciones <literal>vnc</literal> y <literal>vncpassword</literal> o las opciones <literal>usessh</literal> y <literal>sshpassword</literal> en la entrada <literal>install</literal>. Las líneas separadas por <literal>\</literal> deben introducirse en una sola línea, sin salto de línea y sin <literal>\</literal>.
     </para>
<screen>default harddisk 
        
# default
label linux 
  kernel linux 
  append initrd=initrd ramdisk_size=65536 \
     install=nfs://<replaceable>ip_instserver</replaceable>/<replaceable>path_to_repository</replaceable>/<replaceable>product</replaceable>/DVD1 
	    
# repair
label repair
  kernel linux
  append initrd=initrd splash=silent repair=1 showopts

# rescue 
label rescue 
  kernel linux 
  append initrd=initrd ramdisk_size=65536 rescue=1 

# bios test
label firmware
  kernel linux
  append initrd=biostest,initrd splash=silent install=exec:/bin/run_biostest showopts

#  memory test 
label memtest 
  kernel memtest 

#  hard disk 
label harddisk
  localboot 0

implicit     0 
display      message
prompt       1 
timeout      100</screen>
    </step>
    <step performance="required">
     <para>
      Sustituya <replaceable>ip_instserver</replaceable> y <replaceable>path_to_repository</replaceable> por los valores correspondientes a su configuración.
     </para>
     <para>
      La siguiente sección sirve como breve referencia de las opciones de PXELINUX utilizadas en esta configuración. Hay más información sobre las opciones disponibles en la documentación del paquete <systemitem>syslinux</systemitem> que se encuentra en <filename>/usr/share/doc/packages/syslinux/</filename>.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.pxeopts">
   <title>Opciones de configuración de PXELINUX</title>
   <para>
    A continuación aparecen algunas de las opciones disponibles para el archivo de configuración de PXELINUX.
   </para>
   <variablelist>
    <varlistentry>
     <term><option>APPEND <replaceable>opciones... </replaceable></option>
     </term>
     <listitem>
      <para>
       Añada una o más opciones a la línea de comandos del núcleo. Éstas se añaden para arranques automáticos y manuales. Las opciones se añaden al principio de la línea de comandos del núcleo, y normalmente admiten que las opciones del núcleo introducidas explícitamente las anulen.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>APPEND -</option>
     </term>
     <listitem>
      <para>
       Sin nada añadido. Se puede utilizar <option>APPEND</option> con un solo guion como argumento en una sección <option>LABEL</option> para anular un valor de <option>APPEND</option> global.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>DEFAULT <replaceable>opciones del núcleo</replaceable>...</option>
     </term>
     <listitem>
      <para>
       Establece la línea de comandos del núcleo por defecto. Si PXELINUX arranca de manera automática, actúa como si las entradas posteriores a DEFAULT se hubieran escrito en el indicador de inicio, excepto la opción auto que se añade de manera automática, lo que indica un arranque automático.
      </para>
      <para>
       Si no hay ningún archivo de configuración o ninguna entrada DEFAULT en el archivo de configuración, el valor por defecto es el nombre del núcleo <quote>linux</quote>, sin opciones.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>

     <term><option>IFAPPEND <replaceable>FLAG</replaceable></option>
     </term>
     <listitem>
      <para>
       Añade una opción específica a la línea de comando del núcleo dependiendo del valor de <replaceable>FLAG</replaceable>. La opción <option>IFAPPEND</option> solo está disponible en PXELINUX. <replaceable>FLAG</replaceable> espera un valor, descrito en <xref linkend="tab.deployment.boot.pxeopts.ifappend"/>:
      </para>
      <table id="tab.deployment.boot.pxeopts.ifappend">
       <title>Opciones de líneas de comandos del núcleo generadas y añadidas desde <option>IFAPPEND</option></title>
       <tgroup cols="2">
        <colspec colwidth="1*"/>
        <colspec colwidth="4*"/>
        <thead>
         <row>
          <entry valign="middle">
           <para>
            Argumento
           </para>
          </entry>
          <entry>
           <para>
            Línea de comando del núcleo generada/Descripción
           </para>
          </entry>
         </row>
        </thead>
        <tbody>
         <row>
          <entry>
           <para>
            <option>1</option>
           </para>
          </entry>
          <entry>
<screen>ip=<replaceable>CLIENT_IP</replaceable>:<replaceable>BOOT_SERVER_IP</replaceable>:<replaceable>GW_IP</replaceable>:<replaceable>NETMASK</replaceable></screen>
           <para>
            Los espacios reservados se reemplazan según la entrada del servidor de arranque DHCP/BOOTP o PXE.
           </para>
           <para>
            Tenga en cuenta que esta opción no sustituye a la ejecución de un cliente DHCP en el sistema arrancado. Sin renovaciones regulares, la asignación adquirida por el BIOS PXE caducará, con lo que la dirección IP vuelve a estar disponible para que la reutilice el servidor DHCP.
           </para>
          </entry>
         </row>
         <row>
          <entry>
           <para>
            <option>2</option>
           </para>
          </entry>
          <entry>
<screen>BOOTIF=<replaceable>MAC_ADDRESS_OF_BOOT_INTERFACE</replaceable></screen>
           <para>
            Esta opción resulta útil si quiere evitar interrupciones cuando el servidor de instalación sondea una interfaz LAN tras otra hasta que recibe respuesta de un servidor DHCP. El uso de esta opción permite que un programa initrd determine desde qué interfaz se ha arrancado el sistema. linuxrc lee esta opción y utiliza la interfaz de red.
           </para>
          </entry>
         </row>
         <row>
          <entry>
           <para>
            <option>4</option>
           </para>
          </entry>
          <entry>
<screen>SYSUUID=<replaceable>SYSTEM_UUID</replaceable></screen>
           <para>
            Añade UUID en formato hexadecimal en minúscula, ver <filename>/usr/share/doc/packages/syslinux/pxelinux.txt</filename>
           </para>
          </entry>
         </row>
        </tbody>
       </tgroup>
      </table>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>LABEL <replaceable>etiqueta</replaceable> KERNEL <replaceable>imagen</replaceable> APPEND <replaceable>opciones</replaceable>...</option>
     </term>
     <listitem>
      <para>
       Indica que si se introduce <replaceable>etiqueta</replaceable> como el núcleo que se debe arrancar, PXELINUX debe arrancar <replaceable>imagen</replaceable> en su lugar y utilizar las opciones <option>APPEND</option> especificadas en lugar de las indicadas en el apartado global del archivo (antes del primer comando <option>LABEL</option>). El valor por defecto de <replaceable>imagen</replaceable> es el mismo que <replaceable>label</replaceable> y, si no se introduce ningún <option>APPEND</option>, el valor por defecto consiste en utilizar la entrada global (si hubiera alguna). Se permiten hasta 128 entradas <option>LABEL</option>.
      </para>

      <para>
       PXELINUX utiliza la siguiente sintaxis:
      </para>
<screen>label <replaceable>mylabel</replaceable> 
  kernel <replaceable>mykernel</replaceable> 
  append <replaceable>myoptions</replaceable></screen>
      <para>
       Las etiquetas se truncan como si fueran nombres de archivo, y deben ser únicas después del truncamiento. Por ejemplo, dos etiquetas como <quote>v2.6.30</quote> y <quote>v2.6.31</quote> no podrán distinguirse en PXELINUX, ya que ambas se truncan con el mismo nombre de archivo de DOS.
      </para>
      <para>
       No es necesario que el núcleo sea un núcleo de Linux; puede ser un sector de arranque o un archivo COMBOOT.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>LOCALBOOT <replaceable>tipo</replaceable></option>
     </term>
     <listitem>
      <para>
       En PXELINUX, especificar <option>LOCALBOOT 0</option> en lugar de una opción de <option>KERNEL</option> significa la invocación de esa etiqueta en particular y provoca un arranque del disco local en lugar de un arranque del núcleo.
      </para>
      <informaltable frame="topbot" rowsep="0">
       <tgroup cols="2">

        <thead>
         <row>
          <entry colname="1">
           <para>
            Argumento
           </para>
          </entry>
          <entry colname="2">
           <para>
            Descripción
           </para>
          </entry>
         </row>
        </thead>
        <tbody>
         <row>
          <entry colname="1">
           <para>
            <literal>0</literal>
           </para>
          </entry>
          <entry colname="2">
           <para>
            Realiza un arranque normal
           </para>
          </entry>
         </row>
         <row>
          <entry colname="1">
           <para>
            <literal>4</literal>
           </para>
          </entry>
          <entry colname="2">
           <para>
            Realiza un arranque local con el controlador Universal Network Driver Interface (UNDI) aún residente en memoria
           </para>
          </entry>
         </row>
         <row>
          <entry colname="1">
           <para>
            <literal>5</literal>
           </para>
          </entry>
          <entry colname="2">
           <para>
            Realiza un arranque local con el stack de PXE completo, incluido el controlador UNDI, aún residente en memoria
           </para>
          </entry>
         </row>
        </tbody>
       </tgroup>
      </informaltable>
      <para>
       Los demás valores no están definidos. Si desconoce los stacks UNDI o PXE, especifique <literal>0</literal>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>TIMEOUT <replaceable>tiempo límite</replaceable></option>
     </term>
     <listitem>
      <para>
       Indica cuánto tiempo deberá esperar en el indicador de inicio antes de arrancar automáticamente, en décimas de segundo. El tiempo límite queda cancelado si el usuario pulsa alguna tecla, en cuyo caso se asume que será este quien complete el comando iniciado. Un tiempo límite de cero inhabilita la opción de tiempo límite (es el ajuste por defecto). El máximo valor posible para el valor del tiempo límite es de 35996 (algo menos de una hora).
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>PROMPT<replaceable> valor_de_indicador</replaceable></option>
     </term>
     <listitem>
      <para>
       Si <option>valor de indicador</option> es 0, muestra el indicador de inicio solo si se pulsan las teclas <keycap function="shift">Mayús</keycap> o <keycap function="alt">Alt</keycap> o si están activados <keycap>Bloq Mayús</keycap> o <keycap>Bloq Despl</keycap> (es la opción por defecto). Si <option>valor_de_indicador</option> es 1, siempre se muestra el indicador de inicio.
      </para>
<screen>F2  <replaceable>filename</replaceable>
F1  <replaceable>filename</replaceable> 
..etc... 
F9  <replaceable>filename</replaceable>
F10 <replaceable>filename</replaceable></screen>
      <para>
       Muestra en la pantalla el archivo indicado cuando se pulsa una tecla de función en el indicador de inicio. También se puede utilizar para implementar una ayuda en línea para antes del arranque (normalmente para las opciones de la línea de comandos del núcleo). Por compatibilidad con versiones anteriores, <keycap>F10</keycap> también puede introducirse como <keycap>F0</keycap>. Tenga en cuenta que no es posible asociar nombres de archivos a <keycap>F11</keycap> ni <keycap>F12</keycap>.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.pxeprep">
   <title>Preparación del sistema de destino para arranque en PXE</title>
   <para>
    Prepare el BIOS del sistema para arranque en PXE incluyendo la opción de PXE en el orden de arranque del BIOS.
   </para>
   <warning>
    <title>orden de arranque del BIOS</title>
    <para>
     No coloque la opción de PXE por encima de la opción de arranque desde disco duro en el BIOS. De lo contrario, el sistema intentará reinstalarse cada vez que lo arranque.
    </para>
   </warning>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.wolprep">
   <title>Preparación del sistema de destino para Wake on LAN</title>
   <para>
    Wake on LAN (WOL) necesita que se habilite la opción correspondiente del BIOS antes de la instalación. Además, es necesario tomar nota de la dirección MAC del sistema de destino. Este dato es necesario para iniciar Wake on LAN.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.wol">
   <title>Wake on LAN</title>
   <para>
    Wake on LAN permite encender una máquina mediante el envío de un paquete de red especial que contiene la dirección MAC de la máquina. Dado que los identificadores MAC deben ser únicos para cada máquina, no hay que preocuparse si se conecta accidentalmente una máquina equivocada.
   </para>
   <important>
    <title>Wake on LAN en distintos segmentos de red</title>
    <para>
     Si la máquina de control no se encuentra en el mismo segmento de red que el destino de la instalación que debe encenderse, configure las peticiones WOL para que se envíen como multidifusión o bien controle remotamente una máquina de dicho segmento de red para que actúe como remitente de las peticiones.
    </para>
   </important>
   <para>
    Los usuarios de SUSE Linux Enterprise Server pueden usar un módulo YaST llamado WOL para configurar Wake on LAN fácilmente. Los usuarios de otras versiones de sistemas operativos basados en SUSE Linux pueden usar una herramienta de línea de comandos.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.wolyast">
   <title>Wake on LAN con YaST</title>
   <procedure id="proc.deployment.remoteinst.boot.wolyast">
    <step performance="required">
     <para>
      Entre a la sesión como usuario <systemitem class="username">root</systemitem>.
     </para>
    </step>
    <step performance="required">
     <para>
      Inicie <menuchoice><guimenu>YaST</guimenu><guimenu>Servicios de red</guimenu><guimenu>WOL</guimenu></menuchoice>.
     </para>
    </step>
    <step performance="required">
     <para>
      Haga clic en <guimenu>Añadir</guimenu> y escriba el nombre de host y la dirección MAC del sistema de destino.
     </para>
    </step>
    <step performance="required">
     <para>
      Para encender este equipo, seleccione la entrada adecuada y haga clic en <guimenu>Wake up.</guimenu>
     </para>
    </step>
   </procedure>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.bootinst">
  <title>Arranque del sistema de destino para la instalación</title>

  <para>
   Existen dos maneras de personalizar el proceso de arranque para la instalación, además de las mencionadas en la <xref linkend="sec.deployment.remoteinst.boot.wol"/> y en la <xref linkend="sec.deployment.remoteinst.boot.pxe"/>. Es posible utilizar las opciones de arranque por defecto y las teclas de función, o bien utilizar el indicador de opciones de arranque de la pantalla de arranque de la instalación para pasar las opciones de arranque que el núcleo de la instalación pueda necesitar para este hardware en concreto.
  </para>

  <sect2 id="sec.deployment.remoteinst.bootinst.opts">
   <title>Uso de las opciones de arranque por defecto</title>
   <para>
    Las opciones de arranque se describen en detalle en el <xref linkend="cha.inst"/>. En general, el proceso de arranque de la instalación se inicia al seleccionar <guimenu>Instalación.</guimenu>
   </para>
   <para>
    Si se detectan problemas, utilice <guimenu>Instalación - ACPI desactivado</guimenu> o <guimenu>Instalación - Ajustes seguros.</guimenu> Para obtener más información acerca de resolución de problemas durante el proceso de instalación, consulte la <xref linkend="sec.trouble.install"/>.
   </para>
   <para>
    La barra de menú de la parte inferior de la pantalla ofrece algunas funciones avanzadas que son necesarias en determinadas configuraciones. Con las teclas de función puede especificar opciones adicionales para pasarlas a las rutinas de instalación sin necesidad de conocer la sintaxis detallada de estos parámetros (consulte la <xref linkend="sec.deployment.remoteinst.bootinst.custom"/>). La <xref linkend="sec.i.yast2.bootscreen"/> incluye una descripción detallada de las teclas de función disponibles.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.bootinst.custom">
   <title>Uso de opciones de arranque personalizadas</title>
   <para>
    El uso de las opciones de arranque adecuadas facilita el procedimiento de instalación. Muchos parámetros también pueden configurarse con posterioridad mediante las rutinas de linuxrc, pero el uso de opciones de arranque es más sencillo. En algunas configuraciones automáticas, las opciones de arranque pueden incorporarse con <filename>initrd</filename> o con un archivo <filename>info</filename>.
   </para>
   <para>
    La siguiente tabla muestra las situaciones de instalación comentadas en el presente capítulo junto con los parámetros necesarios para el arranque y las correspondientes opciones de arranque. Añádalas en el orden en el que aparecen en la tabla para obtener una cadena de opciones de arranque que se pasará a las rutinas de instalación. Por ejemplo (en una sola línea):
   </para>
<screen>install=<replaceable>xxx</replaceable> netdevice<replaceable>=xxx</replaceable> hostip=<replaceable>xxx</replaceable> netmask=<replaceable>xxx</replaceable> vnc=<replaceable>xxx</replaceable> vncpassword=<replaceable>xxx</replaceable></screen>

   <para>
    Sustituya todos los valores <replaceable>xxx</replaceable> de esta cadena con los valores correspondientes de su configuración.
   </para>
   <variablelist id="tab.deployment.remoteinst.bootinst.custom">
    <varlistentry>
     <term><xref linkend="cha.inst"/>
     </term>
     <listitem>
      <formalpara>
       <title>Parámetros necesarios para el arranque</title>
       <para>
        Ninguna
       </para>
      </formalpara>
      <formalpara>
       <title>Opciones de arranque</title>
       <para>
        No se necesita ninguna
       </para>
      </formalpara>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.vncstat"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Parámetros necesarios para el arranque</title>
       <listitem>
        <para>
         Ubicación del servidor de la instalación
        </para>
       </listitem>
       <listitem>
        <para>
         Dispositivo de red
        </para>
       </listitem>
       <listitem>
        <para>
         Dirección IP
        </para>
       </listitem>
       <listitem>
        <para>
         Máscara de red
        </para>
       </listitem>
       <listitem>
        <para>
         Gateway
        </para>
       </listitem>
       <listitem>
        <para>
         Habilitación de VNC
        </para>
       </listitem>
       <listitem>
        <para>
         Contraseña de VNC
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Opciones de arranque</title>
       <listitem>
        <para>
         <filename>install=(nfs,http,ftp,smb)://<replaceable>via_a_mediosinst</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netdevice=<replaceable>algun_dispositivo_de_red</replaceable></filename> (solo es necesario si hay varios dispositivos de red disponibles)
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>hostip=<replaceable>alguna_ip</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netmask=<replaceable>alguna_mascara_de_red</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>gateway=<replaceable>gateway_ip</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>vnc=1</filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>vncpassword=<replaceable>alguna_contraseña</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.vncdyn"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Parámetros necesarios para el arranque</title>
       <listitem>
        <para>
         Ubicación del servidor de la instalación
        </para>
       </listitem>
       <listitem>
        <para>
         Habilitación de VNC
        </para>
       </listitem>
       <listitem>
        <para>
         Contraseña de VNC
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Opciones de arranque</title>
       <listitem>
        <para>
         <filename>install=(nfs,http,ftp,smb)://<replaceable>via_a_mediosinst</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <literal>vnc=1</literal>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>vncpassword=<replaceable>alguna_contraseña</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.vncpxe"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Parámetros necesarios para el arranque</title>
       <listitem>
        <para>
         Ubicación del servidor de la instalación
        </para>
       </listitem>
       <listitem>
        <para>
         Ubicación del servidor TFTP
        </para>
       </listitem>
       <listitem>
        <para>
         Habilitación de VNC
        </para>
       </listitem>
       <listitem>
        <para>
         Contraseña de VNC
        </para>
       </listitem>
      </itemizedlist>
      <formalpara>
       <title>Opciones de arranque</title>
       <para>
        No aplicable; el proceso se gestiona mediante PXE y DHCP
       </para>
      </formalpara>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.sshstat"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Parámetros necesarios para el arranque</title>
       <listitem>
        <para>
         Ubicación del servidor de la instalación
        </para>
       </listitem>
       <listitem>
        <para>
         Dispositivo de red
        </para>
       </listitem>
       <listitem>
        <para>
         Dirección IP
        </para>
       </listitem>
       <listitem>
        <para>
         Máscara de red
        </para>
       </listitem>
       <listitem>
        <para>
         Gateway
        </para>
       </listitem>
       <listitem>
        <para>
         Habilitación de SSH
        </para>
       </listitem>
       <listitem>
        <para>
         Contraseña SSH
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Opciones de arranque</title>
       <listitem>
        <para>
         <filename>install=(nfs,http,ftp,smb)://<replaceable>via_a_mediosinst</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netdevice=<replaceable>algun_dispositivo_de_red</replaceable></filename> (solo es necesario si hay varios dispositivos de red disponibles)
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>hostip=<replaceable>alguna_ip</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netmask=<replaceable>alguna_mascara_de_red</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>gateway=<replaceable>gateway_ip</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <literal>usessh=1</literal>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>sshpassword=<replaceable>alguna_contraseña</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.sshdyn"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Parámetros necesarios para el arranque</title>
       <listitem>
        <para>
         Ubicación del servidor de la instalación
        </para>
       </listitem>
       <listitem>
        <para>
         Habilitación de SSH
        </para>
       </listitem>
       <listitem>
        <para>
         Contraseña SSH
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Opciones de arranque</title>
       <listitem>
        <para>
         <filename>install=(nfs,http,ftp,smb)://<replaceable>via_a_mediosinst</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>usessh=1</filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>sshpassword=<replaceable>alguna_contraseña</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.sshpxe"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <listitem>
        <para>
         Ubicación del servidor de la instalación
        </para>
       </listitem>
       <listitem>
        <para>
         Ubicación del servidor TFTP
        </para>
       </listitem>
       <listitem>
        <para>
         Habilitación de SSH
        </para>
       </listitem>
       <listitem>
        <para>
         Contraseña SSH
        </para>
       </listitem>
      </itemizedlist>
      <formalpara>
       <title>Opciones de arranque</title>
       <para>
        No aplicable; el proceso se gestiona mediante PXE y DHCP
       </para>
      </formalpara>
     </listitem>
    </varlistentry>
   </variablelist>
   <tip>
    <title>información adicional sobre las opciones de arranque de linuxrc</title>
    <para>
     Puede encontrar más información acerca de las opciones de arranque de linuxrc que se utilizan para arrancar sistemas Linux en <ulink url="http://en.opensuse.org/SDB:Linuxrc"/>.
    </para>
   </tip>
   <sect3 id="sec.deployment.remoteinst.bootinst.custom.addon">
    <title>Instalación de productos adicionales y actualizaciones de controladores</title>
    <para>
     <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> admite la instalación de productos adicionales que proporcionan extensiones (por ejemplo, SUSE Linux Enterprise High Availability Extension), productos de otros fabricantes, así como controladores o software adicional. Para poder instalar automáticamente un producto adicional al distribuir <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> de forma remota, especifique el parámetro <literal>addon=<replaceable>REPOSITORIO</replaceable></literal>.
    </para>
    <para>
     <replaceable>REPOSITORIO</replaceable> debe ser un repositorio alojado que pueda leer YaST (YaST2 o YUM (rpm-md)). De momento, no se admiten imágenes ISO.
    </para>
    <tip>
     <title>actualizaciones de controladores</title>
     <para>
      Las actualizaciones de controladores se encuentran en <ulink url="http://drivers.suse.com/"/>. No todas las actualizaciones de controladores se ofrecen como repositorios. Algunas solo están disponibles como imágenes ISO y no es posible instalarlas mediante el parámetro <literal>addon</literal>. Encontrará instrucciones acerca de cómo instalar actualizaciones de controladores mediante imágenes ISO en <ulink url="http://drivers.suse.com/doc/SolidDriver/Driver_Kits.html"/>.
     </para>
    </tip>
   </sect3>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.monitor">
  <title>Supevisión del proceso de instalación</title>

  <para>
   Existen varias opciones para supervisar de manera remota el proceso de instalación. Si se especifican las opciones de arranque adecuadas durante el arranque para la instalación, se puede utilizar VNC o bien SSH para controlar la instalación y la configuración del sistema desde una estación de trabajo remota.
  </para>

  <sect2 id="sec.deployment.remoteinst.monitor.vnc">
   <title>Instalación de VNC</title>
   <para>
    Mediante cualquier software de visor de VNC es posible controlar de forma remota la instalación de <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> desde prácticamente cualquier sistema operativo. En esta sección se describe la configuración cuando se utiliza una aplicación para la visualización de VNC o un navegador Web.
   </para>
   <sect3 id="sec.deployment.remoteinst.monitor.vnc.prep">
    <title>Preparación para la instalación con VNC</title>
    <para>
     Todo lo que hay que hacer en el destino de la instalación para prepararlo para una instalación con VNC es incorporar las opciones de arranque correspondientes en el arranque para la instalación inicial (consulte la <xref linkend="sec.deployment.remoteinst.bootinst.custom"/>). El sistema de destino arranca en un entorno basado en texto y espera a que un cliente VNC se conecte al programa de instalación.
    </para>
    <para>
     El programa de instalación muestra la dirección IP y el número de pantalla a los que es necesario conectarse para la instalación. Si se tiene acceso físico al sistema de destino, esta información se introduce inmediatamente después de que el sistema arranque para la instalación. Introduzca los datos cuando el software cliente VNC los solicite, así como la contraseña VNC.
    </para>
    <para>
     Dado que el destino de la instalación se anuncia a sí mismo mediante OpenSLP, es posible obtener información de la dirección del destino de la instalación a través de un navegador SLP, sin necesidad de acceder físicamente a la instalación en sí, siempre y cuando la configuración de la red y todos los equipos admitan OpenSLP:
    </para>
    <procedure id="proc.deployment.remoteinst.monitor.vnc.prep">
     <title>Localización de las instalaciones de VNC mediante OpenSLP</title>
     <step performance="required">
      <para>
       Ejecute el comando <command>slptool findsrvtypes | grep vnc</command> para obtener una lista de todos los servicios que ofrecen VNC. Los destinos de instalación de VNC deben estar disponibles en un servicio denominado <literal>YaST.installation.suse</literal>.
      </para>
     </step>
     <step performance="required">
      <para>
       Ejecute el comando <command>slptool findsrvs <replaceable>YaST.installation.suse</replaceable></command> para obtener una lista de las instalaciones disponibles. Use la dirección IP y el puerto (suele ser el <systemitem>5901</systemitem>) proporcionados con el visor de VNC.
      </para>
     </step>
    </procedure>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.monitor.vnc.connect">
    <title>Conexión al programa de instalación</title>
    <para>
     Existen dos maneras de conectarse al servidor VNC (en este caso, el destino de la instalación). Es posible iniciar una aplicación para la visualización de VNC independiente en cualquier sistema operativo o bien conectarse mediante un navegador Web con Java habilitado.
    </para>
    <para>
     Gracias a VNC es posible controlar la instalación de un sistema Linux desde cualquier otro sistema operativo, incluidas otras versiones de Linux, Windows y Mac OS.
    </para>
    <para>
     En un sistema Linux, asegúrese de que el paquete <systemitem>tightvnc</systemitem> se encuentra instalado. En un sistema Windows, instale el puerto Windows de esta aplicación, que puede obtenerse en la página principal de TightVNC (<ulink url="http://www.tightvnc.com/download.html"/>).
    </para>
    <procedure id="proc.deployment.remoteinst.monitor.vnc.connect.app">
     <para>
      Para conectarse al programa de instalación que se ejecuta en la máquina de destino, siga estos pasos:
     </para>
     <step performance="required">
      <para>
       Inicie el visualizador de VNC.
      </para>
     </step>
     <step performance="required">
      <para>
       Introduzca la dirección IP y el número de pantalla del destino de la instalación ofrecido por el navegador SLP o por el propio programa de instalación.

      </para>
<screen><replaceable>ip_address</replaceable>:<replaceable>display_number</replaceable></screen>
      <para>
       Se abrirá una ventana en el escritorio con las pantallas de YaST como en una instalación local normal:
      </para>
     </step>
    </procedure>
    <para>
     El uso de un navegador Web para conectarse al programa de instalación evita la dependencia de un software de VNC y del sistema operativo en el que se ejecute. Siempre que la compatibilidad con Java esté habilitada, podrá usar cualquier navegador (Firefox, Internet Explorer, Chromium, Opera, etc.) para realizar la instalación del sistema Linux.
    </para>
    <para>
     Siga este procedimiento para realizar una instalación VNC:
    </para>
    <procedure id="proc.deployment.remoteinst.monitor.vnc.connect.brow">
     <step performance="required">
      <para>
       Inicie el navegador Web que prefiera.
      </para>
     </step>
     <step performance="required">
      <para>
       Introduzca lo siguiente como dirección:

      </para>
<screen>http://<replaceable>ip_address_of_target</replaceable>:5801</screen>
     </step>
     <step performance="required">
      <para>
       Introduzca la contraseña de VNC cuando se le pida. La ventana del navegador mostrará las pantallas de YaST como en una instalación local normal.
      </para>
     </step>
    </procedure>
   </sect3>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.monitor.ssh">
   <title>Instalación con SSH</title>
   <para>
    Gracias a SSH, es posible controlar de forma remota la instalación del sistema Linux mediante cualquier software cliente de SSH.
   </para>
   <sect3 id="sec.deployment.remoteinst.monitor.ssh.prep">
    <title>Preparación para la instalación con SSH</title>
    <para>
     Aparte de la instalación de los paquetes de software correspondientes (OpenSSH para Linux y PuTTY para Windows), es necesario pasar las opciones de arranque apropiadas para habilitar SSH para la instalación. Consulte la <xref linkend="sec.deployment.remoteinst.bootinst.custom"/> para obtener más información. OpenSSH se instala por defecto en todos los sistemas operativos basados en SUSE Linux.
    </para>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.monitor.ssh.connect">
    <title>Conexión al programa de instalación</title>
    <procedure id="proc.deployment.remoteinst.monitor.ssh.connect">
     <step performance="required">
      <para>
       Obtenga la dirección IP del destino de la instalación. Si dispone de acceso físico al equipo de destino, utilice la dirección IP que las rutinas de instalación muestran en la consola tras el arranque inicial. También es posible tomar la dirección IP asignada a este host en particular en la configuración del servidor DHCP.
      </para>
     </step>
     <step performance="required">
      <para>
       En la línea de comandos, introduzca el comando siguiente:
      </para>
<screen>ssh -X root@<replaceable>
ip_address_of_target</replaceable></screen>
      <para>
       Sustituya <replaceable>ip_address_of_target</replaceable> por la dirección IP real del destino de la instalación.
      </para>
     </step>
     <step performance="required">
      <para>
       Cuando se pida un nombre de usuario, introduzca <literal>root</literal>.
      </para>
     </step>
     <step performance="required">
      <para>
       Cuando se le pida una contraseña, introduzca la contraseña establecida mediante la opción de arranque de SSH. Una vez realizada correctamente la autenticación, aparecerá un indicador de línea de comandos correspondiente al destino de la instalación.
      </para>
     </step>
     <step performance="required">
      <para>
       Introduzca <command>yast</command> para iniciar el programa de instalación. Se abrirá una ventana que mostrará las pantallas normales de YaST, tal y como se describe en el <xref linkend="cha.inst"/>.
      </para>
     </step>
    </procedure>
   </sect3>
  </sect2>
 </sect1>
</chapter>
