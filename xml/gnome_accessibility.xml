<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter
[
  <!ENTITY % entities SYSTEM "entity-decl.ent">
    %entities;
]>
<!-- Converted by suse-upgrade version 1.1 -->
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.0" xml:id="cha.gnome.accessibility">
<!--  <para>
   For a more detailed guide, see:
   <ulink
    url="http://library.gnome.org/users/gnome-access-guide/"/>
  </para>

  <sect2 id="sec.desctop.appearence.enhancement">
   <title>Desktop Appearance Enhancement</title>
   <para>
    There are several ways to customize the appearance of the GNOME desktop.
    Themes are the most effective way to change the appearance of the
    desktop in a consistent manner. You can also customize different
    applications and the desktop to get the display settings that you need,
    in addition or as an alternative to themes. For details, see
    <xref linkend="cha.gnome.settings"/>.
   </para>
   <sect3 id="custom.components.desktop">
    <title>Font Settings of the Desktop</title>
    <para>
     If you have difficulties with the default font type and size on the
     desktop and desktop background, you can customize the font settings to
     suit your needs.
    </para>
    <para>
     To change the font, select <menuchoice> <guimenu>Computer</guimenu>
     <guimenu>Control Center</guimenu> <guimenu>Look and Feel</guimenu>
     <guimenu>Appearance</guimenu> </menuchoice>. The
     <guimenu>Fonts</guimenu> tab contains the following options:
    </para>
    <variablelist>
     <varlistentry>
      <term><guimenu>Application Font</guimenu>
      </term>
      <listitem>
       <para>
        <guimenu>Application Font</guimenu> selects a default font for the
        text that is displayed on the desktop, including the text displayed
        on the windows and dialogs associated with GNOME-compliant
        applications.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><guimenu>Document Font</guimenu>
      </term>
      <listitem>
       <para>
        <guimenu>Document Font</guimenu> selects a font for displaying
        documents.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><guimenu>Desktop Font</guimenu>
      </term>
      <listitem>
       <para>
        <guimenu> Desktop</guimenu> selects a font to use for the text that
        is displayed on the desktop background only.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><guimenu>Window Title Font</guimenu>
      </term>
      <listitem>
       <para>
        <guimenu>Window Title Font</guimenu> selects a font for the text
        that is displayed in the title bar area of windows.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><guimenu>Fixed Width Font</guimenu>
      </term>
      <listitem>
       <para>
        <guimenu>Fixed Width Font</guimenu> selects a font to use for
        editing documents.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><guimenu>Rendering</guimenu>
      </term>
      <listitem>
       <para>
        To specify how to render fonts on the desktop select either:
       </para>
       <itemizedlist>
        <listitem>
         <para>
          <guimenu>Monochrome</guimenu>,
         </para>
        </listitem>
        <listitem>
         <para>
          <guimenu>Best Shapes</guimenu>,
         </para>
        </listitem>
        <listitem>
         <para>
          <guimenu>Best Contrast</guimenu>
         </para>
        </listitem>
        <listitem>
         <para>
          or <guimenu>Subpixel Smoothing</guimenu>.
         </para>
        </listitem>
       </itemizedlist>
      </listitem>
     </varlistentry>
    </variablelist>
    <para>
     If you use large fonts, you may need to change the size of panes in
     some applications, such as the file manager and Help browser.
    </para>
    <para>
     By default, GNOME applications use the default font specified in the
     <guimenu>Appearance</guimenu> tool. Some applications allow this
     default font to be customized.
    </para>
   </sect3>
   <sect3 id="Custom.desktop.contrast">
    <title>Desktop Contrast</title>
    <para>
     If you have difficulties with the desktop contrast, you can change the
     contrast.
    </para>
    <para>
     To change change the contrast theme, select <menuchoice>
     <guimenu>Computer</guimenu> <guimenu>Control Center</guimenu>
     <guimenu>Look and Feel</guimenu> <guimenu>Appearance</guimenu>
     </menuchoice>. The <guimenu>Theme</guimenu> tab contains several high-
     and low-contrast themes.
    </para>
    <para>
     To change the desktop background to a solid color, select <menuchoice>
     <guimenu>Computer</guimenu> <guimenu>Control Center</guimenu>
     <guimenu>Look and Feel</guimenu> <guimenu>Appearance</guimenu>
     <guimenu>Background</guimenu> </menuchoice>.
    </para>
    <itemizedlist>
     <listitem>
      <para>
       Choose a color background from the wallpapers available.
      </para>
     </listitem>
     <listitem>
      <para>
       Set the <guimenu>Colors</guimenu> to <quote>Solid Color</quote>.
      </para>
     </listitem>
     <listitem>
      <para>
       Select a background color that suits your needs.
      </para>
     </listitem>
    </itemizedlist>
   </sect3>
   <sect3 id="custom.desktop.fonts">
    <title>A Large-Print Desktop</title>
    <para>
     If you have problems with the desktop print size, you can select the
     <guimenu>Large Print</guimenu> theme. To configure the large-print
     desktop, click <menuchoice> <guimenu>Computer</guimenu>
     <guimenu>Control Center</guimenu> <guimenu>Look and Feel</guimenu>
     <guimenu>Appearance</guimenu> <guimenu>Theme</guimenu> </menuchoice>
     and select <guimenu>Large Print</guimenu>.
    </para>
    <para>
     Click the <guimenu>Apply Font</guimenu> to increase the size of the
     font that is used on the desktop and on window frames.
    </para>
    <para>
     If you use applications that use panes, like the file manager and help
     browser, you may need to change the size of panes to accommodate the
     large print. See the online help for the appropriate application for
     more information.
    </para>
   </sect3>
  </sect2>
 </sect1>
 <sect1 id="mobil.impair">
  <title>Mobility Impairments</title>

  <para>
   Mobility impairments can have many causes. Poor muscle control or
   weakness can make using standard keyboards and mouse devices difficult.
   For instance, some people are unable to type two keys simultaneously,
   others hit multiple keys or bounce keys when pressing or releasing them.
   People who are able to use only one hand often have difficulties with
   some keyboard and mouse tasks.
  </para>

  <sect2 id="sec.access.mouse.behavior">
   <title>Mouse Behavior</title>
   <para>
    The following sections describe how to modify the behavior of the mouse
    to suit your needs.
   </para>
   <sect3 id="mouse.lefthand">
    <title>Mouse for Left-Handed Use</title>
    <para>
     If you have difficulties using the right-handed mouse, you can change
     the mouse setting to left-handed. To configure the mouse for
     left-handed use, select <menuchoice> <guimenu>Computer</guimenu>
     <guimenu>Control Center</guimenu> <guimenu>Hardware</guimenu>
     <guimenu>Mouse</guimenu> <guimenu> General</guimenu> </menuchoice> and
     select the <guimenu>Left-handed</guimenu> mouse orientation. When you
     select this option, the system swaps the functions of the left and the
     right mouse button.
    </para>
   </sect3>
   <sect3 id="access.double.click">
    <title>Double-Click Behavior</title>
    <para>
     If you have difficulties double-clicking, you can increase the period
     of time that the system between the first and the second click of a
     double-click. For example, if the double-click timeout setting is 0.4
     seconds, you must perform the second click of a double-click within 0.4
     seconds of the first click. If the second click occurs more than 0.4
     seconds after the first click, the system interprets the two clicks as
     two single clicks.
    </para>
    <para>
     To configure the double-click timeout setting , select <menuchoice>
     <guimenu>Computer</guimenu> <guimenu>Control Center</guimenu>
     <guimenu>Hardware</guimenu> <guimenu>Mouse</guimenu> <guimenu>
     General</guimenu> </menuchoice> and specify the <guimenu>Double-Click
     Timeout</guimenu>. Double-click the light bulb beneath the slider to
     test the setting. If you perform the two clicks of the double-click
     within the specified period, the light bulb energizes fully to display
     a yellow glow around the bulb. If you do not double-click within the
     timeout specified, the light bulb does not energize fully. You should
     increase the timeout setting and try again. When the light bulb
     energizes, the timeout setting is suitable for your needs.
    </para>
   </sect3>
   <sect3 id="mouse.speed">
    <title>Mouse Speed and Sensitivity</title>
    <para>
     If you have difficulties with the behavior of the pointer, you can
     change the speed and sensitivity of the pointer. To configure the speed
     and sensitivity of the pointer, select <menuchoice>
     <guimenu>Computer</guimenu> <guimenu> Control Center</guimenu>
     <guimenu>Hardware</guimenu> <guimenu>Mouse</guimenu> <guimenu>
     General</guimenu> </menuchoice>.
    </para>
    <para>
     Use the <guimenu>Acceleration</guimenu> slider to specify the mouse
     pointer speed when moving the mouse. Moving the slider to the right
     will increase speed and moving it to the left will decrease speed.
    </para>
    <para>
     Use the <guimenu>Sensitivity</guimenu> slider to specify how responsive
     the pointer is to movements of the mouse. Moving the slider to the
     right will increase sensitivity and moving it to the left will decrease
     sensitivity.
    </para>
    <para>
     Use the slider in the <guimenu>Drag and Drop</guimenu> section to
     modify the distance that you must move an item with the pointer before
     the action is interpreted as a drag and drop action.
    </para>
    <para>
     If you select a low acceleration setting, the pointer moves at a speed
     similar to the speed at which you are physically moving the mouse or
     other pointer device. This means that you need to physically move the
     mouse larger distances to cover the screen area. If you select a high
     setting, the pointer moves at a faster speed than the speed at which
     you physically move the mouse. This means that you need to physically
     move the mouse smaller distances to cover the screen area.
    </para>
   </sect3>
   <sect3 id="access.mouse">
    <title>Accessible Mouse</title>
    <para>
     Use the <guimenu>Mouse Preferences</guimenu> tool to configure the
     mouse accessibility options. To open the <guimenu>Mouse
     Preferences</guimenu> tool, choose <menuchoice>
     <guimenu>Computer</guimenu> <guimenu>Control Center</guimenu>
     <guimenu>Hardware</guimenu> <guimenu>Mouse</guimenu> </menuchoice>.
     Accessibility options can be enabled by selecting the
     <guimenu>Accessibility</guimenu> tab.
    </para>
   </sect3>
   <sect3 id="sec.accessible.keyboard">
    <title>Accessible Keyboard</title>
    <para>
     Use the <guimenu>Keyboard</guimenu> tool to configure the keyboard
     accessibility options. To open it, choose <menuchoice>
     <guimenu>Computer</guimenu> <guimenu>Control Center</guimenu>
     <guimenu>Hardware</guimenu> <guimenu>Keyboard</guimenu> </menuchoice>.
     Accessibility options can be enabled by selecting the
     <guimenu>Accessibility</guimenu> tab.
    </para>
   </sect3>
  </sect2>

  <sect2 id="mobil.gok">
   <title>GNOME On-Screen Keyboard</title>
   <para>
    The GNOME On-Screen Keyboard displays virtual keyboards on your desktop.
    You can use the standard pointer or an alternative pointing device to
    operate the virtual keyboards. GOK displays the following types of
    keyboards:
   </para>
   <itemizedlist>
    <listitem>
     <para>
      Composer Keyboards enable you to compose text. To type alphanumeric
      characters, select the characters on the composer keyboard.
     </para>
    </listitem>
   </itemizedlist>
   <itemizedlist>
    <listitem>
     <para>
      Dynamic Keyboards reflect the applications that are currently running
      on the desktop. For example, On-Screen Keyboard generates dynamic
      keyboards that contain keys to represent the applications that are
      running on your desktop or the menus that are contained in an
      application.
     </para>
    </listitem>
   </itemizedlist>
   <para>
    To start the On-Screen Keyboard, choose <menuchoice>
    <guimenu>Computer</guimenu> <guimenu>More Applications</guimenu>
    <guimenu>Tools</guimenu> <guimenu>On-Screen Keyboard</guimenu>
    </menuchoice>.
   </para>
   <note>
    <para>
     In order to use the GNOME On-Screen Keyboard, the Assistive
     Technologies need to be enabled as described under
     <xref
      linkend="sec.general.accessibility.enable"/>.
    </para>
   </note>
   <para>
    If you are an On-Screen Keyboard user, you cannot use any application in
    Full-Screen mode because the application window obscures the On-Screen
    Keyboard display.
   </para>
   <para>
    To resize the window for use with the On-Screen Keyboard application,
    perform the following steps:
   </para>
   <procedure>
    <step>
     <para>
      Do not enable the Full-Screen mode in the application.
     </para>
    </step>
    <step>
     <para>
      Give focus to the application window.
     </para>
    </step>
    <step>
     <para>
      Press <keycap>F10</keycap> to maximize the application.
     </para>
    </step>
   </procedure>
  </sect2>
 </sect1>
 <sect1 id="Orca">
  <title>Orca</title>

  <para>
   Orca is a flexible, extensible, and powerful assistive technology for
   people with visual impairments. Using various combinations of speech
   synthesis, braille and magnification, the Orca Screen Reader and Orca
   Magnifier application enable users with limited or no vision to use the
   GNOME desktop and associated applications. The screen reader also
   supports Braille.
  </para>

  <para>
   Start Orca by choosing <menuchoice> <guimenu>Computer</guimenu>
   <guimenu>More Applications</guimenu> <guimenu>Tools</guimenu>
   <guimenu>Orca</guimenu> </menuchoice>. When you run Orca for the first
   time, it will automatically enter setup mode where you can configure it
   according to your needs. The setup allows you to enable features like
   speech and braille support, word or key echoes and screen magnifier.
  </para>


  <para>
   Start Orca by typing <command>orca</command> in a terminal session
   window. You can do so from a virtual console window if you do not yet
   have access to the GUI. With the GUI installed you can also press
   <keycombo> <keycap function="alt"/> <keycap>F2</keycap>
   </keycombo> to bring up the <guimenu>Run</guimenu> dialog and then type
   the command <command>orca</command> (followed by any optional
   parameters) and press <keycap function="enter"/>. Orca will
   automatically enter text setup mode if you run it from a virtual console
   window or if your GUI environment is not yet set up for accessibility.
   </para>
   <para>
   To quit Orca press <keycombo> <keycap function="insert"/>
    <keycap>Q</keycap> </keycombo> (or <keycombo> <keycap>CapsLock</keycap>
    <keycap>Q</keycap> </keycombo> in laptop layout mode). A
   <guimenu>Confirmation</guimenu> dialog will appear.
   </para>
  
  <sect2 id="run.orca">
   <title>Setting up Orca</title>
   <para>
    When you run Orca for the first time, it will automatically enter setup
    mode. If you want to run setup at some later point, you can pass the
    <option>setup</option> option to Orca the next time you run it.
    Furthermore, while Orca is running you can press <keycombo>
    <keycap function="insert"/> <keycap function="space"/> </keycombo> to
    bring up Orca's configuration GUI. Finally, Orca provides a text setup
    utility that you can start by passing the <option>\-\-text-setup</option>
    option to Orca. All of these options will create a
    <filename>~/.orca/user-settings.py</filename> file that holds your
    Control Center and will also enable the accessibility infrastructure.
    You need to log out and log back in for the new setting to take effect.
   </para>

   <sect3 id="orca.braille">
    <title>Configuring Braille</title>
   <para>
     The braille tab in the <guimenu>Orca Preferences</guimenu> dialog allows
     you to customize various aspects of braille usage.
   </para>
   <para>
    The first control on the braille page is the <guimenu>Enable braille
      support</guimenu> check box. This check box toggles whether Orca
    will make use of a braille display.
   </para>
   <para>
    Orca's braille monitor provides an on-screen representation of what
    takes place on the braille display. This feature is mostly for
    demonstration purposes, but is also useful for Orca developers who do
    not have access to a braille display.
   </para>
   <para>
    The <guimenu>Abbreviated role names</guimenu> check box determines the
    manner in which role names are displayed and can be used to help
    conserve real estate on the braille display.
   </para>
   <para>
    When checked, this feature tells Orca to not present the
    <literal>$1</literal> string at the end of a line.
   </para>
   <para>
    The <guimenu>verbosity</guimenu> radio button group determines the
    amount of information that will be, in certain situations, converted to
    braille.
   </para>
  </sect3>

  <sect2 id="orca.magnifier">
   <title>Magnifier Page</title>
   <para>
    The magnifier page allows you to enable/disable magnification and
    specify how magnification is performed.
   </para>
   <para>
    The first control on the magnifier page is the <guimenu>Enable
    magnifier</guimenu> check box. This check box toggles whether 
    Orca will provide magnification. This option, along with the ability to
    enable speech and to enable braille support, allows Orca to be tailored
    to meet the needs of a wide variety of users.
   </para>
   <para>
    Two zoomer GUI controls determine magnification characteristics:
    <guimenu>Scale factor</guimenu>, which sets magnification power and
    <guimenu>Position</guimenu>, which sets the location and size of the
    magnifier window.
   </para>
  </sect2>
-->
 <title>Assistive Technologies</title>
 <info>
      <abstract>
        <para>
    The GNOME desktop includes assistive technologies to support users with
    various impairments and special needs, and to interact with common
    assistive devices. This chapter describes several assistive technology
    applications designed to meet the needs of users with physical
    disabilities like low vision or impaired motor skills.
   </para>
      </abstract>
      <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
        <dm:bugtracker>
        </dm:bugtracker>
        <dm:translation>yes</dm:translation>
      </dm:docmanager>
    </info>
    <sect1 xml:id="sec.general.accessibility.enable">
  <title>Enabling Assistive Technologies</title>

  <para>
   To configure accessibility features, open the GNOME Settings dialog (for
   example using <menuchoice> <guimenu>Applications</guimenu>
   <guimenu>System Tools</guimenu> <guimenu>Settings</guimenu></menuchoice>)
   and click <guimenu>Universal Access</guimenu>. Each assistive feature is
   enabled separately using this dialog.
  </para>

  <para>
   If you need a more direct access to individual assistive features, check
   <guimenu>Always Show Universal Access Menu</guimenu> in the
   <guimenu>Universal Access</guimenu> dialog. A new menu will appear on the
   bottom panel.
  </para>
 </sect1>
 <sect1 xml:id="sec.general.accessibility.visual">
  <title>Visual Impairments</title>

  <para>
   In the <guimenu>Visual Impairments</guimenu> section of the
   <guimenu>Universal Access</guimenu> dialog, you can enable features that
   help people with impaired vision.
  </para>

  <para>
   Turning <guimenu>High Contrast</guimenu> on enables high contrast black
   and white icons in the GNOME desktop.
  </para>

  <para>
   Turning <guimenu>Large Text</guimenu> on enlarges the font used in the
   user interface.
  </para>

  <para>
   Turning <guimenu>Zoom</guimenu> on enables a screen magnifier. You can
   set the desired magnification and magnifier behavior, including color
   effects.
<!--FIXME-->
  </para>

  <para>
   If the <guimenu>Screen Reader</guimenu> is turned on, any text under the
   cursor (as you move it) is read aloud.
  </para>

  <para>
   If the <guimenu>Sound Keys</guimenu> are turned on, a sound is played
   whenever Num Lock or Caps Lock are turned on.
  </para>
 </sect1>
 <sect1 xml:id="sec.general.accessibility.hearing">
  <title>Hearing Impairments</title>

  <para>
   In the <guimenu>Hearing Impairments</guimenu> section of the
   <guimenu>Universal Access</guimenu> dialog, you can enable features
   helping people with impaired hearing.
  </para>

  <para>
   If the <guimenu>Visual Alerts</guimenu> are turned on, a window title or
   the entire screen is flashed when an alert sound occurs.
  </para>
 </sect1>
 <sect1 xml:id="sec.general.accessibility.mobility">
  <title>Mobility Impairments</title>

  <para>
   In the <guimenu>Typing</guimenu> and <guimenu>Pointing and
   Clicking</guimenu> sections of the <guimenu>Universal Access</guimenu>
   dialog, you can enable features that help people with mobility
   impairments.
  </para>

  <para>
   If the <guimenu>Screen Keyboard</guimenu> is turned on, a virtual
   keyboard appears whenever you need to enter text. You can use the screen
   keyboard by clicking the virtual keys.
  </para>

  <para>
   Click <guimenu>Typing Assist</guimenu> to open a dialog where you can
   enable various features that make typing easier: sticky keys, slow keys
   and bounce keys. You can turn these features on individually.
  </para>

  <para>
   <guimenu>Sticky Keys</guimenu> allows you to type key combinations one
   key at a time rather than having to hold down all of the keys at once.
   For example, the <keycombo>
   <keycap function="alt"/><keycap function="tab"/></keycombo> shortcut
   switches between windows. Without sticky keys turned on, you have to hold
   down both keys at the same time. With sticky keys turned on, press
   <keycap function="alt"/> and then <keycap function="tab"/> to do the
   same.
  </para>

  <para>
   Turn on <guimenu>Slow Keys</guimenu> if you want a delay between pressing
   a key and the letter being displayed on the screen. This means that you
   need to hold down each key you want to type for a little while before it
   appears. Use slow keys if you accidentally press several keys at a time
   when you type, or if you find it difficult to press the right key on the
   keyboard first time.
  </para>

  <para>
   Turn on <guimenu>Bounce Keys</guimenu> to ignore key presses that are
   rapidly repeated. For example, if you have hand tremors which cause you
   to press a key multiple times when you only want to press it once, you
   should turn on bounce keys.
  </para>

  <para>
   Turn on <guimenu>Mouse Keys</guimenu> to control the mouse pointer using
   the numeric keypad on your keyboard.
  </para>

  <para>
   Click <guimenu>Click Assist</guimenu> to open a dialog where you can
   enable various features that make clicking easier: simulated secondary
   click and hover click.
  </para>

  <para>
   Turn on <guimenu>Simulated Secondary Click</guimenu> to activate the
   secondary click (usually the right mouse button) by holding down the
   primary button for a predefined <guimenu>Acceptance delay</guimenu>. This
   is useful if you find it difficult to move your fingers individually on
   one hand, or if your pointing device only has a single button.
  </para>

  <para>
   Turn on <guimenu>Hover Click</guimenu> to trigger a click by hovering
   your mouse pointer over an object on the screen. This is useful if you
   find it difficult to move the mouse and click at the same time. If this
   feature is turned on, a small Hover Click window opens and stays above
   all of your other windows. You can use this to choose what sort of click
   should happen when you hover. When you hover your mouse pointer over a
   button and don't move it, the pointer gradually changes color. When it
   has fully changed color, the button will be clicked.
  </para>
 </sect1>
<!--  <para>
   For a more detailed guide, see:
   <ulink
    url="http://library.gnome.org/users/gnome-access-guide/"/>
  </para>

  <sect2 id="sec.desctop.appearence.enhancement">
   <title>Desktop Appearance Enhancement</title>
   <para>
    There are several ways to customize the appearance of the GNOME desktop.
    Themes are the most effective way to change the appearance of the
    desktop in a consistent manner. You can also customize different
    applications and the desktop to get the display settings that you need,
    in addition or as an alternative to themes. For details, see
    <xref linkend="cha.gnome.settings"/>.
   </para>
   <sect3 id="custom.components.desktop">
    <title>Font Settings of the Desktop</title>
    <para>
     If you have difficulties with the default font type and size on the
     desktop and desktop background, you can customize the font settings to
     suit your needs.
    </para>
    <para>
     To change the font, select <menuchoice> <guimenu>Computer</guimenu>
     <guimenu>Control Center</guimenu> <guimenu>Look and Feel</guimenu>
     <guimenu>Appearance</guimenu> </menuchoice>. The
     <guimenu>Fonts</guimenu> tab contains the following options:
    </para>
    <variablelist>
     <varlistentry>
      <term><guimenu>Application Font</guimenu>
      </term>
      <listitem>
       <para>
        <guimenu>Application Font</guimenu> selects a default font for the
        text that is displayed on the desktop, including the text displayed
        on the windows and dialogs associated with GNOME-compliant
        applications.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><guimenu>Document Font</guimenu>
      </term>
      <listitem>
       <para>
        <guimenu>Document Font</guimenu> selects a font for displaying
        documents.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><guimenu>Desktop Font</guimenu>
      </term>
      <listitem>
       <para>
        <guimenu> Desktop</guimenu> selects a font to use for the text that
        is displayed on the desktop background only.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><guimenu>Window Title Font</guimenu>
      </term>
      <listitem>
       <para>
        <guimenu>Window Title Font</guimenu> selects a font for the text
        that is displayed in the title bar area of windows.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><guimenu>Fixed Width Font</guimenu>
      </term>
      <listitem>
       <para>
        <guimenu>Fixed Width Font</guimenu> selects a font to use for
        editing documents.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><guimenu>Rendering</guimenu>
      </term>
      <listitem>
       <para>
        To specify how to render fonts on the desktop select either:
       </para>
       <itemizedlist>
        <listitem>
         <para>
          <guimenu>Monochrome</guimenu>,
         </para>
        </listitem>
        <listitem>
         <para>
          <guimenu>Best Shapes</guimenu>,
         </para>
        </listitem>
        <listitem>
         <para>
          <guimenu>Best Contrast</guimenu>
         </para>
        </listitem>
        <listitem>
         <para>
          or <guimenu>Subpixel Smoothing</guimenu>.
         </para>
        </listitem>
       </itemizedlist>
      </listitem>
     </varlistentry>
    </variablelist>
    <para>
     If you use large fonts, you may need to change the size of panes in
     some applications, such as the file manager and Help browser.
    </para>
    <para>
     By default, GNOME applications use the default font specified in the
     <guimenu>Appearance</guimenu> tool. Some applications allow this
     default font to be customized.
    </para>
   </sect3>
   <sect3 id="Custom.desktop.contrast">
    <title>Desktop Contrast</title>
    <para>
     If you have difficulties with the desktop contrast, you can change the
     contrast.
    </para>
    <para>
     To change change the contrast theme, select <menuchoice>
     <guimenu>Computer</guimenu> <guimenu>Control Center</guimenu>
     <guimenu>Look and Feel</guimenu> <guimenu>Appearance</guimenu>
     </menuchoice>. The <guimenu>Theme</guimenu> tab contains several high-
     and low-contrast themes.
    </para>
    <para>
     To change the desktop background to a solid color, select <menuchoice>
     <guimenu>Computer</guimenu> <guimenu>Control Center</guimenu>
     <guimenu>Look and Feel</guimenu> <guimenu>Appearance</guimenu>
     <guimenu>Background</guimenu> </menuchoice>.
    </para>
    <itemizedlist>
     <listitem>
      <para>
       Choose a color background from the wallpapers available.
      </para>
     </listitem>
     <listitem>
      <para>
       Set the <guimenu>Colors</guimenu> to <quote>Solid Color</quote>.
      </para>
     </listitem>
     <listitem>
      <para>
       Select a background color that suits your needs.
      </para>
     </listitem>
    </itemizedlist>
   </sect3>
   <sect3 id="custom.desktop.fonts">
    <title>A Large-Print Desktop</title>
    <para>
     If you have problems with the desktop print size, you can select the
     <guimenu>Large Print</guimenu> theme. To configure the large-print
     desktop, click <menuchoice> <guimenu>Computer</guimenu>
     <guimenu>Control Center</guimenu> <guimenu>Look and Feel</guimenu>
     <guimenu>Appearance</guimenu> <guimenu>Theme</guimenu> </menuchoice>
     and select <guimenu>Large Print</guimenu>.
    </para>
    <para>
     Click the <guimenu>Apply Font</guimenu> to increase the size of the
     font that is used on the desktop and on window frames.
    </para>
    <para>
     If you use applications that use panes, like the file manager and help
     browser, you may need to change the size of panes to accommodate the
     large print. See the online help for the appropriate application for
     more information.
    </para>
   </sect3>
  </sect2>
 </sect1>
 <sect1 id="mobil.impair">
  <title>Mobility Impairments</title>

  <para>
   Mobility impairments can have many causes. Poor muscle control or
   weakness can make using standard keyboards and mouse devices difficult.
   For instance, some people are unable to type two keys simultaneously,
   others hit multiple keys or bounce keys when pressing or releasing them.
   People who are able to use only one hand often have difficulties with
   some keyboard and mouse tasks.
  </para>

  <sect2 id="sec.access.mouse.behavior">
   <title>Mouse Behavior</title>
   <para>
    The following sections describe how to modify the behavior of the mouse
    to suit your needs.
   </para>
   <sect3 id="mouse.lefthand">
    <title>Mouse for Left-Handed Use</title>
    <para>
     If you have difficulties using the right-handed mouse, you can change
     the mouse setting to left-handed. To configure the mouse for
     left-handed use, select <menuchoice> <guimenu>Computer</guimenu>
     <guimenu>Control Center</guimenu> <guimenu>Hardware</guimenu>
     <guimenu>Mouse</guimenu> <guimenu> General</guimenu> </menuchoice> and
     select the <guimenu>Left-handed</guimenu> mouse orientation. When you
     select this option, the system swaps the functions of the left and the
     right mouse button.
    </para>
   </sect3>
   <sect3 id="access.double.click">
    <title>Double-Click Behavior</title>
    <para>
     If you have difficulties double-clicking, you can increase the period
     of time that the system between the first and the second click of a
     double-click. For example, if the double-click timeout setting is 0.4
     seconds, you must perform the second click of a double-click within 0.4
     seconds of the first click. If the second click occurs more than 0.4
     seconds after the first click, the system interprets the two clicks as
     two single clicks.
    </para>
    <para>
     To configure the double-click timeout setting , select <menuchoice>
     <guimenu>Computer</guimenu> <guimenu>Control Center</guimenu>
     <guimenu>Hardware</guimenu> <guimenu>Mouse</guimenu> <guimenu>
     General</guimenu> </menuchoice> and specify the <guimenu>Double-Click
     Timeout</guimenu>. Double-click the light bulb beneath the slider to
     test the setting. If you perform the two clicks of the double-click
     within the specified period, the light bulb energizes fully to display
     a yellow glow around the bulb. If you do not double-click within the
     timeout specified, the light bulb does not energize fully. You should
     increase the timeout setting and try again. When the light bulb
     energizes, the timeout setting is suitable for your needs.
    </para>
   </sect3>
   <sect3 id="mouse.speed">
    <title>Mouse Speed and Sensitivity</title>
    <para>
     If you have difficulties with the behavior of the pointer, you can
     change the speed and sensitivity of the pointer. To configure the speed
     and sensitivity of the pointer, select <menuchoice>
     <guimenu>Computer</guimenu> <guimenu> Control Center</guimenu>
     <guimenu>Hardware</guimenu> <guimenu>Mouse</guimenu> <guimenu>
     General</guimenu> </menuchoice>.
    </para>
    <para>
     Use the <guimenu>Acceleration</guimenu> slider to specify the mouse
     pointer speed when moving the mouse. Moving the slider to the right
     will increase speed and moving it to the left will decrease speed.
    </para>
    <para>
     Use the <guimenu>Sensitivity</guimenu> slider to specify how responsive
     the pointer is to movements of the mouse. Moving the slider to the
     right will increase sensitivity and moving it to the left will decrease
     sensitivity.
    </para>
    <para>
     Use the slider in the <guimenu>Drag and Drop</guimenu> section to
     modify the distance that you must move an item with the pointer before
     the action is interpreted as a drag and drop action.
    </para>
    <para>
     If you select a low acceleration setting, the pointer moves at a speed
     similar to the speed at which you are physically moving the mouse or
     other pointer device. This means that you need to physically move the
     mouse larger distances to cover the screen area. If you select a high
     setting, the pointer moves at a faster speed than the speed at which
     you physically move the mouse. This means that you need to physically
     move the mouse smaller distances to cover the screen area.
    </para>
   </sect3>
   <sect3 id="access.mouse">
    <title>Accessible Mouse</title>
    <para>
     Use the <guimenu>Mouse Preferences</guimenu> tool to configure the
     mouse accessibility options. To open the <guimenu>Mouse
     Preferences</guimenu> tool, choose <menuchoice>
     <guimenu>Computer</guimenu> <guimenu>Control Center</guimenu>
     <guimenu>Hardware</guimenu> <guimenu>Mouse</guimenu> </menuchoice>.
     Accessibility options can be enabled by selecting the
     <guimenu>Accessibility</guimenu> tab.
    </para>
   </sect3>
   <sect3 id="sec.accessible.keyboard">
    <title>Accessible Keyboard</title>
    <para>
     Use the <guimenu>Keyboard</guimenu> tool to configure the keyboard
     accessibility options. To open it, choose <menuchoice>
     <guimenu>Computer</guimenu> <guimenu>Control Center</guimenu>
     <guimenu>Hardware</guimenu> <guimenu>Keyboard</guimenu> </menuchoice>.
     Accessibility options can be enabled by selecting the
     <guimenu>Accessibility</guimenu> tab.
    </para>
   </sect3>
  </sect2>

  <sect2 id="mobil.gok">
   <title>GNOME On-Screen Keyboard</title>
   <para>
    The GNOME On-Screen Keyboard displays virtual keyboards on your desktop.
    You can use the standard pointer or an alternative pointing device to
    operate the virtual keyboards. GOK displays the following types of
    keyboards:
   </para>
   <itemizedlist>
    <listitem>
     <para>
      Composer Keyboards enable you to compose text. To type alphanumeric
      characters, select the characters on the composer keyboard.
     </para>
    </listitem>
   </itemizedlist>
   <itemizedlist>
    <listitem>
     <para>
      Dynamic Keyboards reflect the applications that are currently running
      on the desktop. For example, On-Screen Keyboard generates dynamic
      keyboards that contain keys to represent the applications that are
      running on your desktop or the menus that are contained in an
      application.
     </para>
    </listitem>
   </itemizedlist>
   <para>
    To start the On-Screen Keyboard, choose <menuchoice>
    <guimenu>Computer</guimenu> <guimenu>More Applications</guimenu>
    <guimenu>Tools</guimenu> <guimenu>On-Screen Keyboard</guimenu>
    </menuchoice>.
   </para>
   <note>
    <para>
     In order to use the GNOME On-Screen Keyboard, the Assistive
     Technologies need to be enabled as described under
     <xref
      linkend="sec.general.accessibility.enable"/>.
    </para>
   </note>
   <para>
    If you are an On-Screen Keyboard user, you cannot use any application in
    Full-Screen mode because the application window obscures the On-Screen
    Keyboard display.
   </para>
   <para>
    To resize the window for use with the On-Screen Keyboard application,
    perform the following steps:
   </para>
   <procedure>
    <step>
     <para>
      Do not enable the Full-Screen mode in the application.
     </para>
    </step>
    <step>
     <para>
      Give focus to the application window.
     </para>
    </step>
    <step>
     <para>
      Press <keycap>F10</keycap> to maximize the application.
     </para>
    </step>
   </procedure>
  </sect2>
 </sect1>
 <sect1 id="Orca">
  <title>Orca</title>

  <para>
   Orca is a flexible, extensible, and powerful assistive technology for
   people with visual impairments. Using various combinations of speech
   synthesis, braille and magnification, the Orca Screen Reader and Orca
   Magnifier application enable users with limited or no vision to use the
   GNOME desktop and associated applications. The screen reader also
   supports Braille.
  </para>

  <para>
   Start Orca by choosing <menuchoice> <guimenu>Computer</guimenu>
   <guimenu>More Applications</guimenu> <guimenu>Tools</guimenu>
   <guimenu>Orca</guimenu> </menuchoice>. When you run Orca for the first
   time, it will automatically enter setup mode where you can configure it
   according to your needs. The setup allows you to enable features like
   speech and braille support, word or key echoes and screen magnifier.
  </para>


  <para>
   Start Orca by typing <command>orca</command> in a terminal session
   window. You can do so from a virtual console window if you do not yet
   have access to the GUI. With the GUI installed you can also press
   <keycombo> <keycap function="alt"/> <keycap>F2</keycap>
   </keycombo> to bring up the <guimenu>Run</guimenu> dialog and then type
   the command <command>orca</command> (followed by any optional
   parameters) and press <keycap function="enter"/>. Orca will
   automatically enter text setup mode if you run it from a virtual console
   window or if your GUI environment is not yet set up for accessibility.
   </para>
   <para>
   To quit Orca press <keycombo> <keycap function="insert"/>
    <keycap>Q</keycap> </keycombo> (or <keycombo> <keycap>CapsLock</keycap>
    <keycap>Q</keycap> </keycombo> in laptop layout mode). A
   <guimenu>Confirmation</guimenu> dialog will appear.
   </para>
  
  <sect2 id="run.orca">
   <title>Setting up Orca</title>
   <para>
    When you run Orca for the first time, it will automatically enter setup
    mode. If you want to run setup at some later point, you can pass the
    <option>setup</option> option to Orca the next time you run it.
    Furthermore, while Orca is running you can press <keycombo>
    <keycap function="insert"/> <keycap function="space"/> </keycombo> to
    bring up Orca's configuration GUI. Finally, Orca provides a text setup
    utility that you can start by passing the <option>\-\-text-setup</option>
    option to Orca. All of these options will create a
    <filename>~/.orca/user-settings.py</filename> file that holds your
    Control Center and will also enable the accessibility infrastructure.
    You need to log out and log back in for the new setting to take effect.
   </para>

   <sect3 id="orca.braille">
    <title>Configuring Braille</title>
   <para>
     The braille tab in the <guimenu>Orca Preferences</guimenu> dialog allows
     you to customize various aspects of braille usage.
   </para>
   <para>
    The first control on the braille page is the <guimenu>Enable braille
      support</guimenu> check box. This check box toggles whether Orca
    will make use of a braille display.
   </para>
   <para>
    Orca's braille monitor provides an on-screen representation of what
    takes place on the braille display. This feature is mostly for
    demonstration purposes, but is also useful for Orca developers who do
    not have access to a braille display.
   </para>
   <para>
    The <guimenu>Abbreviated role names</guimenu> check box determines the
    manner in which role names are displayed and can be used to help
    conserve real estate on the braille display.
   </para>
   <para>
    When checked, this feature tells Orca to not present the
    <literal>$1</literal> string at the end of a line.
   </para>
   <para>
    The <guimenu>verbosity</guimenu> radio button group determines the
    amount of information that will be, in certain situations, converted to
    braille.
   </para>
  </sect3>

  <sect2 id="orca.magnifier">
   <title>Magnifier Page</title>
   <para>
    The magnifier page allows you to enable/disable magnification and
    specify how magnification is performed.
   </para>
   <para>
    The first control on the magnifier page is the <guimenu>Enable
    magnifier</guimenu> check box. This check box toggles whether 
    Orca will provide magnification. This option, along with the ability to
    enable speech and to enable braille support, allows Orca to be tailored
    to meet the needs of a wide variety of users.
   </para>
   <para>
    Two zoomer GUI controls determine magnification characteristics:
    <guimenu>Scale factor</guimenu>, which sets magnification power and
    <guimenu>Position</guimenu>, which sets the location and size of the
    magnifier window.
   </para>
  </sect2>
-->
</chapter>