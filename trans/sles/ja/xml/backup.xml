<?xml version="1.0"?>
<!--
*********************************
Please see LICENSE.txt for this document's license.
*********************************
-->
<chapter xml:base="backup.xml" id="cha.userbackup">
 <title>ユーザデータのバックアップ</title>
 <abstract>
  <para>
   バックアップツールは、ユーザがホームディレクトリや選択したファイルなどのデータをバックアップおよび復元できるようにするシンプルなフレームワークです。スケジュールされたバックアップまたは要求に応じたバックアップを作成し、このデータの以前の状態を再生できます。
  </para>
 </abstract>
 <sect1 id="cha.userbackup.backup">
  <title>バックアップの作成</title>

  <para>
   まず、バックアップするデータと、バックアップをいつ実行するかをスケジュールします。
  </para>

  <procedure>
   <step performance="required">
    <para>
     <menuchoice> <guimenu>Applications (アプリケーション)</guimenu> <guimenu>Utilities (ユーティリティ)</guimenu> <guimenu>Backup (バックアップ)</guimenu></menuchoice>の順にクリックします。
    </para>
   </step>
   <step performance="required">
    <para>
     <guimenu>Overview (概要)</guimenu>タブで、<guimenu>Automatic backups (自動バックアップ)</guimenu>をオン/オフにできます。また、現在の設定の概要も参照できます。<guimenu>Back Up Now (今すぐバックアップ)</guimenu>を使用して、スケジュールされていないバックアップを手動で実行することもできます。
    </para>
   </step>
   <step performance="required">
    <para>
     <guimenu>対象外</guimenu>タブで、バックアップから除外したいファイルとディレクトリを追加します。

    </para>
   </step>
   <step performance="required">
    <para>
     <guimenu>Folders (フォルダ)</guimenu>タブで、バックアップするディレクトリと無視するディレクトリを選択します。たとえば、<filename>Downloads</filename>ディレクトリ以外のホームディレクトリをバックアップする場合は、バックアップするホームディレクトリ、および無視する<filename>Downloads</filename>ディレクトリを追加します。
    </para>
   </step>
   <step performance="required">
    <para>
     <guimenu>Schedule (スケジュール)</guimenu>タブで、自動バックアップの実行頻度(毎日または毎週)と、バックアップを保持する期間を選択します。
    </para>
   </step>
  </procedure>
 </sect1>
 <sect1 id="cha.userbackup.restore">
  <title>データの復元</title>

  <para>
   データの以前の状態を復元するには、次の手順に従います。
  </para>

  <procedure>
   <step performance="required">
    <para>
     <menuchoice> <guimenu>Applications (アプリケーション)</guimenu> <guimenu>Utilities (ユーティリティ)</guimenu> <guimenu>Backup (バックアップ)</guimenu></menuchoice>の順に選択します。<guimenu>Overview (概要)</guimenu>タブで、<guimenu>復元</guimenu>をクリックします。
    </para>
   </step>
   <step performance="required">
    <para>
     復元する元の場所を選択します。<guimenu>進む</guimenu>をクリックします。
    </para>
   </step>
   <step performance="required">
    <para>
     日付を選択します。<guimenu>進む</guimenu>をクリックします。
    </para>
   </step>
   <step performance="required">
    <para>
     復元先の場所を選択します。<guimenu>復元</guimenu>をクリックします。
    </para>
   </step>
  </procedure>
 </sect1>
</chapter>
