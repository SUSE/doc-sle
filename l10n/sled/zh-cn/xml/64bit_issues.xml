<?xml version="1.0" encoding="UTF-8"?>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="64bit_issues.xml" version="5.0" xml:id="cha-64bit">
 <title>64 位系统环境中的 32 位和 64 位应用程序</title>

 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>
 <para>
  <phrase role="productname"><phrase os="sled">SUSE® Linux Enterprise Desktop</phrase></phrase> 可用于 64 位平台。开发商尚未将所有 32 位应用程序移植到 64 位系统。本章简要概述了如何在 64 位 <phrase role="productname"><phrase os="sled">SUSE Linux Enterprise Desktop</phrase></phrase> 平台上实现 32 位支持。
 </para>
 <para>
  <phrase os="sled;osuse"><phrase role="productname"><phrase os="sled">用于 64 位平台 AMD64 和 Intel 64 的 SUSE Linux Enterprise Desktop</phrase></phrase></phrase> 可让现有的 32 位应用程序<quote>无需额外配置</quote>即可在 64 位环境中运行。这种支持意味着您可以继续使用首选的 32 位应用程序，而无需等待对应的 64 位端口可用。
 </para>


 <note>
  <title>不支持构建 32 位应用程序</title>
  <para>
   <phrase role="productname"><phrase os="sled">SUSE Linux Enterprise Desktop</phrase></phrase> 不支持编译 32 位应用程序。它只为 32 位二进制文件提供运行时支持。
  </para>
 </note>


 <sect1 xml:id="sec-64bit-runt">
  <title>运行时支持</title>

  <important>
   <title>不同应用程序版本之间的冲突</title>
   <para>
    如果某个应用程序同时适用于 32 位和 64 位环境，安装这两个版本可能会导致出现问题。在这种情况下，请决定要安装哪一个版本，以避免潜在的运行时错误。
   </para>
   <para>
    此规则的一个例外是 PAM（可插入身份验证模块）。<phrase role="productname"><phrase os="sled">SUSE Linux Enterprise Desktop</phrase></phrase> 在身份验证过程中使用 PAM 充当用户和应用程序之间的中间层。在同时运行了 32 位应用程序的 64 位操作系统上，请始终安装 32 位和 64 位 PAM 版本。
   </para>
  </important>

  <para>
   每个应用程序都需要一系列的库才能正常执行。不巧的是，这些库的 32 位和 64 位版本的名称是相同的。必须通过另一种方法对它们加以区分。
  </para>

  <para>
   为了与 32 位版本保持兼容，64 位和 32 位库将储存在同一位置。在 32 位和 64 位环境中，<filename>libc.so.6</filename> 的 32 位版本都位于 <filename>/lib/libc.so.6</filename> 下。
  </para>

  <para>
   所有 64 位库和对象文件都位于名为 <filename>lib64</filename> 的目录中。以往通常出现在 <filename>/lib</filename> 和 <filename>/usr/lib</filename> 下的 64 位对象文件现在位于 <filename>/lib64</filename> 和 <filename>/usr/lib64</filename> 下。这意味着 <filename>/lib</filename> 和 <filename>/usr/lib</filename> 下有储存 32 位库的空间，因此两个版本的文件名都可以保持不变。
  </para>

  <para>
   如果 <filename>/lib</filename> 下的 32 位子目录的数据内容与字词大小无关，则不会移动这些子目录。此方案符合 LSB（Linux 标准库）和 FHS（文件系统层次标准）。
  </para>
 </sect1>
 <sect1 xml:id="sec-64bit-kernel">
  <title>内核规范</title>

  <para>
   适用于 AMD 64/Intel 64 的 64 位内核提供了 64 位和 32 位两种内核 ABI（应用程序二进制接口）。后者与对应的 32 位内核的 ABI 相同。这意味着，32 位和 64 位应用程序与 64 位内核的通讯方式是相同的。
  </para>

  <para>
   针对 64 位内核的 32 位系统调用仿真不支持系统程序使用的某些 API。这取决于平台。因此，必须对 <command>lspci</command> 之类的一些应用程序进行编译。
  </para>

  <para>
   64 位内核只能装载 64 位内核模块。必须专门为 64 位内核编译 64 位模块。无法将 32 位内核模块与 64 位内核搭配使用。
  </para>

  <tip>
   <title>内核可装载模块</title>
   <para>
    某些应用程序需要单独的内核可装载模块。如果您想要在 64 位系统环境中使用 32 位应用程序，请与应用程序供应商和 SUSE 联系。确保 64 位版本的内核可装载模块以及 32 位编译版本的内核 API 适用于此模块。
   </para>
  </tip>
 </sect1>
</chapter>
