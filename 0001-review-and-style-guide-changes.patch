From 9f390afd51dc3cc0bec03416b061eca4d86a8e7f Mon Sep 17 00:00:00 2001
From: Tanja Roth <taroth@suse.com>
Date: Fri, 27 Sep 2019 10:42:51 +0200
Subject: [PATCH] review and style guide changes

---
 xml/ay_bigfile.xml | 92 ++++++++++++++++++++++++++--------------------
 1 file changed, 53 insertions(+), 39 deletions(-)

diff --git a/xml/ay_bigfile.xml b/xml/ay_bigfile.xml
index a084251d4..fa85473d1 100644
--- a/xml/ay_bigfile.xml
+++ b/xml/ay_bigfile.xml
@@ -6686,13 +6686,17 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-          Boot protocol used by interface. Possible values:
-            <literal>static</literal> for statically assigned address. Needs to specify also <literal>ipaddr</literal> element.
-            <literal>dhcp</literal>,<literal>dhcp4</literal>,<literal>dhcp6</literal> for getting address both, respective IPv4 respective IPv6 only from DHCP server.
-            <literal>dhcp+autoip</literal> to get IPv4 from zeroconf and IPv6 from DHCP.
-            <literal>autoip</literal> to get IPv4 from zeroconf.
-            <literal>ibft</literal> to get address via iBFT protocol.
-            <literal>none</literal> to not get address. Useful for bridge and bond slaves.
+          Boot protocol used by the interface. Possible values:
+          <literal>static</literal> for statically assigned address. In this
+          case, you need to specify the <literal>ipaddr</literal> element, too.
+          Use <literal>dhcp</literal> to get both IPv4 and IPv6 addresses from
+          the DHCP server. Use <literal>dhcp4</literal> or <literal>dhcp6</literal> 
+          for getting only the respective address from the DHCP server.
+          <literal>dhcp+autoip</literal> gets the IPv4 address from zeroconf and 
+          the IPv6 address from DHCP. <literal>autoip</literal> gets the IPv4
+          address from zeroconf.  <literal>ibft</literal> to get an address via
+          <literal>iBFT</literal> protocol. Use <literal>none</literal> to skip
+          getting an address. This option is useful for bridge and bond slaves.
          </para>
         </entry>
         <entry>
@@ -6743,7 +6747,7 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           Device name e.g. <literal>eth0</literal>
+           Device name, for example, <literal>eth0</literal>.
          </para>
         </entry>
         <entry>
@@ -6765,7 +6769,8 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           Used only with <literal>static</literal> boot protocol. Can contain network prefix.
+           Used only with <literal>static</literal> boot protocol. Can contain a
+          network prefix.
          </para>
         </entry>
        </row>
@@ -6828,7 +6833,7 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           Network prefix e.g. <literal>24</literal>.
+           Network prefix, for example, <literal>24</literal>.
          </para>
         </entry>
         <entry>
@@ -6845,13 +6850,18 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           When start device. Possible values:
-            <literal>hotplug</literal> start when device is plugged in. Useful for e.g. USB network cards.
-            <literal>auto</literal>,<literal>onboot</literal> for starting when system start. <literal>onboot</literal> is deprecated alias.
-            <literal>ifplugd</literal> when device is managed by ifplugd daemon.
-            <literal>manual</literal> to start device only manually.
-            <literal>nfsroot</literal> for devices that is needed to mount root e.g. when root is on NFS volume.
-            <literal>off</literal> never start device.
+           When to start the device. Possible values:
+            <literal>hotplug</literal> means: start when the device is plugged in.
+            (Useful for USB network cards, for example.)
+            <literal>auto</literal> starts the device at system start.
+            (<literal>onboot</literal> is a deprecated alias for
+             <literal>auto</literal>.)
+            Use <literal>ifplugd</literal> if the device is managed by the
+            <systemitem class="daemon">fplugd</systemitem> daemon.
+            <literal>manual</literal> to start the device only manually.
+            Use <literal>nfsroot</literal> for devices that need to mount
+            root, for example, when the root file system is on an NFS volume.
+            <literal>off</literal> means: never start the device.
          </para>
         </entry>
         <entry>
@@ -6867,7 +6877,8 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           Priority for ifplugd daemon for order in which devices are activated.
+          Priority for <systemitem class="daemon">ifplugd</systemitem> daemon
+          (order in which the devices are activated).
          </para>
         </entry>
         <entry>
@@ -6884,29 +6895,31 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           No longer used parameter
+           Do no longer use.
          </para>
         </entry>
         <entry>
          <para>
-           deprecated
+           Deprecated.
          </para>
         </entry>
        </row>
        <row>
         <entry>
          <para>
-          bonding_slaveX
+          bonding_slave<replaceable>X</replaceable>
          </para>
         </entry>
         <entry>
          <para>
-           Name of bonding device.
+           Name of the bonding device.
          </para>
         </entry>
         <entry>
          <para>
-           Required for bond device. X is replaced by number from 0 e.g. <literal>bonding_slave0</literal> each slave have to have unique number.
+           Required for a bonding device. Each bonding slave has a unique number.
+           Replace <replaceable>X</replaceable> with the respective number, for
+          example: <literal>bonding_slave0</literal>.
          </para>
         </entry>
        </row>
@@ -6918,12 +6931,12 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           Options for bond device
+           Options for bonding device.
          </para>
         </entry>
         <entry>
          <para>
-           Used only with <literal>bond</literal> device.
+           Used only with a <literal>bonding</literal> device.
          </para>
         </entry>
        </row>
@@ -6935,7 +6948,7 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           Maximum transmission unit for given device
+           Maximum transmission unit for the given device.
          </para>
         </entry>
         <entry>
@@ -6951,7 +6964,7 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           Ethtool options during device activation
+           Ethtool options during device activation.
          </para>
         </entry>
         <entry>
@@ -6967,7 +6980,7 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           Firewall zone name to which device is attached
+           Firewall zone name to which the device is attached.
          </para>
         </entry>
         <entry>
@@ -6983,12 +6996,12 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           ID used for this VLAN
+           ID used for this VLAN.
          </para>
         </entry>
         <entry>
          <para>
-           Used only with <literal>vlan</literal> device.
+           Used only with a <literal>vlan</literal> device.
          </para>
         </entry>
        </row>
@@ -7005,7 +7018,7 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           Used only with <literal>vlan</literal> device and required for it.
+           Used only with a <literal>vlan</literal> device and required for it.
          </para>
         </entry>
        </row>
@@ -7017,12 +7030,12 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           "yes" if device is bridge
+           Set to <literal>yes</literal> if the device is a bridge.
          </para>
         </entry>
         <entry>
          <para>
-           deprecated. Recognized by other attributes
+           Deprecated. Recognized by other attributes
          </para>
         </entry>
        </row>
@@ -7034,12 +7047,12 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           Comma separated list of bridge ports e.g. <literal>eth0 eth1</literal>.
+           Comma-separated list of bridge ports, for example, <literal>eth0 eth1</literal>.
          </para>
         </entry>
         <entry>
          <para>
-           Used only with <literal>bridge</literal> device and required for it.
+           Used only with a <literal>bridge</literal> device and required for it.
          </para>
         </entry>
        </row>
@@ -7051,12 +7064,13 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           Spanning tree protocol. Possible values are <literal>on</literal> when enabled and <literal>off</literal> when disabled.
+           Spanning tree protocol. Set to <literal>on</literal> for enabling it
+          and to <literal>off</literal> for disabling it.
          </para>
         </entry>
         <entry>
          <para>
-           Used only with <literal>bridge</literal> device.
+           Used only with a <literal>bridge</literal> device.
          </para>
         </entry>
        </row>
@@ -7068,12 +7082,12 @@ config:type="boolean"&gt;true&lt;/keep_install_network&gt;</screen>
         </entry>
         <entry>
          <para>
-           Forward delay for bridge e.g. <literal>15</literal>.
+           Forward delay for bridge, for example <literal>15</literal>.
          </para>
         </entry>
         <entry>
          <para>
-           Used only with <literal>bridge</literal> device. Valid values are between <literal>4</literal> and <literal>30</literal>.
+           Used only with a <literal>bridge</literal> device. Valid values are between <literal>4</literal> and <literal>30</literal>.
          </para>
         </entry>
        </row>
-- 
2.21.0

