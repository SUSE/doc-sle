<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter PUBLIC "-//Novell//DTD NovDoc XML V1.0//EN" "novdocx.dtd">
<!--
*********************************
Please see LICENSE.txt for this document's license.
*********************************
-->
<chapter xml:base="net_nfs.xml" id="cha.nfs">
 <title>使用 NFS 共享檔案系統</title>
<indexterm> <primary>NFS</primary> </indexterm> <indexterm> <primary>網路檔案系統</primary> <see>NFS</see> </indexterm>
 <abstract>
  <para>
   透過網路配送和共享檔案系統在公司環境中極為常見。這種成熟的網路檔案系統 (<emphasis>NFS</emphasis>) 可與 <emphasis>NIS</emphasis> (黃頁通訊協定) 搭配使用。如需可與 <emphasis>LDAP</emphasis> 搭配使用並且還要使用 Kerberos 的更安全通訊協定，請選擇 <emphasis>NFSv4</emphasis>。若與 pNFS 結合使用，可以消除效能瓶頸。
  </para>

  <para>
   NFS 與 NIS 攜手能讓使用者對網路有清楚的瞭解。可以使用 NFS 透過網路配送任意檔案系統。如果安裝妥當，使用者將會發現，不論他們目前使用的是哪個終端機，他們始終處於同一個環境中。
  </para>
 </abstract>
 <important os="sles">
  <title>DNS 所需</title>
  <para>
   原則上，可以只使用 IP 位址來進行所有的輸出。為了避免逾時，您需要有正常運作的 DNS 系統。即使為了記錄目的，DNS 也必不可少，因為掛接的精靈執行的是反向查詢。
  </para>
 </important>
 <sect1 id="sec.nfs.term">
  <title>術語</title>

  <para>
   YaST 模組中使用了下列詞彙。
  </para>



  <variablelist>
   <varlistentry>
    <term>輸出</term>
    <listitem>
     <para>
      由 NFS 伺服器<emphasis>輸出</emphasis>的目錄，用戶端可將該目錄整合到其系統中。
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term>NFS 用戶端</term>
    <listitem>
     <para>
      NFS 用戶端是指透過「網路檔案系統」通訊協定使用 NFS 伺服器提供之 NFS 服務的系統。TCP/IP 通訊協定已整合到 Linux 核心中；不需要安裝任何其他軟體。
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term>NFS 伺服器</term>
    <listitem>
     <para>
      NFS 伺服器向用戶端提供 NFS 服務。執行中的伺服器依賴於下列精靈運作：<systemitem class="daemon">nfsd</systemitem> (工作者)、<systemitem class="daemon">idmapd</systemitem> (使用者及群組名稱至 ID 的映射，反之亦然)、<systemitem class="daemon">statd</systemitem> (檔案鎖定) 及 <systemitem class="daemon">mountd</systemitem> (掛接要求)。
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term>NFSv3</term>
    <listitem>
     <para>
      NFSv3 是第 3 版實作，即支援用戶端驗證的<quote>舊</quote>無狀態 NFS。
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term>NFSv4</term>
    <listitem>
     <para>
      NFSv4 是新版 (第 4 版) 實作，支援透過 kerberos 的安全使用者驗證。NFSv4 只需要一個連接埠，因此比 NFSv3 更適合在防火牆後的環境中使用。
     </para>
     <para>
      協定的定義如 <ulink url="http://tools.ietf.org/html/rfc3530"/> 所示。
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term>pNFS</term>
    <listitem>
     <para>
      平行 NFS，NFSv4 的通訊協定延伸。所有 pNFS 用戶端都可以直接存取 NFS 伺服器上的資料。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </sect1>

 

 <sect1 os="sles" id="sec.nfs.installation">
  <title>安裝 NFS 伺服器</title>

  <para>
   在進行預設安裝時並不會安裝 NFS 伺服器軟體。如果依<xref linkend="sec.nfs.configuring-nfs-server"/> 中所示設定 NFS 伺服器，系統會自動提示您安裝必要的套件。或者，使用 YaST 或 Zypper 安裝套件 <systemitem class="resource">nfs-kernel-server</systemitem>。
  </para>

  <para>
   NFS 與 NIS 一樣，都是主從式系統。但一台機器可同時扮演這兩種角色 — 它可透過網路提供檔案系統 (輸出)，也可以從其他主機掛接檔案系統 (輸入)。
  </para>

  <note>

   <title>在輸出伺服器上本地掛接 NFS 磁區</title>
   <para>
    SUSE Linux Enterprise 系統不支援在輸出伺服器上本地掛接 NFS 磁碟區，所有企業級 Linux 系統也不支援。
   </para>
  </note>
 </sect1>
 <sect1 id="sec.nfs.configuring-nfs-server" os="sles">
  <title>設定 NFS 伺服器</title>

  <para>
   NFS 伺服器可透過 YaST 來設定，也可以手動設定。若要進行驗證，還可以將 NFS 與 Kerberos 結合使用。
  </para>

  <sect2 id="sec.nfs.export-yast2">
   <title>以 YaST 輸出檔案系統</title><indexterm> <primary>NFS</primary> <secondary>伺服器</secondary> </indexterm>
   <para>
    使用 YaST，將您網路中的主機轉變為 NFS 伺服器，此類伺服器可將目錄和檔案輸出到擁有其存取權的所有主機或一個群組下的全體成員。因此，伺服器無需在每個主機上本地安裝應用程式，也可以提供應用程式。
   </para>
   <para>
    若要設定這樣的伺服器，請執行下列步驟：
   </para>
   <procedure id="pro.nfs.export-yast2.nfs">
    <title>設定 NFS 伺服器</title>
    <step performance="required">
     <para>
      啟動 YaST 並選取<menuchoice><guimenu>網路服務</guimenu> <guimenu>NFS 伺服器</guimenu></menuchoice>；請參閱<xref linkend="fig.inst.nfsserver1"/>。系統可能會提示您安裝其他軟體。
     </para>
     <figure id="fig.inst.nfsserver1">
      <title>NFS 伺服器組態工具</title>
      <mediaobject>
       <imageobject role="fo">
        <imagedata fileref="yast2_inst_nfsserver1.png" width="75%" format="PNG"/>
       </imageobject>
       <imageobject role="html">
        <imagedata fileref="yast2_inst_nfsserver1.png" width="75%" format="PNG"/>
       </imageobject>
      </mediaobject>
     </figure>
    </step>
    <step performance="required">
     <para>
      啟用<guimenu>開始</guimenu>選項圓鈕。
     </para>
    </step>
    <step performance="required">
     <para>
      如果系統上的防火牆 (SuSEfirewall2) 處於使用中狀態，請核取<guimenu>在防火牆中開啟連接埠</guimenu>。YaST 會啟用 <systemitem class="service">nfs</systemitem> 服務來調整其組態以使其適用於 NFS 伺服器。
     </para>
    </step>
    <step performance="required">
     <para>
      檢查是否要<guimenu>啟用 NFSv4</guimenu>。如果您停用 NFSv4，YaST 將只支援 NFSv3 和 NFSv2。
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        如果選取 NFSv4，請另行輸入適當的 NFSv4 網域名稱。
       </para>
       <para>
        請確定該名稱與存取此特定伺服器的 NFSv4 用戶端之<filename> /etc/idmapd.conf</filename> 檔案中的一個名稱相同。此參數適用於 <systemitem class="daemon">idmapd</systemitem> 精靈，而此精靈在支援 (伺服器和用戶端二者的) NFSv4 時是必要的。如果您沒有任何特殊需求，則請讓它維持為 <literal>localdomain</literal> (預設)。
       </para>
      </step>
     </substeps>
    </step>
    <step performance="required">
     <para>
      若您需要安全存取伺服器，請按一下<guimenu>啟用 GSS 安全性</guimenu>。先決條件是您的網域中安裝有 Kerberos，且伺服器和用戶端都可進行 Kerberos 驗證。按<guimenu>下一步</guimenu>繼續執行下一個組態對話方塊。
     </para>
    </step>
    <step performance="required">
     <para>
      按一下對話方塊上半部分中的<guimenu>新增目錄</guimenu>以輸出目錄。
     </para>
    </step>
    <step performance="required">
     <para>
      如果您尚未設定允許的主機，系統會自動彈出另一個對話方塊，可讓您輸入用戶端資訊及選項。輸入主機萬用字元 (通常您可以保留預設設定不變)。
     </para>

     <para>
      有四種主機萬用字元類型可讓您針對各主機進行設定：單一主機 (名稱或 IP 位址)、網路群組、萬用字元 (例如 <literal>*</literal> 摽是所有機器都可存取伺服器) 以及 IP 網路。
     </para>
     <para>
      如需這些選項的詳細資訊，請參閱<literal>輸出</literal>手冊頁。
     </para>
    </step>
    <step performance="required">
     <para>
      按一下<guimenu>完成</guimenu>以完成組態。
     </para>
    </step>
   </procedure>






  </sect2>



  <sect2 id="sec.nfs.export.manual" os="sles">
   <title>手動輸出檔案系統</title><indexterm> <primary>NFS</primary> <secondary>輸出</secondary> </indexterm>
   <para>
    NFS 輸出服務的組態檔案為 <filename>/etc/exports</filename> 和 <filename>/etc/sysconfig/nfs</filename>。除了這些檔案之外，NFSv4 伺服器組態還需要 <filename>/etc/idmapd.conf</filename>。若要啟動或重新啟動服務，請執行 <command>systemctl restart nfsserver.service</command> 指令。如果已在 <filename>/etc/sysconfig/nfs</filename> 中設定 NFSv4，還會啟動 <literal>rpc.idmapd</literal>。NFS 伺服器需依賴執行的 RPC 埠對應程式。因此，它還會啟動或重新啟動該埠對應程式服務。

   </para>


   <note>
    <para>
     NFSv4 是 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 上可用的最新版 NFS 通訊協定。現在，針對 NFSv4 輸出的目錄設定方法與 NFSv3 相同。
    </para>
    <para>
     在 SUSE Linux Enterprise Server 11 舊版本上，會將 BIND 強制掛接於 <filename>/etc/exports</filename> 中。現在仍支援此方式，但已有了取代方案。
    </para>
   </note>
   <variablelist>
    <varlistentry>
     <term>/etc/exports</term>
     <listitem>
      <para>
       <filename>/etc/exports</filename> 檔案包含一份項目清單。每一個項目都指出一個共享的目錄，並記錄它的共享方式。<filename>/etc/exports</filename> 中的典型項目會包含：
      </para>
<screen>/<replaceable>shared</replaceable>/<replaceable>directory</replaceable>   <replaceable>host</replaceable>(<replaceable>option_list</replaceable>)</screen>
      <para>
       例如：
      </para>
<screen>/export/data   192.168.1.2(rw,sync)</screen>
      <para>
       這裡使用了 IP 位址 <literal>192.168.1.2</literal>，以識別允許的用戶端。您也可以使用主機的名稱以及指向一組主機 (<literal>*.abc.com</literal>、<literal>*</literal> 等) 或網路群組 (<literal>@my-hosts</literal>) 的萬用字元。
      </para>
      <para>
       如需所有選項及其意義的詳細說明，請參閱 man 頁面 <command>exports</command> (<command>man exports</command>)。
      </para>

     </listitem>
    </varlistentry>
    <varlistentry>
     <term>/etc/sysconfig/nfs</term>
     <listitem>
      <para>
       <filename>/etc/sysconfig/nfs</filename> 檔案包含一些決定 NFSv4 伺服器精靈行為的參數。<systemitem>NFS4_SUPPORT</systemitem> 參數必須設定為 <literal>yes</literal>。<systemitem>NFS4_SUPPORT</systemitem> 決定 NFS 伺服器是否支援 NFSv4 輸出和用戶端。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>/etc/idmapd.conf</term>
     <listitem>
      <para>
       Linux 機器的所有使用者都有名稱和 ID。idmapd 會針對伺服器接收的 NFSv4 要求進行名稱和 ID 的對應，然後對用戶端發出回覆。此動作必須同時在 NFSv4 的伺服器和用戶端上執行，因為 NFSv4 的通訊中只使用名稱。
      </para>
      <para>
       對於可能使用 NFS 來共用檔案系統的機器，請確定有一個統一的方式來為各機器之間的使用者指定使用者名稱和 ID (uid)。您可以透過 NIS、LDAP 或您網域中的任何統一網域驗證機制來達成這個目的。
      </para>
      <para>
       在用戶端和伺服器兩端的 <filename>/etc/idmapd.conf</filename> 檔案中，<literal>Domain</literal> 參數必須設定為相同的值。如果您不確定，請讓伺服器和用戶端檔案中的網域保持為 <literal>localdomain</literal>。我們在此提出一個組態檔案的例子，如下所示：
      </para>
<screen>[General]
Verbosity = 0
Pipefs-Directory = /var/lib/nfs/rpc_pipefs
Domain = localdomain

[Mapping]
Nobody-User = nobody
Nobody-Group = nobody</screen>
      <para>
       如需更多資訊，請參閱 <literal>idmapd</literal> 與 <literal>idmapd.conf</literal> 的手冊頁 (<literal>man idmapd</literal> 與 <literal>man idmapd.conf</literal>)。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
   <para>
    對 <filename>/etc/exports</filename> 或 <filename>/etc/sysconfig/nfs</filename> 進行變更後，請啟動或重新啟動 NFS 伺服器服務。
   </para>
<screen>systemctl restart nfsserver.service</screen>
   <para>
    變更 <filename>/etc/idmapd.conf</filename> 之後，重新載入組態檔案︰
   </para>
<screen>killall -HUP rpc.idmapd</screen>
   <para>
    如果 NFS 服務需要在開機時啟動，請執行︰
   </para>
<screen>systemctl enable nfsserver.service</screen>
  </sect2>



  <sect2 id="sec.nfs.kerberos">
   <title>NFS 搭配使用 Kerberos</title>
   <para>
    若要為 NFS 使用 Kerberos 驗證，則 GSS 安全性必須啟用。在初始 YaST NFS 伺服器對話方塊中選取<guimenu>啟用 GSS 安全性</guimenu>。您必須有一個工作中的 Kerberos 伺服器才能使用此功能。YaST 不會設定伺服器，而只是使用所提供的功能。如果您要使用 Kerberos 驗證，則除了 YaST 組態之外，還必須要完成以下步驟才能執行 NFS 組態︰
   </para>
   <procedure>
    <step performance="required">
     <para>
      請確定伺服器和用戶端位於相同的 Kerberos 領域中。它們必須存取相同的 KDC (金鑰配送中心) 伺服器，並共享它們的 <filename>krb5.keytab</filename> 檔案 (所有機器上的預設位置都是 <filename>/etc/krb5.keytab</filename>)。如需有關 Kerberos 的詳細資訊，請參閱<xref linkend="cha.security.kerberos"/>
     </para>
    </step>
    <step performance="required">
     <para>
      請以 <command>systemctl start gssd.service</command> 來啟動用戶端上的 gssd 服務。
     </para>
    </step>
    <step os="sles" performance="required">
     <para>
      請以 <command>systemctl start svcgssd.service</command> 來啟動伺服器上的 svcgssd 服務。
     </para>
    </step>
   </procedure>
   <para>
    如需有關設定啟用 Kerberos 之 NFS 的詳細資訊，請參閱<xref linkend="sec.nfs.info" xrefstyle="SectTitleOnPage"/> 中的連結所提供的內容。
   </para>
  </sect2>
 </sect1>
 <sect1 id="sec.nfs.configuring-nfs-clients">
  <title>設定用戶端</title>

  <para>
   您不需安裝其他軟體就能將您的主機設定為 NFS 用戶端。所有需要的套件預設都會安裝。
  </para>

  <sect2 id="sec.nfs.import-yast2">
   <title>以 YaST 輸入檔案系統</title><indexterm> <primary>NFS</primary> <secondary>用戶端</secondary> </indexterm>
   <para>
    授權使用者可以使用 YaST NFS 用戶端模組將NFS 目錄從 NFS 伺服器掛接到本地檔案樹。請執行下列步驟：
   </para>
   <procedure id="pro.nfs.import-yast2">
    <title>輸入 NFS 目錄</title>
    <step performance="required">
     <para>
      啟動 YaST NFS 用戶端模組。
     </para>
    </step>

    <step performance="required">
     <para>
      在<guimenu>NFS 共用</guimenu>索引標籤中按一下<guimenu>新增</guimenu>。輸入 NFS 伺服器的主機名稱、要輸入的目錄和在本地掛接此目錄的掛接點。
     </para>
    </step>


    <step performance="required">
     <para>
      使用 NFSv4 時，在<guimenu>NFS 設定</guimenu>標籤中選取 <guimenu>啟用 NFSv4</guimenu>。另外，<guimenu>NFSv4 網域名稱</guimenu>必須包含 NFSv4 伺服器所用的相同值。預設網域為 <literal>localdomain</literal>。
     </para>
    </step>
    <step performance="required">
     <para>
      若要為 NFS 使用 Kerberos 驗證，則 GSS 安全性必須啟用。選取<guimenu>啟用 GSS 安全性</guimenu>。
     </para>
    </step>
    <step performance="required">
     <para>
      如果您要使用防火牆並希望允許遠端電腦存取服務，請啟用<guimenu>NFS 設定</guimenu>索引標籤中的<guimenu>在防火牆中開啟埠</guimenu>。防火牆的狀態顯示於核取方塊旁。
     </para>
    </step>

    <step performance="required">
     <para>
      按一下<guimenu>確定</guimenu>，儲存變更。
     </para>
    </step>
   </procedure>
   <para>
    組態將會寫入 <filename>/etc/fstab</filename> 中，並會掛接指定的檔案系統。當您稍後啟動 YaST 組態用戶端時，它也會從這個檔案讀取現有組態。
   </para>

  </sect2>

  <sect2 id="sec.nfs.import">
   <title>手動輸入檔案系統</title>
<indexterm> <primary>NFS</primary> <secondary>輸入</secondary> </indexterm> <indexterm> <primary>NFS</primary> <secondary>裝載</secondary> </indexterm>

   <para>
    從 NFS 伺服器手動輸入檔案系統的先決條件是有 RPC 埠對應程式正在執行。<option>nfs.service</option> 負責正確啟動該程式；因此，請以 <systemitem class="username">root</systemitem> 身分輸入 <command>systemctl start nfs.service</command>，以將它啟動。接著，可以像處理本地分割區一樣，使用 <command>mount</command> 指令在檔案系統中掛接遠端檔案系統︰
   </para>
<screen>mount <replaceable>host</replaceable>:<replaceable>remote-path</replaceable><replaceable>local-path</replaceable></screen>
   <para>
    例如，若要從 <systemitem>nfs.example.com</systemitem> 機器輸入使用者目錄，可以使用︰
   </para>
<screen>mount nfs.example.com:/home /home</screen>
   <sect3 id="sec.nfs.automount">
    <title>使用自動裝載服務</title>
    <para>
     autofs 精靈可用於自動掛接遠端檔案系統。請將下列項目加入您的 <filename>/etc/auto.master</filename> 檔案：
    </para>
<screen>/nfsmounts /etc/auto.nfs</screen>
    <para>
     如果能正確填入 <filename>auto.nfs</filename> 檔案，<filename>/nfsmounts</filename> 目錄此後便會成為用戶端上所有 NFS 掛接作業的根部。選擇 <filename>auto.nfs</filename> 這個名稱是從方便角度考量，您可以自行選擇任何名稱。使用以下指令在 <filename>auto.nfs</filename> 中為所有 NFS 掛接作業新增項目︰
    </para>
<screen>localdata -fstype=nfs server1:/data
nfs4mount -fstype=nfs4 server2:/</screen>
    <para>
     以 <systemitem class="username">root</systemitem> 身分使用 <command>systemctl start autofs.service</command> 啟動設定。在此範例中，<systemitem>server1</systemitem> 的 <filename>/data</filename> 目錄 <filename>/nfsmounts/localdata</filename> 會掛接 NFS，而 <systemitem>server2</systemitem> 的 <filename>/nfsmounts/nfs4mount</filename> 會掛接 NFSv4。
    </para>
    <para>
     如果在 autofs 服務執行期間有程式編輯了 <filename>/etc/auto.master</filename> 檔案，則必須使用 <command>systemctl restart autofs.service</command> 重新啟動自動掛載器，才能使變更生效。
    </para>
   </sect3>
   <sect3 id="sec.nfs.fstab">
    <title>手動編輯 <filename>/etc/fstab</filename></title>
    <para>
     <filename>/etc/fstab</filename> 中典型的 NFSv3 掛接項目如下：
    </para>
<screen>nfs.example.com:/data /local/path nfs rw,noauto 0 0</screen>
    <para>
     對於 NFSv4 掛接，請在第三欄中使用 <literal>nfs4</literal> 而不是 <literal>nfs</literal>︰
    </para>
<screen>nfs.example.com:/data /local/pathv4 nfs4 rw,noauto 0 0</screen>
    <para>
     <literal>noauto</literal> 選項可防止在啟動時自動掛接檔案系統。如果要手動掛接各檔案系統，可以縮短掛接指令的長度，僅指定掛接點︰
    </para>
<screen>mount /local/path</screen>
    <note>
     <para>
      請注意，如果未輸入 <literal>noauto</literal> 選項，系統的 init 程序檔會在啟動時處理這些檔案系統的掛接。
     </para>
    </note>
   </sect3>
  </sect2>

  <sect2 id="sec.nfs.pnfs">
   <title>平行 NFS (pNFS)</title>
   <para>
    NFS 是最舊的通訊協定之一，開發於八十年代。雖然如此，NFS 對於共用小型檔案還是綽綽有餘的。但是，如果您要傳輸大型檔案或大量用戶端想要存取資料，NFS 伺服器將會成為瓶頸，並對系統效能造成明顯影響。出現這種情況的原因是檔案大小快速變大，而乙太網路的相對速度無法完全跟上這一變化。
   </para>
   <para>
    當您申請<quote>一般</quote>NFS 伺服器中的檔案時，該伺服器會尋找檔案中繼資料、收集所有資料並透過網路將其傳輸至您的用戶端。但是，不論檔案的大小，效能瓶頸都會變得很明顯：
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      對於小型檔案，大部分時間都用在收集中繼資料上。
     </para>
    </listitem>
    <listitem>
     <para>
      對於大型檔案，大部分時間則用在將資料從伺服器傳輸至用戶端上。
     </para>
    </listitem>
   </itemizedlist>
   <para>
    pNFS (或平行 NFS) 克服了這個局限性，因為它將檔案系統中繼資料與資料位置分隔開來。因此，pNFS 需要兩種類型的伺服器：
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      <emphasis>中繼資料</emphasis>或<emphasis>控制伺服器</emphasis>，用於處理所有非資料流量
     </para>
    </listitem>
    <listitem>
     <para>
      一或多個<emphasis>儲存伺服器</emphasis>，用於存放資料
     </para>
    </listitem>
   </itemizedlist>
   <para>
    中繼資料與儲存伺服器構成了一個邏輯 NFS 伺服器。當用戶端想要讀取或寫入時，中繼資料伺服器會告知 NFSv4 用戶端使用哪個儲存伺服器來存取檔案區塊。用戶端可以直接存取伺服器上的資料。
   </para>
   <para>
    SUSE Linux Enterprise 僅支援在用戶端上的 pNFS。
   </para>
   <sect3 id="sec.nfs.pnfs.yast">
    <title>使用 YaST 設定 pNFS 用戶端</title>
    <para>
     依<xref linkend="pro.nfs.import-yast2"/> 中所示繼續操作，但要按一下<guimenu>pNFS (v4.1)</guimenu>核取方塊並選擇性地按一下<guimenu>NFSv4 共享</guimenu>。YaST 會執行所有必要的步驟，並將所有需要的選項寫入檔案 <filename>/etc/exports</filename>。
     
    </para>
   </sect3>
   <sect3 id="sec.nfs.pnfs.manual">
    <title>手動設定 pNFS 用戶端</title>
    <para>
     請參閱<xref linkend="sec.nfs.import"/> 開始設定。大部分組態均由 NFSv4 伺服器執行。對於 pNFS，唯一的差異是將 <option>minorversion</option> 選項和中繼資料伺服器 <replaceable>MDS 伺服器</replaceable>新增至 <command>mount</command> 指令：
    </para>
<screen>mount -t nfs4 -o minorversion=1 <replaceable>MDS_SERVER</replaceable> <replaceable>MOUNTPOINT</replaceable></screen>
    <para>
     為了協助進行除錯，請在 <filename>/proc</filename> 檔案系統中變更該值：
    </para>
<screen>echo 32767 &gt; /proc/sys/sunrpc/nfsd_debug
echo 32767 &gt; /proc/sys/sunrpc/nfs_debug</screen>
   </sect3>
  </sect2>
 </sect1>
 <sect1 id="sec.nfs.info">
  <title>如需更多資訊</title>

  <para>
   除了 <command>exports</command>、<command>nfs</command> 和 <command>mount</command> 的 man 頁面以外，<filename>/usr/share/doc/packages/nfsidmap/README</filename> 中也提供了有關設定 NFS 伺服器和用戶端的資訊。如需更多線上文件，請參閱下列網站：
  </para>

  <itemizedlist mark="bullet" spacing="normal">
   <listitem>
    <para>
     如需詳細的線上技術文件，請造訪 <ulink url="http://nfs.sourceforge.net/">SourceForge</ulink>。
    </para>
   </listitem>
   <listitem>
    <para>
     如需設定已監督之 NFS 的指示，請參閱 <ulink url="http://www.citi.umich.edu/projects/nfsv4/linux/krb5-setup.html">NFS 第 4 版開放原始碼實作參考</ulink>。
    </para>
   </listitem>
   <listitem>
    <para>
     如果您有任何關於 NFSv4 的問題，請參閱 <ulink url="http://www.citi.umich.edu/projects/nfsv4/linux/faq/">Linux NFSv4 常見問答集</ulink>。
    </para>
   </listitem>
  </itemizedlist>
 </sect1>
</chapter>
