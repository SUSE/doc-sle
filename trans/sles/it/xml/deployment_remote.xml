<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter PUBLIC "-//Novell//DTD NovDoc XML V1.0//EN" "novdocx.dtd">
<!--
*********************************
Please see LICENSE.txt for this document's license.
*********************************
-->
<chapter xml:base="deployment_remote.xml" role="General" id="cha.deployment.remoteinst">
 <title>Installazione remota</title>
 <para>
  <phrase role="productname"><phrase os="sles">SUSE® Linux Enterprise Server</phrase></phrase> può essere installato in diversi modi. Oltre alla procedura di installazione più comune mediante supporti, descritta nel <xref linkend="cha.inst"/>, è possibile scegliere tra numerosi tipi di approccio basati sulla rete, nonché adottare un approccio completamente automatico per l'installazione di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>.
 </para>
 <para>
  Ciascun metodo viene introdotto per mezzo di due brevi elenchi di controllo: in uno sono elencati i prerequisiti per questo metodo e nell'altro è illustrata la procedura di base. Vengono inoltre forniti ulteriori dettagli su tutte le tecniche utilizzate in questi scenari di installazione.
 </para>
 <note>
  <para>
   Nelle sezioni seguenti, il sistema per l'installazione del nuovo <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> viene indicato come <emphasis>sistema di destinazione</emphasis> o <emphasis>destinazione dell'installazione</emphasis>. Il termine <emphasis>archivio</emphasis> (chiamato in precedenza <quote>origine dell'installazione</quote>) viene utilizzato per tutte le origini dei dati di installazione. Tra queste vi sono i supporti fisici, quali CD e DVD, e i server di rete che distribuiscono i dati di installazione all'interno della rete in uso.
  </para>
 </note>
 <sect1 id="sec.deployment.remoteinst.scenario">
  <title>Scenari di installazione remota</title>

  <para>
   In questa sezione vengono introdotti gli scenari di installazione più comuni relativi alle installazioni remote. Per ogni scenario, verificare attentamente l'elenco dei prerequisiti e seguire la procedura corrispondente indicata. Se per un determinato passaggio sono necessarie istruzioni più dettagliate, seguire il collegamento fornito per ogni passaggio.
  </para>

  <sect2 id="sec.deployment.remoteinst.scenario.vncstat">
   <title>Installazione remota semplice tramite VNC - Configurazione di rete statica</title>
   <para>
    Questo tipo di installazione richiede ancora un certo grado di accesso fisico al sistema di destinazione per l'avvio dell'installazione. L'installazione stessa viene interamente controllata mediante una workstation remota utilizzando VNC per la connessione al programma di installazione. L'interazione dell'utente è necessaria come per l'installazione manuale descritta nel <xref linkend="cha.inst"/>.
   </para>
   <para>
    Per questo tipo di installazione, verificare che i requisiti seguenti siano soddisfatti:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Archivio remoto: NFS, HTTP, FTP o SMB con connessione di rete funzionante.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema di destinazione con connessione di rete funzionante.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema di controllo con connessione di rete funzionante e software per la visualizzazione VNC o browser abilitato per Java (Firefox, Chromium, Internet Explorer, Opera ecc.)
     </para>
    </listitem>
    <listitem>
     <para>
      Supporto di avvio fisico (unità CD, DVD o disco USB flash) per l'avvio sul sistema di destinazione.
     </para>
    </listitem>
    <listitem>
     <para>
      Indirizzi IP statici validi, già assegnati all'archivio e al sistema di controllo.
     </para>
    </listitem>
    <listitem>
     <para>
      Indirizzo IP statico valido da assegnare al sistema di destinazione.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.vncstat">
    <para>
     Per eseguire questo tipo di installazione, procedere come indicato di seguito:
    </para>
    <step performance="required">
     <para>
      Configurare l'archivio, come descritto nella <xref linkend="sec.deployment.remoteinst.instserver"/>. Scegliere un server di rete SNFS, HTTP o FTP. Per un archivio SMB, vedere <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Avviare il sistema di destinazione utilizzando il DVD1 del kit dei supporti di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>.
     </para>
    </step>
    <step performance="required">
     <para>
      Quando viene visualizzata la schermata di avvio, utilizzare il prompt delle opzioni di avvio per impostare le opzioni VNC appropriate e l'indirizzo dell'archivio. Questo passaggio è descritto dettagliatamente nella <xref linkend="sec.deployment.remoteinst.bootinst"/>.
     </para>
     <para>
      Il sistema di destinazione viene avviato in un ambiente basato su testo che fornisce l'indirizzo di rete e il numero di display al quale è possibile indirizzare l'ambiente grafico di installazione tramite un visualizzatore VNC o un browser. Le installazioni VNC vengono comunicate automaticamente tramite OpenSLP, se le impostazioni firewall lo consentono. Possono inoltre essere individuate tramite <command>slptool</command>, come descritto nella sezione <xref linkend="proc.deployment.remoteinst.monitor.vnc.prep"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Nella workstation di controllo, aprire l'applicazione visualizzatore VNC oppure un browser Web e connettersi al sistema di destinazione come descritto nella <xref linkend="sec.deployment.remoteinst.monitor.vnc"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Eseguire l'installazione come descritto nel <xref linkend="cha.inst"/>. Dopo averlo riavviato, è necessario connettersi nuovamente al sistema di destinazione per la parte finale dell'installazione.
     </para>
    </step>
    <step performance="required">
     <para>
      Completare l'installazione.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.vncdyn">
   <title>Installazione remota semplice tramite VNC - Configurazione di rete dinamica</title>
   <para>
    Questo tipo di installazione richiede ancora un certo grado di accesso fisico al sistema di destinazione per l'avvio dell'installazione. La configurazione di rete viene effettuata tramite DHCP (Dynamic Host Configuration Protocol). L'installazione stessa viene interamente controllata mediante una workstation remota utilizzando VNC per connettersi al programma di installazione, ma richiede ancora l'interazione dell'utente per il processo di configurazione effettivo.
   </para>
   <para>
    Per questo tipo di installazione, verificare che i requisiti seguenti siano soddisfatti:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Archivio remoto: NFS, HTTP, FTP o SMB con connessione di rete funzionante.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema di destinazione con connessione di rete funzionante.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema di controllo con una connessione di rete funzionante e un software visualizzatore VNC o un browser con supporto Java (Firefox, Chromium, Internet Explorer oppure Opera).
     </para>
    </listitem>
    <listitem>
     <para>
      Avviare il sistema di destinazione utilizzando il DVD1 del kit dei supporti di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>.
     </para>
    </listitem>
    <listitem>
     <para>
      Server DHCP in esecuzione con fornitura degli indirizzi IP.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.vncdyn">
    <para>
     Per eseguire questo tipo di installazione, procedere come indicato di seguito:
    </para>
    <step performance="required">
     <para>
      Configurare l'archivio, come descritto nella <xref linkend="sec.deployment.remoteinst.instserver"/>. Scegliere un server di rete SNFS, HTTP o FTP. Per un archivio SMB, vedere <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Avviare il sistema di destinazione utilizzando il DVD1 del kit dei supporti di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>.
     </para>
    </step>
    <step performance="required">
     <para>
      Quando viene visualizzata la schermata di avvio, utilizzare il prompt delle opzioni di avvio per impostare le opzioni VNC appropriate e l'indirizzo dell'archivio. Questo passaggio è descritto dettagliatamente nella <xref linkend="sec.deployment.remoteinst.bootinst"/>.
     </para>
     <para>
      Il sistema di destinazione viene avviato in un ambiente basato su testo che fornisce l'indirizzo di rete e il numero di display al quale è possibile indirizzare l'ambiente grafico di installazione tramite un visualizzatore VNC o un browser. Le installazioni VNC vengono comunicate automaticamente tramite OpenSLP, se le impostazioni firewall lo consentono. Possono inoltre essere individuate tramite <command>slptool</command>, come descritto nella sezione <xref linkend="proc.deployment.remoteinst.monitor.vnc.prep"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Nella workstation di controllo, aprire l'applicazione visualizzatore VNC oppure un browser Web e connettersi al sistema di destinazione come descritto nella <xref linkend="sec.deployment.remoteinst.monitor.vnc"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Eseguire l'installazione come descritto nel <xref linkend="cha.inst"/>. Dopo averlo riavviato, è necessario connettersi nuovamente al sistema di destinazione per la parte finale dell'installazione.
     </para>
    </step>
    <step performance="required">
     <para>
      Completare l'installazione.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.vncpxe">
   <title>Installazione remota tramite VNC - Avvio PXE e Wake on LAN</title>
   <para>
    Questo tipo di installazione è completamente automatico. Il computer di destinazione viene avviato in remoto. L'interazione dell'utente è necessaria solo per l'installazione effettiva. Questo approccio è consigliato per la distribuzione intersito.
   </para>
   <para>
    Per eseguire questo tipo di installazione, verificare che i requisiti seguenti siano soddisfatti:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Archivio remoto: NFS, HTTP, FTP o SMB con connessione di rete funzionante.
     </para>
    </listitem>
    <listitem>
     <para>
      Server TFTP.
     </para>
    </listitem>
    <listitem>
     <para>
      Server DHCP in esecuzione per la rete in uso.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema di destinazione abilitato all'avvio PXE, al networking e a Wake on LAN, collegato e connesso alla rete.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema di controllo con una connessione di rete funzionante e un software visualizzatore VNC o un browser con supporto Java (Firefox, Chromium, Internet Explorer oppure Opera).
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.vncpxe">
    <para>
     Per eseguire questo tipo di installazione, procedere come indicato di seguito:
    </para>
    <step performance="required">
     <para>
      Configurare l'archivio, come descritto nella <xref linkend="sec.deployment.remoteinst.instserver"/>. Scegliere un server di rete NFS, HTTP o FTP oppure configurare un archivio SMB, come descritto nella <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Configurare un server TFTP che contenga un'immagine di avvio che può essere ricavata dal sistema di destinazione. Questo passaggio viene descritto nella <xref linkend="sec.deployment.remoteinst.boot.tftp"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Configurare un server DHCP per la fornitura di indirizzi IP a tutti i computer e rivelare la posizione del server TFTP al sistema di destinazione. Questo passaggio viene descritto nella <xref linkend="sec.deployment.remoteinst.boot.dhcp"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Preparare il sistema di destinazione per l'avvio PXE. Questo passaggio è descritto dettagliatamente nella <xref linkend="sec.deployment.remoteinst.boot.pxeprep"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Iniziare il processo di avvio del sistema di destinazione tramite Wake on LAN. Questo passaggio viene descritto nella <xref linkend="sec.deployment.remoteinst.boot.wol"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Nella workstation di controllo, aprire l'applicazione visualizzatore VNC oppure un browser Web e connettersi al sistema di destinazione come descritto nella <xref linkend="sec.deployment.remoteinst.monitor.vnc"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Eseguire l'installazione come descritto nel <xref linkend="cha.inst"/>. Dopo averlo riavviato, è necessario connettersi nuovamente al sistema di destinazione per la parte finale dell'installazione.
     </para>
    </step>
    <step performance="required">
     <para>
      Completare l'installazione.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.sshstat">
   <title>Installazione remota semplice tramite SSH - Configurazione di rete statica</title>
   <para>
    Questo tipo di installazione richiede ancora un certo grado di accesso fisico al sistema di destinazione per l'avvio dell'installazione e per la determinazione degli indirizzi IP della destinazione dell'installazione. L'installazione stessa viene interamente controllata mediante una workstation remota utilizzando SSH per la connessione al programma di installazione. L'interazione dell'utente è necessaria come per la normale installazione descritta nel <xref linkend="cha.inst"/>.
   </para>
   <para>
    Per questo tipo di installazione, verificare che i requisiti seguenti siano soddisfatti:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Archivio remoto: NFS, HTTP, FTP o SMB con connessione di rete funzionante.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema di destinazione con connessione di rete funzionante.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema di controllo con una connessione di rete funzionante e un client SSH.
     </para>
    </listitem>
    <listitem>
     <para>
      Avviare il sistema di destinazione utilizzando il DVD1 del kit dei supporti di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>.
     </para>
    </listitem>
    <listitem>
     <para>
      Indirizzi IP statici validi, già assegnati all'archivio e al sistema di controllo.
     </para>
    </listitem>
    <listitem>
     <para>
      Indirizzo IP statico valido da assegnare al sistema di destinazione.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.sshstat">
    <para>
     Per eseguire questo tipo di installazione, procedere come indicato di seguito:
    </para>
    <step performance="required">
     <para>
      Configurare l'archivio, come descritto nella <xref linkend="sec.deployment.remoteinst.instserver"/>. Scegliere un server di rete SNFS, HTTP o FTP. Per un archivio SMB, vedere <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Avviare il sistema di destinazione utilizzando il DVD1 del kit dei supporti di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>.
     </para>
    </step>
    <step performance="required">
     <para>
      Quando viene visualizzata la schermata di avvio del sistema di destinazione, utilizzare il prompt delle opzioni di avvio per impostare i parametri appropriati per la connessione di rete, l'indirizzo dell'archivio e l'abilitazione a SSH. Questo passaggio è descritto dettagliatamente nella <xref linkend="sec.deployment.remoteinst.bootinst.custom"/>.
     </para>
     <para>
      Il sistema di destinazione viene avviato in un ambiente basato su testo che fornisce l'indirizzo di rete al quale è possibile indirizzare l'ambiente grafico di installazione tramite un client SSH.
     </para>
    </step>
    <step performance="required">
     <para>
      Nella workstation di controllo, aprire una finestra di terminale e connettersi al sistema di destinazione come descritto nella <xref linkend="sec.deployment.remoteinst.monitor.ssh.connect"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Eseguire l'installazione come descritto nel <xref linkend="cha.inst"/>. Dopo averlo riavviato, è necessario connettersi nuovamente al sistema di destinazione per la parte finale dell'installazione.
     </para>
    </step>
    <step performance="required">
     <para>
      Completare l'installazione.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.sshdyn">
   <title>Installazione remota semplice tramite SSH - Configurazione di rete dinamica</title>
   <para>
    Questo tipo di installazione richiede ancora un certo grado di accesso fisico al sistema di destinazione per l'avvio dell'installazione e per la determinazione degli indirizzi IP della destinazione dell'installazione. L'installazione stessa viene interamente controllata mediante una workstation remota che utilizza SSH per connettersi al programma di installazione; viene comunque richiesta l'interazione dell'utente per il processo di configurazione effettivo.
   </para>
   <note>
    <title>evitare connessioni perse dopo il 2° passaggio (installazione)</title>
    <para>
     Nella finestra di dialogo delle impostazioni di rete selezionare <guimenu>Metodo tradizionale con ifup</guimenu> ed evitare NetworkManager. In caso contrario, si perderà la connessione SSH durante l'installazione. Reimpostare <guimenu>Utente controllato con NetworkManager</guimenu> al termine dell'installazione.
    </para>
   </note>
   <para>
    Per questo tipo di installazione, verificare che i requisiti seguenti siano soddisfatti:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Archivio remoto: NFS, HTTP, FTP o SMB con connessione di rete funzionante.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema di destinazione con connessione di rete funzionante.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema di controllo con una connessione di rete funzionante e un client SSH.
     </para>
    </listitem>
    <listitem>
     <para>
      Supporto di avvio fisico (unità CD, DVD o disco USB flash) per l'avvio sul sistema di destinazione.
     </para>
    </listitem>
    <listitem>
     <para>
      Server DHCP in esecuzione con fornitura degli indirizzi IP.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.sshdyn">
    <para>
     Per eseguire questo tipo di installazione, procedere come indicato di seguito:
    </para>
    <step performance="required">
     <para>
      Configurare l'origine dell'archivio, come descritto nella <xref linkend="sec.deployment.remoteinst.instserver"/>. Scegliere un server di rete SNFS, HTTP o FTP. Per un archivio SMB, vedere <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Avviare il sistema di destinazione utilizzando il DVD1 del kit dei supporti di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>.
     </para>
    </step>
    <step performance="required">
     <para>
      Quando viene visualizzata la schermata di avvio del sistema di destinazione, utilizzare il prompt delle opzioni di avvio per passare i parametri appropriati per la connessione di rete, la posizione dell'origine dell'installazione e l'abilitazione a SSH. Vedere la <xref linkend="sec.deployment.remoteinst.bootinst.custom"/> per istruzioni dettagliate sull'utilizzo di questi parametri.
     </para>
     <para>
      Il sistema di destinazione viene avviato in un ambiente basato su testo che fornisce l'indirizzo di rete al quale è possibile indirizzare l'ambiente grafico di installazione tramite un client SSH.
     </para>
    </step>
    <step performance="required">
     <para>
      Nella workstation di controllo, aprire una finestra di terminale e connettersi al sistema di destinazione come descritto nella <xref linkend="sec.deployment.remoteinst.monitor.ssh.connect"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Eseguire l'installazione come descritto nel <xref linkend="cha.inst"/>. Dopo averlo riavviato, è necessario connettersi nuovamente al sistema di destinazione per la parte finale dell'installazione.
     </para>
    </step>
    <step performance="required">
     <para>
      Completare l'installazione.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.sshpxe">
   <title>Installazione remota tramite SSH - Avvio PXE e Wake on LAN</title>
   <para>
    Questo tipo di installazione è completamente automatico. Il computer di destinazione viene avviato in remoto.
   </para>
   <para>
    Per eseguire questo tipo di installazione, verificare che i requisiti seguenti siano soddisfatti:
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Archivio remoto: NFS, HTTP, FTP o SMB con connessione di rete funzionante.
     </para>
    </listitem>
    <listitem>
     <para>
      Server TFTP.
     </para>
    </listitem>
    <listitem>
     <para>
      Server DHCP in esecuzione per la rete in uso, che fornisce un indirizzo IP statico all'host da installare.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema di destinazione abilitato all'avvio PXE, al networking e a Wake on LAN, collegato e connesso alla rete.
     </para>
    </listitem>
    <listitem>
     <para>
      Sistema di controllo con una connessione di rete funzionante e un client SSH.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.sshpxe">
    <para>
     Per eseguire questo tipo di installazione, procedere come indicato di seguito:
    </para>
    <step performance="required">
     <para>
      Configurare l'archivio, come descritto nella <xref linkend="sec.deployment.remoteinst.instserver"/>. Scegliere un server di rete SNFS, HTTP o FTP. Per la configurazione di un archivio SMB, vedere <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Configurare un server TFTP che contenga un'immagine di avvio che può essere ricavata dal sistema di destinazione. Questo passaggio viene descritto nella <xref linkend="sec.deployment.remoteinst.boot.tftp"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Configurare un server DHCP per la fornitura di indirizzi IP a tutti i computer e rivelare la posizione del server TFTP al sistema di destinazione. Questo passaggio viene descritto nella <xref linkend="sec.deployment.remoteinst.boot.dhcp"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Preparare il sistema di destinazione per l'avvio PXE. Questo passaggio è descritto dettagliatamente nella <xref linkend="sec.deployment.remoteinst.boot.pxeprep"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Iniziare il processo di avvio del sistema di destinazione tramite Wake on LAN. Questo passaggio viene descritto nella <xref linkend="sec.deployment.remoteinst.boot.wol"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Nella workstation di controllo, avviare un client SSH e connettersi al sistema di destinazione come descritto nella <xref linkend="sec.deployment.remoteinst.monitor.ssh"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Eseguire l'installazione come descritto nella <xref linkend="cha.inst"/>. Dopo averlo riavviato, è necessario connettersi nuovamente al sistema di destinazione per la parte finale dell'installazione.
     </para>
    </step>
    <step performance="required">
     <para>
      Completare l'installazione.
     </para>
    </step>
   </procedure>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.instserver">
  <title>Configurazione del server contenente le origini dell'installazione</title>

  <para>
   In base al sistema operativo in esecuzione nel computer da utilizzare come origine dell'installazione di rete per <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>, sono disponibili varie opzioni per la configurazione del server. Il modo più semplice per configurare un server per l'installazione è utilizzare YaST in SUSE Linux Enterprise Server 11/opensuse; 11.1 o versioni successive.
  </para>

  <tip>
   <para>
    È anche possibile utilizzare un computer Microsoft Windows come server di installazione per la distribuzione di Linux. Per ulteriori informazioni, vedere <xref linkend="sec.deployment.remoteinst.instserver.smb"/>.
   </para>
  </tip>

  <sect2 id="sec.deployment.remoteinst.instserver.sles9">
   <title>Configurazione di un server di installazione con YaST</title>
   <para>
    In YaST è disponibile uno strumento grafico per la creazione di archivi di rete. Sono supportati server di installazione di rete HTTP, FTP e NFS.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.sles9">
    <step performance="required">
     <para>
      Eseguire il login come <systemitem class="username">root</systemitem> al computer che deve svolgere la funzione di server di installazione.
     </para>
    </step>
    <step performance="required">
     <para>
      Avviare  <menuchoice><guimenu>YaST </guimenu><guimenu>Varie </guimenu><guimenu>Server di installazione</guimenu></menuchoice>.
     </para>
    </step>
    <step performance="required">
     <para>
      Selezionare il tipo di archivio (HTTP, FTP o NFS). Il servizio di selezionato viene avviato automaticamente a ogni avvio del sistema. Se un servizio del tipo selezionato è già in esecuzione nel sistema in uso e si desidera configurarlo manualmente per il server, disattivare la configurazione automatica del servizio di server mediante <guimenu>Non configurare alcun servizio di rete</guimenu>. In entrambi i casi, definire la directory in cui i dati dell'installazione dovrebbero essere disponibili nel server.
     </para>
    </step>
    <step performance="required">
     <para>
      Configurare il tipo di archivio richiesto. Questo passaggio si riferisce alla configurazione automatica dei servizi server  e viene ignorato quando la configurazione automatica è disattivata.
     </para>
     <para>

      Definire un alias per la directory radice del server FTP o HTTP in cui si trovano i dati dell'installazione. L'archivio verrà in seguito ubicato in <literal>ftp://<replaceable>Server-IP</replaceable>/<replaceable>Alias</replaceable>/<replaceable>Nome</replaceable></literal> (FTP) o <literal>http://<replaceable>Server-IP</replaceable>/<replaceable>Alias</replaceable>/<replaceable>Nome</replaceable></literal> (HTTP). <replaceable>Nome</replaceable> indica il nome dell'archivio, descritto nel passaggio successivo. Se nel passaggio precedente è stato selezionato NFS, definire i caratteri jolly e le opzioni di esportazione. Il server NFS sarà accessibile in <literal>nfs:// <replaceable>Server-IP </replaceable>/ <replaceable>Nome</replaceable></literal>. Per informazioni su NFS e sulle esportazioni, vedere il <xref linkend="cha.nfs"/>.
     </para>
     <tip>
      <title>impostazioni firewall</title>
      <para>
       Accertarsi che le impostazioni del firewall del sistema server consentano traffico sulle porte HTTP, NFS e FTP. Se non lo consentono, abilitare <guimenu>Porta aperta nel firewall</guimenu> oppure selezionare prima <guimenu>Dettagli firewall</guimenu>.
      </para>
     </tip>
    </step>
    <step performance="required">
     <para>
      Configurare l'archivio. Prima di copiare i supporti di installazione nella destinazione corrispondente, definire il nome dell'archivio, possibilmente mediante un'abbreviazione del prodotto e della versione facile da ricordare. In YaST è possibile fornire immagini ISO dei supporti invece delle copie dei DVD di installazione. Se si sceglie questa soluzione, attivare la casella di controllo corrispondente e specificare il percorso di directory in cui è possibile trovare i file ISO localmente. A seconda del prodotto da distribuire tramite questo server di installazione, è possibile che siano necessari ulteriori CD aggiuntivi o con service pack, che dovranno essere aggiunti come archivi addizionali. Per annunciare in rete il server di installazione in uso tramite OpenSLP, attivare l'opzione corrispondente.
     </para>
     <tip>
      <para>
       Valutare la possibilità di annunciare l'archivio tramite OpenSLP se la configurazione di rete in uso supporta tale opzione. In questo modo è possibile evitare di immettere il percorso di installazione della rete in ogni computer di destinazione. I sistemi di destinazione vengono avviati semplicemente utilizzando l'opzione di avvio tramite SLP (Service Location Protocol) e individuano l'archivio di rete senza ulteriori configurazioni. Per informazioni su questa opzione, vedere la <xref linkend="sec.deployment.remoteinst.bootinst"/>.
      </para>
     </tip>
    </step>
    <step performance="required">
     <para>
      Caricare i dati di installazione. Il passaggio più lungo nella configurazione di un server per l'installazione è la copia dei supporti di installazione effettivi. Inserire i supporti in base alla sequenza richiesta da YaST e attendere la fine della procedura di copia. Quando le origini sono state interamente copiate, tornare alla panoramica degli archivi esistenti e chiudere la configurazione selezionando <guimenu>Fine</guimenu>.
     </para>
     <para>
      Il server di configurazione in uso è ora completamente configurato e pronto al servizio e viene avviato automaticamente a ogni avvio del sistema. Non sono necessari altri interventi. È necessario solo configurare e avviare il servizio manualmente in modo corretto se la configurazione automatica del servizio di rete selezionato con YaST è stata disattivata nel passaggio iniziale.
     </para>
    </step>
   </procedure>
   <para>
    Per disattivare un archivio, selezionare l'archivio da rimuovere, quindi scegliere <guimenu>Cancella</guimenu>. I dati di installazione vengono rimossi dal sistema. Per disattivare il servizio di rete, utilizzare il rispettivo modulo YaST.
   </para>
   <para>
    Se il server di installazione in uso deve fornire dati di installazione a più prodotti o versioni di prodotto, avviare il modulo YaST per il server di installazione e selezionare <guimenu>Aggiungi</guimenu> nella panoramica degli archivi esistenti per configurare il nuovo archivio.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.nfs">
   <title>Configurazione manuale di un archivio NFS</title>
   
   <para>
    La configurazione di un'origine NFS per l'installazione avviene in due passaggi principali. Nel primo passaggio, creare la struttura della directory che contiene i dati di installazione e copiare i supporti di installazione in tale struttura. In secondo luogo, esportare in rete la directory con i dati di installazione.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.cdir">
    <para>
     Per creare una directory con i dati di installazione, procedere come indicato di seguito:
    </para>
    <step performance="required">
     <para>
      Eseguire il login come utente <systemitem class="username">root</systemitem>.
     </para>
    </step>
    <step performance="required">
     <para>
      Creare la directory destinata in seguito a contenere i dati di installazione e passare a questa directory. Ad esempio:
     </para>
<screen>mkdir install/<replaceable>product</replaceable>/<replaceable>productversion</replaceable>
cd install/<replaceable>product</replaceable>/<replaceable>productversion</replaceable></screen>
     <para>
      Sostituire <replaceable>product</replaceable> con un'abbreviazione del nome del prodotto e <replaceable>productversion</replaceable> mediante una stringa contenente il nome e la versione del prodotto.
     </para>
    </step>
    <step performance="required">
     <para>
      Per ciascun DVD contenuto nel kit di supporti, eseguire i comandi seguenti:
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        Copiare l'intero contenuto del DVD di installazione nella directory del server di installazione:
       </para>
<screen>cp -a /media/<replaceable>path_to_your_DVD_drive .</replaceable></screen>
       <para>
        Sostituire <replaceable>path_to_your_DVD_drive</replaceable> con il percorso effettivo al quale è indirizzato il DVD in uso. In base al tipo di drive utilizzato nel sistema, può essere indicato <filename>cdrom</filename>, <filename>cdrecorder</filename>, <filename>dvd</filename> oppure <filename>dvdrecorder</filename>.
       </para>
      </step>
      <step performance="required">
       <para>
        Rinominare la directory impostando il numero del disco DVD.
       </para>
<screen>mv <replaceable>path_to_your_DVD_drive</replaceable> DVD<replaceable>x</replaceable></screen>
       <para>
        Sostituire <replaceable>x</replaceable> con il numero effettivo del DVD.
       </para>
      </step>
     </substeps>
    </step>
   </procedure>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.export">
    <para>
     In <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> è possibile esportare l'archivio con NFS utilizzando YaST. Procedere come segue:
    </para>
    <step performance="required">
     <para>
      Eseguire il login come utente <systemitem class="username">root</systemitem>.
     </para>
    </step>
    <step performance="required">
     <para>
      Avviare <menuchoice><guimenu>YaST</guimenu><guimenu>Servizi di rete</guimenu><guimenu>Server NFS</guimenu></menuchoice>.
     </para>
    </step>
    <step performance="required">
     <para>
      Selezionare <guimenu>Avvio</guimenu>, <guimenu>Porta aperta nel firewall</guimenu> e fare clic su <guimenu>Avanti</guimenu>.
     </para>
    </step>
    <step performance="required">
     <para>
      Selezionare <guimenu>Aggiungi directory</guimenu> e cercare la directory contenente le origini di installazione, in questo caso <filename><replaceable>productversion</replaceable></filename>.
     </para>
    </step>
    <step performance="required">
     <para>
      Selezionare <guimenu>Aggiungi host</guimenu> e immettere i nomi host del computer nel quale esportare i dati di installazione. Anziché specificare i nomi host, è possibile utilizzare caratteri jolly, intervalli di indirizzi di rete o semplicemente il nome di dominio della rete in uso. Immettere le opzioni di esportazione appropriate oppure lasciare quelle di default, che funzionano correttamente nella maggior parte delle configurazioni. Per ulteriori informazioni sulla sintassi utilizzata per l'esportazione delle condivisioni NFS, leggere la manpage <systemitem>export</systemitem>.
     </para>
    </step>
    <step performance="required">
     <para>
      Fare clic su <guimenu>Fine</guimenu>. Il server NFS che contiene l'archivio di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> viene avviato e integrato automaticamente nel processo di avvio.
     </para>
    </step>
   </procedure>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.exportman">
    <para>
     Se si preferisce esportare manualmente l'archivio tramite NFS invece di utilizzare il modulo YaST per il server NFS, procedere come indicato di seguito:
    </para>
    <step performance="required">
     <para>
      Eseguire il login come utente <systemitem class="username">root</systemitem>.
     </para>
    </step>
    <step performance="required">
     <para>
      Aprire il file <filename>/etc/exports </filename> e immettere la riga seguente:
     </para>
<screen>/<replaceable>productversion</replaceable> *(ro,root_squash,sync)</screen>
     <para>
      In questo modo la directory <filename>/<replaceable>productversion</replaceable></filename> viene esportata in un host che è parte di questa rete o in un host che può essere connesso a questo server. Per limitare l'accesso a questo server, utilizzare maschere di rete o nomi di dominio al posto del carattere jolly generico <literal>*</literal>. Per informazioni, vedere la manpage relativa a <systemitem>export</systemitem>. Salvare e uscire da questo file di configurazione.
     </para>
    </step>
    <step performance="required">
     <para>
      Per aggiungere il servizio NFS all'elenco di server avviati durante l'avvio del sistema, eseguire i comandi seguenti:
     </para>
<screen>systemctl enable nfsserver.service</screen>
    </step>
    <step performance="required">
     <para>
      Avviare il server NFS con il comando <command>systemctl start nfsserver.service</command>. Se si desidera modificare la configurazione del server NFS successivamente, modificare il file di configurazione e riavviare il daemon NFS mediante <command>systemctl start nfsserver.service</command>.
     </para>
    </step>
   </procedure>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.slpann">
    <para>
     L'annuncio del server NFS tramite OpenSLP rende il suo indirizzo noto a tutti i client della rete in uso.
    </para>
    <step performance="required">
     <para>
      Eseguire il login come utente <systemitem class="username">root</systemitem>.
     </para>
    </step>
    <step performance="required">
     <para>
      Creare il file di configurazione <filename>/etc/slp.reg.d/install.suse.nfs.reg</filename> contenente le seguenti righe:
     </para>
<screen><?dbsuse-fo font-size="7pt"?>

# Register the NFS Installation Server
service:install.suse:nfs://$HOSTNAME/<replaceable>path_to_repository</replaceable>/DVD1,en,65535 
description=NFS Repository</screen>
     <para>
      Sostituire <replaceable>path_to_repository</replaceable> con il percorso effettivo dell'origine dell'installazione sul server.
     </para>
    </step>
    <step performance="required">
     <para>
      Avviare il daemon OpenSLP con il comando <command>systemctl start nfsserver.service</command>.
     </para>
    </step>
   </procedure>
   <para os="sles">
    Per ulteriori informazioni su OpenSLP, consultare la documentazione relativa ai pacchetti in <filename>/usr/share/doc/packages/openslp/</filename> o vedere <xref linkend="cha.slp"/>. Per ulteriori informazioni su NFS, vedere <xref linkend="cha.nfs"/>.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.ftp">
   <title>Configurazione manuale di un archivio FTP</title>
   <para>
    La procedura di creazione di un archivio FTP è molto simile a quella per la creazione di un archivio NFS. Anche un archivio FTP può essere annunciato in rete tramite OpenSLP.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.ftp">
    <step performance="required">
     <para>
      Creare una directory contenente le origini dell'installazione come descritto nella <xref linkend="sec.deployment.remoteinst.instserver.nfs"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Configurare il server FTP per la distribuzione dei contenuti della directory di installazione in uso:
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        Eseguire il login come <systemitem class="username">root</systemitem> e installare il pacchetto <systemitem>vsftpd</systemitem> utilizzando il programma di gestione del software YaST.
       </para>
      </step>
      <step performance="required">
       <para>
        Immettere la directory radice del server FTP:
       </para>
<screen>cd <option>/srv/ftp</option></screen>
      </step>
      <step performance="required">
       <para>
        Creare una sottodirectory contenente le origini dell'installazione nella directory radice di FTP:
       </para>
<screen>mkdir <replaceable>repository</replaceable></screen>
       <para>
        Sostituire <replaceable>repository</replaceable> con il nome del prodotto.
       </para>
      </step>
      <step performance="required">
       <para>
        Montare i contenuti dell'archivio di installazione nell'ambiente di modifica radice del server FTP:
       </para>
<screen>mount --bind <replaceable>path_to_repository</replaceable> /srv/ftp/<replaceable>repository</replaceable></screen>
       <para>
        Sostituire <replaceable>path_to_repository</replaceable> e <replaceable>repository</replaceable> con i valori che corrispondono alla configurazione. Se si desidera che questa sostituzione sia permanente, aggiungerla a <filename>/etc/fstab</filename>.
       </para>
      </step>
      <step performance="required">
       <para>
        Avviare vsftpd con <command>vsftpd</command>.
       </para>
      </step>
     </substeps>
    </step>
    <step performance="required">
     <para>
      Annunciare l'archivio tramite OpenSLP, se supportato dalla configurazione di rete in uso:
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        Creare il file di configurazione <filename>/etc/slp.reg.d/install.suse.ftp.reg</filename> contenente le seguenti righe:
       </para>
<screen><?dbsuse-fo font-size="7pt"?>

# Register the FTP Installation Server
service:install.suse:ftp://$HOSTNAME/<replaceable>repository</replaceable>/DVD1,en,65535 
description=FTP Repository</screen>
       <para>
        Sostituire <replaceable>repository</replaceable> con il nome effettivo della directory dell'archivio sul server. La riga <literal>service:</literal> deve essere immessa come un'unica riga continua.
       </para>
      </step>
      <step performance="required">
       <para>
        Avviare il daemon OpenSLP con il comando <command>systemctl start nfsserver.service</command>.
       </para>
      </step>
     </substeps>
    </step>
   </procedure>
   <tip os="sles">
    <title>Configurazione di un server FTP con YaST</title>
    <para>
     Se si preferisce utilizzare YaST anziché configurare manualmente il server FTP di installazione, vedere <xref linkend="cha.ftp"/> per ulteriori informazioni sull'utilizzo del modulo YaST per il server FTP.
    </para>
   </tip>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.http">
   <title>Configurazione manuale di un archivio HTTP</title>
   <para>
    La procedura di creazione di un archivio HTTP è molto simile a quella per la creazione di un archivio NFS. Anche un archivio HTTP può essere annunciato in rete tramite OpenSLP.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.http">
    <step performance="required">
     <para>
      Creare una directory contenente le origini dell'installazione come descritto nella <xref linkend="sec.deployment.remoteinst.instserver.nfs"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Configurare il server HTTP per la distribuzione dei contenuti della directory di installazione in uso:
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        <phrase os="sles">Installare il server Web Apache come descritto in <xref linkend="sec.apache2.quickstart.installation"/></phrase>.
       </para>
      </step>
      <step performance="required">
       <para>
        Immettere la directory root del server HTTP (<filename>/srv/www/htdocs</filename>) e creare una sottodirectory per contenere le origini dell'installazione:
       </para>
<screen>mkdir <replaceable>repository</replaceable></screen>
       <para>
        Sostituire <replaceable>repository</replaceable> con il nome del prodotto.
       </para>
      </step>
      <step performance="required">
       <para>
        Creare un collegamento simbolico dalla posizione delle origini dell'installazione nella directory radice del server Web (<filename>/srv/www/htdocs</filename>):
       </para>
<screen>ln -s <replaceable>/path_to_repository </replaceable>/srv/www/htdocs/<replaceable>repository</replaceable></screen>
      </step>
      <step performance="required">
       <para>
        Modificare il file di configurazione del server HTTP <filename>/etc/apache2/default-server.conf</filename> in modo che segua i collegamenti simbolici. Sostituire la riga seguente:
       </para>
<screen>Options None</screen>
       <para>
        con
       </para>
<screen>Options Indexes FollowSymLinks</screen>
      </step>
      <step performance="required">
       <para>
        Ricaricare la configurazione del server HTTP con il comando <command>systemctl reload apache2.service</command>.
       </para>
      </step>
     </substeps>
    </step>
    <step performance="required">
     <para>
      Annunciare l'archivio tramite OpenSLP, se supportato dalla configurazione di rete in uso:
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        Creare il file di configurazione <filename>/etc/slp.reg.d/install.suse.http.reg</filename> contenente le seguenti righe:
       </para>
<screen><?dbsuse-fo font-size="6.5pt"?>

# Register the HTTP Installation Server
service:install.suse:http://$HOSTNAME/<replaceable>repository</replaceable>/DVD1/,en,65535 
description=HTTP Repository</screen>
       <para>
        Sostituire <replaceable>repository</replaceable> con il percorso effettivo dell'archivio sul server. La riga <literal>service:</literal> deve essere immessa come un'unica riga continua.
       </para>
      </step>
      <step performance="required">
       <para>
        Avviare il daemon OpenSLP con il comando <command>systemctl start nfsserver.service</command>.
       </para>
      </step>
     </substeps>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.smb">
   <title>Gestione di un archivio SMB</title>
   <para>
    L'uso di SMB consente di importare le origini dell'installazione da un server Microsoft Windows e avviare la distribuzione di Linux anche senza nessun computer Linux.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.smb.export">
    <para>
     Per configurare una condivisione Windows esportata contenente l'archivio di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>, procedere come indicato di seguito:
    </para>
    <step performance="required">
     <para>
      Eseguire il login nel computer Windows in uso.
     </para>
    </step>
    <step performance="required">
     <para>
      Creare la nuova directory destinata a contenere l'intero albero di installazione e assegnare a essa un nome; ad esempio <filename>INSTALL</filename>.
     </para>
    </step>
    <step performance="required">
     <para>
      Esportare questa condivisione seguendo la procedura indicata nella documentazione Windows.
     </para>
    </step>
    <step performance="required">
     <para>
      Accedere a questa condivisione e creare una sottodirectory, denominata <filename><replaceable>product</replaceable></filename>. Sostituire <replaceable>product</replaceable> con il nome effettivo del prodotto.
     </para>
    </step>
    <step performance="required">
     <para>
      Accedere alla directory <filename>INSTALL/<replaceable>product</replaceable></filename> e copiare ciascun DVD in una directory separata, come <literal>DVD1</literal> e <literal>DVD2</literal>.
     </para>
    </step>
   </procedure>
   <para>
    Per utilizzare una condivisione SMB montata come archivio, procedere come indicato di seguito:
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.smb.use">
    <step performance="required">
     <para>
      Avviare la destinazione dell'installazione.
     </para>
    </step>
    <step performance="required">
     <para>
      Selezionare <guimenu>Installazione</guimenu>.
     </para>
    </step>
    <step performance="required">
     <para>
      Premere <keycap>F4</keycap> per una selezione dell'archivio.
     </para>
    </step>
    <step performance="required">
     <para>
      Scegliere SMB e immettere il nome o l'indirizzo IP del computer Windows, il nome della condivisione (in questo esempio <filename>INSTALL/<replaceable>product</replaceable>/DVD1</filename>), il nome utente e la password. La sintassi è la seguente:
     </para>
<screen>smb://workdomain;user:password@server/INSTALL/DVD1</screen>
     <para>
      Dopo aver premuto <keycap function="enter">Invio</keycap>, YaST verrà avviato automaticamente e sarà possibile eseguire l'installazione.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.iso">
   <title>Utilizzo delle immagini ISO dei supporti di installazione sul server</title>
   <para>
    Anziché copiare manualmente i supporti fisici nella directory del server, è possibile montare le immagini ISO dei supporti di installazione nel server di installazione e utilizzarle come archivio. Per impostare un server HTTP, NFS o FTP che utilizzi immagini ISO anziché copie di supporti, procedere come segue:
   </para>
   <procedure>
    <step performance="required">
     <para>
      Effettuare il download delle immagini ISO e salvarle sul computer da utilizzare come server di installazione.
     </para>
    </step>
    <step performance="required">
     <para>
      Eseguire il login come utente <systemitem class="username">root</systemitem>.
     </para>
    </step>
    <step performance="required">
     <para>
      Scegliere e creare un'ubicazione appropriata per i dati di installazione, come descritto nella <xref linkend="sec.deployment.remoteinst.instserver.nfs"/>, <xref linkend="sec.deployment.remoteinst.instserver.ftp"/> o <xref linkend="sec.deployment.remoteinst.instserver.http"/>.
     </para>
    </step>
    <step performance="required">
     <para>
      Creare le sottodirectory per ciascun DVD.
     </para>
    </step>
    <step performance="required">
     <para>
      Per montare e scompattare ciascuna immagine ISO nella posizione finale, immettere il comando seguente:
     </para>
<screen>mount -o loop <replaceable>path_to_iso</replaceable><replaceable>path_to_repository</replaceable>/<replaceable>product</replaceable>/<replaceable>mediumx</replaceable></screen>
     <para>
      Sostituire <replaceable>path_to_iso</replaceable> con il percorso della copia locale dell'immagine ISO, <replaceable>path_to_repository</replaceable> con la directory di origine del server, <replaceable>product</replaceable> con il nome del prodotto e <replaceable>mediumx</replaceable> con il tipo (CD o DVD) e il numero di supporto utilizzato.
     </para>
    </step>
    <step performance="required">
     <para>
      Ripetere il passaggio precedente per montare tutte le immagini ISO necessarie per il prodotto.
     </para>
    </step>
    <step performance="required">
     <para>
      Avviare il server di installazione normalmente, come descritto nella <xref linkend="sec.deployment.remoteinst.instserver.nfs"/>, <xref linkend="sec.deployment.remoteinst.instserver.ftp"/> o <xref linkend="sec.deployment.remoteinst.instserver.http"/>.
     </para>
    </step>
   </procedure>
   <para>
    Per montare automaticamente le immagini ISO all'avvio, aggiungere i rispettivi punti di montaggio a <filename>/etc/fstab</filename>. Secondo l'esempio precedente, una voce avrà l'aspetto seguente:
   </para>
<screen><replaceable>path_to_iso</replaceable> <replaceable>path_to_repository</replaceable>/<replaceable>product</replaceable><replaceable>medium</replaceable> auto loop</screen>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.boot">
  <title>Preparazione dell'avvio del sistema di destinazione</title>

  <para>
   In questa sezione vengono illustrate le attività di configurazione necessarie per scenari di avvio complessi. Vengono presentati esempi di configurazione pronti all'uso per DHCP, avvio PXE, TFTP e Wake on LAN.
  </para>

  <sect2 id="sec.deployment.remoteinst.boot.dhcp">
   <title>Configurazione di un server DHCP</title>
   <para>
    Esistono due tipi di configurazione di un server DHCP. Per <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>, YaST fornisce un'interfaccia grafica al processo. Gli utenti possono anche modificare manualmente i file di configurazione.<phrase os="sles">Per ulteriori informazioni DHCP, consultare anche <xref linkend="cha.dhcp"/>.</phrase>
   </para>
   <sect3 id="sec.deployment.remoteinst.boot.dhcp.yast">
    <title>Configurazione di un server DHCP con YaST</title>
    <para>
     Per annunciare ai client di rete la posizione del server TFTP e specificare il file immagine di avvio che deve utilizzare la destinazione dell'installazione, aggiungere due dichiarazioni alla configurazione del server DHCP.
    </para>
    <procedure id="proc.deployment.remoteinst.boot.dhcp.yast">
     <step performance="required">
      <para>
       Eseguire il login come <systemitem class="username">root</systemitem> nel computer che ospita il server DHCP.
      </para>
     </step>
     <step performance="required">
      <para>
       Avviare <menuchoice><guimenu>YaST</guimenu><guimenu>Servizi di rete</guimenu><guimenu>Server DHCP</guimenu></menuchoice>.
      </para>
     </step>
     <step performance="required">
      <para>
       Completare la procedura guidata per la configurazione di base del server DHCP.
      </para>
     </step>
     <step id="st.deployment.remoteinst.boot.dhcp.yast.expert" performance="required">
      <para>
       Alla richiesta di chiusura della finestra di dialogo di avvio, scegliere <guimenu>Impostazioni avanzate</guimenu>, quindi <guimenu>Sì</guimenu>.
      </para>
     </step>
     <step performance="required">
      <para>
       Nella finestra di dialogo <guimenu>Dichiarazioni configurate</guimenu>, scegliere la sottorete in cui deve posizionarsi il nuovo sistema, quindi fare clic su <guimenu>Modifica</guimenu>.
      </para>
     </step>
     <step performance="required">
      <para>
       Nella finestra di dialogo <guimenu>Configurazione sottorete</guimenu> scegliere <guimenu>Aggiungi</guimenu> per aggiungere una nuova opzione alla configurazione della sottorete.
      </para>
     </step>
     <step performance="required">
      <para>
       Scegliere <option>filename</option> e inserire il valore <literal>pxelinux.0</literal>.
      </para>
     </step>
     <step performance="required">
      <para>
       Aggiungere un'altra opzione (<option>next-server</option>) e impostarne il valore sull'indirizzo del server TFTP.
      </para>
     </step>
     <step performance="required">
      <para>
       Scegliere <guimenu>OK</guimenu>, quindi <guimenu>Fine</guimenu> per completare la configurazione del server DHCP.
      </para>
     </step>
    </procedure>
    <para>
     Per configurare il DHCP in modo da fornire un indirizzo IP statico a un host specifico, accedere alle <guimenu>Impostazioni avanzate</guimenu> del modulo di configurazione del server DHCP (<xref linkend="st.deployment.remoteinst.boot.dhcp.yast.expert" xrefstyle="StepOnPage"/>), quindi aggiungere una nuova dichiarazione del tipo di host. Aggiungere le opzioni <option>hardware</option> e <option>fixed-address</option> a questa dichiarazione dell'host, quindi inserire i valori appropriati.
    </para>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.boot.dhcp.man">
    <title>Configurazione manuale di un server DHCP</title>
    <para>
     Oltre a fornire un'allocazione di indirizzo automatica ai client della rete in uso, il server DHCP annuncia l'indirizzo IP del server TFTP e il file che deve essere ricavato dalle routine di installazione nel computer di destinazione.
    </para>
    <procedure id="proc.deployment.remoteinst.boot.dhcp.man">
     <step performance="required">
      <para>
       Eseguire il login come <systemitem class="username">root</systemitem> nel computer che ospita il server DHCP.
      </para>
     </step>
     <step performance="required">
      <para>
       Aggiungere le righe seguenti ad una configurazione di sottorete del file di configurazione del server DHCP ubicato in <filename>/etc/dhcpd.conf</filename>:
      </para>
<screen>subnet 192.168.1.0 netmask 255.255.255.0 { 
  range dynamic-bootp 192.168.1.200 192.168.1.228;
  # PXE related settings
  #
  # "next-server" defines the TFTP server that will be used
  next-server <replaceable>ip_tftp_server</replaceable>;
  #
  # "filename" specifies the pxelinux image on the TFTP server
  # the server runs in chroot under /srv/tftpboot
  filename  "pxelinux.0";
}</screen>
      <para>
       Sostituire <replaceable>ip_of_the_tftp_server</replaceable> con l'indirizzo IP effettivo del server TFTP. Per ulteriori informazioni sulle opzioni disponibili in <filename>dhcpd.conf</filename>, vedere la documentazione relativa a <systemitem>dhcpd.conf</systemitem>.
      </para>
     </step>
     <step performance="required">
      <para>
       Riavviare il server DHCP eseguendo il comando <command>systemctl restart dhcpd.service</command>.
      </para>
     </step>
    </procedure>
    <para>
     Se si prevede di utilizzare SSH per il controllo remoto di un'installazione PXE e Wake on LAN, specificare esplicitamente l'indirizzo IP che dovrebbe essere fornito alla destinazione dell'installazione da DHCP. A tale scopo, modificare la configurazione DHCP precedentemente indicata in base all'esempio seguente:
    </para>
<screen>group {
  # PXE related settings
  # 
  # "next-server" defines the TFTP server that will be used 
  next-server <replaceable>ip_tftp_server</replaceable>:
  # 
  # "filename" specifies the pxelinux image on the TFTP server 
  # the server runs in chroot under /srv/tftpboot
  filename "pxelinux.0";
  host test {
    hardware ethernet <replaceable>mac_address</replaceable>; 
    fixed-address <replaceable>some_ip_address</replaceable>;
    }
}
</screen>
    <para>
     L'istruzione host introduce il nome host della destinazione dell'installazione. Per associare il nome host e l'indirizzo IP a un host specifico, è necessario conoscere e specificare l'indirizzo hardware (MAC) del sistema. Sostituire tutte le variabili utilizzate in questo esempio con i valori effettivi che corrispondono all'ambiente in uso.
    </para>
    <para>
     Dopo aver riavviato il server DHCP, viene fornito un indirizzo IP statico per l'host specificato che consente la connessione al sistema tramite SSH.
    </para>
   </sect3>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.tftp">
   <title>Configurazione di un server TFTP</title>
   <para>
    Se si utilizza un'installazione basata su SUSE, è possibile utilizzare YaST per configurare un server TFTP. In alternativa, configurarlo manualmente. Il server TFTP fornisce l'immagine di avvio al sistema di destinazione dopo che questo viene avviato e invia una richiesta per l'immagine.
   </para>
   <sect3 id="sec.deployment.remoteinst.boot.tftp.yast">
    <title>Configurazione di un server TFTP mediante YaST</title>
    <procedure id="proc.deployment.remoteinst.boot.tftp.yast">
     <step performance="required">
      <para>
       Eseguire il login come utente <systemitem class="username">root</systemitem>.
      </para>
     </step>
     <step performance="required">
      <para>
       Avviare <menuchoice><guimenu>YaST</guimenu><guimenu>Servizi di rete</guimenu><guimenu>Server TFTP</guimenu></menuchoice> e installare il pacchetto necessario.
      </para>
     </step>
     <step performance="required">
      <para>
       Fare clic su <guimenu>Abilita</guimenu> per verificare se il server è stato avviato e incluso nelle routine di avvio. Non sono necessarie ulteriori azioni da parte dell'utente per garantire che xinetd esegua tftpd al momento dell'avvio.
      </para>
     </step>
     <step performance="required">
      <para>
       Fare clic su <guimenu>Apri porta nel Firewall</guimenu> per aprire la porta corrispondente nel firewall in esecuzione nel computer in uso. Se nel server in uso non vi sono firewall in esecuzione, questa opzione non è disponibile.
      </para>
     </step>
     <step performance="required">
      <para>
       Fare clic su <guimenu>Sfoglia</guimenu> per esplorare la directory dell'immagine di avvio. La directory di default <filename>/tftpboot</filename> viene creata e selezionata automaticamente.
      </para>
     </step>
     <step performance="required">
      <para>
       Fare clic su <guimenu>Fine</guimenu> per applicare le impostazioni desiderate e avviare il server.
      </para>
     </step>
    </procedure>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.boot.tftp.man">
    <title>Configurazione manuale di un server TFTP</title>
    <procedure id="proc.deployment.remoteinst.boot.tftp.man">
     <step performance="required">
      <para>
       Eseguire il login come <systemitem class="username">root</systemitem> e installare i pacchetti <systemitem>tftp</systemitem> e <systemitem>xinetd</systemitem>.
      </para>
     </step>
     <step performance="required">
      <para>
       Se non sono disponibili, creare le directory <filename>/srv/tftpboot</filename> e <filename>/srv/tftpboot/pxelinux.cfg</filename>.
      </para>
     </step>
     <step performance="required">
      <para>
       Aggiungere i file corrispondenti necessari per l'immagine di avvio come descritto nella <xref linkend="sec.deployment.remoteinst.boot.pxe"/>.
      </para>
     </step>
     <step performance="required">
      <para>
       Modificare la configurazione di xinetd in <filename>/etc/xinetd.d</filename> per verificare che il server TFTP venga eseguito all'avvio:
      </para>
      <substeps performance="required">
       <step performance="required">
        <para>
         Se non esiste, creare un file denominato <filename>tftp</filename> in questa directory mediante il comando <command>touch tftp</command>. Quindi eseguire <command>chmod 755 tftp</command>.
        </para>
       </step>
       <step performance="required">
        <para>
         Aprire il file <filename>tftp</filename> e aggiungere le righe seguenti:
        </para>
<screen>service tftp 
{ 
        socket_type            = dgram
        protocol               = udp 
        wait                   = yes 
        user                   = root 
        server                 = /usr/sbin/in.tftpd 
        server_args            = -s /srv/tftpboot 
        disable                = no 
}
</screen>
       </step>
       <step performance="required">
        <para>
         Salvare il file e riavviare xinetd mediante il comando <command>rcxinetd restart</command>.

        </para>
       </step>
      </substeps>
     </step>
    </procedure>
   </sect3>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.pxe">
   <title>Uso dell'avvio PXE</title>
   <para>
    Alcune informazioni tecniche generali, nonché le specifiche di PXE complete sono disponibili nel documento Preboot Execution Environment (PXE) Specification (<ulink url="http://www.pix.net/software/pxeboot/archive/pxespec.pdf"/>).
   </para>
   <procedure id="proc.deployment.remoteinst.boot.pxe">
    <step performance="required">
     <para>
      Passare alla directory <filename>boot/&lt;architecture&gt;/loader</filename> dell'archivio di installazione in uso e copiare i file <filename>linux</filename>, <filename>initrd</filename>, <filename>message</filename>, <filename>biostest</filename> e <filename>memtest</filename> nella directory <filename>/srv/tftpboot</filename> immettendo quanto indicato di seguito:
     </para>
<screen>cp -a linux initrd message biostest memtest /srv/tftpboot</screen>
    </step>
    <step performance="required">
     <para>
      Installare il pacchetto <systemitem>syslinux</systemitem> direttamente dai DVD di installazione mediante YaST.
     </para>
    </step>
    <step performance="required">
     <para>
      Copiare il file <filename>/usr/share/syslinux/pxelinux.0</filename> nella directory <filename>/srv/tftpboot</filename> immettendo quanto indicato di seguito:
     </para>
<screen>cp -a /usr/share/syslinux/pxelinux.0 /srv/tftpboot
     </screen>
    </step>
    <step performance="required">
     <para>
      Passare alla directory dell'archivio di installazione in uso e copiare il file <filename>isolinux.cfg</filename> in <filename>/srv/tftpboot/pxelinux.cfg/default</filename> immettendo quanto indicato di seguito:
     </para>
<screen>cp -a boot/&lt;architecture&gt;/loader/isolinux.cfg /srv/tftpboot/pxelinux.cfg/default
     </screen>
    </step>
    <step performance="required">
     <para>
      Modificare il file <filename>/srv/tftpboot/pxelinux.cfg/default</filename> e rimuovere le righe che iniziano con <literal>readinfo</literal> e <literal>framebuffer</literal>.
     </para>
    </step>
    <step performance="required">
     <para>

      Inserire le voci seguenti nelle righe aggiuntive delle etichette di default <option>failsafe</option> e <option>apic</option>:
     </para>
     <variablelist>
      <varlistentry>
       <term><option>insmod=<replaceable>kernel module</replaceable></option>
       </term>
       <listitem>
        <para>
         Mediante questa voce, accedere al modulo del Kernel di rete necessario per supportare l'installazione di rete sul client PXE. Sostituire <replaceable>kernel module</replaceable> con il nome del modulo appropriato per il proprio dispositivo di rete.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term><option>netdevice=<replaceable>interface</replaceable></option>
       </term>
       <listitem>
        <para>
         Questa voce definisce l'interfaccia di rete del client che deve essere utilizzata per l'installazione di rete. È necessario soltanto che il client sia dotato di più schede di rete e che sia adattato di conseguenza. Nel caso di una singola scheda di rete, questa voce può essere omessa.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term>
<option>install=nfs://<replaceable>ip_instserver</replaceable>/<replaceable>path_to_repository</replaceable>/DVD1</option>
       </term>
       <listitem>
        <para>
         Questa voce definisce il server NFS e l'archivio per l'installazione del client. Sostituire <replaceable>ip_instserver</replaceable> con l'indirizzo IP effettivo del server dell'installazione. Sostituire <replaceable>path_to_repository</replaceable> con il percorso effettivo dell'archivio. Gli archivi HTTP, FTP o SMB vengono indirizzati in modo simile, ad eccezione del prefisso di protocollo, che deve essere <literal>http</literal>, <literal>ftp</literal> o <literal>smb</literal>.
        </para>
        <important>
         <title>aggiunta di opzioni di avvio</title>
         <para>
          Se è necessario passare altre opzioni di avvio alle routine di installazione, quali i parametri di avvio SSH o VNC, aggiungerli alla voce <option>install. </option> Una panoramica dei parametri e alcuni esempi sono disponibili nella <xref linkend="sec.deployment.remoteinst.bootinst"/>.
         </para>
        </important>
       </listitem>
      </varlistentry>
     </variablelist>
     <tip>
      <title>modifica dei nomi file kernel e initrd</title>
      <para>
       È possibile utilizzare nomi file differenti per le immagini Kernel e initrd. Ciò risulta utile quando si intende fornire sistemi operativi differenti dallo stesso server di avvio. Tenere presente tuttavia che nei nomi file forniti da TFTP per l'avvio PXE è possibile inserire un solo punto.
      </para>
     </tip>
     <para>
      Di seguito viene illustrato il file di esempio <option>/srv/tftpboot/pxelinux.cfg/default.</option> Modificare il prefisso del protocollo per l'archivio in modo che corrisponda alla configurazione della rete e specificare il metodo di connessione preferito al programma di installazione mediante l'aggiunta delle opzioni <literal>vnc</literal> e <literal>vncpassword</literal> o <literal>usessh</literal> e <literal>sshpassword</literal> alla voce <literal>install</literal>. Le righe separate da <literal>\</literal> devono essere immesse come un'unica riga continua senza interruzione di riga e senza <literal>\</literal>.
     </para>
<screen>default harddisk 
        
# default
label linux 
  kernel linux 
  append initrd=initrd ramdisk_size=65536 \
     install=nfs://<replaceable>ip_instserver</replaceable>/<replaceable>path_to_repository</replaceable>/<replaceable>product</replaceable>/DVD1 
	    
# repair
label repair
  kernel linux
  append initrd=initrd splash=silent repair=1 showopts

# rescue 
label rescue 
  kernel linux 
  append initrd=initrd ramdisk_size=65536 rescue=1 

# bios test
label firmware
  kernel linux
  append initrd=biostest,initrd splash=silent install=exec:/bin/run_biostest showopts

#  memory test 
label memtest 
  kernel memtest 

#  hard disk 
label harddisk
  localboot 0

implicit     0 
display      message
prompt       1 
timeout      100</screen>
    </step>
    <step performance="required">
     <para>
      Sostituire <replaceable>ip_instserver</replaceable> e <replaceable>path_to_repository</replaceable> con i valori usati nella configurazione.
     </para>
     <para>
      La sezione seguente rappresenta un breve riferimento alle opzioni PXELINUX utilizzate in questa configurazione. È possibile trovare ulteriori informazioni sulle opzioni disponibili nella documentazione del pacchetto <systemitem>syslinux</systemitem> in <filename>/usr/share/doc/packages/syslinux/</filename>.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.pxeopts">
   <title>Opzioni di configurazione di PXELINUX</title>
   <para>
    Le opzioni elencate di seguito sono solo una parte di tutte le opzioni disponibili per il file di configurazione di PXELINUX.
   </para>
   <variablelist>
    <varlistentry>
     <term><option>APPEND <replaceable>options... </replaceable></option>
     </term>
     <listitem>
      <para>
       Consente di aggiungere una o più opzioni alla riga di comando del Kernel. Queste vengono aggiunte sia per l'avvio automatico, sia per quello manuale. Le opzioni vengono aggiunte all'inizio della riga di comando del Kernel e generalmente possono essere ignorate dalle opzioni del Kernel immesse esplicitamente.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>APPEND -</option>
     </term>
     <listitem>
      <para>
       Indica che non vi sono aggiunte. <option>APPEND</option>, seguito da un singolo trattino come argomento in una sezione <option>LABEL</option>, può essere utilizzato per ignorare l'opzione <option>APPEND</option> generale.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>DEFAULT <replaceable>kernel options</replaceable>...</option>
     </term>
     <listitem>
      <para>
       Consente di impostare la riga di comando del Kernel di default. Se PXELINUX viene avviato automaticamente, agisce come se le voci che seguono DEFAULT fossero state digitate al prompt di avvio, ad eccezione dell'opzione auto che viene aggiunta automaticamente, indicando un avvio automatico.
      </para>
      <para>
       Se non sono presenti file di configurazione o nel file di configurazione non è presente alcuna voce DEFAULT, per default il nome del Kernel è <quote>linux</quote> senza opzioni.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>

     <term><option>IFAPPEND <replaceable>FLAG</replaceable></option>
     </term>
     <listitem>
      <para>
       Aggiunge un'opzione specifica alla riga di comando del kernel a seconda del valore di <replaceable>FLAG</replaceable>. L'opzione <option>IFAPPEND</option> è disponibile solo su PXELINUX. Per <replaceable>FLAG</replaceable> è previsto un valore descritto nella <xref linkend="tab.deployment.boot.pxeopts.ifappend"/>:
      </para>
      <table id="tab.deployment.boot.pxeopts.ifappend">
       <title>Opzioni della riga di comando del kernel generate e aggiunte da <option>IFAPPEND</option></title>
       <tgroup cols="2">
        <colspec colwidth="1*"/>
        <colspec colwidth="4*"/>
        <thead>
         <row>
          <entry valign="middle">
           <para>
            Argomento
           </para>
          </entry>
          <entry>
           <para>
            Riga di comando del kernel generata/Descrizione
           </para>
          </entry>
         </row>
        </thead>
        <tbody>
         <row>
          <entry>
           <para>
            <option>1</option>
           </para>
          </entry>
          <entry>
<screen>ip=<replaceable>CLIENT_IP</replaceable>:<replaceable>BOOT_SERVER_IP</replaceable>:<replaceable>GW_IP</replaceable>:<replaceable>NETMASK</replaceable></screen>
           <para>
            I segnaposto vengono sostituiti in base all'input ricevuto dal server di avvio DHCP/BOOTP o PXE.
           </para>
           <para>
            Questa opzione non sostituisce l'esecuzione di un client DHCP nel sistema avviato. Senza rinnovi regolari, il lease acquisito dal BIOS PXE scadrà e l'indirizzo IP potrà essere riutilizzato dal server DHCP.
           </para>
          </entry>
         </row>
         <row>
          <entry>
           <para>
            <option>2</option>
           </para>
          </entry>
          <entry>
<screen>BOOTIF=<replaceable>MAC_ADDRESS_OF_BOOT_INTERFACE</replaceable></screen>
           <para>
            Questa opzione è utile se si desidera evitare timeout quando il server di installazione esamina un'interfaccia LAN dopo l'altra fino a quando non ottiene una risposta da un server DHCP. L'utilizzo di questa funzione consente a un programma initrd di determinare l'interfaccia di avvio del sistema. linuxrc legge l'opzione e utilizza l'interfaccia di rete specificate.
           </para>
          </entry>
         </row>
         <row>
          <entry>
           <para>
            <option>4</option>
           </para>
          </entry>
          <entry>
<screen>SYSUUID=<replaceable>SYSTEM_UUID</replaceable></screen>
           <para>
            Aggiunge UUID sotto forma di esadecimali in minuscolo. Vedere <filename>/usr/share/doc/packages/syslinux/pxelinux.txt</filename>
           </para>
          </entry>
         </row>
        </tbody>
       </tgroup>
      </table>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>LABEL <replaceable>label</replaceable> KERNEL <replaceable>image</replaceable> APPEND <replaceable>options</replaceable>...</option>
     </term>
     <listitem>
      <para>
       Indica che, se come Kernel da avviare viene immesso <replaceable>label</replaceable>, PXELINUX deve invece eseguire <replaceable>image</replaceable> e devono essere utilizzate le opzioni <option>APPEND</option> specificate anziché quelle specificate nella sezione generale del file prima del primo comando <option>LABEL</option>. L'impostazione di default di <replaceable>image</replaceable> è la stessa di <replaceable>label</replaceable> e, se non viene fornita alcuna opzione <option>APPEND</option>, per default viene utilizzata la voce generale, se presente. Sono ammesse fino a 128 voci <option>LABEL</option>.
      </para>

      <para>
       PXELINUX utilizza la sintassi seguente:
      </para>
<screen>label <replaceable>mylabel</replaceable> 
  kernel <replaceable>mykernel</replaceable> 
  append <replaceable>myoptions</replaceable></screen>
      <para>
       Le etichette vengono modificate come se fossero nomi di file e devono essere univoche dopo essere state modificate. Le due etichette <quote>v2.6.30</quote> e <quote>v2.6.31</quote>, ad esempio, non sarebbero distinguibili in PXELINUX poiché entrambe vengono modificate e risultano nello stesso nome file DOS.
      </para>
      <para>
       Non è necessario che il kernel sia un kernel Linux. Può anche essere un settore di avvio o un file COMBOOT.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>LOCALBOOT <replaceable>type</replaceable></option>
     </term>
     <listitem>
      <para>
       In PXELINUX, se si specifica <option>LOCALBOOT 0</option> invece dell'opzione <option>KERNEL</option>, viene chiamata questa etichetta e invece del Kernel viene avviato il disco locale.
      </para>
      <informaltable frame="topbot" rowsep="0">
       <tgroup cols="2">

        <thead>
         <row>
          <entry colname="1">
           <para>
            Argomento
           </para>
          </entry>
          <entry colname="2">
           <para>
            Descrizione
           </para>
          </entry>
         </row>
        </thead>
        <tbody>
         <row>
          <entry colname="1">
           <para>
            <literal>0</literal>
           </para>
          </entry>
          <entry colname="2">
           <para>
            Consente di eseguire un avvio normale.
           </para>
          </entry>
         </row>
         <row>
          <entry colname="1">
           <para>
            <literal>4</literal>
           </para>
          </entry>
          <entry colname="2">
           <para>
            Consente di eseguire un avvio locale mediante Universal Network Driver Interface (UNDI) ancora residente in memoria.
           </para>
          </entry>
         </row>
         <row>
          <entry colname="1">
           <para>
            <literal>5</literal>
           </para>
          </entry>
          <entry colname="2">
           <para>
            Consente di eseguire l'avvio locale mediante lo stack PXE completo, incluso il driver UNDI, ancora residente in memoria.
           </para>
          </entry>
         </row>
        </tbody>
       </tgroup>
      </informaltable>
      <para>
       Tutti gli altri valori non sono definiti. Se non si conoscono gli stack UNDI o PXE, specificare <literal>0</literal>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>TIMEOUT <replaceable>time-out</replaceable></option>
     </term>
     <listitem>
      <para>
       Indica il tempo di attesa al prompt di avvio prima dell'avvio automatico, espresso in unità di 1/10 di secondo. Il timeout viene annullato non appena l'utente digita qualsiasi testo sulla tastiera, in base al presupposto che l'utente completi il comando iniziato. Un valore di timeout pari a zero disabilita completamente il timeout. Questa è anche l'impostazione di default. Il valore massimo di timeout possibile è 35996 (poco meno di un'ora).
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>PROMPT<replaceable> flag_val</replaceable></option>
     </term>
     <listitem>
      <para>
       Se <option>flag_val</option> è 0, il prompt di avvio viene visualizzato solo se vengono premuti i tasti <keycap function="shift">Maiusc</keycap> o <keycap function="alt">Alt</keycap> oppure se si imposta <keycap>Bloc Maiusc</keycap> o <keycap>Bloc Scorr</keycap>. Questa è l'impostazione di default. Se <option>flag_val</option> è 1, il prompt di avvio viene sempre visualizzato.
      </para>
<screen>F2  <replaceable>filename</replaceable>
F1  <replaceable>filename</replaceable> 
..etc... 
F9  <replaceable>filename</replaceable>
F10 <replaceable>filename</replaceable></screen>
      <para>
       Consente di visualizzare il file indicato sullo schermo quando viene premuto un tasto funzione al prompt di avvio. Può essere utilizzato per implementare la Guida in linea relativa al preavvio, presumibilmente per le opzioni della riga di comando del Kernel. Per la compatibilità con versioni precedenti, è possibile inoltre immettere <keycap>F10</keycap> come <keycap>F0</keycap>. Si noti che non esiste attualmente alcun modo per associare i nomi file a <keycap>F11</keycap> e <keycap>F12</keycap>.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.pxeprep">
   <title>Preparazione del sistema di destinazione per l'avvio PXE</title>
   <para>
    Preparare il BIOS del sistema per l'avvio PXE includendo l'opzione PXE nell'ordine di avvio del BIOS.
   </para>
   <warning>
    <title>ordine di avvio nel BIOS</title>
    <para>
     Non posizionare l'opzione PXE all'inizio dell'opzione di avvio del disco rigido nel BIOS, altrimenti il sistema cercherà di reinstallarsi a ogni avvio.
    </para>
   </warning>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.wolprep">
   <title>Preparazione del sistema di destinazione per Wake on LAN</title>
   <para>
    Con Wake on LAN (WOL) è necessario che l'opzione BIOS corrispondente sia abilitata prima dell'installazione. Trascrivere inoltre l'indirizzo MAC del sistema di destinazione. Questi dati sono indispensabili per avviare Wake on LAN.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.wol">
   <title>Wake on LAN</title>
   <para>
    Con Wake on LAN è possibile accendere un computer mediante uno speciale pacchetto di rete contenente l'indirizzo MAC del computer. Poiché ogni computer dispone di un identificatore MAC univoco, non si corre il rischio di accendere accidentalmente il computer sbagliato.
   </para>
   <important>
    <title>wake on LAN su segmenti di rete diversi</title>
    <para>
     Se il computer di controllo non si trova nello stesso segmento di rete della destinazione dell'installazione che si desidera attivare, configurare le richieste WOL da inviare come multidiffusioni oppure controllare un computer in remoto in quel segmento di rete per agire come mittente delle richieste.
    </para>
   </important>
   <para>
    Gli utenti di SUSE Linux Enterprise Server possono utilizzare il modulo WOL di YaST per configurare facilmente Wake on LAN. Gli utenti di altre versioni di sistemi operativi basati su SUSE Linux possono utilizzare uno strumento a riga di comando.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.wolyast">
   <title>Wake on LAN con YaST</title>
   <procedure id="proc.deployment.remoteinst.boot.wolyast">
    <step performance="required">
     <para>
      Eseguire il login come utente <systemitem class="username">root</systemitem>.
     </para>
    </step>
    <step performance="required">
     <para>
      Avviare <menuchoice><guimenu>YaST</guimenu><guimenu>Servizi di rete</guimenu><guimenu>WOL</guimenu></menuchoice>.
     </para>
    </step>
    <step performance="required">
     <para>
      Fare clic su <guimenu>Aggiungi</guimenu>, quindi aggiungere il nome e l'indirizzo MAC del sistema di destinazione.
     </para>
    </step>
    <step performance="required">
     <para>
      Per accendere questo computer, scegliere la voce appropriata, quindi fare clic su <guimenu>Wake up (Attiva)</guimenu>.
     </para>
    </step>
   </procedure>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.bootinst">
  <title>Avvio del sistema di destinazione per l'installazione</title>

  <para>
   Esistono due diverse procedure per personalizzare il processo di avvio per l'installazione oltre a quelle illustrate nella <xref linkend="sec.deployment.remoteinst.boot.wol"/> e <xref linkend="sec.deployment.remoteinst.boot.pxe"/>. È possibile utilizzare le opzioni di avvio di default e i tasti funzione oppure il prompt delle opzioni di avvio della schermata di avvio dell'installazione per passare su questo hardware specifico qualsiasi opzione di avvio di cui il Kernel dell'installazione potrebbe avere bisogno.
  </para>

  <sect2 id="sec.deployment.remoteinst.bootinst.opts">
   <title>Uso delle opzioni di avvio di default</title>
   <para>
    Le opzioni di avvio vengono descritte in dettaglio nel <xref linkend="cha.inst"/>. In genere, selezionando <guimenu>Installazione</guimenu> viene avviato il processo di avvio dell'installazione.
   </para>
   <para>
    In caso di problemi, utilizzare <guimenu>Installation—ACPI Disabled (Installazione - ACPI disabilitato)</guimenu> o <guimenu>Installation—Safe Settings (Installazione - Safe Settings)</guimenu>. Per ulteriori informazioni sulla soluzione dei problemi relativi al processo di installazione, vedere la <xref linkend="sec.trouble.install"/>.
   </para>
   <para>
    La barra dei menu nella schermata inferiore presenta alcune funzionalità avanzate necessarie in determinate configurazioni. L'uso dei tasti funzione consente di specificare opzioni aggiuntive da passare alle routine di installazione senza la necessità di conoscere la sintassi dettagliata di questi parametri (vedere <xref linkend="sec.deployment.remoteinst.bootinst.custom"/>). Una descrizione dettagliata dei tasti funzione disponibili è reperibile nella <xref linkend="sec.i.yast2.bootscreen"/>.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.bootinst.custom">
   <title>Uso delle opzioni di avvio personalizzate</title>
   <para>
    L'uso del gruppo appropriato di opzioni di avvio semplifica la procedura di installazione. Molti parametri possono essere configurati successivamente mediante le routine linuxrc, tuttavia l'uso delle opzioni di avvio è più semplice. In alcune configurazioni automatiche, le opzioni di avvio possono essere fornite mediante un file <filename>initrd</filename> o <filename>info </filename>.
   </para>
   <para>
    La tabella seguente elenca tutti gli scenari di installazione indicati in questo capitolo con i parametri necessari per l'avvio e le opzioni di avvio corrispondenti. È sufficiente aggiungerli tutti nell'ordine di apparizione nella tabella per ottenere una stringa dell'opzione di avvio che viene passata alle routine di installazione. Ad esempio (tutto in una sola riga):
   </para>
<screen>install=<replaceable>xxx</replaceable> netdevice<replaceable>=xxx</replaceable> hostip=<replaceable>xxx</replaceable> netmask=<replaceable>xxx</replaceable> vnc=<replaceable>xxx</replaceable> vncpassword=<replaceable>xxx</replaceable></screen>

   <para>
    Sostituire tutti i valori <replaceable>xxx</replaceable> in questa stringa con i valori appropriati per la configurazione.
   </para>
   <variablelist id="tab.deployment.remoteinst.bootinst.custom">
    <varlistentry>
     <term><xref linkend="cha.inst"/>
     </term>
     <listitem>
      <formalpara>
       <title>Parametri necessari per l'avvio</title>
       <para>
        Nessuno
       </para>
      </formalpara>
      <formalpara>
       <title>Opzioni di avvio</title>
       <para>
        Non sono necessarie
       </para>
      </formalpara>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.vncstat"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Parametri necessari per l'avvio</title>
       <listitem>
        <para>
         Posizione del server di installazione
        </para>
       </listitem>
       <listitem>
        <para>
         Dispositivo di rete
        </para>
       </listitem>
       <listitem>
        <para>
         Indirizzo IP
        </para>
       </listitem>
       <listitem>
        <para>
         Maschera di rete
        </para>
       </listitem>
       <listitem>
        <para>
         Gateway
        </para>
       </listitem>
       <listitem>
        <para>
         Abilitazione VNC
        </para>
       </listitem>
       <listitem>
        <para>
         Password VNC
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Opzioni di avvio</title>
       <listitem>
        <para>
         <filename>install=(nfs,http, ftp,smb)://<replaceable>path_to_instmedia</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netdevice= <replaceable>some_netdevice</replaceable></filename> (necessario solo se sono disponibili più dispositivi di rete)
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>hostip=<replaceable>some_ip</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netmask= <replaceable>some_netmask</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>gateway=<replaceable>ip_gateway</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>vnc=1</filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>vncpassword=<replaceable>some_password</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.vncdyn"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Parametri necessari per l'avvio</title>
       <listitem>
        <para>
         Posizione del server di installazione
        </para>
       </listitem>
       <listitem>
        <para>
         Abilitazione VNC
        </para>
       </listitem>
       <listitem>
        <para>
         Password VNC
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Opzioni di avvio</title>
       <listitem>
        <para>
         <filename>install=(nfs,http, ftp,smb)://<replaceable>path_to_instmedia</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <literal>vnc=1</literal>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>vncpassword=<replaceable>some_password</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.vncpxe"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Parametri necessari per l'avvio</title>
       <listitem>
        <para>
         Posizione del server di installazione
        </para>
       </listitem>
       <listitem>
        <para>
         Posizione del server TFTP
        </para>
       </listitem>
       <listitem>
        <para>
         Abilitazione VNC
        </para>
       </listitem>
       <listitem>
        <para>
         Password VNC
        </para>
       </listitem>
      </itemizedlist>
      <formalpara>
       <title>Opzioni di avvio</title>
       <para>
        Non applicabile. Processo gestito mediante PXE e DHCP
       </para>
      </formalpara>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.sshstat"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Parametri necessari per l'avvio</title>
       <listitem>
        <para>
         Posizione del server di installazione
        </para>
       </listitem>
       <listitem>
        <para>
         Dispositivo di rete
        </para>
       </listitem>
       <listitem>
        <para>
         Indirizzo IP
        </para>
       </listitem>
       <listitem>
        <para>
         Maschera di rete
        </para>
       </listitem>
       <listitem>
        <para>
         Gateway
        </para>
       </listitem>
       <listitem>
        <para>
         Abilitazione SSH
        </para>
       </listitem>
       <listitem>
        <para>
         Password SSH
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Opzioni di avvio</title>
       <listitem>
        <para>
         <filename>install=(nfs,http, ftp,smb)://<replaceable>path_to_instmedia</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netdevice= <replaceable>some_netdevice</replaceable></filename> (necessario solo se sono disponibili più dispositivi di rete)
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>hostip=<replaceable>some_ip</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netmask= <replaceable>some_netmask</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>gateway=<replaceable>ip_gateway</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <literal>usessh=1</literal>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>sshpassword= <replaceable>some_password</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.sshdyn"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Parametri necessari per l'avvio</title>
       <listitem>
        <para>
         Posizione del server di installazione
        </para>
       </listitem>
       <listitem>
        <para>
         Abilitazione SSH
        </para>
       </listitem>
       <listitem>
        <para>
         Password SSH
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>Opzioni di avvio</title>
       <listitem>
        <para>
         <filename>install=(nfs,http, ftp,smb)://<replaceable>path_to_instmedia</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>usessh=1</filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>sshpassword= <replaceable>some_password</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.sshpxe"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <listitem>
        <para>
         Posizione del server di installazione
        </para>
       </listitem>
       <listitem>
        <para>
         Posizione del server TFTP
        </para>
       </listitem>
       <listitem>
        <para>
         Abilitazione SSH
        </para>
       </listitem>
       <listitem>
        <para>
         Password SSH
        </para>
       </listitem>
      </itemizedlist>
      <formalpara>
       <title>Opzioni di avvio</title>
       <para>
        Non applicabile. Processo gestito mediante PXE e DHCP
       </para>
      </formalpara>
     </listitem>
    </varlistentry>
   </variablelist>
   <tip>
    <title>ulteriori informazioni sulle opzioni di avvio di linuxrc</title>
    <para>
     Per ulteriori informazioni sulle opzioni di avvio linuxrc utilizzate per l'avvio di un sistema Linux, vedere <ulink url="http://en.opensuse.org/SDB:Linuxrc"/>.
    </para>
   </tip>
   <sect3 id="sec.deployment.remoteinst.bootinst.custom.addon">
    <title>Installazione di prodotti aggiuntivi e aggiornamenti del driver</title>
    <para>
     <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> supporta l'installazione dei prodotti aggiuntivi che forniscono le estensioni (ad esempio SUSE Linux Enterprise High Availability Extension), prodotti e driver di terze parti o software aggiuntivo. Per installare automaticamente un prodotto aggiuntivo quando si distribuisce <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> in remoto, specificare il parametro <literal>addon=<replaceable>REPOSITORY</replaceable></literal>.
    </para>
    <para>
     <replaceable>REPOSITORY</replaceable> deve essere un archivio ospitato che può essere letto da YaST (YaST2 o YUM (rpm-md)). Le immagini ISO al momento non sono supportate.
    </para>
    <tip>
     <title>aggiornamenti del driver </title>
     <para>
      Gli aggiornamenti del driver sono disponibili in <ulink url="http://drivers.suse.com/"/>. Non tutti gli aggiornamenti del driver vengono forniti come archivi, alcuni sono disponibili solo come immagini iso, pertanto non è possibile installarli con il parametro <literal>addon</literal>. Le istruzioni su come installare gli aggiornamenti del driver mediante l'immagine iso sono disponibili in <ulink url="http://drivers.suse.com/doc/SolidDriver/Driver_Kits.html"/>.
     </para>
    </tip>
   </sect3>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.monitor">
  <title>Monitoraggio del processo di installazione</title>

  <para>
   Vi sono diverse opzioni per eseguire il monitoraggio remoto del processo di installazione. Se sono state specificate le opzioni di avvio corrette, è possibile utilizzare VNC o SSH per controllare l'installazione e la configurazione del sistema da una workstation remota.
  </para>

  <sect2 id="sec.deployment.remoteinst.monitor.vnc">
   <title>Installazione VNC</title>
   <para>
    L'uso di qualsiasi visualizzatore VNC consente di controllare in remoto l'installazione di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> virtualmente da ogni sistema operativo. In questa sezione viene illustrata la configurazione eseguita utilizzando un visualizzatore VNC o un browser Web.
   </para>
   <sect3 id="sec.deployment.remoteinst.monitor.vnc.prep">
    <title>Preparazione per l'installazione VNC</title>
    <para>
     Per preparare la destinazione dell'installazione a un'installazione VNC è sufficiente fornire le opzioni di avvio appropriate all'avvio iniziale (vedere la <xref linkend="sec.deployment.remoteinst.bootinst.custom"/>). Il sistema di destinazione viene avviato avvia in un ambiente basato su testo e attende la connessione di un client VNC al programma di installazione.
    </para>
    <para>
     Il programma di installazione annuncia l'indirizzo IP e il numero di display necessario alla connessione per l'installazione. Se si dispone dell'accesso fisico al sistema di destinazione, questa informazione viene fornita subito dopo l'avvio del sistema per l'installazione. Immettere questi dati quando il client VNC li richiede e fornire la password VNC in uso.
    </para>
    <para>
     Poiché la destinazione dell'installazione annuncia se stessa tramite OpenSLP, è possibile recuperare le informazioni relative all'indirizzo della destinazione dell'installazione mediante un browser SLP senza la necessità di un contatto fisico con l'installazione stessa, a condizione che la configurazione di rete in uso e tutti i computer supportino OpenSLP:
    </para>
    <procedure id="proc.deployment.remoteinst.monitor.vnc.prep">
     <title>Individuazione delle installazioni VNC tramite OpenSLP</title>
     <step performance="required">
      <para>
       Per ottenere un elenco di tutti i servizi che offrono vnc, eseguire il comando <command>slptool findsrvtypes | grep vnc</command>. Le destinazioni dell'installazione vnc dovrebbero essere disponibili in un servizio denominato <literal>YaST.installation.suse</literal>.
      </para>
     </step>
     <step performance="required">
      <para>
       Per ottenere un elenco delle installazioni disponibili, eseguire il comando <command>slptool findsrvs <replaceable>YaST.installation.suse</replaceable></command>. Utilizzare l'indirizzo IP e la porta (in genere <systemitem>5901</systemitem>) forniti con il visualizzatore VNC.
      </para>
     </step>
    </procedure>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.monitor.vnc.connect">
    <title>Connessione al programma di installazione</title>
    <para>
     Esistono due modi per connettersi al server VNC, in questo caso la destinazione dell'installazione. È possibile avviare un visualizzatore VNC come applicazione indipendente in qualsiasi sistema operativo oppure connettersi utilizzando un browser Web con supporto Java.
    </para>
    <para>
     L'uso di VNC consente di controllare l'installazione di un sistema Linux da qualsiasi altro sistema operativo, incluse altre varianti Linux, Windows o Mac OS.
    </para>
    <para>
     In un computer Linux, verificare che il pacchetto <systemitem>tightvnc </systemitem> sia installato. In un computer Windows, installare la versione Windows di questa applicazione, che può essere ottenuta dalla home page di TightVNC (<ulink url="http://www.tightvnc.com/download.html"/>).
    </para>
    <procedure id="proc.deployment.remoteinst.monitor.vnc.connect.app">
     <para>
      Per connettersi al programma di installazione in esecuzione nel computer di destinazione, procedere come indicato di seguito:
     </para>
     <step performance="required">
      <para>
       Avviare il visualizzatore VNC.
      </para>
     </step>
     <step performance="required">
      <para>
       Immettere l'indirizzo IP e il numero di display della destinazione dell'installazione fornito dal browser SLP o dal programma di installazione stesso:

      </para>
<screen><replaceable>ip_address</replaceable>:<replaceable>display_number</replaceable></screen>
      <para>
       Sul desktop si apre una finestra in cui vengono visualizzate le schermate YaST come per una normale installazione locale.
      </para>
     </step>
    </procedure>
    <para>
     L'uso di un browser Web per la connessione al programma di installazione rende l'utente completamente indipendente da qualsiasi software VNC o sistema operativo sottostante. Se il proprio browser supporta Java, è possibile utilizzarne uno qualsiasi (Firefox, Internet Explorer, Chromium, Opera ecc.) per eseguire l'installazione del sistema Linux.
    </para>
    <para>
     Per eseguire un'installazione VNC, procedere come indicato di seguito:
    </para>
    <procedure id="proc.deployment.remoteinst.monitor.vnc.connect.brow">
     <step performance="required">
      <para>
       Avviare il browser Web preferito.
      </para>
     </step>
     <step performance="required">
      <para>
       Al prompt dell'indirizzo, immettere quanto indicato di seguito:

      </para>
<screen>http://<replaceable>ip_address_of_target</replaceable>:5801</screen>
     </step>
     <step performance="required">
      <para>
       Immettere la password VNC quando viene richiesta. Nella finestra del browser vengono ora visualizzate le schermate YaST come per una normale installazione locale.
      </para>
     </step>
    </procedure>
   </sect3>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.monitor.ssh">
   <title>Installazione SSH</title>
   <para>
    L'uso di SSH consente di controllare in remoto l'installazione del computer Linux utilizzando un client SSH.
   </para>
   <sect3 id="sec.deployment.remoteinst.monitor.ssh.prep">
    <title>Preparazione per l'installazione SSH</title>
    <para>
     Oltre a installare il pacchetto software più appropriato (OpenSSH per Linux e PuTTY per Windows), è necessario passare le opzioni di avvio appropriate per abilitare SSH all'installazione. Per ulteriori informazioni, vedere <xref linkend="sec.deployment.remoteinst.bootinst.custom"/>. Per default OpenSSH viene installato in ogni sistema operativo basato su SUSE Linux.
    </para>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.monitor.ssh.connect">
    <title>Connessione al programma di installazione</title>
    <procedure id="proc.deployment.remoteinst.monitor.ssh.connect">
     <step performance="required">
      <para>
       Recuperare l'indirizzo IP della destinazione dell'installazione. Se si dispone dell'accesso fisico al computer di destinazione, utilizzare l'indirizzo IP fornito dalla routine di installazione alla console dopo l'avvio iniziale. In alternativa, utilizzare l'indirizzo IP assegnato a quel determinato host nella configurazione del server DHCP.
      </para>
     </step>
     <step performance="required">
      <para>
       Nella riga di comando immettere il comando seguente:
      </para>
<screen>ssh -X root@<replaceable>
ip_address_of_target</replaceable></screen>
      <para>
       Sostituire <replaceable>ip_address_of_target</replaceable> con l'indirizzo IP effettivo della destinazione dell'installazione.
      </para>
     </step>
     <step performance="required">
      <para>
       Alla richiesta di un nome utente, immettere <literal>root</literal>.
      </para>
     </step>
     <step performance="required">
      <para>
       Alla richiesta della password, immettere la password impostata tramite l'opzione di avvio SSH. Al termine dell'autenticazione viene visualizzato un prompt della riga di comando per la destinazione dell'installazione.
      </para>
     </step>
     <step performance="required">
      <para>
       Immettere <command>yast</command> per avviare il programma di installazione. Viene aperta una finestra in cui vengono visualizzate le normali schermate YaST, come descritto nel <xref linkend="cha.inst"/>.
      </para>
     </step>
    </procedure>
   </sect3>
  </sect2>
 </sect1>
</chapter>
