<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter
[
  <!ENTITY % entities SYSTEM "entity-decl.ent">
    %entities;
]>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.0" xml:id="cha.gnome.settings">
 <title>Customizing Your Settings</title>
 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker></dm:bugtracker>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>
 <para>
  You can change the way the &gnome; desktop looks and behaves to suit your own
  personal tastes and needs. Some possible changes of settings are:
 </para>
 <itemizedlist mark="bullet" spacing="normal">
  <listitem>
   <para>
    Keyboard and mouse configuration, as described in
    <xref linkend="sec.gnome.settings.hardware.keyboard"/> and
    <xref linkend="sec.gnome.settings.hardware.mouse"/>
   </para>
  </listitem>
  <listitem>
   <para>
    Desktop background, as described in
    <xref linkend="sec.gnome.settings.look.deskback"/>
   </para>
  </listitem>
  <listitem>
   <para>
    Sounds, as described in <xref linkend="sec.gnome.settings.hardware.audio"/>
   </para>
  </listitem>
 </itemizedlist>
 <para>
  These settings and others can be changed in the <guimenu>All
  Settings</guimenu> dialog.
 </para>
 <sect1 xml:id="sec.gnome.settings.cc">
  <title>The &gnome; Settings Dialog</title>

  <para>
   Whereas &yast; is a desktop-independent system-wide tool to configure most
   aspects of your product installation, the settings dialog is a &gnome;
   configuration tool. It focuses on look and feel, personal settings and
   preferences of your &gnome; desktop.
  </para>

  <para>
   To access the GNOME settings dialog, click <menuchoice>
   <guimenu>Applications</guimenu> <guimenu>System Tools</guimenu>
   <guimenu>Settings</guimenu> </menuchoice>. The dialog is divided into the
   following three categories:
  </para>

  <variablelist>
   <varlistentry>
    <term><guimenu>Personal</guimenu>
    </term>
    <listitem>
     <para>
      From here, you can change the background of your desktop or of the lock
      screen and configure language settings. For more information, see
      <xref linkend="sec.gnome.settings.personal"/>.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><guimenu>Hardware</guimenu>
    </term>
    <listitem>
     <para>
      Allows you to configure hardware components such as monitors, printers,
      mouses/touchpads, network adapters and sound devices. You can also change
      key combination settings and set up power-saving features. For more
      information, see <xref linkend="sec.gnome.settings.hardware"/>.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><guimenu>System</guimenu>
    </term>
    <listitem>
     <para>
      Lets you configure system settings such as date and time, whether to
      start software when inserting flash disks or whether you want to share
      your screen with others. You can also set up user accounts. If you want,
      you can also start &yast; from this screen, though it is also available
      separately from within the menu. For more information, see
      <xref linkend="sec.gnome.settings.system"/>.
     </para>
    </listitem>
   </varlistentry>
  </variablelist>

  <figure>
   <title>&gnome; Settings Dialog</title>
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="controlcenter.png" width="90%" format="PNG"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="controlcenter.png" width="75%" format="PNG"/>
    </imageobject>
   </mediaobject>
  </figure>

  <para>
   To change some system-wide settings, the control center will prompt you for
   the <systemitem>root</systemitem> password and start &yast;. This is mostly
   the case for administrator settings (including most of the hardware, the
   graphical user interface, Internet access, security settings, user
   administration, software installation and system updates and information).
   Follow the instructions in &yast; to configure these settings. For
   information about using &yast;, refer to the integrated &yast; help texts or
   to the
   <phrase os="sles;sled"><xref
   linkend="book.sle.deployment"/></phrase><phrase os="osuse"><xref
   linkend="book.opensuse.startup"/></phrase>.
  </para>

  <para>
   This chapter focuses on individual settings you can change directly in the
   &gnome; settings dialog, without having to use &yast;.
  </para>
 </sect1>
 <sect1 xml:id="sec.gnome.settings.personal">
  <title>Personal</title>

  <para>
   The following sections introduce examples of how to configure some personal
   aspects of your &gnome; desktop, like your languages used or desktop
   backgrounds.
  </para>

  <sect2 xml:id="sec.gnome.settings.look.deskback">
   <title>Changing the Desktop Background</title>
   <para>
    The desktop background is the image or color that is applied to your
    desktop. You can also customize the image shown when the screen is locked.
   </para>
   <para>
    To change the desktop background or the lock screen:
   </para>
   <procedure>
    <step>
     <para>
      Click <menuchoice> <guimenu>Applications</guimenu> <guimenu>System
      Tools</guimenu> <guimenu>Settings</guimenu> <guimenu>Background</guimenu>
      </menuchoice>.
     </para>
<!--<informalfigure>
      <mediaobject>
       <imageobject role="fo">
        <imagedata fileref="preferences_background.png" width="45%"
         format="PNG"/>
       </imageobject>
       <imageobject role="html">
        <imagedata fileref="preferences_background.png" width="45%"
         format="PNG"/>
       </imageobject>
      </mediaobject>
     </informalfigure>-->
    </step>
    <step>
     <para>
      Click <guimenu>Background</guimenu> or <guimenu>Lock Screen</guimenu>.
     </para>
    </step>
    <step>
     <para>
      Click <guimenu>Wallpapers</guimenu>, <guimenu>Pictures</guimenu>, or
      <guimenu>Colors</guimenu>.
     </para>
     <para>
      Wallpapers are preconfigured images distributed with your system.
      Pictures are your own images from your <filename>Pictures</filename>
      directory (<filename>~/Pictures</filename>). Colors are predefined colors
      chosen by &gnome; developers.
     </para>
    </step>
    <step>
     <para>
      Choose an option from the list.
     </para>
    </step>
    <step>
     <para>
      When you are satisfied with your choice, click <guimenu>Select</guimenu>.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.lang">
   <title>Configuring Language Settings</title>
   <para>
    &productname; can be configured to use any of several languages. The
    language setting determines the language of dialogs and menus and can also
    determine the keyboard and clock layout.
   </para>
   <para>
    To configure your language settings click <menuchoice>
    <guimenu>Applications</guimenu> <guimenu>System Tools</guimenu>
    <guimenu>Settings</guimenu> <guimenu>Region and Language</guimenu>
    </menuchoice>.
   </para>
   <para>
    Here you can choose:
   </para>
   <itemizedlist>
    <listitem>
     <para>
      Interface language.
     </para>
    </listitem>
    <listitem>
     <para>
      Date and number formats, currency and related options.
     </para>
    </listitem>
    <listitem>
     <para>
      Input sources (keyboard layout). For non-alphabetic languages there can
      be additional settings.
     </para>
    </listitem>
   </itemizedlist>
  </sect2>
 </sect1>
 <sect1 xml:id="sec.gnome.settings.hardware">
  <title>Hardware</title>

  <para>
   In the following sections you will find examples of how to configure some
   hardware aspects of your &gnome; desktop, including keyboard or mouse
   preferences, handling of removable drives (and other media) or screen
   resolution.
  </para>

  <sect2 xml:id="sec.gnome.settings.hardware.bluetooth">
   <title>Configuring Bluetooth Settings</title>
   <para>
    <remark>taroth 2016-05-25: FIXME - need to check this on a laptop</remark>
    The Bluetooth module lets you set the visibility of your machine over
    Bluetooth and connect to available Bluetooth devices. To configure
    Bluetooth connectivity, follow these steps:
   </para>
   <procedure>
    <step>
     <para>
      Click <menuchoice> <guimenu>Applications</guimenu> <guimenu>System
      Tools</guimenu> <guimenu>Settings</guimenu> <guimenu>Bluetooth</guimenu>
      </menuchoice> to open the Bluetooth settings module.
     </para>
    </step>
    <step>
     <para>
      To use Bluetooth, turn the <guimenu>Bluetooth</guimenu> switch on.
     </para>
    </step>
    <step>
     <para>
      To make your computer visible over Bluetooth, turn the
      <guimenu>Visibility</guimenu> switch on. The computer will start
      searching for other visible Bluetooth devices in the vicinity and display
      any found devices in the <guimenu>Devices</guimenu> list. At first, the
      list may be empty.
     </para>
     <note>
      <title>Temporary Visibility</title>
      <para>
       The <guimenu>Visibility</guimenu> switch is meant to be used only
       temporarily. You only need to turn it on for the initial setup of a
       connection to a Bluetooth device. After the connection has been
       established, turn off the switch.
      </para>
     </note>
    </step>
    <step>
     <para>
      On the device you want to connect, turn on Bluetooth connectivity and
      visibility, too.
     </para>
    </step>
    <step>
     <para>
      If the desired device has been found and is shown in the list, click it
      to establish a connection to it.
     </para>
     <para>
      You will be asked whether the PINs of the two devices match.
     </para>
    </step>
    <step>
     <para>
      If the PINs match, confirm this on <emphasis>both</emphasis> your
      computer and the device.
     </para>
     <para>
      Both are now paired. On your computer, the device in the list is shown as
      <guimenu>Connected</guimenu>.
     </para>
     <informalfigure>
      <mediaobject>
       <imageobject role="fo">
        <imagedata fileref="bluetooth_connect.png" width="95%"
         format="PNG"></imagedata>
       </imageobject>
       <imageobject role="html">
        <imagedata fileref="bluetooth_connect.png" width="90%"
         format="PNG"></imagedata>
       </imageobject>
      </mediaobject>
     </informalfigure>
     <para>
      Depending on the device type, you can now either see it as a storage
      device in &nautilus;, set a volume for it in the Sound settings or other
      things.
     </para>
    </step>
   </procedure>
   <para>
    To connect to a paired Bluetooth device, select the device in the list. In
    the dialog that appears, turn the <guimenu>Connection</guimenu> switch on.
    You can send files to the connected device by using the <guimenu>Send
    Files</guimenu> button. If you are connected to a device such as a mobile
    phone, you can use it as a network device by activating the appropriate
    option.
   </para>
   <para>
    To remove a connected device from the list on your computer, click
    <guimenu>Remove Device</guimenu> and confirm your choice. To completely
    removed the pairing, you also need to do so on your device.
   </para>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.hardware.power">
   <title>Configuring Power Settings</title>
   <procedure>
    <step>
     <para>
      Click <menuchoice> <guimenu>Applications</guimenu> <guimenu>System
      Tools</guimenu> <guimenu>Settings</guimenu> <guimenu>Power</guimenu>
      </menuchoice> to open the Power settings module.
     </para>
    </step>
    <step>
     <para>
      In the upper part of the dialog, you can see the current state of the
      battery.
     </para>
    </step>
    <step>
     <para>
      In the <guimenu>Power Saving</guimenu> section of the dialog, set the
      <guimenu>Screen Brightness</guimenu> to conserve power. You can also set
      whether to dim the screen after a period of inactivity and set the time
      interval. You can also set whether to turn off wireless networking after
      the period of inactivity.
     </para>
    </step>
    <step>
     <para>
      In the <guimenu>Suspend and Power Button</guimenu> section of the dialog,
      set the <guimenu>Automatic Suspend</guimenu>. When you click it, a
      separate dialog opens.
     </para>
     <para>
      In it, you can turn on automatic suspending and associated time
      intervals. If you are using a computer with a battery, you can set these
      separately for computer running on battery power or plugged in.
     </para>
     <para>
      You can also set the action performed when the power button is pressed.
      Choose <guimenu>Hibernate</guimenu> to use a mode where the computer
      turns off completely but saves your running session to the hard disk.
      Alternatively, choose <guimenu>Suspend</guimenu> or
      <guimenu>Nothing</guimenu>.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.hardware.keyboard">
   <title>Modifying Keyboard Shortcuts</title>
   <para>
    To modify keyboard shortcuts click <menuchoice>
    <guimenu>Applications</guimenu> <guimenu>System Tools</guimenu>
    <guimenu>Settings</guimenu> <guimenu>Keyboard</guimenu> </menuchoice>.
   </para>
<!--taroth 2016-05-25: commenting as key autorepetition
     and the cursor blink rate have moved to 'Universal Access':
     To modify keyboard settings (such as key autorepetition
     and the cursor blink rate),click <menuchoice>
     <guimenu>Applications</guimenu> <guimenu>System Tools</guimenu>
     <guimenu>Settings</guimenu> <guimenu>Keyboard</guimenu> </menuchoice>
     -->
   <figure xml:id="fig.gnome.settings.hardware.keyboard">
    <title>Keyboard Dialog</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="preferences_keyboard.png" width="50%" format="PNG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="preferences_keyboard.png" width="55%" format="PNG"/>
     </imageobject>
    </mediaobject>
   </figure>
   <para>
    The <guimenu>Keyboard</guimenu> dialog shows the keyboard shortcuts that
    are configured for your system. Click the categories on the right to view
    the current shortcuts.
   </para>
   <para>
    To edit a key combination, first click the row. To set a new key
    combination, press the keys. To disable a shortcut, press
    <keycap
     function="backspace"/> instead.
   </para>
<!--
    taroth 2016-05-25: no longer available in the dialog,
    maybe it can be re-used for the accessibility options...
    <itemizedlist>
    <listitem>
     <para>
      On the <guimenu>Typing</guimenu> tab, you can set some general keyboard
      preferences, such as enabling keyboard repeat with individual delay
      and speed options or enabling or disabling the blinking of the cursor
      and defining the speed.
     </para>
    </listitem>
    <listitem>
     <para>
      On the <guimenu>Shortcuts</guimenu> tab, you can set key combinations for
      the desktop.
     </para>
    </listitem>
   </itemizedlist>-->
   <para>
    To configure keyboard accessibility options, refer to
    <xref linkend="sec.general.accessibility.mobility"/>. To configure your
    keyboard layout, refer to <xref linkend="sec.gnome.settings.lang"/>.
   </para>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.hardware.mouse">
   <title>Configuring the Mouse and Touchpad</title>
   <para>
    To modify mouse and touchpad options, click <menuchoice>
    <guimenu>Applications</guimenu> <guimenu>System Tools</guimenu>
    <guimenu>Settings</guimenu> <guimenu>Mouse and Touchpad</guimenu>
    </menuchoice>.
   </para>
   <figure xml:id="fig.gnome.settings.hardware.mouse">
    <title>Mouse and Touchpad Settings Dialog</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="preferences_mouse.png" width="55%" format="PNG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="preferences_mouse.png" width="45%" format="PNG"/>
     </imageobject>
    </mediaobject>
   </figure>
   <itemizedlist>
    <listitem>
     <para>
      In the <guimenu>General</guimenu> section of the dialog, you can set the
      <guimenu>Primary button</guimenu> orientation (left or right).
<!-- and the
      speed of the double-click.-->
     </para>
    </listitem>
    <listitem>
     <para>
      In the <guimenu>Mouse</guimenu> section of the dialog, use <guimenu>Mouse
      Speed</guimenu> to adjust the sensitivity of the mouse pointer.
     </para>
    </listitem>
    <listitem>
     <para>
      In the <guimenu>Touchpad</guimenu> section of the dialog, you can turn
      the touchpad on and off. Use <guimenu>Touchpad Speed</guimenu> to adjust
      the sensitivity of the touchpad pointer. You can also disable the
      touchpad while typing and enable clicks by tapping the touchpad.
     </para>
    </listitem>
    <listitem>
     <para>
      To test your settings, click <guimenu>Test Your Settings</guimenu> and
      try the pointing device.
<!-- Click <guimenu>Done</guimenu> when you are finished.-->
     </para>
    </listitem>
   </itemizedlist>
   <para>
    For configuration of mouse accessibility options, refer to the
    <xref linkend="sec.general.accessibility.mobility"/>.
   </para>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.hardware.printer">
   <title>Installing and Configuring Printers</title>
   <para>
    The <guimenu>Printers</guimenu> module lets you connect to any available
    local or remote CUPS server and configure printers.
   </para>
   <para>
    To start the Printers module, click <menuchoice>
    <guimenu>Applications</guimenu> <guimenu>System Tools</guimenu>
    <guimenu>Settings</guimenu> <guimenu>Printers</guimenu> </menuchoice>. For
    detailed information, refer to <xref linkend="cha.gnome.print"/>.
   </para>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.hardware.screen">
   <title>Configuring Screens</title>
   <para>
    <remark>taroth 2016-05-25: FIXME - would need to check this on bare
    metal, not on a VM</remark>
    To specify resolution and orientation for your screen or to configure
    multiple screens, click <menuchoice> <guimenu>Applications</guimenu>
    <guimenu>System Tools</guimenu> <guimenu>Settings</guimenu>
    <guimenu>Displays</guimenu> </menuchoice>.
   </para>
   <procedure>
    <title>Changing the Settings for a Monitor</title>
    <step>
     <para>
      To find the right monitor, look for the numbers displayed in the upper
      left corner of all monitors after you have opened the
      <guimenu>Display</guimenu> dialog. To set options for a monitor, click
      the list item of the monitor. A new dialog appears.
     </para>
    </step>
    <step>
     <para>
      If multiple monitors are attached to the computer, the left part of the
      dialog will allow you to choose how to use the monitor. You can choose
      between:
     </para>
     <variablelist>
      <varlistentry>
       <term><guimenu>Primary</guimenu>
       </term>
       <listitem>
        <para>
         The screen that shows the panel and important messages.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term><guimenu>Secondary Display</guimenu>
       </term>
<!-- sic! Don't know how this blatant inconsistency got through QA or if
            it actually means something. - sknorr, 2015-09-16 -->
       <listitem>
        <para>
         A monitor that expands the desktop of the primary monitor.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term><guimenu>Mirror</guimenu>
       </term>
       <listitem>
        <para>
         A monitor that mirrors the image on the primary monitor. In terms of
         resolution, the lowest common denominator will be used.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term><guimenu>Turn Off</guimenu>
       </term>
       <listitem>
        <para>
         A screen that is not used.
        </para>
       </listitem>
      </varlistentry>
     </variablelist>
     <para>
      To rotate the displayed image, use the buttons with the arrows pointing
      left and right. To mirror the displayed image, use the button with the
      double arrow icon.
     </para>
     <para>
      You can set a different resolution by changing the value next to
      <guimenu>Resolution</guimenu>. Not all resolutions provide a sharp and
      unstretched image. To find the best resolution for your monitor, refer to
      its manual.
     </para>
    </step>
    <step>
     <para>
      When you are done, click <guimenu>Apply</guimenu>.
     </para>
     <para>
      The monitors will now readjust. This can take multiple seconds during
      which the screen can be black or distorted.
     </para>
     <para>
      Afterward, a confirmation dialog will appear.
     </para>
    </step>
    <step>
     <para>
      If the configuration looks correct, click <guimenu>Keep
      Changes</guimenu>.
     </para>
     <para>
      If the configuration is not what you hoped for, click <guimenu>Revert
      Settings</guimenu> or wait for 20 seconds. The changes will then be
      reverted.
     </para>
    </step>
   </procedure>
   <figure xml:id="fig.gnome.settings.hardware.screen">
    <title>Monitor Resolution Settings Dialog</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="preferences_screen.png" width="45%" format="PNG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="preferences_screen.png" width="45%" format="PNG"/>
     </imageobject>
    </mediaobject>
   </figure>
   <procedure>
    <title>Changing the Arrangement of Multiple Monitors</title>
    <para>
     If you are using multiple screens, set up how they are arranged, so you
     can use the mouse pointer properly across monitors.
    </para>
    <step>
     <para>
      Click <guimenu>Arrange Combined Displays</guimenu>.
     </para>
    </step>
    <step>
     <para>
      To find the right monitor, look for the numbers displayed in the upper
      left corner of all monitors. Click and drag the monitor image around to
      move it.
     </para>
    </step>
    <step>
     <para>
      When you are done, click <guimenu>Apply</guimenu>.
     </para>
    </step>
    <step>
     <para>
      If the configuration looks correct, click <guimenu>Keep
      Changes</guimenu>.
     </para>
     <para>
      If the configuration is not what you hoped for, click <guimenu>Revert
      Settings</guimenu> or wait for 20 seconds. The changes will then be
      reverted.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.hardware.audio">
   <title>Configuring Sound Settings</title>
   <para>
    <remark>taroth 2016-05-25: FIXME - would need to check this on bare
    metal, not on a VM</remark>
    The <guimenu>Sound</guimenu> tool lets you manage sound devices and set the
    sound effects. In the top part of the dialog, you can select the general
    output volume or turn the sound off completely.
   </para>
   <para>
    To open the sound settings, click
    <menuchoice><guimenu>Applications</guimenu><guimenu>System Tools</guimenu>
    <guimenu>Settings</guimenu><guimenu>Sound</guimenu></menuchoice>.
   </para>
   <figure>
    <title>Configuring Sound Settings</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="sound_devices.png" width="65%" format="PNG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="sound_devices.png" width="65%" format="PNG"/>
     </imageobject>
    </mediaobject>
   </figure>
   <sect3 xml:id="sec.gnome.settings.hardware.audio.dev">
    <title>Configuring Sound Devices</title>
    <para>
     Use the <guimenu>Output</guimenu> tab to select the device for sound
     output. Below the list, choose the sound device setting you prefer, for
     example balance.
    </para>
    <para>
     Use the <guimenu>Input</guimenu> tab to set the input device volume or to
     mute the input temporarily. If you have more than one sound device, you
     can also select a default device for audio input in the <guimenu>Choose a
     device for sound input</guimenu> list.
    </para>
   </sect3>
   <sect3 xml:id="sec.gnome.settings.hardware.audio.effects">
    <title>Configuring Sound Effects</title>
    <para>
     Use the <guimenu>Sound Effects</guimenu> tab to configure whether and how
     you want sound to be played when message boxes appear.
    </para>
    <para>
     Specify the volume at which the sound effects will be played under
     <guimenu>Alert volume</guimenu>. You can also turn the effects on and off.
    </para>
    <para>
     Select the <guimenu>Alert Sound</guimenu> to use.
    </para>
   </sect3>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.system.proxy">
   <title>Networking</title>
   <para>
    To set up networking options, click
    <menuchoice><guimenu>Applications</guimenu><guimenu>System Tools</guimenu>
    <guimenu>Settings</guimenu><guimenu>Network</guimenu></menuchoice>.
   </para>
   <para>
    In the appearing dialog, you can configure wired or wireless connections
    and proxies and VPNs. <phrase os="sled">If you are unsure which network
    parameters to use, refer to your system administrator.</phrase>
   </para>
   <para>
    To learn more about setting up network connections, see
    <xref linkend="cha.nm"/>.
   </para>
  </sect2>
 </sect1>
 <sect1 xml:id="sec.gnome.settings.system">
  <title>System</title>

  <para>
   In the following sections, you will find examples of how to configure some
   system aspects of your &gnome; desktop. These include preferred
   applications, changing your user password, and session sharing preferences.
  </para>

  <para>
   To learn more about configuring assistive technologies, see
   <xref linkend="cha.gnome.accessibility"/>.
  </para>

  <sect2 xml:id="sec.gnome.settings.personal.password">
   <title>Changing Your Password</title>
   <para>
    For security reasons, it is a good idea to change your login password from
    time to time. To change your password:
   </para>
   <procedure>
    <step>
     <para>
      Click <menuchoice><guimenu>Applications</guimenu><guimenu>System
      Tools</guimenu>
      <guimenu>Settings</guimenu><guimenu>Users</guimenu></menuchoice>.
     </para>
    </step>
    <step>
     <para>
      Click the button labeled with dots next to <guimenu>Password</guimenu>.
     </para>
    </step>
    <step>
     <para>
      In the first text box, type your current password.
     </para>
    </step>
    <step>
     <para>
      In the next text box, type a new password.
     </para>
     <para>
      You can also click the cog wheel icon at the end of the text box to
      generate a random password.
     </para>
    </step>
    <step>
     <para>
      Confirm your new password by typing it again in the last text box.
     </para>
    </step>
    <step>
     <para>
      Click <guimenu>Change</guimenu>.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.prefapps">
   <title>Setting Preferred Applications</title>
   <procedure>
    <step>
     <para>
      To change the default application for various common tasks such as
      browsing the Internet, sending mails or playing multimedia files, click
      <menuchoice> <guimenu>Applications</guimenu> <guimenu>System
      Tools</guimenu> <guimenu>Settings</guimenu><guimenu>Details</guimenu>
      </menuchoice>.
     </para>
     <figure>
      <title>Preferred Applications</title>
      <mediaobject>
       <imageobject role="fo">
        <imagedata fileref="preferences_preferred_apps.png" width="65%" format="PNG"/>
       </imageobject>
       <imageobject role="html">
        <imagedata fileref="preferences_preferred_apps.png" width="65%" format="PNG"/>
       </imageobject>
      </mediaobject>
     </figure>
    </step>
    <step>
     <para>
      Click <guimenu>Default Applications</guimenu>.
     </para>
    </step>
    <step>
     <para>
      Select one of the available applications from the drop-down box. You can
      choose an application to handle Web, mail, calendar, music, videos or
      photographs.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 xml:id="sec.gnome.settings.share">
   <title>Setting Session Sharing Preferences</title>
   <para>
    To open a configuration dialog for sharing a &gnome; desktop session
    between multiple users and set session-sharing preferences, click
    <menuchoice><guimenu>Applications</guimenu><guimenu>System Tools</guimenu>
    <guimenu>Settings</guimenu> <guimenu>Sharing</guimenu></menuchoice>.
   </para>
   <important>
    <title>Sharing Desktop Sessions Affects System Security</title>
    <para>
     Sharing desktop sessions can be a security risk. Use the restriction
     options available.
    </para>
   </important>
   <para>
    Before you can share anything, you need to turn on the switch in the upper
    part of the dialog. The switch also helps you if you quickly need to
    disable all sharing options.
   </para>
   <itemizedlist>
    <listitem>
     <para>
      To share your public directory over the network, click <guimenu>Personal
      File Sharing</guimenu> and turn on <guimenu>Share Public Folder On This
      Network</guimenu>. You can also set a password.
     </para>
    </listitem>
    <listitem>
     <para>
      To share your desktop session with other users, click <guimenu>Screen
      Sharing</guimenu> and activate <guimenu>Allow Remote Control</guimenu>.
      To allow other users to control your screen, activate also
      <guimenu>Remote Control</guimenu>. You can also set a password.
     </para>
    </listitem>
    <listitem>
     <para>
      To enable logging in via SSH, click <guimenu>Remote Login</guimenu>.
     </para>
    </listitem>
   </itemizedlist>
   <para>
    All the sharing screens contain an address which you can give to other
    users, so they can reach you. To copy a sharing address, click it and
    select <guimenu>Copy</guimenu>. You can then paste it into an e-mail or
    messaging software.
   </para>
  </sect2>

<!--
   taroth 2016-06-09: the following does not seem to exist any longer, at least
   neither sknorr nor me could find an option to configure this
  -->

<!--
  <sect2 xml:id="sec.gnome.settings.system.updates">
   <title>Setting Software Updates</title>
   <para><remark>taroth 2016-05-25: FIXME - could not find that category, see
    sec.updater.gnome.config and check again with next beta</remark>
    The <guimenu>Software Updates</guimenu> tool lets you configure the
    frequency of update checking and which sources you want to receive updates
    from.
   </para>
   <para>
    Click
    <menuchoice><guimenu>Applications</guimenu><guimenu>System Tools</guimenu>
    <guimenu>Settings</guimenu><guimenu>Software Settings</guimenu></menuchoice>.
   </para>
   <para>For more information, see
    <xref linkend="sec.updater.gnome.config"/>.
   </para>
  </sect2>
  -->

  <sect2 xml:id="sec.gnome.settings.admin">
   <title>Configuring Administrative Settings with &yast;</title>
   <para>
    For your convenience, &yast; is available from the GNOME Settings as well
    as from the Applications menu. For information about using &yast;, refer to
    <phrase os="sles;sled"><xref
    linkend="book.sle.deployment"/></phrase><phrase os="osuse"><xref
    linkend="book.opensuse.startup"/></phrase>.
   </para>
  </sect2>
 </sect1>
</chapter>
