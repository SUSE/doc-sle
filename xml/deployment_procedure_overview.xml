<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter
[
  <!ENTITY % entities SYSTEM "generic-entities.ent">
    %entities;
]>
<chapter version="5.0" xml:id="cha-images-description"
 xmlns="http://docbook.org/ns/docbook"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink">
 <title>Overview of the deployment process</title>
 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker></dm:bugtracker>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>
 <para>
  &productname; provides raw images that can be directly deployed to your
  device storage: a memory card, USB flash drive, or a hard disk. The
  options for which type of device you can deploy the image to depend on your
  particular hardware&mdash;follow the guidance in your vendor documentation.
 </para>

 <para>
  To prepare the setup, you may need two separate devices. One for the raw disk
  image, where &slema; runs, and another one that serves as a configuration
  medium, for example, a USB disk. You can perform basic configuration even
  without the configuration medium. For a more complex configuration, you
  need to prepare the configuration medium. 
 </para>
 <para>
  The deployment process of &productnameshort; pre-built images can be summarized into threes stages:
 </para>
 <variablelist>
  <varlistentry>
  <term>Preparing a configuration device</term>
  <listitem>
    <para>
If you need a more complex configuration other than setting time, &rootuser; password and language,
you have to prepare a configuration device as described in <xref linkend="cha-prepare-device"/>. 
    </para>
  </listitem>
  </varlistentry>
  <varlistentry>
    <term>Deploying the pre-built image</term>
    <listitem>
<para>
After downloading the image, you can boot your machine with  either the configuration device
attached or using &jeosfirstboot; perform basic configuration. See <xref linkend=""/>.
</para>
    </listitem>
  </varlistentry>
  <varlistentry>
    <term>Post-deployment procedures</term>
    <listitem>
      <para>
After you deploy the image, register it or activate extensions. For details, refer to <xref linkend=""/>.
      </para>
    </listitem>
  </varlistentry>
 </variablelist>
</chapter>
