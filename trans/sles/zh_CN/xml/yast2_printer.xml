<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//Novell//DTD NovDoc XML V1.0//EN" "novdocx.dtd">
<!--
*********************************
Please see LICENSE.txt for this document's license.
*********************************
-->
<sect1 xml:base="yast2_printer.xml" id="sec.y2.hw.print">
 <title>设置打印机</title>

 <para>
  YaST 可用于配置通过 USB 直接连接到您的计算机的本地打印机，还可用于设置使用网络打印机进行的打印。还可以通过网络共享打印机。<xref linkend="cha.p"/>中提供了有关打印的进一步信息（一般信息、技术细节和查错）。
 </para>

 <para>
  在 YaST 中，单击<menuchoice><guimenu>硬件</guimenu> <guimenu>打印机</guimenu></menuchoice>以启动打印机模块。默认情况下，其会在<guimenu>打印机配置</guimenu>视图中打开，显示所有可用和已配置打印机的列表。这一点在通过网络访问大量打印机时尤其有用。在这里，您还可以<guimenu>打印测试页</guimenu>及配置打印机。
 </para>

 <note>
  <title>启动 CUPS</title>
  <para>
   要能够从系统打印，CUPS 必须处于运行状态。如果它未在运行，系统会要求您启动它。回答<guimenu>是</guimenu>，否则您将无法配置打印。如果 CUPS 不会在引导时启动，系统还会要求您启用此功能。建议您回答<guimenu>是</guimenu>，否则每次重引导之后，都需要手动启动 CUPS。
  </para>
 </note>

 <sect2 id="sec.y2.hw.print.local">
  <title>配置打印机</title>
  <para>
   系统通常可以自动检测到 USB 打印机。没有自动检测到打印机的原因可能有两个：
  </para>
  <itemizedlist mark="bullet" spacing="normal">
   <listitem>
    <para>
     USB 打印机关闭。
    </para>
   </listitem>
   <listitem>
    <para>
     打印机和计算机之间无法建立通讯。检查电缆和插头，确保打印机连接正确。如果属于这种情况，问题可能不在于打印机，而是与 USB 有关。
    </para>
   </listitem>
  </itemizedlist>
  <para>
   配置打印机分为三个步骤：指定连接类型、选择驱动程序，以及为此设置命名打印队列。
  </para>
  <para>
   许多打印机型号都有多个可用驱动程序。配置打印机时，YaST 默认使用标有<literal>推荐</literal>的那个，这是常规。通常不需要更改驱动程序。但是，举例来说，如果希望彩色打印机只打印黑白两种颜色，使用不支持彩色打印的驱动程序最为方便。如果用 PostScript 打印机打印图形时遇到性能问题，从 PostScript 驱动程序切换到 PCL 驱动程序（假定您的打印机可以识别 PCL）可能有帮助。
  </para>
  <para>
   如果没有列出您的打印机所适用的驱动程序，可以尝试从列表中选择带有相应标准语言的通用驱动程序。请参见您的打印机文档，以找出您的打印机可以识别的语言（控制打印机的命令集）。如果不起作用，请参见<xref linkend="sec.y2.hw.print.local.get_ppd"/>了解其他可能的解决方案。
  </para>
  <para>
   打印机从不直接使用，通常都是通过打印队列来使用的。这可确保将并发任务逐个排队和处理。每个打印队列都会被指派给一个特定驱动程序，而一台打印机可有多个队列。这样就能在一台彩色打印机上设置第二个队列，例如，该队列只打印黑色和白色。请参见<xref linkend="sec.p.workflow"/>以获取有关打印队列的更多信息。
  </para>
  <procedure id="pro.y2.hw.print.local.config">
   <title>添加新打印机</title>
   <step performance="required">
    <para>
     通过<menuchoice><guimenu>硬件</guimenu> <guimenu>打印机</guimenu></menuchoice>启动 YaST 打印机模块。
    </para>
   </step>
   <step performance="required">
    <para>
     在<guimenu>打印机配置</guimenu>屏幕中，单击<guimenu>添加</guimenu>。
    </para>
   </step>
   <step performance="required">
    <para>
     如果<literal>指定连接</literal>下已列出您的打印机，则继续下一步。否则，尝试<guimenu>检测更多</guimenu>或启动<guimenu>连接向导</guimenu>。
    </para>
   </step>
   <step performance="required">
    <para>
     在<literal>查找并指派驱动程序</literal>下的文本框中输入供应商名称和型号名称，然后单击<guimenu>搜索</guimenu>。
    </para>
   </step>
   <step performance="required">
    <para>
     选择与您的打印机匹配的驱动程序。建议您选择最先列出的驱动程序。如果未显示适合的驱动程序：
    </para>
    <substeps performance="required">
     <step performance="required">
      <para>
       检查搜索条件
      </para>
     </step>
     <step performance="required">
      <para>
       单击<guimenu>查找更多</guimenu>，借此扩大搜索范围
      </para>
     </step>
     <step performance="required">
      <para>
       按<xref linkend="sec.y2.hw.print.local.get_ppd"/>中所述添加驱动程序
      </para>
     </step>
    </substeps>
   </step>
   <step performance="required">
    <para>
     指定<literal>默认纸张大小</literal>.
    </para>
   </step>
   <step performance="required">
    <para>
     在<guimenu>设置任意名称</guimenu>字段中，为打印队列输入一个唯一的名称。
    </para>
   </step>
   <step performance="required">
    <para>
     此时，打印机配置了默认设置并准备好可供使用。单击<guimenu>确定</guimenu>以返回到<guimenu>打印机配置</guimenu>视图。现在可以在打印机列表中看到新配置的打印机。
    </para>
   </step>
  </procedure>
  <sect3 id="sec.y2.hw.print.local.get_ppd">
   <title>使用 YaST 添加驱动程序</title>
   <para>
    并非所有适用于 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 的打印机驱动程序都会默认安装在系统上。在添加新打印机时，如果<guimenu>查找并指派驱动程序</guimenu>对话框中没有合适的驱动程序可用，请安装含有适合您打印机的驱动程序的驱动程序包：
   </para>
   <procedure>
    <title>安装其他驱动程序包</title>
    <step performance="required">
     <para>
      通过<menuchoice><guimenu>硬件</guimenu> <guimenu>打印机</guimenu></menuchoice>启动 YaST 打印机模块。
     </para>
    </step>
    <step performance="required">
     <para>
      在<guimenu>打印机配置</guimenu>屏幕中，单击<guimenu>添加</guimenu>。
     </para>
    </step>
    <step performance="required">
     <para>
      在<literal>查找和指派驱动程序</literal>部分中，单击<guimenu>驱动程序包</guimenu>。
     </para>
    </step>
    <step performance="required">
     <para>
      从列表中选择一个或多个合适的驱动程序包。<emphasis>不要</emphasis>指定打印机描述文件的路径。
     </para>
    </step>
    <step performance="required">
     <para>
      选择<guimenu>确定</guimenu>并确认安装包。
     </para>
    </step>
    <step performance="required">
     <para>
      要直接使用这些驱动程序，请按照<xref linkend="pro.y2.hw.print.local.config"/>所述操作。
     </para>
    </step>
   </procedure>
   <para>
    PostScript 打印机不需要打印机驱动程序软件。PostScript 打印机只需要与特定型号匹配的 PostScript 打印机描述 (PPD) 文件。PPD 文件由打印机制造商提供。
   </para>
   <para>
    在添加 PostScript 打印机时，如果<guimenu>查找并指派驱动程序</guimenu>对话框中没有合适的 PPD 文件可用，请安装适合您打印机的 PPD 文件。
   </para>
   <para>
    可从多个来源取得 PPD 文件。建议先尝试 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 随附但默认未安装的其他驱动程序包（有关安装说明，请参见下文）。如果这些包不包含适合您的打印机的驱动程序，请直接从打印机供应商处或从 PostScript 打印机的驱动程序 CD 中取得 PPD 文件。有关详细信息，请参见<xref linkend="sec.drucken.prob.ppd"/>。或者，也可在 <ulink url="http://www.linuxfoundation.org/collaborate/workgroups/openprinting/database/databaseintro"/>（<quote>OpenPrinting.org 打印机数据库</quote>）中查找 PPD 文件。从 OpenPrinting 中下载 PPD 文件时，请记住它始终显示最新的 Linux 支持状态，未必适合 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>。
   </para>
   <procedure>
    <title>添加 PostScript 打印机的 PPD 文件</title>
    <step performance="required">
     <para>
      通过<menuchoice><guimenu>硬件</guimenu> <guimenu>打印机</guimenu></menuchoice>启动 YaST 打印机模块。
     </para>
    </step>
    <step performance="required">
     <para>
      在<guimenu>打印机配置</guimenu>屏幕中，单击<guimenu>添加</guimenu>。
     </para>
    </step>
    <step performance="required">
     <para>
      在<literal>查找和指派驱动程序</literal>部分中，单击<guimenu>驱动程序包</guimenu>。
     </para>
    </step>
    <step performance="required">
     <para>
      在<literal>使打印机描述文件可用</literal>下的文本框中输入 PPD 文件的完整路径。
     </para>
    </step>
    <step performance="required">
     <para>
      单击<guimenu>确定</guimenu>返回<literal>添加新的打印机配置</literal>屏幕。
     </para>
    </step>
    <step performance="required">
     <para>
      要想直接使用该 PPD 文件，请按照<xref linkend="pro.y2.hw.print.local.config"/>中所述操作。
     </para>
    </step>
   </procedure>
  </sect3>
  <sect3 id="sec.y2.hw.print.local.tune">
   <title>编辑本地打印机配置</title>
   <para>
    您可以编辑打印机的现有配置来更改连接类型和驱动程序等基本设置。还可以调整纸张大小、分辨率、媒体来源等的默认设置。通过修改打印机描述或位置，可以更改打印机的标识符。
   </para>
   <procedure>
    <step performance="required">
     <para>
      通过<menuchoice><guimenu>硬件</guimenu> <guimenu>打印机</guimenu></menuchoice>启动 YaST 打印机模块。
     </para>
    </step>
    <step performance="required">
     <para>
      在<guimenu>打印机配置</guimenu>屏幕的列表中选择本地打印机配置，然后单击<guimenu>编辑</guimenu>。
     </para>
    </step>
    <step performance="required">
     <para>
      按<xref linkend="pro.y2.hw.print.local.config"/>中所述更改连接类型或驱动程序。只有当前配置存在问题时，才需要执行该操作。
     </para>
    </step>
    <step performance="required">
     <para>
      （选择性）选中<guimenu>默认打印机</guimenu>将该打印机设为默认打印机。
     </para>
    </step>
    <step performance="required">
     <para>
      通过单击<guimenu>当前驱动程序的所有选项</guimenu>调整默认设置。要更改设置，可以通过单击相对的 <literal>+</literal> 符号展开选项列表。通过单击选项更改默认值。单击<guimenu>确定</guimenu>应用您的更改。
     </para>
    </step>
   </procedure>
  </sect3>
 </sect2>

 <sect2 id="sec.y2.hw.print.network">
  <title>使用 YaST 配置网络打印</title>
  <para>
   无法自动检测到网络打印机。必须使用 YaST 打印机模块手动进行配置。视您的网络设置而定，可以打印到打印服务器（CUPS、LPD、SMB 或 IPX）或直接打印到网络计算机（首选通过 TCP）。通过从 YaST 打印机模块的左窗格中选择<guimenu>通过网络打印</guimenu>来访问网络打印配置视图。
  </para>
  <sect3 id="sec.y2.hw.print.network.cups">
   <title>使用 CUPS</title>
   <para>
    在 Linux 环境中，CUPS 通常用于通过网络打印。最简单的设置是仅通过所有客户端都可直接访问的一台 CUPS 服务器来打印。通过多个 CUPS 服务器进行打印时，需要运行与远程 CUPS 服务器通讯的本地 CUPS 守护程序。
   </para>
   <important>
    <title>浏览网络打印机队列</title>
    <para>
     CUPS 服务器通过传统的 CUPS 浏览协议或 Bonjour/DND-SD 在网络上公告它们的打印队列。客户端需要能浏览这些列表，以便用户可以选择要向其发送打印作业的特定打印机。为了能够浏览网络打印队列，在通过 CUPS 服务器打印的所有客户端上都需要运行 <systemitem class="resource">cups-filters-cups-browsed</systemitem> 包提供的 <systemitem class="daemon">cups-browsed</systemitem> 服务。使用 YaST 配置网络打印时，<systemitem class="daemon">cups-browsed</systemitem> 会自动启动。
    </para>
    <para>
     如果在启动 <systemitem class="daemon">cups-browsed</systemitem> 之后浏览不起作用，则表明 CUPS 服务器可能是通过 Bonjour/DND-SD 公告网络打印机队列的。在此情况下，您需要另外安装 <systemitem class="resource">avahi</systemitem> 包，并在所有客户端上使用 <command>sudo systemctl start avahi-daemon.service</command> 启动关联的服务。
    </para>
   </important>
   <procedure>
    <title>通过单个 CUPS 服务器进行打印</title>
    <step performance="required">
     <para>
      通过<menuchoice><guimenu>硬件</guimenu> <guimenu>打印机</guimenu></menuchoice>启动 YaST 打印机模块。
     </para>
    </step>
    <step performance="required">
     <para>
      在左窗格中，启动<guimenu>通过网络打印</guimenu>屏幕。
     </para>
    </step>
    <step performance="required">
     <para>
      选中<guimenu>直接通过一台远程 CUPS 服务器来执行您的所有打印</guimenu>并指定该服务器的名称或 IP 地址。
     </para>
    </step>
    <step performance="required">
     <para>
      单击<guimenu>测试服务器</guimenu>以确保您已选择正确的名称或 IP 地址。
     </para>
    </step>
    <step performance="required">
     <para>
      单击“确定”返回到<guimenu>打印机配置</guimenu>屏幕。现在会列出通过 CUPS 服务器提供的所有打印机。
     </para>
    </step>
   </procedure>
   <procedure>
    <title>通过多个 CUPS 服务器进行打印</title>
    <step performance="required">
     <para>
      通过<menuchoice><guimenu>硬件</guimenu> <guimenu>打印机</guimenu></menuchoice>启动 YaST 打印机模块。
     </para>
    </step>
    <step performance="required">
     <para>
      在左窗格中，启动<guimenu>通过网络打印</guimenu>屏幕。
     </para>
    </step>
    <step performance="required">
     <para>
      选中<guimenu>接受来自 CUPS 服务器的打印机公告</guimenu>。
     </para>
    </step>
    <step performance="required">
     <para>
      在<literal>常规设置</literal>下，指定要使用的服务器。您可以接受来自所有可用网络或来自特定主机的连接。如果选择后一个选项，则需要指定主机名或 IP 地址。
     </para>
    </step>
    <step performance="required">
     <para>
      在询问是否启动本地 CUPS 服务器时，通过单击<guimenu>确定</guimenu>和<guimenu>是</guimenu>进行确认。服务器启动之后，YaST 会返回<guimenu>打印机配置</guimenu>屏幕。单击<guimenu>刷新列表</guimenu>查看目前为止检测到的打印机。如果以后有更多的打印机可用，请再次单击此按钮。
     </para>
    </step>
   </procedure>
  </sect3>
  <sect3 id="sec.y2.hw.print.network.other">
   <title>使用打印服务器而非 CUPS</title>
   <para>
    如果网络通过 CUPS 之外的其他打印服务器提供打印服务，可以通过<menuchoice><guimenu>硬件</guimenu> <guimenu>打印机</guimenu></menuchoice>启动 YaST 打印机模块，并从左窗格中启动<guimenu>通过网络打印</guimenu>屏幕。启动<guimenu>连接向导</guimenu>并选择相应的<guimenu>连接类型</guimenu>。关于在您的环境中配置网络打印机的细节，请咨询您的网络管理员。
   </para>
  </sect3>
 </sect2>

 <sect2 id="sec.y2.hw.print.share">
  <title>通过网络共享打印机</title>
  <para>
   本地 CUPS 守护程序所管理的打印机可通过网络共享，这样就能将您的计算机转变为 CUPS 服务器。通常，可通过启用 CUPS 所谓的<quote>浏览模式</quote>来共享一台打印机。如果浏览已启用，就可将本地打印队列用于在网络上侦听远程 CUPS 守护程序。也可设置专用 CUPS 服务器，用于管理所有打印队列且通过远程客户端直接访问。这种情况下，无需启用浏览。
  </para>
  <procedure>
   <title>共享打印机 </title>
   <step performance="required">
    <para>
     通过<menuchoice><guimenu>硬件</guimenu> <guimenu>打印机</guimenu></menuchoice>启动 YaST 打印机模块。
    </para>
   </step>
   <step performance="required">
    <para>
     从左窗格中启动<guimenu>共享打印机</guimenu>屏幕。
    </para>
   </step>
   <step performance="required">
    <para>
     选择<guimenu>允许远程访问</guimenu>。另外请选中<guimenu>对于本地网络中的计算机</guimenu>，并通过选中<guimenu>默认情况下，在本地网络中发布打印机</guimenu>启用浏览模式。
    </para>
   </step>
   <step performance="required">
    <para>
     单击<guimenu>确定</guimenu>重启动 CUPS 服务器，并返回到<guimenu>打印机配置</guimenu>屏幕。
    </para>
   </step>
   <step performance="required">
    <para>
     有关 CUPS 和防火墙设置，请参见 <ulink url="http://en.opensuse.org/SDB:CUPS_and_SANE_Firewall_settings"/>。
    </para>
   </step>
  </procedure>
 </sect2>
</sect1>
