<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter
[
  <!ENTITY % entities SYSTEM "entity-decl.ent">
    %entities;
]>
<chapter version="5.0" xml:id="cha.install"
  xmlns="http://docbook.org/ns/docbook"
  xmlns:xi="http://www.w3.org/2001/XInclude"
  xmlns:xlink="http://www.w3.org/1999/xlink">
 <title>Installation Steps</title>
 <info>
  <abstract>
   <para>
    This chapter describes the procedure in which the data for &productname;
    is copied to the target device. Some basic configuration parameters for
    the newly installed system are set during the procedure. A graphical
    user interface will guide you through the installation. The command line
    installation contains the same steps and only looks different.
   </para>
  </abstract>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker></dm:bugtracker>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>

 <para os="sles">
  Before running the installer, read <xref linkend="part.prep"/>. The procedure
  described here is also valid for remote installation procedures described
  in <xref linkend="cha.remote_installation"/> after connecting via SSH or
  VNC. The text mode installation has the same steps and only looks different.
  For information about performing non-interactive automated installations, see
  <xref linkend="book.autoyast"/>.
 </para>

 <para os="sled;osuse">
  If you are a first-time user of
  &productname;, you might want to follow the default &yast; proposals in
  most parts, but you can also adjust the settings as described here to
  fine-tune your system according to your preferences. Help for each
  installation step is provided by clicking <guimenu>Help</guimenu>.
 </para>

 <tip>
  <title>Installation Without a Mouse</title>
  <para>
   If the installer does not detect your mouse correctly, use
   <keycap function="tab"/> for navigation, arrow keys to scroll, and
   <keycap function="enter"/> to confirm a selection. Various buttons or
   selection fields contain a letter with an underscore. Use
   <keycombo><keycap function="alt"/><keycap>Letter</keycap></keycombo> to
   select a button or a selection directly instead of navigating there with
   <keycap function="tab"/>.
  </para>
 </tip>

 <sect1 xml:id="sec.yast_install.overview">
  <title>Overview</title>
  <para>
   This section provides an overview of all installation steps. Each step
   contains a link to a detailed description.
  </para>
  <procedure>
   <step>
    <para>
     Before the installation starts, the installer can update itself. For details,
     see <xref linkend="sec.yast_install.self_update" />.
    </para>
   </step>
   <step>
    <para>
     <phrase os="sles;sled">The actual installation starts with choosing the language and the
     product.</phrase><phrase os="osuse">The actual installation starts with choosing the language and
     accepting the license agreement.</phrase> For details, see <xref linkend="sec.yast_install.welcome"
     />.
    </para>
   </step>
   <step os="sles">
    <para>
     Accept the license agreement. For details,
     see <xref linkend="sec.yast_install.license_agreement" />.
    </para>
   </step>
   <step arch="zseries" os="sles">
    <para>
     &zseries; machines need to activate disks. For details, see
     <xref linkend="sec.yast_install.s390.part"/>.
    </para>
   </step>
   <step>
    <para>
     Configure the network. This is required when installing packages from
     network sources. For details, see <xref linkend="sec.yast_install.network"/>.
    </para>
   </step>
   <step os="sles;sled">
    <para>
     With a working network connection you can register the machine at the
     &scc; or an &rmt; server. For details, see <xref linkend="sec.yast_install.scc_registration"/>.
    </para>
   </step>
   <step os="sles;sled">
    <para>
     Select the modules you want to enable for the machine. This impacts the
     availability of system roles in the next step and packages later on.
     For details, see <xref linkend="sec.yast_install.modules"/>.
    </para>
   </step>
   <step os="sles;sled">
    <para>
     You can manually add additional repositories.
     For details, see <xref linkend="sec.yast_install.addons"/>.
    </para>
   </step>
   <step>
    <para>
     Select a role for your system. This sets a default list of packages
     to be installed. For details, see <xref linkend="sec.yast_install.conf.manual.role"/>.
    </para>
   </step>
   <step>
    <para>
     Partition the hard disks of your system. For details, see
     <xref linkend="sec.yast_install.partitioning"/>.
    </para>
   </step>
   <step>
    <para>
     Choose a time zone. For details, see
     <xref linkend="sec.yast_install.timezone"/>.
    </para>
   </step>
   <step>
    <para>
     Create the first user. For details, see
     <xref linkend="sec.yast_install.user"/>.
    </para>
   </step>
   <step>
    <para>
     Optionally, set a different password for the &rootuser;. For
     details, see <xref linkend="sec.yast_install.user.root"/>.
    </para>
   </step>
   <step>
    <para>
     In a final step, the installer presents on overview of all settings.
     If required, you can change them. For details, see <xref linkend="sec.yast_install.proposal"/>.
    </para>
   </step>
   <step>
    <para>
     The installer copies all required data and informs you about the progress.
     For details, see <xref linkend="sec.yast_install.perform" />.
    </para>
   </step>
  </procedure>
 </sect1>

 <sect1 xml:id="sec.yast_install.self_update">
  <title>Installer Self-Update</title>

  <para>
   During the installation and upgrade process, &yast; can update itself
   to solve bugs in the installer that were discovered after the release.  This
   functionality is <phrase os="osuse">disabled</phrase><phrase
    os="sled;sles">enabled</phrase> by default; to <phrase
    os="osuse">enable</phrase><phrase os="sled;sles">disable</phrase> it, set
   the boot parameter <option>self_update</option> to <phrase
    os="osuse"><literal>1</literal></phrase><phrase
    os="sled;sles"><literal>0</literal></phrase>.  For more information, see
   <xref linkend="sec.boot_parameters.advanced.self_update"/>.
  </para>

  <para>
   Although this feature was designed to run without user intervention, it is
   worth knowing how it works. If you are not interested, you can jump directly
   to <xref linkend="sec.yast_install.welcome"/> and skip the rest of this
   section.
  </para>

  <tip>
   <title>Language Selection</title>
   <para>
    The installer self-update is executed before the language selection step.
    This means that progress and errors which happen during this process are
    displayed in English by default.
   </para>
   <para>
    To use another language for this part of the installer, press
    <keycap>F2</keycap> in the DVD boot menu and select the language from the
    list.  Alternatively, use the <literal>language</literal> boot parameter
    (for example, <literal>language=de_DE</literal>).
   </para>
  </tip>

  <sect2 xml:id="sec.yast_install.self_update.process">
   <title>Self-Update Process</title>

   <para>
    The process can be broken down into two different parts:
   </para>

   <orderedlist spacing="normal">
    <listitem>
     <para>
      Determine the update repository location.
     </para>
    </listitem>
    <listitem>
     <para>
      Download and apply the updates to the installation system.
     </para>
    </listitem>
   </orderedlist>

   <sect3 xml:id="sec.yast_install.self_update.determine_repo">
    <title>Determining the Update Repository Location</title>

    <para>
     Installer Self-Updates are distributed as regular RPM packages via a
     dedicated repository, so the first step is to find out the repository URL.
    </para>
    <important>
     <title>Installer Self-Update Repository Only</title>
     <para>
      No matter which of the following options you use, only the installer
      self-update repository URL is expected, for example:
     </para>
<screen>self_update=https://www.example.com/my_installer_updates/</screen>
     <para>
      Do not supply any other repository URL&mdash;for example the URL of the
      software update repository.
     </para>
    </important>
    <para>
     &yast; will try the following sources of information:
    </para>

    <orderedlist spacing="normal">
     <listitem>
      <para>
       The <literal>self_update</literal> boot parameter. (For more details,
       see <xref linkend="sec.boot_parameters.advanced.self_update"/>.) If you
       specify a URL, it will take precedence over any other method.
      </para>
     </listitem>
     <listitem>
      <para>
       The <literal>/general/self_update_url</literal> profile element in case
       you are using &ay;.
      </para>
     </listitem>
     <listitem os="sles;sled">
      <para>
       A registration server. &yast; will query the registration server for the
       URL. The server to be used is determined in the following order:
      </para>
      <orderedlist spacing="normal">
       <listitem>
        <para>
         By evaluating the <literal>regurl</literal> boot
         parameter (<xref linkend="sec.boot_parameters.advanced.smt"/>).
        </para>
       </listitem>
       <listitem>
        <para>
         By evaluating the <literal>/suse_register/reg_server</literal> profile
         element if you are using &ay;.
        </para>
       </listitem>
       <listitem>
        <para>
         By performing an SLP lookup. If an SLP server is found, &yast; will
         ask you whether it should be used because there is no authentication
         involved and everybody on the local network could announce a
         registration server.
        </para>
       </listitem>
       <listitem>
        <para>
         By querying the &scc;.
        </para>
       </listitem>
      </orderedlist>
     </listitem>
     <listitem>
      <para>
       If none of the previous attempts worked, the fallback URL (defined in
       the installation media) will be used.
      </para>
     </listitem>
    </orderedlist>
   </sect3>

   <sect3 xml:id="sec.yast_install.self_update.apply_updates">
    <title>Downloading and Applying the Updates</title>

    <para>
     When the updates repository is determined, &yast; will check whether an
     update is available. If so, all the updates will be downloaded and
     applied to the installation system.
    </para>

    <para>
     Finally, &yast; will be restarted to load the new version and the welcome
     screen will be shown. If no updates were available, the installation will
     continue without restarting &yast;.
    </para>

    <note>
     <title>Update Integrity</title>
     <para>
      Update signatures will be checked to ensure integrity and
      authorship. If a signature is missing or invalid, you will be asked
      whether you want to apply the update.
     </para>
    </note>
   </sect3>
  </sect2>

  <sect2 xml:id="sec.yast_install.self_update.networking">
   <title>Networking during Self-Update</title>

   <para>
    To download installer updates, &yast; needs network access. By default,
    it tries to use DHCP on all network interfaces. If there is a DHCP server
    in the network, it will work automatically.
   </para>

   <para>
    If you need a static IP setup, you can use the <literal>ifcfg</literal>
    boot argument. For more details, see the linuxrc documentation at
    <link xlink:href="https://en.opensuse.org/Linuxrc"/>.
   </para>
  </sect2>

  <sect2 xml:id="sec.yast_install.self_update.custom">
   <title>Custom Self-Update Repositories</title>

   <para>
    &yast; can use a user-defined repository instead of the official one by
    specifying a URL through the <literal>self_update</literal> boot parameter.
    However, the following points should be considered:
   </para>

   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      Only HTTP/HTTPS and FTP repositories are supported.
     </para>
    </listitem>
    <listitem>
     <para>
      Only RPM-MD repositories are supported (required by &rmt;).
     </para>
    </listitem>
    <listitem>
     <para>
      Packages are not installed in the usual way: They are uncompressed only
      and no scripts are executed.
     </para>
    </listitem>
    <listitem>
     <para>
      No dependency checks are performed. Packages are installed in
      alphabetical order.
     </para>
    </listitem>
    <listitem>
     <para>
      Files from the packages override the files from the original
      installation media. This means that the update packages might not need to
      contain all files, only files that have changed. Unchanged files are
      omitted to save memory and download bandwidth.
     </para>
    </listitem>
   </itemizedlist>

   <note>
    <title>Only One Repository</title>
    <para>
     Currently, it is not possible to use more than one repository as source
     for installer self-updates.
    </para>
   </note>
  </sect2>
 </sect1>

 <sect1 xml:id="sec.yast_install.welcome">
  <title os="sles;sled">Language, Keyboard, and Product Selection</title>
  <title os="osuse">Language, Keyboard, and License Agreement</title>
  <informalfigure>
   <mediaobject>
    <imageobject role="fo">
     <imagedata os="sled" fileref="install_product_sled.png" width="100%"/>
     <imagedata os="sles" fileref="install_product_sles.png" width="100%"/>
     <imagedata os="osuse" fileref="install_welcome_osuse.png" width="100%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata os="sled" fileref="install_product_sled.png" width="100%"/>
     <imagedata os="sles" fileref="install_product_sles.png" width="100%"/>
     <imagedata os="osuse" fileref="install_welcome_osuse.png" width="100%"/>
    </imageobject>
   </mediaobject>
  </informalfigure>
  <para>
   The <guimenu>Language</guimenu> and <guimenu>Keyboard Layout</guimenu>
   settings are initialized with English (US) as default. Change them here,
   if necessary. Use the <guimenu>Keyboard Test</guimenu> text box to test
   the layout.
  </para>
  <para os="sles;sled">
   With the &leanos; you can install all &sle; base products:
  </para>
  <itemizedlist os="sles;sled">
   <listitem>
    <para>
     &sls; (<phrase os="sles">covered here</phrase><phrase os="sled">refer
     to <link xlink:href="https://www.suse.com/documentation/sles/"/> for
     installation instructions</phrase>)
    </para>
   </listitem>
   <listitem>
    <para>
     &sled; (<phrase os="sled">covered here</phrase><phrase os="sles">refer
     to <link xlink:href="https://www.suse.com/documentation/sled/"/> for
     installation instructions</phrase>)
    </para>
   </listitem>
   <listitem>
    <para>
     &suse; Linux Enterprise High Performance Computing (refer to FIXME for
     installation instructions)
    </para>
   </listitem>
   <listitem>
    <para>
     &sles4sap; (refer to <link
     xlink:href="https://www.suse.com/documentation/sles-for-sap/"/> for
     installation instructions)
    </para>
   </listitem>
  </itemizedlist>
  <para os="sles;sled">
   Select a product for installation. You need to have a registration code
   for the respective product. In the course of this document it is assumed
   you have chosen <phrase os="sles">&sls;</phrase><phrase
   os="sled">&sled;</phrase>. Proceed with <guimenu>Next</guimenu>.
  </para>
  <para os="osuse">
   Read the License Agreement. It is presented in the language you have
   chosen on the boot screen. Translations are available via the
   <guimenu>License Language</guimenu> drop-down box . You need to accept
   the agreement by checking <guimenu>I Agree to the License Terms</guimenu>
   to install &productname;. Proceed with <guimenu>Next</guimenu>.
  </para>
 </sect1>

 <sect1 xml:id="sec.yast_install.license_agreement" os="sles;sled">
  <title>License Agreement</title>
  <informalfigure os="sles;sled">
   <mediaobject>
    <imageobject role="fo">
     <imagedata os="sled" fileref="install_license_sled.png" width="100%"/>
     <imagedata os="sles" fileref="install_license_sles.png" width="100%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata os="sled" fileref="install_license_sled.png" width="100%"/>
     <imagedata os="sles" fileref="install_license_sles.png" width="100%"/>
    </imageobject>
   </mediaobject>
  </informalfigure>
  <para>
   Read the License Agreement. It is presented in the language you have
   chosen on the boot screen. Translations are available via the
   <guimenu>License Language</guimenu> drop-down box . You need to accept
   the agreement by checking <guimenu>I Agree to the License Terms</guimenu>
   to install &productname;. Proceed with <guimenu>Next</guimenu>.
  </para>
 </sect1>

 <sect1 xml:id="sec.yast_install.s390.part" arch="zseries" os="sles">
  <title>&zseries;: Disk Activation</title>
  <figure>
   <title>Disk Activation</title>
   <mediaobject>
    <textobject role="description"><phrase>Disk Activation</phrase></textobject>
    <imageobject role="fo">
     <imagedata fileref="install_zseries_disk_activation.png" width="75%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="install_zseries_disk_activation.png" width="75%"/>
    </imageobject>
   </mediaobject>
  </figure>
  <para>
   When installing on &zseries; platforms, the language selection dialog is
   followed by a dialog to configure the attached hard disks. Select DASD,
   Fibre Channel Attached SCSI Disks (zFCP), or iSCSI for installation of
   &productname;. The DASD and zFCP configuration buttons are only available if
   the corresponding devices are attached. For instructions on how to configure
   iSCSI disks, refer to <xref linkend="sec.iscsi.initiator"/>.
  </para>
  <para>
   You can also <guimenu>Change the Network Configuration</guimenu> in this
   screen by launching the <guimenu>Network Settings</guimenu> dialog. Choose a
   network interface from the list and click <guimenu>Edit</guimenu> to change
   its settings. Use the tabs to configure DNS and routing. See
   <xref linkend="sec.network.yast"/> for more details.
  </para>
  <remark condition="clarity">
   2014-06-05 - fs: Screenshot needs to be replaced by a version showing all 4
   options.
  </remark>

  <sect2 xml:id="sec.yast_install.s390.part.dasd">
   <title>Configuring DASD Disks</title>
   <figure xml:id="fig.s390_yast2.i_y2_sel_dasd">
    <title>&zseries;: Selecting a DASD</title>
    <mediaobject>
     <textobject role="description"><phrase>&zseries;: Selecting a DASD</phrase></textobject>
     <imageobject role="fo">
      <imagedata width="70%" fileref="s390_i_y2_sel_dasd.png"/>
     </imageobject>
     <imageobject role="html">
      <imagedata width="70%" fileref="s390_i_y2_sel_dasd.png"/>
     </imageobject>
    </mediaobject>
   </figure>
   <para>
    After selecting <guimenu>Configure DASD Disks</guimenu>, an overview lists
    all available DASDs. To get a clearer picture of the available devices, use
    the text box located above the list to specify a range of channels to
    display. To filter the list according to such a range, select
    <guimenu>Filter</guimenu>.
   </para>
   <para>
    Specify the DASDs to use for the installation by selecting the
    corresponding entries in the list. Use <guimenu>Select All</guimenu> to
    select all DASDs currently displayed. Activate and make the selected DASDs
    available for the installation by selecting <menuchoice> <guimenu>Perform
    Action</guimenu> <guimenu>Activate</guimenu> </menuchoice>. To format the
    DASDs, select <menuchoice> <guimenu>Perform Action</guimenu>
    <guimenu>Format</guimenu> </menuchoice>. Alternatively, use the &yast;
    partitioner later as described in
    <xref linkend="sec.expert_partitioner"/>.
   </para>
  </sect2>
  <sect2 xml:id="sec.yast_install.s390.part.zfcp">
   <title>Configuring zFCP Disks</title>
   <para>
    To use zFCP disks for the &productname; installation, select
    <guimenu>Configure zFCP Disks</guimenu> in the selection dialog. This opens
    a dialog with a list of the zFCP disks available on the system. In this
    dialog, select <guimenu>Add</guimenu> to open another dialog in which to
    enter zFCP parameters.
   </para>
   <para>
    To make a zFCP disk available for the &productname; installation, choose an
    available <guimenu>Channel Number</guimenu> from the drop-down box.
    <guimenu>Get WWPNs</guimenu> (World Wide Port Number) and <guimenu>Get
    LUNs</guimenu> (Logical Unit Number) return lists with available WWPNs and
    FCP-LUNs, respectively, to choose from. Automatic LUN scanning only works
    with NPIV enabled.
   </para>
   <para>
    When completed, exit the zFCP
    dialog with <guimenu>Next</guimenu> and the general hard disk configuration
    dialog with <guimenu>Finish</guimenu> to continue with the rest of the
    configuration.
   </para>
  </sect2>
 </sect1>

 <sect1 xml:id="sec.yast_install.network">
  <title>Network Settings</title>
  <figure>
   <title>Network Settings</title>
   <mediaobject>
    <textobject role="description"><phrase>Network Settings</phrase></textobject>
    <imageobject role="fo">
     <imagedata os="sles;sled" fileref="install_network.png" width="75%"/>
     <imagedata os="osuse" fileref="install_network_osuse.png" width="75%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata os="sles;sled" fileref="install_network.png" width="75%"/>
     <imagedata os="osuse" fileref="install_network_osuse.png" width="75%"/>
    </imageobject>
   </mediaobject>
  </figure>
  <para>
   After booting into the installation, the installation routine is set up.
   During this setup, an attempt to configure at least one network interface
   with DHCP is made. In case this attempt fails, the <guimenu>Network
   Settings</guimenu> dialog launches. Choose a network interface from the list
   and click <guimenu>Edit</guimenu> to change its settings. Use the tabs to
   configure DNS and routing. See <xref linkend="sec.network.yast"/> for more
   details. <phrase os="sles" arch="zseries">On &zseries; this dialog does
   not start automatically. It can be started in the <guimenu>Disk
   Activation</guimenu> step.</phrase>
  </para>
  <para os="sles;sled">
   In case DHCP was successfully configured during installation setup, you can
   also access this dialog by clicking <guimenu>Network Configuration</guimenu>
   at the <guimenu>&scc; Registration</guimenu> step. It lets you change the
   automatically provided settings.
  </para>
  <note>
   <title>Network Interface Configured via linuxrc</title>
   <para>
    If at least one network interface is configured via linuxrc, automatic
    DHCP configuration is disabled and configuration from linuxrc
    is imported and used.
   </para>
  </note>
  <tip>
   <title>Accessing Network Storage or Local RAID</title>
   <para>
    To access a SAN or a local RAID during the installation,
    you can use the libstorage command line client for this purpose:
   </para>
   <orderedlist>
    <listitem>
     <para>
      Switch to a console with <keycombo><keycap function="control"/>
      <keycap
      function="alt"/> <keycap>F2</keycap></keycombo>.
     </para>
    </listitem>
    <listitem>
     <para>
      Install the libstoragemgmt extension by running <command>extend
      libstoragemgmt</command>.
     </para>
    </listitem>
    <listitem>
     <para>
      Now you have access to the <command>lsmcli</command> command. For more
      information, run <command>lsmcli --help</command>.
     </para>
    </listitem>
    <listitem>
     <para>
      To return to the installer, press <keycombo><keycap function="alt"/>
      <keycap>F7</keycap></keycombo>
     </para>
    </listitem>
   </orderedlist>
   <para>
    Supported are Netapp Ontap, all SMI-S compatible SAN providers, and LSI
    MegaRAID.
   </para>
  </tip>
 </sect1>

 <sect1 xml:id="sec.yast_install.scc_registration" os="sles;sled">
  <title>&scc; Registration</title>
  <para>
   To get technical support and product updates, you need to register and
   activate your product with the &scc;. Registering &productname; grants
   you access to the update repository. This enables you to install
   the system with the latest updates and patches available. If you are offline
   or want to skip this step, select <guimenu>Skip Registration</guimenu>. You
   can register your system at any time later from the installed system.
  </para>
  <sect2 xml:id="sec.yast_install.scc_registration.manually">
   <title>Registering Manually</title>
   <figure>
    <title>&scc; Registration</title>
    <mediaobject>
     <textobject role="description"><phrase>&scc; Registration</phrase></textobject>
     <imageobject role="fo">
      <imagedata os="sles" fileref="install_scc_sles.png" width="75%"/>
      <imagedata os="sled" fileref="install_scc_sled.png" width="75%"/>
     </imageobject>
     <imageobject role="html">
      <imagedata os="sles" fileref="install_scc_sles.png" width="75%"/>
      <imagedata os="sled" fileref="install_scc_sled.png" width="75%"/>
     </imageobject>
    </mediaobject>
   </figure>
   <para>
    To register your system, provide the <guimenu>E-mail</guimenu> address
    associated with the &suse; account you or your organization uses to manage
    subscriptions. In case you do not have a SUSE account yet, go to the &scc;
    home page (<link xlink:href="&sccurl;"/>) to create one.
   </para>
   <para>
    Enter the <guimenu>Registration Code</guimenu> you received with your copy
    of &productname;. &yast; can also read registration codes from a USB storage
    device such as a flash disk. For details, see
    <xref linkend="sec.yast_install.scc_registration.usb"/>.
   </para>
   <para>
    Proceed with <guimenu>Next</guimenu> to start the registration process. If
    one or more local registration servers are available on your network, you
    can choose one of them from a list. By default, &productname; is
    registered at the &scc;. If your local registration server was not
    discovered automatically, choose <guimenu>Cancel</guimenu>, select
    <guimenu>Register System via local SMT Server</guimenu> and enter the URL of
    the server. Restart the registration by choosing <guimenu>Next</guimenu>
    again.
   </para>
   <para>
    During the registration, the online update repositories will be added to
    your installation setup. When finished, you can choose whether to install
    the latest available package versions from the update repositories. This
    ensures that &productname; is installed with the latest security updates
    available. If you choose <guimenu>No</guimenu>, all packages will be
    installed from the installation media. Proceed with <guimenu>Next</guimenu>.
   </para>
   <para>
    If the system was successfully registered during installation, &yast; will
    disable repositories from local installation media such as CD/DVD or flash
    disks when the installation has been completed. This prevents problems if
    the installation source is no longer available and ensures that you always
    get the latest updates from the online repositories.
   </para>
   <tip>
    <title>Release Notes</title>
    <para>
     From this point on, the Release Notes can be viewed from any screen during
     the installation process by selecting <guimenu>Release Notes</guimenu>.
    </para>
   </tip>
  </sect2>
  <sect2 xml:id="sec.yast_install.scc_registration.usb">
   <title>Loading Registration Codes from USB Storage</title>
   <para>
    To make the registration more convenient, you can also store your
    registration codes on a USB storage device such as a flash disk. &yast;
    will automatically pre-fill the corresponding text box. This is
    particularly useful when testing the installation or if you need to
    register many systems or extensions.
   </para>
   <note>
    <title>Limitations</title>
    <para>
     Currently flash disks are only scanned during installation or upgrade, but
     not when registering a running system.
    </para>
   </note>
   <para>
    Create a file named <filename>regcodes.txt</filename> or
    <filename>regcodes.xml</filename> on the USB disk. If both are present, the
    XML takes precedence.
   </para>
   <para>
    In that file, identify the product with the name returned by
    <command>zypper search --type product</command> and assign it a
    registration code as follows:
   </para>
   <example xml:id="ex.i.yast2.conf.manual.cc.usb.txt">
    <title><filename>regcodes.txt</filename></title>
<screen>SLES    cc36aae1
SLED    309105d4

sle-we  5eedd26a
sle-live-patching 8c541494</screen>
   </example>
   <example xml:id="ex.i.yast2.conf.manual.cc.usb.xml">
    <title><filename>regcodes.xml</filename></title>
<screen language="xml"><![CDATA[
<?xml version="1.0"?>
<profile xmlns="http://www.suse.com/1.0/yast2ns"
 xmlns:config="http://www.suse.com/1.0/configns">
  <suse_register>
    <addons config:type="list">
      <addon>
<name>SLES</name>
<reg_code>cc36aae1</reg_code>
      </addon>
      <addon>
<name>SLED</name>
<reg_code>309105d4</reg_code>
      </addon>
      <addon>
<name>sle-we</name>
<reg_code>5eedd26a</reg_code>
      </addon>
      <addon>
<name>sle-live-patching</name>
<reg_code>8c541494</reg_code>
      </addon>
    </addons>
  </suse_register>
</profile>]]></screen>
   </example>
   <para>
    Note that <literal>&slsa;</literal> and <literal>&sleda;</literal> are not
    extensions, but listing them as add-ons allows for combining several base
    product registration codes in a single file. <phrase os="sles">See
    <xref
    linkend="CreateProfile.Register.Extension"/> for
    details.</phrase>
   </para>
  </sect2>
 </sect1>

 <sect1 xml:id="sec.yast_install.modules" os="sles;sled">
  <title>Extension and Module Selection</title>
  <figure>
   <title>Extension and Module Selection</title>
   <mediaobject>
    <textobject role="description"><phrase>Extension Selection</phrase></textobject>
    <imageobject role="fo">
     <imagedata os="sles" fileref="install_extensions_sles.png" width="75%"/>
     <imagedata os="sled" fileref="install_extensions_sled.png" width="75%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata os="sles" fileref="install_extensions_sles.png" width="75%"/>
     <imagedata os="sled" fileref="install_extensions_sled.png" width="75%"/>
    </imageobject>
   </mediaobject>
  </figure>
  <para>
   If you have successfully registered your system in the previous step, a list
   of available modules and extensions based on &productname; is shown.
   Otherwise this configuration step is skipped. It is also possible to add
   modules and extensions from the installed system, see
   <xref linkend="cha.add-ons"/> for details.
  </para>
  <para>
   The list contains modules for &productname; and
   extensions requiring a registration key that is liable for costs. Click an
   entry to see its description. Select a module or extension for installation
   by activating its check mark. This will add its repository from the &scc;
   server to your installation&mdash;no additional installation sources are
   required. Furthermore the installation pattern for the module or extension
   is added to the default installation to ensure it gets installed
   automatically.
  </para>
  <para>
   The amount of available extensions and modules depends on the registration
   server. A local registration server may only offer update repositories and
   no additional extensions.
  </para>
  <para>
   The modules you select in this step influences the available basic roles
   available in the next installation step, see <xref linkend="sec.yast_install.conf.manual.role"/>.
   If you want to install a desktop with GNOME, you need
   to select the <emphasis>Desktop Applications Module</emphasis>.
  </para>
  <tip xml:id="ann.i.yast2.inst_mode.add-on.modules">
   <title>Modules</title>
   <para>
    Modules are fully supported parts of &productname; with a different life
    cycle. They have a clearly defined scope and are delivered via online
    channel only. Registering at the &scc; is a prerequisite for being able to
    subscribe to these channels.
   </para>
  </tip>
  <tip>
   <title>&sled;</title>
   <para>
    &sled; is available as a separate product, but also as a workstation
    extension for &sls;. If you register at the &scc;,
    the <literal>&sle; Workstation Extension</literal> can be selected for
    installation. Note that installing it requires a valid registration key.
   </para>
  </tip>
  <para>
   Proceed with <guimenu>Next</guimenu> to the <guimenu>Add-on
   Product</guimenu> dialog
  </para>
 </sect1>

 <sect1 xml:id="sec.yast_install.addons" os="sles;sled">
  <title>Add-On Product</title>
  <figure>
   <title>Add-On Product</title>
   <mediaobject>
    <textobject role="description"><phrase>Add-On Product</phrase></textobject>
    <imageobject role="fo">
     <imagedata fileref="install_addon.png" width="75%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="install_addon.png" width="75%"/>
    </imageobject>
   </mediaobject>
  </figure>
  <para>
   Here you can specify sources for additional
   add-on products not available on the registration server.
  </para>
  <para>
   If you do not want to install add-ons, proceed with <guimenu>Next</guimenu>.
   Otherwise activate <guimenu>I would like to install an additional Add-on
   Product</guimenu>. Specify the Media Type by choosing from CD, DVD, Hard
   Disk, USB Mass Storage, a Local Directory or a Local ISO Image. If
   network access has been configured you can choose from additional remote
   sources such as HTTP, SLP, FTP, etc. Alternatively you may directly specify
   a URL. Check <guimenu>Download Repository Description Files</guimenu> to
   download the files describing the repository now. If deactivated, they will
   be downloaded after the installation starts. Proceed with
   <guimenu>Next</guimenu> and insert a CD or DVD if required.
  </para>
  <para>
   Depending on the add-on's content, it may be necessary to accept additional
   license agreements. If you have chosen an add-on product requiring a
   registration key, you will be asked to enter it at the <guimenu>Extension
   and Module Registration Codes</guimenu> page. Proceed with
   <guimenu>Next</guimenu>.
  </para>
  <tip>
   <title><quote>No Registration Key</quote> Error</title>
   <para>
    If you have chosen a product in the <guimenu>Extension
    Selection</guimenu> dialog for which you do not have a valid registration
    key, choose <guimenu>Back</guimenu> until you see the <guimenu>Extension
    Selection</guimenu> dialog. Deselect the module or extension and proceed
    with <guimenu>Next</guimenu>. Modules or extensions can also be installed
    at any time later from the running system as described in
    <xref linkend="cha.add-ons"/>.
   </para>
  </tip>
 </sect1>

 <sect1 xml:id="sec.yast_install.conf.manual.role">
  <title>System Role</title>
  <figure os="sles;sled">
   <title>System Role Selection</title>
   <mediaobject>
    <imageobject role="fo">
     <imagedata os="sles" fileref="install_system_role_sles.png" width="75%"/>
     <imagedata os="sled" fileref="install_system_role_sled.png" width="75%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata os="sles" fileref="install_system_role_sles.png" width="75%"/>
     <imagedata os="sled" fileref="install_system_role_sled.png" width="75%"/>
    </imageobject>
   </mediaobject>
  </figure>
  <para>
   &productname; supports a broad range of features. To simplify the
   installation, &yast; offers predefined use cases which adjust the system to
   be installed so it is tailored for the selected scenario. Currently this
   affects the package set and the suggested partitioning scheme.
  </para>
  <para>
   Choose the <guimenu>System Role</guimenu> that meets your requirements best:
  </para>
  <variablelist>
   <varlistentry>
    <term><guimenu>&slea; with GNOME</guimenu>
    </term>
    <listitem>
     <para>
      This option installs a basic &slea; server with a desktop
      environment. To have this option available, you need to enable
      the <emphasis>Desktop Applications Module</emphasis>.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><guimenu>Text Mode</guimenu>
    </term>
    <listitem>
     <para>
      This option installs a basic &slea; server without a desktop
      environment but a rich set of command line tools. You can choose
      this option without any module enabled.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><guimenu>Minimal</guimenu>
    </term>
    <listitem>
     <para>
      Select this role if you want a very small installation with only
      the basic command line tools. You can choose this option without
      any module enabled.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><guimenu>KVM Virtualization Host</guimenu>
    </term>
    <listitem>
     <para>
      Select this scenario when installing on a machine that should
      serve as a KVM host that can run other virtual machines. To have
      this option available, you need to enable the <emphasis>Server
      Applications Module</emphasis>.
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><guimenu>Xen Virtualization Host</guimenu>
    </term>
    <listitem>
     <para>
      Select this scenario when installing on a machine that should
      serve as a &xen; host that can run other virtual machines. To
      have this option available, you need to enable the
      <emphasis>Server Applications Module</emphasis>.
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </sect1>

 <sect1 xml:id="sec.yast_install.partitioning">
  <title>Partitioning</title>
  <sect2 xml:id="sec.yast_install.partitioning.important">
   <title>Important Information</title>
   <warning>
    <title>Read this Section Carefully</title>
    <para>
     Read this section carefully before continuing with
     <xref linkend="sec.yast_install.partitioning.suggested"/>.
    </para>
   </warning>
   <variablelist>
    <varlistentry>
     <term>Custom Partitioning on UEFI Machines</term>
     <listitem>
      <para>
       A UEFI machine <emphasis>requires</emphasis> an EFI system partition
       that must be mounted to <filename>/boot/efi</filename>. This partition
       must be formatted with the <literal>FAT</literal> file system.
      </para>
      <para>
       If an EFI system partition is already present on your system (for
       example from a previous Windows installation) use it by mounting it to
       <filename>/boot/efi</filename> without formatting it.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Custom Partitioning and Snapper</term>
     <listitem>
      <para os="sles;osuse">
       If the root partition is larger than 16 GB, &productname; by
       default enables file system snapshots.
      </para>
      <para os="sled">
       &productname; can be configured to use snapshots which
       provide the ability to do rollbacks of system changes.
      </para>
      <para>
       &productname; uses Snapper in conjunction with Btrfs for this
       feature. Btrfs needs to be set up with snapshots enabled for the
       root partition.
      </para>
      <para>
       Being able to create system snapshots that enable rollbacks
       requires important system directories to be mounted on a
       single partition, for example <filename>/usr</filename> and
       <filename>/var</filename>. Only directories that are excluded
       from snapshots may reside on separate partitions, for example
       <filename>/usr/local</filename>, <filename>/var/log</filename>,
       and <filename>/tmp</filename>.
      </para>
      <para>
       For details, see <xref linkend="cha.snapper"/>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Btrfs on an Encrypted Root Partition</term>
     <listitem>
      <para>
       The default partitioning setup suggests the root partition as Btrfs. To
       encrypt the root partition, make sure to use the GPT partition table type
       instead of the default MSDOS type. Otherwise the GRUB2 boot loader may
       not have enough space for the second stage loader.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>&zseries;: Using Minidisks in z/VM</term>
     <listitem>
      <para>
       If &productname; is installed on minidisks in z/VM, which reside
       on the same physical disk, the access path of the minidisks
       (/dev/disk/by-id/) is not unique. This is because it represents
       the ID of the physical disk. If two or more minidisks are on the
       same physical disk, they all have the same ID.
      </para>
      <para>
       To avoid problems when mounting minidisks, always mount them either
       <emphasis>by path</emphasis> or <emphasis>by UUID</emphasis>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>&zseries;: LVM Root File System</term>
     <listitem>
      <para>
       If you configure the system with a root file system on LVM or software
       raid array, you must place <filename>/boot</filename> on a separate,
       non-LVM or non-Raid partition, otherwise the system will fail to boot.
       The recommended size for such a partition is 500&nbsp;MB and the
       recommended file system is Ext4.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Supported Software RAID Volumes</term>
     <listitem>
      <para>
       Installing to and booting from existing software RAID volumes is supported
       for Disk Data Format (DDF) volumes and Intel Matrix Storage Manager (IMSM)
       volumes. IMSM is also known by the following names:
      </para>
      <itemizedlist mark="bullet" spacing="normal">
       <listitem>
        <para>
         Intel Rapid Storage Technology
        </para>
       </listitem>
       <listitem>
        <para>
         Intel Matrix Storage Technology
        </para>
       </listitem>
       <listitem>
        <para>
         Intel Application Accelerator / Intel Application Accelerator RAID
         Edition
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Mount Points for FCoE and iSCSI Devices</term>
     <listitem>
      <para>
       FCoE and iSCSI devices will appear asynchronously during the
       boot process. While the initrd guarantees that those devices are
       set up correctly for the root file system, there are no such
       guarantees for any other file systems or mount points like
       <filename>/usr</filename>. Hence any system mount points like
       <filename>/usr</filename> or <filename>/var</filename> are not
       supported. To use those devices, ensure correct
       synchronization of the respective services and devices.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Handling of Windows Partitions in Proposals</term>
     <listitem>
      <para>
       In case the disk selected for the suggested partitioning proposal contains
       a large Windows FAT or NTFS partition, it will automatically be resized to
       make room for the &productname; installation. To avoid data loss it is
       strongly recommended to
      </para>
      <itemizedlist mark="bullet" spacing="normal">
       <listitem>
        <para>
         make sure the partition is not fragmented (run a defragmentation program
         from Windows prior to the &productname; installation)
        </para>
       </listitem>
       <listitem>
        <para>
         double-check the suggested size for the Windows partition is big enough
        </para>
       </listitem>
       <listitem>
        <para>
         back up your data prior to the &productname; installation
        </para>
       </listitem>
      </itemizedlist>
      <para>
       To adjust the proposed size of the Windows partition, use the
       <guimenu>Expert Partitioner</guimenu>.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>
  <sect2 xml:id="sec.yast_install.partitioning.suggested">
   <title>Suggested Partitioning</title>
   <figure>
    <title>Partitioning</title>
    <mediaobject>
     <textobject role="description"><phrase>Partitioning</phrase>
     </textobject>
     <imageobject role="fo">
      <imagedata os="sles;sled" fileref="install_partitioner.png" width="75%"/>
      <imagedata os="osuse" fileref="install_partitioner_osuse.png" width="75%"/>
     </imageobject>
     <imageobject role="html">
      <imagedata os="sles;sled" fileref="install_partitioner.png" width="75%"/>
      <imagedata os="osuse" fileref="install_partitioner_osuse.png" width="75%"/>
     </imageobject>
    </mediaobject>
   </figure>
   <para os="sled;osuse">
    Define a partition setup for &productname; in this step. The installer
    creates a proposal for one of the available disks containing a root
    partition formatted with Btrfs, a swap partition, and a home partition
    formatted with XFS. On hard disks smaller than 20 GB the proposal does not
    include a separate home partition. If one or more swap partitions have been
    detected on the available hard disks, these partitions will be used. You have several options to
    proceed:
   </para>
   <para os="sles">
    Define a partition setup for &productname; in this step. Depending on the
    system role, the installer creates a proposal for one of the disks
    available. All proposals contain a root partition formatted with Btrfs (with
    snapshots enabled) and a swap partition. If you have chosen the system role
    <guimenu>Default System</guimenu> in the previous step, a home partition
    formatted with XFS will be created, too. On hard disks smaller than 20 GB
    the proposal does not include a separate home partition. If one or more swap
    partitions have been detected on the available hard disks, these existing
    ones will be used (rather than proposing a new swap partition). You have
    several options to proceed:
   </para>
   <variablelist>
    <varlistentry>
     <term><guimenu>Next</guimenu>
     </term>
     <listitem>
      <para>
       To accept the proposal without any changes, click <guimenu>Next</guimenu>
       to proceed with the installation workflow.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><guimenu>Guided Setup</guimenu>
     </term>
     <listitem>
      <para>
       To adjust the proposal, choose <guimenu>Guided Setup</guimenu>. In the
       <guimenu>Partitioning Scheme</guimenu> screen, you can enable Logical
       Volume Management (LVM) and activate disk encryption.
      </para>
      <para>
       Confirm with <guimenu>Next</guimenu> and specify the <guimenu>Filesystem
        Options</guimenu>. You can adjust the file system for the root partition
       and create a separate home partition.
      </para>
      <para>
       If the root file system format is Btrfs, you can also enable or disable
       Btrfs snapshots here.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><guimenu>Expert Partitioner</guimenu>
     </term>
     <listitem>
      <para>
       To create a custom partition setup click <guimenu>Expert
        Partitioner</guimenu>. Select either <guimenu>Start with Current
        Proposal</guimenu> if you want start with the suggested disk layout, or
       <guimenu>Start with Existing Partitions</guimenu> to ignore the suggested
       layout and start with the existing layout on the disk.  You can
       <guimenu>Add</guimenu>, <guimenu>Edit</guimenu>,
       <guimenu>Resize</guimenu>, or <guimenu>Delete</guimenu> partitions.
      </para>
      <para>
       You can also set up Logical Volumes (LVM), configure software RAID and
       device mapping (DM), encrypt Partitions, mount NFS shares and manage
       tmpfs volumes with the Expert Partitioner. To fine-tune settings such as
       the subvolume and snapshot handling for each Btrfs partition, choose
       <guimenu>Btrfs</guimenu>. For more information about custom partitioning
       and configuring advanced features, refer to
       <xref linkend="sec.expert_partitioner"/>.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>
 </sect1>

 <sect1 xml:id="sec.yast_install.timezone">
  <title>Clock and Time Zone</title>
  <figure>
   <title>Clock and Time Zone</title>
   <mediaobject>
    <textobject role="description"><phrase>Clock and Time Zone</phrase>
    </textobject>
    <imageobject os="sles;sled" role="fo">
     <imagedata fileref="install_timezone.png" width="75%"/>
    </imageobject>
    <imageobject os="sles;sled" role="html">
     <imagedata fileref="install_timezone.png" width="75%"/>
    </imageobject>
    <imageobject os="osuse" role="fo">
     <imagedata fileref="install_timezone_osuse.png" width="75%"/>
    </imageobject>
    <imageobject os="osuse" role="html">
     <imagedata fileref="install_timezone_osuse.png" width="75%"/>
    </imageobject>
   </mediaobject>
  </figure>
  <para>
   In this dialog, select your region and time zone. Both are preselected
   according to the installation language. To change the preselected values,
   either use the map or the drop-down boxes for <guimenu>Region</guimenu> and
   <guimenu>Time Zone</guimenu>. When using the map, point the cursor at the
   rough direction of your region and left-click to zoom. Now choose your
   country or region by left-clicking. Right-click to return to the world map.
  </para>
  <para>
   To set up the clock, choose whether the <guimenu>Hardware Clock is Set to
   UTC</guimenu>. If you run another operating system on your machine, such as
   Microsoft Windows, it is likely your system uses local time instead. If you
   run Linux on your machine, set the hardware clock to UTC and have the
   switch from standard time to daylight saving time performed automatically.
  </para>
  <important>
   <title>Set the Hardware Clock to UTC</title>
   <para>
    The switch from standard time to daylight saving time (and vice versa) can
    only be performed automatically when the hardware clock (CMOS clock) is set
    to UTC. This also applies if you use automatic time synchronization with
    NTP, because automatic synchronization will only be performed if the time
    difference between the hardware and system clock is less than 15 minutes.
   </para>
   <para>
    Since a wrong system time can cause serious problems (missed backups,
    dropped mail messages, mount failures on remote file systems, etc.), it is
    strongly recommended to <emphasis>always</emphasis> set the hardware clock
    to UTC.
   </para>
  </important>
  <para arch="power;x86_64" os="sles">
   If a network is already configured, you can configure time synchronization
   with an NTP server. Click <guimenu>Other Settings</guimenu> to either alter
   the NTP settings or to <guimenu>Manually</guimenu> set the time. See
   <xref linkend="cha.ntp"/> for more information on configuring the NTP
   service. When finished, click <guimenu>Accept</guimenu> to continue the
   installation.
  </para>
  <para arch="power;x86_64" os="sles">
   If running without NTP configured, consider setting
   <literal>SYSTOHC=no</literal> (<literal>sysconfig</literal> variable) to
   avoid saving unsynchronized time into the hardware clock.
  </para>
  <para os="sled;osuse">
   If a network is already configured, you can configure time synchronization
   with an NTP server. Click <guimenu>Other Settings</guimenu> to either alter
   the NTP settings or to <guimenu>Manually</guimenu> set the time. See
   <xref linkend="cha.ntp"/> for more information on configuring the NTP
   service. When finished, click <guimenu>Accept</guimenu> to continue the
   installation.
  </para>
  <para os="sled;osuse">
   If running without NTP configured, consider setting
   <literal>SYSTOHC=no</literal> (<literal>sysconfig</literal> variable) to
   avoid saving unsynchronized time into the hardware clock.
  </para>
  <note arch="zseries" os="sles">
   <title>Time Cannot Be Changed on &zseries;</title>
   <para>
    Since the operating system is not allowed to change time and date directly,
    the <guimenu>Other Settings</guimenu> option is not available on
    &zseries;.
   </para>
  </note>
 </sect1>

 <sect1 xml:id="sec.yast_install.ui" os="osuse">
  <title>User Interface</title>
  <informalfigure>
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="install_ui_osuse.png" width="75%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="install_ui_osuse.png" width="100%"/>
    </imageobject>
   </mediaobject>
  </informalfigure>
  <para>
   Select the desktop system you would like to use in the <guimenu>User
   Interface</guimenu> dialog. <guimenu>KDE</guimenu> and
   <guimenu>GNOME</guimenu> are among the most widely used desktops on Linux.
  </para>
  <para>
   If setting up a server, you probably do not need a graphical user
   interface. Choose <guimenu>Server (Text Mode)</guimenu> in this case.
  </para>
  <para>
   More desktop systems, such as XFCE, LXDE, MATE, and Enlightenment are
   available after having enabled the online repositories. Doing so is also
   recommended to get the latest security updates and fixes during
   the installation. A working Internet connection is required. To install a
   <guimenu>Custom</guimenu> user interface, choose <guimenu>Configure Online
   Repositories</guimenu>. You have the following choices:
  </para>
  <itemizedlist>
   <listitem>
    <para>
     The <guimenu>Main Repository (OSS)</guimenu> contains open source software
     (OSS). Compared to the DVD installation media, it contains many additional
     software packages, among them the above mentioned desktop systems. Choose
     this repository to install them.
    </para>
   </listitem>
   <listitem>
    <para>
     The <guimenu>Main Update Repository</guimenu> contains security updates and
     fixes for packages from the <guimenu>Main Repository (OSS)</guimenu> and
     the DVD installation media. Choosing this repository is recommended for all
     installation scenarios.
    </para>
   </listitem>
   <listitem>
    <para>
     The <guimenu>Main Repository (Non-OSS)</guimenu> contains packages with a
     proprietary software license. Choosing it is not required for installing a
     custom desktop system.
    </para>
   </listitem>
   <listitem>
    <para>
     Choosing <guimenu>Main Update Repository (Non-OSS)</guimenu> is recommended
     when also having chosen the <guimenu>Main Repository
     (Non-OSS)</guimenu>. It contains the respective updates and security fixes.
    </para>
   </listitem>
   <listitem>
    <para>
     All other repositories are intended for experienced users and
     developers. Click on a repository name to get more information.
    </para>
   </listitem>
  </itemizedlist>
  <informalfigure>
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="install_onlinerepos_osuse.png" width="75%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="install_onlinerepos_osuse.png" width="100%"/>
    </imageobject>
   </mediaobject>
  </informalfigure>
  <para>
   Confirm your selection with <guimenu>Next</guimenu>. Depending on your
   choice, you need to confirm one or more license agreements. Do so by choosing
   <guimenu>Next</guimenu> until you return to the <guimenu>User
   Interface</guimenu> screen. Now choose <guimenu>Custom</guimenu> and
   <guimenu>Next</guimenu>, to proceed to the <guimenu>Software Selection and
   System Task</guimenu>, where you can choose a custom desktop system from the
   left-hand pane.
  </para>
  <informalfigure>
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="install_custom_ui_osuse.png" width="75%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="install_custom_ui_osuse.png" width="100%"/>
    </imageobject>
   </mediaobject>
  </informalfigure>
</sect1>

 <sect1 xml:id="sec.yast_install.user">
  <title>Create New User</title>
  <para>
   Create a local user in this step. <phrase os="sles;sled">For more details,
   use the expert settings.</phrase>
  </para>
  <sect2 xml:id="sec.yast_install.user.default">
   <title>Default Dialog</title>
   <figure>
    <title>Create New User</title>
    <mediaobject>
     <textobject role="description"><phrase>Create New User</phrase>
     </textobject>
     <imageobject os="sles;sled" role="fo">
      <imagedata fileref="install_user.png" width="75%"/>
     </imageobject>
     <imageobject os="sles;sled" role="html">
      <imagedata fileref="install_user.png" width="75%"/>
     </imageobject>
     <imageobject os="osuse" role="fo">
      <imagedata fileref="install_user_osuse.png" width="75%"/>
     </imageobject>
     <imageobject os="osuse" role="html">
      <imagedata fileref="install_user_osuse.png" width="75%"/>
     </imageobject>
    </mediaobject>
   </figure>
   <para>
     After entering the first name and last
    name, either accept the proposal or specify a new
    <guimenu>User name</guimenu> that will be used to log in. Only
    use lowercase letters (a-z), digits (0-9) and the characters
    <literal>.</literal> (dot), <literal>-</literal> (hyphen) and
    <literal>_</literal> (underscore). Special characters, umlauts and accented
    characters are not allowed.
   </para>
   <para>
    Finally, enter a password for the user. Re-enter it for confirmation (to
    ensure that you did not type something else by mistake). To provide
    effective security, a password should be at least six characters long and
    consist of uppercase and lowercase letters, number and special characters
    (7-bit ASCII). Umlauts or accented characters are not allowed. Passwords you
    enter are checked for weakness. When entering a password that is easy to
    guess (such as a dictionary word or a name) you will see a warning. It is a
    good security practice to use strong passwords.
   </para>
   <important>
    <title>User Name and Password</title>
    <para>
     Remember both your user name and the password because they are needed each
     time you log in to the system.
    </para>
   </important>
   <para>
    If you install &productname; on a machine with one or more existing Linux
    installations, &yast; allows you to import user data such as user names
    and passwords. Select <guimenu>Import User Data from a Previous
    Installation</guimenu> and then <guimenu>Choose Users</guimenu> for
    import.
   </para>
   <para>If you do not want to configure any
    local users (for example when setting up a client on a network with
    centralized user authentication), skip this step by choosing
    <guimenu>Next</guimenu> and confirming the warning. Network user
    authentication can be configured at any time later in the installed system;
    refer to <xref linkend="cha.yast.userman"/> for instructions.
   </para>
   <para>
    Two additional options are available:
   </para>
   <variablelist>
    <varlistentry>
     <term><guimenu>Use this Password for System Administrator</guimenu>
     </term>
     <listitem>
      <para>
       If checked, the same password you have entered for the user will be used
       for the system administrator &rootuser;. This option is suitable for
       stand-alone workstations or machines in a home network that are
       administrated by a single user. When not checked, you are prompted for a
       system administrator password in the next step of the installation
       workflow (see <xref linkend="sec.yast_install.user.root"/>).
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><guimenu>Automatic Login</guimenu>
     </term>
     <listitem>
      <para>
       This option automatically logs the current user in to the system when it
       starts. This is mainly useful if the computer is operated by only one
       user. <phrase os="sles">For automatic login to work, the option must be
       explicitly enabled.</phrase>
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
   <warning os="sled;osuse">
    <title>Automatic Login</title>
    <para>
     With the automatic login enabled, the system boots straight into your
     desktop with no authentication. If you store sensitive data on your system,
     you should not enable this option if the computer can also be accessed by
     others.
    </para>
   </warning>
   <para os="osuse">
    If you install on a system where a previous Linux installation was found,
    you may <guimenu>Import User Data from a Previous
    Installation</guimenu>. Click <guimenu>Choose User</guimenu> for a list of
    available user accounts. Select one or more user.
   </para>
   <para os="osuse">
    In an environment where users are centrally managed (for example by NIS or
    LDAP) you may want to skip the creation of local users. Select <guimenu>Skip
    User Creation</guimenu> in this case.
   </para>
  </sect2>
  <sect2 xml:id="sec.yast_install.user.expert" os="sles;sled">
   <title>Expert Settings</title>
   <para>
    Click <guimenu>Change</guimenu> in the Create User dialog to import users
    from a previous installation (if present). Also change the password
    encryption type in this dialog.
   </para>
   <para>
    The default authentication method is <guimenu>Local
    (/etc/passwd)</guimenu>. If a former version of &productname; or another
    system using <filename>/etc/passwd</filename> is detected, you may import
    local users. To do so, check <guimenu>Read User Data from a Previous
    Installation</guimenu> and click <guimenu>Choose</guimenu>. In the next
    dialog, select the users to import and finish with <guimenu>OK</guimenu>.
   </para>
   <para>
    By default the passwords are encrypted with the SHA-512 hash function.
    Changing this method is not recommended unless needed for compatibility
    reasons.
   </para>
  </sect2>
 </sect1>

 <sect1 xml:id="sec.yast_install.user.root">
  <title>Password for the System Administrator</title>
  <figure>
   <title>Password for the System Administrator</title>
   <mediaobject>
    <textobject role="description"><phrase>Password for the System Administrator &rootuser;</phrase>
    </textobject>
    <imageobject role="fo">
     <imagedata os="sles;sled" fileref="install_root.png" width="75%"/>
     <imagedata os="osuse" fileref="install_root_osuse.png" width="75%"/>
    </imageobject>
    <imageobject role="html">
     <imagedata os="sles;sled" fileref="install_root.png" width="75%"/>
     <imagedata os="osuse" fileref="install_root_osuse.png" width="75%"/>
    </imageobject>
   </mediaobject>
  </figure>
  <para>
   If you have not chosen <guimenu>Use this Password for System
   Administrator</guimenu> in the previous step, you will be prompted to enter
   a password for the System Administrator &rootuser;. Otherwise this
   configuration step is skipped.
  </para>
  <para>
   &rootuser; is the name of the superuser, or the administrator of the system.
   Unlike regular users, &rootuser; has unlimited
   rights to change the system configuration, install programs, and set up new
   hardware. If users forget their passwords or have other problems with the
   system, &rootuser; can help. The &rootuser; account should only be used for
   system administration, maintenance, and repair. Logging in as &rootuser; for
   daily work is rather risky: a single mistake could lead to irretrievable
   loss of system files.
  </para>
  <para>
   For verification purposes, the password for &rootuser; must be entered
   twice. Do not forget the &rootuser; password. After having been entered,
   this password cannot be retrieved.
  </para>
  <tip>
   <title>Passwords and Keyboard Layout</title>
   <para>
    It is recommended to only use characters that are available on an English
    keyboard. In case of a system error or when you need to start your system
    in rescue mode a localized keyboard might not be available.
   </para>
  </tip>
  <para>
   The &rootuser; password can be changed any time later in the installed
   system. To do so run &yast; and start <menuchoice> <guimenu>Security and
   Users</guimenu> <guimenu>User and Group Management</guimenu> </menuchoice>.
  </para>
  <important>
   <title>The <systemitem class="username">root</systemitem> User</title>
   <para>
    The user &rootuser; has all the permissions needed to make changes to the
    system. To carry out such tasks, the &rootuser; password is required. You
    cannot carry out any administrative tasks without this password.
   </para>
  </important>
 </sect1>

 <sect1 xml:id="sec.yast_install.proposal">
  <title>Installation Settings</title>
  <figure>
   <title>Installation Settings</title>
   <mediaobject>
    <textobject role="description"><phrase>Installation Settings</phrase>
    </textobject>
    <imageobject os="sles" role="fo">
     <imagedata fileref="install_summary_sles.png" width="75%"/>
    </imageobject>
    <imageobject os="sles" role="html">
     <imagedata fileref="install_summary_sles.png" width="75%"/>
    </imageobject>
    <imageobject os="sled" role="fo">
     <imagedata fileref="install_summary_sled.png" width="75%"/>
    </imageobject>
    <imageobject os="sled" role="html">
     <imagedata fileref="install_summary_sled.png" width="75%"/>
    </imageobject>
    <imageobject os="osuse" role="fo">
     <imagedata fileref="install_summary_osuse.png" width="75%"/>
    </imageobject>
    <imageobject os="osuse" role="html">
     <imagedata fileref="install_summary_osuse.png" width="75%"/>
    </imageobject>
   </mediaobject>
  </figure>
  <para>
   On the last step before the real installation takes place, you can alter
   installation settings suggested by the installer. To modify the suggestions,
   click the respective headline. After having made changes to a particular
   setting, you are always returned to the Installation Settings window, which
   is updated accordingly.
  </para>
  <sect2 xml:id="sec.yast_install.proposal.sofware">
   <title><guimenu>Software</guimenu></title>
   <para>
    &productname; contains several software patterns for various application
    purposes. Click <guimenu>Software</guimenu> to open the <guimenu>Software
    Selection and System Tasks</guimenu> screen where you can modify the
    pattern selection according to your needs. Select a pattern from the list
    and see a description in the right-hand part of the window. Each pattern
    contains several software packages needed for specific functions (for
    example <phrase os="sled;osuse">Multimedia or Office
    software</phrase><phrase os="sles">Web and LAMP server or a print
    server</phrase>). For a more detailed selection based on software packages
    to install, select <guimenu>Details</guimenu> to switch to the &yast;
    Software Manager.
   </para>
   <para>
    You can also install additional software packages or remove software
    packages from your system at any later time with the &yast; Software
    Manager. For more information, refer to <xref linkend="cha.yast-software"/>.
   </para>
   <para os="sles">
    If you choose to install GNOME, &productname; is installed with the X.org
    display server. As an alternative to &gnome;, the light-weight
    window manager IceWM can be installed. Select <guimenu>Details</guimenu>
    from the <guimenu>Software Selection and System Tasks</guimenu> screen and
    search for <literal>icewm</literal>.
   </para>
   <para os="sled">
    By default, &productname; uses the Wayland display server protocol.
   </para>
   <figure>
    <title>Software Selection and System Tasks</title>
    <mediaobject>
     <textobject role="description"><phrase>Software Selection and System Tasks</phrase>
     </textobject>
     <imageobject role="fo">
      <imagedata os="sles" fileref="install_software_sles.png" width="75%"/>
      <imagedata os="sled" fileref="install_software_sled.png" width="75%"/>
      <imagedata os="osuse" fileref="install_software_osuse.png" width="75%"/>
     </imageobject>
     <imageobject role="html">
      <imagedata os="sles" fileref="install_software_sles.png" width="75%"/>
      <imagedata os="sled" fileref="install_software_sled.png" width="75%"/>
      <imagedata os="osuse" fileref="install_software_osuse.png" width="75%"/>
     </imageobject>
    </mediaobject>
   </figure>
   <tip os="sles" arch="zseries">
    <title>&zseries;: Hardware Cryptography Support</title>
    <para>
     The hardware cryptography stack is not installed by default. To install
     it, select <guimenu>System z HW crypto support</guimenu> in the
     <guimenu>Software Selection and System Tasks</guimenu> screen.
    </para>
   </tip>
   <tip xml:id="ann.sec.yast_install.proposal.sofware.language">
    <title>Adding Secondary Languages</title>
    <para>
     The language you selected with the first step of the installation will be
     used as the primary (default) language for the system. You can add
     secondary languages from within the <guimenu>Software</guimenu> dialog by
     choosing <menuchoice> <guimenu>Details</guimenu> <guimenu>View</guimenu>
     <guimenu>Languages</guimenu></menuchoice>.
    </para>
   </tip>
  </sect2>
  <sect2 xml:id="sec.yast_install.proposal.booting">
   <title><guimenu>Booting</guimenu></title>
   <para>
    The installer proposes a boot configuration for your system. Other
    operating systems found on your computer, such as Microsoft Windows or
    other Linux installations, will automatically be detected and added to the
    boot loader. However, &productname; will be booted by default. Normally,
    you can leave these settings unchanged. If you need a custom setup, modify
    the proposal according to your needs. For information, see
    <xref linkend="sec.grub2.yast2.config"/>.
   </para>
   <important>
    <title>Software RAID 1</title>
    <para>
     Booting a configuration where <filename>/boot</filename> resides on a
     software RAID 1 device is supported, but it requires to install the boot
     loader into the MBR (<menuchoice> <guimenu>Boot Loader Location</guimenu>
     <guimenu>Boot from Master Boot Record</guimenu> </menuchoice>). Having
     <filename>/boot</filename> on software RAID devices with a level other
     than RAID 1 is not supported. <phrase os="sles">Also see
     <xref
     linkend="cha.raidroot"/>.</phrase>
    </para>
   </important>
  </sect2>
  <sect2 xml:id="sec.yast_install.proposal.firewall">
   <title><guimenu>Firewall and SSH</guimenu></title>
   <para>
    By default &susefirewall; is enabled on all configured network interfaces.
    To globally disable the firewall for this computer, click
    <guimenu>Disable</guimenu> (not recommended).
   </para>
   <note>
    <title>Firewall Settings</title>
    <para>
     If the firewall is activated, all interfaces are configured to be in the
     <quote>External Zone</quote>, where all ports are closed by default,
     ensuring maximum security. The only port you can open during the
     installation is port 22 (SSH), to allow remote access. All other services
     requiring network access (such as FTP, Samba, Web server, etc.) will only
     work after having adjusted the firewall settings. Refer to
     <xref linkend="cha.security.firewall"/> for more information.
    </para>
   </note>
   <para>
    To enable remote access via the secure shell (SSH), make sure the
    <literal>SSH service</literal> is enabled and the <literal>SSH
    port</literal> is open.
   </para>
   <tip>
    <title>Existing SSH Host Keys</title>
    <para>
     If you install &productname; on a machine with existing Linux
     installations, the installation routine imports an SSH host key. It
     chooses the host key with the most recent access time by default.
     <phrase os="sles;sled">See also
     <xref linkend="sec.yast_install.proposal.sshimport"/>.</phrase>
    </para>
   </tip>
   <para>
    If you are performing a remote administration over VNC, you can also
    specify whether the machine should be accessible via VNC after the
    installation. Note that enabling VNC also requires you to set the
    <guimenu>Default systemd Target</guimenu> to <guimenu>graphical</guimenu>.
   </para>
  </sect2>
  <sect2 xml:id="sec.yast_install.proposal.kdump" os="sles">
   <title><guimenu>Kdump</guimenu></title>
   <para>
    Using Kdump, you can save a dump of the kernel (in case of a crash) to
    analyze what went wrong. Use this dialog to enable and configure Kdump.
    Find detailed information at <xref linkend="cha.tuning.kexec"/>.
   </para>
  </sect2>
  <sect2 xml:id="sec.yast_install.proposal.blacklist" os="sles" arch="zseries">
   <title>&zseries;: Blacklist Devices</title>
   <para>
    To save memory, all channels for devices currently not in use are
    blacklisted by default (each channel that is not blacklisted occupies
    approximately 50 KB of memory). To configure additional hardware in the
    installed system using channels that are currently blacklisted, run the
    respective &yast; module to enable the respective channels first.
    <remark condition="clarity">
     2014-06-05 - fs: FIXME What's the name of the yast module?
    </remark>
   </para>
   <para>
    To disable blacklisting, click <guimenu>disable</guimenu>.
   </para>
  </sect2>
  <sect2 xml:id="sec.yast_install.proposal.runlevel">
   <title><guimenu>Default systemd Target</guimenu></title>
   <para>
    &productname; can boot into two different targets (formerly known as
    <quote>runlevels</quote>). The <guimenu>graphical</guimenu> target starts a
    display manager, whereas the <guimenu>multi-user</guimenu> target starts
    the command line interface.
   </para>
   <para>
    The default target is <guimenu>graphical</guimenu>. In case you have not
    installed the <guimenu>X Window System</guimenu> patterns, you need to
    change it to <guimenu>multi-user</guimenu>. If the system should be
    accessible via VNC, you need to choose <guimenu>graphical</guimenu>.
   </para>
  </sect2>
  <sect2 os="sles;sled" xml:id="sec.yast_install.proposal.sshimport"> <title><guimenu>Import SSH Host Keys and Configuration</guimenu></title>
   <para>
    If an existing Linux installation on your computer was detected, &yast;
    will import the most recent SSH host key found in
    <filename>/etc/ssh</filename> by default, optionally including other files
    in the directory as well. This makes it possible to reuse the SSH identity
    of the existing installation, avoiding the <literal>REMOTE HOST
    IDENTIFICATION HAS CHANGED</literal> warning on the first connection. Note
    that this item is not shown in the installation summary if &yast; has not
    discovered any other installations.
   </para>
   <figure>
    <title>Import SSH Host Keys and Configuration</title>
    <mediaobject>
     <textobject role="description"><phrase>Import SSH Host Keys and Configuration</phrase>
     </textobject>
     <imageobject role="fo">
      <imagedata os="sles" fileref="install_ssh_import_sles.png" width="75%"/>
      <imagedata os="sled" fileref="install_ssh_import_sled.png" width="75%"/>
     </imageobject>
     <imageobject role="html">
      <imagedata os="sles" fileref="install_ssh_import_sles.png"/>
      <imagedata os="sled" fileref="install_ssh_import_sled.png"/>
     </imageobject>
    </mediaobject>
   </figure>
   <variablelist>
    <varlistentry>
     <term><guimenu>
       I would like to import SSH keys from a previous install:
      </guimenu>
     </term>
     <listitem>
      <para>
       Select this option to import the SSH host key and optionally
       the configuration of an installed system. You can select the
       installation to import from in the option list below.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><guimenu>
       Import SSH Configuration
      </guimenu>
     </term>
     <listitem>
      <para>
       Enable this to copy other files in <filename>/etc/ssh</filename> to the
       installed system in addition to the host keys.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>
  <sect2 xml:id="sec.yast_install.proposal.system">
   <title><guimenu>System Information</guimenu></title>
   <para>
    This screen lists all the hardware information the installer could obtain
    about your computer. When opened for the first time, the hardware detection
    is started. Depending on your system, this may take some time. Select any
    item in the list and click <guimenu>Details</guimenu> to see detailed
    information about the selected item. Use <guimenu>Save to File</guimenu> to
    save a detailed list to either the local file system or a removable device.
   </para>
   <para>
    Advanced users can also change the <guimenu>PCI ID Setup</guimenu> and
    kernel settings by choosing <guimenu>Kernel Settings</guimenu>. A screen
    with two tabs opens:
   </para>
   <variablelist>
    <varlistentry>
     <term><guimenu>PCI ID Setup</guimenu>
     </term>
     <listitem>
      <para>
       Each kernel driver contains a list of device IDs of all devices it
       supports. If a new device is not in any driver's database, the device is
       treated as unsupported, even if it can be used with an existing driver.
       You can add PCI IDs to a device driver here. Only advanced users should
       attempt to do so.
      </para>
      <para>
       To add an ID, click <guimenu>Add</guimenu> and select whether to
       <guimenu>Manually</guimenu> enter the data, or whether to choose from a
       list. Enter the required data. The <guimenu>SysFS Dir</guimenu> is the
       directory name from <filename>/sys/bus/pci/drivers</filename>&mdash;if
       empty, the <guimenu>driver</guimenu> name is used as the directory name.
       Existing entries can be managed with <guimenu>Edit</guimenu> and
       <guimenu>Delete</guimenu>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><guimenu>Kernel Settings</guimenu>
     </term>
     <listitem>
      <para>
       Change the <guimenu>Global I/O Scheduler</guimenu> here. If <guimenu>Not
       Configured</guimenu> is chosen, the default setting for the respective
       architecture will be used. This setting can also be changed at any time
       later from the installed system. Refer to
       <xref linkend="cha.tuning.io"/> for details on I/O tuning.
      </para>
      <para>
       Also activate the <guimenu>Enable SysRq Keys</guimenu> here. These keys
       will let you issue basic commands (such as rebooting the system or
       writing kernel dumps) in case the system crashes. Enabling these keys is
       recommended when doing kernel development. Refer to
       <link xlink:href="https://www.kernel.org/doc/html/latest/admin-guide/sysrq.html"/>
       for details.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>
 </sect1>

 <sect1 xml:id="sec.yast_install.perform">
  <title>Performing the Installation</title>
  <para>
   After configuring all installation settings, click
   <guimenu>Install</guimenu> in the Installation Settings window to start the
   installation. Some software may require a license confirmation. If your
   software selection includes such software, license confirmation dialogs are
   displayed. Click <guimenu>Accept</guimenu> to install the software package.
   When not agreeing to the license, click <guimenu>I Disagree</guimenu> and
   the software package will not be installed. In the dialog that follows,
   confirm with <guimenu>Install</guimenu> again.
  </para>
  <para>
   The installation usually takes between 15 and 30 minutes, depending on the
   system performance and the selected software scope. After having prepared
   the hard disk and having saved and restored the user settings, the software
   installation starts. During this procedure a slide show introduces the
   features of &productname;. Choose <guimenu>Details</guimenu> to switch to
   the installation log or <guimenu>Release Notes</guimenu> to read important
   up-to-date information that was not available when the manuals were printed.
  </para>
  <para>
   After the software installation has completed, the system reboots into the
   new installation where you can log in. To customize the system configuration
   or to install additional software packages, start &yast;.
  </para>
  <note os="sles;sled">
   <title>One-Stage Installation</title>
   <para>
    Starting
    with &productname; 12 the system installation and basic configuration
    including the network setup is done in a single stage. After having
    rebooted into the installed system, you can log in and start using the
    system. To fine-tune the setup, to configure services or to install
    additional software, start &yast;.
   </para>
  </note>
  <sect2 xml:id="sec.s390.inst.reipl" arch="zseries" os="sles">
   <title>&zseries;: IPLing the Installed System</title>
   <para>
    &yast; usually reboots into the installed system on the &zseries;
    platform. Exceptions are installations where the boot loader
    resides on an FCP device in environments with LPAR on a machine older than
    z196 or with z/VM older than release 5.4. The boot loader gets written to a
    separate partition mounted as <filename>/boot/zipl/</filename>.
   </para>
   <para>
    In cases where an automatic reboot is not possible, &yast; will show a
    dialog containing information about from which device to do an IPL. Accept
    the shutdown option and perform an IPL after the shutdown. The procedure
    varies according to the type of installation:
   </para>
   <variablelist>
    <varlistentry>
     <term>LPAR Installation</term>
     <listitem>
      <para>
       In the &zseries; HMC, select <guimenu>Load</guimenu>, select
       <guimenu>Clear</guimenu>, then enter the loading address (the address of
       the device containing the <filename>/boot/zipl</filename> directory with
       the boot loader). If using a zFCP disk as the boot device, choose
       <guimenu>Load from SCSI</guimenu> and specify the load address of your
       FCP adapter plus WWPN and LUN of the boot device. Now start the loading
       process.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>z/VM Installation</term>
     <listitem>
      <para>
       Log in to the VM guest (see
       <xref linkend="ex.prep.prep_ipl.vm.linux_guest.config"/> for the
       configuration) as <systemitem class="username">LINUX1</systemitem> and
       proceed to IPL the installed system:
      </para>
      <screen>IPL 151 CLEAR</screen>
      <para>
       <literal>151</literal> is an example address of the DASD boot device,
       replace this value with the correct address.
      </para>
      <para>
       If using a zFCP disk as the boot device, specify both the zFCP WWPN and
       LUN of the boot device before initiating the IPL. The parameter length
       is limited to eight characters. Longer numbers must be separated by
       spaces:
      </para>
      <screen>SET LOADDEV PORT 50050763 00C590A9 LUN 50010000 00000000 </screen>
      <para>
       Finally, initiate the IPL:
      </para>
      <screen>IPL FC00</screen>
      <para>
       <literal>FC00</literal> is an example address of the zFCP adapter,
       replace this value with the correct address.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>KVM Guest Installation</term>
     <listitem>
      <para>
       After the installation has finished, the virtual machine is shut
       down. At this point, log in to the KVM host, edit the virtual machine's
       description file and restart it to IPL into the installed system:
      </para>
      <procedure>
       <step>
        <para>
        Log in to the KVM host.
        </para>
       </step>
       <step>
        <para>
        Edit the domain XML file by running
        </para>
        <screen>&prompt.sudo;<command>virsh edit s12-1</command></screen>
        <para>
        and remove the following lines:
        </para>
<screen><![CDATA[  <!-- Boot kernel - remove 3 lines after successfull installation -->
  <kernel>/var/lib/libvirt/images/s12-kernel.boot</kernel>
  <initrd>/var/lib/libvirt/images/s12-initrd.boot</initrd>
  <cmdline>linuxrcstderr=/dev/console</cmdline>]]></screen>
       </step>
       <step>
        <para>
        Restart the &vmguest; to IPL into the installed system:
        </para>
        <screen>&prompt.sudo;<command>virsh start s12-1 --console</command></screen>
       </step>
      </procedure>
      <note>
       <title><literal>cio_ignore</literal> Is Disabled for KVM Installations</title>
       <para>
        The kernel parameter <literal>cio_ignore</literal> prevents the kernel
        from looking at all the available hardware devices. However, for KVM
        guests, the hypervisor already takes care to only provide access to the
        correct devices. Therefore <literal>cio_ignore</literal> is disabled by
        default when installing a KVM guest (for z/VM and LPAR installations it
        is activated by default).
       </para>
      </note>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>
  <sect2 xml:id="sec.s390.inst.reipl.reconnect" arch="zseries" os="sles">
   <title>&zseries;: Connecting to the Installed System</title>
   <para>
    After IPLing the system, establish a connection via VNC, SSH, or X to log
    in to the installed system. Using either VNC or SSH is recommended. To
    customize the system configuration or to install additional software
    packages, start &yast;.
   </para>
   <sect3 xml:id="sec.s390.inst.reipl.reconnect.vnc">
    <title>Using VNC to Connect</title>
    <para>
     A message in the 3270&nbsp;terminal asks you to connect to the Linux
     system using a VNC client. However, this message is easily missed, because
     it is mixed with kernel messages and the terminal process might quit
     before you notice the message. If nothing happens for five
     minutes, try to initiate a connection to the Linux system using a VNC
     viewer.
    </para>
    <para>
     If you connect using a JavaScript-capable browser, enter the complete URL,
     consisting of the IP address of the installed system along with the port
     number, in the following fashion:
    </para>
<screen>http://<replaceable>IP_OF_INSTALLED_SYSTEM</replaceable>:5801/</screen>
   </sect3>
   <sect3 xml:id="sec.s390.inst.reipl.reconnect.ssh">
    <title>Using SSH to Connect</title>
    <para>
     A message in the 3270&nbsp;terminal asks you to connect to the Linux
     system with an SSH client. This message is easily missed, however, because
     it is mixed with kernel messages and the terminal process might quit
     before you become aware of the message.
    </para>
    <para>
     When the message appears, use SSH to log in to the Linux system as
     &rootuser;. If the connection is denied or times out, wait for the login
     timeout to expire, then try again (this time depends on server
     settings).
    </para>
   </sect3>
   <sect3 xml:id="sec.s390.inst.reipl.reconnect.x">
    <title>Using X to Connect</title>
    <para>
     When IPLing the installed system, make sure that the X server used for the
     first phase of the installation is up and still available before booting
     from the DASD. &yast; opens on this X server to finish the installation.
     Complications may arise if the system is booted up but unable to connect
     to the X server in a timely fashion.
    </para>
   </sect3>
  </sect2>
 </sect1>
</chapter>
