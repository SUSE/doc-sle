<?xml version="1.0" encoding="UTF-8"?>
<sect1 xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="deployment_prep_zseries_prep_io_auto_config.xml" version="5.0" xml:id="sec-zseries-prep-io-auto-config">
 <title>IBM Z 系統上的 I/O 裝置自動組態</title>

 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>

 <para>
  I/O 裝置自動組態機制可讓使用者指定應在 Linux 上自動啟用的 I/O 裝置的 ID 和設定。透過在 DPM (動態分割區管理員) 模式下執行的 HMC 針對 LPAR 指定這些資訊。
 </para>

 <note>
  <para>
   執行 DPM 的系統上可以使用 I/O 裝置自動組態功能。DPM 在 LinuxONE 機器上預設即會執行。對於 IBM Z，則必須訂購此功能。
  </para>
 </note>

 <para>
  可以使用 <option>rd.zdev=no-auto</option> 核心參數來停用自動組態。
 </para>

 <para>
  若要使用 YaST 啟用 I/O 自動組態，請執行 <command>yast2 system_settings</command> 指令，移至<guimenu>核心設定</guimenu>區段，然後啟用<guimenu>自動組態</guimenu>選項。
 </para>

 <para>
  若要在 AutoYaST 設定檔中停用 I/O 自動組態，請將 <option>rd.zdev=no-auto</option> 附加至開機選項。例如：
 </para>

<screen>
&lt;bootloader&gt;
  &lt;global&gt;
    &lt;append&gt;rd.zdev=no-auto&lt;/append&gt;
  &lt;/global&gt;
&lt;/bootloader&gt;</screen>

 <para>
  安裝期間，<guimenu>安裝設定</guimenu>螢幕的<guimenu>裝置設定</guimenu>區段中會顯示自動組態設定的狀態。
 </para>
</sect1>
