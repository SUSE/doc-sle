<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter PUBLIC "-//Novell//DTD NovDoc XML V1.0//EN" "novdocx.dtd">
<!--
*********************************
Please see LICENSE.txt for this document's license.
*********************************
-->
<chapter xml:base="deployment_remote.xml" role="General" id="cha.deployment.remoteinst">
 <title>원격 설치</title>
 <para>
  <phrase role="productname"><phrase os="sles">SUSE® Linux Enterprise Server</phrase></phrase>는 다양한 방법으로 설치할 수 있습니다. <xref linkend="cha.inst"/>에서 다루는 일반적인 미디어 설치를 비롯하여 다양한 네트워크 기반 접근 방식에서 선택하거나, <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>의 설치에 대해 완전한 자동 접근법을 적용할 수도 있습니다.
 </para>
 <para>
  각 방법은 두 개의 간단한 점검 목록을 통해 소개되는 데, 하나는 이 방법에 대한 전제 조건이 나열되어 있고 다른 하나는 기본 절차를 설명합니다. 다음으로 이 설치 시나리오에 사용된 모든 기법에 대한 자세한 내용이 제공됩니다.
 </para>
 <note>
  <para>
   다음 절에서 새 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 설치가 보관된 시스템을 <emphasis>대상 시스템</emphasis> 또는 <emphasis>설치 대상</emphasis>이라고 합니다. 이전에 <quote>설치 원본</quote>이라고 부르던 <emphasis>리포지토리</emphasis>라는 용어는 설치 데이터의 모든 원본에 사용됩니다. 여기에는 CD 및 DVD와 같은 실제 미디어와 네트워크에서 설치 데이터를 배포하는 네트워크 서버가 포함됩니다.
  </para>
 </note>
 <sect1 id="sec.deployment.remoteinst.scenario">
  <title>원격 설치의 설치 시나리오</title>

  <para>
   이 절에서는 원격 설치의 가장 일반적인 설치 시나리오에 대해 소개합니다. 각 시나리오에 대해 전제 조건 목록을 신중하게 확인한 후 이 시나리오에 간략하게 설명된 절차를 따르십시오. 특정 단계에 대한 상세 지침이 필요할 경우 각 지침에 제공된 링크를 눌러 이동하십시오. 
  </para>

  <sect2 id="sec.deployment.remoteinst.scenario.vncstat">
   <title>VNC를 통한 단순 원격 설치 - 정적 네트워크 구성</title>
   <para>
    설치를 위해 부팅하기 위해서는 이 설치 유형에 대상 시스템에 대한 물리적 액세스가 어느 정도 필요합니다. 설치 자체는 설치 프로그램에 연결할 VNC를 사용하여 원격 워크스테이션에서 전적으로 제어되지만, 사용자 개입은 <xref linkend="cha.inst"/>의 수동 설치에서와 같이 필요합니다.
   </para>
   <para>
    이 설치 유형에서 다음과 같은 요구사항이 충족되는지 확인합니다.
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      네트워크 연결이 작동 중인 원격 리포지토리(NFS, HTTP, FTP 또는 SMB)
     </para>
    </listitem>
    <listitem>
     <para>
      네트워크 연결 중인 대상 시스템
     </para>
    </listitem>
    <listitem>
     <para>
      네트워크 연결 중인 제어 시스템과 VNC 뷰어 소프트웨어 또는 Java 지원 브라우저(Firefox, Chromium, Internet Explorer, Opera 등)
     </para>
    </listitem>
    <listitem>
     <para>
      대상 시스템을 부팅할 실제 부팅 미디어(CD, DVD 또는 USB 플래시 드라이브)
     </para>
    </listitem>
    <listitem>
     <para>
      리포지토리와 제어 시스템에 이미 지정된 유효한 고정 IP 주소
     </para>
    </listitem>
    <listitem>
     <para>
      대상 시스템에 지정할 유효한 정적 IP 주소
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.vncstat">
    <para>
     이 설치 유형을 실행하려면 다음을 수행하십시오.
    </para>
    <step performance="required">
     <para>
      <xref linkend="sec.deployment.remoteinst.instserver"/>에서 설명한 대로 리포지토리를 설정합니다. NFS, HTTP 또는 FTP 네트워크 서버를 선택합니다. SMB 리포지토리의 경우 <xref linkend="sec.deployment.remoteinst.instserver.smb"/>를 참조하십시오.
     </para>
    </step>
    <step performance="required">
     <para>
      <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 미디어 키트의 DVD1을 사용하여 대상 시스템을 부팅합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      대상 시스템의 부팅 화면이 나타나면 부팅 옵션 프롬프트를 사용하여 적절한 VNC 옵션과 리포지토리의 주소를 설정합니다. 자세한 내용은 <xref linkend="sec.deployment.remoteinst.bootinst"/>에서 설명됩니다.
     </para>
     <para>
      대상 시스템은 텍스트 기반 환경에서 부팅합니다. 이 환경은 VNC 뷰어 응용 프로그램 또는 브라우저에서 사용할 수 있는 그래픽 설치 환경 하에서 네트워크 주소와 디스플레이 번호를 제공합니다. VNC 설치는 OpenSLP를 통해 알립니다(방화벽 설정에서 허용하는 경우). VNC 설치는 <xref linkend="proc.deployment.remoteinst.monitor.vnc.prep"/>에 설명된 대로 <command>slptool</command>을 사용하여 찾을 수 있습니다.
     </para>
    </step>
    <step performance="required">
     <para>
      제어 워크스테이션에서 VNC 보기 응용 프로그램 또는 웹 브라우저를 열고 <xref linkend="sec.deployment.remoteinst.monitor.vnc"/>에서 설명한 대로 대상 시스템에 연결합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <xref linkend="cha.inst"/>에서 설명한 대로 설치를 수행합니다. 설치의 마지막 부분에서 재부팅한 후에 대상 시스템에 다시 연결합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      설치를 완료합니다.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.vncdyn">
   <title>VNC를 통한 단순 원격 설치 - 동적 네트워크 구성</title>
   <para>
    설치를 위해 부팅하기 위해서는 이 설치 유형에 대상 시스템에 대한 물리적 액세스가 어느 정도 필요합니다. 네트워크 구성은 DHCP로 설정됩니다. 설치 자체는 설치 프로그램에 연결할 VNC를 사용하여 원격 워크스테이션에서 전적으로 제어되지만, 실제 구성을 위해서는 사용자 개입이 필요합니다.
   </para>
   <para>
    이 설치 유형에서 다음과 같은 요구사항이 충족되는지 확인합니다.
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      네트워크 연결이 작동 중인 원격 리포지토리(NFS, HTTP, FTP 또는 SMB)
     </para>
    </listitem>
    <listitem>
     <para>
      네트워크 연결 중인 대상 시스템
     </para>
    </listitem>
    <listitem>
     <para>
      네트워크 연결 중인 제어 시스템과 VNC 뷰어 소프트웨어 또는 Java 지원 브라우저(Firefox, Chromium, Internet Explorer 또는 Opera)
     </para>
    </listitem>
    <listitem>
     <para>
      <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 미디어 키트의 DVD1을 사용하여 대상 시스템을 부팅합니다.
     </para>
    </listitem>
    <listitem>
     <para>
      IP 주소를 제공하는 실행 중인 DHCP 서버
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.vncdyn">
    <para>
     이 설치 유형을 실행하려면 다음을 수행하십시오.
    </para>
    <step performance="required">
     <para>
      <xref linkend="sec.deployment.remoteinst.instserver"/>에서 설명한 대로 리포지토리를 설정합니다. NFS, HTTP 또는 FTP 네트워크 서버를 선택합니다. SMB 리포지토리의 경우 <xref linkend="sec.deployment.remoteinst.instserver.smb"/>를 참조하십시오.
     </para>
    </step>
    <step performance="required">
     <para>
      <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 미디어 키트의 DVD1을 사용하여 대상 시스템을 부팅합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      대상 시스템의 부팅 화면이 나타나면 부팅 옵션 프롬프트를 사용하여 적절한 VNC 옵션과 리포지토리의 주소를 설정합니다. 자세한 내용은 <xref linkend="sec.deployment.remoteinst.bootinst"/>에서 설명됩니다.
     </para>
     <para>
      대상 시스템은 텍스트 기반 환경에서 부팅합니다. 이 환경은 VNC 뷰어 응용 프로그램 또는 브라우저에서 사용할 수 있는 그래픽 설치 환경 하에서 네트워크 주소와 디스플레이 번호를 제공합니다. VNC 설치는 OpenSLP를 통해 알립니다(방화벽 설정에서 허용하는 경우). VNC 설치는 <xref linkend="proc.deployment.remoteinst.monitor.vnc.prep"/>에 설명된 대로 <command>slptool</command>을 사용하여 찾을 수 있습니다.
     </para>
    </step>
    <step performance="required">
     <para>
      제어 워크스테이션에서 VNC 보기 응용 프로그램 또는 웹 브라우저를 열고 <xref linkend="sec.deployment.remoteinst.monitor.vnc"/>에서 설명한 대로 대상 시스템에 연결합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <xref linkend="cha.inst"/>에서 설명한 대로 설치를 수행합니다. 설치의 마지막 부분에서 재부팅한 후에 대상 시스템에 다시 연결합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      설치를 완료합니다.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.vncpxe">
   <title>VNC를 통한 원격 설치 - PXE 부팅 및 WOL(Wake On LAN)</title>
   <para>
    이 설치 유형은 완전 자동입니다. 대상 시스템은 원격으로 시작되고 부팅됩니다. 사용자 개입은 실제 설치에만 필요합니다. 이 접근법은 교차 사이트 배포에 적합합니다.
   </para>
   <para>
    이 설치 유형을 수행하려면 다음과 같은 요구사항이 충족되는지 확인합니다.
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      네트워크 연결이 작동 중인 원격 리포지토리(NFS, HTTP, FTP 또는 SMB)
     </para>
    </listitem>
    <listitem>
     <para>
      TFTP 서버
     </para>
    </listitem>
    <listitem>
     <para>
      네트워크에서 실행 중인 DHCP 서버
     </para>
    </listitem>
    <listitem>
     <para>
      PXE 부팅, 네트워킹 및 WOL(Wake On Lan)을 네트워크에 연결할 수 있는 대상 시스템
     </para>
    </listitem>
    <listitem>
     <para>
      네트워크 연결 중인 제어 시스템과 VNC 뷰어 소프트웨어 또는 Java 지원 브라우저(Firefox, Chromium, Internet Explorer 또는 Opera)
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.vncpxe">
    <para>
     이 설치 유형을 실행하려면 다음을 수행하십시오.
    </para>
    <step performance="required">
     <para>
      <xref linkend="sec.deployment.remoteinst.instserver"/>에서 설명한 대로 리포지토리를 설정합니다. NFS, HTTP 또는 FTP 네트워크 서버를 선택하거나 <xref linkend="sec.deployment.remoteinst.instserver.smb"/>에서 설명한 대로 SMB 리포지토리를 구성합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      대상 시스템에서 끌어올 수 있는 부팅 이미지를 보관하도록 TFTP 서버를 설정합니다. 방법은 <xref linkend="sec.deployment.remoteinst.boot.tftp"/>에 기술되어 있습니다.
     </para>
    </step>
    <step performance="required">
     <para>
      모든 시스템에 IP 주소를 제공하도록 DHCP 서버를 설정하고 대상 시스템에 TFTP 서버의 위치를 공개합니다. 방법은 <xref linkend="sec.deployment.remoteinst.boot.dhcp"/>에 기술되어 있습니다.
     </para>
    </step>
    <step performance="required">
     <para>
      PXE 부팅을 위한 대상 시스템을 준비합니다. 자세한 내용은 <xref linkend="sec.deployment.remoteinst.boot.pxeprep"/>에서 설명됩니다.
     </para>
    </step>
    <step performance="required">
     <para>
      WOL(Wake On Lan)을 사용하여 대상 시스템의 부팅 프로세스를 시작합니다. 방법은 <xref linkend="sec.deployment.remoteinst.boot.wol"/>에 기술되어 있습니다.
     </para>
    </step>
    <step performance="required">
     <para>
      제어 워크스테이션에서 VNC 보기 응용 프로그램 또는 웹 브라우저를 열고 <xref linkend="sec.deployment.remoteinst.monitor.vnc"/>에서 설명한 대로 대상 시스템에 연결합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <xref linkend="cha.inst"/>에서 설명한 대로 설치를 수행합니다. 설치의 마지막 부분에서 재부팅한 후에 대상 시스템에 다시 연결합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      설치를 완료합니다.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.sshstat">
   <title>SSH를 통한 단순 원격 설치 - 정적 네트워크 구성</title>
   <para>
    설치를 위해 부팅하고 설치 대상의 IP 주소를 결정하기 위해서는 이 설치 유형에 대상 시스템에 대한 물리적 액세스가 어느 정도 필요합니다. 설치 자체는 설치 프로그램에 연결할 SSH를 사용하여 원격 워크스테이션에서 전적으로 제어되지만, 사용자 개입은 <xref linkend="cha.inst"/>의 일반 설치에서와 같이 필요합니다.
   </para>
   <para>
    이 설치 유형에서 다음과 같은 요구사항이 충족되는지 확인합니다.
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      네트워크 연결이 작동 중인 원격 리포지토리(NFS, HTTP, FTP 또는 SMB)
     </para>
    </listitem>
    <listitem>
     <para>
      네트워크 연결 중인 대상 시스템
     </para>
    </listitem>
    <listitem>
     <para>
      네트워크 연결 및 SSH 클라이언트 소프트웨어 작업 중인 제어 시스템
     </para>
    </listitem>
    <listitem>
     <para>
      <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 미디어 키트의 DVD1을 사용하여 대상 시스템을 부팅합니다.
     </para>
    </listitem>
    <listitem>
     <para>
      리포지토리와 제어 시스템에 이미 지정된 유효한 고정 IP 주소
     </para>
    </listitem>
    <listitem>
     <para>
      대상 시스템에 지정할 유효한 정적 IP 주소
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.sshstat">
    <para>
     이 설치 유형을 실행하려면 다음을 수행하십시오.
    </para>
    <step performance="required">
     <para>
      <xref linkend="sec.deployment.remoteinst.instserver"/>에서 설명한 대로 리포지토리를 설정합니다. NFS, HTTP 또는 FTP 네트워크 서버를 선택합니다. SMB 리포지토리의 경우 <xref linkend="sec.deployment.remoteinst.instserver.smb"/>를 참조하십시오.
     </para>
    </step>
    <step performance="required">
     <para>
      <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 미디어 키트의 DVD1을 사용하여 대상 시스템을 부팅합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      대상 시스템의 부팅 화면이 나타나면 부팅 옵션 프롬프트를 사용하여 네트워크 연결에 적절한 파라미터, 리포지토리의 주소 및 SSH 실행을 설정합니다. 자세한 내용은 <xref linkend="sec.deployment.remoteinst.bootinst.custom"/>에서 설명됩니다.
     </para>
     <para>
      대상 시스템은 텍스트 기반 환경에서 부팅합니다. 이 환경은 SSH 클라이언트에서 사용할 수 있는 그래픽 설치 환경 하에서 네트워크 주소를 제공합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      제어 워크스테이션에서 터미널 창을 열고 <xref linkend="sec.deployment.remoteinst.monitor.ssh.connect"/>에서 설명한 대로 대상 시스템에 연결합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <xref linkend="cha.inst"/>에서 설명한 대로 설치를 수행합니다. 설치의 마지막 부분에서 재부팅한 후에 대상 시스템에 다시 연결합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      설치를 완료합니다.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.sshdyn">
   <title>SSH를 통한 단순 원격 설치 - 동적 네트워크 구성</title>
   <para>
    설치를 위해 부팅하고 설치 대상의 IP 주소를 결정하기 위해서는 이 설치 유형에 대상 시스템에 대한 물리적 액세스가 어느 정도 필요합니다. 설치 자체는 설치 프로그램에 연결할 SSH를 사용하여 원격 워크스테이션에서 완전히 제어되지만, 실제 구성 작업에는 사용자의 조작이 필요합니다.
   </para>
   <note>
    <title>2단계(설치) 후 연결 끊어짐 방지</title>
    <para>
     [네트워크 설정] 대화 상자에서 <guimenu>ifup를 사용한 일반 방법</guimenu>을 선택하고 NetworkManager를 사용하지 마십시오. 그렇지 않을 경우 설치 중에 SSH 연결이 끊어집니다. 설치가 완료된 후 <guimenu>사용자가 NetworkManager를 사용하여 제어</guimenu>로 설정을 재설정합니다.
    </para>
   </note>
   <para>
    이 설치 유형에서 다음과 같은 요구사항이 충족되는지 확인합니다.
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      네트워크 연결이 작동 중인 원격 리포지토리(NFS, HTTP, FTP 또는 SMB)
     </para>
    </listitem>
    <listitem>
     <para>
      네트워크 연결 중인 대상 시스템
     </para>
    </listitem>
    <listitem>
     <para>
      네트워크 연결 및 SSH 클라이언트 소프트웨어 작업 중인 제어 시스템
     </para>
    </listitem>
    <listitem>
     <para>
      대상 시스템을 부팅할 실제 부팅 미디어(CD, DVD 또는 USB 플래시 드라이브)
     </para>
    </listitem>
    <listitem>
     <para>
      IP 주소를 제공하는 실행 중인 DHCP 서버
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.sshdyn">
    <para>
     이 설치 유형을 실행하려면 다음을 수행하십시오.
    </para>
    <step performance="required">
     <para>
      <xref linkend="sec.deployment.remoteinst.instserver"/>에서 설명한 대로 리포지토리 원본을 설정합니다. NFS, HTTP 또는 FTP 네트워크 서버를 선택합니다. SMB 리포지토리의 경우 <xref linkend="sec.deployment.remoteinst.instserver.smb"/>를 참조하십시오.
     </para>
    </step>
    <step performance="required">
     <para>
      <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 미디어 키트의 DVD1을 사용하여 대상 시스템을 부팅합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      대상 시스템의 부팅 화면이 나타나면 부팅 옵션 프롬프트를 사용하여 네트워크 연결에 적절한 파라미터, 설치 원본의 위치 및 SSH 실행을 전달합니다. 이 파라미터의 사용에 대한 자세한 내용은 <xref linkend="sec.deployment.remoteinst.bootinst.custom"/>을 참조하십시오.
     </para>
     <para>
      대상 시스템은 텍스트 기반 환경에서 부팅합니다. 이 환경은 SSH 클라이언트에서 사용할 수 있는 그래픽 설치 환경 하에서 네트워크 주소를 제공합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      제어 워크스테이션에서 터미널 창을 열고 <xref linkend="sec.deployment.remoteinst.monitor.ssh.connect"/>에서 설명한 대로 대상 시스템에 연결합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <xref linkend="cha.inst"/>에서 설명한 대로 설치를 수행합니다. 설치의 마지막 부분에서 재부팅한 후에 대상 시스템에 다시 연결합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      설치를 완료합니다.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.scenario.sshpxe">
   <title>SSH를 통한 원격 설치 - PXE 부팅 및 WOL(Wake On LAN)</title>
   <para>
    이 설치 유형은 완전 자동입니다. 대상 시스템은 원격으로 시작되고 부팅됩니다.
   </para>
   <para>
    이 설치 유형을 수행하려면 다음과 같은 요구사항이 충족되는지 확인합니다.
   </para>
   <itemizedlist mark="bullet" spacing="normal">
    <listitem>
     <para>
      네트워크 연결이 작동 중인 원격 리포지토리(NFS, HTTP, FTP 또는 SMB)
     </para>
    </listitem>
    <listitem>
     <para>
      TFTP 서버
     </para>
    </listitem>
    <listitem>
     <para>
      네트워크에서 실행 중인 DHCP 서버(설치할 호스트에 정적 IP 제공)
     </para>
    </listitem>
    <listitem>
     <para>
      PXE 부팅, 네트워킹 및 WOL(Wake On Lan)을 네트워크에 연결할 수 있는 대상 시스템
     </para>
    </listitem>
    <listitem>
     <para>
      네트워크 연결 및 SSH 클라이언트 소프트웨어 작업 중인 제어 시스템
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.deployment.remoteinst.scenario.sshpxe">
    <para>
     이 설치 유형을 실행하려면 다음을 수행하십시오.
    </para>
    <step performance="required">
     <para>
      <xref linkend="sec.deployment.remoteinst.instserver"/>에서 설명한 대로 리포지토리를 설정합니다. NFS, HTTP 또는 FTP 네트워크 서버를 선택합니다. SMB 리포지토리의 구성은 <xref linkend="sec.deployment.remoteinst.instserver.smb"/>를 참조하십시오.
     </para>
    </step>
    <step performance="required">
     <para>
      대상 시스템에서 끌어올 수 있는 부팅 이미지를 보관하도록 TFTP 서버를 설정합니다. 방법은 <xref linkend="sec.deployment.remoteinst.boot.tftp"/>에 기술되어 있습니다.
     </para>
    </step>
    <step performance="required">
     <para>
      모든 시스템에 IP 주소를 제공하도록 DHCP 서버를 설정하고 대상 시스템에 TFTP 서버의 위치를 공개합니다. 방법은 <xref linkend="sec.deployment.remoteinst.boot.dhcp"/>에 기술되어 있습니다.
     </para>
    </step>
    <step performance="required">
     <para>
      PXE 부팅을 위한 대상 시스템을 준비합니다. 자세한 내용은 <xref linkend="sec.deployment.remoteinst.boot.pxeprep"/>에서 설명됩니다.
     </para>
    </step>
    <step performance="required">
     <para>
      WOL(Wake On Lan)을 사용하여 대상 시스템의 부팅 프로세스를 시작합니다. 방법은 <xref linkend="sec.deployment.remoteinst.boot.wol"/>에 기술되어 있습니다.
     </para>
    </step>
    <step performance="required">
     <para>
      제어 워크스테이션에서 SSH 클라이언트를 시작하고 <xref linkend="sec.deployment.remoteinst.monitor.ssh"/>에서 설명한 대로 대상 시스템에 연결합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <xref linkend="cha.inst"/>에서 설명한 대로 설치를 수행합니다. 설치의 마지막 부분에서 재부팅한 후에 대상 시스템에 다시 연결합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      설치를 완료합니다.
     </para>
    </step>
   </procedure>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.instserver">
  <title>설치 원본을 보유하는 서버 설정</title>

  <para>
   <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>의 네트워크 설치 원본으로 사용할 시스템에서 실행 중인 운영 체제에 따라 서버 구성에 필요한 몇 가지 옵션이 있습니다. 설치 서버를 설정하는 가장 쉬운 방법은 SUSE Linux Enterprise Server 11/opensuse; 11.1 이상에서 YaST를 사용하는 것입니다.
  </para>

  <tip>
   <para>
    Linux 배포를 위해 설치 서버로 Microsoft Windows 시스템을 사용할 수도 있습니다. 자세한 내용은 <xref linkend="sec.deployment.remoteinst.instserver.smb"/>를 참조하십시오.
   </para>
  </tip>

  <sect2 id="sec.deployment.remoteinst.instserver.sles9">
   <title>YaST를 사용하여 설치 서버 설정</title>
   <para>
    YaST는 네트워크 리포지토리를 생성하기 위한 그래픽 도구를 제공하며, HTTP, FTP 및 NFS 네트워크 설치 서버를 지원합니다.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.sles9">
    <step performance="required">
     <para>
      설치 서버로 작동할 시스템에 <systemitem class="username">root</systemitem>로 로그인합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <menuchoice> <guimenu>YaST</guimenu> <guimenu>기타</guimenu> <guimenu>설치 서버</guimenu></menuchoice>를 시작합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      리포지토리 유형(HTTP, FTP 또는 NFS)을 선택합니다. 선택한 서비스가 시스템이 시작될 때마다 자동으로 시작됩니다. 선택한 유형의 서비스가 시스템에서 이미 실행 중이고 이 서비스를 서버에 수동으로 구성하려면 <guimenu>네트워크 서비스를 구성하지 않음</guimenu>을 사용하여 서버 서비스의 자동 구성을 비활성화합니다. 두 경우 모두, 서버에서 설치 데이터를 사용 가능하도록 설정할 디렉토리를 정의합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      필요한 리포지토리 유형을 구성합니다. 이 단계는 서버 서비스의 자동 구성과 관련되며 자동 구성이 비활성화되면 건너뜁니다.
     </para>
     <para>

      설치 데이터를 검색할 FTP 또는 HTTP 서버의 루트 디렉토리 별칭을 정의합니다. 리포지토리는 나중에 <literal>ftp://<replaceable>Server-IP</replaceable>/<replaceable>Alias</replaceable>/<replaceable>Name</replaceable></literal>(FTP) 또는 <literal>http://<replaceable>Server-IP</replaceable>/<replaceable>Alias</replaceable>/<replaceable>Name</replaceable></literal>(HTTP)에 배치됩니다. <replaceable>Name</replaceable>은 다음 단계에서 정의된 리포지토리의 이름을 나타냅니다. 이전 단계에서 NFS를 선택한 경우 와일드카드와 엑스포트 옵션을 정의합니다. NFS 서버는 <literal>nfs://<replaceable>Server-IP</replaceable>/<replaceable>Name</replaceable></literal>에서 액세스할 수 있습니다. NFS 및 엑스포트에 대한 자세한 내용은 <xref linkend="cha.nfs"/>에서 확인할 수 있습니다.
     </para>
     <tip>
      <title>방화벽 설정</title>
      <para>
       서버 시스템의 방화벽 설정이 HTTP, NFS 및 FTP에 대한 포트에서 트래픽을 허용하는지 확인합니다. 방화벽 설정이 현재 트래픽을 허용하지 않으면 <guimenu>방화벽에서 포트 열기</guimenu>를 선택하거나 먼저 <guimenu>방화벽 세부 사항</guimenu>을 확인하십시오.
      </para>
     </tip>
    </step>
    <step performance="required">
     <para>
      리포지토리를 구성합니다. 설치 미디어가 대상에 복사되기 전에 리포지토리의 이름(기억하기 쉬운 제품 및 버전의 약어 등)을 정의합니다. YaST는 설치 DVD의 복사본 대신 미디어의 ISO 이미지 제공을 허용합니다. 그러려면, 관련 체크 박스를 활성화하고 ISO 파일을 로컬로 검색할 수 있는 디렉토리 경로를 지정합니다. 이 설치 서버를 사용하여 배포할 제품에 따라 추가 기능 CD 또는 서비스 팩 CD가 더 많이 필요할 수 있으며 해당 CD를 추가 리포지토리로 추가해야 합니다. OpenSLP를 통해 네트워크에서 설치 서버를 알리려면 적합한 옵션을 활성화합니다.
     </para>
     <tip>
      <para>
       네트워크 설정이 이 옵션을 지원하는 경우 OpenSLP를 통해 리포지토리를 알리도록 고려해 보십시오. 그러면 대상 시스템마다 네트워크 설치 경로를 입력하지 않아도 됩니다. 대상 시스템은 SLP 부팅 옵션을 사용하여 부팅되고 추가 구성 없이도 네트워크 리포지토리를 검색합니다. 이 옵션에 대한 자세한 내용은 <xref linkend="sec.deployment.remoteinst.bootinst"/>을 참조하십시오.
      </para>
     </tip>
    </step>
    <step performance="required">
     <para>
      설치 데이터를 업로드합니다. 설치 서버 구성 시 가장 시간이 오래 걸리는 단계는 실제 설치 미디어를 복사하는 것입니다. YaST에서 요청한 미디어를 순서대로 삽입하고 복사 절차가 끝날 때까지 기다립니다. 원본이 완전히 복사되면 기존 리포지토리의 개요로 돌아가서 <guimenu>완료</guimenu>를 선택하여 구성을 닫습니다.
     </para>
     <para>
      이제 설치 서버가 완전히 구성되고 서비스를 제공할 준비가 됩니다. 설치 서버는 시스템이 시작될 때마다 자동으로 시작되며 추가 개입은 필요하지 않습니다. 초기 단계로 YaST를 사용하여 선택한 네트워크 서비스의 자동 구성을 비활성화한 경우 이 서비스를 직접 올바르게 구성하고 시작하기만 하면 됩니다.
     </para>
    </step>
   </procedure>
   <para>
    리포지토리를 비활성화하려면 제거할 리포지토리를 선택한 다음 <guimenu>삭제</guimenu>를 선택합니다. 설치 데이터가 시스템에서 제거됩니다. 네트워크 서비스를 비활성화하려면 각 YaST 모듈을 사용합니다.
   </para>
   <para>
    설치 서버에서 제품 버전이 둘 이상인 제품에 대해 설치 데이터를 제공해야 할 경우 YaST 설치 서버 모듈을 시작하고 기존 리포지토리 개요에서 <guimenu>추가</guimenu>를 선택하여 새 리포지토리를 구성합니다.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.nfs">
   <title>수동으로 NFS 리포지토리 설정</title>
   
   <para>
    설치할 NFS 원본 설정은 기본적으로 2단계로 수행됩니다. 1단계는 설치 데이터를 보관하고 있는 디렉토리 구조를 생성하여 설치 미디어를 이 구조에 복사하는 것입니다. 2단계는 설치 데이터를 보관하고 있는 디렉토리를 네트워크로 엑스포트하는 것입니다.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.cdir">
    <para>
     설치 데이터를 보관할 디렉토리를 생성하려면 다음 작업을 수행하십시오.
    </para>
    <step performance="required">
     <para>
      <systemitem class="username">root</systemitem>로 로그인합니다. 
     </para>
    </step>
    <step performance="required">
     <para>
      나중에 모든 설치 데이터를 보관해야 할 디렉토리를 생성하고 이 디렉토리로 변경합니다. 예:
     </para>
<screen>mkdir install/<replaceable>product</replaceable>/<replaceable>productversion</replaceable>
cd install/<replaceable>product</replaceable>/<replaceable>productversion</replaceable></screen>
     <para>
      <replaceable>product</replaceable>를 제품 이름의 약어로 바꾸고 <replaceable>productversion</replaceable>을 제품 이름과 버전을 포함하는 스트링으로 바꿉니다.
     </para>
    </step>
    <step performance="required">
     <para>
      미디어 키트에 포함된 각 DVD의 경우 다음 명령을 실행합니다.
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        설치 DVD의 전체 내용을 설치 서버 디렉토리로 복사합니다.
       </para>
<screen>cp -a /media/<replaceable>path_to_your_DVD_drive .</replaceable></screen>
       <para>
        <replaceable>path_to_your_DVD_drive</replaceable>를 DVD 드라이브가 지정된 실제 경로로 바꿉니다. 이 경로는 시스템에서 사용된 드라이브 유형에 따라 <filename>cdrom</filename>, <filename>cdrecorder</filename>, <filename>dvd</filename> 또는 <filename>dvdrecorder</filename>가 될 수 있습니다.
       </para>
      </step>
      <step performance="required">
       <para>
        DVD 번호에 대해 디렉토리의 이름을 바꿉니다.
       </para>
<screen>mv <replaceable>path_to_your_DVD_drive</replaceable> DVD<replaceable>x</replaceable></screen>
       <para>
        <replaceable>x</replaceable>를 DVD의 실제 번호로 바꿉니다.
       </para>
      </step>
     </substeps>
    </step>
   </procedure>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.export">
    <para>
     <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>에서 YaST를 사용하여 리포지토리를 NFS로 엑스포트할 수 있습니다. 다음 단계를 수행하십시오.
    </para>
    <step performance="required">
     <para>
      <systemitem class="username">root</systemitem>로 로그인합니다. 
     </para>
    </step>
    <step performance="required">
     <para>
      <menuchoice> <guimenu>YaST</guimenu> <guimenu>네트워크 서비스</guimenu> <guimenu>NFS 서버</guimenu></menuchoice>를 시작합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <guimenu>시작</guimenu> 및 <guimenu>방화벽 포트 열기</guimenu>를 선택하고 <guimenu>다음</guimenu>을 클릭합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <guimenu>디렉토리 추가</guimenu>를 선택하고 설치 원본을 포함하는 디렉토리(이 경우는 <filename><replaceable>productversion</replaceable></filename>)를 찾아봅니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <guimenu>호스트 추가</guimenu>를 선택하고 설치 데이터를 엑스포트할 시스템의 호스트 이름을 입력합니다. 여기에 호스트 이름을 지정하는 대신 와일드카드, 네트워크 주소 범위 또는 네트워크의 도메인 이름을 사용할 수도 있습니다. 적절한 엑스포트 옵션을 입력하거나 기본값(대부분의 설정에서 정상 작동)으로 그대로 둡니다. NFS 공유 엑스포트에 사용된 구문에 대한 자세한 내용은 <systemitem>엑스포트</systemitem> 맨 페이지를 읽어 보십시오.
     </para>
    </step>
    <step performance="required">
     <para>
      <guimenu>완료</guimenu>를 클릭합니다. <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 리포지토리를 보관하고 있는 NFS 서버가 자동으로 시작되고 부팅 프로세스로 통합됩니다.
     </para>
    </step>
   </procedure>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.exportman">
    <para>
     YaST NFS 서버 모듈을 사용하는 대신 NFS를 통해 리포지토리를 수동으로 엑스포트하려면 다음 작업을 수행하십시오.
    </para>
    <step performance="required">
     <para>
      <systemitem class="username">root</systemitem>로 로그인합니다. 
     </para>
    </step>
    <step performance="required">
     <para>
      <filename>/etc/exports</filename> 파일을 열고 다음 행을 입력합니다.
     </para>
<screen>/<replaceable>productversion</replaceable> *(ro,root_squash,sync)</screen>
     <para>
      <filename>/<replaceable>productversion</replaceable></filename> 디렉토리가 이 네트워크의 일부인 호스트 또는 이 서버에 연결할 수 있는 호스트로 엑스포트됩니다. 이 서버에 대한 액세스를 제한하려면 일반 와일드카드 <literal>*</literal> 대신 넷마스크 또는 도메인 이름을 사용합니다. 자세한 내용은 <systemitem>엑스포트</systemitem> 맨 페이지를 참조하십시오. 이 구성 파일을 저장하고 종료합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      시스템 부팅 동안 시작된 서버 목록에 NFS 서비스를 추가하려면 다음 명령을 실행합니다.
     </para>
<screen>systemctl enable nfsserver.service</screen>
    </step>
    <step performance="required">
     <para>
      <command>systemctl start nfsserver.service</command>로 NFS 서버를 시작합니다. 나중에 NFS 서버의 구성을 변경해야 할 경우 구성 파일을 수정하고 <command>systemctl restart nfsserver.service</command>로 NFS 데몬을 다시 시작합니다.
     </para>
    </step>
   </procedure>
   <procedure id="proc.deployment.remoteinst.instserver.nfs.slpann">
    <para>
     OpenSLP를 통해 NFS 서버를 알리면 해당 주소가 네트워크의 모든 클라이언트에 알려집니다.
    </para>
    <step performance="required">
     <para>
      <systemitem class="username">root</systemitem>로 로그인합니다. 
     </para>
    </step>
    <step performance="required">
     <para>
      다음 행을 사용하여 <filename>/etc/slp.reg.d/install.suse.nfs.reg</filename> 구성 파일을 생성합니다.
     </para>
<screen>
# Register the NFS Installation Server
service:install.suse:nfs://$HOSTNAME/<replaceable>path_to_repository</replaceable>/DVD1,en,65535 
description=NFS Repository</screen>
     <para>
      서버에서 <replaceable>path_to_repository</replaceable>를 설치 원본의 실제 경로로 바꿉니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <command>systemctl start slpd.service</command>로 OpenSLP 데몬을 시작합니다.
     </para>
    </step>
   </procedure>
   <para os="sles">
    OpenSLP에 대한 자세한 내용은 <filename>/usr/share/doc/packages/openslp/</filename>의 패키지 설명서를 참조하거나 <xref linkend="cha.slp"/>를 참조하십시오. NFS에 대한 자세한 내용은 <xref linkend="cha.nfs"/>를 참조하십시오.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.ftp">
   <title>수동으로 FTP 리포지토리 설정</title>
   <para>
    FTP 리포지토리를 생성하는 것은 NFS 리포지토리를 생성하는 것과 매우 유사합니다. FTP 리포지토리는 OpenSLP를 사용하여 네트워크를 통해서도 알릴 수 있습니다.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.ftp">
    <step performance="required">
     <para>
      <xref linkend="sec.deployment.remoteinst.instserver.nfs"/>에서 설명한 대로 설치 원본을 보관하고 있는 디렉토리를 생성합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      설치 디렉토리의 컨텐트를 배포할 FTP 서버를 구성합니다.
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        <systemitem class="username">root</systemitem>로 로그인하고 YaST 소프트웨어 관리를 사용하여 패키지 <systemitem>vsftpd</systemitem>를 설치합니다.
       </para>
      </step>
      <step performance="required">
       <para>
        FTP 서버 루트 디렉토리를 입력합니다.
       </para>
<screen>cd <option>/srv/ftp</option></screen>
      </step>
      <step performance="required">
       <para>
        FTP 루트 디렉토리에서 설치 원본을 보관하고 있는 하위 디렉토리를 생성합니다.
       </para>
<screen>mkdir <replaceable>repository</replaceable></screen>
       <para>
        <replaceable>repository</replaceable>를 제품 이름으로 바꿉니다.
       </para>
      </step>
      <step performance="required">
       <para>
        설치 리포지토리의 컨텐트를 FTP 서버의 변경 루트 환경으로 탑재합니다.
       </para>
<screen>mount --bind <replaceable>path_to_repository</replaceable> /srv/ftp/<replaceable>repository</replaceable></screen>
       <para>
        <replaceable>path_to_repository</replaceable> 및 <replaceable>repository</replaceable>를 설정과 일치하는 값으로 바꿉니다. 이를 영구적으로 설정해야 할 경우 <filename>/etc/fstab</filename>에 추가합니다.
       </para>
      </step>
      <step performance="required">
       <para>
        <command>vsftpd</command>로 vsftpd를 시작합니다.
       </para>
      </step>
     </substeps>
    </step>
    <step performance="required">
     <para>
      리포지토리가 네트워크 설정에서 지원되는 경우 OpenSLP를 통해 알립니다.
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        다음 행을 사용하여 <filename>/etc/slp.reg.d/install.suse.ftp.reg</filename> 구성 파일을 생성합니다.
       </para>
<screen>
# Register the FTP Installation Server
service:install.suse:ftp://$HOSTNAME/<replaceable>repository</replaceable>/DVD1,en,65535 
description=FTP Repository</screen>
       <para>
        서버에서 <replaceable>repository</replaceable>를 리포지토리 디렉토리의 실제 이름으로 바꿉니다. <literal>service:</literal> 행을 하나의 연속 행으로 입력해야 합니다.
       </para>
      </step>
      <step performance="required">
       <para>
        <command>systemctl start slpd.service</command>로 OpenSLP 데몬을 시작합니다.
       </para>
      </step>
     </substeps>
    </step>
   </procedure>
   <tip os="sles">
    <title>YaST를 사용하여 FTP 서버 구성</title>
    <para>
     FTP 설치 서버의 수동 구성에 대해 YaST를 사용할 경우 YaST FTP 서버 모듈을 사용하는 방법에 대한 자세한 내용은 <xref linkend="cha.ftp"/>을 참조하십시오.
    </para>
   </tip>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.http">
   <title>수동으로 HTTP 리포지토리 설정</title>
   <para>
    HTTP 리포지토리를 생성하는 것은 NFS 리포지토리를 생성하는 것과 매우 유사합니다. HTTP 리포지토리는 OpenSLP를 사용하여 네트워크를 통해서도 알릴 수 있습니다.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.http">
    <step performance="required">
     <para>
      <xref linkend="sec.deployment.remoteinst.instserver.nfs"/>에서 설명한 대로 설치 원본을 보관하고 있는 디렉토리를 생성합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      설치 디렉토리의 컨텐트를 배포할 HTTP 서버를 구성합니다.
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        <phrase os="sles"><xref linkend="sec.apache2.quickstart.installation"/></phrase>에서 설명한 대로 웹 서버 Apache를 설치합니다.
       </para>
      </step>
      <step performance="required">
       <para>
        HTTP 서버의 루트 디렉토리(<filename>/srv/www/htdocs</filename>)를 입력하고 설치 원본을 보관할 하위 디렉토리를 생성합니다.
       </para>
<screen>mkdir <replaceable>repository</replaceable></screen>
       <para>
        <replaceable>repository</replaceable>를 제품 이름으로 바꿉니다.
       </para>
      </step>
      <step performance="required">
       <para>
        설치 원본의 위치에서 웹 서버의 루트 디렉토리(<filename>/srv/www/htdocs</filename>)로 심볼 링크를 생성합니다.
       </para>
<screen>ln -s <replaceable>/path_to_repository </replaceable>/srv/www/htdocs/<replaceable>repository</replaceable></screen>
      </step>
      <step performance="required">
       <para>
        HTTP 서버의 구성 파일(<filename>/etc/apache2/default-server.conf</filename>)이 심볼 링크를 따르도록 수정합니다. 다음 줄을 바꿉니다.
       </para>
<screen>Options None</screen>
       <para>
        사용
       </para>
<screen>Options Indexes FollowSymLinks</screen>
      </step>
      <step performance="required">
       <para>
        <command>systemctl reload apache2.service</command>를 사용하여 HTTP 서버 구성을 다시 로드합니다.
       </para>
      </step>
     </substeps>
    </step>
    <step performance="required">
     <para>
      리포지토리가 네트워크 설정에서 지원되는 경우 OpenSLP를 통해 알립니다.
     </para>
     <substeps performance="required">
      <step performance="required">
       <para>
        다음 행을 사용하여 <filename>/etc/slp.reg.d/install.suse.http.reg</filename> 구성 파일을 생성합니다.
       </para>
<screen>
# Register the HTTP Installation Server
service:install.suse:http://$HOSTNAME/<replaceable>repository</replaceable>/DVD1/,en,65535 
description=HTTP Repository</screen>
       <para>
        서버에서 <replaceable>repository</replaceable>를 리포지토리의 실제 경로로 바꿉니다. <literal>service:</literal> 행을 하나의 연속 행으로 입력해야 합니다.
       </para>
      </step>
      <step performance="required">
       <para>
        <command>systemctl start slpd.service</command>를 사용하여 OpenSLP 데몬을 시작합니다.
       </para>
      </step>
     </substeps>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.instserver.smb">
   <title>SMB 리포지토리 관리</title>
   <para>
    SMB를 사용하여 Microsoft Windows 서버에서 설치 원본을 임포트하고 Linux 시스템이 주위에 없는 경우에도 Linux 배포를 시작할 수 있습니다.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.smb.export">
    <para>
     <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 리포지토리를 보관하고 있는 엑스포트된 Windows 공유를 설정하려면 다음 작업을 수행하십시오.
    </para>
    <step performance="required">
     <para>
      Windows 시스템에 로그인합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      전체 설치 트리를 보관할 새 디렉토리를 생성한 후 이름을 <filename>INSTALL</filename> 등으로 지정합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      Windows 설명서에서 개략적으로 설명하는 절차에 따라 이 공유를 엑스포트합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      이 공유를 입력하고 <filename><replaceable>product</replaceable></filename>라는 하위 디렉토리를 생성합니다. <replaceable>product</replaceable>를 실제 제품 이름으로 바꿉니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <filename>INSTALL/<replaceable>product</replaceable></filename> 디렉토리를 입력하고 각 DVD를 다른 폴더(예: <literal>DVD1</literal> 및 <literal>DVD2</literal>)에 복사합니다.
     </para>
    </step>
   </procedure>
   <para>
    SMB 탑재 공유를 리포지토리로 사용하려면 다음 작업을 수행하십시오.
   </para>
   <procedure id="proc.deployment.remoteinst.instserver.smb.use">
    <step performance="required">
     <para>
      설치 대상을 부팅합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <guimenu>설치</guimenu>를 선택합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      리포지토리를 선택하려면 <keycap>F4</keycap> 키를 누릅니다.
     </para>
    </step>
    <step performance="required">
     <para>
      SMB를 선택하고 Windows 시스템의 이름 또는 IP 주소, 공유 이름(이 예에서는 <filename>INSTALL/<replaceable>product</replaceable>/DVD1</filename>), 사용자 이름 및 비밀번호를 입력합니다. 구문은 다음과 같습니다.
     </para>
<screen>smb://workdomain;user:password@server/INSTALL/DVD1</screen>
     <para>
      <keycap function="enter">Enter</keycap> 키를 누르면 YaST가 시작되어 설치를 수행할 수 있습니다.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.iso">
   <title>서버에서 설치 미디어의 ISO 이미지 사용</title>
   <para>
    실제 미디어를 서버 디렉토리에 수동으로 복사하는 대신, 설치 미디어의 ISO 이미지를 설치 서버에 탑재하고 해당 이미지를 리포지토리로 사용할 수도 있습니다. 미디어 복사본 대신 ISO 이미지를 사용하는 HTTP, NFS 또는 FTP 서버를 설정하려면 다음을 수행하십시오.
   </para>
   <procedure>
    <step performance="required">
     <para>
      ISO 이미지를 다운로드하여 설치 서버로 사용할 시스템에 저장합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <systemitem class="username">root</systemitem>로 로그인합니다. 
     </para>
    </step>
    <step performance="required">
     <para>
      <xref linkend="sec.deployment.remoteinst.instserver.nfs"/>, <xref linkend="sec.deployment.remoteinst.instserver.ftp"/> 또는 <xref linkend="sec.deployment.remoteinst.instserver.http"/>에서 설명한 대로 설치 데이터의 적절한 위치를 선택하고 생성합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      각 DVD의 하위 디렉토리를 생성합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      각 ISO 이미지를 최종 위치에 탑재하고 압축을 풀려면 다음 명령을 실행합니다.
     </para>
<screen>mount -o loop <replaceable>path_to_iso</replaceable><replaceable>path_to_repository</replaceable>/<replaceable>product</replaceable>/<replaceable>mediumx</replaceable></screen>
     <para>
      <replaceable>path_to_iso</replaceable>를 ISO 이미지의 로컬 복사본 경로로, <replaceable>path_to_repository</replaceable>를 서버의 원본 디렉토리로, <replaceable>product</replaceable>를 제품 이름으로, <replaceable>mediumx</replaceable>를 사용 중인 미디어 유형(CD 또는 DVD) 및 개수로 바꿉니다.
     </para>
    </step>
    <step performance="required">
     <para>
      제품에 필요한 모든 ISO 이미지를 탑재하려면 이전 단계를 반복합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <xref linkend="sec.deployment.remoteinst.instserver.nfs"/>, <xref linkend="sec.deployment.remoteinst.instserver.ftp"/> 또는 <xref linkend="sec.deployment.remoteinst.instserver.http"/>에서 설명한 대로 설치 서버를 원래대로 시작합니다.
     </para>
    </step>
   </procedure>
   <para>
    부팅 시 ISO 이미지를 자동으로 탑재하려면 <filename>/etc/fstab</filename>에 각 탑재 항목을 추가합니다. 이전 예제에 따른 항목은 다음과 같이 나타납니다.
   </para>
<screen><replaceable>path_to_iso</replaceable> <replaceable>path_to_repository</replaceable>/<replaceable>product</replaceable><replaceable>medium</replaceable> auto loop</screen>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.boot">
  <title>대상 시스템의 부팅 준비</title>

  <para>
   이 절에서는 복잡한 부팅 시나리오에 필요한 구성 작업에 대해 다룹니다. 여기에는 DHCP, PXE 부팅, TFTP 및 WOL(Wake On Lan)에 즉시 적용 가능한 구성 예가 포함됩니다.
  </para>

  <sect2 id="sec.deployment.remoteinst.boot.dhcp">
   <title>DHCP 서버 설정</title>
   <para>
    DHCP 서버의 설정에는 두 가지 방법이 있습니다. <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>의 경우 YaST는 프로세스에 대한 그래픽 인터페이스를 제공합니다. 사용자는 구성 파일을 수동으로 편집할 수도 있습니다.<phrase os="sles"> DHCP 서버에 대한 자세한 내용은 <xref linkend="cha.dhcp"/>도 참조하십시오.</phrase>
   </para>
   <sect3 id="sec.deployment.remoteinst.boot.dhcp.yast">
    <title>YaST를 사용하여 DHCP 서버 설정</title>
    <para>
     TFTP 서버의 위치를 네트워크 클라이언트에 알리고 설치 대상에서 사용할 부팅 이미지 파일을 지정하려면 두 개의 선언을 DHCP 서버 구성에 추가합니다.
    </para>
    <procedure id="proc.deployment.remoteinst.boot.dhcp.yast">
     <step performance="required">
      <para>
       DHCP 서버를 호스팅하는 시스템에 <systemitem class="username">root</systemitem>로 로그인합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       <menuchoice> <guimenu>YaST</guimenu> <guimenu>네트워크 서비스</guimenu> <guimenu>DHCP 서버</guimenu></menuchoice>를 시작합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       기본 DHCP 서버 설정에 대해 설정 마법사를 완료합니다.
      </para>
     </step>
     <step id="st.deployment.remoteinst.boot.dhcp.yast.expert" performance="required">
      <para>
       시작 대화 상자 종료에 대한 경고가 발생하면 <guimenu>고급 설정</guimenu>을 선택하고 <guimenu>예</guimenu>를 선택합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       <guimenu>구성된 선언</guimenu> 대화 상자에서 새 시스템이 배치될 서브넷을 선택하고 <guimenu>편집</guimenu>을 클릭합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       <guimenu>서브넷 구성</guimenu> 대화 상자에서 <guimenu>추가</guimenu>를 선택하여 서브넷의 구성에 새 옵션을 추가합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       <option>파일 이름</option>을 선택하고 값으로 <literal>pxelinux.0</literal>을 입력합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       또 다른 옵션(<option>next-server</option>)을 추가하고 해당 값을 TFTP 서버의 주소에 설정합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       <guimenu>확인</guimenu>과 <guimenu>완료</guimenu>를 선택하여 DHCP 서버 구성을 완료합니다.
      </para>
     </step>
    </procedure>
    <para>
     특정 호스트에 정적 IP 주소를 제공하도록 DHCP를 구성하려면 DHCP 서버 구성 모듈(<xref linkend="st.deployment.remoteinst.boot.dhcp.yast.expert" xrefstyle="StepOnPage"/>)의 <guimenu>고급 설정</guimenu>을 입력하고 호스트 유형의 새 선언을 추가합니다. <option>hardware</option> 및 <option>fixed-address</option> 옵션을 이 호스트 선언에 추가하고 적합한 값을 제공합니다.
    </para>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.boot.dhcp.man">
    <title>DHCP 서버를 수동으로 설정</title>
    <para>
     모든 DHCP 서버는 네트워크 클라이언트에 자동 주소 할당을 제공하는 것 외에도, 대상 시스템의 설치 루틴에서 가져와야 하는 TFTP 서버와 파일의 IP 주소를 알려야 합니다.
    </para>
    <procedure id="proc.deployment.remoteinst.boot.dhcp.man">
     <step performance="required">
      <para>
       DHCP 서버를 호스팅하는 시스템에 <systemitem class="username">root</systemitem>로 로그인합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       <filename>/etc/dhcpd.conf</filename>에 있는 DHCP 서버 구성 파일의 서브넷 구성에 다음 행을 추가합니다.
      </para>
<screen>subnet 192.168.1.0 netmask 255.255.255.0 { 
  range dynamic-bootp 192.168.1.200 192.168.1.228;
  # PXE related settings
  #
  # "next-server" defines the TFTP server that will be used
  next-server <replaceable>ip_tftp_server</replaceable>;
  #
  # "filename" specifies the pxelinux image on the TFTP server
  # the server runs in chroot under /srv/tftpboot
  filename  "pxelinux.0";
}</screen>
      <para>
       <replaceable>ip_of_the_tftp_server</replaceable>를 TFTP 서버의 실제 IP 주소로 바꿉니다. <filename>dhcpd.conf</filename>에서 사용할 수 있는 옵션에 대한 자세한 내용은 <systemitem>dhcpd.conf</systemitem> 매뉴얼 페이지를 참조하십시오.
      </para>
     </step>
     <step performance="required">
      <para>
       <command>systemctl restart dhcpd.service</command>를 실행하여 DHCP 서버를 다시 시작합니다.
      </para>
     </step>
    </procedure>
    <para>
     PXE 및 WOL(Wake On Lan) 설치의 원격 제어에 SSH를 사용하려는 경우 DHCP에서 설치 대상에 제공해야 하는 IP 주소를 명시적으로 지정합니다. 이렇게 하려면 다음 예에 따라 위에서 언급한 DHCP 구성을 수정합니다.
    </para>
<screen>group {
  # PXE related settings
  # 
  # "next-server" defines the TFTP server that will be used 
  next-server <replaceable>ip_tftp_server</replaceable>:
  # 
  # "filename" specifies the pxelinux image on the TFTP server 
  # the server runs in chroot under /srv/tftpboot
  filename "pxelinux.0";
  host test {
    hardware ethernet <replaceable>mac_address</replaceable>; 
    fixed-address <replaceable>some_ip_address</replaceable>;
    }
}
</screen>
    <para>
     Host 문은 설치 대상의 호스트 이름을 소개합니다. 특정 호스트에 호스트 이름과 IP 주소를 바인딩하려면 시스템의 하드웨어(MAC) 주소를 알고 이를 지정해야 합니다. 이 예에서 사용된 모든 변수를 사용자 환경과 일치하는 실제 값으로 바꿉니다.
    </para>
    <para>
     DHCP 서버를 다시 시작한 후에는 정적 IP가 지정된 호스트에 제공되어 SSH를 통해 시스템에 연결할 수 있습니다.
    </para>
   </sect3>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.tftp">
   <title>TFTP 서버 설정</title>
   <para>
    SUSE 기반 설치를 사용하는 경우 YaST를 사용하여 TFTP 서버를 설정할 수도 있습니다. 또는 수동으로 설정할 수도 있습니다. TFTP 서버는 부팅 후 요청을 전송하고 나면 부팅 이미지를 대상 시스템에 전달합니다.
   </para>
   <sect3 id="sec.deployment.remoteinst.boot.tftp.yast">
    <title>YaST를 사용하여 TFTP 서버 설정</title>
    <procedure id="proc.deployment.remoteinst.boot.tftp.yast">
     <step performance="required">
      <para>
       <systemitem class="username">root</systemitem>로 로그인합니다. 
      </para>
     </step>
     <step performance="required">
      <para>
       <menuchoice> <guimenu>YaST</guimenu> <guimenu>네트워크 서비스</guimenu> <guimenu>TFTP 서버</guimenu></menuchoice>를 시작하고 요청된 패키지를 설치합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       서버가 시작되었고 부팅 루틴에 포함되었는지 확인하려면 <guimenu>활성화</guimenu>를 클릭합니다. 사용자 측에서 이를 보호하기 위한 추가 작업은 필요하지 않습니다. xinetd는 부팅 시 tftpd를 시작합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       시스템에서 실행 중인 방화벽에서 적합한 포트를 열려면 <guimenu>방화벽 포트 열기</guimenu>를 클릭합니다. 서버에서 실행 중인 방화벽이 없으면 이 옵션은 사용할 수 없습니다.
      </para>
     </step>
     <step performance="required">
      <para>
       부팅 이미지 디렉토리를 찾아보려면 <guimenu>찾아보기</guimenu>를 클릭합니다. 기본 디렉토리 <filename>/tftpboot</filename>가 생성되고 자동으로 선택됩니다.
      </para>
     </step>
     <step performance="required">
      <para>
       <guimenu>완료</guimenu>를 클릭하여 설정을 적용하고 서버를 시작합니다.
      </para>
     </step>
    </procedure>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.boot.tftp.man">
    <title>TFTP 서버를 수동으로 설정</title>
    <procedure id="proc.deployment.remoteinst.boot.tftp.man">
     <step performance="required">
      <para>
       <systemitem class="username">root</systemitem>로 로그인하고 패키지 <systemitem>tftp</systemitem> 및 <systemitem>xinetd</systemitem>를 설치합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       이 패키지를 사용할 수 없는 경우 <filename>/srv/tftpboot</filename> 및 <filename>/srv/tftpboot/pxelinux.cfg</filename> 디렉토리를 생성합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       <xref linkend="sec.deployment.remoteinst.boot.pxe"/>에서 설명한 대로 부팅 이미지에 필요한 해당 파일을 추가합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       <filename>/etc/xinetd.d</filename>에 있는 xinetd의 구성을 수정하여 TFTP 서버가 부팅 시 시작되었는지 확인합니다.
      </para>
      <substeps performance="required">
       <step performance="required">
        <para>
         이 항목이 없는 경우 <command>touch tftp</command>가 포함된 이 디렉토리에 있는 <filename>tftp</filename>라는 파일을 생성합니다. 그런 다음 <command>chmod 755 tftp</command>를 실행합니다.
        </para>
       </step>
       <step performance="required">
        <para>
         파일 <filename>tftp</filename>를 열고 다음 행을 추가합니다.
        </para>
<screen>service tftp 
{ 
        socket_type            = dgram
        protocol               = udp 
        wait                   = yes 
        user                   = root 
        server                 = /usr/sbin/in.tftpd 
        server_args            = -s /srv/tftpboot 
        disable                = no 
}
</screen>
       </step>
       <step performance="required">
        <para>
         파일을 저장하고 <command>rcxinetd restart</command>로 xinetd를 다시 시작합니다.

        </para>
       </step>
      </substeps>
     </step>
    </procedure>
   </sect3>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.pxe">
   <title>PXE 부팅 사용</title>
   <para>
    PXE의 전체 사양을 비롯한 일부 기술적 배경 정보는 PXE(Preboot Execution Environment) 사양(<ulink url="http://www.pix.net/software/pxeboot/archive/pxespec.pdf"/>)에서 이용할 수 있습니다.
   </para>
   <procedure id="proc.deployment.remoteinst.boot.pxe">
    <step performance="required">
     <para>
      설치 리포지토리의 <filename>boot/&lt;architecture&gt;/loader</filename> 디렉토리로 변경하고 다음을 입력하여 <filename>linux</filename>, <filename>initrd</filename>, <filename>message</filename>, <filename>biostest</filename> 및 <filename>memtest</filename> 파일을 <filename>/srv/tftpboot</filename> 디렉토리에 복사합니다.
     </para>
<screen>cp -a linux initrd message biostest memtest /srv/tftpboot</screen>
    </step>
    <step performance="required">
     <para>
      YaST를 사용하여 설치 DVD에서 직접 <systemitem>syslinux</systemitem> 패키지를 설치합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      다음을 입력하여 <filename>/usr/share/syslinux/pxelinux.0</filename> 파일을 <filename>/srv/tftpboot</filename> 디렉토리에 복사합니다.
     </para>
<screen>cp -a /usr/share/syslinux/pxelinux.0 /srv/tftpboot
     </screen>
    </step>
    <step performance="required">
     <para>
      설치 리포지토리의 디렉토리로 변경하고 다음을 입력하여 <filename>isolinux.cfg</filename> 파일을 <filename> /srv/tftpboot/pxelinux.cfg/default</filename>에 복사합니다.
     </para>
<screen>cp -a boot/&lt;architecture&gt;/loader/isolinux.cfg /srv/tftpboot/pxelinux.cfg/default
     </screen>
    </step>
    <step performance="required">
     <para>
      <filename>/srv/tftpboot/pxelinux.cfg/default</filename> 파일을 편집하고 <literal>readinfo</literal> 및 <literal>framebuffer</literal>로 시작하는 행을 제거합니다.
     </para>
    </step>
    <step performance="required">
     <para>

      기본 <option>failsafe</option> 및 <option>apic</option> 레이블의 추가 행에 다음 항목을 삽입합니다.
     </para>
     <variablelist>
      <varlistentry>
       <term><option>insmod=<replaceable>kernel module</replaceable></option>
       </term>
       <listitem>
        <para>
         이 항목을 사용하여 PXE 클라이언트에서 네트워크 설치를 지원하는 데 필요한 네트워크 커널 모듈을 입력합니다. <replaceable>kernel module</replaceable>을 네트워크 장치에 적합한 모듈 이름으로 바꿉니다.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term><option>netdevice=<replaceable>interface</replaceable></option>
       </term>
       <listitem>
        <para>
         이 항목은 네트워크 설치에 사용해야 하는 클라이언트의 네트워크 인터페이스를 정의합니다. 이 항목은 클라이언트에 여러 네트워크 카드가 포함되어 있고 그에 따라 적용해야 하는 경우에만 필요합니다. 단일 네트워크 카드의 경우 이 항목은 생략될 수 있습니다.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term>
<option>install=nfs://<replaceable>ip_instserver</replaceable>/<replaceable>path_to_repository</replaceable>/DVD1</option>
       </term>
       <listitem>
        <para>
         이 항목은 클라이언트 설치를 위한 NFS 서버와 리포지토리를 정의합니다. <replaceable>ip_instserver</replaceable>를 설치 서버의 실제 IP 주소로 바꿉니다. <replaceable>path_to_repository</replaceable>는 리포지토리의 실제 경로로 바꿔야 합니다. HTTP, FTP 또는 SMB 리포지토리는 <literal>http</literal>, <literal>ftp</literal> 또는 <literal>smb</literal> 프로토콜 접두어를 제외하고 동일한 방식으로 사용됩니다.
        </para>
        <important>
         <title>부팅 옵션 추가</title>
         <para>
          SSH 또는 VNC 부팅 파라미터 등의 다른 부팅 옵션을 설치 루틴에 전달해야 하는 경우 이 옵션을 <option>install</option> 항목에 추가합니다. 파라미터와 일부 예에 대한 개요는 <xref linkend="sec.deployment.remoteinst.bootinst"/>에 나와 있습니다.
         </para>
        </important>
       </listitem>
      </varlistentry>
     </variablelist>
     <tip>
      <title>커널 및 initrd 파일 이름 변경</title>
      <para>
       커널 및 initrd 이미지에 다른 파일 이름을 사용할 수 있습니다. 이는 동일한 부팅 서버에서 다른 운영 체제를 제공할 경우 유용합니다. 그러나 PXE 부팅에 대해 TFTP에서 제공된 파일 이름에는 점을 한 개만 사용할 수 있습니다.
      </para>
     </tip>
     <para>
      <option>/srv/tftpboot/pxelinux.cfg/default</option> 파일의 예는 다음과 같습니다. 리포지토리의 프로토콜 접두어를 네트워크 설정과 일치하도록 조정하고 <literal>vnc</literal>와 <literal>vncpassword</literal> 또는 <literal>usessh</literal>와 <literal>sshpassword</literal> 옵션을 <literal>install</literal> 항목에 추가하여 설치 프로그램에 연결할 기본 방법을 지정합니다. <literal>\</literal>로 구분된 행은 줄 바꿈과 <literal>\</literal> 없이 한 개의 연속 행으로 입력해야 합니다.
     </para>
<screen>default harddisk 
        
# default
label linux 
  kernel linux 
  append initrd=initrd ramdisk_size=65536 \
     install=nfs://<replaceable>ip_instserver</replaceable>/<replaceable>path_to_repository</replaceable>/<replaceable>product</replaceable>/DVD1 
	    
# repair
label repair
  kernel linux
  append initrd=initrd splash=silent repair=1 showopts

# rescue 
label rescue 
  kernel linux 
  append initrd=initrd ramdisk_size=65536 rescue=1 

# bios test
label firmware
  kernel linux
  append initrd=biostest,initrd splash=silent install=exec:/bin/run_biostest showopts

#  memory test 
label memtest 
  kernel memtest 

#  hard disk 
label harddisk
  localboot 0

implicit     0 
display      message
prompt       1 
timeout      100</screen>
    </step>
    <step performance="required">
     <para>
      <replaceable>ip_instserver</replaceable>와 <replaceable>path_to_repository</replaceable>를 설정에 사용된 값으로 바꿉니다.
     </para>
     <para>
      다음 절은 이 설정에 사용된 PXELINUX 옵션에 대한 간단한 참조로 제공됩니다. 옵션에 대한 자세한 내용은 <filename>/usr/share/doc/packages/syslinux/</filename>에 있는 <systemitem>syslinux</systemitem> 패키지의 설명서를 참조하십시오.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.pxeopts">
   <title>PXELINUX 구성 옵션</title>
   <para>
    여기에 나열된 옵션은 PXELINUX 구성 파일에 사용할 수 있는 모든 옵션의 하위 집합입니다.
   </para>
   <variablelist>
    <varlistentry>
     <term><option>APPEND <replaceable>options... </replaceable></option>
     </term>
     <listitem>
      <para>
       하나 이상의 옵션을 커널 명령줄에 추가합니다. 이 옵션은 자동 부팅과 수동 부팅 모두에 추가됩니다. 옵션은 커널 명령줄의 맨 처음에 추가되고, 일반적으로 명시적으로 입력된 커널 옵션을 무효화하도록 허용됩니다.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>APPEND -</option>
     </term>
     <listitem>
      <para>
       아무것도 추가하지 않습니다. <option>LABEL</option> 섹션에서 인수로 단일 하이픈이 있는 <option>APPEND</option>는 전역 <option>APPEND</option>를 무효화하는 데 사용할 수 있습니다.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>DEFAULT <replaceable>kernel options</replaceable>...</option>
     </term>
     <listitem>
      <para>
       기본 커널 명령줄을 설정합니다. PXELINUX가 자동으로 부팅되면 자동 옵션이 자동으로 추가되어 자동 부팅을 나타내는 것을 제외하고, DEFAULT 이후의 항목이 부팅 프롬프트 시 입력된 것처럼 작동합니다.
      </para>
      <para>
       구성 파일이 없거나 DEFAULT 항목이 구성 파일에 없는 경우 기본값은 옵션이 없는 커널 이름 <quote>linux</quote>입니다.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>

     <term><option>IFAPPEND <replaceable>FLAG</replaceable></option>
     </term>
     <listitem>
      <para>
       <replaceable>FLAG</replaceable> 값에 따라 특정 옵션을 커널 명령 줄에 추가합니다. <option>IFAPPEND</option> 옵션은 PXELINUX에서만 사용 가능합니다. <replaceable>FLAG</replaceable>에는 다음과 같은 값이 필요합니다(<xref linkend="tab.deployment.boot.pxeopts.ifappend"/> 참조).
      </para>
      <table id="tab.deployment.boot.pxeopts.ifappend">
       <title>생성 및 추가된 <option>IFAPPEND</option>에서 커널 명령 줄 옵션</title>
       <tgroup cols="2">
        <colspec colwidth="1*"/>
        <colspec colwidth="4*"/>
        <thead>
         <row>
          <entry valign="middle">
           <para>
            인수
           </para>
          </entry>
          <entry>
           <para>
            생성된 커널 명령 줄/설명
           </para>
          </entry>
         </row>
        </thead>
        <tbody>
         <row>
          <entry>
           <para>
            <option>1</option>
           </para>
          </entry>
          <entry>
<screen>ip=<replaceable>CLIENT_IP</replaceable>:<replaceable>BOOT_SERVER_IP</replaceable>:<replaceable>GW_IP</replaceable>:<replaceable>NETMASK</replaceable></screen>
           <para>
            DHCP/BOOTP 또는 PXE 부팅 서버의 입력을 기반으로 자리 표시자가 바뀝니다.
           </para>
           <para>
            이 옵션은 부팅된 시스템에서 DHCP 클라이언트를 실행하기 위한 대체 옵션이 아닙니다. 정기적으로 갱신하지 않으면 PXE BIOS가 얻은 임대가 만료되므로 IP 주소를 DHCP 서버가 다시 사용할 수 있습니다.
           </para>
          </entry>
         </row>
         <row>
          <entry>
           <para>
            <option>2</option>
           </para>
          </entry>
          <entry>
<screen>BOOTIF=<replaceable>MAC_ADDRESS_OF_BOOT_INTERFACE</replaceable></screen>
           <para>
            DHCP 서버에서 응답을 받을 때까지 설치 서버가 LAN 인터페이스를 하나씩 검색할 때 시간 제한을 방지하려면 이 옵션이 유용합니다. 이 옵션을 사용하면 initrd 프로그램에서 시스템이 부팅되는 인터페이스를 확인할 수 있습니다. linuxrc에서 이 옵션을 읽고 이 네트워크 인터페이스를 사용합니다.
           </para>
          </entry>
         </row>
         <row>
          <entry>
           <para>
            <option>4</option>
           </para>
          </entry>
          <entry>
<screen>SYSUUID=<replaceable>SYSTEM_UUID</replaceable></screen>
           <para>
            소문자로 된 16진수의 UUID를 추가합니다. <filename>/usr/share/doc/packages/syslinux/pxelinux.txt</filename> 참조
           </para>
          </entry>
         </row>
        </tbody>
       </tgroup>
      </table>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>LABEL <replaceable>label</replaceable> KERNEL <replaceable>image</replaceable> APPEND <replaceable>options</replaceable>...</option>
     </term>
     <listitem>
      <para>
       <replaceable>label</replaceable>이 부팅 시 커널로 입력되면 PXELINUX는 대신 <replaceable>image</replaceable>를 부팅해야 하고, 지정된 <option>APPEND</option> 옵션은 첫 번째 <option>LABEL</option> 명령 전에 파일의 전역 섹션에서 지정된 옵션 대신 사용해야 합니다. <replaceable>image</replaceable>의 기본값은 <replaceable>label</replaceable>과 동일하고, <option>APPEND</option>가 지정되지 않은 경우 기본값은 전역 항목(있는 경우)을 사용하는 것입니다. 최대 128개의 <option>LABEL</option> 항목이 허용됩니다.
      </para>

      <para>
       PXELINUX는 다음 구문을 사용합니다.
      </para>
<screen>label <replaceable>mylabel</replaceable> 
  kernel <replaceable>mykernel</replaceable> 
  append <replaceable>myoptions</replaceable></screen>
      <para>
       레이블은 파일 이름이었던 것처럼 바뀌고, 바뀐 후에는 고유해야 합니다. 예를 들어 <quote>v2.6.30</quote>과 <quote>v2.6.31</quote>의 두 레이블은 두 항목 모두 동일한 DOS 파일 이름으로 바뀌기 때문에 PXELINUX에서 구분할 수 없게 됩니다.
      </para>
      <para>
       커널은 Linux 커널일 필요가 없으며 부팅 섹터나 COMBOOT 파일일 수 있습니다.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>LOCALBOOT <replaceable>type</replaceable></option>
     </term>
     <listitem>
      <para>
       PXELINUX에서 <option>KERNEL</option> 옵션 대신 <option>LOCALBOOT 0</option>을 지정하는 것은 이 특수 레이블을 호출하는 것을 의미하며, 이로 인해 커널 부팅 대신 로컬 디스크 부팅이 실행될 수 있습니다.
      </para>
      <informaltable frame="topbot" rowsep="0">
       <tgroup cols="2">

        <thead>
         <row>
          <entry colname="1">
           <para>
            인수
           </para>
          </entry>
          <entry colname="2">
           <para>
            설명
           </para>
          </entry>
         </row>
        </thead>
        <tbody>
         <row>
          <entry colname="1">
           <para>
            <literal>0</literal>
           </para>
          </entry>
          <entry colname="2">
           <para>
            일반 부팅을 수행합니다.
           </para>
          </entry>
         </row>
         <row>
          <entry colname="1">
           <para>
            <literal>4</literal>
           </para>
          </entry>
          <entry colname="2">
           <para>
            메모리에 있는 UNDI(Universal Network Driver Interface) 드라이버를 사용하여 로컬 부팅을 수행합니다.
           </para>
          </entry>
         </row>
         <row>
          <entry colname="1">
           <para>
            <literal>5</literal>
           </para>
          </entry>
          <entry colname="2">
           <para>
            메모리에 있는 UNDI 드라이버를 포함하여 전체 PXE 스택이 있는 로컬 부팅을 수행합니다.
           </para>
          </entry>
         </row>
        </tbody>
       </tgroup>
      </informaltable>
      <para>
       모든 다른 값은 정의되지 않았습니다. UNDI 또는 PXE 스택의 정의를 모르는 경우 <literal>0</literal>을 지정하십시오.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>TIMEOUT <replaceable>time-out</replaceable></option>
     </term>
     <listitem>
      <para>
       부팅 프롬프트에서 자동으로 부팅될 때까지 기다려야 하는 시간을 1/10초 단위로 나타냅니다. 시간 초과는 키보드의 아무 키나 누르는 즉시 취소되고 사용자가 시작된 명령을 완료할 것으로 가정합니다. 제로(0) 시간 초과는 시간 초과를 완전히 비활성화합니다(이 항목도 기본값임). 허용 가능한 최대 시간 초과 값은 35996입니다(1시간 미만).
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>PROMPT<replaceable> flag_val</replaceable></option>
     </term>
     <listitem>
      <para>
       <option>flag_val</option>이 0인 경우 <keycap function="shift">Shift</keycap> 또는 <keycap function="alt">Alt</keycap> 키를 눌렀거나 <keycap>Caps Lock</keycap> 또는 <keycap>Scroll Lock</keycap> 키를 설정(기본값)한 경우에만 부팅 프롬프트를 표시합니다. <option>flag_val</option>이 1인 경우 항상 부팅 프롬프트를 표시합니다.
      </para>
<screen>F2  <replaceable>filename</replaceable>
F1  <replaceable>filename</replaceable> 
..etc... 
F9  <replaceable>filename</replaceable>
F10 <replaceable>filename</replaceable></screen>
      <para>
       부팅 프롬프트에서 기능 키를 눌렀을 때 화면에 나타난 파일을 표시합니다. 이 파일은 프리부팅 온라인 도움말(커널 명령 줄 옵션에 해당)을 구현하는 데 사용할 수 있습니다. 이전 릴리스에서의 이전 버전과의 호환성의 경우 <keycap>F10</keycap>을 <keycap>F0</keycap>으로도 입력할 수 있습니다. 현재는 파일 이름을 <keycap>F11</keycap> 및 <keycap>F12</keycap>로 바인딩할 방법이 없습니다.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.pxeprep">
   <title>PXE 부팅을 위한 대상 시스템 준비</title>
   <para>
    BIOS 부팅 순서에 PXE 옵션을 포함시켜 PXE 부팅을 위한 시스템의 BIOS를 준비합니다.
   </para>
   <warning>
    <title>BIOS 부팅 순서</title>
    <para>
     PXE 옵션을 BIOS의 하드 디스크 부팅 옵션 앞에 배치하지 마십시오. 그렇지 않으면 이 시스템은 부팅할 때마다 재설치를 시도합니다.
    </para>
   </warning>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.wolprep">
   <title>WOL(Wake On Lan)을 위한 대상 시스템 준비</title>
   <para>
    WOL(Wake On Lan)에는 설치 전에 활성화할 적합한 BIOS 옵션이 필요합니다. 대상 시스템의 MAC 주소도 기록해 두십시오. 이 데이터는 WOL(Wake On Lan)을 시작하는 데 필요합니다.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.wol">
   <title>WOL(Wake On Lan)</title>
   <para>
    WOL(Wake On Lan)을 사용하면 시스템의 MAC 주소가 포함된 특수 네트워크 패킷에서 시스템을 켤 수 있습니다. 전 세계 모든 시스템에는 고유 MAC ID가 있기 때문에 잘못된 시스템을 실수로 켜는 것에 대해 걱정할 필요가 없습니다.
   </para>
   <important>
    <title>여러 네트워크 세그먼트에 대한 WOL(Wake On Lan)</title>
    <para>
     제어 시스템이 활성화해야 하는 설치 대상으로 동일한 네트워크 세그먼트에 없는 경우 멀티캐스트로 전송할 WOL 요청을 구성하거나, 이러한 요청을 보낸 사람으로 역할을 수행할 해당 네트워크 세그먼트에서 시스템을 원격으로 제어합니다.
    </para>
   </important>
   <para>
    SUSE Linux Enterprise Server의 사용자는 WOL이라는 YaST 모듈을 사용하여 WOL(Wake On Lan)을 쉽게 구성할 수 있습니다. 다른 버전의 SUSE Linux 기반 운영 체제 사용자는 명령줄 도구를 사용할 수 있습니다.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.boot.wolyast">
   <title>YaST를 사용하여 WOL(Wake On Lan)</title>
   <procedure id="proc.deployment.remoteinst.boot.wolyast">
    <step performance="required">
     <para>
      <systemitem class="username">root</systemitem>로 로그인합니다. 
     </para>
    </step>
    <step performance="required">
     <para>
      <menuchoice> <guimenu>YaST</guimenu> <guimenu>네트워크 서비스</guimenu> <guimenu>WOL</guimenu></menuchoice>을 시작합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      <guimenu>추가</guimenu>를 클릭하고 대상 시스템의 호스트 이름과 MAC 주소를 입력합니다.
     </para>
    </step>
    <step performance="required">
     <para>
      이 시스템을 켜려면 해당 항목을 선택하고 <guimenu>부팅하기</guimenu>를 클릭합니다.
     </para>
    </step>
   </procedure>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.bootinst">
  <title>설치할 대상 시스템 부팅</title>

  <para>
   <xref linkend="sec.deployment.remoteinst.boot.wol"/> 및 <xref linkend="sec.deployment.remoteinst.boot.pxe"/>에서 언급한 항목 외에도, 설치할 부팅 프로세스를 사용자 정의하는 방법이 두 가지 있습니다. 즉, 기본 부팅 옵션과 기능 키를 사용하거나 설치 부팅 화면의 부팅 옵션 프롬프트를 사용하여 이 특수 하드웨어에서 설치 커널이 필요할 수 있는 부팅 옵션을 전달합니다.
  </para>

  <sect2 id="sec.deployment.remoteinst.bootinst.opts">
   <title>기본 부팅 옵션 사용</title>
   <para>
    부팅 옵션은 <xref linkend="cha.inst"/>에서 자세히 설명합니다. 일반적으로 <guimenu>설치</guimenu>를 선택하면 설치 부팅 프로세스가 시작됩니다.
   </para>
   <para>
    문제가 발생하면 <guimenu>설치 - ACPI 비활성화됨</guimenu> 또는 <guimenu>설치 - 안전 설정</guimenu>을 사용합니다. 설치 프로세스의 문제 해결에 대한 자세한 내용은 <xref linkend="sec.trouble.install"/>을 참조하십시오.
   </para>
   <para>
    아래쪽 화면의 메뉴 모음은 일부 설정에 필요한 몇 개의 고급 기능을 제공합니다. F 키를 사용하면 이러한 파라미터의 상세 구문을 알 필요 없이 설치 루틴에 전달할 추가 옵션을 지정할 수 있습니다(<xref linkend="sec.deployment.remoteinst.bootinst.custom"/> 참조). 사용 가능한 기능 키의 상세 설명은 <xref linkend="sec.i.yast2.bootscreen"/>에 나와 있습니다.
   </para>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.bootinst.custom">
   <title>사용자 정의 부팅 옵션 사용</title>
   <para>
    적절한 부팅 옵션 집합은 설치 절차를 간소화하도록 도와줍니다. 많은 파라미터는 linuxrc 루틴을 사용하여 나중에 구성할 수도 있지만, 부팅 옵션을 사용하는 것이 더 쉽습니다. 일부 자동 설정에서 부팅 옵션은 <filename>initrd</filename> 또는 <filename>info</filename> 파일과 함께 제공될 수 있습니다.
   </para>
   <para>
    다음 테이블에는 이 장에서 언급된 모든 설치 시나리오가 부팅에 필요한 파라미터 및 해당 부팅 옵션과 함께 나열됩니다. 설치 루틴에 전달된 한 개의 부팅 옵션 스트링을 가져오려면 이 항목들을 모두 해당 테이블에 나타난 순서대로 추가합니다. 예를 들면 다음과 같습니다(한 줄에 모두 표시).
   </para>
<screen>install=<replaceable>xxx</replaceable> netdevice<replaceable>=xxx</replaceable> hostip=<replaceable>xxx</replaceable> netmask=<replaceable>xxx</replaceable> vnc=<replaceable>xxx</replaceable> vncpassword=<replaceable>xxx</replaceable></screen>

   <para>
    이 스트링의 모든 값 <replaceable>xxx</replaceable>를 설정에 적합한 값으로 바꿉니다.
   </para>
   <variablelist id="tab.deployment.remoteinst.bootinst.custom">
    <varlistentry>
     <term><xref linkend="cha.inst"/>
     </term>
     <listitem>
      <formalpara>
       <title>부팅에 필요한 파라미터</title>
       <para>
        없음
       </para>
      </formalpara>
      <formalpara>
       <title>부팅 옵션</title>
       <para>
        필요한 항목 없음
       </para>
      </formalpara>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.vncstat"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>부팅에 필요한 파라미터</title>
       <listitem>
        <para>
         설치 서버의 위치
        </para>
       </listitem>
       <listitem>
        <para>
         네트워크 장치
        </para>
       </listitem>
       <listitem>
        <para>
         IP 주소
        </para>
       </listitem>
       <listitem>
        <para>
         넷마스크
        </para>
       </listitem>
       <listitem>
        <para>
         게이트웨이
        </para>
       </listitem>
       <listitem>
        <para>
         VNC 활용
        </para>
       </listitem>
       <listitem>
        <para>
         VNC 비밀번호
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>부팅 옵션</title>
       <listitem>
        <para>
         <filename>install=(nfs,http, ftp,smb)://<replaceable>path_to_instmedia</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netdevice=<replaceable>some_netdevice</replaceable></filename>(여러 네트워크 장치를 사용할 수 있는 경우에만 필요)
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>hostip=<replaceable>some_ip</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netmask=<replaceable>some_netmask</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>gateway=<replaceable>ip_gateway</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>vnc=1</filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>vncpassword=<replaceable>some_password</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.vncdyn"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>부팅에 필요한 파라미터</title>
       <listitem>
        <para>
         설치 서버의 위치
        </para>
       </listitem>
       <listitem>
        <para>
         VNC 활용
        </para>
       </listitem>
       <listitem>
        <para>
         VNC 비밀번호
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>부팅 옵션</title>
       <listitem>
        <para>
         <filename>install=(nfs,http, ftp,smb)://<replaceable>path_to_instmedia</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <literal>vnc=1</literal>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>vncpassword=<replaceable>some_password</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.vncpxe"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>부팅에 필요한 파라미터</title>
       <listitem>
        <para>
         설치 서버의 위치
        </para>
       </listitem>
       <listitem>
        <para>
         TFTP 서버의 위치
        </para>
       </listitem>
       <listitem>
        <para>
         VNC 활용
        </para>
       </listitem>
       <listitem>
        <para>
         VNC 비밀번호
        </para>
       </listitem>
      </itemizedlist>
      <formalpara>
       <title>부팅 옵션</title>
       <para>
        해당 안 됨: PXE 및 DHCP를 통해 관리되는 프로세스
       </para>
      </formalpara>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.sshstat"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>부팅에 필요한 파라미터</title>
       <listitem>
        <para>
         설치 서버의 위치
        </para>
       </listitem>
       <listitem>
        <para>
         네트워크 장치
        </para>
       </listitem>
       <listitem>
        <para>
         IP 주소
        </para>
       </listitem>
       <listitem>
        <para>
         넷마스크
        </para>
       </listitem>
       <listitem>
        <para>
         게이트웨이
        </para>
       </listitem>
       <listitem>
        <para>
         SSH 활용
        </para>
       </listitem>
       <listitem>
        <para>
         SSH 비밀번호
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>부팅 옵션</title>
       <listitem>
        <para>
         <filename>install=(nfs,http, ftp,smb)://<replaceable>path_to_instmedia</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netdevice=<replaceable>some_netdevice</replaceable></filename>(여러 네트워크 장치를 사용할 수 있는 경우에만 필요)
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>hostip=<replaceable>some_ip</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>netmask=<replaceable>some_netmask</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>gateway=<replaceable>ip_gateway</replaceable> </filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <literal>usessh=1</literal>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>sshpassword=<replaceable>some_password</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.sshdyn"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <title>부팅에 필요한 파라미터</title>
       <listitem>
        <para>
         설치 서버의 위치
        </para>
       </listitem>
       <listitem>
        <para>
         SSH 활용
        </para>
       </listitem>
       <listitem>
        <para>
         SSH 비밀번호
        </para>
       </listitem>
      </itemizedlist>
      <itemizedlist spacing="compact" mark="bullet">
       <title>부팅 옵션</title>
       <listitem>
        <para>
         <filename>install=(nfs,http, ftp,smb)://<replaceable>path_to_instmedia</replaceable></filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>usessh=1</filename>
        </para>
       </listitem>
       <listitem>
        <para>
         <filename>sshpassword=<replaceable>some_password</replaceable> </filename>
        </para>
       </listitem>
      </itemizedlist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><xref linkend="sec.deployment.remoteinst.scenario.sshpxe"/>
     </term>
     <listitem>
      <itemizedlist spacing="compact" mark="bullet">
       <listitem>
        <para>
         설치 서버의 위치
        </para>
       </listitem>
       <listitem>
        <para>
         TFTP 서버의 위치
        </para>
       </listitem>
       <listitem>
        <para>
         SSH 활용
        </para>
       </listitem>
       <listitem>
        <para>
         SSH 비밀번호
        </para>
       </listitem>
      </itemizedlist>
      <formalpara>
       <title>부팅 옵션</title>
       <para>
        해당 안 됨: PXE 및 DHCP를 통해 관리되는 프로세스
       </para>
      </formalpara>
     </listitem>
    </varlistentry>
   </variablelist>
   <tip>
    <title>linuxrc 부팅 옵션에 대한 자세한 내용</title>
    <para>
     Linux 시스템 부팅에 사용된 linuxrc 부팅 옵션에 대한 자세한 내용은 <ulink url="http://en.opensuse.org/SDB:Linuxrc"/>를 참조하십시오.
    </para>
   </tip>
   <sect3 id="sec.deployment.remoteinst.bootinst.custom.addon">
    <title>추가 기능 제품 및 드라이버 업데이트 설치</title>
    <para>
     <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>에서는 확장 기능(예: SUSE Linux Enterprise High Availability Extension), 타사 제품 및 드라이버 또는 추가 소프트웨어를 제공하는 추가 기능 제품의 설치를 지원합니다. <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>를 원격으로 배포할 때 추가 기능 제품을 자동으로 설치하려면 <literal>addon=<replaceable>REPOSITORY</replaceable></literal> 파라미터를 지정하십시오.
    </para>
    <para>
     <replaceable>REPOSITORY</replaceable>는 YaST(YaST2 또는 YUM(rpm-md))에서 읽을 수 있는 호스팅된 리포지토리여야 합니다. ISO 이미지는 현재 지원되지 않습니다.
    </para>
    <tip>
     <title>드라이버 업데이트</title>
     <para>
      드라이버 업데이트는 <ulink url="http://drivers.suse.com/"/>에 있습니다. 일부 드라이버 업데이트만 리포지토리로 제공됩니다. 일부만 iso 이미지로 사용 가능하므로 <literal>addon</literal> 파라미터와 함께 설치될 수 없습니다. iso 이미지를 통해 드라이버 업데이트를 설치하는 방법은 <ulink url="http://drivers.suse.com/doc/SolidDriver/Driver_Kits.html"/>에서 확인할 수 있습니다.
     </para>
    </tip>
   </sect3>
  </sect2>
 </sect1>
 <sect1 id="sec.deployment.remoteinst.monitor">
  <title>설치 프로세스 모니터링</title>

  <para>
   설치 프로세스를 원격으로 모니터링하는 옵션에는 몇 가지가 있습니다. 설치 부팅 중에 적절한 부팅 옵션이 지정된 경우 VNC 또는 SSH를 사용하여 원격 워크스테이션에서 설치 및 시스템 구성을 제어할 수 있습니다.
  </para>

  <sect2 id="sec.deployment.remoteinst.monitor.vnc">
   <title>VNC 설치</title>
   <para>
    VNC 뷰어 소프트웨어를 사용하여 실제로 모든 운영 체제에서 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>의 설치를 원격으로 제어할 수 있습니다. 이 절에서는 VNC 뷰어 응용 프로그램 또는 웹 브라우저를 사용한 설정을 소개합니다.
   </para>
   <sect3 id="sec.deployment.remoteinst.monitor.vnc.prep">
    <title>VNC 설치 준비</title>
    <para>
     VNC 설치를 준비하기 위해 설치 대상에서 수행해야 할 일은 초기 부팅 시 설치하기에 적합한 부팅 옵션을 제공하는 것뿐입니다(<xref linkend="sec.deployment.remoteinst.bootinst.custom"/> 참조). 대상 시스템은 텍스트 기반 환경으로 부팅하고 VNC 클라이언트가 설치 프로그램에 연결할 때까지 기다립니다.
    </para>
    <para>
     설치 프로그램은 IP 주소를 알리고 설치를 위해 연결에 필요한 번호를 표시합니다. 대상 시스템에 대한 물리적 액세스가 있는 경우 이 정보는 시스템이 설치를 위해 부팅된 즉시 제공됩니다. VNC 클라이언트 소프트웨어에 관련 메시지가 표시되면 이 데이터를 입력하고 VNC 비밀번호를 제공합니다.
    </para>
    <para>
     설치 대상은 OpenSLP를 통해 자체적으로 알리기 때문에 네트워크 설정과 모든 시스템이 OpenSLP를 제공하는 한, 설치 자체에 실제로 접속할 필요 없이 SLP 브라우저를 통해 설치 대상의 주소 정보를 검색할 수 있습니다.
    </para>
    <procedure id="proc.deployment.remoteinst.monitor.vnc.prep">
     <title>OpenSLP를 통해 VNC 설치 찾기</title>
     <step performance="required">
      <para>
       VNC를 제공하는 모든 서비스 목록을 가져오려면 <command>slptool findsrvtypes | grep vnc</command>를 실행합니다. VNC 설치 대상은 <literal>YaST.installation.suse</literal> 서비스 아래에서 사용할 수 있습니다.
      </para>
     </step>
     <step performance="required">
      <para>
       사용 가능한 설치 목록을 가져오려면 <command>slptool findsrvs <replaceable>YaST.installation.suse</replaceable></command>를 실행합니다. VNC 뷰어와 함께 제공된 IP 주소 및 포트(일반적으로 <systemitem>5901</systemitem>)를 사용합니다.
      </para>
     </step>
    </procedure>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.monitor.vnc.connect">
    <title>설치 프로그램에 연결</title>
    <para>
     VNC 서버(이 경우는 설치 대상)에 연결하는 방법은 두 가지가 있습니다. 즉, 운영 체제에서 독립 VNC 뷰어 응용 프로그램을 시작하거나 Java 지원 웹 브라우저를 사용하여 연결할 수 있습니다.
    </para>
    <para>
     VNC를 사용하여 Linux 특징, Windows 또는 Mac OS를 포함한 기타 운영 체제에서 Linux 시스템의 설치를 제어할 수 있습니다.
    </para>
    <para>
     Linux 시스템에서 패키지 <systemitem>tightvnc</systemitem>가 설치되었는지 확인합니다. Windows 시스템에서 이 응용 프로그램의 Windows 포트를 설치합니다. 이 내용은 TightVNC 홈페이지(<ulink url="http://www.tightvnc.com/download.html"/>)에서 확인할 수 있습니다.
    </para>
    <procedure id="proc.deployment.remoteinst.monitor.vnc.connect.app">
     <para>
      대상 시스템에서 실행 중인 설치 프로그램에 연결하려면 다음을 수행하십시오.
     </para>
     <step performance="required">
      <para>
       VNC 뷰어를 시작합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       SLP 브라우저 또는 설치 프로그램 자체에서 제공된 대로 설치 대상의 IP 주소와 디스플레이 번호를 입력합니다.

      </para>
<screen><replaceable>ip_address</replaceable>:<replaceable>display_number</replaceable></screen>
      <para>
       바탕 화면에서 창이 열리고 YaST 화면이 일반 로컬 설치에서와 같이 표시됩니다.
      </para>
     </step>
    </procedure>
    <para>
     설치 프로그램에 연결할 웹 브라우저를 사용하는 것은 VNC 소프트웨어 또는 기본 운영 체제와 완전히 무관합니다. 브라우저 응용 프로그램에서 Java를 지원하는 한, 원하는 브라우저(Firefox, Internet Explorer, Chromium, Opera 등)를 사용하여 Linux 시스템의 설치를 수행할 수 있습니다.
    </para>
    <para>
     VNC 설치를 실행하려면 다음을 수행하십시오.
    </para>
    <procedure id="proc.deployment.remoteinst.monitor.vnc.connect.brow">
     <step performance="required">
      <para>
       기본 웹 브라우저를 시작합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       주소 프롬프트에서 다음을 입력합니다.

      </para>
<screen>http://<replaceable>ip_address_of_target</replaceable>:5801</screen>
     </step>
     <step performance="required">
      <para>
       메시지가 나타나면 VNC 비밀번호를 입력합니다. 이제 브라우저 창에는 일반 로컬 설치에서와 같이 YaST 화면이 표시됩니다.
      </para>
     </step>
    </procedure>
   </sect3>
  </sect2>

  <sect2 id="sec.deployment.remoteinst.monitor.ssh">
   <title>SSH 설치</title>
   <para>
    SSH를 사용하여 SSH 클라이언트 소프트웨어를 사용하는 Linux 시스템의 설치를 원격으로 제어할 수 있습니다.
   </para>
   <sect3 id="sec.deployment.remoteinst.monitor.ssh.prep">
    <title>SSH 설치 준비</title>
    <para>
     적합한 소프트웨어 패키지(Linux의 경우 OpenSSH, Windows의 경우 PuTTY)의 설치 외에도 적합한 부팅 옵션을 전달하여 설치를 위해 SSH를 활성화해야 합니다. 자세한 내용은 <xref linkend="sec.deployment.remoteinst.bootinst.custom"/>를 참조하십시오. OpenSSH는 기본적으로 모든 SUSE Linux 기반 운영 체제에서 설치됩니다.
    </para>
   </sect3>
   <sect3 id="sec.deployment.remoteinst.monitor.ssh.connect">
    <title>설치 프로그램에 연결</title>
    <procedure id="proc.deployment.remoteinst.monitor.ssh.connect">
     <step performance="required">
      <para>
       설치 대상의 IP 주소를 검색합니다. 대상 시스템에 대한 물리적 액세스가 있는 경우 초기 부팅 후에 설치 루틴이 콘솔에서 제공하는 IP 주소를 가져옵니다. 그렇지 않으면, DHCP 서버 구성에서 이 특정 호스트에 지정된 IP 주소를 가져옵니다.
      </para>
     </step>
     <step performance="required">
      <para>
       명령줄에서 다음 명령을 입력합니다.
      </para>
<screen>ssh -X root@<replaceable>
ip_address_of_target</replaceable></screen>
      <para>
       <replaceable>ip_address_of_target</replaceable>을 설치 대상의 실제 IP 주소로 바꿉니다.
      </para>
     </step>
     <step performance="required">
      <para>
       사용자 이름을 입력하라는 메시지가 나타나면 <literal>root</literal>를 입력합니다.
      </para>
     </step>
     <step performance="required">
      <para>
       비밀번호를 입력하라는 메시지가 나타나면 SSH 부팅 옵션과 함께 설정된 비밀번호를 입력합니다. 인증에 성공하면 설치 대상에 대한 명령줄 프롬프트가 나타납니다.
      </para>
     </step>
     <step performance="required">
      <para>
       <command>yast</command>를 입력하여 설치 프로그램을 시작합니다. <xref linkend="cha.inst"/>에서 설명한 대로 창이 열리고 일반 YaST 화면이 표시됩니다.
      </para>
     </step>
    </procedure>
   </sect3>
  </sect2>
 </sect1>
</chapter>
