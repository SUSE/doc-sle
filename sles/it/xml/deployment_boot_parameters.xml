<?xml version="1.0" encoding="UTF-8"?>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="deployment_boot_parameters.xml" version="5.0" xml:id="cha-boot-parameters">
 <title>Parametri di avvio</title>
 <info>
  <abstract>
   <para>
    <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> consente di impostare diversi parametri durante l'avvio, ad esempio la scelta dell'origine dei dati di installazione o l'impostazione della configurazione di rete.
   </para>

  </abstract>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>sì</dm:translation>
  </dm:docmanager>
 </info>

 <para>
  L'uso del set di parametri di avvio appropriato consente di semplificare la procedura di installazione. Molti parametri possono essere configurati successivamente mediante le routine linuxrc, tuttavia l'uso dei parametri di avvio è più semplice. In alcune configurazioni automatiche, i parametri di avvio possono essere forniti mediante un file <filename>initrd </filename> o <filename>info</filename>.
 </para>
 <para>
  La modalità di avvio del sistema per l'installazione dipende dall'architettura. Ad esempio, l'avvio di un PC (AMD64/Intel 64) è diverso da quello di un mainframe. Se si installa <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> come guest VM in un hypervisor KVM o Xen, seguire le istruzioni relative all'architettura AMD64/Intel 64.
 </para>
 <note>
  <title>Opzioni e parametri di avvio</title>
  <para>
   I termini <emphasis>Parametri di avvio</emphasis> e <emphasis>Opzioni di avvio</emphasis> vengono utilizzati spesso in modo intercambiabile. Nella presente documentazione prevale l'uso del termine <emphasis>Parametri di avvio</emphasis>.
  </para>
 </note>
 <sect1 xml:id="sec-boot-parameters-default">
  <title>Uso dei parametri di avvio di default</title>
  <para>
   La descrizione dettagliata dei parametri di avvio è riportata nel <xref linkend="cha-install"/>. In genere, selezionando <guimenu>Installazione</guimenu> viene avviato il processo di avvio dell'installazione.
  </para>
  <para>
   In caso di problemi, utilizzare <guimenu>Installation—ACPI Disabled (Installazione - ACPI disabilitato)</guimenu> o <guimenu>Installation—Safe Settings (Installazione - Safe Settings)</guimenu>. Per ulteriori informazioni sulla soluzione dei problemi relativi al processo di installazione, vedere il <xref linkend="cha-installation-troubleshooting"/>.
  </para>
  <para>
   La barra dei menu nella parte inferiore della schermata include alcune funzionalità avanzate necessarie in determinate configurazioni. L'uso dei tasti funzione (<keycap>F1</keycap> ... <keycap>F12</keycap>), consente di specificare opzioni aggiuntive per passare alle routine di installazione senza dover conoscere necessariamente la sintassi dettagliata di questi parametri (vedere <xref linkend="cha-boot-parameters"/>). Per una descrizione dettagliata dei tasti funzione disponibili, vedere <xref linkend="sec-boot-parameters-screen"/>.
  </para>
 </sect1>

 <sect1 xml:id="sec-boot-parameters-start-x86-64" arch="x86_64">
  <title>PC (AMD64/Intel 64/ARM AArch64)</title>
  <para>
   In questa sezione è descritto come modificare i parametri di avvio per AMD64, Intel 64 e ARM AArch64.
  </para>
  <sect2 xml:id="sec-boot-parameters-screen">
   <title>Schermata di avvio nei computer con BIOS tradizionale</title>
   <para>
    Nella schermata di avvio sono disponibili numerose opzioni per la procedura di installazione. <guimenu>Avvia dal disco rigido</guimenu> avvia il sistema installato ed è sempre l'opzione di default perché il CD viene spesso lasciato nell'unità. Selezionare una delle altre opzioni con i tasti freccia <keycap function="enter"/> Le opzioni sono le seguenti:
   </para>
   <variablelist>
    <varlistentry>
     <term><guimenu>Installazione</guimenu>
     </term>
     <listitem>
      <para>
       Modalità di installazione normale. Verranno attivate tutte le funzioni hardware moderne. Nel caso l'installazione risulti impossibile, consultare <xref linkend="vle-boot-parameters-screen-kernel"/> per i parametri di avvio che disabilitano funzioni potenzialmente problematiche.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><guimenu>Upgrade</guimenu>
     </term>
     <listitem>
      <para>
       Eseguire un upgrade del sistema. Per ulteriori informazioni, fare riferimento al <xref os="sles;sled" linkend="cha-upgrade-paths"/>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>
      <menuchoice><guimenu>Altro</guimenu> <guimenu>Sistema di ripristino</guimenu></menuchoice>
     </term>
     <listitem>
      <para>
       Avvia un sistema Linux minimo senza interfaccia grafica. Per ulteriori informazioni, vedere il <xref linkend="sec-trouble-data-recover-rescue"/>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>
      <menuchoice><guimenu>Altro</guimenu> <guimenu>Avvia sistema Linux</guimenu></menuchoice>
     </term>
     <listitem>
      <para>
       Avviare un sistema Linux già installato. Verrà chiesto di specificare la partizione dalla quale avviare il sistema.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>
      <menuchoice><guimenu>Altro</guimenu> <guimenu>Verifica supporto di installazione</guimenu></menuchoice>
     </term>
     <listitem>
      <para>
       Questa opzione è disponibile solo quando si effettua l'installazione dal supporto creato dall'immagine ISO di cui si è effettuato il download. In questo caso, è consigliabile controllare l'integrità del mezzo di installazione. Questa opzione consente di avviare il sistema di installazione prima del controllo automatico dei supporti. Se il controllo ha esito positivo, si avvia la normale routine di installazione. Se viene rilevato un supporto danneggiato, la routine di installazione si interrompe. Sostituire il supporto rotto e riavviare il processo di installazione.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>
      <menuchoice><guimenu>Altro</guimenu> <guimenu>Verifica della memoria</guimenu></menuchoice>
     </term>
     <listitem>
      <para>
       Consente di verificare la memoria RAM del sistema mediante cicli ripetuti di lettura e scrittura. Terminare il test riavviando. Per ulteriori informazioni, consultare il <xref linkend="sec-installation-troubleshooting-noboot"/>.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
   <figure>
    <title>Schermata di avvio nei computer con BIOS tradizionale</title>
    <mediaobject>
     <textobject role="description"><phrase>Schermata di avvio nei computer con BIOS tradizionale</phrase>
     </textobject>
     <imageobject os="sles;sled">
      <imagedata fileref="install_boot.png" width="75%"/>
     </imageobject>       
     
    </mediaobject>
   </figure>
   <para>
    Utilizzare i tasti funzione illustrati in fondo alla schermata per modificare la lingua, la risoluzione dello schermo, l'origine di installazione oppure per aggiungere un ulteriore driver del fornitore dell'hardware:
   </para>
   <variablelist>
    <varlistentry>
     <term><keycap>F1</keycap><guimenu>Aiuto</guimenu>
     </term>
     <listitem>
      <para>
       Apre la guida contestuale per l'elemento attivo della schermata di avvio. Utilizzare i tasti freccia per navigare, <keycap function="enter"/> seguire un collegamento e <keycap function="escape"/> abbandonare la schermata di aiuto.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><keycap>F2</keycap><guimenu>Lingua</guimenu>
     </term>
     <listitem>
      <para>
       Consente di selezionare la lingua di visualizzazione e il layout di tastiera corrispondente per l'installazione. La lingua di default è Inglese (Stati Uniti).
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><keycap>F3</keycap><guimenu>Modalità video</guimenu>
     </term>
     <listitem>
      <para>
       Consente di selezionare diverse modalità di visualizzazione grafica per l'installazione. Per <guimenu>default</guimenu>, la risoluzione video viene determinata automaticamente tramite KMS (<quote>Kernel Mode Settings</quote>). Se questa impostazione non funziona nel sistema, scegliere <guimenu>Nessuna KMS</guimenu> e, facoltativamente, specificare <literal>vga=ask</literal> nella riga di comando di avvio in modo che venga richiesta l'immissione della risoluzione video. Scegliere <guimenu>Modalità testo</guimenu> in caso di problemi di installazione grafica.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><keycap>F4</keycap><guimenu>Origine</guimenu>
     </term>
     <listitem>
      <para>
       Di norma, l'installazione viene eseguita dal supporto di installazione inserito nell'unità. A questo livello, è possibile selezionare altre fonti, tra cui server FTP o NFS. Se l'installazione viene distribuita su una rete con un server SLP, utilizzare questa opzione per selezionare un'origine di installazione disponibile sul server. <phrase os="sles;sled">Per informazioni sulla configurazione di un server di installazione con SLP, vedere <xref linkend="cha-deployment-instserver"/>.</phrase>
      </para>
     </listitem>
    </varlistentry>
    <varlistentry xml:id="vle-boot-parameters-screen-kernel">
     <term><keycap>F5</keycap><guimenu>Kernel</guimenu>
     </term>
     <listitem>
      <para>
       Se si rilevano problemi con la normale installazione, è possibile utilizzare questo menu per disabilitare alcune funzioni potenzialmente problematiche. Se l'hardware non supporta ACPI (Advanced Configuration and Power Interface), selezionare <guimenu>No ACPI</guimenu> per eseguire l'installazione senza supporto ACPI. <guimenu>No local APIC</guimenu> disabilita il supporto APIC (Advanced Programmable Interrupt Controllers), che potrebbe causare problemi con alcuni tipi di hardware. <guimenu>Impostazioni sicure</guimenu> consente di avviare il sistema in modalità DMA (per le unità CD/DVD-ROM) e con le funzioni di risparmio energetico disabilitate.
      </para>
      <para>
       Se non si è certi, provare prima le seguenti opzioni: <guimenu>Installazione - ACPI disabilitata</guimenu> o <guimenu>Installazione - Impostazioni di protezione</guimenu>. Gli utenti esperti possono anche utilizzare la riga di comando (<guimenu>Opzioni di boot</guimenu>) per immettere o cambiare i parametri del kernel.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><keycap>F6</keycap><guimenu>Driver</guimenu>
     </term>
     <listitem>
      <para>
       Premere questo tasto per notificare al sistema che è disponibile un aggiornamento facoltativo per il driver di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>. Utilizzando <guimenu>File</guimenu> o <guimenu>URL</guimenu>, è possibile caricare i driver direttamente, prima dell'inizio dell'installazione. Se si seleziona <guimenu>Sì</guimenu>, verrà richiesto di inserire il disco di aggiornamento al momento adatto del processo di installazione.
      </para>
      <tip os="sles;sled">
       <title>come ottenere i dischi di aggiornamento del driver</title>
       <para>
        Gli aggiornamenti dei driver per SUSE Linux Enterprise sono disponibili all'indirizzo <link xlink:href="http://drivers.suse.com/"/>. I presenti driver sono stati creati mediante il programma SUSE SolidDriver.
       </para>
      </tip>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>
  <sect2 xml:id="sec-boot-parameters-uefi">
   <title>Schermata di avvio nei computer dotati di UEFI</title>
   <para>
    UEFI (Unified Extensible Firmware Interface) è un nuovo standard di settore che sostituisce ed estende il BIOS tradizionale. Le implementazioni più recenti di UEFI contengono l'estensione <quote>Secure Boot</quote> che impedisce l'avvio del codice dannoso consentendo solo l'esecuzione di boot loader firmati. Per ulteriori informazioni, vedere <xref linkend="cha-uefi"/>.
   </para>
   <para>
    Il boot manager GRUB 2, utilizzato per avviare i computer con BIOS tradizionale, non supporta UEFI, pertanto GRUB 2 viene sostituito con GRUB 2 per EFI. Se è abilitato l'avvio sicuro, YaST seleziona automaticamente GRUB 2 per EFI per l'installazione. Poiché dal punto di vista dell'amministratore e dell'utente le implementazioni del boot manager si comportano allo stesso modo, nel seguito verranno entrambe chiamate semplicemente <literal>GRUB 2</literal>.
   </para>
   <tip>
    <title>Utilizzo di driver aggiuntivi con Avvio sicuro</title>
    <para>
     Quando si esegue l'installazione con Avvio sicuro abilitato, non è possibile caricare driver non forniti in dotazione con <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>. Ciò vale anche per i driver forniti tramite SolidDriver, perché per default la loro chiave di firma non è ritenuta attendibile.
    </para>
    <para>
     Per caricare i driver non forniti con <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>, eseguire una delle seguenti operazioni:
    </para>
    <itemizedlist>
     <listitem>
      <para>
       Prima dell'installazione, aggiungere le chiavi necessarie al database del firmware tramite gli strumenti di gestione del firmware/sistema.
      </para>
     </listitem>
     <listitem>
      <para>
       Utilizzare un'immagine ISO avviabile dalla quale verrà eseguita la registrazione delle chiavi necessarie nell'elenco MOK al primo avvio.
      </para>
     </listitem>
    </itemizedlist>
    <para>
     Per ulteriori informazioni, vedere <xref linkend="sec-uefi-secboot"/>.
    </para>
   </tip>
   <para>
    Nella schermata di avvio sono disponibili numerose opzioni per la procedura di installazione. Modificare l'opzione selezionata con i tasti freccia e premere <keycap function="enter"/> per avviarla. Le opzioni sono le seguenti:
   </para>
   <variablelist>
    <varlistentry>
     <term><guimenu>Installazione</guimenu>
     </term>
     <listitem>
      <para>
       Modalità di installazione normale. Verranno attivate tutte le funzioni hardware moderne. Nel caso l'installazione risulti impossibile, consultare <xref linkend="vle-boot-parameters-screen-kernel"/> per i parametri di avvio che disabilitano funzioni potenzialmente problematiche.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><guimenu>Upgrade</guimenu>
     </term>
     <listitem>
      <para>
       Eseguire un upgrade del sistema. Per ulteriori informazioni, fare riferimento al <xref os="sles;sled" linkend="cha-upgrade-paths"/>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>
      <menuchoice><guimenu>Altro</guimenu> <guimenu>Sistema di ripristino</guimenu></menuchoice>
     </term>
     <listitem>
      <para>
       Avvia un sistema Linux minimo senza interfaccia grafica. Per ulteriori informazioni, vedere il <xref linkend="sec-trouble-data-recover-rescue"/>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>
      <menuchoice><guimenu>Altro</guimenu> <guimenu>Avvia sistema Linux</guimenu></menuchoice>
     </term>
     <listitem>
      <para>
       Avviare un sistema Linux già installato. Verrà chiesto di specificare la partizione dalla quale avviare il sistema.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>
      <menuchoice><guimenu>Altro</guimenu> <guimenu>Verifica supporto di installazione</guimenu></menuchoice>
     </term>
     <listitem>
      <para>
       Questa opzione è disponibile solo quando si effettua l'installazione dal supporto creato dall'immagine ISO di cui si è effettuato il download. In questo caso, è consigliabile controllare l'integrità del mezzo di installazione. Questa opzione consente di avviare il sistema di installazione prima del controllo automatico dei supporti. Se il controllo ha esito positivo, si avvia la normale routine di installazione. Se viene rilevato un supporto danneggiato, la routine di installazione si interrompe.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
   <figure>
    <title>Schermata di avvio nei computer dotati di UEFI</title>
    <mediaobject>
     <textobject role="description"><phrase>Schermata di avvio nei computer dotati di UEFI</phrase>
     </textobject>
     <imageobject os="sles;sled">
      <imagedata fileref="install_boot_uefi.png" width="75%"/>
     </imageobject>
     
    </mediaobject>
   </figure>
   <para>
    In <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>, GRUB 2 per EFI non supporta il prompt di avvio o i tasti funzione per l'aggiunta dei parametri di avvio. Per default, l'installazione viene avviata con l'inglese americano e i supporti di avvio come origine di installazione. Viene eseguita una ricerca DHCP per configurare la rete. Per modificare tali impostazioni di default o aggiungere parametri di avvio, è necessario modificare la rispettiva voce di avvio. Evidenziarla utilizzando i tasti freccia e premere <keycap>E</keycap>. Per i suggerimenti relativi alla modifica, vedere la Guida su schermo (al momento è disponibile solo una tastiera inglese). La voce <guimenu>Installazione</guimenu> assomiglierà a quanto riportato di seguito:
   </para>
<screen>setparams 'Installation'

   set gfxpayload=keep
   echo 'Loading kernel ...'
   linuxefi /boot/x86_64/loader/linux splash=silent
   echo 'Loading initial ramdisk ...'
   initrdefi /boot/x86_64/loader/initrd</screen>
   <para>
    Aggiungere parametri separati da spazi alla fine della riga che inizia con <literal>linuxefi</literal>. Per avviare la voce modificata premere <keycap>F10</keycap>. Se si accede al computer tramite una console seriale, premere <keycombo><keycap function="escape"/> <keycap> 0</keycap></keycombo>. In <link xlink:href="http://en.opensuse.org/Linuxrc"/> è disponibile un elenco completo dei parametri. 
   </para>
  </sect2>
 </sect1>
 <sect1 xml:id="sec-boot-parameters-list">
  <title>Elenco dei parametri di avvio importanti</title>
  <para>
   In questa sezione è riportata una selezione dei parametri di avvio importanti.
  </para>
  <sect2 xml:id="sec-boot-parameters-list-general">
   <title>Parametri di avvio generali</title>
   <para/>
   <variablelist>
    <varlistentry>
     <term><option>autoyast=</option><replaceable>URL</replaceable>
     </term>
     <listitem>
      <para>
       Il parametro <envar>AutoYaST</envar> specifica la posizione del file di controllo <filename>autoinst.xml</filename> per l'installazione automatica.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>manual=&lt;0|1&gt;</option></term>
     <listitem>
      <para>
       Il parametro <envar>manual</envar> controlla se gli altri parametri sono solo valori di default che devono essere ancora riconosciuti dall'utente. Se si desidera che tutti i valori siano accettati senza richieste di conferma, impostare il valore di questo parametro su <literal>0.</literal> L'impostazione di <envar>AutoYaST</envar> implica l'impostazione del parametro <envar>manual</envar> sul valore <literal>0</literal>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>Info=</option><replaceable>URL</replaceable>
     </term>
     <listitem>
      <para>
       Specifica l'ubicazione del file da cui possono essere lette le opzioni aggiuntive.
      </para>
      <para arch="zseries" os="sles">
       Ciò è utile per superare la limitazione di 10 righe (e di 80 caratteri per riga in z/VM) per il parmfile. Ulteriore documentazione per il file Info è disponibile nel <xref linkend="invoking-autoinst-linuxrc"/>. Dato che in genere è possibile accedere al file Info solo tramite la rete su IBM Z, non è possibile utilizzarlo per specificare le opzioni richieste per la configurazione della rete (tali opzioni sono descritte nella <xref linkend="sec-boot-parameters-list-network"/>). Inoltre, altre opzioni specifiche di linuxrc, quali quelle per il debug, dovranno essere specificate nel parmfile per essere effettive.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>upgrade=&lt;0|1&gt;</option></term>
     <listitem>
      <para>Per eseguire l'upgrade di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>, specificare <option>Upgrade=1</option>.</para>
      <para os="sles" arch="zseries">
       È necessario un parmfile personalizzato per eseguire l'upgrade di un'installazione esistente di SUSE Linux Enterprise. Senza questo parametro, l'installazione non fornisce alcuna opzione di upgrade.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>dud=</option><replaceable>URL</replaceable></term>
     <listitem>
      <para>
       Carica gli aggiornamenti del driver da <replaceable>URL</replaceable>.
      </para>
      <para>
       Impostare <literal>dud=ftp://ftp.esempio.com/PATH_TO_DRIVER</literal> o <literal>dud=http://www.esempio.com/PATH_TO_DRIVER </literal> per caricare i driver da un URL. Quando <literal>dud=1</literal> viene richiesto di specificare l'URL durante l'avvio.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>language=</option><replaceable>LANGUAGE</replaceable></term>
     <listitem>
      <para>
       Imposta la lingua di installazione. Alcuni valori supportati sono <literal>cs_CZ</literal>, <literal>de_DE</literal>, <literal>es_ES</literal>, <literal>fr_FR</literal>, <literal>ja_JP</literal>, <literal>pt_BR</literal>, <literal>pt_PT</literal>, <literal>ru_RU</literal>, <literal>zh_CN</literal> e <literal>zh_TW</literal>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>acpi=off</option></term>
     <listitem>
      <para>
       Disabilita il supporto ACPI.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>noapic</option></term>
     <listitem>
      <para>
       Nessun APIC logico.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>nomodeset</option></term>
     <listitem>
      <para>
       Disabilita KMS.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>textmode=1</option></term>
     <listitem>
      <para>
       Avvia il programma di installazione in modalità testo.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>console=</option><replaceable>SERIAL_DEVICE</replaceable>[,<replaceable>MODE</replaceable>]</term>
     <listitem>
      <para>
       <replaceable>SERIAL_DEVICE</replaceable> può essere un dispositivo seriale o parallelo effettivo (ad esempio <literal>ttyS0</literal>) o un terminale virtuale (ad esempio <literal>tty1</literal>). <replaceable>MODE</replaceable> è la velocità di trasmissione, parità e bit di stop (ad esempio <literal>9600n8</literal>). Il valore di default per questa impostazione è definito dal firmware della scheda madre. Se nel monitor non è visualizzato l'output, provare con l'impostazione <literal>console=tty1</literal>. È possibile applicarla per definire più dispositivi.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>

  <sect2 xml:id="sec-boot-parameters-list-network">
   <title>configurazione dell'interfaccia di rete</title>
   <important>
    <title>Configurazione dell'interfaccia di rete</title>
    <para>
     Le impostazioni descritte in questa sezione si riferiscono solo all'interfaccia di rete utilizzata nel corso dell'installazione. Configurare le interfacce di rete aggiuntive nel sistema installato seguendo le istruzioni fornite nel manuale <xref linkend="sec-network-manconf"/>.
    </para>
   </important>
   <para>
    La rete verrà configurata solo se necessario durante l'installazione. Per forzare la configurazione della rete, utilizzare il parametro <literal>netsetup</literal>.
   </para>
   <variablelist>
    <varlistentry>
     <term><option>netsetup=<replaceable>VALUE</replaceable></option></term>
     <listitem>
      <para>
       <literal>netsetup=dhcp</literal> forza la configurazione tramite DHCP. Impostare <literal>netsetup=-dhcp</literal> quando si configura la rete con i parametri di avvio <literal>hostip</literal>, <literal>gateway</literal> e <literal>nameserver</literal>. Con l'opzione <literal>netsetup=hostip,netmask,gateway,nameserver</literal> il programma di installazione richiede le impostazioni di rete durante l'avvio.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option> ifcfg=<replaceable>INTERFACE</replaceable>[.<replaceable>VLAN</replaceable>]=<replaceable>SETTINGS</replaceable>
     </option></term>
     <listitem>
      <para>
       <replaceable>INTERFACE</replaceable> può essere <literal>*</literal> per trovare la corrispondenza con tutte le interfacce o <literal>eth*</literal> per trovare ad esempio la corrispondenza con tutte le interfacce che iniziano con <literal>eth</literal>. È inoltre possibile utilizzare gli indirizzi MAC come valori.
      </para>
      <para>
       Facoltativamente, è possibile impostare VLAN dietro al nome dell'interfaccia, separata da un punto.
      </para>
      <para>
       Se <replaceable>SETTINGS</replaceable> è <literal>dhcp</literal>, tutte le interfacce corrispondenti vengono configurate con DHCP. È possibile impostare parametri statici. Con i parametri statici, viene configurata solo la prima interfaccia corrispondente. La sintassi per la configurazione statica è:
      </para>
      <screen>ifcfg=*="<replaceable>IPS_NETMASK</replaceable>,<replaceable>GATEWAYS</replaceable>,<replaceable>NAMESERVERS</replaceable>,<replaceable>DOMAINS</replaceable>"</screen>
      <para>
       Ogni valore separato da virgola può a sua volta contenere un elenco di valori separati da spazi. <replaceable>IPS_NETMASK</replaceable> è in <emphasis>CIDR notation</emphasis>, ad esempio <literal>10.0.0.1/24</literal>. Le virgolette sono necessarie solo quando si utilizzano elenchi separati da spazi. Esempio con due server dei nomi:
      </para>
      <screen>ifcfg=*="<replaceable>10.0.0.10/24</replaceable>,<replaceable>10.0.0.1</replaceable>,<replaceable>10.0.0.1 10.0.0.2</replaceable>,<replaceable>example.com</replaceable>"</screen>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>hostname=host.esempio.com</option>
     </term>
     <listitem>
      <para>
       Immettere il nome host completo.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>domain=example.com</option>
     </term>
     <listitem>
      <para>
       Percorso di ricerca dominio per DNS. Consente di utilizzare nomi host brevi anziché nomi completi.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>hostip=192.168.1.2[/24]</option>
     </term>
     <listitem>
      <para>
       Inserire l'indirizzo IP dell'interfaccia da configurare. L'IP può contenere la maschera di sottorete, ad esempio <literal>hostip=192.168.1.2/24</literal>. Questa impostazione viene valutata solo se è richiesta la rete durante l'installazione.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>gateway=192.168.1.3</option>
     </term>
     <listitem>
      <para>
       Specificare il gateway da utilizzare. Questa impostazione viene valutata solo se è richiesta la rete durante l'installazione.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>nameserver=192.168.1.4</option>
     </term>
     <listitem>
      <para>
       Specificare il server DNS attivo. Questa impostazione viene valutata solo se è richiesta la rete durante l'installazione.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>domain=example.com</option>
     </term>
     <listitem>
      <para>
       Percorso di ricerca del dominio. Questa impostazione viene valutata solo se è richiesta la rete durante l'installazione.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>

  <sect2 xml:id="sec-boot-parameters-list-install-source">
   <title>Specifica dell'origine di installazione</title>
   <para>
    Se non si utilizza il DVD per l'installazione, specificare un'origine di installazione alternativa.
   </para>
   <variablelist>
    <varlistentry>
     <term><option>install=SOURCE</option>
     </term>
     <listitem>
      <para>
       Specificare la posizione dell'origine dell'installazione da utilizzare. È possibile utilizzare i protocolli <literal>cd</literal>, <literal>hd</literal>, <literal>slp</literal>, <literal>nfs</literal>, <literal>smb</literal> (Samba/CIFS), <literal>ftp</literal>, <literal>tftp</literal>
       <literal>http</literal> e <literal>https</literal>. Non tutti i tipi di origine sono disponibili su tutte le piattaforme. Ad esempio IBM Z non supporta <literal>cd</literal> e <literal>hd</literal>.
      </para>
      <para>
       L'opzione di default è <literal>cd</literal>.
      </para>
      <para>
       Se viene definito un URL <literal>ftp</literal>, <literal>tftp</literal> o <literal>smb</literal>, specificare il nome utente e la password con l'URL. Questi parametri sono opzionali, quindi in caso non vengano definiti viene effettuato un login anonimo o come utente guest. Esempio:
      </para>
<screen>install=ftp://<replaceable>USER</replaceable>:<replaceable>PASSWORD</replaceable>@<replaceable>SERVER</replaceable>/<replaceable>DIRECTORY</replaceable>/DVD1/</screen>
      <para>
       Per eseguire l'installazione tramite una connessione cifrata, utilizzare un URL <literal>https</literal>. Se non è possibile verificare il certificato, utilizzare il parametro di avvio <literal>sslcerts=0</literal> per disabilitare la verifica del certificato.
      </para>
      <para>
       In caso di installazione Samba/CIFS è possibile anche specificare il dominio da utilizzare:
      </para>
<screen>install=smb://<replaceable>WORKDOMAIN</replaceable>;<replaceable>USER</replaceable>:<replaceable>PASSWORD</replaceable>@<replaceable>SERVER</replaceable>/<replaceable>DIRECTORY</replaceable>/DVD1/</screen>
      <para>
       Per utilizzare <literal>cd</literal>, <literal>hd</literal> o <literal>slp</literal>, impostarli come nell'esempio seguente:
      </para>
      <screen>install=cd:/
install=hd:/?device=<replaceable>sda/PATH_TO_ISO</replaceable>
install=slp:/</screen>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>
  <sect2 xml:id="sec-boot-parameters-list-remote-access">
   <title>Specifica dell'accesso remoto</title>
   <para>
    Si deve specificare un solo metodo di controllo remoto alla volta. I vari metodi disponibili sono: SSH, VNC, server X remoto. <phrase os="sles;sled">Per informazioni su come utilizzare i parametri elencati in questa sezione, vedere <xref linkend="cha-remote-installation"/>.</phrase>
   </para>
   <variablelist>
    <varlistentry>
     <term><option>display_ip=</option><replaceable>IP_ADDRESS</replaceable></term>
     <listitem>
      <para>
       <envar>Display_IP</envar> provoca l'installazione del sistema per tentare la connessione a un server X in un dato indirizzo.
      </para>
      <important>
       <title>meccanismo di autenticazione di X</title>
       <para>
        L'installazione diretta con il sistema X Window si fonda su un meccanismo di autenticazione primitivo basato sui nomi degli host. Questo meccanismo è disabilitato sulle versioni <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> correnti. È preferibile l'installazione con SSH o VNC.
       </para>
      </important>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>vnc=1</option></term>
     <listitem>
      <para>
       Abilita un server VNC durante l'installazione.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>vncpassword=</option><replaceable>PASSWORD</replaceable></term>
     <listitem>
      <para>
       Imposta la password per il server VNC.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>ssh=1</option></term>
     <listitem>
      <para>
       <envar>ssh</envar> abilita l'installazione SSH.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>ssh.password=</option><replaceable>PASSWORD</replaceable></term>
     <listitem>
      <para>
       Specifica una password SSH per l'utente root durante l'installazione.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>
 </sect1>

 <sect1 xml:id="sec-boot-parameters-advanced">
  <title>Configurazioni avanzate</title>
  <para>
   Per configurare l'accesso a un RMT locale o a un server <systemitem>supportconfig</systemitem> per l'installazione, è possibile specificare i parametri di avvio per configurare tali servizi durante l'installazione. Ciò vale anche se è necessario il supporto IPv6 durante l'installazione.
  </para>
  <sect2 xml:id="sec-boot-parameters-advanced-smt" os="sles;sled">
   <title>Immissione dei dati per accedere a un server SMT</title>
   <para>
    Per default, gli aggiornamenti di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> vengono distribuiti da SUSE Customer Center. Se la rete offre un server di tipo RMT per fornire un'origine di aggiornamento locale, sarà necessario fornire al client l'URL del server. Client e server comunicano esclusivamente tramite protocollo HTTPS, pertanto è anche necessario immettere un percorso al certificato del server, nel caso questo non sia stato rilasciato da un ente di certificazione esterno.
   </para>
   <note>
    <title>solo installazioni non interattive</title>
    <para>
     I parametri per l'accesso a un server RMT sono necessari solo per le installazioni non interattive. Per le installazioni interattive, i dati possono essere forniti durante l'installazione (per informazioni, vedere <xref linkend="sec-yast-install-scc-registration"/>).
    </para>
   </note>
   <variablelist>
    <varlistentry>
     <term><option>regurl</option></term>
     <listitem>
      <para>
       URL del server RMT. Questo URL presenta un formato fisso <literal>https://<replaceable>FQN</replaceable>/center/regsvc/</literal>. <replaceable>FQN</replaceable> deve essere il nome host completo del server RMT. Esempio:
      </para>
<screen>regurl=https://smt.example.com/center/regsvc/</screen>
      <para>
       Accertarsi che i valori immessi siano corretti. Se <literal>regurl</literal> non è stato specificato correttamente, la registrazione dell'origine di aggiornamento non riuscirà.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>regcert</option></term>
     <listitem>
      <para>
       Ubicazione del certificato del server RMT. Specificare una delle ubicazioni seguenti:
      </para>
      <variablelist>
       <varlistentry>
        <term>URL</term>
        <listitem>
         <para>
          Ubicazione remota (HTTP, HTTPS o FTP) dalla quale è possibile effettuare il download del certificato. Nel caso regcert non sia specificato, l'impostazione di default sarà <literal>http://<replaceable>FQN</replaceable>/smt.crt</literal> dove <literal>FQN</literal> è il nome del server RMT. Esempio:
         </para>
         <screen>regcert=http://rmt.example.com/smt-ca.crt</screen>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term>Percorso locale</term>
        <listitem>
         <para>
          Percorso assoluto per il certificato sul computer locale. Esempio:
         </para>
         <screen>regcert=/data/inst/smt/smt-ca.cert</screen>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term>Interattivo</term>
        <listitem>
         <para>
          Utilizzare <literal>ask</literal> per aprire un menu a comparsa durante l'installazione, dove specificare il percorso del certificato. Non utilizzare questa opzione con AutoYaST. Esempio
         </para>
         <screen>regcert=ask</screen>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term>Installazione del certificato di disattivazione</term>
        <listitem>
         <para>
          Utilizzare <literal>done</literal> se il certificato sarà installato da un prodotto aggiuntivo o se si utilizza un certificato emesso da un'autorità di certificazione ufficiale. Ad esempio:
         </para>
         <screen>regcert=done</screen>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>
  <sect2 xml:id="sec-boot-parameters-advanced-supportconfigurl" os="sles;sled">
   <title>Configurazione di un altro server dati per <systemitem>supportconfig</systemitem></title>
   <para>
    Per default, i dati raccolti da supportconfig (vedere la sezione <xref linkend="cha-adm-support"/> per ulteriori informazioni) vengono inviati a SUSE Customer Center. È possibile anche configurare un server locale per la raccolta di questi dati. Se tale server è disponibile in rete, è necessario impostare l'URL del server sul client. Questa informazione deve essere immessa al prompt d'avvio.
   </para>
   <formalpara>
    <title><literal>supporturl</literal></title>
    <para>
     URL del server. L'URL presenta il formato <literal>http://<replaceable>FQN</replaceable>/<replaceable>Path</replaceable>/</literal>, dove <replaceable>FQN</replaceable> è il nome host completo del server e <replaceable>Path</replaceable> è l'ubicazione sul server. Ad esempio:
    </para>
   </formalpara>
<screen>supporturl=http://support.example.com/supportconfig/data/</screen>
  </sect2>
  <sect2 xml:id="sec-boot-parameters-advanced-ipv6">
   <title>Utilizzo di IPv6 per l'installazione</title>
   <para>
    Per default è possibile solo assegnare indirizzi di rete IPv4 al computer. Per abilitare IPv6 durante l'installazione, immettere uno dei seguenti parametri al prompt di avvio:
   </para>
   <variablelist>
    <varlistentry>
     <term>Accetta IPv4 e IPv6</term>
     <listitem>
<screen>ipv6=1</screen>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Accetta solo IPv6</term>
     <listitem>
<screen>ipv6only=1</screen>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>
  <sect2 xml:id="sec-boot-parameters-advanced-proxy">
   <title>Utilizzo di un proxy per l'installazione</title>
   <para>
    Nelle reti che impongono l'uso di un server proxy per l'accesso a siti Web remoti, la registrazione durante l'installazione è possibile solo quando si configura un server proxy.
   </para>
   <para>
    Per utilizzare un proxy durante l'installazione, premere <keycap>F4</keycap> nella schermata di avvio e impostare i parametri richiesti nella finestra di dialogo <guimenu>Proxy HTTP</guimenu>. In alternativa, specificare il parametro kernel <literal>proxy</literal> al prompt di avvio:
   </para>
<screen>proxy=http://<replaceable>USER</replaceable>:<replaceable>PASSWORD</replaceable>@proxy.example.com:<replaceable>PORT</replaceable></screen>
   <para>
    L'impostazione di <replaceable>UTENTE</replaceable> e <replaceable>PASSWORD</replaceable> è facoltativa. Se il server consente l'accesso anonimo, sono sufficienti i dati seguenti: <literal>http://proxy.example.com:<replaceable>PORT</replaceable></literal>.
   </para>
  </sect2>
  <sect2 xml:id="sec-boot-parameters-advanced-selinux">
   <title>Abilitazione del supporto SELinux</title>
   <para>
    L'abilitazione di SELinux all'avvio dell'installazione consente di configurarlo al completamento dell'installazione senza riavviare. Utilizzare i seguenti parametri:
   </para>
<screen>security=selinux selinux=1</screen>
  </sect2>
  <sect2 xml:id="sec-boot-parameters-advanced-self-update">
   <title>Abilitazione dell'aggiornamento automatico del programma di installazione</title>
   <para>
    Durante l'installazione e l'upgrade, YaST può essere aggiornato automaticamente, come descritto nella <xref linkend="sec-yast-install-self-update"/>, per risolvere alcuni bug potenziali rilevati dopo il rilascio. È possibile utilizzare il parametro <literal>self_update</literal> per modificare il comportamento di questa funzione.
   </para>
   <para>
    Per abilitare l'aggiornamento automatico del programma di installazione, impostare il parametro su <literal>1</literal>:
   </para>
<screen>self_update=1</screen>
   <para>
    Per utilizzare un archivio definito dall'utente, specificare un URL:
   </para>
<screen>self_update=https://updates.example.com/</screen>
  </sect2>
  <sect2 xml:id="sec-boot-parameters-advanced-high-dpi">
   <title>Adattamento dell'interfaccia utente per l'alta risoluzione (DPI)</title>
   <para>
    Se la risoluzione (DPI) dello schermo è molto alta, utilizzare il parametro di avvio <literal>QT_AUTO_SCREEN_SCALE_FACTOR</literal>. In tal modo il font e gli elementi dell'interfaccia utente vengono adattati alla risoluzione dello schermo.
   </para>
   <screen>QT_AUTO_SCREEN_SCALE_FACTOR=1</screen>
  </sect2>
 </sect1>
 <sect1 xml:id="sec-boot-parameters-zseries" os="sles">
  <title>IBM Z</title>
   <para>
    Per piattaforme IBM Z, il sistema viene avviato (IPL, Initial Program Load) come descritto nella <xref linkend="sec-prep-ipling"/>. <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> non visualizza una schermata iniziale in questi sistemi. Durante l'installazione, caricare manualmente il kernel, initrd e parmfile. Quando viene stabilita una connessione al sistema di installazione tramite VNC, X o SSH, viene visualizzata la schermata di installazione di YaST. Dato che non vi è una schermata di avvio, i parametri del kernel o di avvio non possono essere immessi sullo schermo, ma devono essere specificati in un parmfile (vedere la <xref linkend="sec-appdendix-parm"/>).
   </para>
   <variablelist>
    <varlistentry>
     <term><option>InstNetDev=osa</option>
     </term>
     <listitem>
      <para>
       Inserire il tipo di interfaccia da configurare. I valori possibili sono <literal>osa</literal>, <literal>hsi</literal>, <literal>ctc</literal>, <literal>escon</literal> e <literal>iucv</literal> (CTC, ESCON e IUCV non sono più supportati ufficialmente).
      </para>
      <para>
       Per il tipo di interfacce <literal>hsi</literal> e <literal>osa</literal> specificare una maschera di rete appropriata e un indirizzo di diffusione opzionale:
      </para>
<screen>Netmask=255.255.255.0
Broadcast=192.168.255.255</screen>
      <para>
       Per le interfacce di tipo <literal>ctc</literal>, <literal>escon</literal> e <literal>iucv</literal> (CTC, ESCON e IUCV non sono più ufficialmente supportate), inserire l'indirizzo IP del peer:
      </para>
<screen>Pointopoint=192.168.55.20</screen>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>OsaInterface=&lt;lcs|qdio&gt;</option>
     </term>
     <listitem>
      <para>
       Per i dispositivi di rete <literal>osa</literal>, specificare l'interfaccia host (<literal>qdio</literal> o <literal>lcs</literal>).
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>Layer2=&lt;0|1&gt;</option>
     </term>
     <listitem>
      <para>
       Per i dispositivi Ethernet QDIO <literal>osa</literal> e <literal>hsi</literal>, specificare se abilitare (<literal>1</literal>) o disabilitare (<literal>0</literal>) il supporto per OSI Layer 2.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>OSAHWAddr=02:00:65:00:01:09</option>
     </term>
     <listitem>
      <para>
       Per i dispositivi QDIO Ethernet <literal>osa</literal> abilitati per Layer 2, specificare manualmente un indirizzo MAC o dichiarare <literal>OSAHWADDR= </literal> (con spazio bianco finale) per il default del sistema.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>PortNo=&lt;0|1&gt;</option>
     </term>
     <listitem>
      <para>
       Per i dispositivi di rete <literal>osa</literal> specificare il numero di porta (se il dispositivo supporta questa funzione). Il valore di default è 0.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
   <para>
    Ciascuna interfaccia richiede alcune opzioni di configurazione:
   </para>
   <itemizedlist>
    <listitem>
     <para>
      Interfacce <literal>ctc</literal> e <literal>escon</literal> (CTC e ESCON non sono più ufficialmente supportate):
     </para>
<screen>ReadChannel=0.0.0600
WriteChannel=0.0.0601</screen>
     <para>
      Il parametro <envar>ReadChannel</envar> specifica il canale di lettura da utilizzare. Il parametro <envar>WriteChannel</envar> è il canale di SCRITTURA.
     </para>
    </listitem>
    <listitem>
     <para>
      Per l'interfaccia <literal>ctc</literal> (non più supportata ufficialmente), specificare il protocollo da utilizzare:
     </para>
<screen>CTCProtocol=&lt;0/1/2&gt;</screen>
     <para>
      I valori validi sono:
     </para>
     <informaltable>
      <tgroup cols="2">
       <tbody>
        <row>
         <entry>
          <para>
           <literal>0</literal>
          </para>
         </entry>
         <entry>
          <para>
           Modalità di compatibilità, anche per peer non Linux diversi da OS/390 e z/OS (modalità di default)
          </para>
         </entry>
        </row>
        <row>
         <entry>
          <para>
           <literal>1</literal>
          </para>
         </entry>
         <entry>
          <para>
           Modalità estesa
          </para>
         </entry>
        </row>
        <row>
         <entry>
          <para>
           <literal>2</literal>
          </para>
         </entry>
         <entry>
          <para>
           Modalità di compatibilità con OS/390 e z/OS
          </para>
         </entry>
        </row>
       </tbody>
      </tgroup>
     </informaltable>
    </listitem>
    <listitem>
     <para>
      Tipo di dispositivo di rete <literal>osa</literal> con interfaccia <literal>lcs</literal>:
     </para>
<screen>ReadChannel=0.0.0124</screen>
     <para>
      Il parametro <envar>ReadChannel</envar> rappresenta il numero di canale utilizzato in questa configurazione. Da questo parametro è possibile definire un secondo numero di porta aggiungendo 1 al valore di <envar>ReadChannel</envar>. Il parametro <envar>Portnumber</envar> è utilizzato per specificare il relativo numero di porta.
     </para>
    </listitem>
    <listitem>
     <para>
      Interfaccia <literal>iucv</literal>:
     </para>
<screen>IUCVPeer=PEER</screen>
     <para>
      Inserire il nome del computer peer.
     </para>
    </listitem>
    <listitem>
     <para>
      Tipo di dispositivo di rete <literal>osa</literal> con interfaccia <literal>qdio</literal> per OSA-Express Gigabit Ethernet:
     </para>
<screen>ReadChannel=0.0.0700
WriteChannel=0.0.0701
DataChannel=0.0.0702</screen>
     <para>
      Per <envar>ReadChannel</envar>, inserire il numero del canale di lettura. Per <envar>WriteChannel</envar>, inserire il numero del canale di scrittura. Il parametro <envar>DataChannel</envar> specifica il canale dati. Assicurarsi che il canale di lettura presenti un numero di dispositivo pari.
     </para>
    </listitem>
    <listitem>
     <para>
      Interfaccia <literal>hsi</literal> per HiperSockets e LAN guest per VM:
     </para>
<screen>ReadChannel=0.0.0800
WriteChannel=0.0.0801
DataChannel=0.0.0802</screen>
     <para>
      Per <envar>ReadChannel</envar>, inserire il numero appropriato del canale di lettura. Per <envar>WriteChannel</envar> e <envar>DataChannel</envar>, inserire rispettivamente i numeri dei canali di lettura e dati.
     </para>
    </listitem>
   </itemizedlist>
 </sect1>
 <sect1 xml:id="sec-boot-parameters-more-information">
  <title>Ulteriori informazioni</title>
  <para>
   Ulteriori informazioni sui parametri di avvio sono disponibili nel wiki openSUSE in <link xlink:href="https://en.opensuse.org/SDB:Linuxrc#Parameter_Reference"/>.
  </para>
 </sect1>
</chapter>
