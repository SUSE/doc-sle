<?xml version="1.0" encoding="UTF-8"?>
<sect1 xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="deployment_prep_power_preparing.xml" version="5.0" xml:id="cha.power.prep">
 <title>Preparazione</title>

 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>sì</dm:translation>
  </dm:docmanager>
 </info>

 <para>
  In questa sezione viene illustrata la procedura di preparazione che deve essere eseguita prima dell'installazione effettiva di SUSE Linux Enterprise Server. La procedura di installazione dipende dal sistema utilizzato. Sono supportati i metodi seguenti:
 </para>

 <itemizedlist>
  <listitem>
   <para>
    <xref linkend="sec.prep.powerlinux.powerkvm.kimchi" xrefstyle="select:title nopage"/>
   </para>
  </listitem>
  <listitem>
   <para>
    <xref linkend="sec.prep.powerlinux.powerkvm.virt-install" xrefstyle="select:title nopage"/>
   </para>
  </listitem>
  <listitem>
   <para>
    <xref linkend="sec.prep.powerlinux.powerkvm.ivm" xrefstyle="select:title nopage"/>
   </para>
  </listitem>
  <listitem>
   <para>
    <xref linkend="sec.prep.powerlinux.powerkvm.bare_metal" xrefstyle="select:title nopage"/>
   </para>
  </listitem>
 </itemizedlist>

 <para>
  Se occorre installare SUSE® Linux Enterprise Server in numerosi sistemi o partizioni, è consigliabile creare un'origine per l'installazione dalla rete. La stessa origine può essere utilizzata per l'installazione contemporanea su più partizioni o più sistemi. La configurazione dell'origine per l'installazione da rete viene descritta nella <xref linkend="sec.deployment.instserver.sles9"/>.
 </para>

 <sect2 version="5.0" xml:id="sec.prep.powerlinux.powerkvm.kimchi">
  <title>Installazione nei server con IBM PowerKVM utilizzando Kimchi</title>
  <para>
   In questa sezione viene illustrata la procedura di preparazione dell'installazione nei sistemi IBM PowerLinux con PowerKVM. Viene descritta l'installazione da un'immagine ISO con l'interfaccia Web Kimchi. Kimchi è uno strumento per l'amministrazione di IBM PowerKVM.
  </para>
  <para>
   Questa sezione parte dal presupposto che PowerKVM sia in esecuzione su un server IBM PowerLinux. Se PowerKVM non è preinstallato, vedere la sezione <quote>Configuring IBM PowerKVM on Power Systems</quote> all'indirizzo <link xlink:href="http://www.ibm.com/support/knowledgecenter/linuxonibm/liabp/liabpkickoff.htm"/> per informazioni sull'installazione e sulla configurazione di PowerKVM.
  </para>
  <sect3 xml:id="sec.prep.powerlinux.powerkvm.template">
   <title>Creazione di un modello di SUSE Linux Enterprise Server con Kimchi</title>
   <para>
    I modelli sono l'origine di installazione per i guest PowerKVM. È possibile creare un modello, modificare un modello esistente o clonare un modello. È possibile clonare un modello da un guest esistente solo se quest'ultimo è disattivato.
   </para>
   <procedure>
    <title>creazione di un modello con Kimchi</title>
    <step>
     <para>
      Nel browser Web immettere l'URL del server PowerLinux in cui viene eseguito PowerKVM, ad esempio <literal>https://<replaceable>POWERLINUX_IP</replaceable>:8001</literal> (sostituire <replaceable>POWERLINUX_IP</replaceable> con l'indirizzo IP del sistema in uso).
     </para>
    </step>
    <step>
     <para>
      Fare clic sulla scheda <guimenu>Modelli</guimenu> per attivare la pagina <guimenu>Modelli</guimenu>.
     </para>
    </step>
    <step>
     <para>
      Fare clic sul segno più verde (<guimenu>+</guimenu>) per creare il modello di SUSE Linux Enterprise Server.
     </para>
    </step>
    <step>
     <para>
      Nella finestra di dialogo <guimenu>Aggiungi modello</guimenu> selezionare una delle opzioni seguenti:
     </para>
     <variablelist>
      <varlistentry>
       <term><guimenu>Immagine ISO locale</guimenu>
       </term>
       <listitem>
        <para>
         Selezionare questa opzione per effettuare la scansione dei pool di archiviazione per l'installazione delle immagini ISO disponibili nel sistema.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term><guimenu>File di immagine locale</guimenu>
       </term>
       <listitem>
        <para>
         Selezionare questa opzione per specificare il percorso di un file di immagine locale.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term><guimenu>File ISO remoto</guimenu>
       </term>
       <listitem>
        <para>
         Selezionare questa opzione per specificare il percorso remoto di un'immagine ISO di installazione.
        </para>
       </listitem>
      </varlistentry>
     </variablelist>
    </step>
    <step>
     <para>
      Selezionare il file ISO che si desidera utilizzare per creare un guest e fare clic su <guimenu>Crea modelli da file ISO selezionato</guimenu>.
     </para>
    </step>
    <step>
     <para>
      Per configurare il modello appena creato, fare clic su <menuchoice><guimenu>Azioni</guimenu>
      <guimenu>Modifica</guimenu></menuchoice> e modificare i valori di default in base ai requisiti del workload.
     </para>
     <informalfigure>
      <mediaobject>
       <imageobject role="fo">
        <imagedata fileref="kimchi_create_template.png" width="50%"/>
       </imageobject>
       <imageobject role="html">
        <imagedata fileref="kimchi_create_template.png" width="50%"/>
       </imageobject>
      </mediaobject>
     </informalfigure>
    </step>
   </procedure>
   <para>
    Per ulteriori informazioni, vedere <quote>Setting up a template using Kimchi</quote> all'indirizzo <link xlink:href="http://www.ibm.com/support/knowledgecenter/linuxonibm/liabp/liabpkimchitemplate.htm"/>.
   </para>
  </sect3>
  <sect3 xml:id="sec.prep.powerlinux.powerkvm.install">
   <title>Installazione di SUSE Linux Enterprise Server come guest con Kimchi</title>
   <para/>
   <procedure>
    <step>
     <para>
      Nel browser Web immettere l'URL del server PowerLinux in cui viene eseguito PowerKVM, ad esempio <literal>https://<replaceable>POWERLINUX_IP</replaceable>:8001</literal> (sostituire <replaceable>POWERLINUX_IP</replaceable> con l'indirizzo IP del sistema in uso).
     </para>
    </step>
    <step>
     <para>
      Fare clic sulla scheda <guimenu>Guest</guimenu> per attivare la pagina <guimenu>Guest</guimenu>.
     </para>
    </step>
    <step>
     <para>
      Fare clic sul segno più verde (<guimenu>+</guimenu>) per creare il guest di SUSE Linux Enterprise Server.
     </para>
    </step>
    <step>
     <para>
      Immettere un <guimenu>Nome macchina virtuale</guimenu> per il guest di SUSE Linux Enterprise Server.
     </para>
     <para>
      Scegliere il modello di SUSE Linux Enterprise Server creato in <xref linkend="sec.prep.powerlinux.powerkvm.template"/> e fare clic su <guimenu>Crea</guimenu>.
     </para>
     <informalfigure>
      <mediaobject>
       <imageobject role="fo">
        <imagedata fileref="kimchi_create_guest.png" width="70%"/>
       </imageobject>
       <imageobject role="html">
        <imagedata fileref="kimchi_create_guest.png" width="70%"/>
       </imageobject>
      </mediaobject>
     </informalfigure>
    </step>
    <step>
     <para>
      Al termine della creazione il guest è pronto per essere avviato. Fare clic sul pulsante di alimentazione rosso per avviare il guest di SUSE Linux Enterprise Server. In alternativa, selezionare <menuchoice><guimenu>Azioni</guimenu>
      <guimenu> Avvia</guimenu></menuchoice>.
     </para>
    </step>
    <step>
     <para>
      Fare clic su <menuchoice><guimenu> Azioni</guimenu>
      <guimenu>Connetti</guimenu></menuchoice> , quindi connettere il visualizzatore VNC al processo di installazione, come illustrato in <xref linkend="sec.deployment.remoteinst.monitor.vnc.connect"/>.
     </para>
    </step>
   </procedure>
   <tip>
    <title>Creazione di più guest</title>
    <para>
     Per creare più guest di tipo simile, selezionare <guimenu>Clona</guimenu> dal menu <guimenu>Azioni</guimenu> di un guest esistente.
    </para>
   </tip>
   <para>
    Ora è possibile continuare con l'installazione di default tramite VNC, come spiegato in <xref linkend="sec.i.yast2.steps"/>.
   </para>
  </sect3>
 </sect2>

 <sect2 xml:id="sec.prep.powerlinux.powerkvm.virt-install">
  <title>
   Installazione nei server con IBM PowerKVM utilizzando <command>virt-install</command>
  </title>
  <para>
   In alternativa all'installazione con Kimchi, per eseguire l'installazione nei server con IBM PowerKVM è possibile utilizzare lo strumento da riga di comando <command>virt-install</command>, che risulta particolarmente utile quando occorre installare più macchine virtuali nei sistemi IBM PowerLinux Server. <command>virt-install</command> consente più scenari di installazione. Di seguito viene illustrato uno scenario di installazione remota tramite avvio PXE e VNC. Per ulteriori informazioni su <command>virt-install</command>, vedere <xref linkend="sec.libvirt.inst.virt-install"/>.
  </para>
  <para>
   Preparare un archivio con le origini di installazione e il sistema di destinazione abilitato all'avvio PXE come descritto in <xref linkend="sec.deployment.remoteinst.scenario.vncpxe"/>.
  </para>
  <para>
   Nella riga di comando immettere comandi simili ai seguenti (modificare le opzioni in base alle esigenze e all'hardware in dotazione):
  </para>

<screen>virt-install --name server_sle12 --memory 4096 --vcpus=2 --pxe \
--graphics vnc --os-variant sles11 \
--disk pool=default,size=3000,format=qcow2,allocation=1G,bus=virtio \
-w mac=<replaceable>mac_address</replaceable>,model=spapr-vlan</screen>
  <para>
   Vengono utilizzate le immagini VNC e viene avviato automaticamente il client grafico. Completare l'installazione come spiegato nella <xref linkend="sec.i.yast2.steps"/>.
  </para>
 </sect2>

 <sect2 xml:id="sec.prep.powerlinux.powerkvm.ivm">
  <title>
   Installazione in una partizione tramite IVM
  </title>
  <para>
   In questa guida viene illustrato come installare <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> in una partizione di un server Power Systems, utilizzando l'interfaccia Web Integrated Virtualization Manager (IVM). Prima di iniziare l'installazione, verificare che i requisiti seguenti siano soddisfatti:
  </para>
  <itemizedlist>
   <listitem>
    <para>
     Il sistema Linux on Power è attivo
    </para>
   </listitem>
   <listitem>
    <para>
     Virtual I/O Server è installato
    </para>
   </listitem>
   <listitem>
    <para>
     L'interfaccia IVM è inizialmente configurata
    </para>
   </listitem>
  </itemizedlist>
  <procedure>
   <title>Eseguire il login all'interfaccia Web IVM</title>
   <step>
    <para>
     Aprire una finestra del browser Web e connettersi, utilizzando il protocollo HTTP o HTTPS, all'indirizzo IP assegnato a IVM durante il processo di installazione (ad esempio https://<replaceable>IP_ADDRESS</replaceable>). Viene visualizzata la finestra di benvenuto.
    </para>
   </step>
   <step>
    <para>
     Eseguire il login come <systemitem class="username">padmin</systemitem>, specificando la password definita durante il processo di installazione. Viene visualizzata l'interfaccia IVM.
    </para>
   </step>
   <step>
    <para>
     Selezionare <guimenu>Visualizza/Modifica connessione Ethernet virtuale</guimenu>.
    </para>
   </step>
   <step>
    <para>
     Fare clic su <guimenu>Inizializza connessione Ethernet virtuale</guimenu> per fornire connettività Ethernet fra le partizioni.
    </para>
   </step>
   <step>
    <para>
     Terminata l'inizializzazione della connessione Ethernet virtuale fare clic su Applica.
    </para>
   </step>
   <step>
    <para>
     Se l'installazione richiede networking esterno, creare un bridge Ethernet virtuale
    </para>
    <substeps>
     <step>
      <para>
       Selezionare la scheda <guimenu>Bridge Ethernet virtuale</guimenu>.
      </para>
     </step>
     <step>
      <para>
       Selezionare l'adattatore fisico per cui creare il bridge e fare clic su <guimenu>Applica</guimenu>.
      </para>
     </step>
    </substeps>
   </step>
  </procedure>
  <para>Creare quindi una partizione attenendosi alla procedura seguente:</para>
  <procedure>
   <title>Creazione di una partizione</title>
   <step>
    <para>
     Nell'interfaccia Web IVM fare clic su <menuchoice><guimenu>Visualizza/Modifica partizione</guimenu> <guimenu> Crea partizione</guimenu></menuchoice>.
    </para>
   </step>
   <step>
    <para>
     Immettere un nome per la partizione. Per procedere con il passaggio successivo, fare clic su <guimenu>Avanti</guimenu> nel passaggio corrente e in quelli che seguono.
    </para>
   </step>
   <step>
    <para>
     Specificare la memoria per la partizione. Se è stato creato un pool di memoria condivisa, le partizioni create possono condividere la memoria. In caso contrario selezionare <guimenu>Dedicata</guimenu>.
    </para>
   </step>
   <step>
    <para>
     Specificare il numero di processori e la modalità di elaborazione per la partizione.
    </para>
   </step>
   <step>
    <para>
     Specificare una connessione Ethernet virtuale per la partizione. Se non si desidera configurare un adattatore, selezionare <guimenu>Nessuno</guimenu> per la connessione Ethernet virtuale.
    </para>
   </step>
   <step>
    <para>
     Creare un nuovo disco virtuale o assegnare volumi fisici e dischi virtuali esistenti che non risultano attualmente assegnati ad alcuna partizione.
    </para>
   </step>
   <step>
    <para>
     Verificare il nome del disco virtuale<guimenu/> e il nome del pool di memorizzazione<guimenu/> per il nuovo disco, quindi specificare la dimensione del disco virtuale<guimenu/>.
    </para>
   </step>
   <step>
    <para>
     Configurare i dispositivi ottici per la partizione. A tale scopo, espandere <guimenu>Dispositivi ottici fisici</guimenu> e <guimenu>Dispositivi ottici virtuali</guimenu>, quindi selezionare i dispositivi che si desidera assegnare alla partizione.
    </para>
   </step>
   <step>
    <para>
     Verificare le impostazioni di configurazione della partizione e fare clic su <guimenu>Fine</guimenu>. La partizione viene creata e inclusa nell'elenco <guimenu>Visualizza/Modifica partizioni</guimenu>.
    </para>
   </step>
  </procedure>
  <para>Attivare quindi la partizione appena creata:</para>
  <procedure>
   <title>Attivare la partizione</title>
   <step>
    <para>
     Nell'interfaccia Web IVM fare clic su <guimenu>Visualizza/Modifica partizione</guimenu> e selezionare la casella di controllo accanto alla partizione che si desidera attivare.
    </para>
   </step>
   <step>
    <para>
     Selezionare <guimenu>Altre attività</guimenu>.
    </para>
   </step>
   <step>
    <para>
     Selezionare <guimenu>Apri una finestra di terminale</guimenu>.
    </para>
   </step>
   <step>
    <para>
     Fare clic su <guimenu>Attiva</guimenu> accanto alla partizione.
    </para>
   </step>
   <step>
    <para>
     Nella finestra di terminale immettere <keycap>1</keycap> per avviare i servizi di gestione dei sistemi (SMS, System Management Services).
    </para>
   </step>
  </procedure>
  <para>
   La configurazione del computer è terminata e ora è possibile avviare l'installazione:
  </para>
  <procedure>
   <title>Avviare l'installazione Linux</title>
   <step>
    <para>
     Nella finestra <guimenu>Selezione avvio</guimenu> immettere <keycap>1</keycap> per selezionare <guimenu>Menu SMS</guimenu>. Immettere <keycap>1</keycap> prima che la schermata di avvio del firmware sia completamente visualizzata, perché scompare immediatamente appena viene completata. Se tale schermata è già stata superata, riavviare il sistema.
    </para>
   </step>
   <step>
    <para>
     A questo punto è possibile inserire il supporto di Virtual I/O Server (VIOS) nell'unità disco.
    </para>
   </step>
   <step>
    <para>
     Immettere <keycap>2</keycap> per procedere con l'immissione della password nel menu <guimenu>Selezione lingua</guimenu>. Immettere la password per l'ID admin.
    </para>
   </step>
   <step>
    <para>
     Nel menu SMS principale immettere <keycap>5</keycap> per scegliere <guimenu>Seleziona opzioni di avvio</guimenu>.
    </para>
   </step>
   <step>
    <para>
     Immettere <keycap>1</keycap> per selezionare <guimenu>Dispositivo di installazione/avvio</guimenu>.
    </para>
   </step>
   <step>
    <para>
     Immettere <keycap>7</keycap> per visualizzare tutti i dispositivi di avvio disponibili.
    </para>
   </step>
   <step>
    <para>
     Immettere il numero corrispondente al dispositivo che si desidera utilizzare. Se tale dispositivo non compare nell'elenco, immettere <keycap>N</keycap> per visualizzarne altri.
    </para>
   </step>
   <step>
    <para>
     Immettere <keycap>2</keycap> per eseguire un avvio in modalità normale<guimenu/>.
    </para>
   </step>
   <step>
    <para>
     Immettere <keycap>1</keycap> per uscire dal menu SMS e iniziare il processo di avvio.
    </para>
   </step>
   <step>
    <para>
     Al prompt di avvio del programma di installazione digitare
    </para>
    <screen>install vnc=1
vncpassword=<replaceable>VNC_PASSWORD</replaceable></screen>
    <para>
     sostituendo <replaceable>VNC_PASSWORD</replaceable> con una password a scelta (contenente almeno 8 caratteri), quindi premere <keycap function="enter"/> per avviare l'installazione di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>. Avrà inizio il caricamento del kernel.
    </para>
   </step>
  </procedure>
  <para>
   Una volta iniziato il caricamento del kernel, il programma di installazione deve ottenere alcune informazioni dal sistema per configurare una sessione VNC. Per utilizzare VNC è necessario disporre di uno stack TCP/IP valido. Utilizzare DHCP o specificare manualmente le informazioni di networking, basandosi sulle indicazioni fornite dal programma di installazione.
  </para>
  <procedure>
   <title>Avviare la sessione VNC</title>
   <step>
    <para>
     Nella finestra <guimenu>Dispositivo di rete</guimenu> selezionare <guimenu>eth0</guimenu> come dispositivo di rete. Selezionare <guimenu>OK</guimenu> e premere <keycap function="enter"/>.
    </para>
   </step>
   <step>
    <para>
     Testare i supporti di installazione. In alternativa, selezionare <guimenu>Salta</guimenu> per procedere senza testare i supporti.
    </para>
   </step>
   <step>
    <para>
     Dopo l'avvio del server VNC da parte del sistema viene visualizzato un messaggio per la connessione del client VNC in uso, seguito da un indirizzo IP. Annotare l'indirizzo IP.
    </para>
   </step>
   <step>
    <para>
     Avviare un client VNC sul proprio computer portatile o PC. Immettere l'indirizzo IP del passaggio precedente seguito da <literal>:1</literal>, ad esempio 192.168.2.103:1.
    </para>
   </step>
   <step>
    <para>
     Completare l'installazione come spiegato nella <xref linkend="sec.i.yast2.steps"/>.
    </para>
   </step>
  </procedure>
 </sect2>
 <sect2 xml:id="sec.prep.powerlinux.powerkvm.bare_metal">
  <title>
   Installazione in server privi di livello di astrazione Open Power
  </title>
  <para>
   Utilizzare tali informazioni per installare Linux tramite una console seriale oppure tramite un monitor con tastiera collegato a un server Power Systems. Questa procedura di installazione presuppone l'utilizzo di un sistema non gestito (autonomo) pronto per l'avvio.
  </para>
  <procedure>
   <step>
    <para>
     Accendere il sistema selezionando <guimenu>Accendi</guimenu> dal menu <guimenu>Accendi/Spegni sistema</guimenu>. Quando viene richiesto se si desidera continuare a utilizzare la console, immettere <keycap>0</keycap> per confermare.
    </para>
   </step>
   <step>
    <para>
     Inserire il supporto di installazione di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> nell'unità disco.
    </para>
   </step>
   <step>
    <para>
     Nella finestra <guimenu>Seleziona lingua</guimenu> immettere <keycap>2</keycap> per continuare la procedura di avvio.
    </para>
   </step>
   <step>
    <para>
     Immettere <keycap>1</keycap> per accettare il contratto di licenza.
    </para>
   </step>   <step>
    <para>
     Nella finestra <guimenu>Selezione avvio</guimenu> immettere <keycap>1</keycap> per selezionare <guimenu>Menu SMS</guimenu>. Immettere <keycap>1</keycap> prima che la schermata di avvio del firmware sia completamente visualizzata, perché scompare immediatamente appena viene completata. Se tale schermata è già stata superata, riavviare il sistema.
    </para>
   </step>
   <step>
    <para>
     Immettere <keycap>2</keycap> per procedere con l'immissione della password nel menu <guimenu>Selezione lingua</guimenu>. Immettere la password per l'ID admin.
    </para>
   </step>
   <step>
    <para>
     Nel menu SMS principale immettere <keycap>5</keycap> per scegliere <guimenu>Seleziona opzioni di avvio</guimenu>.
    </para>
   </step>
   <step>
    <para>
     Immettere <keycap>7</keycap> per visualizzare tutti i dispositivi di avvio disponibili.
    </para>
   </step>
   <step>
    <para>
     Immettere il numero corrispondente al dispositivo che si desidera utilizzare. Se tale dispositivo non compare nell'elenco, immettere <keycap>N</keycap> per visualizzarne altri.
    </para>
   </step>
   <step>
    <para>
     Immettere <keycap>2</keycap> per eseguire un avvio in modalità normale<guimenu/>.
    </para>
   </step>   <step>
    <para>
     Immettere <keycap>1</keycap> per uscire dal menu SMS e iniziare il processo di avvio.
    </para>
   </step>
   <step>
    <para>
     Al prompt di avvio del programma di installazione digitare
    </para>
    <screen>install vnc=1
vncpassword=<replaceable>VNC_PASSWORD</replaceable></screen>
    <para>
     sostituendo <replaceable>VNC_PASSWORD</replaceable> con una password a scelta (contenente almeno 8 caratteri), quindi premere <keycap function="enter"/> per avviare l'installazione di <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>. Avrà inizio il caricamento del kernel.
    </para>
   </step>
  </procedure>

  <para>
   Una volta iniziato il caricamento del kernel, il programma di installazione deve ottenere alcune informazioni dal sistema per configurare una sessione VNC. Per utilizzare VNC è necessario disporre di uno stack TCP/IP valido. Utilizzare DHCP o specificare manualmente le informazioni di networking, basandosi sulle indicazioni fornite dal programma di installazione.
  </para>
  <procedure>
   <title>Avviare la sessione VNC</title>
   <step>
    <para>
     Nella finestra <guimenu>Dispositivo di rete</guimenu> selezionare <guimenu>eth0</guimenu> come dispositivo di rete. Selezionare <guimenu>OK</guimenu> e premere <keycap function="enter"/>.
    </para>
   </step>
   <step>
    <para>
     Testare i supporti di installazione. In alternativa, selezionare <guimenu>Salta</guimenu> per procedere senza testare i supporti.
    </para>
   </step>
   <step>
    <para>
     Dopo l'avvio del server VNC da parte del sistema viene visualizzato un messaggio per la connessione del client VNC in uso, seguito da un indirizzo IP. Annotare l'indirizzo IP.
    </para>
   </step>
   <step>
    <para>
     Avviare un client VNC sul proprio computer portatile o PC. Immettere l'indirizzo IP del passaggio precedente seguito da <literal>:1</literal>, ad esempio 192.168.2.103:1.
    </para>
   </step>
   <step>
    <para>
     Completare l'installazione come spiegato nella <xref linkend="sec.i.yast2.steps"/>.
    </para>
   </step>
  </procedure>
 </sect2>

</sect1>
