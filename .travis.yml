language: generic
dist: trusty
os: linux

addons:
  apt:
    packages:
    # Necessary to get xmllint available
    - libxml2-utils

script:
    - find ./xml -name "*.xml" | xargs xmllint --noout --noent
