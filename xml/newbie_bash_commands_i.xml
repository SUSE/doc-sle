<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1
[
  <!ENTITY % entities SYSTEM "entity-decl.ent">
    %entities;
]>
<sect1 version="5.0" xml:lang="en" xml:id="sec.shell.commands"
  xmlns="http://docbook.org/ns/docbook"
  xmlns:xi="http://www.w3.org/2001/XInclude"
  xmlns:xlink="http://www.w3.org/1999/xlink">
 <title>Important Linux Commands</title><indexterm xml:id="idx.Shell_Befehle" class="startofrange">

 <primary>shells</primary>

 <secondary>commands</secondary></indexterm><indexterm xml:id="idx.Befehle" class="startofrange">

 <primary>commands</primary></indexterm>

 <para>
  This section gives insight into the most important commands. There are
  many more commands than listed in this chapter. Along with the individual
  commands, parameters are listed and, where appropriate, a typical sample
  application is introduced. To learn more about the various commands, use
  the manual pages, accessed with <command>man</command> followed by the
  name of the command, for example, <command>man ls</command>. <indexterm>
  <primary>commands</primary>
  <secondary>man</secondary>
  </indexterm> <indexterm>
  <primary>help</primary>
  <secondary>man pages</secondary>
  </indexterm> <indexterm>
  <primary>man pages</primary>
  </indexterm>
 </para>

 <para>
  Man pages are displayed directly in the shell. To navigate them, move up
  and down with <keycap function="pageup"/> and
  <keycap function="pagedown"/>. Move between the beginning and the end of a
  document with <keycap function="home"/> and <keycap function="end"/>. End
  this viewing mode by pressing <keycap>Q</keycap>. Learn more about the
  <command>man</command> command itself with <command>man man</command>.
 </para>

 <para>
  In the following overview, the individual command elements are written in
  different typefaces. The actual command and its mandatory options are
  always printed as <command>command option</command>. Specifications or
  parameters that are not required are placed in <option>[square
  brackets]</option>.
 </para>

 <para>
  Adjust the settings to your needs. It makes no sense to write <command>ls
  file</command> if no file named <filename>file</filename> actually exists.
  You can usually combine several parameters, for example, by writing
  <command>ls -la</command> instead of <command>ls -l -a</command>.
 </para>

 <sect2 xml:id="sec.commands.file">
  <title>File Commands</title>
  <para>
   The following section lists the most important commands for file
   management. It covers everything from general file administration to the
   manipulation of file system ACLs.
  </para>
  <sect3 xml:id="sec.shell.fileadmin">
   <title>File Administration</title>
   <variablelist>
    <varlistentry>
     <term><command>ls</command>&#x20;<option>[options]</option>&#x20;<option>[files]</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>ls</secondary></indexterm><indexterm>
      <primary>ls</primary></indexterm>
      <para>
       If you run <command>ls</command> without any additional parameters,
       the program lists the contents of the current directory in short
       form.
      </para>
      <variablelist>
       <varlistentry>
        <term><option>-l</option>
        </term>
        <listitem>
         <para>
          Detailed list
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-a</option>
        </term>
        <listitem>
         <para>
          Displays hidden files
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>cp</command>&#x20;<option>[options]</option>&#x20;<option>source target</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>cp</secondary></indexterm><indexterm>
      <primary>cp</primary></indexterm><indexterm>
      <primary>files</primary>
      <secondary>copying</secondary></indexterm>
      <para>
       Copies <filename>source</filename> to <filename>target</filename>.
      </para>
      <variablelist>
       <varlistentry>
        <term>-i</term>
        <listitem>
         <para>
          Waits for confirmation, if necessary, before an existing
          <literal>target</literal> is overwritten
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term>-r</term>
        <listitem>
         <para>
          Copies recursively (includes subdirectories)
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>mv</command>&#x20;<option>[options]</option>&#x20;<option>source target</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>mv</secondary></indexterm><indexterm>
      <primary>mv</primary></indexterm><indexterm>
      <primary>files</primary>
      <secondary>moving</secondary></indexterm>
      <para>
       Copies <filename>source</filename> to <filename>target</filename>
       then deletes the original <filename>source</filename>.
      </para>
      <variablelist>
       <varlistentry>
        <term>-b</term>
        <listitem>
         <para>
          Creates a backup copy of the <filename>source</filename> before
          moving
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term>-i</term>
        <listitem>
         <para>
          Waits for confirmation, if necessary, before an existing
          <filename>targetfile</filename> is overwritten
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>rm</command>&#x20;<option>[options]</option>&#x20;<option>files</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>rm</secondary></indexterm><indexterm>
      <primary>rm</primary></indexterm><indexterm>
      <primary>files</primary>
      <secondary>deleting</secondary></indexterm>
      <para>
       Removes the specified files from the file system. Directories are not
       removed by <command>rm</command> unless the option
       <option>-r</option> is used.
      </para>
      <variablelist>
       <varlistentry>
        <term><option>-r</option>
        </term>
        <listitem>
         <para>
          Deletes any existing subdirectories
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-i</option>
        </term>
        <listitem>
         <para>
          Waits for confirmation before deleting each file
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>ln</command>&#x20;<option>[options]</option>&#x20;<option>source</option>&#x20;<option>target</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>ln</secondary></indexterm><indexterm>
      <primary>ln</primary></indexterm>
      <para>
       Creates an internal link from <option>source</option> to
       <option>target</option>. Normally, such a link points directly to
       <option>source</option> on the same file system. However, if
       <command>ln</command> is executed with the <option>-s</option>
       option, it creates a symbolic link that only points to the directory
       in which <filename>source</filename> is located, enabling linking
       across file systems.
      </para>
      <variablelist>
       <varlistentry>
        <term>-s</term>
        <listitem>
         <para>
          Creates a symbolic link
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>cd</command>&#x20;<option>[options]</option>&#x20;<filename>[directory]</filename>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>cd</secondary></indexterm><indexterm>
      <primary>cd</primary></indexterm><indexterm>
      <primary>directories</primary>
      <secondary>changing</secondary></indexterm>
      <para>
       Changes the current directory. <command>cd</command> without any
       parameters changes to the user's home directory.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>mkdir</command>&#x20;<option>[options]</option>&#x20;<option>directory</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>mkdir</secondary></indexterm><indexterm>
      <primary>mkdir</primary></indexterm><indexterm>
      <primary>directories</primary>
      <secondary>creating</secondary></indexterm>
      <para>
       Creates a new directory.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>rmdir</command>&#x20;<option>[options]</option>&#x20;<option>directory</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>rmdir</secondary></indexterm><indexterm>
      <primary>rmdir</primary></indexterm><indexterm>
      <primary>directories</primary>
      <secondary>deleting</secondary></indexterm>
      <para>
       Deletes the specified directory if it is already empty.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>chown</command>&#x20;<option>[options] username[:[group]]</option>&#x20;<filename>files</filename>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>chown</secondary></indexterm><indexterm>
      <primary>chown</primary></indexterm>
      <para>
       Transfers ownership of a file to the user with the specified
       username.
      </para>
      <variablelist>
       <varlistentry>
        <term><option>-R</option>
        </term>
        <listitem>
         <para>
          Changes files and directories in all subdirectories
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>chgrp</command>&#x20;<option>[options]</option>&#x20;<option>groupname</option>&#x20;<filename>files</filename>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>chgrp</secondary></indexterm><indexterm>
      <primary>chgrp</primary></indexterm>
      <para>
       Transfers the group ownership of a given <filename>file</filename> to
       the group with the specified group name. The file owner can change
       group ownership only if a member of both the current and the new
       group.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>chmod</command>&#x20;<option>[options]</option>&#x20;<option>mode</option>&#x20;<option>files</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>chmod</secondary></indexterm><indexterm>
      <primary>chmod</primary></indexterm><indexterm>
      <primary>permissions</primary>
      <secondary>changing</secondary></indexterm>
      <para>
       Changes the access permissions.
      </para>
      <para>
       The <literal>mode</literal> parameter has three parts:
       <literal>group</literal>, <literal>access</literal>, and
       <literal>access type</literal>. <literal>group</literal> accepts the
       following characters:
      </para>
      <variablelist>
       <varlistentry>
        <term>u</term>
        <listitem>
         <para>
          User
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term>g</term>
        <listitem>
         <para>
          Group
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term>o</term>
        <listitem>
         <para>
          Others
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
      <para>
       For <literal>access</literal>, grant access with <literal>+</literal>
       and deny it with <literal>-</literal>.
      </para>
      <para>
       The <literal>access type</literal> is controlled by the following
       options:
      </para>
      <variablelist>
       <varlistentry>
        <term>r</term>
        <listitem>
         <para>
          Read
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term>w</term>
        <listitem>
         <para>
          Write
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term>x</term>
        <listitem>
         <para>
          Execute&mdash;executing files or changing to the directory
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term>s</term>
        <listitem>
         <para>
          Setuid bit&mdash;the application or program is started as if it
          were started by the owner of the file
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
      <para>
       As an alternative, a numeric code can be used. The four digits of
       this code are composed of the sum of the values 4, 2, and 1&mdash;the
       decimal result of a binary mask. The first digit sets the set user ID
       (SUID) (4), the set group ID (2), and the sticky (1) bits. The second
       digit defines the permissions of the owner of the file. The third
       digit defines the permissions of the group members and the last digit
       sets the permissions for all other users. The read permission is set
       with 4, the write permission with 2, and the permission for executing
       a file is set with 1. The owner of a file would usually receive a 6
       or a 7 for executable files.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>gzip</command>&#x20;<option>[parameters]</option>&#x20;<filename>files</filename>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>gzip</secondary></indexterm><indexterm>
      <primary>gzip</primary></indexterm><indexterm>
      <primary>files</primary>
      <secondary>compressing</secondary></indexterm>
      <para>
       This program compresses the contents of files using complex
       mathematical algorithms. Files compressed in this way are given the
       extension <filename>.gz</filename> and need to be uncompressed before
       they can be used. To compress several files or even entire
       directories, use the <command>tar</command> command.
      </para>
      <variablelist>
       <varlistentry>
        <term>-d</term>
        <listitem>
         <para>
          Decompresses the packed gzip files so they return to their
          original size and can be processed normally (like the command
          <command>gunzip</command>)
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>tar</command>&#x20;<option>options</option>&#x20;<option>archive</option>&#x20;<option>files</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>tar</secondary></indexterm><indexterm>
      <primary>tar</primary></indexterm><indexterm>
      <primary>files</primary>
      <secondary>archiving</secondary></indexterm>
      <para>
       <command>tar</command> puts one or more files into an archive.
       Compression is optional. <command>tar</command> is a quite complex
       command with a number of options available. The most frequently used
       options are:
      </para>
      <variablelist>
       <varlistentry>
        <term><option>-f</option>
        </term>
        <listitem>
         <para>
          Writes the output to a file and not to the screen as is usually
          the case
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-c</option>
        </term>
        <listitem>
         <para>
          Creates a new tar archive
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-r</option>
        </term>
        <listitem>
         <para>
          Adds files to an existing archive
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-t</option>
        </term>
        <listitem>
         <para>
          Outputs the contents of an archive
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-u</option>
        </term>
        <listitem>
         <para>
          Adds files, but only if they are newer than the files already
          contained in the archive
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-x</option>
        </term>
        <listitem>
         <para>
          Unpacks files from an archive (<emphasis>extraction</emphasis>)
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-z</option>
        </term>
        <listitem>
         <para>
          Packs the resulting archive with <command>gzip</command>
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-j</option>
        </term>
        <listitem>
         <para>
          Compresses the resulting archive with <command>bzip2</command>
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-v</option>
        </term>
        <listitem>
         <para>
          Lists files processed
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
      <para>
       The archive files created by <command>tar</command> end with
       <filename>.tar</filename>. If the tar archive was also compressed
       using <command>gzip</command>, the ending is
       <filename>.tgz</filename> or <filename>.tar.gz</filename>. If it was
       compressed using <command>bzip2</command>, the ending is
       <filename>.tar.bz2</filename>.
       <remark>taroth 060929: commented link because
        dropped section from bash.xml - to be integrated into newbie_bash.xml
        later</remark>
<!-- Application examples can be found
       in <xref linkend="sec.bash.tar"/>.-->
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>locate</command>&#x20;<option>patterns</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>locate</secondary></indexterm><indexterm>
      <primary>locate</primary></indexterm><indexterm>
      <primary>files</primary>
      <secondary>searching for</secondary></indexterm>
      <para>
       This command is only available if you have installed the
       <systemitem class="resource">findutils-locate</systemitem> package.
       The <command>locate</command> command can find in which directory a
       specified file is located. If desired, use wild cards <indexterm>
       <primary>wild cards</primary>
       </indexterm> to specify filenames. The program is very fast, because
       it uses a database specifically created for the purpose (rather than
       searching through the entire file system). This very fact, however,
       also results in a major drawback: locate is unable to find any files
       created after the latest update of its database. The database can be
       generated by <systemitem class="username">root</systemitem> with
       <command>updatedb</command>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>updatedb</command>&#x20;<option>[options]</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>updatedb</secondary></indexterm><indexterm>
      <primary>updatedb</primary></indexterm>
      <para>
       This command performs an update of the database used by
       <command>locate</command>. To include files in all existing
       directories, run the program as
       <systemitem class="username">root</systemitem>. It also makes sense
       to place it in the background by appending an ampersand
       (<systemitem>&amp;</systemitem>), so you can immediately continue
       working on the same command line (<command>updatedb &amp;</command>).
       This command usually runs as a daily cron job (see
       <filename>cron.daily</filename>).
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>find</command>&#x20;<option>[options]</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>find</secondary></indexterm><indexterm>
      <primary>find</primary></indexterm><indexterm>
      <primary>files</primary>
      <secondary>searching for</secondary></indexterm>
      <para>
       With <command>find</command>, search for a file in a given directory.
       The first argument specifies the directory in which to start the
       search. The option <command>-name</command> must be followed by a
       search string, which may also include wild cards. Unlike
       <command>locate</command>, which uses a database,
       <command>find</command> scans the actual directory.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect3>
  <sect3 xml:id="sec.commands.filecontents">
   <title>Commands to Access File Contents</title><indexterm>
   <primary>files</primary>
   <secondary>viewing</secondary></indexterm>
   <variablelist>
    <varlistentry>
     <term><command>file</command>&#x20;<option>[options]</option>&#x20;<option>[files]</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>file</secondary></indexterm><indexterm>
      <primary>file</primary></indexterm>
      <para>
       With <command>file</command>, detect the contents of the specified
       files.
      </para>
      <variablelist>
       <varlistentry>
        <term>-z</term>
        <listitem>
         <para>
          Tries to look inside compressed files
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
   </variablelist>
   <variablelist>
    <varlistentry>
     <term><command>cat</command>&#x20;<option>[options]</option>&#x20;<filename>files</filename>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>cat</secondary></indexterm><indexterm>
      <primary>cat</primary></indexterm>
      <para>
       The <command>cat</command> command displays the contents of a file,
       printing the entire contents to the screen without interruption.
      </para>
      <variablelist>
       <varlistentry>
        <term>-n</term>
        <listitem>
         <para>
          Numbers the output on the left margin
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>less</command>&#x20;<option>[options]</option>&#x20;<filename>files</filename>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>less</secondary></indexterm><indexterm>
      <primary>less</primary></indexterm>
      <para>
       This command can be used to browse the contents of the specified
       file. Scroll half a screen page up or down with <keycap>PgUp</keycap>
       and <keycap>PgDn</keycap> or a full screen page down with
       <keycap>Space</keycap>. Jump to the beginning or end of a file using
       <keycap>Home</keycap> and <keycap>End</keycap>. Press
       <keycap>Q</keycap> to exit the program.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>grep</command>&#x20;<option>[options]</option>&#x20;<option>searchstring</option>&#x20;<filename>files</filename>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>grep</secondary></indexterm><indexterm>
      <primary>grep</primary></indexterm><indexterm>
      <primary>files</primary>
      <secondary>searching contents</secondary></indexterm>
      <para>
       The grep command finds a specific search string in the specified
       files. If the search string is found, the command displays the line
       in which <literal>searchstring</literal> was found along with the
       filename.
      </para>
      <variablelist>
       <varlistentry>
        <term><option>-i</option>
        </term>
        <listitem>
         <para>
          Ignores case
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-H</option>
        </term>
        <listitem>
         <para>
          Only displays the names of the relevant files, but not the text
          lines
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-n</option>
        </term>
        <listitem>
         <para>
          Additionally displays the numbers of the lines in which it found a
          hit
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-l</option>
        </term>
        <listitem>
         <para>
          Only lists the files in which <literal>searchstring</literal> does
          not occur
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>diff</command>&#x20;<option>[options]</option>&#x20;<filename>file1</filename>&#x20;<filename>file2</filename>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>diff</secondary></indexterm><indexterm>
      <primary>diff</primary></indexterm><indexterm>
      <primary>files</primary>
      <secondary>comparing</secondary></indexterm>
      <para>
       The <command>diff</command> command compares the contents of any two
       files. The output produced by the program lists all lines that do not
       match. This is frequently used by programmers who need only to send
       their program alterations and not the entire source code.
      </para>
      <variablelist>
       <varlistentry>
        <term><option>-q</option>
        </term>
        <listitem>
         <para>
          Only reports whether the two files differ
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-u</option>
        </term>
        <listitem>
         <para>
          Produces a <quote>unified</quote> diff, which makes the output
          more readable
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect3>
<!-- ** -->
  <sect3 xml:id="sec.commands.filesystems">
   <title>File Systems</title>
   <variablelist>
    <varlistentry>
     <term><command>mount</command>&#x20;<option>[options]</option>&#x20;<option>[device]</option>&#x20;<filename>mountpoint</filename>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>mount</secondary></indexterm><indexterm>
      <primary>mount</primary></indexterm><indexterm>
      <primary>drives</primary>
      <secondary>mounting</secondary></indexterm>
      <para>
       This command can be used to mount any data media, such as hard disks,
       CD-ROM drives, and other drives, to a directory of the Linux file
       system.
      </para>
      <variablelist>
       <varlistentry>
        <term><option>-r</option>
        </term>
        <listitem>
         <para>
          Mount read-only
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-t filesystem</option>
        </term>
        <listitem>
         <para>
          Specify the file system, commonly <literal>ext2</literal> for
          Linux hard disks, <literal>msdos</literal> for MS-DOS media,
          <literal>vfat</literal> for the Windows file system, and
          <literal>iso9660</literal> for CDs
         </para>
        </listitem>
       </varlistentry>
      </variablelist><indexterm>
      <primary>configuration files</primary>
      <secondary>fstab</secondary></indexterm>
      <para>
       For hard disks not defined in the file
       <filename>/etc/fstab</filename>, the device type must also be
       specified. In this case, only
       <systemitem class="username">root</systemitem> can mount it. If the
       file system needs to also be mounted by other users, enter the option
       <literal>user</literal> in the appropriate line in the
       <filename>/etc/fstab</filename> file (separated by commas) and save
       this change. Further information is available in the
       <systemitem>mount(1)</systemitem> man page.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>umount</command>&#x20;<option>[options]</option>&#x20;<filename>mountpoint</filename>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>umount</secondary></indexterm><indexterm>
      <primary>umount</primary></indexterm><indexterm>
      <primary>drives</primary>
      <secondary>unmounting</secondary></indexterm>
      <para>
       This command unmounts a mounted drive from the file system. To
       prevent data loss, run this command before taking a removable data
       medium from its drive. Normally, only
       <systemitem class="username">root</systemitem> is allowed to run the
       commands <command>mount</command> and <command>umount</command>. To
       enable other users to run these commands, edit the
       <filename>/etc/fstab</filename> file to specify the option
       <literal>user</literal> for the relevant drive.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect3>
 </sect2>

 <sect2 xml:id="sec.commands.system">
  <title>System Commands</title>
  <para>
   The following section lists a few of the most important commands needed
   for retrieving system information and controlling processes and the
   network.
  </para>
  <sect3 xml:id="sec.commands.systeminfo">
   <title>System Information</title>
   <variablelist>
    <varlistentry>
     <term><command>df</command>&#x20;<option>[options]</option>&#x20;<filename>[directory]</filename>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>df</secondary></indexterm><indexterm>
      <primary>df</primary></indexterm>
      <para>
       The <command>df</command> (disk free) command, when used without any
       options, displays information about the total disk space, the disk
       space currently in use, and the free space on all the mounted drives.
       If a directory is specified, the information is limited to the drive
       on which that directory is located.
      </para>
      <variablelist>
       <varlistentry>
        <term><option>-h</option>
        </term>
        <listitem>
         <para>
          Shows the number of occupied blocks in gigabytes, megabytes, or
          kilobytes&mdash;in human-readable format
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-T</option>
        </term>
        <listitem>
         <para>
          Type of file system (ext2, nfs, etc.)
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>du</command>&#x20;<option>[options]</option>&#x20;<filename>[path]</filename>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>du</secondary></indexterm><indexterm>
      <primary>du</primary></indexterm>
      <para>
       This command, when executed without any parameters, shows the total
       disk space occupied by files and subdirectories in the current
       directory.
      </para>
      <variablelist>
       <varlistentry>
        <term><option>-a</option>
        </term>
        <listitem>
         <para>
          Displays the size of each individual file
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-h</option>
        </term>
        <listitem>
         <para>
          Output in human-readable form
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-s</option>
        </term>
        <listitem>
         <para>
          Displays only the calculated total size
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>free</command>&#x20;<option>[options]</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>free</secondary></indexterm><indexterm>
      <primary>free</primary></indexterm>
      <para>
       The command <command>free</command> displays information about RAM
       and swap space usage, showing the total and the used amount in both
       categories. See <xref linkend="sec.suse.free"/> for more information.
      </para>
      <variablelist>
       <varlistentry>
        <term><option>-b</option>
        </term>
        <listitem>
         <para>
          Output in bytes
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-k</option>
        </term>
        <listitem>
         <para>
          Output in kilobytes
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-m</option>
        </term>
        <listitem>
         <para>
          Output in megabytes
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>date</command>&#x20;<option>[options]</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>date</secondary></indexterm><indexterm>
      <primary>date</primary></indexterm>
      <para>
       This simple program displays the current system time. If run as
       <systemitem class="username">root</systemitem>, it can also be used
       to change the system time. Details about the program are available in
       the date(1) man page.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect3>
<!-- *** -->
  <sect3 xml:id="sec.commands.processes">
   <title>Processes</title><indexterm>
   <primary>processes</primary></indexterm>
   <variablelist>
    <varlistentry>
     <term><command>top</command>&#x20;<option>[options]</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>top</secondary></indexterm><indexterm>
      <primary>top</primary></indexterm>
      <para>
       <command>top</command> provides a quick overview of the currently
       running processes. Press <keycap>H</keycap> to access a page that
       briefly explains the main options for customizing the program.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>ps</command>&#x20;<option>[options]</option>&#x20;<option>[process_ID]</option>
     </term>
     <listitem>
      <para>
       If run without any options, this command displays a table of all your
       own programs or processes&mdash;those you started. The options for
       this command are not preceded by hyphen.
      </para><indexterm>
      <primary>commands</primary>
      <secondary>ps</secondary></indexterm><indexterm>
      <primary>ps</primary></indexterm><indexterm>
      <primary>processes</primary>
      <secondary>overview</secondary></indexterm>
      <variablelist>
       <varlistentry>
        <term>aux</term>
        <listitem>
         <para>
          Displays a detailed list of all processes, independent of the
          owner
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>kill</command>&#x20;<option>[options]</option>&#x20;<option>process_ID</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>kill</secondary></indexterm><indexterm>
      <primary>kill</primary></indexterm><indexterm>
      <primary>processes</primary>
      <secondary>killing</secondary></indexterm>
      <para>
       Unfortunately, sometimes a program cannot be terminated in the normal
       way. In most cases, you should still be able to stop such a runaway
       program by executing the <command>kill</command> command, specifying
       the respective process ID (see <command>top</command> and
       <command>ps</command>). <command>kill</command> sends a
       <emphasis>TERM</emphasis> signal that instructs the program to shut
       itself down. If this does not help, the following parameter can be
       used:
      </para>
      <variablelist>
       <varlistentry>
        <term>-9</term>
        <listitem>
         <para>
          Sends a <emphasis>KILL</emphasis> signal instead of a
          <emphasis>TERM</emphasis> signal, bringing the specified process
          to an end in almost all cases
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>killall</command>&#x20;<option>[options]</option>&#x20;<option>processname</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>killall</secondary></indexterm><indexterm>
      <primary>killall</primary></indexterm>
      <para>
       This command is similar to <command>kill</command>, but uses the
       process name (instead of the process ID) as an argument, killing all
       processes with that name.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect3>
  <sect3 xml:id="sec.commands.network">
   <title>Network</title>
   <variablelist>
    <varlistentry>
     <term><command>ping</command>&#x20;<option>[options]</option>&#x20;<option>hostname_or_IP address</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>ping</secondary></indexterm><indexterm>
      <primary>ping</primary></indexterm>
      <para>
       The <command>ping</command> command is the standard tool for testing
       the basic functionality of TCP/IP networks. It sends a small data
       packet to the destination host, requesting an immediate reply. If
       this works, <command>ping</command> displays a message to that
       effect, which indicates that the network link is basically
       functioning.
      </para>
      <variablelist>
       <varlistentry>
        <term><option>-c</option><replaceable>number</replaceable>
        </term>
        <listitem>
         <para>
          Determines the total number of packages to send and ends after
          they have been dispatched (by default, there is no limitation set)
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-f</option>
        </term>
        <listitem>
         <para>
          <emphasis>flood ping</emphasis>: sends as many data packages as
          possible; a popular means, reserved for
          <systemitem class="username">root</systemitem>, to test networks
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><option>-i</option><replaceable>value</replaceable>
        </term>
        <listitem>
         <para>
          Specifies the interval between two data packages in seconds
          (default: one second)
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>host</command>&#x20;<option>[options]</option>&#x20;<option>hostname</option>&#x20;<option>[server]</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>host</secondary></indexterm><indexterm>
      <primary>host</primary></indexterm>
      <para>
       The domain name system resolves domain names to IP addresses. With
       this tool, send queries to name servers (DNS servers).
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>ssh</command>&#x20;<option>[options]</option>&#x20;<option>[user@]hostname</option>&#x20;<option>[command]</option>
     </term>
     <listitem>
      <para>
       SSH is actually an Internet protocol that enables you to work on
       remote hosts across a network. SSH is also the name of a Linux
       program that uses this protocol to enable operations on remote
       computers.
      </para><indexterm>
      <primary>commands</primary>
      <secondary>ssh</secondary></indexterm><indexterm>
      <primary>ssh</primary></indexterm>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect3>
  <sect3 xml:id="sec.commands.misc">
   <title>Miscellaneous</title>
   <variablelist>
    <varlistentry>
     <term><command>passwd</command>&#x20;<option>[options]</option>&#x20;<option>[username]</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>passwd</secondary></indexterm><indexterm>
      <primary>passwd</primary></indexterm><indexterm>
      <primary>passwords</primary>
      <secondary>changing</secondary></indexterm>
      <para>
       Users may change their own passwords at any time using this command.
       The administrator <systemitem class="username">root</systemitem> can
       use the command to change the password of any user on the system.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>su</command>&#x20;<option>[options]</option>&#x20;<option>[username]</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>su</secondary></indexterm><indexterm>
      <primary>su</primary></indexterm>
      <para>
       The <command>su</command> command makes it possible to log in under a
       different username from a running session. Specify a username and the
       corresponding password. The password is not required from
       <systemitem class="username">root</systemitem>, because
       <systemitem class="username">root</systemitem> is authorized to
       assume the identity of any user. When using the command without
       specifying a username, you are prompted for the
       <systemitem class="username">root</systemitem> password and change to
       the superuser (<systemitem class="username">root</systemitem>). Use
       <command>su -</command> to start a login shell for a different user.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>halt</command>&#x20;<option>[options]</option>
     </term>
     <listitem>
      <para>
       To avoid loss of data, you should always use this program to shut
       down your system.
      </para><indexterm>
      <primary>commands</primary>
      <secondary>halt</secondary></indexterm><indexterm>
      <primary>halt</primary></indexterm><indexterm>
      <primary>system</primary>
      <secondary>shutdown</secondary></indexterm>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>reboot</command>&#x20;<option>[options]</option>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>reboot</secondary></indexterm><indexterm>
      <primary>reboot</primary></indexterm><indexterm>
      <primary>system</primary>
      <secondary>rebooting</secondary></indexterm>
      <para>
       Does the same as <command>halt</command> except the system performs
       an immediate reboot.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><command>clear</command>
     </term>
     <listitem><indexterm>
      <primary>commands</primary>
      <secondary>clear</secondary></indexterm><indexterm>
      <primary>clear</primary></indexterm>
      <para>
       This command cleans up the visible area of the console. It has no
       options.
      </para>
     </listitem>
    </varlistentry>
   </variablelist><indexterm class="endofrange" startref="idx.Shell_Befehle"/><indexterm class="endofrange" startref="idx.Befehle"/>
  </sect3>
 </sect2>

 <sect2 xml:id="sec.shell.commands.info">
  <title>For More Information</title>
  <para>
   There are many more commands than listed in this chapter. For information
   about other commands or more detailed information, the O'Reilly
   publication <emphasis>Linux in a Nutshell</emphasis> is recommended.
  </para>
 </sect2>
</sect1>
