<?xml version="1.0" encoding="UTF-8"?>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="sle_update_preparation.xml" version="5.0" xml:id="cha-update-preparation">
 <title>Vorbereiten des Upgrades</title>
 <info>
  <abstract>
   <para>
    Vor Beginn des Upgrades muss das System ordnungsgemäß vorbereitet werden. Zur Vorbereitung gehören unter anderem das Sichern der Daten und das Lesen der Versionshinweise.
   </para>
  </abstract>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:translation>Ja</dm:translation>
  </dm:docmanager>
 </info>

 <sect1 xml:id="sec-update-preparation-update">
  <title>Prüfen, ob das aktuelle System auf dem neuesten Stand ist</title>
  <para>
   Ein Upgraden des Systems wird nur von der jeweils letzten Patch-Stufe aus unterstützt. Prüfen Sie, ob die neuesten Systemaktualisierungen installiert sind. Führen Sie hierzu entweder <command>zypper patch</command> aus oder starten Sie das YaST-Modul <guimenu>Online-Update</guimenu>.
  </para>
 </sect1>

 <sect1 xml:id="sec-update-preparation-relnotes">
  <title>Lesen Sie die Versionshinweise</title>
  <para>
   In den Versionshinweisen finden Sie zusätzliche Informationen zu den Änderungen, die seit der vorigen Version von <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> vorgenommen wurden. Informieren Sie sich in den Versionshinweisen über Folgendes:
  </para>
  <itemizedlist>
   <listitem>
    <para>
     Sind bei der Hardware besondere Überlegungen zu beachten?
    </para>
   </listitem>
   <listitem>
    <para>
     Wurden erhebliche Änderungen an den verwendeten Software-Paketen vorgenommen?
    </para>
   </listitem>
   <listitem>
    <para>
     Gelten besondere Vorsichtsmaßnahmen für die vorliegende Installation?
    </para>
   </listitem>
  </itemizedlist>
  <para>
   In den Versionshinweisen finden Sie auch Informationen, die erst nach der Fertigstellung des Handbuchs bekannt wurden. Auch bekannte Probleme werden beschrieben.
  </para>
  <para os="sles">
   Falls Sie mindestens ein Service Pack überspringen, beachten Sie auch die Versionshinweise der übersprungenen Service Packs. Die Versionshinweise enthalten in der Regel nur die Änderungen zwischen zwei aufeinander folgenden Versionen. Wenn Sie nur die aktuellen Versionshinweise lesen, entgehen Ihnen eventuell wichtige Änderungen.
  </para>
  <para>
   Die aktuellen Versionshinweise stehen online unter <link xlink:href="https://www.suse.com/releasenotes/"/> bereit.
  </para>
  <para>
   Alternativ finden Sie die Versionshinweise auf der Installations-DVD im Verzeichnis <filename>docu</filename>.
  </para>
 </sect1>
 <sect1 xml:id="sec-update-preparation-backup">
  <title>Anlegen einer Sicherungskopie</title>
  <para>
   Kopieren Sie die bestehenden Konfigurationsdateien vor der Aktualisierung auf ein separates Medium (wie ein Bandlaufwerk oder eine externe Festplatte), um die Daten zu sichern. Dies gilt hauptsächlich für die in <filename>/etc</filename> gespeicherten Dateien sowie für bestimmte Verzeichnisse und Dateien in <filename>/var</filename> und <filename>/opt</filename>. Zudem empfiehlt es sich, die Benutzerdaten in <filename>/home</filename> (den <envar>HOME</envar>-Verzeichnissen) auf ein Sicherungsmedium zu schreiben. Melden Sie sich zur Sicherung dieser Daten als <systemitem class="username">root</systemitem> an. Nur der Benutzer <systemitem class="username">root</systemitem> verfügt über die Leseberechtigung für alle lokalen Dateien.
  </para>
  <para>
   Wenn Sie in YaST den Installationsmodus <guimenu>Vorhandenes System aktualisieren</guimenu> ausgewählt haben, können Sie später wahlweise eine (System-)Sicherung ausführen. Sie können alle geänderten Dateien und die Dateien aus dem Verzeichnis <filename>/etc/sysconfig</filename> einschließen. Dies ist allerdings keine vollständige Sicherung, da alle anderen wichtigen, oben genannten Verzeichnisse außer Acht gelassen werden. Die Sicherungskopie befindet sich im Verzeichnis<filename>/var/adm/backup</filename>.
  </para>
 </sect1>
 <sect1 xml:id="sec-update-preparation-packagelist">
  <title>Auflisten installierter Pakete und Repositorys</title>
  <para>
   Eine Liste der installierten Pakete ist häufig von Nutzen, beispielsweise bei einer Neuinstallation einer neuen SLE-Hauptversion oder beim Zurücksetzen des Systems auf die bisherige Version.
  </para>
  <para>
   Denken Sie daran, dass nicht alle installierten Pakete oder verwendeten Repositorys in neueren Versionen von SUSE Linux Enterprise vorliegen. Einige Elemente wurden unter Umständen umbenannt, andere ersetzt. Außerdem könnten bestimmte Pakete weiterhin zu Legacy-Zwecken verfügbar sein, während standardmäßig ein anderes Paket herangezogen wird. Aus diesem Grund müssen die Dateien ggf. manuell bearbeitet werden. Dies können Sie mit einem beliebigen Texteditor durchführen.
  </para>
  <para>
   Erstellen Sie die Datei <filename>repositories.bak-repo</filename> mit einer Liste aller verwendeten Repositorys.
  </para>
  <screen><prompt role="root">root # </prompt><command>zypper</command> lr -e repositories.bak</screen>
  <para>
   Erstellen Sie außerdem die Datei <filename>installed-software.bak</filename> mit einer Liste aller installierten Pakete:
  </para>
  <screen><prompt role="root">root # </prompt><command>rpm</command> -qa --queryformat '%{NAME}\n' &gt; installed-software.bak</screen>
  <para>
   Erstellen Sie Sicherungskopien beider Dateien. Die Repositorys und die installierten Pakete können mit den folgenden Befehlen wiederhergestellt werden:
  </para>
  <screen><prompt role="root">root # </prompt><command>zypper</command> ar repositories.bak.repo
<prompt role="root">root # </prompt><command>zypper</command> install $(cat installed-software.bak)</screen>

  <note xml:id="note-update-prep-backup-package-amount">
   <title>
    Höhere Paketanzahl bei einem Upgrade auf eine neue Hauptversion
   </title>
   <para>
    Ein System, das auf eine neue Hauptversion upgegradet wurde (SLE <replaceable>X+1</replaceable>), enthält eventuell mehr Pakete als das ursprüngliche System (SLE <replaceable>X</replaceable>). Die Anzahl der Pakete ist außerdem höher als bei einer Neuinstallation von SLE <replaceable>X+1</replaceable> mit derselben Schemaauswahl. Hierfür sind folgende Gründe zu nennen:
   </para>
   <itemizedlist>
    <listitem>
     <para>
      Die Pakete wurden aufgeteilt, sodass Sie die gewünschte Paketauswahl noch genauer festlegen können. Zum Beispiel 37 <package>texlive</package> -Pakete aus SLE 11 auf 422 Pakete in SLE 12 aufgeteilt.
     </para>
    </listitem>
    <listitem>
     <para>
      Wenn ein Paket auf mehrere andere Pakete aufgeteilt wurde, werden bei einem Upgrade alle Neupakete installiert, damit in jedem Fall derselbe Funktionsumfang wie in der früheren Version zur Verfügung steht. Bei einer Neuinstallation von SLE <replaceable>X+1</replaceable> werden jedoch unter Umständen nicht mehr alle Pakete standardmäßig installiert.
     </para>
    </listitem>
    <listitem>
     <para>
      Ältere Pakete aus SLE <replaceable>X</replaceable> werden ggf. aus Kompatibilitätsgründen beibehalten.
     </para>
    </listitem>
    <listitem>
     <para>
      Die Paketabhängigkeiten und der Schemaumfang haben sich unter Umständen geändert.
     </para>
    </listitem>
   </itemizedlist>
  </note>
 </sect1>

 <sect1 os="sles" xml:id="sec-update-preparation-sle11">
  <title>Upgraden von <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 11 SP4</title>
  <para>
   Wenn Sie MySQL-, PostgreSQL- oder Java MD5-gestützte Zertifikate in <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 11 SP4 verwenden, bereiten Sie das System gemäß den nachfolgenden Abschnitten vor.
  </para>
  <sect2 xml:id="sec-update-preparation-sle11-mariadb">
   <title>Migration der MySQL-Datenbank</title>
   <remark>toms 2015-09-03: already reviewed by Ondrej and Kristýna.</remark>
   <para>
    Ab SUSE Linux Enterprise 12 hat SUSE von MySQL auf MariaDB umgestellt. Bevor Sie das Upgrade starten, wird dringend empfohlen, die Datenbank zu sichern.
   </para>
   <para>
    So führen Sie die Datenbankmigration aus:
   </para>
   <procedure>
    <step>
     <para>
      Melden Sie sich auf dem SUSE Linux Enterprise 11-Computer an.
     </para>
    </step>
    <step>
     <para>
      Erstellen Sie eine Dump-Datei:
     </para>
     <screen><prompt role="root">root # </prompt><command>mysqldump</command> -u root -p --all-databases &gt; mysql_backup.sql</screen>
     <para>
      Standardmäßig wird die Datenbank <literal>INFORMATION_SCHEMA</literal> oder <literal>performance_schema</literal> nicht im Speicherauszug mit <command>mysqldump</command> berücksichtigt. Weitere Einzelheiten finden Sie unter <link xlink:href="https://dev.mysql.com/doc/refman/5.5/en/mysqldump.html"/>.
     </para>
    </step>
    <step>
     <para>
      Speichern Sie Ihre Dump-Datei, die Konfigurationsdatei <filename>/etc/my.cnf</filename> sowie das Verzeichnis <filename>/etc/mysql/</filename> an einem sicheren Speicherort, um sie später als Referenz (<emphasis>nicht</emphasis> zur Installation!) zu verwenden.
     </para>
    </step>
    <step>
     <para>
      Führen Sie das Upgrade aus. Nach dem Upgrade ist die bisherige Konfigurationsdatei <filename>/etc/my.cnf</filename> unverändert. Die neue Konfiguration finden Sie in der Datei <filename>/etc/my.cnf.rpmnew</filename>.
     </para>
    </step>
    <step>
     <para>
      Konfigurieren Sie die MariaDB-Datenbank je nach Bedarf. Bearbeiten Sie dabei <emphasis>nicht</emphasis> die bisherige Konfigurationsdatei und das frühere Verzeichnis, sondern nutzen Sie diese nur als Vorlage, und passen Sie die Einstellungen entsprechend an.
     </para>
    </step>
    <step>
     <para>
      Starten Sie den MariaDB-Server:
     </para>
     <screen><prompt role="root">root # </prompt><command>systemctl</command> start mysql</screen>
     <para>
      Soll der MariaDB-Server bei jedem Booten gestartet werden, aktivieren Sie den Dienst:
     </para>
     <screen><prompt role="root">root # </prompt><command>systemctl</command> enable mysql</screen>
    </step>
    <step>
     <para>
      Prüfen Sie, ob MariaDB ordnungsgemäß ausgeführt wird. Stellen Sie hierzu eine Verbindung zur Datenbank her:
     </para>
     <screen><prompt role="root">root # </prompt><command>mysql</command> -u root -p</screen>
    </step>
   </procedure>
  </sect2>

  <sect2 xml:id="sec-update-preparation-sle11-postgresql">
   <title>Migration der PostgreSQL-Datenbank</title>
   <para>
    Eine neuere Version der PostgreSQL-Datenbank ist im Lieferumfang von <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>
    <phrase role="productnumber"><phrase os="sles;sled">15 SP1</phrase></phrase> enthalten. Aufgrund der erforderlichen Migrationsschritte für die Datenbank ist kein automatischer Upgradevorgang verfügbar. Die Umstellung auf die neue Version muss daher manuell erfolgen.
   </para>
   <para>
    Der Migrationsvorgang wird mit dem Befehl <command>pg_upgrade</command> ausgeführt. Dieser Befehl ist eine alternative Methode zur bewährten Methode mit Erstellen eines Speicherauszugs und Neuladen. Im Vergleich zu <quote>Speicherauszug und Neuladen</quote> ist die Migration mit <command>pg_upgrade</command> weniger zeitaufwändig.
   </para>
   <para>
    Die Dateien der verschiedenen PostgreSQL-Versionen sind jeweils in verschiedenen versionsabhängigen Verzeichnissen gespeichert. Nach dem Upgrade gilt nicht mehr das Verzeichnis <filename>/usr/lib/postgresql96/</filename>, sondern das Verzeichnis <filename>/usr/lib/postgresql10/</filename>.
   </para>
   <para>
    So führen Sie die Datenbankmigration aus:
   </para>
   <procedure>
    <step>
     <para>
      Prüfen Sie, ob die folgenden Voraussetzungen erfüllt sind:
     </para>
     <itemizedlist>
      <listitem>
       <para>
        Upgraden Sie alle Pakete der alten PostgreSQL-Version per Wartungsaktualisierung auf die aktuelle Version, sofern Sie dies noch nicht erledigt haben.
       </para>
      </listitem>
      <listitem>
       <para>
        Erstellen Sie eine Sicherung der vorhandenen Datenbank.
       </para>
      </listitem>
      <listitem>
       <para>
        Installieren Sie die Pakete der neuen PostgreSQL-Hauptversion. Bei SLE 15 installieren Sie
        <package>postgresql10-server</package>
        und alle Pakete, von denen dieses Paket abhängig ist.
       </para>
      </listitem>
      <listitem>
       <para>
        Installieren Sie das Paket
        <package>postgresql10-contrib</package>
        (enthält den Befehl <command>pg_upgrade</command>).
       </para>
      </listitem>
      <listitem>
       <para>
        Prüfen Sie, ob ausreichend freier Speicherplatz im PostgreSQL-Datenbereich verfügbar ist (standardmäßig <filename>/var/lib/pgsql/data</filename>). Falls der Speicherplatz nicht ausreicht, versuchen Sie, die einzelnen Datenbanken mit dem folgenden SQL-Befehl zu verkleinern (kann sehr lange dauern!):
       </para>
       <screen>VACUUM FULL</screen>
      </listitem>
     </itemizedlist>
    </step>
    <step>
     <para>
      Halten Sie den PostgreSQL-Server an:
     </para>
     <screen><prompt role="root">root # </prompt>systemctl stop postgresql.service</screen>
    </step>
    <step>
     <para>
      Benennen Sie das alte Datenverzeichnis um:
     </para>
     <screen><prompt role="root">root # </prompt><command>mv</command> /var/lib/pgsql/data /var/lib/pgsql/data.old</screen>
    </step>
    <step>
     <para>
      Initialisieren Sie die neue Datenbankinstanz manuell mit <command>initdb</command> oder starten und stoppen Sie PostgreSQL, wodurch die Instanz automatisch initialisiert wird:
     </para>
<screen><prompt role="root">root # </prompt>systemctl start postgresql.service
<prompt role="root">root # </prompt>systemctl stop postgresql.service</screen>
    </step>
    <step>
     <para>
      Sollten Sie in der alten Version die Konfigurationsdateien bearbeitet haben, ziehen Sie in Betracht, diese Änderungen in die neuen Konfigurationsdateien zu übernehmen. Dieser Umstand könnte sich auf die Dateien <filename>postgresql.auto.conf</filename>, <filename>postgresql.conf</filename>, <filename>pg_hba.conf</filename> und <filename>pg_ident.conf</filename> auswirken. Die alten Versionen dieser Dateien finden sich unter <filename>/var/lib/pgsql/data.old/</filename>, die neuen Versionen unter <filename>/var/lib/pgsql/data</filename>.
     </para>
     <para>
      Beachten Sie, dass wir nicht empfehlen, einfach die alten Konfigurationsdateien zu kopieren, da somit möglicherweise neue Optionen, neue Standardeinstellungen und geänderte Kommentare überschrieben werden.
     </para>
    </step>
    <step>
     <para>
      Beginnen Sie als Benutzer <systemitem class="username">postgres</systemitem> mit der Migration:
     </para>
     <screen><prompt role="root">root # </prompt>su - postgres
postgres &gt; <command>pg_upgrade</command> \
  --old-datadir "/var/lib/pgsql/data.old" \
  --new-datadir "/var/lib/pgsql/data" \
  --old-bindir "/usr/lib/postgresql96/bin/" \
  --new-bindir "/usr/lib/postgresql10/bin/"</screen>
    </step>
    <step>
     <para>
      Starten Sie die neue Datenbankinstanz:
     </para>
     <screen><prompt role="root">root # </prompt>systemctl start postgresql.service</screen>
    </step>
    <step>
     <para>
      Überprüfen Sie, ob die Migration erfolgreich war. Der Testumfang hängt von Ihrem Anwendungsfall ab. Zur Automatisierung dieses Schritts steht kein allgemeines Tool zur Verfügung.
     </para>
    </step>
    <step>
     <para>
      Entfernen Sie alle alten PostgreSQL-Pakete und das alte Datenverzeichnis:
     </para>
<screen><prompt role="root">root # </prompt><command>zypper</command> search -s postgresql94 | xargs zypper rm -u
<prompt role="root">root # </prompt><command>rm</command> -rf /var/lib/pgsql/data.old</screen>
    </step>
   </procedure>
  </sect2>

  <sect2 xml:id="sec-update-preparation-sle11-ssl">
   <title>Erstellen von Nicht-MD5-Server-Zertifikaten für Java-Anwendungen</title>
   <remark>toms 2016-07-27: from bsc#970153, c#24</remark>
   <para>
    Beim Update von SP1 auf SP2 wurden MD5-basierte Zertifikate im Rahmen eines Sicherheits-Fixes deaktiviert. Wenn Sie über als MD5 erstellte Zertifikate verfügen, erstellen Sie diese mit folgenden Schritten neu:
   </para>

   <procedure>
    <step>
     <para>
      Öffnen Sie ein Terminal und melden Sie sich als <systemitem class="username">root</systemitem> an.
     </para>
    </step>
    <step>
     <para>
      Erstellen Sie einen privaten Schlüssel:
     </para>
     <screen><prompt role="root">root # </prompt><command>openssl</command> genrsa -out server.key 1024</screen>
     <para>
      Wenn Sie einen Schlüssel mit höherer Sicherheit wünschen, ersetzen Sie <literal>1024</literal> durch eine höhere Zahl, z. B. <literal>4096</literal>.
     </para>
    </step>
    <step>
     <para>
      Erstellen Sie einen Zertifizierungsantrag (CSR):
     </para>
     <screen><prompt role="root">root # </prompt><command>openssl</command> req -new -key server.key -out server.csr</screen>
    </step>
    <step>
     <para>
      Führen Sie eine Eigensignierung des Zertifikats durch:
     </para>
     <screen><prompt role="root">root # </prompt><command>openssl</command> x509 -req -days 365 -in server.csr -signkey server.key -out server.crt</screen>
    </step>
    <step>
     <para>
      Erstellen Sie die PEM-Datei:
     </para>
     <screen><prompt role="root">root # </prompt><command>cat</command> server.key server.crt &gt; server.pem</screen>
    </step>
    <step>
     <para>Legen Sie die Dateien <filename>server.crt</filename>, <filename>server.csr</filename>, <filename>server.key</filename> und <filename>server.pem</filename> in den jeweiligen Verzeichnissen ab, in denen die Schlüssel gefunden werden können. Für Tomcat beispielsweise lautet das Verzeichnis <filename>/etc/tomcat/ssl/</filename>.
     </para>
    </step>
   </procedure>
  </sect2>
 </sect1>

 <sect1 xml:id="sec-update-preparation-vms">
  <title>Herunterfahren von VM-Gästen</title>
  <para>
   Wenn Ihr Rechner als VM-Hostserver für KVM oder Xen fungiert, müssen Sie vor der Aktualisierung alle aktiven VM-Gäste ordnungsgemäß herunterfahren. Andernfalls können Sie nach der Aktualisierung wahrscheinlich nicht mehr auf die Gäste zugreifen.
  </para>
 </sect1>

 

  <sect1 xml:id="sec-update-preparation-rmt">
   <title>Anpassen der Einrichtung Ihres SMT-Clients</title>
   <para>
    Beachten Sie Folgendes, wenn der Rechner, der upgegradet werden soll, als Client bei einem SMT-Server registriert werden soll: 
   </para>
   <para>
    Prüfen Sie, ob die Version des Skripts <command>clientSetup4SMT.sh</command> auf Ihrem Host aktuell ist. <command>clientSetup4SMT.sh</command> aus älteren Versionen von SMT kann nicht zum Verwalten von SMT 12-Clients verwendet werden. Wenn Sie auf Ihrem SMT-Server regelmäßig Software-Patches anwenden, finden Sie die neueste Version von <command>clientSetup4SMT.sh</command> immer unter <filename>&lt;SMT_HOSTNAME&gt;/repo/tools/clientSetup4SMT.sh</filename>.
   </para>

   <para>
    Falls das Upgrade Ihres Rechners auf eine höhere Version von <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> nicht ausgeführt ist, heben Sie die Registrierung des Rechners beim SMT-Server wie in <xref linkend="pro-sec-update-prep-smt-de-register" xrefstyle="select:label"/> beschrieben auf. Starten Sie danach den Upgradevorgang neu.</para>
   <procedure xml:id="pro-sec-update-prep-smt-de-register">
    <title>Aufheben einer Registrierung von SUSE Linux Enterprise Client bei einem SMT-Server</title>
     <step>
      <para>
       Melden Sie sich beim Client-Rechner an.
      </para>
     </step>
     <step>
     <para>Der folgende Schritt hängt vom aktuellen Betriebssystem des Client ab:</para>
     <itemizedlist>
      <listitem>
       <para>
        Für SUSE Linux Enterprise 11 führen Sie folgende Kommandos aus:
       </para>
<screen><prompt>tux &gt; </prompt><command>sudo</command> suse_register -E
<prompt>tux &gt; </prompt><command>sudo</command> rm -f /etc/SUSEConnect
<prompt>tux &gt; </prompt><command>sudo</command> rm -rf /etc/zypp/credentials.d/*
<prompt>tux &gt; </prompt><command>sudo</command> rm -rf /etc/zypp/repos.d/*
<prompt>tux &gt; </prompt><command>sudo</command> rm -f /etc/zypp/services.d/*
<prompt>tux &gt; </prompt><command>sudo</command> rm -f /var/cache/SuseRegister/*
<prompt>tux &gt; </prompt><command>sudo</command> rm -f /etc/suseRegister*
<prompt>tux &gt; </prompt><command>sudo</command> rm -f /var/cache/SuseRegister/lastzmdconfig.cache
<prompt>tux &gt; </prompt><command>sudo</command> rm -f /etc/zmd/deviceid
<prompt>tux &gt; </prompt><command>sudo</command> rm -f /etc/zmd/secret</screen>
      </listitem>
      <listitem>
       <para>
        Für SUSE Linux Enterprise 12 führen Sie folgende Kommandos aus:
       </para>
<screen><prompt>tux &gt; </prompt><command>sudo</command> SUSEConnect --de-register
<prompt>tux &gt; </prompt><command>sudo</command> SUSEConnect --cleanup
<prompt>tux &gt; </prompt><command>sudo</command> rm -f /etc/SUSEConnect
<prompt>tux &gt; </prompt><command>sudo</command> rm -rf /etc/zypp/credentials.d/*
<prompt>tux &gt; </prompt><command>sudo</command> rm -rf /etc/zypp/repos.d/*
<prompt>tux &gt; </prompt><command>sudo</command> rm -f /etc/zypp/services.d/*</screen>
      </listitem>
     </itemizedlist>
    </step>
    <step>
     <para>
      Melden Sie sich beim SMT-Server an.
     </para>
    </step>
    <step>
     <para>
      Prüfen Sie, ob die Registrierung des Client aufgehoben wurde, indem Sie alle Client-Registrierungen aufrufen:
     </para>
<screen><prompt>tux &gt; </prompt><command>sudo</command> smt-list-registrations</screen>
    </step>
    <step>
     <para>
      Wird der Hostname des Client in der Ausgabe dieses Kommandos noch aufgelistet, rufen Sie die <literal>Eindeutige ID</literal> des Client in der ersten Spalte ab. (Der Client ist möglicherweise mit mehreren IDs aufgeführt.)
     </para>
    </step>
    <step>
     <para>
      Löschen Sie die Registrierung für diesen Client:
     </para>
<screen><prompt>tux &gt; </prompt><command>sudo</command> smt-delete-registration -g <replaceable>UNIQUE_ID</replaceable></screen>
    </step>
     <step>
     <para>
      Sollte der Client mit mehreren IDs aufgeführt sein, wiederholen Sie den obigen Schritt für jede einzelne ID.
     </para>
    </step>
    <step>
     <para>
      Prüfen Sie danach, ob die Registrierung des Client nun aufgehoben wurde, indem Sie das folgende Kommando erneut ausführen:
     </para>
<screen><prompt>tux &gt; </prompt><command>sudo</command> smt-list-registrations</screen>
    </step>
   </procedure>
  </sect1>

 <sect1 xml:id="sec-update-preparation-disk">
  <title>Festplattenspeicher</title>

  <para>
   Software weist normalerweise von Version zu Version mehr Umfang auf. Folglich sollten Sie vor dem Aktualisieren den verfügbaren Partitionsspeicher überprüfen. Wenn Sie befürchten, dass der Speicherplatz nicht ausreicht, sichern Sie Ihre Daten und erhöhen Sie dann den verfügbaren Speicherplatz, indem Sie beispielsweise die Größe von Partitionen ändern. Es gibt keine Faustregel hinsichtlich des Speicherplatzes einzelner Partitionen. Die Platzanforderungen hängen von Ihrem bestimmten Partitionsprofil und von der ausgewählten Software ab.
  </para>

  <note>
   <title>Automatische Prüfung des verfügbaren Speicherplatzes in YaST</title>
   <para>
    YaST prüft während des Aktualisierungsvorgangs den freien verfügbaren Speicherplatz und zeigt dem Benutzer eine Warnmeldung an, wenn die verfügbare Menge möglicherweise nicht für die Installation ausreicht. Wenn Sie die Aktualisierung in diesem Fall dennoch durchführen, kann das <emphasis>System unbrauchbar</emphasis> werden! Sie sollten die Warnmeldung nur dann ignorieren und mit der Aktualisierung fortfahren, wenn Sie genau wissen, was Sie tun (da Sie dies in einem Vorabtest abgeklärt haben).
   </para>
  </note>

  <sect2 xml:id="sec-update-preparation-disk-space">
   <title>Ermitteln des freien Speicherplatzes auf Nicht-Btrfs-Dateisystemen</title>
   <para>
    Listen Sie mit dem Kommando <command>df</command> den verfügbaren Speicherplatz auf. In <xref linkend="ex-update-df"/> ist beispielsweise <filename>/dev/sda3</filename> die Root-Partition (eingehängt als <filename>/</filename>).
   </para>
   <example xml:id="ex-update-df">
    <title>Über <command>df -h</command> angezeigte Liste</title>

<screen os="sles">Filesystem     Size  Used Avail Use% Mounted on
/dev/sda3       74G   22G   53G  29% /
tmpfs          506M     0  506M   0% /dev/shm
/dev/sda5      116G  5.8G  111G   5% /home
/dev/sda1       44G    4G   40G   9% /data</screen>
   </example>
  </sect2>

  <sect2 xml:id="sec-update-preparation-disk-btrfs-on-root">
   <title>Ermitteln des freien Speicherplatzes auf Btrfs-Root-Dateisystemen</title>
   <para>
    Wenn Sie Btrfs als Root-Dateisysteme auf dem Computer nutzen, muss ausreichend freier Speicherplatz zur Verfügung stehen. Im schlimmsten Fall belegt ein Upgrade ebenso viel Speicherplatz wie das aktuelle Root-Dateisystem (ohne <filename>/.snapshot</filename>) für einen neuen Snapshot. Mit diesem Befehl rufen Sie den verfügbaren Speicherplatz ab:
   </para>
<screen><prompt role="root">root # </prompt><command>df</command> -h /</screen>
   <para>
    Prüfen Sie auch den verfügbaren Platz auf allen anderen eingehängten Partitionen. Die folgenden Empfehlungen haben sich bewährt:
   </para>
   <itemizedlist>
    <listitem>
     <para>
      Bei allen Dateisystemen (auch Btrfs) benötigen Sie ausreichend freien Speicherplatz, damit Sie große RPMs herunterladen und installieren können. Der Speicherplatz der alten RPMs wird erst dann freigegeben, wenn die neuen RPMs installiert sind.
     </para>
    </listitem>
    <listitem>
     <para>
      Bei Btrfs mit Snapshots benötigen Sie mindestens so viel freien Speicherplatz, wie von der aktuellen Installation belegt wird. Es wird mindestens der doppelte freie Speicherplatz empfohlen, wie von der aktuellen Installation belegt wird.
     </para>
     <para>
      Falls nicht ausreichend freier Speicherplatz verfügbar ist, können Sie ältere Snapshots mit <command>snapper</command> löschen:
     </para>
<screen><prompt role="root">root # </prompt><command>snapper</command> list
<prompt role="root">root # </prompt><command>snapper</command> delete NUMBER</screen>
     <para>
      Dies reicht jedoch nicht in allen Fällen aus. Vor der Migration belegen die meisten Snapshots nur wenig Platz.
     </para>
    </listitem>
   </itemizedlist>
  </sect2>
 </sect1>
 <sect1 xml:id="sec-update-preparation-smt-to-rmt" os="sles">
  <title>Upgraden eines Subscription Management Tool (SMT)-Servers</title>
  <para>
   Für einen Server mit SMT ist ein besonderes Upgradeverfahren erforderlich. Weitere Informationen finden Sie unter <xref linkend="cha-rmt-migrate"/> im <citetitle>Handbuch zum Repository Management Tool</citetitle>.
  </para>
 </sect1>
 <sect1 xml:id="sec-update-preparation-multiversion">
  <title>Vorübergehende Deaktivierung der Unterstützung mehrerer Kernel-Versionen</title>
  <para>
   <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> ermöglicht die Installation mehrerer Kernel-Versionen. Hierfür müssen die entsprechenden Einstellungen in <filename>/etc/zypp/zypp.conf</filename> aktiviert werden. Für das Upgrade auf ein Service Pack muss diese Funktion vorübergehend deaktiviert werden. Sobald die Aktualisierung erfolgreich abgeschlossen wurde, kann die Unterstützung mehrerer Versionen wieder aktiviert werden. Zur Deaktivierung der Unterstützung mehrerer Versionen müssen die entsprechenden Zeilen in der Datei <filename>/etc/zypp/zypp.conf</filename> auf Kommentar gesetzt werden. Das Ergebnis sollte wie folgt aussehen:
  </para>
<screen>#multiversion = provides:multiversion(kernel)
#multiversion.kernels = latest,running</screen>
  <para>
   Wenn Sie diese Funktion nach einer erfolgreichen Aktualisierung wieder aktivieren möchten, entfernen Sie die Kommentarzeichen. Weitere Informationen zur Unterstützung mehrerer Versionen finden Sie in <xref linkend="sec-tuning-multikernel-enable"/>.
  </para>
 </sect1>
 <sect1 xml:id="sec-update-preparation-zseries" os="sles">
  <title>Upgraden auf IBM Z</title>
  <para>Für das Upgrade einer SUSE Linux Enterprise-Installation auf IBM Z muss der Kernel-Parameter <command>Upgrade=1</command> angegeben werden, z. B. in der parmfile. Weitere Informationen hierzu finden Sie in <xref linkend="sec-appdendix-parm" xrefstyle="HeadingOnPage"/>.
  </para>
 </sect1>
 <sect1 xml:id="sec-update-preparation-ppc" os="sles" arch="power">
  <title>IBM POWER: Starten eines X-Servers</title>
  <para>
   Unter SLES 12 für IBM POWER ist der Anzeige-Manager so konfiguriert, dass ein lokaler X-Server nicht standardmäßig gestartet wird. Diese Einstellung wurde in SLES 12 SP1 rückgängig gemacht, sodass der Anzeige-Manager jetzt einen X-Server startet.
  </para>
  <para>
   Die <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>-Einstellung wird nicht automatisch geändert, damit keine Probleme im Rahmen des Upgrades auftreten. Wenn der Anzeige-Manager nach dem Upgrade einen X-Server starten soll, ändern Sie die Einstellung von <envar>DISPLAYMANAGER_STARTS_XSERVER</envar> in <filename>/etc/sysconfig/displaymanager</filename> wie folgt:
  </para>
  <screen>DISPLAYMANAGER_STARTS_XSERVER="yes"</screen>
 </sect1>
</chapter>
