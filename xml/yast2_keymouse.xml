<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1
[
  <!ENTITY % entities SYSTEM "entity-decl.ent">
    %entities;
]>
<!-- Converted by suse-upgrade version 1.1 -->
<sect1 version="5.0" xml:id="cha.y2.hw.keym"
  xmlns="http://docbook.org/ns/docbook"
  xmlns:xi="http://www.w3.org/2001/XInclude"
  xmlns:xlink="http://www.w3.org/1999/xlink">
 <title>Setting Up Your System Keyboard Layout</title>
 <info>
      <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
        <dm:bugtracker>
        </dm:bugtracker>
      </dm:docmanager>
    </info>
    <para>
  The &yast; <guimenu>System Keyboard Layout</guimenu> module lets you
  define the default keyboard layout for the system (also used for the
  console). Users can modify the keyboard layout in their individual X
  sessions, using the desktop's tools.
 </para>

 <procedure>
  <step>
   <para>
    Start the &yast; <guimenu>System Keyboard Configuration</guimenu>
    dialog by clicking <menuchoice> <guimenu>Hardware</guimenu>
    <guimenu>System Keyboard Layout</guimenu> </menuchoice> in &yast;.
    Alternatively, start the module from the command line with <command>sudo
    yast2 keyboard</command>.
   </para>
  </step>
  <step>
   <para>
    Select the desired <guimenu>Keyboard Layout</guimenu> from the list.
   </para>
  </step>
  <step>
   <para>
    Optionally, you can also define the keyboard repeat rate or keyboard
    delay rate in the <guimenu>Expert Settings</guimenu>.
   </para>
  </step>
  <step>
   <para>
    Try the selected settings in the <guimenu>Test</guimenu> text box.
   </para>
  </step>
  <step>
   <para>
    If the result is as expected, confirm your changes and close the dialog.
    The settings are written to
    <filename>/etc/sysconfig/keyboard</filename>.
   </para>
  </step>
 </procedure>
</sect1>