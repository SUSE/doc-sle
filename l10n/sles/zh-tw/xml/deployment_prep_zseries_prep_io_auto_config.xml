<?xml version="1.0" encoding="UTF-8"?>
<sect2 xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="deployment_prep_zseries_prep_io_auto_config.xml" version="5.0" xml:id="sec-zseries-prep-io-auto-config">
 <title>IBM Z 系統上的 I/O 裝置自動組態</title>

 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>

 <para>
  I/O 裝置自動組態機制可讓使用者指定應在 Linux 上自動啟用的 I/O 裝置的 ID 和設定。透過在 DPM (動態分割區管理員) 模式下執行的 HMC 針對 LPAR 指定這些資訊。
 </para>

 <note>
  <para>
   執行 DPM 的系統上可以使用 I/O 裝置自動組態功能。DPM 在 LinuxONE 機器上預設即會執行。對於 IBM Z，則必須訂購此功能。
  </para>
 </note>

 <para>
  在 <command>linuxrc</command> 中，<parameter>DeviceAutoConfig</parameter> 參數用於控制是否為 IBM Z 系統使用 I/O 裝置自動組態資料。
 </para>
 <variablelist>
   <varlistentry>
    <term>DeviceAutoConfig=0</term>
    <listitem>
     <para>
      如果設定為 <literal>0</literal>，則會停用自動組態。
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term>DeviceAutoConfig=1</term>
    <listitem>
     <para>
      如果設定為 <literal>1</literal>，則會套用現有的自動組態資料。
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term>DeviceAutoConfig=2 (預設值)</term>
    <listitem>
     <para>如果設定為 <literal>2</literal> (預設值)，則會顯示一個對話方塊 (如果存在自動組態資料)。將詢問使用者是否應用這些資料。
     </para>
    </listitem>
   </varlistentry>
 </variablelist>

 <para>
  如果使用者停用了裝置自動組態，則會將核心參數 <parameter>rd.zdev=no-auto</parameter> 新增至目標系統的開機選項。
 </para>

 <para>
  若要使用 啟用 I/O 自動組態，請執行 <command>yast2
  system_settings</command>yast 指令，移至<guimenu>核心設定</guimenu>區段，然後啟用<guimenu>自動組態</guimenu>選項。
 </para>

 <para>
  若要在 AutoYaST 設定檔中停用 I/O 自動組態，請在全域開機載入程式選項的 <literal>append</literal> 部分新增以下核心參數。例如：
 </para>

<screen>
&lt;bootloader&gt;
  &lt;global&gt;
    &lt;append&gt;rd.zdev=no-auto&lt;/append&gt;
  &lt;/global&gt;
&lt;/bootloader&gt;</screen>

 <para os="sles;slemicro;osuse">
  如需 AutoYaST 開機載入程式選項的更多背景資訊，請參閱<xref linkend="CreateProfile-Bootloader"/>。
 </para>
 <para>
  安裝期間，<guimenu>安裝設定</guimenu>螢幕的<guimenu>裝置設定</guimenu>區段中會顯示自動組態設定的狀態。
 </para>
</sect2>
