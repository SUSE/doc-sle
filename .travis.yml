language: bash
env:
      global:
      - LD_PRELOAD=/lib/x86_64-linux-gnu/libSegFault.so

#addons:
#  apt:
#    packages:
#    - libxml2-utils
#    - libxml2-dev
#    - libxml2

install:
    - sudo apt-get install -y libxml2 libxml2-dev libxml2-utils

before_script:
    - cat /etc/os-release
    # - ls xml/*.xml
    - echo $TRAVIS_COMMIT
    - xmllint --version

script:
    - for file in xml/*.xml; do echo ">>> $file"; xmllint --noout --noent $file; done
