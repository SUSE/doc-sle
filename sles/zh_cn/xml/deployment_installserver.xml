<?xml version="1.0" encoding="UTF-8"?>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="deployment_installserver.xml" version="5.0" role="General" xml:id="cha.deployment.instserver">
 <title>设置存放安装源的服务器</title>
 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>
 <para>
  可以用多种不同的方法安装 <phrase role="productname"><phrase os="sles">SUSE® Linux Enterprise Server</phrase></phrase>。除了<xref linkend="cha.inst"/>中介绍的常用媒体安装方式之外，还可以选择多种基于网络的方式，甚至可以用完全无人值守的方式来安装 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>。
 </para>
 <para>
  两种方法各通过两个简短的核对清单进行介绍：一个列出此方法的先决条件，另一个则说明基本过程。随后，将会就这些安装方案用到的所有技术提供更详细的信息。
 </para>
 <note>
  <title>术语</title>
  <para>
   以下几节中将要存放新安装的 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 的系统称为<emphasis>目标系统</emphasis>或<emphasis>安装目标</emphasis>。术语<emphasis>安装源</emphasis>（repository，以前称为 <quote>installation source</quote>）用于所有的安装数据源。这包括物理媒体（如 CD 和 DVD）以及在网络中分发安装数据的网络服务器。
  </para>
 </note>
 <para>
  根据用作 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 网络安装源的计算机上所运行的操作系统的不同，服务器配置可有多种选择。设置安装服务器最简单的方法是使用 SUSE Linux Enterprise Server 11/openSUSE 11.1 或更高版本上的 YaST。
 </para>
 <tip>
  <title>安装服务器操作系统</title>
  <para>
   您甚至可以将 Microsoft Windows 计算机用作 Linux 部署的安装服务器。有关详细信息，请参见<xref linkend="sec.deployment.instserver.smb"/>。
  </para>
 </tip>
 <sect1 xml:id="sec.deployment.instserver.sles9">
  <title>使用 YaST 设置安装服务器</title>

  <para>
   YaST 提供了一个用于创建网络储存库的图形工具。它支持 HTTP、FTP 和 NFS 网络安装服务器。
  </para>

  <procedure xml:id="proc.deployment.instserver.sles9">
   <step>
    <para>
     以 <systemitem class="username">root</systemitem> 登录到充当安装服务器的机器上。
    </para>
   </step>
   <step>
    <para>
     启动 <menuchoice> <guimenu>YaST</guimenu>
     <guimenu> 杂项</guimenu> <guimenu> 安装服务器</guimenu></menuchoice>。
    </para>
   </step>
   <step>
    <para>
     选择储存库类型（HTTP、FTP 或 NFS）。所选的服务将在系统每次启动时自动启动。如果所选服务器类型中的某项服务已经在系统上运行，但您希望对该服务器进行手动配置，则请通过<guimenu>不配置任何网络服务</guimenu>来停用服务器服务的自动配置。在这两种情况下，都需要定义服务器上可用安装数据所在的目录。
    </para>
   </step>
   <step>
    <para>
     配置所需的储存库类型。此步骤与服务器服务的自动配置相关。如果停用自动配置，则将跳过此步骤。
    </para>
    <para>

     定义安装数据所在的 FTP 或 HTTP 服务器的 root 目录的别名。该储存库以后将放在 <literal>ftp://<replaceable>Server-IP</replaceable>/<replaceable>Alias</replaceable>/<replaceable>name</replaceable></literal>(ftp) 或 <literal>http://<replaceable>Server-IP</replaceable>/<replaceable>Alias</replaceable>/<replaceable>Name</replaceable></literal>(HTTP) 下。<replaceable>Name</replaceable> 代表储存库的名称，该名称将在下面的步骤中定义。如果您在上一步中选择了 NFS，请定义通配符和导出选项。可在 <literal>nfs://<replaceable>Server-IP</replaceable>/<replaceable>Name</replaceable></literal> 下访问 NFS 服务器。有关 NFS 和导出选项的详细信息，请参见<xref linkend="cha.nfs"/>。
    </para>
    <tip>
     <title>防火墙设置</title>
     <para>
      务必使服务器系统的防火墙设置允许 HTTP、NFS 和 FTP 端口的数据流量。先启用<guimenu>打开防火墙中的端口</guimenu>或选中<guimenu>防火墙细节</guimenu>（如果当前尚未启用或选中）。
     </para>
    </tip>
   </step>
   <step>
    <para>
     配置储存库。在将安装媒体复制到其目标位置前，请先定义该储存库的名称（最好是容易记忆的产品和版本的缩写）。YaST 允许提供安装媒体的 ISO 映像来取代安装 DVD 副本。如果希望使用 ISO 映像，请激活相关的复选框并指定 ISO 文件所在的本地目录路径。根据要使用此安装服务器分发的产品，可能需要添加附加的媒体（例如服务包 DVD）作为附加储存库。要通过 OpenSLP 在网络中就安装服务器发布通告，请激活相应的选项。
    </para>
    <tip>
     <title>通告储存库</title>
     <para>
      如果您的网络设置支持此选项，请考虑通过 OpenSLP 发布储存库通告。这样就无需在每台目标计算机上输入网络安装路径。这些目标系统会使用 SLP 引导选项引导，并会查找网络储存库，而无需任何进一步配置。有关该选项的详细信息，请参见<xref linkend="sec.deployment.remoteinst.bootinst"/>。
     </para>
    </tip>
   </step>
   <step>
    <para>
     配置额外的储存库。YaST 遵循特定的命名约定来配置外接式附件 CD 或服务包 CD 储存库。仅当外接式附件 CD 的储存库名称前面加有安装媒体的储存库名称时，系统才会接受配置；换言之，如果您选择 <literal>SLES12SP1</literal> 作为 DVD1 的储存库名称，则应选择 <literal>SLES12SP1addon</literal> 作为 DVD2 的储存库名称。这同样适用于 SDK CD。
    </para>
   </step>
   <step>
    <para>
     上载安装数据。配置安装服务器过程中最耗时的一步是复制实际的安装媒体。按 YaST 要求的顺序插入媒体，然后等待复制过程结束。当储存库完全复制完毕后，选择<guimenu>完成</guimenu>返回到现有储存库的概述并关闭配置。
    </para>
    <para>
     现在您的安装服务器就已完全配置好并准备提供服务了。它将在每次系统启动时自动启动。不需要执行额外操作。如果您在最初的步骤中使用 YaST 取消了所选网络服务的自动配置，则只需正确地手工配置和启动该服务即可。
    </para>
   </step>
  </procedure>

  <para>
   要停用储存库，可以选择要删除的储存库，然后选择<guimenu>删除</guimenu>。安装数据将从系统删除。要取消对网络服务的激活，请使用相应的 YaST 模块。
  </para>

  <para>
   如果您的安装服务器需要提供用于相同版本的多个产品的安装数据，请启动 YaST 安装服务器模块并在现有储存库的概述中选择<guimenu>添加</guimenu>，以便配置新的储存库。
  </para>
 </sect1>
 <sect1 xml:id="sec.deployment.instserver.nfs">
  <title>手动设置 NFS 安装源</title>

  

  <para>
   设置 NFS 安装源的过程主要有两步。第一步：创建存放安装数据的目录结构，然后将安装媒体全部复制到该结构中。第二步：将存放安装数据的目录导出到网络。
  </para>

  <procedure xml:id="proc.deployment.instserver.nfs.cdir">
   <para>
    要创建存放安装数据的目录，请执行如下操作：
   </para>
   <step>
    <para>
     以 <systemitem class="username">root</systemitem> 身份登录。
    </para>
   </step>
   <step>
    <para>
     创建稍后用于存放所有安装数据的目录，然后切换到该目录。例如：
    </para>
<screen><prompt role="root">root # </prompt>mkdir /srv/install/<replaceable>product</replaceable>/<replaceable>productversion</replaceable>
<prompt role="root">root # </prompt>cd /srv/install/<replaceable>product</replaceable>/<replaceable>productversion</replaceable></screen>
    <para>
     将 <replaceable>product</replaceable> 替换为产品名称的缩写，将 <replaceable>productversion</replaceable> 替换为包含该产品名称和版本的字符串。
    </para>
   </step>
   <step>
    <para>
     对媒体包中的每张 DVD，执行以下命令：
    </para>
    <substeps performance="required">
     <step>
      <para>
       将安装 DVD 的所有内容复制到安装服务器目录中：
      </para>
<screen><prompt role="root">root # </prompt>cp -a /media/<replaceable>path_to_your_DVD_drive .</replaceable></screen>
      <para>
       将 <replaceable>path_to_your_DVD_drive</replaceable> 替换为 DVD 驱动器所在的实际路径。该路径可以是 <filename>cdrom</filename>、<filename>cdrecorder</filename>、<filename>dvd</filename> 或 <filename>dvdrecorder</filename>，这取决于系统中使用的驱动器类型。
      </para>
     </step>
     <step>
      <para>
       将目录重命名为 DVD 编号：
      </para>
<screen><prompt role="root">root # </prompt>mv <replaceable>path_to_your_DVD_drive</replaceable> DVD<replaceable>x</replaceable></screen>
      <para>
       将其中的 <replaceable>x</replaceable> 替换您 DVD 的实际编号。
      </para>
     </step>
    </substeps>
   </step>
  </procedure>

  <procedure xml:id="proc.deployment.instserver.nfs.export">
   <para>
    在 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 上，可以使用 YaST 通过 NFS 导出储存库。按如下所示继续：
   </para>
   <step>
    <para>
     以 <systemitem class="username">root</systemitem> 身份登录。
    </para>
   </step>
   <step>
    <para>
     启动 <menuchoice> <guimenu>YaST</guimenu> <guimenu> 网络服务</guimenu> <guimenu> NFS 服务器</guimenu></menuchoice>。
    </para>
   </step>
   <step>
    <para>
     选择<guimenu>启动</guimenu>和<guimenu>打开防火墙中的端口</guimenu>，然后单击<guimenu>下一步</guimenu>。
    </para>
   </step>
   <step>
    <para>
     选择<guimenu>添加目录</guimenu>并浏览含有安装源的目录，此情况下指 <filename><replaceable>productversion</replaceable></filename>。
    </para>
   </step>
   <step>
    <para>
     选择<guimenu>添加主机</guimenu>，然后输入用于存放导出的安装数据的计算机主机名。此处除了指定主机名之外，还可以使用通配符、网络地址范围或网络域名。输入合适的导出选项或保留默认值，在大多数设置中默认值可有效工作。关于在导出 NFS 共享中使用的语法的更多信息，请阅读<systemitem>导出</systemitem>手册页。
    </para>
   </step>
   <step>
    <para>
     单击<guimenu>完成</guimenu>。存放 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 储存库的 NFS 服务器将自动启动并集成到引导过程中。
    </para>
   </step>
  </procedure>

  <procedure xml:id="proc.deployment.instserver.nfs.exportman">
   <para>
    如果您希望通过 NFS 手动导出储存库，而不使用 YaST NFS 服务器模块，请执行如下操作：
   </para>
   <step>
    <para>
     以 <systemitem class="username">root</systemitem> 身份登录。
    </para>
   </step>
   <step>
    <para>
     打开文件 <filename>/etc/exports</filename>，然后输入以下行：
    </para>
<screen>/<replaceable>productversion</replaceable> *(ro,root_squash,sync)</screen>
    <para>
     这将把目录 <filename>/<replaceable>productversion</replaceable></filename> 导出到该网络中的任意主机或能够连接到该服务器的任意主机。为了限制对该服务器的访问，请使用网络掩码或域名取代常规通配符 <literal>*</literal>。请参见<systemitem>导出</systemitem>手册页获取详细信息。保存并退出该配置文件。
    </para>
   </step>
   <step>
    <para>
     要将 NFS 服务添加到系统引导期间已启动的服务器的列表中，请执行以下命令：
    </para>
<screen><prompt role="root">root # </prompt>systemctl enable nfsserver</screen>
   </step>
   <step>
    <para>
     使用 <command>systemctl start nfsserver</command> 启动 NFS 服务器。如果日后需要更改 NFS 服务器的配置，请修改配置文件，然后通过 <command>systemctl restart nfsserver</command> 重启动 NFS 守护程序。
    </para>
   </step>
  </procedure>

  <procedure xml:id="proc.deployment.instserver.nfs.slpann">
   <para>
    通过 OpenSLP 就该 NFS 服务器发布通告，可使网络中的所有客户端都获知其地址。
   </para>
   <step>
    <para>
     以 <systemitem class="username">root</systemitem> 身份登录。
    </para>
   </step>
   <step>
    <para>
     使用以下命令行创建 <filename>/etc/slp.reg.d/install.suse.nfs.reg</filename> 配置文件：
    </para>
<screen><?dbsuse-fo font-size="7pt"?>

# Register the NFS Installation Server
service:install.suse:nfs://$HOSTNAME/<replaceable>path_to_repository</replaceable>/DVD1,en,65535 
description=NFS Repository</screen>
    <para>
     将 <replaceable>path_to_repository</replaceable> 替换为服务器上的安装源的实际路径。
    </para>
   </step>
   <step>
    <para>
     使用 <command>systemctl start slpd</command> 启动 OpenSLP 守护程序。
    </para>
   </step>
  </procedure>

  <para os="sles">
   关于 OpenSLP 的更多信息，请参见位于 <filename>/usr/share/doc/packages/openslp/</filename> 下的包文档，或参见<xref linkend="cha.slp"/>。有关 NFS 的更多信息，请参见<xref linkend="cha.nfs"/>。
  </para>
 </sect1>
 <sect1 xml:id="sec.deployment.instserver.ftp">
  <title>手动设置 FTP 储存库</title>

  <para>
   创建 FTP 储存库的方式与创建 NFS 储存库非常类似。也可以通过 OpenSLP 在整个网络上发布 FTP 储存库通告。
  </para>

  <procedure xml:id="proc.deployment.instserver.ftp">
   <step>
    <para>
     按<xref linkend="sec.deployment.instserver.nfs"/>中所述创建存放安装源的目录。
    </para>
   </step>
   <step>
    <para>
     配置 FTP 服务器以分发安装目录的内容：
    </para>
    <substeps performance="required">
     <step>
      <para>
       以 <systemitem class="username">root</systemitem> 身份登录，然后使用 YaST 软件管理功能安装 <systemitem>vsftpd</systemitem> 包。
      </para>
     </step>
     <step>
      <para>
       输入 FTP 服务器 root 目录：
      </para>
<screen><prompt role="root">root # </prompt>cd <option>/srv/ftp</option></screen>
     </step>
     <step>
      <para>
       在 FTP root 目录中创建存放安装源的子目录：
      </para>
<screen><prompt role="root">root # </prompt>mkdir <replaceable>repository</replaceable></screen>
      <para>
       将 <replaceable>repository</replaceable> 替换为产品名称。
      </para>
     </step>
     <step>
      <para>
       将已经存在的储存库的内容装入该 FTP 服务器的更改 root 目录环境中。
      </para>
<screen><prompt role="root">root # </prompt>mount --bind <replaceable>path_to_repository</replaceable> /srv/ftp/<replaceable>repository</replaceable></screen>
      <para>
       将 <replaceable>path_to_repository</replaceable> 和 <replaceable>repository</replaceable> 替换为符合您设置的值。如果需要将其永久保留，请将其添加到 <filename>/etc/fstab</filename>。
      </para>
     </step>
     <step>
      <para>
       通过 <command>vsftpd</command> 启动 vsftpd。
      </para>
     </step>
    </substeps>
   </step>
   <step>
    <para>
     通过 OpenSLP 发布储存库通告（如果网络设置支持此操作）：
    </para>
    <substeps performance="required">
     <step>
      <para>
       使用以下命令行创建 <filename>/etc/slp.reg.d/install.suse.ftp.reg</filename> 配置文件：
      </para>
<screen><?dbsuse-fo font-size="7pt"?>

# Register the FTP Installation Server
service:install.suse:ftp://$HOSTNAME/<replaceable>repository</replaceable>/DVD1,en,65535 
description=FTP Repository</screen>
      <para>
       将 <replaceable>repository</replaceable> 替换为服务器上的储存库目录的实际名称。<literal>service:</literal> 行应作为一个连续无中断的行输入。
      </para>
     </step>
     <step>
      <para>
       使用 <command>systemctl start slpd</command> 启动 OpenSLP 守护程序。
      </para>
     </step>
    </substeps>
   </step>
  </procedure>

  <tip os="sles">
   <title>使用 YaST 配置 FTP 服务器</title>
   <para>
    如果您更倾向于通过手动配置 FTP 安装服务器来使用 YaST，请参见<xref linkend="cha.ftp"/>了解有关如何使用 YaST FTP 服务器模块的更多信息。
   </para>
  </tip>
 </sect1>
 <sect1 xml:id="sec.deployment.instserver.http">
  <title>手动设置 HTTP 储存库</title>

  <para>
   创建 HTTP 储存库的方式与创建 NFS 储存库非常类似。也可以通过 OpenSLP 在整个网络上发布 HTTP 储存库通告。
  </para>

  <procedure xml:id="proc.deployment.instserver.http">
   <step>
    <para>
     按<xref linkend="sec.deployment.instserver.nfs"/>中所述创建存放储存库的目录。
    </para>
   </step>
   <step>
    <para>
     配置 HTTP 服务器以分发安装目录的内容：
    </para>
    <substeps performance="required">
     <step>
      <para>
       <phrase os="sles">如<xref linkend="sec.apache2.quickstart.installation"/></phrase>所述安装 Web 服务器 Apache。
      </para>
     </step>
     <step>
      <para>
       进入 HTTP 服务器的 root 目录 (<filename>/srv/www/htdocs</filename>) 并创建用于存放安装源的子目录：
      </para>
<screen><prompt role="root">root # </prompt>mkdir <replaceable>repository</replaceable></screen>
      <para>
       将 <replaceable>repository</replaceable> 替换为产品名称。
      </para>
     </step>
     <step>
      <para>
       创建一个从安装源位置到 Web 服务器 root 目录（<filename>/srv/www/htdocs</filename>）的符号链接：
      </para>
<screen><prompt role="root">root # </prompt>ln -s <replaceable>/path_to_repository </replaceable>/srv/www/htdocs/<replaceable>repository</replaceable></screen>
     </step>
     <step>
      <para>
       修改 HTTP 服务器的配置文件（<filename>/etc/apache2/default-server.conf</filename>），使其遵循符号链接。替换以下行：
      </para>
<screen>Options None</screen>
      <para>
       使用
      </para>
<screen>Options Indexes FollowSymLinks</screen>
     </step>
     <step>
      <para>
       使用 <command>systemctl reload apache2</command> 重新装载 HTTP 服务器配置。
      </para>
     </step>
    </substeps>
   </step>
   <step>
    <para>
     通过 OpenSLP 发布储存库通告（如果网络设置支持此操作）：
    </para>
    <substeps performance="required">
     <step>
      <para>
       使用以下命令行创建 <filename>/etc/slp.reg.d/install.suse.http.reg</filename> 配置文件：
      </para>
<screen><?dbsuse-fo font-size="6.5pt"?>

# Register the HTTP Installation Server
service:install.suse:http://$HOSTNAME/<replaceable>repository</replaceable>/DVD1/,en,65535 
description=HTTP Repository</screen>
      <para>
       将 <replaceable>repository</replaceable> 替换为服务器上的储存库的实际路径。<literal>service:</literal> 行应作为一个连续无中断的行输入。
      </para>
     </step>
     <step>
      <para>
       使用 <command>systemctl start slpd</command> 启动 OpenSLP 守护程序。
      </para>
     </step>
    </substeps>
   </step>
  </procedure>
 </sect1>
 <sect1 xml:id="sec.deployment.instserver.smb">
  <title>管理 SMB 储存库</title>

  <para>
   通过使用 SMB，您可以从 Microsoft Windows 服务器导入储存库，甚至可以在周围没有 Linux 计算机的情况下开始 Linux 部署。
  </para>

  <procedure xml:id="proc.deployment.instserver.smb.export">
   <para>
    要设置存放 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 储存库的导出 Windows 共享，请执行以下操作：
   </para>
   <step>
    <para>
     登录到 Windows 计算机。
    </para>
   </step>
   <step>
    <para>
     新建一个用于存放整个安装树的目录，并将其指定为诸如 <filename>INSTALL</filename> 的名称。
    </para>
   </step>
   <step>
    <para>
     根据 Windows 文档中所述的过程导入此共享。
    </para>
   </step>
   <step>
    <para>
     进入此共享，并创建名为 <filename><replaceable>product </replaceable></filename> 的子目录。请将 <replaceable>product</replaceable> 替换为实际产品名。
    </para>
   </step>
   <step>
    <para>
     进入 <filename>INSTALL/<replaceable>product</replaceable></filename> 目录，并将每个 DVD 复制到独立的目录，例如 <literal>DVD1</literal> 和 <literal>DVD2</literal>。
    </para>
   </step>
  </procedure>

  <para>
   要将装入的 SMB 共享用作储存库，请执行如下操作：
  </para>

  <procedure xml:id="proc.deployment.instserver.smb.use">
   <step>
    <para>
     引导安装目标。
    </para>
   </step>
   <step>
    <para>
     选择<guimenu>安装</guimenu>：
    </para>
   </step>
   <step>
    <para>
     按 <keycap>F4</keycap> 选择储存库。
    </para>
   </step>
   <step>
    <para>
     选择 SMB，然后输入 Windows 计算机的名称或 IP 地址、共享名（在本例中为 <filename>INSTALL/<replaceable>product</replaceable>/DVD1</filename>）、用户名和口令。语法如下所示：
    </para>
<screen>smb://workdomain;user:password@server/INSTALL/DVD1</screen>
    <para>
     按 <keycap function="enter"/> 后 YaST 将会启动，您便可以执行安装了。
    </para>
   </step>
  </procedure>
 </sect1>
 <sect1 xml:id="sec.deployment.remoteinst.iso">
  <title>使用服务器上安装媒体的 ISO 映像</title>

  <para>
   您不用将物理媒体手动复制到服务器目录下，而是可以将安装媒体的 ISO 映像装入安装服务器中并将它们用作储存库。要设置使用 ISO 映像，而不是媒体副本的 HTTP、NFS 或 FTP 服务器，请执行以下操作：
  </para>

  <procedure>
   <step>
    <para>
     下载 ISO 映像并将它们保存到用作安装服务器的计算机上。
    </para>
   </step>
   <step>
    <para>
     以 <systemitem class="username">root</systemitem> 身份登录。
    </para>
   </step>
   <step>
    <para>
     按照<xref linkend="sec.deployment.instserver.nfs"/>、<xref linkend="sec.deployment.instserver.ftp"/>或<xref linkend="sec.deployment.instserver.http"/>中的描述，选择并创建安装数据的合适位置。
    </para>
   </step>
   <step>
    <para>
     创建对应于每个 DVD 的子目录。
    </para>
   </step>
   <step>
    <para>
     要将各个 ISO 映像安装和解开到最终位置，请发出以下命令：
    </para>
<screen><prompt role="root">root # </prompt>mount -o loop <replaceable>path_to_iso</replaceable> <replaceable>path_to_repository</replaceable>/<replaceable>product</replaceable>/<replaceable>mediumx</replaceable></screen>
    <para>
     将 <replaceable>path_to_iso</replaceable> 替换为 ISO 映像本地副本的路径，将 <replaceable>path_to_repository</replaceable> 替换为服务器的目录，将 <replaceable>product</replaceable> 替换为产品名称以及将 <replaceable>mediumx</replaceable> 替换为您正使用的媒体类型（CD 或 DVD）和编号。
    </para>
   </step>
   <step>
    <para>
     多次重复上述步骤，以安装产品所需的全部 ISO 映像。
    </para>
   </step>
   <step>
    <para>
     按照<xref linkend="sec.deployment.instserver.nfs"/>、<xref linkend="sec.deployment.instserver.ftp"/>或<xref linkend="sec.deployment.instserver.http"/>中的描述，与往常一样启动安装服务器。
    </para>
   </step>
  </procedure>

  <para>
   要在引导时自动装入各 ISO 映像，请将各自的装入项添加到 <filename>/etc/fstab</filename> 中。依照上一示例的项显示如下：
  </para>

<screen><replaceable>path_to_iso</replaceable> <replaceable>path_to_repository</replaceable>/<replaceable>product</replaceable><replaceable>medium</replaceable> auto loop</screen>
 </sect1>
</chapter>
