<?xml version="1.0" encoding="UTF-8"?>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="deployment_mksusecd.xml" version="5.0" role="General" xml:id="cha-deployment-prep-mksusecd">
 <title>Personalizzazione delle immagini di installazione con mksusecd</title>
 <info>
  <abstract>
   <para>
    <command>mksusecd</command> è uno strumento utile per creare un'immagine di installazione personalizzata. Utilizzarlo per modificare le normali immagini di installazione di SUSE Linux Enterprise, aggiungendo e rimuovendo file, creando un'immagine di installazione di rete minima, personalizzando le opzioni di avvio e repository e creando un'immagine di avvio minima come alternativa all'avvio di un sistema da un server PXE.
   </para>
  </abstract>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>sì</dm:translation>
  </dm:docmanager>
 </info>
 <sect1 xml:id="sec-deployment-install-mksusecd">
  <title>Installazione di mksusecd</title>

  <para>
   In SLE 15 <command>mksusecd</command> si trova nel <literal>modulo Strumenti di sviluppo</literal>. Se il modulo non è stato abilitato, eseguire questa operazione. Individuare il nome del modulo esatto con <command>zypper</command>:
  </para>

<screen><prompt>tux &gt; </prompt>zypper search-packages mksusecd
Following packages were found in following modules:

Package               Module or Repository
--------------------  -----------------------------------------------------------------
mksusecd              Development Tools Module (sle-module-development-tools/15/x86_64)
mksusecd-debuginfo    Development Tools Module (sle-module-development-tools/15/x86_64)
mksusecd-debugsource  Development Tools Module (sle-module-development-tools/15/x86_64)
mksusecd              Available
srcpackage:mksusecd   Available

To activate the respective module or product, use SUSEConnect --product.
Use SUSEConnect --help for more details.</screen>

  <para>
   Abilitarlo con SUSEConnect:
  </para>

<screen><prompt>tux &gt; </prompt><command>sudo</command> SUSEConnect -p sle-module-development-tools/15/x86_64</screen>

  <para>
   In SLE 15.1 e versioni successive si tratta di <literal>Repository principale degli aggiornamenti</literal>, abilitato per impostazione predefinita.
  </para>

  <para>
   Installare <command>mksusecd</command> come di consueto:
  </para>

<screen><prompt>tux &gt; </prompt><command>sudo</command> zypper se mksusecd</screen>

  <para>
   Eseguire <command>mksusecd --help</command> per visualizzare un elenco completo di comandi.
  </para>

  <para>
   Dopo aver creato l'immagine personalizzata, masterizzarla su un CD/DVD utilizzando un programma di scrittura su disco come Brasero o <command>mybashburn</command> oppure creare un'unità flash USB di avvio mediante il comando <command>dd</command>. Accertarsi che il dispositivo non sia montato, quindi eseguire il comando:
  </para>

<screen><prompt role="root">root # </prompt><command>dd</command> if=<replaceable>min-install.iso</replaceable> of=/dev/<replaceable>SDB</replaceable> bs=4M</screen>

  <para>
   Il nuovo dispositivo di avvio è pronto per l'uso.
  </para>
 </sect1>
 <sect1 xml:id="sec-deployment-prep-boot-mksusecd">
  <title>Creazione di un'immagine di avvio minima</title>

  <para>
   Utilizzare <command>mksusecd</command> per creare un'immagine di avvio minima per l'avvio dei computer client da un CD/DVD o un'unità flash USB, anziché da un server di avvio PXE. L'immagine minima esegue l'avvio del kernel e di initrd, quindi i file di installazione rimanenti vengono recuperati da un server NFS locale (consultare la <xref linkend="sec-deployment-instserver-sles9"/>).
  </para>

  <para>
   Eseguire il comando seguente per creare l'immagine ISO minima:
  </para>

<screen><prompt>tux &gt; </prompt><command>sudo</command> <command>mksusecd</command> --create <replaceable>min-install.iso</replaceable> \
--net=nfs://<replaceable>192.168.1.1:/srv/install/</replaceable><replaceable>ARCH</replaceable>/<replaceable>OS_VERSION</replaceable>/<replaceable>SP_VERSION</replaceable>/cd1  \
/srv/tftpboot/EFI/<replaceable>ARCH</replaceable>/boot</screen>

  <para>
   Sostituire l'indirizzo del server NFS con il proprio. Sostituire <replaceable>ARCH</replaceable> con la directory corrispondente nell'architettura del sistema di destinazione. Sostituire anche <replaceable>OS_version</replaceable> e <replaceable>SP_VERSION</replaceable> (Service Pack) in base ai percorsi nella <xref linkend="sec-deployment-instserver-sles9"/>.
  </para>



  


 </sect1>
 <sect1 xml:id="sec-deployment-custom-image-boot">
  <title>Impostare parametri di avvio del kernel predefiniti</title>

  <para>
   Anziché attendere un prompt di avvio per accedere ai parametri di avvio del kernel, configurarli in un'immagine <command>mksusecd</command> personalizzata:
  </para>

<screen><prompt>tux &gt; </prompt><command>sudo</command> mksusecd --create <replaceable>install.iso</replaceable> \
--boot "<replaceable>textmode=1 splash=silent mitigations=auto</replaceable>"</screen>

  <para>
   Verificare che i parametri personalizzati siano stati caricati correttamente dopo l'avvio eseguendo la query <filename>/proc</filename>:
  </para>

<screen><prompt>tux &gt; </prompt>cat /proc/cmdline</screen>
 </sect1>
 <sect1 xml:id="sec-deployment-custom-modules">
  <title>Personalizzare moduli, estensioni e repository</title>

  <para>
   SUSE Linux Enterprise 15 supporta moduli (da non confondere con i moduli kernel) ed estensioni per vari componenti di prodotti. Si tratta di componenti aggiuntivi del sistema di base predefinito, come Strumenti di sviluppo, Applicazioni desktop e SUSE Linux Enterprise Live Patching. Per ulteriori informazioni, consultare la guida di <citetitle>riferimento rapido per moduli ed estensioni</citetitle>.
  </para>

  <para>
   Con <command>mksusecd</command> è possibile creare un'immagine di installazione contenente tutti i moduli e le estensioni aggiuntivi desiderati. Per iniziare, eseguire una query sulle immagini esistenti, come in questo esempio per SUSE Linux Enterprise <phrase role="productnumber"><phrase os="sles;sled">15 SP2</phrase></phrase>:
  </para>

  <screen><prompt>tux &gt; </prompt><command>sudo</command> mksusecd --list-repos SLE-15-SP2-Full-<replaceable>ARCH</replaceable>-GM-media1.iso
Repositories:
  SLES15-SP2 [15.2-0]
  SLES15 [15.2-0]
  Basesystem-Module [15.2-0]
  SUSE-CAP-Tools-Module [15.2-0]
  Containers-Module [15.2-0]
  Desktop-Applications-Module [15.2-0]
  Development-Tools-Module [15.2-0]
</screen>

  <para>
   Creare una nuova immagine di installazione basata sui moduli, sulle estensioni e sui repository selezionati ed eseguirne l'abilitazione automatica:
  </para>

<screen><prompt>tux &gt; </prompt><command>sudo</command> mksusecd --create <replaceable>myinstaller.iso</replaceable>\
  --enable-repos auto --include-repos <replaceable>Basesystem-Module,Desktop-Applications-Module</replaceable> \
  SLE-15-SP2-Online-<replaceable>ARCH</replaceable>-GM-media1.iso \
  SLE-15-SP2-Full-<replaceable>ARCH</replaceable>-GM-media1.iso
  </screen>

  <para>
   In questo modo, il file <filename>add_on_products.xml</filename> viene creato e aggiunto alla nuova immagine. Sostituire <command>--enable-repos auto</command> con <command>--enable-repos ask</command>. Il programma di installazione mostra una finestra di dialogo per la selezione dei moduli.
  </para>

  <note>
   <title>file di controllo di AutoYaST</title>
   <para>
    Se si utilizza AutoYaST per configurare le installazioni, non è necessario elencare questi moduli nel file di controllo di AutoYaST se si utilizza l'opzione <command>--enable-repos</command>.
   </para>
  </note>
 </sect1>
 <sect1 xml:id="sec-deployment-custom-netinstall">
  <title>Creazione di un'immagine ISO di installazione di rete minima</title>

  <para>
   Per creare un'immagine di installazione minima per l'avvio di un'installazione di rete, utilizzare l'opzione <command>--nano</command>:
  </para>

<screen><prompt>tux &gt; </prompt><command>sudo</command> mksusecd --create <replaceable>netinstall.iso</replaceable> \
--nano SLE-15-SP2-Online-<replaceable>ARCH</replaceable>-GM-media1.iso</screen>
 </sect1>
 <sect1 xml:id="sec-deployment-custom-repo">
  <title>Modificare il repository predefinito</title>

  <para>
   Per impostare un repository diverso, ad esempio un repository locale, utilizzare l'opzione <command>--net</command>:
  </para>

<screen><prompt>tux &gt; </prompt><command>sudo</command> mksusecd --create <replaceable>localinstall.iso</replaceable> \
--net "https://example.com/local" SLE-15-SP2-Online-<replaceable>ARCH</replaceable>-GM-media1.iso</screen>
 </sect1>
</chapter>
