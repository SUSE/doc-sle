<?xml version="1.0" encoding="UTF-8"?>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="net_proxy.xml" version="5.0" xml:id="cha.squid">
 <title>代理服务器 Squid</title>
 <info>
  <abstract>
   <para>
    Squid 是广泛用于 Linux 和 UNIX 平台的代理超速缓存。这表示它会将请求的因特网对象（例如 Web 或 FTP 服务器上的数据）储存在离请求工作站更近（与服务器相比）的计算机上。可以在多个层次中设置它，以确保即使在对最终用户来说是透明的模式下，也能达到最佳的响应速度和较低的带宽用量。可以使用其他诸如 squidGuard 的软件来过滤 Web 内容。
   </para>
  </abstract>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info><indexterm> <primary>Squid</primary></indexterm><indexterm> <primary>代理</primary> <see>Squid</see></indexterm><indexterm> <primary>Squid</primary> <secondary>超速缓存</secondary></indexterm><indexterm> <primary>代理</primary> <secondary>超速缓存</secondary></indexterm>
 <para>
  Squid 可以充当代理缓存。它将来自客户端（这里指来自 Web 浏览器）的对象请求重定向至服务器。当服务器回复所请求的对象后，它会将这些对象传递给客户端并在硬盘缓存中保存对象副本。超速缓存的其中一个优点是，当多个客户端请求同一对象时，可以从硬盘超速缓存提供该对象。这样客户端接收数据的速度要比从因特网接收快得多。此过程还可以减少网络流量。<indexterm><primary>代理</primary><secondary>优点</secondary> </indexterm>
 </para>
 <para>
  <indexterm> <primary>Squid</primary> <secondary>功能</secondary> </indexterm>除实际的超速缓存外，Squid 还提供众多其他功能：
 </para>
 <itemizedlist>
  <listitem>
   <para>
    在代理服务器的互通层次之间分配负载
   </para>
  </listitem>
  <listitem>
   <para>
    为所有访问代理的客户端定义严密的访问控制列表
   </para>
  </listitem>
  <listitem>
   <para>
    允许或拒绝使用其他应用程序访问特定网页
   </para>
  </listitem>
  <listitem>
   <para>
    生成有关频繁访问的网页的统计数字以评估用户的浏览习惯
   </para>
  </listitem>
 </itemizedlist>
 <para>
  Squid 不是通用代理。通常只充当 HTTP 连接的代理。它支持 FTP、Gopher、SSL 和 WAIS 协议，但不支持其他因特网协议，如新闻协议或视频会议协议。由于 Squid 只支持将 UDP 协议用于在不同的超速缓存间提供通讯功能，因此很多多媒体程序都不受支持。
 </para>
 <sect1 xml:id="sec.squid.cacheinfos">
  <title>有关代理缓存的一些事实</title>

  <para>
   作为代理缓存，Squid 的使用方法分为几种。与防火墙结合使用时，能够提高安全性。可以一起使用多个代理。还能确定应该缓存的对象的类型以及缓存的时间。
  </para>

  <sect2 xml:id="sec.squid.security">
   <title>Squid 和安全性</title><indexterm> <primary>Squid</primary><secondary>安全性</secondary></indexterm><indexterm><primary>安全性</primary><secondary>Squid</secondary></indexterm>
   <para>
    Squid 可以与防火墙结合起来，通过使用代理缓存防止内部网络遭受外部攻击。防火墙会拒绝 Squid 之外的所有客户端对外部服务的访问。所有 Web 连接都必须通过代理方式建立。经过此配置后，Squid 便可全面控制 Web 访问。
   </para>
   <para>
    <indexterm><primary>Squid</primary><secondary>安全性</secondary></indexterm> 如果防火墙配置中包含 DMZ，代理应该在此区域内操作。<xref linkend="sec.squid.proxyconfigtrans"/>描述了如何实施<emphasis>透明</emphasis>代理。它能简化客户端的配置，因为在这种情况下，它们不需要有关代理的任何信息。
   </para>
  </sect2>

  <sect2 xml:id="sec.squid.morecaches">
   <title>多个缓存</title><indexterm> <primary>Squid</primary><secondary>缓存</secondary> </indexterm>
   <para>
    可以配置 Squid 的几个实例从而在它们之间交换对象。这样会降低系统总负载，提高从本地网络检索对象的几率。还可以配置超速缓存层次，以便超速缓存能够将对象请求转发给同级超速缓存或父超速缓存，如此其可从本地网络中的其他超速缓存或直接从数据源请求对象。
   </para>
   <para>
    为了不给网络增加总体数据流量，为缓存层次选择适当的拓扑结构是十分重要的。对于超大型网络，合理的做法是：为每个子网配置一个代理服务器并将其连接至父代理，再通过父代理连接至 ISP 的代理超速缓存。
   </para>
   <para>
    所有这些通讯都通过在 UDP 协议之上运行的 ICP（因特网缓存协议）来处理。缓存间的数据传送使用基于 TCP 的 HTTP（超文本传送协议）来处理。
   </para>
   <para>
    为了找到最适合向其请求对象的服务器，一个超速缓存会向所有同级代理发送 ICP 请求。同级代理会通过 ICP 响应回复这些请求。如果检测到对象，它们会使用代码 <literal>HIT</literal>，若未检测到，则会使用 <literal>MISS</literal>。
   </para>
   <para>
    如果发现多个 <literal>HIT</literal> 响应，代理服务器会根据哪个超速缓存发送回复的速度最快或哪个服务器较近等因素决定从哪个服务器下载。如果没有收到满意的响应，该请求会发送至父超速缓存。<remark>Is timestamp a factor? - sknorr, 2016-06-24</remark>
   </para>
   <note>
    <title>Squid 如何避免对象重复</title>
    <para>
     为了避免网络中不同缓存中的对象重复，还会使用其他 ICP 协议，如 CARP（缓存阵列路由协议）或 HTCP（超文本缓存协议）。网络中维护的对象越多，找到所需对象的可能性就越大。
    </para>
   </note>
  </sect2>

  <sect2 xml:id="sec.squid.latching">
   <title>缓存因特网对象</title><indexterm> <primary>Squid</primary><secondary>对象状态</secondary> </indexterm>
   <para>
    网络中提供的许多对象都不是静态的，例如动态生成的页面和 TLS/SSL 加密内容。由于每次访问这类对象时它们都会更改，所以它们不会被超速缓存。<remark>Squid can now decrypt TLS traffic with plugins. Not sure whether
     that is then stored or not. Also, not sure how/whether to get this
     information in here. - sknorr, 2016-06-24
    </remark>
   </para>
   <para>
    为确定对象应保留在超速缓存中的期限，系统会为对象指派多个状态中的一种。Web 和代理服务器会通过为这些对象添加报头来找出对象的状态（如<quote>Last modified</quote>或<quote>Expires</quote>）以及相应的日期。也可以使用指定不能超速缓存的对象的其他报头。
   </para>
   <para>
    超速缓存中的对象通常会因缺少可用磁盘空间而通过 LRU（最近最少使用）之类的算法进行替换。这意味着代理会删除未被请求时间最长的对象。
   </para>
  </sect2>
 </sect1>
 <sect1 xml:id="sec.squid.sysneeds">
  <title>系统要求</title><indexterm> <primary>Squid</primary><secondary>系统要求</secondary> </indexterm>

  <para>
   系统要求主要取决于系统必须承受的最大网络负载。由于负载在高峰期间可能是日均值的四倍以上，因此需要检查负载峰值。如果不能确定，请将系统要求稍微高估一点。让 Squid 在濒临其处理能力上限的状态下工作可能会严重影响其服务质量。以下几节按重要程度依次阐述了各个系统要素：
  </para>



  <orderedlist>
   <listitem>
    <para>
     RAM 大小
    </para>
   </listitem>
   <listitem>
    <para>
     CPU 速度/物理 CPU 内核
    </para>
   </listitem>
   <listitem>
    <para>
     磁盘超速缓存的大小
    </para>
   </listitem>
   <listitem>
    <para>
     硬盘/SSD 及其体系结构
    </para>
   </listitem>
  </orderedlist>

  <sect2 xml:id="sec.squid.ram">
   <title>RAM</title><indexterm> <primary>Squid</primary><secondary>RAM</secondary> </indexterm>
   <para>
    Squid 所需内存 (RAM) 大小与超速缓存中的对象数有直接的关系。随机存取内存比硬盘/SSD 快得多。因此，让 Squid 进程拥有足够的内存非常重要，因为如果进程必须交换到磁盘的话，系统性能会显著降低。
   </para>
   <para>
    Squid 还会在主储存器中储存缓存对象引用和经常请求的对象，以加速对这些数据的检索。除此之外，Squid 还要在内存中保存其他数据，如：所有已处理 IP 地址的表、准确域名缓存、最常请求的对象、访问控制列表、缓冲区等等。
   </para>



  </sect2>

  <sect2 xml:id="sec.squid.cpu">
   <title>CPU</title><indexterm> <primary>Squid</primary><secondary>CPU</secondary> </indexterm>
   <para>
    Squid 已经过优化，最适合处理器内核较少的环境（4–8 个物理内核），这样每个内核都能提供高性能。提供虚拟核心的技术（如超线程）可能会降低性能。
   </para>
   <para>
    要充分利用多个 CPU 内核，必须设置多个写入不同超速缓存设备的工作线程。默认情况下，多核心支持通常都是禁用的。
   </para>

  </sect2>

  <sect2 xml:id="sec.squid.sysneeds.cache_size">
   <title>磁盘缓存的大小</title><indexterm> <primary>Squid</primary><secondary>缓存</secondary><tertiary>大小</tertiary> </indexterm>
   <para>
    在小型超速缓存中，<literal>HIT</literal>（在其中找到所请求的对象）的概率会很小，因为该超速缓存很容易被占满，所以较少请求的对象很快被较新的请求对象替代。例如，如果超速缓存的可用空间为 1 GB，而用户每天浏览所用的空间只有 10 MB，那么至少需要 100 多天才会占满超速缓存。
   </para>
   <para>
    确定所需超速缓存大小的最简单的方法就是参考连接的最大传输速度。1 Mbit/s 连接的最大传输速度为 128 KB/s。如果所有流量都进入超速缓存，1 小时内累计可达到 460 MB。假设所有流量都是在 8 小时工作时间生成的，那么每天将达到 3.6 GB。由于连接速度一般不会达到流量上限，所以可以认为缓存处理的数据总量约为 2 GB。因此，在本示例中，Squid 需要 2 GB 的磁盘空间来保存一天内超速缓存的数据浏览量。
   </para>
  </sect2>

  <sect2 xml:id="sec.squid.sysneeds.disk">
   <title>硬盘/SSD 体系结构</title>

   <para>
    速度在缓存过程中起到重要作用，所以此要素值得特别关注。对于硬盘/SSD，此参数通过以毫秒度量的<emphasis>随机搜索时间</emphasis>或<emphasis>随机读取性能</emphasis>来描述。Squid 从硬盘/SSD 读取或写入其中的数据块一般都较小，因此硬盘/SSD 的搜索时间/读取性能比其数据吞吐量更重要。
   </para>
   <para>
    如果要充当代理，高转速硬盘或者 SSD 会是最好的选择。使用硬盘时，使用多个较小硬盘的效果可能更好，这样每个硬盘都有单独的超速缓存目录，可避免读取次数过多。
   </para>
   <para>
    如果使用 RAID 系统，可以通过牺牲速度来提高可靠性。不过，出于性能原因，请避免使用（软件）RAID5 及类似设置。
   </para>
   <para>
    文件系统的选择通常无关紧要。不过，使用装入选项 <literal>noatime</literal> 可以提高性能 - Squid 提供其自己的时间戳，如此便不需要文件系统来跟踪访问时间。
   </para>
  </sect2>
 </sect1>
 <sect1 xml:id="sec.squid.start">
  <title>Squid 的基本用法</title><indexterm> <primary>Squid</primary><secondary>启动</secondary> </indexterm>

  <para>
   如果尚未安装，请安装包 
   <package>squid</package>
   。<phrase role="productname"><phrase os="sles">SUSE® Linux Enterprise Server</phrase></phrase> 上默认不会安装 <systemitem>squid</systemitem> 包。
  </para><indexterm> <primary>配置文件</primary><secondary>resolv.conf</secondary> </indexterm>

  <para>
   Squid 已在 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 中预先配置好，因此安装完后即可直接启动。为保证顺利启动，应该对网络进行配置，使其至少能连接一个名称服务器和因特网。如果拨号连接使用动态 DNS 配置，则可能出现问题。在这种情况下，至少应该指定名称服务器，因为如果在 <filename>/etc/resolv.conf</filename> 中未检测到 DNS 服务器，Squid 便不会启动。
  </para>

  <sect2 xml:id="sec.squid.start.commands">
   <title>启动 Squid</title><indexterm> <primary>Squid</primary><secondary>目录</secondary></indexterm><indexterm><primary>脚本</primary><secondary>init.d</secondary><tertiary>squid</tertiary></indexterm><indexterm><primary>Squid</primary><secondary>权限</secondary></indexterm><indexterm><primary>配置文件</primary><secondary>squid.conf</secondary></indexterm>
   <para>
    要启动 Squid，请使用：
   </para>
<screen><prompt>tux &gt; </prompt><command>sudo systemctl start squid</command></screen>
   <para>
    如果想让 Squid 随系统一起启动，请使用 <command>systemctl enable squid</command> 启用该服务。
   </para>
  </sect2>

  <sect2 xml:id="sec.squid.check">
   <title>检查 Squid 是否正在工作</title>
   <para>
    要检查 Squid 是否在运行，请选择以下其中一种方法：
   </para>
   <itemizedlist>
    <listitem>
     <para>
      使用 <command>systemctl</command>：
     </para>
<screen><prompt>tux &gt; </prompt><command>systemctl start squid</command></screen>
     <para>
      此命令的输出应指出 Squid <literal>已装载</literal>且<literal>处于活动状态（运行中）</literal>。
     </para>
    </listitem>
    <listitem>
     <para>
      使用 Squid 本身：
     </para>
<screen><prompt>tux &gt; </prompt><command>sudo squid -k | echo $?</command></screen>
     <para>
      此命令的输出应为 <literal>0</literal>，且无其他讯息。
     </para>
    </listitem>
   </itemizedlist>
   <para>
    要测试 Squid 在本地系统上的功能，请选择以下其中一种方法：
   </para>
   <itemizedlist>
    <listitem>
     <para>
      要进行测试，您可以使用命令行工具 <command>squidclient</command>，它可向 Web 请求输出响应，类似于 <command>wget</command> 或 <command>curl</command>。
     </para>
     <para>
      与这些工具不同的是，<command>squidclient</command> 将自动连接到 Squid 的默认代理设置 <literal>localhost:3128</literal>。不过，如果您更改过 Squid 的配置，则需要通过命令行选项将 <command>squidclient</command> 配置为使用不同的设置。有关详细信息，请参见 <command>squidclient --help</command>。
     </para>
     <example xml:id="ex.squidclient.request">
      <title>通过 <command>squidclient</command> 提交的请求</title>
<screen><prompt>tux &gt; </prompt><command>squidclient http://www.example.org</command>
HTTP/1.1 200 OK
Cache-Control: max-age=604800
Content-Type: text/html
Date: Fri, 22 Jun 2016 12:00:00 GMT
Expires: Fri, 29 Jun 2016 12:00:00 GMT
Last-Modified: Fri, 09 Aug 2013 23:54:35 GMT
Server: ECS (iad/182A)
Vary: Accept-Encoding
X-Cache: HIT
x-ec-custom-error: 1
Content-Length: 1270
X-Cache: MISS from moon<co xml:id="co.squid.miss"/>
X-Cache-Lookup: MISS from moon:3128
Via: 1.1 moon (squid/3.5.16)<co xml:id="co.squid.via"/>
Connection: close

&lt;!doctype html&gt;
&lt;html&gt;
&lt;head&gt;
    &lt;title&gt;Example Domain&lt;/title&gt;
[...]
&lt;/body&gt;
&lt;/html&gt;</screen>
      <para>
       <xref linkend="ex.squidclient.request"/>中显示的输出可分成两个部分：
      </para>
      <orderedlist>
       <listitem>
        <para>
         响应的协议报头：空白行前面的行。
        </para>
       </listitem>
       <listitem>
        <para>
         响应的实际内容：空白行后面的行。
        </para>
       </listitem>
      </orderedlist>
      <para>
       要校验是否使用了 Squid，请参见所选的报头行：
      </para>
      <calloutlist>
       <callout arearefs="co.squid.miss">
        <para>
         报头 <literal>X-Cache</literal> 的值告诉您请求的文档不在计算机 <systemitem>moon</systemitem> 的 Squid 超速缓存中 (<literal>MISS</literal>)。
        </para>
        <para>
         上面的示例包含两行 <literal>X-Cache</literal>。您可以忽略第一个 <literal>X-Cache</literal> 报头。它是由源 Web 服务器的内部超速缓存软件生成的。
        </para>
       </callout>
       <callout arearefs="co.squid.via">
        <para>
         报头 <literal>Via</literal> 的值告诉您 HTTP 版本、计算机名称以及所使用的 Squid 版本。
        </para>
       </callout>
      </calloutlist>
     </example>
    </listitem>
    <listitem>
     <para>
      使用浏览器：将代理设置为 <option>localhost</option> 并将端口设置为 <option>3128</option>。然后您可以装载页面，并在浏览器的<emphasis>检查器</emphasis>或<emphasis>开发人员工具</emphasis>的<guimenu>网络</guimenu>面板中检查响应报头。报头应该会以<xref linkend="ex.squidclient.request"/>中所示的类似方式再现。
     </para>
    </listitem>
   </itemizedlist>
   <para>
    要允许用户从本地系统和其他系统访问 Squid 和因特网，需要将配置文件 <filename>/etc/squid/squid.conf</filename> 中的项 <option>http_access deny all</option> 改为 <option>http_access allow all</option>。但在这样做时，要考虑到此操作会让所有人都不受任何限制地访问 Squid。因此，需定义可控制对代理的访问权限的 ACL（访问控制列表）。修改配置文件后，必须重新装载或重启动 Squid。有关 ACL 的详细信息，请参见<xref linkend="sec.squid.acl"/>。
   </para><indexterm> <primary>Squid</primary><secondary>日志文件</secondary></indexterm><indexterm><primary>日志文件</primary><secondary>Squid</secondary></indexterm><indexterm><primary>Squid</primary><secondary>查错</secondary></indexterm>
   <para>
    如果 Squid 在成功启动后不久就终止，请检查名称服务器项是否有误或者是否缺少 <filename>/etc/resolv.conf</filename> 文件。Squid 会在 <filename>/var/log/squid/cache.log</filename> 文件中记录启动失败的原因。
   </para>
  </sect2>

  <sect2 xml:id="sec.squid.stop">
   <title>停止、重新装载和重启动 Squid</title><indexterm> <primary>Squid</primary><secondary>停止</secondary> </indexterm>
   <para>
    请使用 <command>systemctl reload squid</command> 完成此操作。或者，使用 <command>systemctl restart squid</command> 将 Squid 彻底重启动。
   </para>
   <para>
    使用命令 <command>systemctl stop squid</command> 可关闭 Squid。这需要一些时间，因为 Squid 在断开同客户端的连接并将其数据写入磁盘前会等候最多半分钟（<filename>/etc/squid/squid.conf</filename> 中的 <option>shutdown_lifetime</option> 选项）。
   </para>
   <warning>
    <title>终止 Squid</title>
    <para>
     <indexterm><primary>Squid</primary><secondary>缓存</secondary><tertiary>已损坏</tertiary></indexterm> 使用命令 <command>kill</command> 终止 Squid，否则 <command>killall</command> 可损坏缓存。要能够重启动 Squid，必须删除损坏的超速缓存。
    </para>
   </warning>
  </sect2>

  <sect2 xml:id="sec.squid.remove">
   <title>去除 Squid</title><indexterm> <primary>Squid</primary><secondary>卸载</secondary> </indexterm>
   <para>
    从系统中去除 Squid 并不会去除超速缓存层次和日志文件。要删除这些内容，请手动删除 <filename>/var/cache/squid</filename> 目录。
   </para>
  </sect2>

  <sect2 xml:id="sec.squid.start.local_dns">
   <title>本地 DNS 服务器</title><indexterm> <primary>DNS</primary><secondary>Squid 和</secondary></indexterm><indexterm><primary>Squid</primary><secondary>DNS</secondary></indexterm><indexterm><primary>配置文件</primary><secondary>named.conf</secondary></indexterm>
   <para>
    建立本地 DNS 服务器很有意义，即便并不用它来管理自己的域。它仅起到缓存专用名称服务器的作用，并且可以在无需任何特殊配置的情况下通过 root 名称服务器解析 DNS 请求（请参见 <xref linkend="sec.dns.bind"/>）。如何完成上述操作，取决于您在配置因特网连接的过程中是否选择了动态 DNS。
   </para>
   <variablelist>
    <varlistentry xml:id="vle.squid.dynamicdns">
     <term>动态 DNS</term>
     <listitem>
      <para>
       使用动态 DNS 时，因特网服务提供商通常在建立因特网连接过程中设置 DNS 服务器，并自动调整本地文件 <filename>/etc/resolv.conf</filename>。可以在文件 <filename>/etc/sysconfig/network/config</filename> 中使用以下 sysconfig 变量控制此行为：
       <constant>NETCONFIG_DNS_POLICY</constant> 。设置
       <constant>NETCONFIG_DNS_POLICY</constant> 更改为 <constant>""</constant>
       。
      </para>
      <para>
       然后在 <filename>/etc/resolv.conf</filename> 文件中添加本地 DNS 服务器，并将 <systemitem class="ipaddress">localhost</systemitem> 的 IP 地址设置为 <systemitem class="domainname">127.0.0.1</systemitem>。这样，Squid 每次一启动就能找到本地名称服务器。
      </para>
      <remark>FIXME ke: check it. Do you have to set more variables?</remark>
      <para>
       为了使提供商的名称服务器可访问，请在配置文件 <filename>/etc/named.conf</filename> 中的 <systemitem>forwarders</systemitem> 下指定该名称服务器及其 IP 地址。如果使用动态 DNS，则可在建立连接时自动完成此操作，方法是将 sysconfig 变量
       <constant>NETCONFIG_DNS_POLICY</constant> 更改为 <constant>auto</constant>。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>静态 DNS</term>
     <listitem>
      <para>
       有了静态 DNS，在建立连接时自动 DNS 调整便不会发生，所以不需要更改任何 sysconfig 变量。但是，必须按<xref linkend="vle.squid.dynamicdns"/>中所述在 <filename>/etc/resolv.conf</filename> 文件中指定本地 DNS 服务器。此外，还必须在 <filename>/etc/named.conf</filename> 文件中的 <systemitem>forwarders</systemitem> 下手动指定提供商的静态名称服务器及其 IP 地址。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
   <tip>
    <title>DNS 和防火墙</title>
    <para>
     如果运行了防火墙，应确保 DNS 请求能够通过。
    </para>
   </tip>
  </sect2>
 </sect1>
 <sect1 xml:id="sec.squid.configfile">
  <title>配置文件 /etc/squid/squid.conf</title><indexterm> <primary>Squid</primary><secondary>配置</secondary></indexterm><indexterm><primary>配置</primary><secondary>Squid</secondary></indexterm><indexterm><primary>配置文件</primary><secondary>squid.conf</secondary></indexterm>

  <para>
   所有 Squid 代理服务器设置都在 <filename>/etc/squid/squid.conf</filename> 文件中进行。首次启动 Squid 时，不必在此文件中进行任何更改，但是外部客户端最初不具备访问权。代理可供 <systemitem class="domainname">localhost</systemitem> 使用。默认端口为 <option>3128</option>。预装的配置文件 <filename>/etc/squid/squid.conf</filename> 提供了有关选项的详细信息和许多示例。
  </para>

  <para>
   许多条目都已注释掉，因此以注释字符 <literal>#</literal> 开头。行尾处提供了相关规范。<remark>FIXME: Where is that not the case? - sknorr, 2016-06-05</remark>给定值通常与默认值相关，因此仅去除注释符号而不更改任何参数通常没有什么影响。如果可能，请保留原始的注释行，在该行下方插入选项及修改过的值。这样，便可容易地恢复默认值，并将其与所作更改进行比较。
  </para>

  <tip>
   <title>更新后调整配置文件</title>
   <para>
    如果已从较早的 Squid 版本更新，建议编辑新的 <filename>/etc/squid/squid.conf</filename>，并只应用以前文件中的更改。
   </para>
   <para>
    有时，该文件中会添加、去除或修改 Squid 选项。因此，如果尝试使用旧的 <filename>squid.conf</filename>，Squid 可能会无法正常工作。
   </para>
  </tip>

  <sect2 xml:id="sec.squid.configfile.general">
   <title>一般配置选项</title>
   <para>
    下面列出了 Squid 的一些配置选项，但并不详尽。<filename>/etc/squid/squid.conf.documented</filename> 中列出了 Squid 包的完整选项列表，其中仅做了简要记录。
   </para>
   <variablelist>
    <varlistentry>
     <term><literal>http_port <replaceable>端口</replaceable></literal>
     </term>
     <listitem>
      <para>
       这是 Squid 侦听客户端请求所用的端口。默认端口为 <option>3128</option>，但也常使用 <option>8080</option>。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>
       <literal>cache_peer <replaceable>主机名</replaceable> <replaceable>类型</replaceable> <replaceable>代理端口</replaceable> <replaceable>ICP 端口</replaceable></literal>
     </term>
     <listitem>
      <para>
       此选项允许创建能够协同工作的超速缓存网络。超速缓存对等是一台也托管了网络超速缓存并与您自己的计算机有某种关系的计算机。<replaceable>类型</replaceable>指定关系的类型。类型可以是 <literal>parent</literal> 或 <literal>sibling</literal>。
      </para>
      <para>
       对于<replaceable>主机名</replaceable>，请指定要使用的代理的名称或 IP 地址。对于<replaceable>代理端口</replaceable>，请指定浏览器中要使用的端口号（通常为 <option>8080</option>）。请将 <replaceable>ICP 端口</replaceable>设置为 <option>7</option>，如果父代理的 ICP 端口未知并且该端口的使用与提供商无关，则设置为 <option>0</option>。
      </para>
      <para>
       要让 Squid 以 Web 浏览器而非代理的方式工作，请禁止使用 ICP 协议。您可以通过追加 <option>default</option> 和 <option>no-query</option> 选项来实现此目的。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><literal>cache_mem <replaceable>大小</replaceable></literal>
     </term>
     <listitem>
      <para>
       此选项定义 Squid 可用于常用答复的内存大小。默认为 <option>8 MB</option>。它不指定 Squid 的内存使用率，并且可能已经超过。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>
       <literal>cache_dir <replaceable>储存类型</replaceable> <replaceable>超速缓存目录</replaceable> <replaceable>超速缓存大小</replaceable> <replaceable>1 级目录</replaceable> <replaceable>2 级目录</replaceable></literal>
     </term>
     <listitem>
      <para>
       <literal>cache_dir</literal> 选项定义磁盘超速缓存的目录。在 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 上的默认配置中，Squid 不会创建磁盘超速缓存。
      </para>
      <para>
       占位符<replaceable>储存类型</replaceable>可以是以下其中一个：
      </para>
      <itemizedlist>
       <listitem>
        <para>
         基于目录的储存类型：<literal>ufs</literal>、<literal>aufs</literal>（默认值）、<literal>diskd</literal>。这三种类型都是 <literal>ufs</literal> 储存形式的变体。不过，虽然 <literal>ufs</literal> 作为核心 Squid 线程的一部分运行，但 <literal>aufs</literal> 是在单独的线程中运行，而 <literal>diskd</literal> 则使用单独的进程。这表示后两种类型可避免因磁盘 I/O 而阻止 Squid。
        </para>
       </listitem>
       <listitem>
        <para>
         基于数据库的储存系统：<literal>rock</literal>。此储存格式依赖于单一数据库文件，其中每个对象占用一个或多个固定大小的内存单元（<quote>槽</quote>）。
        </para>
       </listitem>
      </itemizedlist>
      <para>
       下文将只介绍基于 <literal>ufs</literal> 的储存类型的参数。<literal>rock</literal> 的参数有些不同。
      </para>
      <para>
       <replaceable>超速缓存目录</replaceable>是磁盘超速缓存的目录，默认为 <filename>/var/cache/squid</filename>。<replaceable>超速缓存大小</replaceable>是该目录的最大大小（以兆字节为单位），默认设置为 100 MB。请将其设置为可用磁盘空间的 50% 到最大 80% 之间的值。
      </para>
      <para>
       最后两个值（<replaceable>1 级目录</replaceable>和 <replaceable>2 级目录</replaceable>）指定<replaceable>超速缓存目录</replaceable>中创建的子目录数量。默认情况下，在<replaceable>超速缓存目录</replaceable>下的第一级会创建 16 个子目录，在其中的每个子目录下创建 256 个子目录。提高这些值时应谨慎，因为创建过多的目录可能会导致性能问题。
      </para>
      <para>
       如果有多个磁盘共享一个超速缓存，请指定数行 <option>cache_dir</option>。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>cache_access_log <replaceable>日志文件</replaceable></option>
     </term>
     <term><option>cache_log <replaceable>日志文件</replaceable></option>
     </term>
     <term><option>cache_store_log <replaceable>日志文件</replaceable></option>
     </term>
     <listitem>
      <para>
       <indexterm> <primary>Squid</primary> <secondary>日志文件</secondary> </indexterm> <indexterm> <primary>日志文件</primary> <secondary>Squid</secondary> </indexterm>这三个选项指定 Squid 用于记录其所有操作的路径。通常无需在这里进行任何更改。如果 Squid 负担过重，则可能需要将超速缓存和日志文件分散到多个磁盘上。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>client_netmask <replaceable>网络掩码</replaceable></option>
     </term>
     <listitem>
      <para>
       此选项允许通过应用子网掩码在日志文件中屏蔽客户端的 IP 地址。例如，要将 IP 地址的最后一位设置为 <literal>0</literal>，请指定 <systemitem class="netmask">255.255.255.0</systemitem>。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>ftp_user <replaceable>电子邮件</replaceable></option>
     </term>
     <listitem>
      <para>
       此选项允许设置 Squid 应该用于匿名 FTP 登录的口令。请在此处指定有效的电子邮件地址，因为有些 FTP 服务器会检查这些信息来验证有效性。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>cache_mgr <replaceable>电子邮件</replaceable></option>
     </term>
     <listitem>
      <para>
       如果 Squid 意外崩溃，将会向此电子邮件地址发送一封邮件。默认为 <emphasis>webmaster</emphasis>。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>logfile_rotate <replaceable>值</replaceable></option>
     </term>
     <listitem>
      <para>
       如果运行 <command>squid</command> <option>-k rotate</option>，<command>Squid</command> 可以轮转日志文件。在此过程中会给文件编号，并且在达到指定值后重写最旧的文件。默认值为 <option>10</option>，即轮转编号为 0 到 9 的日志文件。
      </para>
      <para>
       不过，在 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 上，日志文件的轮转是通过使用 <systemitem class="daemon">logrotate</systemitem> 和配置文件 <filename>/etc/logrotate.d/squid</filename> 自动执行的。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>append_domain <replaceable>域</replaceable></option>
     </term>
     <listitem>
      <para>
       使用 <emphasis>append_domain</emphasis> 可指定当未指定域时自动追加的域。通常在此处指定您自己的域，因此在浏览器中指定 <emphasis>www</emphasis> 将访问您自己的 Web 服务器。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>forwarded_for <replaceable>状态</replaceable></option>
     </term>
     <listitem>
      <para>
       如果此选项设置为 <literal>on</literal>，将会向报头添加如下所示的一行：
      </para>
<screen>X-Forwarded-For: 192.168.0.1</screen>
      <para>
       如果将此选项设置为 <literal>off</literal>，Squid 会将客户端的 IP 地址和系统名称从 HTTP 请求中去除。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>negative_ttl <replaceable>时间</replaceable></option>
     </term>
     <term><option>negative_dns_ttl <replaceable>时间</replaceable></option>
     </term>
     <listitem>
      <para>
       如果设置了这些选项，Squid 将会超速缓存某些类型的失败，例如 <literal>404</literal> 响应。以后，它将拒绝发出新请求，即使当时资源可用。
      </para>
      <para>
       默认情况下，<option>negative_ttl</option> 设置为 <literal>0</literal>，<option>negative_dns_ttl</option> 设置为 <literal>1 minute</literal>。这表示默认不会超速缓存 Web 请求的负响应，但会将 DNS 请求的负响应超速缓存 1 分钟。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>never_direct allow <replaceable>ACL 名称</replaceable></option>
     </term>
     <listitem>
      <para>
       为防止 Squid 接受直接来自因特网的请求，可使用选项 <literal>never_direct</literal> 强制连接连到另一个代理。事先必须已在 <option>cache_peer</option> 中指定该代理。如果将 <replaceable>ACL 名称</replaceable>指定为 <literal>all</literal>，则所有请求都将直接转发给<literal>父代理</literal>。有时这可能是必要的，例如，您使用的提供商规定了其代理的使用方式或拒绝通过其防火墙直接访问因特网时。
      </para>
      <remark>
       Last sentence sounds as if from another era... Does it still make sense?
       - sknorr, 2016-07-07
      </remark>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>

  <sect2 xml:id="sec.squid.acl">
   <title>访问控制选项</title><indexterm> <primary>Squid</primary><secondary>ACL</secondary></indexterm><indexterm><primary>Squid</primary><secondary>权限</secondary></indexterm>
   <para>
    Squid 为控制针对代理的访问提供了一套周密的系统。这些访问控制列表 (ACL) 都是包含按顺序处理的规则的列表。使用 ACL 之前必须先定义 ACL。一些默认的 ACL 已经存在，如 <literal>all</literal> 和 <literal>localhost</literal>。但是，仅仅定义 ACL 并不意味着实际应用 ACL。只有存在相应的 <option>http_acces</option> 规则时，才会应用。
   </para>
   <para>
    选项 <option>acl</option> 的语法如下所示：
   </para>
<screen>acl <replaceable>ACL_NAME</replaceable> <replaceable>TYPE</replaceable> <replaceable>DATA</replaceable></screen>
   <para>
    此语法中的占位符含义如下：
   </para>
   <itemizedlist>
    <listitem>
     <para>
      名称 <replaceable>ACL_NAME</replaceable> 可以任意选择。
     </para>
    </listitem>
    <listitem>
     <para>
      对于 <replaceable>TYPE</replaceable>，<filename>/etc/squid/squid.conf</filename> 文件的 <literal>ACCESS CONTROLS</literal> 部分提供了多个不同的选项，您可以从中加以选择。
     </para>
    </listitem>
    <listitem>
     <para>
      <replaceable>DATA</replaceable> 的规格取决于单个 ACL 类型，也可从文件中读取。例如，<quote>通过</quote>主机名、IP 地址或 URL。
     </para>
    </listitem>
   </itemizedlist>
   <para>
    有关 ACL 规则类型的详细信息，请参见 <link xlink:href="http://www.squid-cache.org/Versions/v3/3.5/cfgman/acl.html"/> 上的 Squid 文档。
   </para>
   <example xml:id="ex.squid.school">
    <title>定义 ACL 规则</title>
<screen>
acl mysurfers srcdomain .example.com <co xml:id="co.squid.school.surfer"/>
acl teachers src 192.168.1.0/255.255.255.0 <co xml:id="co.squid.school.teacher"/>
acl students src 192.168.7.0-192.168.9.0/255.255.255.0 <co xml:id="co.squid.school.student"/>
acl lunch time MTWHF 12:00-15:00 <co xml:id="co.squid.school.time"/>
</screen>
    <calloutlist>
     <callout arearefs="co.squid.school.surfer">
      <para>
       此 ACL 将 <literal>mysurfers</literal> 定义为来自 <literal>.example.com</literal> 中的所有用户（由 IP 的反向查找确定）。
      </para>
     </callout>
     <callout arearefs="co.squid.school.teacher">
      <para>
       此 ACL 将 <literal>teachers</literal> 定义为计算机 IP 地址以 <literal>192.168.1.</literal> 开头的用户。
      </para>
     </callout>
     <callout arearefs="co.squid.school.student">
      <para>
       此 ACL 将 <literal>students</literal> 定义为计算机 IP 地址以 <literal>192.168.7.</literal>、<literal>192.168.8.</literal> 或 <literal>192.168.9.</literal> 开头的用户。
      </para>
     </callout>
     <callout arearefs="co.squid.school.time">
      <para>
       此 ACL 将 <literal>lunch</literal> 定义为星期一至星期五的中午到下午 3 点之间的一个时间。
      </para>
     </callout>
    </calloutlist>
   </example>
   <variablelist>
    <varlistentry>
     <term>http_access allow <replaceable>ACL 名称</replaceable>
     </term>
     <listitem>
      <para>
       <option>http_access</option> 定义谁可以使用代理，以及谁能够访问因特网上的什么内容。对于此选项，必须定义 ACL。上文中已经定义了 <literal>localhost</literal> 和 <literal>all</literal>，您可以通过 <literal>deny</literal> 或 <literal>allow</literal> 拒绝或允许对它们的访问。可以创建一个包含任意数量 <option>http_access</option> 条目的列表，按从上到下的顺序处理各条目。根据出现的先后顺序允许或拒绝访问相应的 URL。最后一项应始终是 <option>http_access deny all</option>。在下例中，<literal>localhost</literal> 可随意访问任何内容，而所有其他主机全部被拒绝访问：
      </para>
<screen>
http_access allow localhost
http_access deny all
</screen>
      <para>
       在另外一个使用这些规则的示例中，<systemitem class="groupname">teachers</systemitem> 组总能访问因特网。<systemitem class="groupname">students</systemitem> 组只能在星期一到星期五的午餐时间访问：
      </para>
<screen>
http_access deny localhost
http_access allow teachers
http_access allow students lunch time
http_access deny all
</screen>
      <para>
       为提高可读性，请在配置文件 <filename>/etc/squid/squid.conf</filename> 中将所有 <option>http_access</option> 选项指定在一个段落中。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>url_rewrite_program <replaceable>路径</replaceable></option>
     </term>
     <listitem>
      <para>
       使用此选项可以指定 URL 重写程序。例如，它可以是 squidGuard (<filename>/usr/sbin/squidGuard</filename>)，用于阻止不需要的 URL。通过此选项，可以使用代理身份验证和适当的 ACL 针对不同用户组分别控制因特网访问。
      </para>
      <para>
       有关 squidGuard 的详细信息，请参见<xref linkend="sec.squid.squidguard"/>。
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>auth_param basic program <replaceable>路径</replaceable></option>
     </term>
     <listitem>
      <para>
       如果必须在代理上对用户进行身份验证，请设置一个相应的程序，如 <filename>/usr/sbin/pam_auth</filename>。当首次访问 <command>pam_auth</command> 时，用户会看到一个需要指定用户名和口令的登录窗口。此外，您还需要一个 ACL，以便让只有有效登录的客户端才能使用因特网：
      </para>
<screen>
acl password proxy_auth REQUIRED

http_access allow password
http_access deny all
</screen>
      <para>
       在 <option>acl proxy_auth</option> 选项中，使用 <literal>REQUIRED</literal> 表示接受所有有效的用户名。<literal>REQUIRED</literal> 还可替换为允许的用户名列表。
      </para>
      <remark>
       FIXME: last sentence used to end in "or with the path to such a list."
       Squid documentation does not mention this possibility, so removing this
       for the moment. - sknorr, 2016-07-07
      </remark>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><option>ident_lookup_access allow <replaceable>ACL 名称</replaceable></option>
     </term>
     <listitem>
      <para>
       使用此选项，可以运行 ident 请求，以便为类型为 <literal>src</literal> 的 ACL 定义的所有客户端确定各个用户的身份。或者，可对所有客户端使用此选项，至于 <replaceable>ACL 名称</replaceable>，则应用预定义的 ACL <literal>all</literal>。
      </para>
      <para>
       <option>ident_lookup_access</option> 涵盖的所有客户端都必须运行 ident 守护程序。在 Linux 上，您可以将 <systemitem class="daemon">pidentd</systemitem>（包
       <package>pidentd</package>
       ）用作 ident 守护程序。对于其他操作系统，通常有免费的软件可供使用。为确保只有 ident 查找成功的客户端才有权访问，请定义相应的 ACL：
      </para>
<screen>
acl identhosts ident REQUIRED

http_access allow identhosts
http_access deny all
</screen>
      <para>
       在 <option>acl identhosts ident</option> 选项中，使用 <literal>REQUIRED</literal> 表示接受所有有效的用户名。<literal>REQUIRED</literal> 还可替换为允许的用户名列表。
      </para>
      <para>
       使用 <literal>ident</literal> 会延缓访问时间，因为对每个请求都要重复 ident 查找过程。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>
 </sect1>
 <sect1 xml:id="sec.squid.proxyconfigtrans">
  <title>配置透明代理</title><indexterm> <primary>代理</primary><secondary>透明</secondary></indexterm><indexterm><primary>Squid</primary><secondary>透明代理</secondary></indexterm>

  <para>
   使用代理服务器的一般方式如下：Web 浏览器向代理服务器的某端口发送请求，代理始终会提供这些所需的对象，而不论它们是否在其超速缓存中。不过，在某些情况下，Squid 的透明代理模式很有效：
  </para>

  <itemizedlist>
   <listitem>
    <para>
     如果出于安全考虑，建议所有客户端都使用代理来浏览因特网。
    </para>
   </listitem>
   <listitem>
    <para>
     如果所有客户端都必须使用代理，无论客户端是否清楚这一点。
    </para>
   </listitem>
   <listitem>
    <para>
     如果网络中的代理已转移，但是现有客户端需要保留其原有配置。
    </para>
   </listitem>
  </itemizedlist>

  <para>
   透明代理会截获并回复 Web 浏览器的请求，因此 Web 浏览器可接收到所请求的页面，但并不知道它们来自何处。顾名思义，整个处理过程对用户而言是透明的。
  </para>

  <procedure xml:id="pro.squid.transparent">
   <title>Squid 充当透明代理</title>
   <step>
    <para>
     在 <filename>/etc/squid/squid.conf</filename> 的选项 <option>http_port</option> 行中添加参数 <literal>transparent</literal>：
    </para>
<screen>http_port 3128 transparent</screen>
   </step>
   <step>
    <para>
     重启动 Squid：
    </para>
<screen><prompt>tux &gt; </prompt><command>sudo systemctl restart squid</command></screen>
   </step>
   <step>
    <para>
     设置 SuSEFirewall2 以将 HTTP 流量重定向到 <option>http_proxy</option> 中指定的端口（在上面的示例中为端口 3128）。为此，请编辑配置文件 <filename>/etc/sysconfig/SuSEfirewall2</filename>。
    </para>
    <para>
     本示例假设您使用的是以下设备：
    </para>
    <itemizedlist>
     <listitem>
      <para>
       设备指向因特网：<literal>FW_DEV_EXT="eth1"</literal>
      </para>
     </listitem>
     <listitem>
      <para>
       设备指向网络：<literal>FW_DEV_INT="eth0"</literal>
      </para>
     </listitem>
    </itemizedlist>
    <para>
     在防火墙上定义供不可信的（外部）网络（如因特网）访问的端口和服务（请参见 <filename>/etc/services</filename>）。在下例中，仅对外部提供 Web 服务：
    </para>
<screen>
FW_SERVICES_EXT_TCP="www"
</screen>
    <para>
     定义防火墙上从安全（内部）网络访问的端口或服务（请参见 <filename>/etc/services</filename>），包括通过 TCP 和 UDP：
    </para>
<screen>
FW_SERVICES_INT_TCP="domain www 3128"
FW_SERVICES_INT_UDP="domain"
</screen>
    <remark>
    FIXME: If I understand the comment above FW_REDIRECT in the config
    correctly, we don't need to mention 3128 here anymore. - sknorr, 2016-07-11
   </remark>
    <para>
     这会允许访问 Web 服务和 Squid（Squid 的默认端口为 <option>3128</option>）。服务<quote>域</quote>代表 DNS（域名服务）。此服务很常用。否则，只需从上面的条目中去除 <literal>domain</literal> 并将下面的选项设置为 <literal>no</literal> 即可：
    </para>
<screen>FW_SERVICE_DNS="yes"</screen>
    <para>
     选项 <option>FW_REDIRECT</option> 非常重要，因为它用于将 HTTP 流量实际重定向到特定端口。配置文件中选项上方的注释中解释了其语法：
    </para>
<screen># Format:
# list of &lt;source network&gt;[,&lt;destination network&gt;,&lt;protocol&gt;[,dport[:lport]]
# Where protocol is either tcp or udp. dport is the original
# destination port and lport the port on the local machine to
# redirect the traffic to
#
# An exclamation mark in front of source or destination network
# means everything EXCEPT the specified network</screen>
    <para>
     即：
    </para>
    <orderedlist>
     <listitem>
      <para>
       指定访问代理防火墙的内部网络的 IP 地址和网络掩码。
      </para>
     </listitem>
     <listitem>
      <para>
       指定这些客户端的请求发往的 IP 地址和网络掩码。如果使用的是 Web 浏览器，请指定网络 <literal>0/0</literal>（表示<quote>至任意地址的通配符）。</quote>
      </para>
     </listitem>
     <listitem>
      <para>
       指定这些请求最初发送到的端口。
      </para>
     </listitem>
     <listitem>
      <para>
       指定所有这些请求要重定向到的端口。在下面的示例中，Web 服务（端口 <literal>80</literal>）重定向到代理端口（端口 <literal>3128</literal>）。如果要添加更多网络或服务，请在相应条目中用空格分隔它们。
      </para>
      <para>
       由于 Squid 支持 HTTP 以外的协议，您也可以将请求从其他端口重定向到该代理。例如，还可以重定向端口 21 （FTP) 和端口 443（HTTPS 或 SSL）。
      </para>
     </listitem>
    </orderedlist>
    <para>
     因此，对于 Squid 配置，您可以使用：
    </para>
<screen>FW_REDIRECT="192.168.0.0/16,0/0,tcp,80,3128"</screen>
   </step>
   <step>
    <para>
     在配置文件 <filename>/etc/sysconfig/SuSEfirewall2</filename> 中，请确保 <option>START_FW</option> 这一项设置为 <literal>"yes"</literal>。
    </para>
   </step>
   <step>
    <para>
     重启动 SuSEFirewall2：
    </para>
<screen><prompt>tux &gt; </prompt><command>sudo systemctl restart SuSEfirewall2</command></screen>
   </step>
   <step>
    <para>
     要确认是否一切正常，请检查 <filename>/var/log/squid/access.log</filename> 中的 Squid 日志文件。要校验所有端口是否都已正确配置，请从网络外的任意计算机对本计算机上的端口进行扫描。只有 Web 服务（端口 80）应该是打开的。要使用 <command>nmap</command> 扫描端口，请使用：
    </para>
<screen><command>nmap</command> <option>-O <replaceable>IP_ADDRESS</replaceable></option></screen>
   </step>
  </procedure>
 </sect1>
 <sect1 xml:id="sec.squid.cachemgr">
  <title>使用 Squid 超速缓存管理器 CGI 接口 (<systemitem class="resource">cachemgr.cgi</systemitem>)</title><indexterm> <primary>Squid</primary><secondary>统计数字</secondary></indexterm><indexterm><primary>Squid</primary><secondary>cachemgr.cgi</secondary></indexterm>

  <para>
   Squid 超速缓存管理器 CGI 接口 (<filename>cachemgr.cgi</filename>) 是一个 CGI 实用程序，用于显示正运行的 Squid 进程所占用内存的相关统计数字。这也是在不登录服务器的情况下，管理超速缓存和查看统计数字的一种便捷方式。
  </para>



  <procedure xml:id="pro.squid.setup.cachemgr">
   <title>设置 <systemitem class="resource">cachemgr.cgi</systemitem></title>
   <step>
    <para>
     确保 Apache Web 服务器正在系统上运行。按<xref linkend="cha.apache2"/>中所示配置 Apache。请着重参见<xref linkend="sec.apache2.cgi"/>。要检查 Apache 是否已在运行，请使用：
    </para>
<screen><prompt>tux &gt; </prompt><command>sudo systemctl status apache2</command></screen>
    <para>
     如果显示 <literal>inactive</literal>，您可以使用 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 默认设置启动 Apache：
    </para>
<screen><prompt>tux &gt; </prompt><command>sudo systemctl start apache2</command></screen>
   </step>
   <step>
    <para>
     现在，在 Apache 中启用 <systemitem class="resource">cachemgr.cgi</systemitem>。要执行此操作，请为 <literal>ScriptAlias</literal> 创建一个配置文件。
    </para>
    <para>
     在目录 <filename>/etc/apache2/conf.d</filename> 中创建该文件并将其命名为 <filename>cachemgr.conf</filename>。在其中添加以下内容：
    </para>

<screen>ScriptAlias /squid/cgi-bin/ /usr/lib64/squid/

&lt;Directory "/usr/lib64/squid/"&gt;
Options +ExecCGI
AddHandler cgi-script .cgi
Require host <replaceable>HOST_NAME</replaceable>
&lt;/Directory&gt;
</screen>
    <para>
     将 <replaceable>HOST_NAME</replaceable> 替换为您要从中访问 <systemitem class="resource">cachemgr.cgi</systemitem> 的计算机的主机名。这样，只有您的计算机才能访问 <systemitem class="resource">cachemgr.cgi</systemitem>。要允许从任何位置访问该文件，请改为使用 <literal>Require all granted</literal>。
    </para>


   </step>
   <step>
    <itemizedlist>
     <listitem>
      <para>
       如果 Squid 与 Apache Web 服务器在同一台计算机上运行，则无需对 <filename>/etc/squid/squid.conf</filename> 进行任何更改。但需校验 <filename>/etc/squid/squid.conf</filename> 是否包含以下几行：
      </para>

<screen>
  http_access allow manager localhost
  http_access deny manager
  </screen>
      <para>
       这几行允许您从您自己的计算机 (<literal>localhost</literal>) 访问管理器界面，但不允许从其他位置访问。
      </para>
     </listitem>
     <listitem>
      <para>
       如果 Squid 与 Apache Web 服务器在不同的计算机上运行，您需要添加额外的规则以允许从 Squid 的 CGI 脚本访问。定义服务器的 ACL（将 <replaceable>WEB_SERVER_IP</replaceable> 替换为您 Web 服务器的 IP 地址）：
      </para>
<screen>
acl webserver src <replaceable>WEB_SERVER_IP</replaceable>/255.255.255.255
</screen>
      <para>
       确保以下规则都包含在配置文件中。与默认配置相比，只有中间的规则是新的。不过，顺序很重要。
      </para>
<screen>
http_access allow manager localhost
http_access allow manager webserver
http_access deny manager
</screen>
     </listitem>
    </itemizedlist>
   </step>
   <step performance="optional">
    <para>
     （可选）您可以为 <systemitem class="resource">cachemgr.cgi</systemitem> 配置一个或多个口令。这样还可允许执行更多操作，例如远程关闭超速缓存或查看超速缓存的更多信息。为此，请使用管理器的一个或多个口令和允许的操作列表配置选项 <option>cache_mgr</option> 和 <option>cachemgr_passwd</option>。
    </para>
    <para>
     例如，若要明确允许不进行身份验证即可查看索引页、菜单、60 分钟计数器的平均值，允许使用口令 <literal>secretpassword</literal> 切换脱机模式，以及完全禁用任何其他功能，请使用以下配置：
    </para>
<screen>
cache_mgr user
cachemgr_passwd none index menu 60min
cachemgr_passwd secretpassword offline_toggle
cachemgr_passwd disable all
</screen>
    <para>
     <option>cache_mgr</option> 定义用户名。<option>cache_mgr</option> 定义使用哪个口令允许哪些操作。
    </para>
    <para>
     <literal>none</literal> 和 <literal>disable</literal> 是特殊关键字：<literal>none</literal> 表示无需输入口令，<literal>disable</literal> 会完全禁用功能。
    </para>
    <para>
     登录 <systemitem class="resource">cachemgr.cgi</systemitem> 后便可全面查看完整的操作列表。要了解配置文件中需如何参照操作，请查看操作页 URL 中 <literal>&amp;operation=</literal> 后面的字符串。<literal>all</literal> 是一个特殊关键字，表示所有操作。
    </para>
   </step>
   <step>
    <para>
     配置文件更改后重新装载 Squid 和 Apache：
    </para>
<screen><prompt>tux &gt; </prompt><command>sudo systemctl reload squid</command></screen>
   </step>
   <step>
    <para>
     要查看统计数字，请转到先前设置的 <systemitem class="resource">cachemgr.cgi</systemitem> 页。例如 <literal>http://webserver.example.org/squid/cgi-bin/cachemgr.cgi</literal>。
    </para>
    <para>
     选择正确的服务器，如已设置，请指定用户名和口令。然后单击<guimenu>继续</guimenu>浏览不同的统计数字。
    </para>
   </step>
  </procedure>
 </sect1>
 <sect1 xml:id="sec.squid.squidguard">
  <title>squidGuard</title><indexterm> <primary>Squid</primary><secondary>squidGuard</secondary> </indexterm>

  <para>
   本节的目的并不是要解释 squidGuard 的详细配置，而只是介绍该程序并为使用该程序提些建议。要深入了解配置问题，请参见 squidGuard 的 网站 <link xlink:href="http://www.squidguard.org"/>。
  </para>

  <para>
   squidGuard 是一款用于 Squid 的免费 (GPL)、灵活而快捷的过滤器、重定向器和访问控制器插件。使用它可以针对 Squid 缓存定义多种访问规则，对不同用户组加以不同的限制。squidGuard 使用 Squid 的标准重定向接口。squidGuard 可以执行以下操作：
  </para>

  <itemizedlist>
   <listitem>
    <para>
     将某些用户的 Web 访问权限制为只能访问一组可接受的或知名 Web 服务器或 URL。
    </para>
   </listitem>
   <listitem>
    <para>
     防止某些用户访问某些列出的或在黑名单中列出的 Web 服务器或 URL。
    </para>
   </listitem>
   <listitem>
    <para>
     防止某些用户访问与一组正则表达式或单词匹配的 URL。
    </para>
   </listitem>
   <listitem>
    <para>
     将拦截的 URL 重定向至基于 CGI 的<quote>智能</quote>信息页面。
    </para>
   </listitem>
   <listitem>
    <para>
     将未注册用户重定向至注册表单。
    </para>
   </listitem>
   <listitem>
    <para>
     将横幅重定向至空白 GIF。
    </para>
   </listitem>
   <listitem>
    <para>
     使用基于时间、周中各天、日期等的不同访问规则。
    </para>
   </listitem>
   <listitem>
    <para>
     对不同用户组使用不同规则。
    </para>
   </listitem>
  </itemizedlist>

  <para>
   squidGuard 和 Squid 不能用于：
  </para>

  <itemizedlist mark="bullet" spacing="normal">
   <listitem>
    <para>
     编辑、过滤或审查文档内的文本。
    </para>
   </listitem>
   <listitem>
    <para>
     编辑、过滤或检查 HTML 嵌入脚本语言，如 JavaScript。
    </para>
   </listitem>
  </itemizedlist><indexterm> <primary>配置文件</primary> <secondary>squidguard.conf</secondary> </indexterm> <indexterm> <primary>配置文件</primary> <secondary>squid.conf</secondary> </indexterm>

  <procedure xml:id="pro.squidguard">
   <title>设置 squidGuard</title>
   <step>
    <para>
     在使用 squidGuard 之前，请先安装
     <package>squidGuard</package>
     。
    </para>
   </step>
   <step>
    <para>
     提供最小的配置文件，如 <filename>/etc/squidguard.conf</filename>。可在 <link xlink:href="http://www.squidguard.org/Doc/examples.html"/> 中找到配置示例。以后可尝试更为复杂的配置设置。
    </para>
   </step>
   <step>
    <para>
     接下来，创建一个<quote>拒绝访问</quote>HTML 页面或 CGI 页面，以便在客户端请求列在黑名单中的网站时，Squid 可重定向到该页面。强烈建议使用 Apache。
    </para>
   </step>
   <step>
    <para>
     现在，配置 Squid 以使用 squidGuard。使用 <filename>/etc/squid/squid.conf</filename> 文件中的以下项：
    </para>
<screen>redirect_program /usr/bin/squidGuard</screen>
   </step>
   <step>
    <para>
     名为 <systemitem>redirect_children</systemitem> 的另一选项配置在该计算机上运行的<quote>重定向</quote>（在此例中是 squidGuard）进程数。设置的进程越多，所需的 RAM 就越多。先尝试使用较小的数字，例如 <literal>4</literal>。
    </para>
<screen>redirect_children 4</screen>
   </step>
   <step>
    <para>
     最后，通过运行 <command>systemctl reload squid</command> 让 Squid 装载新配置。现在，可以通过浏览器测试这些设置。
    </para>
   </step>
  </procedure>
 </sect1>
 <sect1 xml:id="sec.squid.calamaris">
  <title>使用 Calamaris 生成缓存报告</title><indexterm> <primary>Squid</primary><secondary>Calamaris</secondary></indexterm><indexterm><primary>Squid</primary><secondary>报告</secondary></indexterm>

  <para>
   Calamaris 是一个 Perl 脚本，用来以 ASCII 或 HTML 格式生成缓存活动的报告。它可以处理本机 Squid 访问日志文件。Calamaris 的主页为 <link xlink:href="http://cord.de/calamaris-english"/>。此工具不属于 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 默认安装范围 — 要使用它，请安装 <systemitem>calamaris</systemitem> 包。
  </para>

  <para>
   以 <systemitem class="username">root</systemitem> 身份登录，然后输入：
  </para>

<screen>cat access1.log [access2.log access3.log] | calamaris <replaceable>options</replaceable> &gt; reportfile</screen>

  <para>
   使用多个日志文件时，请确保这些文件按时间顺序排列，较早的文件列在前面。这可以通过以下两种方式来实现：如上例一样逐个列出文件，或使用 <filename>access{1..3}.log</filename>。
  </para>

  <para>
   <command>calamaris</command> 可使用以下选项：
  </para>

  <variablelist>
   <varlistentry>
    <term><option>-a</option>
    </term>
    <listitem>
     <para>
      输出所有可用报告
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><option>-w</option>
    </term>
    <listitem>
     <para>
      以 HTML 格式输出报告
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><option>-l</option>
    </term>
    <listitem>
     <para>
      在报告标题处包含消息或徽标
     </para>
    </listitem>
   </varlistentry>
  </variablelist>

  <para>
   有关不同选项的详细信息，可通过 <command>man</command> <option>calamaris</option> 在该程序的手册页中找到。
  </para>

  <para>
   典型示例如下：
  </para>

<screen>
cat access.log.{10..1} access.log | calamaris -a -w \
&gt; /usr/local/httpd/htdocs/Squid/squidreport.html
</screen>

  <para>
   这会将报告放入 Web 服务器目录。需要通过 Apache 来查看这些报告。
  </para>
 </sect1>
 <sect1 xml:id="sec.squid.moreinfo">
  <title>更多信息</title>

  <para>
   访问 Squid 的主页 <link xlink:href="http://www.squid-cache.org/"/>。在此可找到 <quote>Squid 用户指南</quote>及有关 Squid 的大量 FAQ（常见问题解答）信息。
  </para>

  <para>
   此外还通过 <link xlink:href="http://www.squid-cache.org/Support/mailing-lists.html"/> 提供 Squid 的邮件列表。
  </para>
 </sect1>
</chapter>
