<?xml version="1.0" encoding="UTF-8"?>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="deployment_pxe.xml" version="5.0" role="General" xml:id="cha-deployment-prep-pxe">
 <title>準備網路開機環境</title>
 <info>
  <abstract>
   <para>
    本章介紹如何設定 DHCP 和 TFTP 伺服器，以提供所需的基礎架構來透過 PXE 開機。
   </para>
  </abstract>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>是</dm:translation>
  </dm:docmanager>
 </info>

 <para>
  可以透過開機前執行環境 (PXE) 安裝 <phrase role="productname"><phrase os="sles">SUSE® Linux Enterprise Server</phrase></phrase>。用戶端硬體需支援透過 PXE 開機。網路需要提供 DHCP 伺服器和 TFTP 伺服器，以向用戶端提供所需的資料。本章將會引導您設定所需的伺服器。
 </para>
 <para>
  PXE 只會將核心和 initrd 開機。可以使用此方法開機至安裝環境或線上系統。若要設定安裝來源，請參閱<xref linkend="cha-deployment-instserver"/>。
 </para>
 <para>
  本節內容會介紹各種複雜開機方式的組態任務。其中包含 DHCP、PXE 啟動、TFTP 和網路喚醒等現成的組態。
 </para>
 <para>
  這些範例假設 DHCP、TFTP 和 NFS 伺服器位於 IP 為 <literal>192.168.1.1</literal> 的同一部機器上。所有服務都可以位於不同的機器上，不會產生任何問題。請務必視需要變更 IP 位址。
 </para>
 <sect1 xml:id="sec-deployment-dhcp-server">
  <title>設定 DHCP 伺服器</title>
  <para>
   除了向網路用戶端提供自動位址配置外，DHCP 伺服器還會通告 TFTP 伺服器的 IP 位址，以及核心和 Initrd 檔案的路徑。需要載入哪些檔案取決於目標機器的架構，以及使用的是傳統的 BIOS 還是 UEFI 開機方式。用戶端會在 DHCP 要求中傳輸其架構類型。根據此資訊，DHCP 伺服器可以確定必須下載哪些檔案用於開機。
  </para>
  <procedure xml:id="pro-deployment-dhcp-server">
   <step>
    <para>
     以 <systemitem class="username">root</systemitem> 身分登入代管 DHCP 伺服器的機器。
    </para>
   </step>
   <step>
    <para>
     透過執行 <command>systemctl enable dhcpd</command> 啟用 DHCP 伺服器。
    </para>
   </step>
   <step>
    <para>
     將以下幾行附加到 <filename>/etc/dhcpd.conf</filename> 下之 DHCP 伺服器組態檔案的子網路組態︰
    </para>
<screen># The following lines are optional
option domain-name "my.lab";
option domain-name-servers 192.168.1.1;
option routers 192.168.1.1;
option ntp-servers 192.168.1.1;
ddns-update-style none;
default-lease-time 3600;

# The following lines are required
option arch code 93 = unsigned integer 16; # RFC4578
subnet 192.168.1.0 netmask 255.255.255.0 {
 next-server 192.168.1.1;
 range 192.168.1.100 192.168.1.199;
 default-lease-time 3600;
 max-lease-time 3600;
 if option arch = 00:07 or option arch = 00:09 {
   filename "/EFI/x86/grub.efi";
 }
 else if option arch = 00:0b {
   filename "/EFI/aarch64/bootaa64.efi";
 }
 else  {
   filename "/BIOS/x86/pxelinux.0";
 }
}</screen>
    <para>
     此組態範例使用的子網路為 <literal>192.168.1.0/24</literal>，DHCP、DNS 和閘道設定在 IP 為 <literal>192.168.1.1</literal> 的伺服器上。請務必依據您的網路配置變更使用的所有 IP 位址。如需更多 <filename>dhcpd.conf</filename> 中可用選項的詳細資訊，請參閱 <systemitem>dhcpd.conf</systemitem> man 頁面。
    </para>
   </step>
   <step>
    <para>
     執行 <command>systemctl restart dhcpd</command> 重新啟動 DHCP 伺服器。
    </para>
   </step>
  </procedure>
  <para>
   如果您打算使用 SSH 來從遠端控制 PXE 和網路喚醒安裝，請指定 DHCP 應提供給安裝目標的 IP 位址。若要這麼做，請根據以下範例修改上述 DHCP 組態︰
  </para>
<screen>group {
 host test {
   hardware ethernet <replaceable>MAC_ADDRESS</replaceable>;
   fixed-address <replaceable>IP_ADDRESS</replaceable>;
   }
}
</screen>
  <para>
   host 陳述式會引入安裝目標的主機名稱。若要結合特定主機的主機名稱和 IP 位址，您必須瞭解並指定系統的硬體 (MAC) 位址。將此範例中的所有變數取代成符合您環境的實際值。
  </para>
  <para>
   重新啟動 DHCP 伺服器後，它將向所指定的主機提供一個靜態 IP，讓您能透過 SSH 連接到該系統。
  </para>
 </sect1>

 <sect1 xml:id="sec-deployment-tftp-server">
  <title>設定 TFTP 伺服器</title>
  <para>
   下面的程序介紹如何在 32 位元和 64 位元 x86 架構上為配有 UEFI 和 BIOS 的目標機器準備伺服器。準備好的結構也為 AArch64 系統提供了空間。
  </para>
  <sect2 xml:id="sec-deployment-tftp-server-install">
   <title>安裝 TFTP 伺服器</title>
   <para>
    若要安裝 TFTP 伺服器，請執行以下程序︰
   </para>

   <procedure>
    <step>
     <para>
      安裝 <systemitem>tftp</systemitem> 套件。
     </para>
     <screen><prompt>tux &gt; </prompt><command>sudo</command> <command>zypper in tftp</command></screen>
    </step>
    <step>
     <para>
      檢查 <filename>/etc/sysconfig/tftp</filename> 中的 <systemitem class="daemon">tftpd</systemitem> 組態，並視需要新增或變更選項。如需更多詳細資料，請參閱 <command>man 8 tftpd</command>。不需要變更組態，TFTP 精靈也可正常運作。檔案的預設根目錄為 <filename>/srv/tftpboot</filename>。
     </para>
    </step>
    <step>
     <para>
      確定 <systemitem class="daemon">tftpd</systemitem> 在開機時已啟動，並重新啟動它以讀取新組態。
     </para>
     <screen><prompt>tux &gt; </prompt><command>sudo</command> <command>systemctl enable tftp.socket</command>
<prompt>tux &gt; </prompt><command>sudo</command> <command>systemctl restart tftp.socket</command></screen>
    </step>
   </procedure>
  </sect2>
  <sect2 xml:id="sec-deployment-tftp-server-grub-package">
   <title>安裝開機檔案</title>
   <para>
    <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 於 RPM 中提供在 BIOS 或 UEFI 機器上透過 PXE 開機所需的檔案。請在執行 TFTP 伺服器的機器上安裝這些檔案︰
   </para>
   <screen><prompt>tux &gt; </prompt><command>sudo</command> <command>zypper in tftpboot-installation-SLES-<replaceable>OS_VERSION</replaceable>-<replaceable>ARCHITECTURE</replaceable></command></screen>
   <para>
    請以 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 安裝的版本 (例如 <literal>15</literal>) 取代 <replaceable>OS_VERSION</replaceable>，並以您系統的架構 (例如 <literal>x86_64</literal>) 取代 <replaceable>ARCHITECTURE</replaceable>。可以使用 <command>zypper se tftpboot</command> 來搜尋所有可用版本和架構。
   </para>
   <para>
    檔案將安裝在 <filename>/srv/tftpboot/SLES-<replaceable>OS_VERSION</replaceable>-<replaceable>ARCHITECTURE</replaceable></filename> 中。您也可以將 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> 的其他版本和架構的相應檔案複製到 <filename>/srv/tftpboot</filename> 目錄。
   </para>
   <note>
    <title>現有的 <filename>/srv/tftpboot/</filename> 目錄</title>
    <para>
     如果您的機器上已存在目錄 <filename>/srv/tftpboot/</filename>，則所有檔案將會安裝到 <filename>/usr/share/tftpboot-installation/</filename>。如果您正在從舊版 SLES 升級 PXE 伺服器，則會採用這種安裝方式。
    </para>
    <para>
     若要修復此問題，請將 <filename>/usr/share/tftpboot-installation/</filename> 中的檔案手動複製到 <filename>/srv/tftpboot/</filename>。或者，移除 <filename>/srv/tftpboot/</filename> 並重新安裝
     <package>tftpboot-installation-SLES-<replaceable>作業系統版本</replaceable>-<replaceable>架構</replaceable></package>
     套件中可用。
    </para>
   </note>
  </sect2>
  <sect2 xml:id="sec-deployment-tftp-server-pxelinux">
   <title>設定 PXELINUX</title>
   <para>
    在編輯器中開啟檔案 <filename>/srv/tftpboot/SLES-<replaceable>OS_VERSION</replaceable>-<replaceable>ARCHITECTURE</replaceable>/net/pxelinux.cfg/default</filename>。依據<xref linkend="cha-deployment-instserver"/>中所述的設定取代 <literal>install</literal> 參數的路徑。另外，請以 TFTP 伺服器的 IP 位址取代 <replaceable>TFTP_SERVER</replaceable>。如需 PXELINUX 組態選項的綜覽，請參閱<xref linkend="sec-deployment-prep-boot-pxeopts"/>。
   </para>
<screen>default linux

# install
label linux
  ipappend 2
  kernel boot/<replaceable>ARCHITECTURE</replaceable>/loader/linux
  append initrd=boot/x86_64/<replaceable>ARCHITECTURE</replaceable>/initrd instsys=tftp://<replaceable>TFTP_SERVER</replaceable>/SLES-<replaceable>OS_VERSION</replaceable>-<replaceable>ARCHITECTURE</replaceable>/boot/<replaceable>ARCHITECTURE</replaceable>/root install=<replaceable>PROTOCOL</replaceable>://<replaceable>SERVER_IP</replaceable>:/<replaceable>PATH</replaceable>

display  message
implicit 1
prompt  1
timeout  50</screen>
   <para>
    如需 <literal>append</literal> 行中所用開機參數的詳細資料，請參閱<xref linkend="sec-boot-parameters-list"/>。
   </para>
   <para>
    如有需要，請編輯 <filename>/srv/tftpboot/SLES-<replaceable>OS_VERSION</replaceable>-<replaceable>ARCHITECTURE</replaceable>/net/pxelinux.cfg/message</filename>，以在開機功能表中顯示訊息。
   </para>
  </sect2>
  <sect2 xml:id="sec-deployment-tftp-server-efilinux-x86">
   <title>使用 GRUB2 為 EFI 準備 PXE 開機</title>
   <para>
    您無需變更 GRUB2 組態檔案。但是，預設設定中未提供安裝系統的網路來源。若要完全透過網路來安裝 <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>，請將 <option>install</option> 參數新增至 <filename>/srv/tftpboot/SLES-<replaceable>OS_VERSION</replaceable>-<replaceable>ARCHITECTURE</replaceable>/EFI/BOOT/grub.cfg</filename> 檔案中的 <option>linuxefi</option> 行。依據<xref linkend="cha-deployment-instserver"/>中所述的設定來設定 <literal>install</literal> 參數。如需 <literal>efilinux</literal> 行中所用其他開機參數的詳細資料，請參閱<xref linkend="sec-boot-parameters-list"/>。
   </para>
  </sect2>
 </sect1>
 <sect1 xml:id="sec-deployment-prep-boot-pxeopts">
  <title>PXELINUX 組態選項</title>

  <para>
   此處列出的選項為 PXELINUX 組態檔案中所有可用選項的子集合。
  </para>

  <variablelist>
   <varlistentry>
    <term><option>APPEND <replaceable>OPTIONS</replaceable></option>
    </term>
    <listitem>
     <para>
      將一或多個選項新增至核心指令行。這些新增選項是用於自動和手動開機。新增選項會加在每個核心指令行的開頭位置，通常您只要明確輸入核心選項，便可覆寫這些選項。
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><option>APPEND -</option>
    </term>
    <listitem>
     <para>
      不附加任何選項。在 <option>LABEL</option> 區段中做為引數且包含單一連字號的 <option>APPEND</option>，可以用來覆寫全域的 <option>APPEND</option>。
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><option>DEFAULT <replaceable>KERNEL_OPTIONS</replaceable>...</option>
    </term>
    <listitem>
     <para>
      設定預設的核心指令行。當 PXELINUX 是自動啟動，則該選項的作用相當於在啟動提示處輸入了在 DEFAULT 後輸入的所有內容 (表示自動啟動的 auto 選項除外，它是自動新增的)。
     </para>
     <para>
      如果組態檔案不存在或其中未定義 DEFAULT 項目，則預設使用不帶任何選項的核心名稱<quote>linux</quote>。
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>

    <term><option>IFAPPEND <replaceable>旗標</replaceable></option>
    </term>
    <listitem>
     <para>
      根據<replaceable>旗標</replaceable>值將特定選項新增至核心指令行。<option>IFAPPEND</option> 選項僅適用於 PXELINUX。<replaceable>旗標</replaceable>需要一個值，如<xref linkend="tab-deployment-boot-pxeopts-ifappend"/> 中所述。
     </para>
     <table xml:id="tab-deployment-boot-pxeopts-ifappend">
      <title>從 <option>IFAPPEND</option> 產生和新增的核心指令行選項</title>
      <tgroup cols="2">
       <colspec colwidth="1*"/>
       <colspec colwidth="4*"/>
       <thead>
        <row>
         <entry valign="middle">
          <para>
           引數
          </para>
         </entry>
         <entry>
          <para>
           產生的核心指令行/描述
          </para>
         </entry>
        </row>
       </thead>
       <tbody>
        <row>
         <entry>
          <para>
           <option>1</option>
          </para>
         </entry>
         <entry>
<screen>ip=<replaceable>CLIENT_IP</replaceable>:<replaceable>BOOT_SERVER_IP</replaceable>:<replaceable>GW_IP</replaceable>:<replaceable>NETMASK</replaceable></screen>
          <para>
           預留位置根據 DHCP/BOOTP 或 PXE 開機伺服器中的輸入加以取代。
          </para>
          <para>
           請注意，此選項並非在已開機系統中執行 DHCP 用戶端的替代項。如果不進行定期更新，PXE BIOS 取得的租用將會過期，使得該 IP 位址可供 DHCP 伺服器重複使用。
          </para>
         </entry>
        </row>
        <row>
         <entry>
          <para>
           <option>2</option>
          </para>
         </entry>
         <entry>
<screen>BOOTIF=<replaceable>MAC_ADDRESS_OF_BOOT_INTERFACE</replaceable></screen>
          <para>
           當安裝伺服器在收到 DHCP 伺服器的回覆前逐個查探 LAN 介面時，此選項可用於避免逾時。使用此選項可讓 initrd 程式決定系統是從哪個介面開機的。linuxrc 會讀取此選項並使用此網路介面。
          </para>
         </entry>
        </row>
        <row>
         <entry>
          <para>
           <option>4</option>
          </para>
         </entry>
         <entry>
<screen>SYSUUID=<replaceable>SYSTEM_UUID</replaceable></screen>
          <para>
           新增採用小寫十六進位的 UUID，請參閱 <filename>/usr/share/doc/packages/syslinux/pxelinux.txt</filename>
          </para>
         </entry>
        </row>
       </tbody>
      </tgroup>
     </table>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><option>LABEL <replaceable>LABEL</replaceable> KERNEL <replaceable>IMAGE</replaceable> APPEND <replaceable>OPTIONS</replaceable>...</option>
    </term>
    <listitem>
     <para>
      指示如果輸入了 <replaceable>LABEL</replaceable> 做為要開機的核心，則 PXELINUX 應改為將 <replaceable>IMAGE</replaceable> 開機，並且應使用指定的 <option>APPEND</option> 選項。這些值會取代檔案的 global 區段中第一條 <option>LABEL</option> 指令前面指定的值。<replaceable>IMAGE</replaceable> 的預設值與 <replaceable>LABEL</replaceable> 的相同，如果未指定 <option>APPEND</option>，則預設會使用 global 項目 (如果有)。您最多可輸入 128 個 <option>LABEL</option> 項目。
     </para>

     <para>
      PXELINUX 使用以下語法︰
     </para>
<screen>label <replaceable>MYLABEL</replaceable>
  kernel <replaceable>MYKERNEL</replaceable>
  append <replaceable>MYOPTIONS</replaceable></screen>
     <para>
      系統會將標籤當成檔案名稱一樣截斷，截斷後，它們必須是唯一的。舉例來說，<quote>v2.6.30</quote> 和 <quote>v2.6.31</quote> 這兩個標籤在 PXELINUX 中將無法區分，因為它們都會截斷成相同的 DOS 檔案名稱。
     </para>
     <para>
      該核心不必是 Linux 核心。它也可以是開機磁區或 COMBOOT 檔案。
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><option>LOCALBOOT <replaceable>TYPE</replaceable></option>
    </term>
    <listitem>
     <para>
      在 PXELINUX 上，指定 <option>LOCALBOOT 0</option> 而不指定 <option>KERNEL</option> 選項，表示要呼叫此特定標籤；而且最後要使用本地磁碟開機，而不使用核心開機。
     </para>
     <informaltable frame="topbot" rowsep="0">
      <tgroup cols="2">

       <thead>
        <row>
         <entry>
          <para>
           引數
          </para>
         </entry>
         <entry>
          <para>
           描述
          </para>
         </entry>
        </row>
       </thead>
       <tbody>
        <row>
         <entry>
          <para>
           <literal>0</literal>
          </para>
         </entry>
         <entry>
          <para>
           執行正常開機
          </para>
         </entry>
        </row>
        <row>
         <entry>
          <para>
           <literal>4</literal>
          </para>
         </entry>
         <entry>
          <para>
           使用仍常駐在記憶體中的「通用網路驅動程式介面」(Universal Network Driver Interface, UNDI) 執行本地開機。
          </para>
         </entry>
        </row>
        <row>
         <entry>
          <para>
           <literal>5</literal>
          </para>
         </entry>
         <entry>
          <para>
           使用仍常駐在記憶體中完整的 PXE 堆疊 (包括 UNDI 驅動程式) 執行本地開機。
          </para>
         </entry>
        </row>
       </tbody>
      </tgroup>
     </informaltable>
     <para>
      其他所有值都未定義。如果您對 UNDI 或 PXE 堆疊不甚瞭解，請指定 <literal>0</literal>。
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><option>TIMEOUT <replaceable>TIME-OUT</replaceable></option>
    </term>
    <listitem>
     <para>
      指示在開機提示等待自動開機的時間長度，單位為 1/10 秒。若使用者在鍵盤上按下任何一個鍵，就會取消逾時，前提是假設使用者會完成已開始的指令。如果逾時時間設為 0，則會完全停用逾時功能 (此設定值亦為預設值)。可能的最大逾時值為 35996 (小於一小時)。
     </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term><option>PROMPT<replaceable> flag_val</replaceable></option>
    </term>
    <listitem>
     <para>
      如果 <option>flag_val</option> 為 0，只有在按下 <keycap function="shift"/> 或 <keycap function="alt"/>，或者已設定 <keycap>Caps Lock</keycap> 或 <keycap>Scroll Lock</keycap> 時才會顯示開機提示 (此設定值為預設值)。如果 <option>flag_val</option> 為 1，則永遠會顯示開機提示。
     </para>
<screen>F2  <replaceable>FILENAME</replaceable>
F1  <replaceable>FILENAME</replaceable>
..etc...
F9  <replaceable>FILENAME</replaceable>
F10 <replaceable>FILENAME</replaceable></screen>
     <para>
      可在開機提示下按下功能鍵時，於螢幕畫面上顯示指定的檔案。這可用來執行開機前線上說明 (可能是針對核心指令行選項)。如果是使用較早版本的反向相容，<keycap>F10</keycap> 也可以當作 <literal>F0</literal> 輸入。請注意，目前還無法將檔案名稱繫結至 <keycap>F11</keycap> 和 <keycap>F12</keycap>。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </sect1>
 <sect1 xml:id="sec-deployment-prep-boot-pxeprep">
  <title>準備用於 PXE 啟動的目標系統</title>

  <para>
   請在 BIOS 開機順序中包含 PXE 選項，以便準備 PXE 開機的系統 BIOS。
  </para>

  <warning>
   <title>BIOS 開機順序</title>
   <para>
    在 BIOS 中，請勿將 PXE 選項置於硬碟開機參數之前。否則，此系統會在您每次開機時嘗試重新安裝系統。
   </para>
  </warning>
 </sect1>
 <sect1 xml:id="sec-deployment-prep-boot-wolprep">
  <title>準備用於網路喚醒功能的目標系統</title>

  <para>
   您需要使用適當的 BIOS 選項才可在安裝前啟用網路喚醒 (WOL) 功能。另外，請記下目標系統的 MAC 位址。啟動網路喚醒功能時將會用到此資料。
  </para>
 </sect1>
 <sect1 xml:id="sec-deployment-prep-boot-wol">
  <title>區域網路喚醒</title>

  <para>
   「網路喚醒功能」可藉由包含機器 MAC 位址的特定網路封包來開機。因為世界上的每台機器都有一個獨一無二的 MAC 識別碼，所以您不必擔心會不小心開啟錯誤的機器。
  </para>

  <important>
   <title>跨越不同網路區段的網路喚醒功能</title>
   <para>
    如果控制機器與 WOL 指令的目標不在同一網路區段，您可以採用兩種做法。將 WOL 要求設定為以多路廣播形式傳送，或者從遠端控制該網路區段中的某部機器，使其充當這些要求的傳送者。
   </para>
  </important>

  <para>
   SUSE Linux Enterprise Server 的使用者可以使用名為 WOL 的 YaST 模組輕鬆設定網路喚醒功能。其他 SUSE Linux 版本作業系統的使用者則可以使用指令行工具。
  </para>
 </sect1>
 <sect1 xml:id="sec-deployment-prep-boot-wolyast">
  <title>使用 YaST 設定網路喚醒功能</title>
  <para>
   執行以下程序以透過 YaST 設定遠端喚醒。
  </para>
  <procedure xml:id="pro-deployment-prep-boot-wolyast">
   <step>
    <para>
     以 <systemitem class="username">root</systemitem> 身分登入。
    </para>
   </step>
   <step>
    <para>
     啟動<menuchoice> <guimenu>YaST</guimenu> <guimenu> 網路服務</guimenu> <guimenu> WOL</guimenu></menuchoice>。
    </para>
   </step>
   <step>
    <para>
     按一下<guimenu>新增</guimenu>，並輸入目標系統的主機名稱和 MAC 位址。
    </para>
   </step>
   <step>
    <para>
     若要開啟此機器，請選取適當的項目，並按一下<guimenu>喚醒</guimenu>。
    </para>
   </step>
  </procedure>
 </sect1>
 <sect1 xml:id="sec-deployment-prep-boot-mksusecd">
  <title>從 CD 或 USB 磁碟機而非 PXE 開機</title>
  <para>
   您還可以使用包含小型系統影像的 CD、DVD 或 USB 磁碟機來開機，而不透過 PXE 開機。載入核心和 initrd 後，將會透過 NFS 擷取必要檔案。可使用 <command>mksusecd</command> 建立可開機影像。如果目標機器不支援 PXE 開機，此選項可能相當實用。
  </para>
  <para>
   使用 <command>sudo zypper in mksusecd</command> 可安裝該工具。使用以下指令可建立可開機 ISO 影像︰
  </para>
  <screen><prompt>tux &gt; </prompt><command>mksusecd</command> --create image.iso \
--net=nfs://192.168.1.1:/srv/install/<replaceable>ARCH</replaceable>/<replaceable>OS_VERSION</replaceable>/<replaceable>SP_VERSION</replaceable>/cd1  \
/srv/tftpboot/EFI/<replaceable>ARCH</replaceable>/boot</screen>
  <para>
   以目標系統架構對應的目錄取代 <replaceable>ARCH</replaceable>。另外，依據<xref linkend="sec-deployment-tftp-server"/>中的路徑取代 <replaceable>OS_version</replaceable> 和 <replaceable>SP_version</replaceable>。
  </para>
  <para>
   對於 <option>--net</option> 選項，您可以不使用 NFS 伺服器，而是使用 HTTP 儲存庫，例如 openSUSE 儲存庫︰
  </para>
  <screen><prompt>tux &gt; </prompt><command>mksusecd</command> --create image.iso \
--net=http://download.opensuse.org/tumbleweed/repo/oss/suse \
/srv/tftpboot/EFI/<replaceable>ARCH</replaceable>/boot</screen>
  <para>
   可將 <filename>image.iso</filename> 寫入到 DVD 或 CD 中，或使用 <command>dd</command> 將其寫入到 USB 隨身碟中︰
  </para>
  <screen><prompt role="root">root # </prompt><command>dd</command> if=image.iso of=/dev/<replaceable>USB_DEVICE</replaceable></screen>
  <para>
   以 USB 隨身碟的裝置名稱取代 <replaceable>USB_DEVICE</replaceable>。全面檢查裝置名稱，以確認您未意外損毀其他磁碟機上的資料。
  </para>
 </sect1>
</chapter>
