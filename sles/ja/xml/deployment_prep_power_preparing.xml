<?xml version="1.0" encoding="UTF-8"?>
<sect1 xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="deployment_prep_power_preparing.xml" version="5.0" xml:id="cha.power.prep">
 <title>準備</title>

 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>

 <para>
  このセクションでは、SUSE Linux Enterprise Serverを実際にインストールする前に実行する必要がある準備手順について説明します。インストール手順は、使用されているシステムによって異なります。次の方法がサポートされています。
 </para>

 <itemizedlist>
  <listitem>
   <para>
    <xref linkend="sec.prep.powerlinux.powerkvm.kimchi" xrefstyle="select:title nopage"/>
   </para>
  </listitem>
  <listitem>
   <para>
    <xref linkend="sec.prep.powerlinux.powerkvm.virt-install" xrefstyle="select:title nopage"/>
   </para>
  </listitem>
  <listitem>
   <para>
    <xref linkend="sec.prep.powerlinux.powerkvm.ivm" xrefstyle="select:title nopage"/>
   </para>
  </listitem>
  <listitem>
   <para>
    <xref linkend="sec.prep.powerlinux.powerkvm.bare_metal" xrefstyle="select:title nopage"/>
   </para>
  </listitem>
 </itemizedlist>

 <para>
  複数のシステムまたはパーティションにSUSE® Linux Enterprise Serverをインストールする必要がある場合は、ネットワークインストールソースの作成をお勧めします。同じソースを、複数のパーティションまたはシステム上での同時インストールに使用することもできます。ネットワークインストールソースの設定については、<xref linkend="sec.deployment.instserver.sles9"/>で説明されています。
 </para>

 <sect2 version="5.0" xml:id="sec.prep.powerlinux.powerkvm.kimchi">
  <title>IBM PowerKVMを備えたサーバへのKimchiによるインストール</title>
  <para>
   このセクションでは、PowerKVMを備えたIBM PowerLinuxシステムにインストールするための準備作業について説明します。Kimchi Webインタフェースを使用してISOイメージからインストールする方法を取り上げます。Kimchiは、IBM PowerKVMを管理するためのツールです。
  </para>
  <para>
   このセクションでは、IBM PowerLinuxサーバ上でPowerKVMを実行していることを前提とします。PowerKVMを事前にインストールしていなかった場合は、PowerKVMのインストールと設定について、<link xlink:href="http://www.ibm.com/support/knowledgecenter/linuxonibm/liabp/liabpkickoff.htm"/>の<quote>Configuring IBM PowerKVM on Power Systems</quote>を参照してください。
  </para>
  <sect3 xml:id="sec.prep.powerlinux.powerkvm.template">
   <title>KimchiによるSUSE Linux Enterprise Serverテンプレートの作成</title>
   <para>
    テンプレートは、PowerKVMゲストのインストールソースです。テンプレートを作成するか、既存のテンプレートを編集するか、テンプレートをクローニングすることができます。既存のゲストからテンプレートをクローニングするには、そのゲストを無効にする必要があります。
   </para>
   <procedure>
    <title>Kimchiによるテンプレートの作成</title>
    <step>
     <para>
      Webブラウザで、PowerKVMを実行しているPowerLinuxサーバのURLを入力します。たとえば、<literal>https://<replaceable>POWERLINUX_IP</replaceable>:8001</literal>とします(<replaceable>POWERLINUX_IP</replaceable>を実際のシステムのIPアドレスに置き換えてください)。
     </para>
    </step>
    <step>
     <para>
      <guimenu>テンプレート</guimenu>タブをクリックして<guimenu>テンプレート</guimenu>ページを有効にします。
     </para>
    </step>
    <step>
     <para>
      緑色のプラス記号(<guimenu>+</guimenu>)をクリックしてSUSE Linux Enterprise Serverテンプレートを作成します。
     </para>
    </step>
    <step>
     <para>
      <guimenu>Add Template (テンプレートの追加)</guimenu>ダイアログで、次のオプションのいずれかを選択します。
     </para>
     <variablelist>
      <varlistentry>
       <term><guimenu>ローカルISOイメージ</guimenu>
       </term>
       <listitem>
        <para>
         システムで使用可能なインストールISOイメージのストレージプールをスキャンする場合に選択します。
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term><guimenu>Local Image File (ローカルイメージファイル)</guimenu>
       </term>
       <listitem>
        <para>
         ローカルイメージファイルへのパスを指定する場合に選択します。
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term><guimenu>Remote ISO file (リモートISOファイル)</guimenu>
       </term>
       <listitem>
        <para>
         インストールISOイメージのリモートの場所を指定する場合に選択します。
        </para>
       </listitem>
      </varlistentry>
     </variablelist>
    </step>
    <step>
     <para>
      ゲストの作成に使用するISOファイルを選択し、<guimenu>Create Templates from Selected ISO (選択したISOからテンプレートを作成)</guimenu>をクリックします。
     </para>
    </step>
    <step>
     <para>
      新規に作成したテンプレートを設定するには、<menuchoice><guimenu>Actions (アクション)</guimenu>
      <guimenu>編集</guimenu></menuchoice>の順にクリックし、実際のワークロードでの要求に従ってデフォルト値を変更します。
     </para>
     <informalfigure>
      <mediaobject>
       <imageobject role="fo">
        <imagedata fileref="kimchi_create_template.png" width="50%"/>
       </imageobject>
       <imageobject role="html">
        <imagedata fileref="kimchi_create_template.png" width="50%"/>
       </imageobject>
      </mediaobject>
     </informalfigure>
    </step>
   </procedure>
   <para>
    詳細については、<link xlink:href="http://www.ibm.com/support/knowledgecenter/linuxonibm/liabp/liabpkimchitemplate.htm"/>の<quote>Setting up a template using Kimchi</quote>を参照してください。
   </para>
  </sect3>
  <sect3 xml:id="sec.prep.powerlinux.powerkvm.install">
   <title>Kimchiによる、ゲストとしてのSUSE Linux Enterprise Serverのインストール</title>
   <para/>
   <procedure>
    <step>
     <para>
      Webブラウザで、PowerKVMを実行しているPowerLinuxサーバのURLを入力します。たとえば、<literal>https://<replaceable>POWERLINUX_IP</replaceable>:8001</literal>とします(<replaceable>POWERLINUX_IP</replaceable>を実際のシステムのURLに置き換えてください)。
     </para>
    </step>
    <step>
     <para>
      <guimenu>Guests (ゲスト)</guimenu>タブをクリックして<guimenu>Guests (ゲスト)</guimenu>ページを有効にします。
     </para>
    </step>
    <step>
     <para>
      緑色のプラス記号(<guimenu>+</guimenu>)をクリックしてSUSE Linux Enterprise Serverゲストを作成します。
     </para>
    </step>
    <step>
     <para>
      SUSE Linux Enterprise Serverゲストの仮想マシン名を<guimenu>Virtual Machine Name (仮想マシン名)</guimenu>に入力します。
     </para>
     <para>
      <xref linkend="sec.prep.powerlinux.powerkvm.template"/>で作成したSUSE Linux Enterprise Serverテンプレートを選択して<guimenu>Create (作成)</guimenu>をクリックします。
     </para>
     <informalfigure>
      <mediaobject>
       <imageobject role="fo">
        <imagedata fileref="kimchi_create_guest.png" width="70%"/>
       </imageobject>
       <imageobject role="html">
        <imagedata fileref="kimchi_create_guest.png" width="70%"/>
       </imageobject>
      </mediaobject>
     </informalfigure>
    </step>
    <step>
     <para>
      ゲストを作成したら、そのゲストを起動することができます。このSUSE Linux Enterprise Serverゲストを起動するには、赤色の電源ボタンをクリックします。または、<menuchoice><guimenu>アクション</guimenu>
      <guimenu>開始</guimenu></menuchoice>の順に選択します。
     </para>
    </step>
    <step>
     <para>
      <menuchoice><guimenu>Action (アクション)</guimenu>
      <guimenu>Connect (接続)</guimenu></menuchoice>の順にクリックし、<xref linkend="sec.deployment.remoteinst.monitor.vnc.connect"/>の説明に従ってVNCビューアをインストールプロセスに接続します。
     </para>
    </step>
   </procedure>
   <tip>
    <title>複数のゲストの作成</title>
    <para>
     同じ種類の複数のゲストを作成するには、既存のゲストの<guimenu>アクション</guimenu>メニューから<guimenu>クローン</guimenu>を選択します。
    </para>
   </tip>
   <para>
    これにより、<xref linkend="sec.i.yast2.steps"/>で説明している方法で、VNCからデフォルトのインストールを続行できます。
   </para>
  </sect3>
 </sect2>

 <sect2 xml:id="sec.prep.powerlinux.powerkvm.virt-install">
  <title>
   IBM PowerKVMを備えたサーバへの<command>virt-install</command>によるインストール
  </title>
  <para>
   Kimchiでのインストールの代わりに、IBM PowerKVMを備えたサーバでは、<command>virt-install</command>コマンドラインツールを使用してインストールできます。これは、IBM PowerLinux Serverシステムで複数の仮想マシンをインストールする必要がある場合は特に便利です。<command>virt-install</command>を使用すると多彩なインストールシナリオが実現します。以下では、VNCブートおよびPXEブートによるリモートインストールのシナリオを取り上げます。<command>virt-install</command>の詳細については、<xref linkend="sec.libvirt.inst.virt-install"/>を参照してください。
  </para>
  <para>
   <xref linkend="sec.deployment.remoteinst.scenario.vncpxe"/>の説明に従って、インストールソースを格納したリポジトリおよびPXEブートを有効にしたターゲットシステムを用意します。
  </para>
  <para>
   コマンドラインで次のようなコマンドを入力します(実際のニーズと使用しているハードウェアに適合するように各オプションを調整します)。
  </para>

<screen>virt-install --name server_sle12 --memory 4096 --vcpus=2 --pxe \
--graphics vnc --os-variant sles11 \
--disk pool=default,size=3000,format=qcow2,allocation=1G,bus=virtio \
-w mac=<replaceable>mac_address</replaceable>,model=spapr-vlan</screen>
  <para>
   このコマンドでは、VNCグラフィックを使用し、グラフィッククライアントを自動的に起動します。<xref linkend="sec.i.yast2.steps"/>で説明している方法でインストールを完了します。
  </para>
 </sect2>

 <sect2 xml:id="sec.prep.powerlinux.powerkvm.ivm">
  <title>
   IVMを使用したパーティションでのインストール
  </title>
  <para>
   このガイドでは、Integrated Virtualization Manager(IVM) Webインタフェースを使用して、Power Systemsサーバのパーティションに<phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>をインストールする方法について説明します。インストールを開始する前に、次の要件を満たしていることを確認してください。
  </para>
  <itemizedlist>
   <listitem>
    <para>
     Linux on Power System の電源がオンになっていること
    </para>
   </listitem>
   <listitem>
    <para>
     仮想I/Oサーバがインストールされていること
    </para>
   </listitem>
   <listitem>
    <para>
     IVMが初期設定済みであること
    </para>
   </listitem>
  </itemizedlist>
  <procedure>
   <title>IVM Webインタフェースにログインします。</title>
   <step>
    <para>
     Webブラウザウィンドウを開き、HTTPプロトコルまたはHTTPSプロトコルを使用して、インストールプロセスでIVMに割り当てたIPアドレス(https://<replaceable>IP_ADDRESS</replaceable>など)に接続します。［ようこそ］ウィンドウが表示されます。
    </para>
   </step>
   <step>
    <para>
     ユーザ<systemitem class="username">padmin</systemitem>としてログインし、インストールプロセスで定義したパスワードを入力します。IVMインタフェースが表示されます。
    </para>
   </step>
   <step>
    <para>
     <guimenu>View/Modify Virtual Ethernet (仮想イーサネットの表示/変更)</guimenu>を選択します。
    </para>
   </step>
   <step>
    <para>
     <guimenu>Initialize Virtual Ethernet (仮想イーサネットの初期化)</guimenu>をクリックして、パーティション間のイーサネット接続を有効にします。
    </para>
   </step>
   <step>
    <para>
     仮想イーサネットが初期化されたら、［適用］をクリックします。
    </para>
   </step>
   <step>
    <para>
     インストール環境で外部ネットワークが必要な場合は、仮想イーサネットブリッジを作成します。
    </para>
    <substeps>
     <step>
      <para>
       <guimenu>Virtual Ethernet Bridge (仮想イーサネットブリッジ)</guimenu>タブを選択します。
      </para>
     </step>
     <step>
      <para>
       ブリッジの物理アダプタを選択し、<guimenu>適用</guimenu>をクリックして続行します。
      </para>
     </step>
    </substeps>
   </step>
  </procedure>
  <para>次の手順に従って、パーティションを作成します。</para>
  <procedure>
   <title>パーティションの作成</title>
   <step>
    <para>
     IVM Webインタフェースで、<menuchoice><guimenu>View/Modify Partition (パーティションの表示/変更)</guimenu> <guimenu>Create Partition (パーティションの作成)</guimenu></menuchoice>の順にクリックします。
    </para>
   </step>
   <step>
    <para>
     パーティションの名前を入力します。次のステップに進むには、<guimenu>次へ</guimenu>をクリックします。後続のステップでも同様に実行します。
    </para>
   </step>
   <step>
    <para>
     パーティションのメモリを指定します。共有メモリプールを作成している場合は、パーティションでメモリを共有できます。作成していない場合は、<guimenu>Dedicated (専用)</guimenu>を選択します。
    </para>
   </step>
   <step>
    <para>
     プロセッサの数とパーティションの処理モードを指定します。
    </para>
   </step>
   <step>
    <para>
     パーティション用の仮想イーサネットを指定します。アダプタを設定しない場合は、仮想イーサネットに対して<guimenu>なし</guimenu>を選択します。
    </para>
   </step>
   <step>
    <para>
     新しい仮想ディスクを作成するか、現在パーティションに割り当てられていない既存の仮想ディスクと物理ボリュームを割り当てます。
    </para>
   </step>
   <step>
    <para>
     ディスクの<guimenu>Virtual disk name (仮想ディスク名)</guimenu>と<guimenu>Storage pool name (ストレージプール名)</guimenu>を確認し、<guimenu>Virtual disk size (仮想ディスクサイズ)</guimenu>を指定します。
    </para>
   </step>
   <step>
    <para>
     <guimenu>Physical Optical Devices (物理光デバイス)</guimenu>と<guimenu>Virtual Optical Devices (仮想光デバイス)</guimenu>を展開してパーティション用の光デバイスを設定し、パーティションに割り当てるデバイスを選択します。
    </para>
   </step>
   <step>
    <para>
     パーティションの設定を確認したら、<guimenu>完了</guimenu>をクリックします。パーティションが作成され、<guimenu>View/Modify Partition (パーティションの表示/変更)</guimenu>リストから選択できるようになります。
    </para>
   </step>
  </procedure>
  <para>作成したパーティションを有効にします。</para>
  <procedure>
   <title>パーティションの有効化</title>
   <step>
    <para>
     IVM Webインタフェースで<guimenu>View/Modify Partition (パーティションの表示/変更)</guimenu>をクリックして、有効化するパーティションの横のボックスを選択します。
    </para>
   </step>
   <step>
    <para>
     <guimenu>More Tasks (その他のタスク)</guimenu>を選択します。
    </para>
   </step>
   <step>
    <para>
     <guimenu>Open a terminal window (ターミナルウィンドウを開く)</guimenu>を選択します。
    </para>
   </step>
   <step>
    <para>
     パーティションの横にある<guimenu>有効化</guimenu>をクリックします。
    </para>
   </step>
   <step>
    <para>
     ターミナルウィンドウで<keycap>1</keycap>を入力して、システム管理サービス(SMS)を起動します。
    </para>
   </step>
  </procedure>
  <para>
   これでマシンが設定されたため、インストール環境をブートすることができます。
  </para>
  <procedure>
   <title>Linuxインストールのブート</title>
   <step>
    <para>
     <guimenu>Boot selection (ブート選択)</guimenu>ウィンドウで<keycap>1</keycap>を入力して、<guimenu>SMS Menu (SMSメニュー)</guimenu>を選択します。ファームウェアのブート画面が完全に表示される前に<keycap>1</keycap>を入力します。ブート画面は、完全に表示された後で消えてしまうからです。この画面に気付かなかった場合は、システムを再起動します。
    </para>
   </step>
   <step>
    <para>
     この時点で、仮想I/Oサーバ(VIOS)メディアディスクをディスクドライブに挿入します。
    </para>
   </step>
   <step>
    <para>
     <keycap>2</keycap>を入力して、<guimenu>Language selection (言語選択)</guimenu>メニューのパスワード入力に進みます。管理者IDのパスワードを入力します。
    </para>
   </step>
   <step>
    <para>
     メインSMSメニューで<keycap>5</keycap>を入力して、<guimenu>Select Boot Options (ブートオプションの選択)</guimenu>を選択します。
    </para>
   </step>
   <step>
    <para>
     <keycap>1</keycap>を入力して、<guimenu>Install/Boot Device (デバイスのインストール/ブート)</guimenu>を選択します。
    </para>
   </step>
   <step>
    <para>
     <keycap>7</keycap>を入力して、使用可能なすべてのブートデバイスを表示します。
    </para>
   </step>
   <step>
    <para>
     使用したいデバイスに対応する数値を入力します。デバイスが一覧表示されていない場合は、<keycap>N</keycap>を入力してその他のデバイスも表示します。
    </para>
   </step>
   <step>
    <para>
     <keycap>2</keycap>を入力して、<guimenu>Normal Mode Boot (通常モードのブート)</guimenu>を実行します。
    </para>
   </step>
   <step>
    <para>
     <keycap>1</keycap>を入力してSMSメニューを閉じ、ブートプロセスを開始します。
    </para>
   </step>
   <step>
    <para>
     インストーラのブートプロンプトで、次のように入力します。
    </para>
    <screen>install vnc=1
vncpassword=<replaceable>VNC_PASSWORD</replaceable></screen>
    <para>
     <replaceable>VNC_PASSWORD</replaceable>をお気に入りのパスワード(最低8文字)に置き換えてから<keycap function="enter"/>を押して、<phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>のインストールを開始します。カーネルがロードを開始します。
    </para>
   </step>
  </procedure>
  <para>
   カーネルがロードを開始した後、インストーラでは、VNCセッションを設定するための情報をシステムから取得する必要が生じます。VNCを使用するには、有効なTCP/IPスタックが必要です。DHCPを使用するか、インストーラの指示に従ってネットワーキング情報を手動で定義します。
  </para>
  <procedure>
   <title>VNCセッションを開始します。</title>
   <step>
    <para>
     <guimenu>ネットワークデバイス</guimenu>ウィンドウで、ネットワークデバイスとして<guimenu>eth0 (eth0)</guimenu>を選択します。<guimenu>OK</guimenu>を選択して<keycap function="enter"/>を押します。
    </para>
   </step>
   <step>
    <para>
     インストールメディアをテストします。または、<guimenu>Skip (スキップ)</guimenu>を選択して、テストなしで続行します。
    </para>
   </step>
   <step>
    <para>
     システムでVNCサーバが起動された後は、VNCクライアントの接続を求めるメッセージと、それに続いてIPアドレスが表示されます。このIPアドレスを書き留めてください。
    </para>
   </step>
   <step>
    <para>
     ラップトップまたはPCでVNCクライアントを起動します。前のステップのIPアドレスと、それに続いて<literal>:1</literal>を入力します(たとえば、192.168.2.103:1)。
    </para>
   </step>
   <step>
    <para>
     <xref linkend="sec.i.yast2.steps"/>で説明している方法でインストールを完了します。
    </para>
   </step>
  </procedure>
 </sect2>
 <sect2 xml:id="sec.prep.powerlinux.powerkvm.bare_metal">
  <title>
   オープンなPower抽象化レイヤのないサーバへのインストール
  </title>
  <para>
   この情報に基づいて、シリアルコンソールを使用したり、Power Systemsサーバのモニタとキーボードを使用したりしてLinuxをインストールできます。このインストールは、ブートの準備ができた非管理対象(スタンドアロン)システムを想定しています。
  </para>
  <procedure>
   <step>
    <para>
     <guimenu>Power On/Off System (システムの電源オン/オフ)</guimenu>メニューから<guimenu>Power On (電源オン)</guimenu>を選択して、システムの電源を入れます。コンソールを使用し続けるかどうかを尋ねられたら、<keycap>0</keycap>を入力して使用を継続します。
    </para>
   </step>
   <step>
    <para>
     <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>インストールメディアをディスクドライブに挿入します。
    </para>
   </step>
   <step>
    <para>
     <guimenu>Select Language (言語の選択)</guimenu>ウィンドウで<keycap>2</keycap>を入力して、ブートに進みます。
    </para>
   </step>
   <step>
    <para>
     <keycap>1</keycap>を入力して、使用許諾契約書の条項を受け入れます。
    </para>
   </step>   <step>
    <para>
     <guimenu>Boot selection (ブート選択)</guimenu>ウィンドウで<keycap>1</keycap>を入力して、<guimenu>SMS Menu (SMSメニュー)</guimenu>を選択します。ファームウェアのブート画面が完全に表示される前に<keycap>1</keycap>を入力します。ブート画面は、完全に表示された後で消えてしまうからです。この画面に気付かなかった場合は、システムを再起動します。
    </para>
   </step>
   <step>
    <para>
     <keycap>2</keycap>を入力して、<guimenu>Language selection (言語選択)</guimenu>メニューのパスワード入力に進みます。管理者IDのパスワードを入力します。
    </para>
   </step>
   <step>
    <para>
     メインSMSメニューで<keycap>5</keycap>を入力して、<guimenu>Select Boot Options (ブートオプションの選択)</guimenu>を選択します。
    </para>
   </step>
   <step>
    <para>
     <keycap>7</keycap>を入力して、使用可能なすべてのブートデバイスを表示します。
    </para>
   </step>
   <step>
    <para>
     使用したいデバイスに対応する数値を入力します。デバイスが一覧表示されていない場合は、<keycap>N</keycap>を入力してその他のデバイスも表示します。
    </para>
   </step>
   <step>
    <para>
     <keycap>2</keycap>を入力して、<guimenu>Normal Mode Boot (通常モードのブート)</guimenu>を実行します。
    </para>
   </step>   <step>
    <para>
     <keycap>1</keycap>を入力してSMSメニューを閉じ、ブートプロセスを開始します。
    </para>
   </step>
   <step>
    <para>
     インストーラのブートプロンプトで、次のように入力します。
    </para>
    <screen>install vnc=1
vncpassword=<replaceable>VNC_PASSWORD</replaceable></screen>
    <para>
     <replaceable>VNC_PASSWORD</replaceable>をお気に入りのパスワード(最低8文字)に置き換えてから<keycap function="enter"/>を押して、<phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>のインストールを開始します。カーネルがロードを開始します。
    </para>
   </step>
  </procedure>

  <para>
   カーネルがロードを開始した後、インストーラでは、VNCセッションを設定するための情報をシステムから取得する必要が生じます。VNCを使用するには、有効なTCP/IPスタックが必要です。DHCPを使用するか、インストーラの指示に従ってネットワーキング情報を手動で定義します。
  </para>
  <procedure>
   <title>VNCセッションを開始します。</title>
   <step>
    <para>
     <guimenu>ネットワークデバイス</guimenu>ウィンドウで、ネットワークデバイスとして<guimenu>eth0 (eth0)</guimenu>を選択します。<guimenu>OK</guimenu>を選択して<keycap function="enter"/>を押します。
    </para>
   </step>
   <step>
    <para>
     インストールメディアをテストします。または、<guimenu>Skip (スキップ)</guimenu>を選択して、テストなしで続行します。
    </para>
   </step>
   <step>
    <para>
     システムでVNCサーバが起動された後は、VNCクライアントの接続を求めるメッセージと、それに続いてIPアドレスが表示されます。このIPアドレスを書き留めてください。
    </para>
   </step>
   <step>
    <para>
     ラップトップまたはPCでVNCクライアントを起動します。前のステップのIPアドレスと、それに続いて<literal>:1</literal>を入力します(たとえば、192.168.2.103:1)。
    </para>
   </step>
   <step>
    <para>
     <xref linkend="sec.i.yast2.steps"/>で説明している方法でインストールを完了します。
    </para>
   </step>
  </procedure>
 </sect2>

</sect1>
