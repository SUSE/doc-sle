language: bash
dist: trusty
os: linux

addons:
  apt:
    packages:
    # Necessary to get xmllint available
    - libxml2-utils

before_script:
    - echo $TRAVIS_COMMIT
    - ls xml/*.xml

script:
    - xmllint --noout --noent xml/*.xml || false
    - true 
