<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE chapter
[
  <!ENTITY % entities SYSTEM "entity-decl.ent">
    %entities;
]>
<chapter xmlns="http://docbook.org/ns/docbook"
xmlns:xi="http://www.w3.org/2001/XInclude"
xmlns:xlink="http://www.w3.org/1999/xlink" version="5.0"
xml:id="dynamic-profiles">
 <title>Supported approaches to dynamic profiles</title>
 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker></dm:bugtracker>
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>
 <para>
  When dealing with the installation of multiple systems, it might be useful to
  use a single profile (or a reduced set of them) that adapts automatically to
  each system. In this regard, &ay; offers three different mechanisms to modify
  the profile at installation time.
 </para>
 <variablelist>
  <varlistentry>
   <term>Rules and classes</term>
   <listitem>
    <para>
     Rules and classes offer the possibility to configure a system by merging
     multiple control files during installation. You can read more about this
     feature in the <xref linkend="rulesandclass"/> section.
    </para>
   </listitem>
  </varlistentry>
  <varlistentry>
   <term>ERB templates</term>
   <listitem>
    <para>
     &ay; supports Embedded Ruby (ERB) templates syntax to modify the profile's
     content during installation. The <xref
       linkend="erb-templates"/>
     section describes how to use them.
    </para>
   </listitem>
  </varlistentry>
  <varlistentry>
   <term>Pre-installation scripts</term>
   <listitem>
    <para>
     You can use a pre-installation script to modify or even create a brand new
     profile during installation. <xref linkend="pre-install-scripts"/>
     describes how to benefit from them.
    </para>
   </listitem>
  </varlistentry>
  <varlistentry>
   <term>Asking the user during installation</term>
   <listitem>
    <para>
     As an alternative, &ay; can ask the user for values to use in the profile
     at runtime. The installation is not fully unattended in that case, but it
     can be rather useful to set user names, passwords, IP addresses and so on.
     You can find more information about this feature in the
     <xref linkend="CreateProfile-Ask"/> section.
    </para>
   </listitem>
  </varlistentry>
 </variablelist>
</chapter>
