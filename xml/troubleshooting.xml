<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter PUBLIC "-//Novell//DTD NovDoc XML V1.0//EN" "novdocx.dtd"
[
  <!ENTITY % NOVDOC.DEACTIVATE.IDREF "INCLUDE">
  <!ENTITY % entities SYSTEM "entity-decl.ent">
  %entities;
]>
<chapter id="cha.trouble">
 <title>Common Problems and Their Solutions</title>
 <para>
  This chapter describes a range of potential problems and their solutions.
  Even if your situation is not precisely listed here, there may be one
  similar enough to offer hints to the solution of your problem.
 </para>
 <sect1 id="sec.trouble.info">
  <title>Finding and Gathering Information</title>

  <para>
   Linux reports things in a very detailed way. There are several places to
   look when you encounter problems with your system, most of which are
   standard to Linux systems in general, and some of which are relevant to
   &productname; systems. Most log files can be viewed with &yast;
   (<menuchoice> <guimenu>Miscellaneous</guimenu> <guimenu>Start-Up
   Log</guimenu> </menuchoice>).
  </para>

<!-- fs 2010-05-21:
    Weird location for this paragraph - completely out of context (support).
    Move to a better location

    Profiling, since he support query module is not available for openSUSE.
-->

  <para os="sles;sled;">
   <indexterm>
   <primary>YaST</primary>
   <secondary>support query</secondary>
   </indexterm> <indexterm>
   <primary>support query</primary>
   </indexterm> &yast; offers the possibility to collect all system
   information needed by the support team. Use <menuchoice>
<!-- TODO check this vvvvvv -->
   <guimenu>Other</guimenu> <guimenu>Support</guimenu> </menuchoice> and
   select the problem category. When all information is gathered, attach it
   to your support request.
  </para>

  <para>
   A list of the most frequently checked log files follows with the
   description of their typical purpose. Paths containing
   <filename>~</filename> refer to the current user's home directory.
  </para>

  <table id="tab.trouble.info">
   <title>Log Files</title>
   <tgroup cols="2">
    <thead>
     <row>
      <entry>
       <para>
        Log File
       </para>
      </entry>
      <entry>
       <para>
        Description
       </para>
      </entry>
     </row>
    </thead>
    <tbody>
     <row>
      <entry>
       <para>
        <filename>~/.xsession-errors</filename>
       </para>
      </entry>
      <entry>
       <para>
        Messages from the desktop applications currently running.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/var/log/apparmor/</filename>
       </para>
      </entry>
      <entry>
       <para>
        Log files from AppArmor, see <xref linkend="part.apparmor"/> for
        detailed information.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/var/log/audit/audit.log</filename>
       </para>
      </entry>
      <entry>
       <para>
        Log file from Audit to track any access to files, directories, or
        resources of your system, and trace system calls. See
        <xref linkend="part.audit"/> for detailed information.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/var/log/boot.msg</filename>
       </para>
      </entry>
      <entry>
       <para>
        Messages from the kernel reported during the boot process.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/var/log/mail.*</filename>
       </para>
      </entry>
      <entry>
       <para>
        Messages from the mail system.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/var/log/messages</filename>
       </para>
      </entry>
      <entry>
       <para>
        Ongoing messages from the kernel and system log daemon (when
        running).
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/var/log/NetworkManager</filename>
       </para>
      </entry>
      <entry>
       <para>
        Log file from &nm; to collect problems with network connectivity
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/var/log/samba/</filename>
       </para>
      </entry>
      <entry>
       <para>
        Directory containing Samba server and client log messages.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/var/log/SaX.log</filename>
       </para>
      </entry>
      <entry>
       <para>
        Hardware messages from the SaX display and KVM system.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/var/log/warn</filename>
       </para>
      </entry>
      <entry>
       <para>
        All messages from the kernel and system log daemon with the
        <quote>warning</quote> level or higher.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/var/log/wtmp</filename>
       </para>
      </entry>
      <entry>
       <para>
        Binary file containing user login records for the current machine
        session. View it with <command>last</command>.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/var/log/Xorg.*.log</filename>
       </para>
      </entry>
      <entry>
       <para>
        Various start-up and runtime logs from the X Window system. It is
        useful for debugging failed X start-ups.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/var/log/YaST2/</filename>
       </para>
      </entry>
      <entry>
       <para>
        Directory containing &yast;'s actions and their results.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/var/log/zypper.log</filename>
       </para>
      </entry>
      <entry>
       <para>
        Log file of zypper.
       </para>
      </entry>
     </row>
    </tbody>
   </tgroup>
  </table>

  <para>
   Apart from log files, your machine also supplies you with information
   about the running system. See
   <xref linkend="tab.trouble.system" xrefstyle="select:label title nopage"/>
  </para>

  <table id="tab.trouble.system">
   <title>System Information With the <filename>/proc</filename> File System</title>
   <tgroup cols="2">
    <thead>
     <row>
      <entry>
       <para>
        File
       </para>
      </entry>
      <entry>
       <para>
        Description
       </para>
      </entry>
     </row>
    </thead>
    <tbody>
     <row>
      <entry>
       <para>
        <filename>/proc/cpuinfo</filename>
       </para>
      </entry>
      <entry>
       <para>
        Contains processor information, including its type, make, model, and
        performance.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/proc/dma</filename>
       </para>
      </entry>
      <entry>
       <para>
        Shows which DMA channels are currently being used.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/proc/interrupts</filename>
       </para>
      </entry>
      <entry>
       <para>
        Shows which interrupts are in use, and how many of each have been in
        use.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/proc/iomem</filename>
       </para>
      </entry>
      <entry>
       <para>
        Displays the status of I/O (input/output) memory.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/proc/ioports</filename>
       </para>
      </entry>
      <entry>
       <para>
        Shows which I/O ports are in use at the moment.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/proc/meminfo</filename>
       </para>
      </entry>
      <entry>
       <para>
        Displays memory status.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/proc/modules</filename>
       </para>
      </entry>
      <entry>
       <para>
        Displays the individual modules.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/proc/mounts</filename>
       </para>
      </entry>
      <entry>
       <para>
        Displays devices currently mounted.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/proc/partitions</filename>
       </para>
      </entry>
      <entry>
       <para>
        Shows the partitioning of all hard disks.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/proc/version</filename>
       </para>
      </entry>
      <entry>
       <para>
        Displays the current version of Linux.
       </para>
      </entry>
     </row>
    </tbody>
   </tgroup>
  </table>

  <para>
   Apart from the <filename>/proc</filename> file system, the Linux kernel
   exports information with the <literal>sysfs</literal> module, an
   in-memory filesystem. This module represents kernel objects, their
   attributes and relationships. For more information about
   <literal>sysfs</literal>, see the context of udev in
   <xref linkend="cha.udev"/>.
   <xref linkend="tab.trouble.sysfs" xrefstyle="select:label nopage"/>
   contains an overview of the most common directories under
   <filename>/sys</filename>.
  </para>

  <table id="tab.trouble.sysfs">
   <title>System Information With the <filename>/sys</filename> File System</title>
   <tgroup cols="2">
    <thead>
     <row>
      <entry>
       <para>
        File
       </para>
      </entry>
      <entry>
       <para>
        Description
       </para>
      </entry>
     </row>
    </thead>
    <tbody>
     <row>
      <entry>
       <para>
        <filename>/sys/block</filename>
       </para>
      </entry>
      <entry>
       <para>
        Contains subdirectories for each block device discovered in the
        system. Generally, these are mostly disk type devices.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/sys/bus</filename>
       </para>
      </entry>
      <entry>
       <para>
        Contains subdirectories for each physical bus type.
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/sys/class</filename>
       </para>
      </entry>
      <entry>
       <para>
        Contains subdirectories grouped together as a functional types of
        devices (like graphics, net, printer, etc.)
       </para>
      </entry>
     </row>
     <row>
      <entry>
       <para>
        <filename>/sys/device</filename>
       </para>
      </entry>
      <entry>
       <para>
        Contains the global device hierarchy.
       </para>
      </entry>
     </row>
    </tbody>
   </tgroup>
  </table>

  <para>
   Linux comes with a number of tools for system analysis and monitoring.
   See <xref linkend="cha.util"/> for a selection of the most important ones
   used in system diagnostics.
  </para>

  <para>
   Each of the following scenarios begins with a header describing the
   problem followed by a paragraph or two offering suggested solutions,
   available references for more detailed solutions, and cross-references to
   other scenarios that are related.
  </para>
 </sect1>
 <sect1 id="sec.trouble.install">
  <title>Installation Problems</title>

  <para>
   Installation problems are situations when a machine fails to install. It
   may fail entirely or it may not be able to start the graphical installer.
   This section highlights some of the typical problems you may run into,
   and offers possible solutions or workarounds for these kinds of
   situations.
  </para>

  <sect2 id="sec.trouble.checkingMedia">
   <title>Checking Media</title><indexterm>
   <primary>YaST</primary>
   <secondary>media check</secondary></indexterm><indexterm>
   <primary>DVDs</primary>
   <secondary>checking</secondary></indexterm>
   <para>
    If you encounter any problems using the &productname; installation
    media, check the integrity of your installation media. Boot from the
    media and choose <guimenu>Check Installation Media</guimenu> from the
    boot menu. In a running system, start &yast; and choose <menuchoice>
    <guimenu>Software</guimenu> <guimenu>Media Check</guimenu>
    </menuchoice>. To check the &productname; medium, insert it into the
    drive and click <guimenu>Start Check</guimenu> in the <guimenu>Media
    Check</guimenu> screen of &yast;. This may take several minutes. If
    errors are detected, do not use this medium for installation. Media
    problems may occur when having burned the medium yourself. Burning the
    media at a low speed (4x) helps avoiding problems.
   </para>
   <figure id="fig.yast2.mediacheck">
    <title>Checking Media</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="yast2_medaicheck1.png" width="75%" format="PNG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="yast2_medaicheck1.png" width="75%" format="PNG"/>
     </imageobject>
    </mediaobject>
   </figure>
  </sect2>

  <sect2 id="sec.trouble.hardinfo">
   <title>Hardware Information</title><indexterm>
   <primary>hardware</primary>
   <secondary>information</secondary></indexterm><indexterm>
   <primary>YaST</primary>
   <secondary>hardware</secondary>
   <tertiary>information</tertiary></indexterm>
   <para>
    Display detected hardware and technical data using <menuchoice>
    <guimenu>Hardware</guimenu> <guimenu>Hardware Information</guimenu>
    </menuchoice>. Click any node of the tree for more information about a
    device. This module is especially useful, when submitting a support
    request for which you need information about your hardware.
   </para>
   <para>
    Save the displayed hardware information to a file by clicking
    <guimenu>Save to File</guimenu>. Select the desired directory and
    filename then click <guimenu>Save</guimenu> to create the file.
   </para>
   <figure id="fig.yast2.hardinfo">
    <title>Displaying Hardware Information</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="yast2_hwinfo_1.png" width="75%" format="PNG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="yast2_hwinfo_1.png" width="75%" format="PNG"/>
     </imageobject>
    </mediaobject>
   </figure>
  </sect2>

  <sect2 id="sec.trouble.install.cdrom">
   <title>No Bootable DVD Drive Available</title>
   <para>
    If your computer does not contain a bootable DVD-ROM drive or if the one
    you have is not supported by Linux, there are several options you can
    install your machine without a built-in DVD drive:
   </para>
<?dbfo-need height="20em"?>
   <variablelist>
    <varlistentry>
     <term>Booting from a Floppy Disk</term>
     <listitem>
      <para>
       Create a boot floppy and boot from floppy disk instead of DVD.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Using an External Boot Device</term>
     <listitem>
      <para>
       If it is supported by your BIOS and the installation kernel, boot
       from external DVD drives or USB storage devices.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Network Boot via PXE</term>
     <listitem>
      <para>
       If a machine lacks a DVD drive, but provides a working ethernet
       connection, perform a completely network-based installation. See
       <xref linkend="sec.deployment.remoteinst.scenario.vncpxe"/> and
       <xref linkend="sec.deployment.remoteinst.scenario.sshpxe"/> for
       details.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
<!-- fs 2010-05-21:
     This section does not describe the most important thing:
     How to create boot floppies. Therefore commenting it 

   <sect3 id="sec.trouble.install.cdrom.bdisk">
    <title>Booting from a Floppy Disk (SYSLINUX)</title><indexterm>
    <primary>booting</primary>
    <secondary>floppy disks, from</secondary></indexterm>
    <para>
     On some older computers, there is no bootable DVD drive available, but
     there is a floppy disk drive present. To install on such a system,
     create boot disks and boot your system with them.
    </para>
    <para>
     The boot disks include the loader called SYSLINUX and the program
     linuxrc. SYSLINUX enables the selection of a kernel during the boot
     procedure and the specification of any parameters needed for the
     hardware used. The program linuxrc supports the loading of kernel
     modules for your hardware and subsequently starts the installation.
    </para>
    <para>
     When booting from a boot disk, the boot procedure is initiated by the
     boot loader SYSLINUX (package <systemitem>syslinux</systemitem>). When
     the system is booted, SYSLINUX runs a minimum hardware detection that
     mainly consists of the following steps:
    </para>
    <orderedlist>
     <listitem>
      <para>
       The program checks if the BIOS provides VESA&nbsp;2.0&ndash;compliant
       framebuffer support and boots the kernel accordingly.
      </para>
     </listitem>
     <listitem>
      <para>
       The monitor data (DDC info) is read.
      </para>
     </listitem>
     <listitem>
      <para>
       The first block of the first hard disk (MBR) is read to map BIOS IDs
       to Linux device names during the boot loader configuration. The
       program attempts to read the block by means of the the lba32
       functions of the BIOS to determine if the BIOS supports these
       functions.
      </para>
     </listitem>
    </orderedlist>
    <para>
     If you keep <keycap>Shift</keycap> pressed when SYSLINUX starts, all
     these steps are skipped. For troubleshooting purposes, insert the line
    </para>
<screen>verbose 1</screen>
    <para>
     in <filename>syslinux.cfg</filename> for the boot loader to display
     which action is currently being performed.
    </para>
    <para>
     If the machine does not boot from the floppy disk, you may need to
     change the boot sequence in the BIOS to <literal>A,C,CDROM</literal>.
    </para>
   </sect3>
-->
   <sect3 id="sec.trouble.install.cdrom.ext">
    <title>External Boot Devices</title>
    <para>
     Linux supports most existing DVD drives. If the system has neither a
     DVD drive nor a floppy disk, it is still possible that an external DVD
     drive, connected through USB, FireWire, or SCSI, can be used to boot
     the system. This depends mainly on the interaction of the BIOS and the
     hardware used. Sometimes a BIOS update may help if you encounter
     problems.
    </para>
    <para os="osuse">
     When installing from a KDE or GNOME Live CD, you can also create a
     <quote>Live USB stick</quote> to boot from. Refer to
     <ulink url="http://en.opensuse.org/Live_USB_stick"/> for detailed
     instructions.
    </para>
   </sect3>
  </sect2>

  <sect2 id="sec.trouble.install.BIOS">
   <title>Booting from Installation Media Fails</title><indexterm>
   <primary>DVDs</primary>
   <secondary>booting from</secondary></indexterm><indexterm>
   <primary>booting</primary>
   <secondary>DVDs, from</secondary></indexterm>
   <para>
    One reason why a machine does not boot the installation media can be an
    incorrect boot sequence setting in BIOS. The BIOS boot sequence must
    have DVD drive set as the first entry for booting. Otherwise the machine
    would try to boot from another medium, typically the hard disk. Guidance
    for changing the BIOS boot sequence can be found the documentation
    provided with your motherboard, or in the following paragraphs.
   </para>
   <para>
    The BIOS is the software that enables the very basic functions of a
    computer. Motherboard vendors provide a BIOS specifically made for their
    hardware. Normally, the BIOS setup can only be accessed at a specific
    time&mdash;when the machine is booting. During this initialization
    phase, the machine performs a number of diagnostic hardware tests. One
    of them is a memory check, indicated by a memory counter. When the
    counter appears, look for a line, usually below the counter or somewhere
    at the bottom, mentioning the key to press to access the BIOS setup.
    Usually the key to press is one of <keycap>Del</keycap>,
    <keycap>F1</keycap>, or <keycap>Esc</keycap>. Press this key until the
    BIOS setup screen appears.
   </para>
   <procedure id="proc.trouble.install.bios">
    <title>Changing the BIOS Boot Sequence</title>
    <step>
     <para>
      Enter the BIOS using the proper key as announced by the boot routines
      and wait for the BIOS screen to appear.
     </para>
    </step>
    <step>
     <para>
      To change the boot sequence in an AWARD BIOS, look for the
      <guimenu>BIOS FEATURES SETUP</guimenu> entry. Other manufacturers may
      have a different name for this, such as <guimenu>ADVANCED CMOS
      SETUP</guimenu>. When you have found the entry, select it and confirm
      with <keycap>Enter</keycap>.
     </para>
    </step>
    <step>
     <para>
      In the screen that opens, look for a subentry called <guimenu>BOOT
      SEQUENCE</guimenu> or <guimenu>BOOT ORDER</guimenu>. The boot sequence
      looks something like <literal>C,A</literal> or <literal>A,C</literal>.
      In the former case, the machine first searches the hard disk (C) then
      the floppy drive (A) to find a bootable medium. Change the settings by
      pressing <keycap>PgUp</keycap> or <keycap>PgDown</keycap> until the
      sequence is <literal>A,CDROM,C</literal>.
     </para>
    </step>
    <step>
     <para>
      Leave the BIOS setup screen by pressing <keycap>Esc</keycap>. To save
      the changes, select <guimenu>SAVE &amp; EXIT SETUP</guimenu>, or press
      <keycap>F10</keycap>. To confirm that your settings should be saved,
      press <keycap>Y</keycap>.
     </para>
    </step>
   </procedure>
   <procedure>
    <title>Changing the Boot Sequence in a SCSI BIOS (Adaptec Host Adapter)</title>
    <step>
     <para>
      Open the setup by pressing <keycombo> <keycap>Ctrl</keycap>
      <keycap>A</keycap> </keycombo>.
     </para>
    </step>
    <step>
     <para>
      Select <guimenu>Disk Utilities</guimenu>. The connected hardware
      components are now displayed.
     </para>
     <para>
      Make note of the SCSI ID of your DVD drive.
     </para>
    </step>
    <step>
     <para>
      Exit the menu with <keycap>Esc</keycap>.
     </para>
    </step>
    <step>
     <para>
      Open <guimenu>Configure Adapter Settings</guimenu>. Under
      <guimenu>Additional Options</guimenu>, select <guimenu>Boot Device
      Options</guimenu> and press <keycap>Enter</keycap>.
     </para>
    </step>
    <step>
     <para>
      Enter the ID of the DVD drive and press <keycap>Enter</keycap> again.
     </para>
    </step>
    <step>
     <para>
      Press <keycap>Esc</keycap> twice to return to the start screen of the
      SCSI BIOS.
     </para>
    </step>
    <step>
     <para>
      Exit this screen and confirm with <guimenu>Yes</guimenu> to boot the
      computer.
     </para>
    </step>
   </procedure>
   <para>
    Regardless of what language and keyboard layout your final installation
    will be using, most BIOS configurations use the US keyboard layout as
    depicted in the following figure: <indexterm>
    <primary>US Keyboard Layout</primary>
    </indexterm>
   </para>
   <figure id="fig.trouble.install.keyboard_us">
    <title>US Keyboard Layout</title>
    <mediaobject>
     <imageobject role="fo">
      <imagedata fileref="keyboard_us.svg" width="75%" format="SVG"/>
     </imageobject>
     <imageobject role="html">
      <imagedata fileref="keyboard_us.png" width="75%" format="PNG"/>
     </imageobject>
    </mediaobject>
   </figure>
  </sect2>

  <sect2 id="sec.trouble.install.noboot">
   <title>Fails to Boot</title>
   <para>
    Some hardware types, mainly very old or very recent ones, fail to
    install. In many cases, this may happen because support for this type of
    hardware is missing in the installation kernel, or due to certain
    functionality included in this kernel, such as ACPI, that can still
    cause problems on some hardware.
   </para>
   <para>
    If your system fails to install using the standard
    <guimenu>Installation</guimenu> mode from the first installation boot
    screen, try the following:
   </para>
   <procedure>
    <step>
     <para>
      With the DVD still in the drive, reboot the machine with
      <keycombo action="press"> <keycap function="control">Ctrl</keycap>
      <keycap function="alt">Alt</keycap>
      <keycap function="delete">Del</keycap> </keycombo> or using the
      hardware reset button.
     </para>
    </step>
    <step>
     <para>
      When the boot screen appears, press <keycap>F5</keycap>, use the arrow
      keys of your keyboard to navigate to <guimenu>No ACPI</guimenu> and
      press <keycap function="enter" >Enter</keycap> to launch the boot and
      installation process. This option disables the support for ACPI power
      management techniques.
     </para>
    </step>
    <step>
     <para>
      Proceed with the installation as described in
      <xref linkend="cha.inst"/>.
     </para>
    </step>
   </procedure>
   <para>
    If this fails, proceed as above, but choose <guimenu>Safe
    Settings</guimenu> instead. This option disables ACPI and DMA support.
    Most hardware will boot with this option.
   </para>
   <para>
    If both of these options fail, use the boot options prompt to pass any
    additional parameters needed to support this type of hardware to the
    installation kernel. For more information about the parameters available
    as boot options, refer to the kernel documentation located in
    <filename>/usr/src/linux/Documentation/kernel-parameters.txt</filename>.
   </para>
   <tip>
    <title>Obtaining Kernel Documentation</title>
    <para>
     Install the <systemitem class="systemname">kernel-source</systemitem>
     package to view the kernel documentation.
    </para>
   </tip>
   <para>
    There are various other ACPI-related kernel parameters that can be
    entered at the boot prompt prior to booting for installation:
   </para>
   <variablelist>
    <varlistentry>
     <term><systemitem>acpi=off</systemitem>
     </term>
     <listitem>
      <para>
       This parameter disables the complete ACPI subsystem on your computer.
       This may be useful if your computer cannot handle ACPI at all or if
       you think ACPI in your computer causes trouble.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><systemitem>acpi=force</systemitem>
     </term>
     <listitem>
      <para>
       Always enable ACPI even if your computer has an old BIOS dated before
       the year 2000. This parameter also enables ACPI if it is set in
       addition to <systemitem>acpi=off</systemitem>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><systemitem>acpi=noirq</systemitem>
     </term>
     <listitem>
      <para>
       Do not use ACPI for IRQ routing.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><systemitem>acpi=ht</systemitem>
     </term>
     <listitem>
      <para>
       Run only enough ACPI to enable hyper-threading.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><systemitem>acpi=strict</systemitem>
     </term>
     <listitem>
      <para>
       Be less tolerant of platforms that are not strictly ACPI
       specification compliant.
      </para>
     </listitem>
    </varlistentry>
<?dbfo-need height="20em"?>
    <varlistentry>
     <term><systemitem>pci=noacpi</systemitem>
     </term>
     <listitem>
      <para>
       Disable PCI IRQ routing of the new ACPI system.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><systemitem>pnpacpi=off</systemitem>
     </term>
     <listitem>
      <para>
       This option is for serial or parallel problems when your BIOS setup
       contains wrong interrupts or ports.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><systemitem>notsc</systemitem>
     </term>
     <listitem>
      <para>
       Disable the time stamp counter. This option can be used to work
       around timing problems on your systems. It is a recent feature, if
       you see regressions on your machine, especially time related or even
       total hangs, this option is worth a try.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><systemitem>nohz=off</systemitem>
     </term>
     <listitem>
      <para>
       Disable the nohz feature. If your machine hangs, this option may
       help. Otherwise it is of no use.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
   <para>
    Once you have determined the right parameter combination, &yast;
    automatically writes them to the boot loader configuration to make sure
    that the system boots properly next time.
   </para>
   <para>
    If unexplainable errors occur when the kernel is loaded or during the
    installation, select <guimenu>Memory Test</guimenu> in the boot menu to
    check the memory. If <guimenu>Memory Test</guimenu> returns an error, it
    is usually a hardware error.
   </para>
  </sect2>

  <sect2 id="sec.trouble.install.graph">
   <title>Fails to Launch Graphical Installer</title>
   <para>
    After you insert the medium into your drive and reboot your machine, the
    installation screen comes up, but after you select
    <guimenu>Installation</guimenu>, the graphical installer does not start.
   </para>
   <para>
    There are several ways to deal with this situation:
   </para>
   <itemizedlist>
    <listitem>
     <para>
      Try to select another screen resolution for the installation dialogs.
     </para>
    </listitem>
    <listitem>
     <para>
      Select <guimenu>Text Mode</guimenu> for installation.
     </para>
    </listitem>
    <listitem>
     <para>
      Do a remote installation via VNC using the graphical installer.
     </para>
    </listitem>
   </itemizedlist>
   <procedure id="proc.trouble.install.graph">
    <title>Change Screen Resolution for Installation</title>
    <step>
     <para>
      Boot for installation.
     </para>
    </step>
    <step>
     <para>
      Press <keycap>F3</keycap> to open a menu from which to select a lower
      resolution for installation purposes.
     </para>
    </step>
    <step>
     <para>
      Select <guimenu>Installation</guimenu> and proceed with the
      installation as described in <xref linkend="cha.inst"/>.
     </para>
    </step>
   </procedure>
   <procedure id="proc.trouble.install.text">
    <title>Installation in Text Mode</title>
    <step>
     <para>
      Boot for installation.
     </para>
    </step>
    <step>
     <para>
      Press <keycap>F3</keycap> and select <guimenu>Text Mode</guimenu>.
     </para>
    </step>
    <step>
     <para>
      Select <guimenu>Installation</guimenu> and proceed with the
      installation as described in <xref linkend="cha.inst"/>.
     </para>
    </step>
   </procedure>
   <procedure id="proc.trouble.install.vnc">
    <title>VNC Installation</title>
    <step>
     <para>
      Boot for installation.
     </para>
    </step>
    <step>
     <para>
      Enter the following text at the boot options prompt:
     </para>
<screen>vnc=1 vncpassword=<replaceable>some_password</replaceable></screen>
     <para>
      Replace <replaceable>some_password</replaceable> with the password to
      use for VNC installation.
     </para>
    </step>
    <step>
     <para>
      Select <guimenu>Installation</guimenu> then press
      <keycap function="enter" >Enter</keycap> to start the installation .
     </para>
     <para>
      Instead of starting right into the graphical installation routine, the
      system continues to run in a text mode, then halts, displaying a
      message containing the IP address and port number at which the
      installer can be reached via a browser interface or a VNC viewer
      application.
     </para>
    </step>
    <step>
     <para>
      If using a browser to access the installer, launch the browser and
      enter the address information provided by the installation routines on
      the future &productname; machine and hit
      <keycap function="enter">Enter</keycap>:
     </para>
<screen>http://<replaceable>ip_address_of_machine</replaceable>:5801</screen>
     <para>
      A dialog opens in the browser window prompting you for the VNC
      password. Enter it and proceed with the installation as described in
      <xref linkend="cha.inst"/>.
     </para>
     <important>
      <para>
       Installation via VNC works with any browser under any operating
       system, provided Java support is enabled.
      </para>
     </important>
     <para>
      Provide the IP address and password to your VNC viewer when prompted.
      A window opens, displaying the installation dialogs. Proceed with the
      installation as usual.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.trouble.install.mini">
   <title>Only Minimalistic Boot Screen Started</title>
   <para>
    You inserted the medium into the drive, the BIOS routines are finished,
    but the system does not start with the graphical boot screen. Instead it
    launches a very minimalistic text-based interface. This may happen on
    any machine not providing sufficient graphics memory for rendering a
    graphical boot screen.
   </para>
   <para>
    Although the text boot screen looks minimalistic, it provides nearly the
    same functionality as the graphical one:
   </para>
   <variablelist>
    <varlistentry>
     <term>Boot Options</term>
     <listitem>
      <para>
       Unlike the graphical interface, the different boot options cannot be
       selected using the cursor keys of your keyboard. The boot menu of the
       text mode boot screen offers some keywords to enter at the boot
       prompt. These keywords map to the options offered in the graphical
       version. Enter your choice and hit
       <keycap function="enter">Enter</keycap> to launch the boot process.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Custom Boot Options</term>
     <listitem>
      <para>
       After selecting a boot option, enter the appropriate keyword at the
       boot prompt or enter some custom boot options as described in
       <xref linkend="sec.trouble.install.noboot"/>. To launch the
       installation process, press <keycap function="enter">Enter</keycap>.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Screen Resolutions</term>
     <listitem>
      <para>
       Use the F keys to determine the screen resolution for installation.
       If you need to boot in text mode, choose <keycap>F3</keycap>.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>
 </sect1>
 <sect1 id="sec.trouble.boot">
  <title>Boot Problems</title>

  <para>
   Boot problems are situations when your system does not boot properly
   (does not boot to the expected runlevel and login screen).
  </para>

  <sect2 id="sec.trouble.boot.nogrub">
   <title>Fails to Load the GRUB Boot Loader</title>
   <para>
    If the hardware is functioning properly, it is possible that the boot
    loader is corrupted and Linux cannot start on the machine. In this case,
    it is necessary to repair the boot loader. To do so, you need start the
    Rescue System as described in <xref linkend="sec.trouble.data.recover"/>
    and follow the instructions at
    <xref
     linkend="sec.trouble.data.recover.rescue.grub"/>.
   </para>
   <para>
    Other reasons for the machine not booting may be BIOS-related:
   </para>
   <variablelist>
    <varlistentry>
     <term>BIOS Settings</term>
     <listitem>
      <para>
       Check your BIOS for references to your hard drive. GRUB may simply
       not be started if the hard drive itself cannot be found with the
       current BIOS settings.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>BIOS Boot Order</term>
     <listitem>
      <para>
       Check whether your system's boot order includes the hard disk. If the
       hard disk option was not enabled, your system may install properly,
       but fails to boot when access to the hard disk is required.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect2>

  <sect2 id="sec.trouble.boot.hang" os="sled">
   <title>No Login or Prompt Appears</title>
   <para>
    This behavior typically occurs after a failed kernel upgrade and it is
    known as a <emphasis>kernel panic</emphasis> because of the type of
    error on the system console that sometimes can be seen at the final
    stage of the process. If, in fact, the machine has just been rebooted
    following a software update, the immediate goal is to reboot it using
    the old, proven version of the Linux kernel and associated files. This
    can be done in the GRUB boot loader screen during the boot process as
    follows:
   </para>
   <procedure>
    <step>
     <para>
      Reboot the computer using the reset button, or switch it off and on
      again.
     </para>
    </step>
    <step>
     <para>
      When the GRUB boot screen becomes visible, select
      <guimenu>Linux--Failsafe</guimenu> then press
      <keycap function="enter">Enter</keycap>. The machine will boot using
      the prior version of the kernel and its associated files.
     </para>
    </step>
    <step id="st.trouble.boot.hang.modify_grub">
     <para>
      After the boot process has completed, remove the newly installed
      kernel and, if necessary, manually modify
      <filename>/boot/grub/menu.lst</filename> to make the older kernel as
      the default option. For some detailed information about the syntax
      used in this configuration file, refer to <xref linkend="cha.grub"/>.
     </para>
     <para>
      Updating this file is probably not necessary because automated update
      tools normally modify it for you during the rollback process.
     </para>
    </step>
    <step>
     <para>
      Reboot.
     </para>
    </step>
   </procedure>
   <para>
    If this does not fix the problem because the
    <guimenu>Linux--Failsafe</guimenu> option does not boot the computer
    properly, boot the computer using the installation media. After the
    machine has booted, continue with
    <xref linkend="st.trouble.boot.hang.modify_grub" xrefstyle="StepOnPage"/>.
   </para>
  </sect2>

  <sect2 id="sec.trouble.boot.prompt">
   <title>No Graphical Login</title>
   <para>
    If the machine comes up, but does not boot into the graphical login
    manager, anticipate problems either with the choice of the default
    runlevel or the configuration of the X Window System. To check the
    runlevel configuration, log in as the
    <systemitem class="username" >root</systemitem> user and check whether
    the machine is configured to boot into runlevel 5 (graphical desktop). A
    quick way to check this is to examine the contents of
    <filename>/etc/inittab</filename>, as follows:
   </para>
<screen>&exampleuser;@&wsIV;:~&gt; grep "id:" /etc/inittab 
id:5:initdefault:</screen>
   <para>
    The returned line indicates that the machine's default runlevel
    (<literal>initdefault</literal>) is set to <literal>5</literal> and that
    it should boot to the graphical desktop. If the runlevel is set to any
    other number, use the &yast; Runlevel Editor module to set it to
    <literal>5</literal>.
   </para>
   <important>
    <para>
     Do not edit the runlevel configuration manually. Otherwise SuSEconfig
     (run by &yast;) will overwrite these changes on its next run. If you
     need to make manual changes here, disable future SuSEconfig changes by
     setting <replaceable>CHECK_INITTAB</replaceable> in
     <filename>/etc/sysconfig/suseconfig</filename> to <option>no</option>.
    </para>
   </important>
   <para>
    If the runlevel is set to <literal>5</literal>, your desktop or X
    Windows software is probably misconfigured or corrupted. Examine the log
    files at <filename>/var/log/Xorg.*.log</filename> for detailed messages
    from the X server as it attempted to start. If the desktop fails during
    start, it may log error messages to
    <filename>/var/log/messages</filename>. If these error messages hint at
    a configuration problem in the X server, try to fix these issues. If the
    graphical system still does not come up, consider reinstalling the
    graphical desktop.
   </para>
   <tip>
    <title>Starting X Window System Manually</title>
    <para>
     One quick test: the <command>startx</command> command should force the
     X Window System to start with the configured defaults if the user is
     currently logged in on the console. If that does not work, it should
     log errors to the console.
    </para>
   </tip>
  </sect2>
 </sect1>
 <sect1 id="sec.trouble.login">
  <title>Login Problems</title>

  <para>
   Login problems are those where your machine does, in fact, boot to the
   expected welcome screen or login prompt, but refuses to accept the
   username and password, or accepts them but then does not behave properly
   (fails to start the graphic desktop, produces errors, drops to a command
   line, etc.).
  </para>

  <sect2 id="sec.trouble.login.no">
   <title>Valid Username and Password Combinations Fail</title>
   <para>
    This usually occurs when the system is configured to use network
    authentication or directory services and, for some reason, is unable to
    retrieve results from its configured servers. The
    <systemitem class="username">root</systemitem> user, as the only local
    user, is the only user that can still log in to these machines. The
    following are some common reasons why a machine appears functional but
    is unable to process logins correctly:
   </para>
   <itemizedlist>
    <listitem>
     <para>
      The network is not working. For further directions on this, turn to
      <xref linkend="sec.trouble.netconfig"/>.
     </para>
    </listitem>
    <listitem>
     <para>
      DNS is not working at the moment (which prevents GNOME or KDE from
      working and the system from making validated requests to secure
      servers). One indication that this is the case is that the machine
      takes an extremely long time to respond to any action. Find more
      information about this topic in
      <xref linkend="sec.trouble.netconfig"/>.
     </para>
    </listitem>
    <listitem>
     <para>
      If the system is configured to use Kerberos, the system's local time
      may have drifted past the accepted variance with the Kerberos server
      time (this is typically 300 seconds). If NTP (network time protocol)
      is not working properly or local NTP servers are not working, Kerberos
      authentication ceases to function because it depends on common clock
      synchronization across the network.
     </para>
    </listitem>
    <listitem>
     <para>
      The system's authentication configuration is misconfigured. Check the
      PAM configuration files involved for any typographical errors or
      misordering of directives. For additional background information about
      PAM and the syntax of the configuration files involved, refer to
      <xref linkend="cha.pam"/>.
     </para>
    </listitem>
    <listitem>
     <para>
      The home partition is encrypted. Find more information about this
      topic in <xref linkend="sec.trouble.login.encrypted"/>.
     </para>
    </listitem>
   </itemizedlist>
   <para>
    In all cases that do not involve external network problems, the solution
    is to reboot the system into single-user mode and repair the
    configuration before booting again into operating mode and attempting to
    log in again. To boot into single-user mode:
   </para>
   <procedure id="proc.trouble.login.no">
    <step>
     <para>
      Reboot the system. The boot screen appears, offering a prompt.
     </para>
    </step>
    <step>
     <para>
      Enter <command>1</command> at the boot prompt to make the system boot
      into single-user mode.
     </para>
    </step>
    <step>
     <para>
      Enter the username and password for
      <systemitem class="username">root</systemitem>.
     </para>
    </step>
    <step>
     <para>
      Make all the necessary changes.
     </para>
    </step>
    <step>
     <para>
      Boot into the full multiuser and network mode by entering
      <command>telinit 5</command> at the command line.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.trouble.login.pw">
   <title>Valid Username and Password Not Accepted</title>
   <para>
    This is by far the most common problem users encounter, because there
    are many reasons this can occur. Depending on whether you use local user
    management and authentication or network authentication, login failures
    occur for different reasons.
   </para>
   <para>
    Local user management can fail for the following reasons:
   </para>
   <itemizedlist>
    <listitem>
     <para>
      The user may have entered the wrong password.
     </para>
    </listitem>
    <listitem>
     <para>
      The user's home directory containing the desktop configuration files
      is corrupted or write protected.
     </para>
    </listitem>
    <listitem>
     <para>
      There may be problems with the X Window System authenticating this
      particular user, especially if the user's home directory has been used
      with another Linux distribution prior to installing the current one.
     </para>
    </listitem>
   </itemizedlist>
   <para>
    To locate the reason for a local login failure, proceed as follows:
   </para>
   <procedure>
    <step>
     <para>
      Check whether the user remembered his password correctly before you
      start debugging the whole authentication mechanism. If the user may
      not remember his password correctly, use the &yast; User Management
      module to change the user's password. Pay attention to the
      <keycap>Caps Lock</keycap> key and unlock it, if necessary.
     </para>
    </step>
    <step>
     <para>
      Log in as <systemitem class="username">root</systemitem> and check
      <filename>/var/log/messages</filename> for error messages of the login
      process and of PAM.
     </para>
    </step>
    <step>
     <para>
      Try to log in from a console (using <keycombo>
      <keycap function="control">Ctrl</keycap>
      <keycap function="alt">Alt</keycap> <keycap>F1</keycap> </keycombo>).
      If this is successful, the blame cannot be put on PAM, because it is
      possible to authenticate this user on this machine. Try to locate any
      problems with the X Window System or the desktop (GNOME or KDE). For
      more information, refer to <xref linkend="sec.trouble.login.dk"/> and
      <xref linkend="sec.trouble.login.kde"/>.
     </para>
    </step>
    <step>
     <para>
      If the user's home directory has been used with another Linux
      distribution, remove the <filename>Xauthority</filename> file in the
      user's home. Use a console login via <keycombo>
      <keycap function="control">Ctrl</keycap>
      <keycap function="alt">Alt</keycap> <keycap>F1</keycap> </keycombo>
      and run <command>rm .Xauthority</command> as this user. This should
      eliminate X authentication problems for this user. Try graphical login
      again.
     </para>
    </step>
    <step>
     <para>
      If graphical login still fails, do a console login with <keycombo>
      <keycap function="control">Ctrl</keycap>
      <keycap function="alt">Alt</keycap> <keycap>F1</keycap> </keycombo>.
      Try to start an X session on another display&mdash;the first one
      (<literal>:0</literal>) is already in use:
     </para>
<screen>startx -- :1</screen>
     <para>
      This should bring up a graphical screen and your desktop. If it does
      not, check the log files of the X Window System
      (<filename>/var/log/Xorg.<replaceable>displaynumber</replaceable>.log</filename>)
      or the log file for your desktop applications
      (<filename>.xsession-errors</filename> in the user's home directory)
      for any irregularities.
     </para>
    </step>
    <step>
     <para>
      If the desktop could not start because of corrupt configuration files,
      proceed with <xref linkend="sec.trouble.login.dk"/> or
      <xref linkend="sec.trouble.login.kde"/>.
     </para>
    </step>
   </procedure>
   <para>
    The following are some common reasons why network authentication for a
    particular user may fail on a specific machine:
   </para>
   <itemizedlist>
    <listitem>
     <para>
      The user may have entered the wrong password.
     </para>
    </listitem>
    <listitem>
     <para>
      The username exists in the machine's local authentication files and is
      also provided by a network authentication system, causing conflicts.
     </para>
    </listitem>
    <listitem>
     <para>
      The home directory exists but is corrupt or unavailable. Perhaps it is
      write protected or is on a server that is inaccessible at the moment.
     </para>
    </listitem>
    <listitem>
     <para>
      The user does not have permission to log in to that particular host in
      the authentication system.
     </para>
    </listitem>
    <listitem>
     <para>
      The machine has changed hostnames, for whatever reason, and the user
      does not have permission to log in to that host.
     </para>
    </listitem>
    <listitem>
     <para>
      The machine cannot reach the authentication server or directory server
      that contains that user's information.
     </para>
    </listitem>
    <listitem>
     <para>
      There may be problems with the X Window System authenticating this
      particular user, especially if the user's home has been used with
      another Linux distribution prior to installing the current one.
     </para>
    </listitem>
   </itemizedlist>
   <para>
    To locate the cause of the login failures with network authentication,
    proceed as follows:
   </para>
   <procedure id="proc.trouble.login.pw">
    <step>
     <para>
      Check whether the user remembered their password correctly before you
      start debugging the whole authentication mechanism.
     </para>
    </step>
    <step>
     <para>
      Determine the directory server which the machine relies on for
      authentication and make sure that it is up and running and properly
      communicating with the other machines.
     </para>
    </step>
    <step>
     <para>
      Determine that the user's username and password work on other machines
      to make sure that his authentication data exists and is properly
      distributed.
     </para>
    </step>
    <step>
     <para>
      See if another user can log in to the misbehaving machine. If another
      user can log in without difficulty or if
      <systemitem class="username">root</systemitem> can log in, log in and
      examine the <filename>/var/log/messages</filename> file. Locate the
      time stamps that correspond to the login attempts and determine if PAM
      has produced any error messages.
     </para>
    </step>
    <step>
     <para>
      Try to log in from a console (using <keycombo>
      <keycap function="control">Ctrl</keycap>
      <keycap function="alt">Alt</keycap> <keycap>F1</keycap> </keycombo>).
      If this is successful, the problem is not with PAM or the directory
      server on which the user's home is hosted, because it is possible to
      authenticate this user on this machine. Try to locate any problems
      with the X Window System or the desktop (GNOME or KDE). For more
      information, refer to <xref linkend="sec.trouble.login.dk"/> and
      <xref linkend="sec.trouble.login.kde"/>.
     </para>
    </step>
    <step>
     <para>
      If the user's home directory has been used with another Linux
      distribution, remove the <filename>Xauthority</filename> file in the
      user's home. Use a console login via <keycombo>
      <keycap function="control">Ctrl</keycap>
      <keycap function="alt">Alt</keycap> <keycap>F1</keycap> </keycombo>
      and run <command>rm .Xauthority</command> as this user. This should
      eliminate X authentication problems for this user. Try graphical login
      again.
     </para>
    </step>
    <step>
     <para>
      If graphical login still fails, do a console login with <keycombo>
      <keycap function="control">Ctrl</keycap>
      <keycap function="alt">Alt</keycap> <keycap>F1</keycap> </keycombo>.
      Try to start an X session on another display&mdash;the first one
      (<literal>:0</literal>) is already in use:
     </para>
<screen>startx -- :1</screen>
     <para>
      This should bring up a graphical screen and your desktop. If it does
      not, check the log files of the X Window System
      (<filename>/var/log/Xorg.<replaceable>displaynumber</replaceable>.log</filename>)
      or the log file for your desktop applications
      (<filename>.xsession-errors</filename> in the user's home directory)
      for any irregularities.
     </para>
    </step>
    <step>
     <para>
      If the desktop could not start because of corrupt configuration files,
      proceed with <xref linkend="sec.trouble.login.dk"/> or
      <xref linkend="sec.trouble.login.kde"/>.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.trouble.login.encrypted">
   <title>Login to Encrypted Home Partition Fails</title><indexterm>
   <primary>encrypting</primary>
   <secondary>home partition problem</secondary></indexterm>
   <para>
    It is recommended to use an encrypted home partition for laptops. If you
    cannot log in to your laptop, the reason is usually simple: your
    partition could not be unlocked.
   </para>
   <para>
    During the boot time, you have to enter the passphrase to unlock your
    encrypted partition. If you do not enter it, the boot process continues,
    leaving the partition locked.
   </para>
   <para>
    To unlock your encrypted partition, proceed as follows:
   </para>
   <procedure>
    <step>
     <para>
      Switch to the text console with <keycombo>
      <keycap function="control"/> <keycap function="alt"/>
      <keycap>F1</keycap> </keycombo>.
     </para>
    </step>
    <step>
     <para>
      Become <systemitem class="username">root</systemitem>.
     </para>
    </step>
    <step>
     <para>
      Restart the unlocking process again with:
     </para>
<screen>/etc/init.d/boot.crypto restart</screen>
    </step>
    <step>
     <para>
      Enter your passphrase to unlock your encrypted partition.
     </para>
    </step>
    <step>
     <para>
      Exit the text console and switch back to the login screen with
      <keycombo> <keycap function="alt"/> <keycap>F7</keycap> </keycombo>.
     </para>
    </step>
    <step>
     <para>
      Log in as usual.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.trouble.login.dk">
   <title>Login Successful but GNOME Desktop Fails</title>
   <para>
    If this is the case, it is likely that your GNOME configuration files
    have become corrupted. Some symptoms may include the keyboard failing to
    work, the screen geometry becoming distorted, or even the screen coming
    up as a bare gray field. The important distinction is that if another
    user logs in, the machine works normally. It is then likely that the
    problem can be fixed relatively quickly by simply moving the user's
    GNOME configuration directory to a new location, which causes GNOME to
    initialize a new one. Although the user is forced to reconfigure GNOME,
    no data is lost.
   </para>
   <procedure id="proc.trouble.login.dk">
    <step>
     <para>
      Switch to a text console by pressing <keycombo>
      <keycap function="control"/> <keycap function="alt"/>
      <keycap>F1</keycap> </keycombo>.
     </para>
    </step>
    <step>
     <para>
      Log in with your user name.
     </para>
    </step>
    <step>
     <para>
      Move the user's GNOME configuration directories to a temporary
      location:
     </para>
<screen>mv .gconf  .gconf-ORIG-RECOVER
mv .gnome2 .gnome2-ORIG-RECOVER</screen>
    </step>
    <step>
     <para>
      Log out.
     </para>
    </step>
    <step>
     <para>
      Log in again, but do not run any applications.
     </para>
    </step>
    <step>
     <para>
      Recover your individual application configuration data (including the
      Evolution e-mail client data) by copying the
      <filename>~/.gconf-ORIG-RECOVER/apps/</filename> directory back into
      the new <filename>~/.gconf </filename> directory as follows:
     </para>
<screen>cp -a .gconf-ORIG-RECOVER/apps .gconf/</screen>
     <para>
      If this causes the login problems, attempt to recover only the
      critical application data and reconfigure the remainder of the
      applications.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.trouble.login.kde">
   <title>Login Successful but KDE Desktop Fails</title>
   <para>
    There are several reasons why a KDE desktop would not allow users to
    login. Corrupted cache data can cause login problems as well as corrupt
    KDE desktop configuration files.
   </para>
   <para>
    Cache data is used at desktop start-up to increase performance. If this
    data is corrupted, start-up is slowed down or fails entirely. Removing
    them forces the desktop start-up routines to start from scratch. This
    takes more time than a normal start-up, but data is intact after this
    and the user can login.
   </para>
   <para>
    To remove the cache files of the KDE desktop, issue the following
    command as <systemitem class="username">root</systemitem>:
   </para>
<screen>rm -rf /tmp/kde-<replaceable>user</replaceable> /tmp/ksocket-<replaceable>user</replaceable></screen>
   <para>
    Replace <replaceable>user</replaceable> with your username. Removing
    these two directories just removes the corrupted cache files. No real
    data is harmed using this procedure.
   </para>
   <para>
    Corrupted desktop configuration files can always be replaced with the
    initial configuration files. If you want to recover the user's
    adjustments, carefully copy them back from their temporary location
    after the configuration has been restored, using the default
    configuration values.
   </para>
   <para>
    To replace a corrupted desktop configuration with the initial
    configuration values, proceed as follows:
   </para>
   <procedure id="proc.trouble.login.kde">
    <step>
     <para>
      Switch to a text console by pressing <keycombo>
      <keycap function="control"/> <keycap function="alt"/>
      <keycap>F1</keycap> </keycombo>.
     </para>
    </step>
    <step>
     <para>
      Log in with your username.
     </para>
    </step>
    <step id="st.trouble.login.kde.mv">
     <para>
      Move the KDE configuration directory and the
      <filename>.skel</filename> files to a temporary location:
     </para>
     <itemizedlist>
      <listitem>
       <para>
        For KDE3 use these commands:
       </para>
<screen>mv .kde  .kde-ORIG-RECOVER 
mv .skel .skel-ORIG-RECOVER<replaceable/></screen>
      </listitem>
      <listitem>
       <para>
        For KDE4 use these commands:
       </para>
<screen>mv .kde4  .kde4-ORIG-RECOVER 
mv .skel .skel-ORIG-RECOVER<replaceable/></screen>
      </listitem>
     </itemizedlist>
    </step>
    <step>
     <para>
      Log out.
     </para>
    </step>
    <step>
     <para>
      Log in again.
     </para>
    </step>
    <step>
     <para>
      After the desktop has started successfully, copy the user's own
      configurations back into place:
     </para>
<screen>cp -a <replaceable>KDEDIR</replaceable>/share .kde/share</screen>
     <para>
      Replace <replaceable>KDEDIR</replaceable> with the directory from
      <xref linkend="st.trouble.login.kde.mv"/>.
     </para>
     <important>
      <para>
       If the user's own adjustments caused the login to fail and continue
       to do so, repeat the procedure as described above, but do not copy
       the <filename>.kde/share</filename> directory.
      </para>
     </important>
    </step>
   </procedure>
  </sect2>
 </sect1>
 <sect1 id="sec.trouble.netconfig">
  <title>Network Problems</title>

  <para>
   Many problems of your system may be network-related, even though they do
   not seem to be at first. For example, the reason for a system not
   allowing users to log in may be a network problem of some kind. This
   section introduces a simple check list you can apply to identify the
   cause of any network problem encountered.
  </para>

  <procedure id="proc.trouble.config.net">
   <title>How to Identify Network Problems</title>
   <para>
    When checking the network connection of your machine, proceed as
    follows:
   </para>
   <step>
    <para>
     If you use an ethernet connection, check the hardware first. Make sure
     that your network cable is properly plugged into your computer and
     router (or hub, etc.). The control lights next to your ethernet
     connector are normally both be active.
    </para>
    <para>
     If the connection fails, check whether your network cable works with
     another machine. If it does, your network card causes the failure. If
     hubs or switches are included in your network setup, they may be
     faulty, as well.
    </para>
   </step>
   <step>
    <para>
     If using a wireless connection, check whether the wireless link can be
     established by other machines. If not, contact the wireless network's
     administrator.
    </para>
   </step>
   <step>
    <para>
     Once you have checked your basic network connectivity, try to find out
     which service is not responding. Gather the address information of all
     network servers needed in your setup. Either look them up in the
     appropriate &yast; module or ask your system administrator. The
     following list gives some of the typical network servers involved in a
     setup together with the symptoms of an outage.
    </para>
    <variablelist>
     <varlistentry>
      <term>DNS (Name Service)</term>
      <listitem>
       <para>
        A broken or malfunctioning name service affects the network's
        functionality in many ways. If the local machine relies on any
        network servers for authentication and these servers cannot be found
        due to name resolution issues, users would not even be able to log
        in. Machines in the network managed by a broken name server would
        not be able to <quote>see</quote> each other and communicate.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>NTP (Time Service)</term>
      <listitem>
       <para>
        A malfunctioning or completely broken NTP service could affect
        Kerberos authentication and X server functionality.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>NFS (File Service)</term>
      <listitem>
       <para>
        If any application needs data stored in an NFS mounted directory, it
        will not be able to start or function properly if this service was
        down or misconfigured. In the worst case scenario, a user's personal
        desktop configuration would not come up if their home directory
        containing the <filename>.gconf</filename> or
        <filename>.kde</filename> subdirectories could not be found due to a
        faulty NFS server.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>Samba (File Service)</term>
      <listitem>
       <para>
        If any application needs data stored in a directory on a faulty
        Samba server, it will not be able to start or function properly.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>NIS (User Management)</term>
      <listitem>
       <para>
        If your &productname; system relies on a faulty NIS server to
        provide the user data, users will not be able to log in to this
        machine.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>LDAP (User Management)</term>
      <listitem>
       <para>
        If your &productname; system relies on a faulty LDAP server to
        provide the user data, users will not be able to log in to this
        machine.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>Kerberos (Authentication)</term>
      <listitem>
       <para>
        Authentication will not work and login to any machine fails.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>CUPS (Network Printing)</term>
      <listitem>
       <para>
        Users cannot print.
       </para>
      </listitem>
     </varlistentry>
    </variablelist>
   </step>
   <step>
    <para>
     Check whether the network servers are running and whether your network
     setup allows you to establish a connection:
    </para>
    <important>
     <para>
      The debugging procedure described below only applies to a simple
      network server/client setup that does not involve any internal
      routing. It assumes both server and client are members of the same
      subnet without the need for additional routing.
     </para>
    </important>
    <substeps>
     <step>
      <para>
       Use <command>ping <replaceable>IP address</replaceable> or
       <replaceable>hostname</replaceable></command> (replace
       <replaceable>hostname</replaceable> with the hostname of the server)
       to check whether each one of them is up and responding to the
       network. If this command is successful, it tells you that the host
       you were looking for is up and running and that the name service for
       your network is configured correctly.
      </para>
      <para>
       If ping fails with <literal>destination host unreachable</literal>,
       either your system or the desired server is not properly configured
       or down. Check whether your system is reachable by running
       <command>ping <replaceable>IP address</replaceable> or
       <replaceable>your_hostname</replaceable> </command> from another
       machine. If you can reach your machine from another machine, it is
       the server that is not running at all or not configured correctly.
      </para>
      <para>
       If ping fails with <literal>unknown host</literal>, the name service
       is not configured correctly or the hostname used was incorrect. For
       further checks on this matter, refer to
       <xref linkend="st.trouble.config.net.host" xrefstyle="StepOnPage"/>.
       If ping still fails, either your network card is not configured
       correctly or your network hardware is faulty.
      </para>
     </step>
     <step id="st.trouble.config.net.host">
      <para>
       Use <command>host <replaceable>hostname</replaceable></command> to
       check whether the hostname of the server you are trying to connect to
       is properly translated into an IP address and vice versa. If this
       command returns the IP address of this host, the name service is up
       and running. If the <command>host</command> command fails, check all
       network configuration files relating to name and address resolution
       on your host:
      </para>
<?dbfo-need height="20em"?>
      <variablelist>
       <varlistentry>
        <term><filename>/etc/resolv.conf</filename>
        </term>
        <listitem>
         <para>
          This file is used to keep track of the name server and domain you
          are currently using. It can be modified manually or automatically
          adjusted by &yast; or DHCP. Automatic adjustment is preferable.
          However, make sure that this file has the following structure and
          all network addresses and domain names are correct:
         </para>
<screen>search <replaceable>fully_qualified_domain_name</replaceable>
nameserver <replaceable>ipaddress_of_nameserver</replaceable></screen>
         <para>
          This file can contain more than one name server address, but at
          least one of them must be correct to provide name resolution to
          your host. If needed, adjust this file using the &yast; Network
          Setting module (Hostname/DNS tab).
         </para>
         <para>
          If your network connection is handled via DHCP, enable DHCP to
          change hostname and name service information by selecting
          <guimenu>Change Hostname via DHCP</guimenu> and <guimenu>Update
          Name Servers and Search List via DHCP</guimenu> in the &yast; DNS
          and Hostname module.
         </para>
        </listitem>
       </varlistentry>
       <varlistentry>
        <term><filename>/etc/nsswitch.conf</filename>
        </term>
        <listitem>
         <para>
          This file tells Linux where to look for name service information.
          It should look like this:
         </para>
<screen> ...
hosts: files dns
networks: files dns
...</screen>
         <para>
          The <option>dns</option> entry is vital. It tells Linux to use an
          external name server. Normally, these entries are automatically
          managed by &yast;, but it would be prudent to check.
         </para>
         <para>
          If all the relevant entries on the host are correct, let your
          system administrator check the DNS server configuration for the
          correct zone information. <phrase
           os="sles;osuse">For
          detailed information about DNS, refer to
          <xref linkend="cha.dns"
           />.</phrase> If you have made
          sure that the DNS configuration of your host and the DNS server
          are correct, proceed with checking the configuration of your
          network and network device.
         </para>
        </listitem>
       </varlistentry>
      </variablelist>
     </step>
     <step>
      <para>
       If your system cannot establish a connection to a network server and
       you have excluded name service problems from the list of possible
       culprits, check the configuration of your network card.
      </para>
      <para>
       Use the command <command>ifconfig
       <replaceable>network_device</replaceable></command> (executed as
       <systemitem class="username">root</systemitem>) to check whether this
       device was properly configured. Make sure that both <option>inet
       address</option> and <option>Mask</option> are configured correctly.
       An error in the IP address or a missing bit in your network mask
       would render your network configuration unusable. If necessary,
       perform this check on the server as well.
      </para>
     </step>
     <step>
      <para>
       If the name service and network hardware are properly configured and
       running, but some external network connections still get long
       time-outs or fail entirely, use <command>traceroute
       <replaceable>fully_qualified_domain_name</replaceable></command>
       (executed as <systemitem class="username">root</systemitem>) to track
       the network route these requests are taking. This command lists any
       gateway (hop) that a request from your machine passes on its way to
       its destination. It lists the response time of each hop and whether
       this hop is reachable at all. Use a combination of traceroute and
       ping to track down the culprit and let the administrators know.
      </para>
     </step>
    </substeps>
   </step>
  </procedure>

  <para>
   Once you have identified the cause of your network trouble, you can
   resolve it yourself (if the problem is located on your machine) or let
   the system administrators of your network know about your findings so
   they can reconfigure the services or repair the necessary systems.
  </para>

  <sect2 id="sec.trouble.networkmanager">
   <title>&nm; Problems</title>
   <remark>This is a bit small, needs more text.</remark>
   <para>
    If you have a problem with network connectivity, narrow it down as
    described in <xref
     linkend="proc.trouble.config.net"/>. If &nm;
    seems to be the culprit, proceed as follows to get logs providing hints
    on why &nm; fails:
   </para>
   <procedure>
    <step>
     <para>
      Open a shell and log in as
      <systemitem class="username">root</systemitem>.
     </para>
    </step>
    <step>
     <para>
      Restart the NetworkManager:
     </para>
<screen>rcnetwork restart -o nm</screen>
    </step>
    <step>
     <para>
      Open a Web page, for example, <ulink url="http://www.opensuse.org"/>
      as normal user to see, if you can connect.
     </para>
    </step>
    <step>
     <para>
      Collect any information about the state of NetworkManager in
      <filename>/var/log/NetworkManager</filename>.
     </para>
    </step>
   </procedure>
   <para>
    For more information about NetworkManager, refer to
    <xref linkend="cha.nm"/>.
   </para>
  </sect2>
 </sect1>
 <sect1 id="sec.trouble.data">
  <title>Data Problems</title>

  <para>
   Data problems are when the machine may or may not boot properly but, in
   either case, it is clear that there is data corruption on the system and
   that the system needs to be recovered. These situations call for a backup
   of your critical data, enabling you to recover the system state from
   before your system failed. &productname; offers dedicated &yast; modules
   for system backup and restoration as well as a rescue system that can be
   used to recover a corrupted system from the outside.
  </para>

  <sect2 id="sec.trouble.data.partitions">
   <title>Managing Partition Images</title>
   <para>
    Sometimes you need to perform a backup from an entire partition or even
    hard disk. Linux comes with the <command>dd</command> tool which can
    create a exact copy of your disc. Combined with <command>gzip</command>
    you save some space.
   </para>
   <procedure>
    <title>Backing up and Restoring Harddisks</title>
    <step>
     <para>
      Start a Shell as user &rootuser;.
     </para>
    </step>
    <step>
     <para>
      Select your source device. Typically this is something like
      <filename>/dev/sda</filename> (labeled as
      <replaceable>SOURCE</replaceable>).
     </para>
    </step>
    <step>
     <para>
      Decide where you want to store your image (labeled as
      <replaceable>BACKUP_PATH</replaceable>). It must be different from
      your source device. In other words: if you make a backup from
      <filename>/dev/sda</filename>, your image file must not to be stored
      under <filename>/dev/sda</filename>.
     </para>
    </step>
    <step>
     <para>
      Run the commands to create a compressed image file:
     </para>
<screen>dd if=/dev/<replaceable>SOURCE</replaceable> | gzip > /<replaceable>BACKUP_PATH</replaceable>/image.gz </screen>
    </step>
    <step>
     <para>
      Restore the hard disk with the following commands:
     </para>
<screen>gzip -dc /<replaceable>BACKUP_PATH</replaceable>/image.gz | dd of=/dev/<replaceable>SOURCE</replaceable></screen>
    </step>
   </procedure>
   <para>
    If you only need a partition to backup, replace the
    <replaceable>SOURCE</replaceable> placeholder with your respective
    partition. In this case, your image file can lie on the same hard disk,
    but on a different partition.
   </para>
  </sect2>

<!-- TODO: please move this section to admin guide. Requirement from fate
    #305036 is:
- how and what to backup / recommendations 
  o System configuration with YaST
- backup/restore recommentation: 
  o customized config settings
  o ssh keys, certificates
  o system data backup (hints)
  o available backup tools
  o 3rd party backup tools (marketing task)
-->

  <sect2 id="sec.trouble.data.backup">
   <title>Backing Up Critical Data</title>
   <procedure id="proc.trouble.data.backup">
    <para>
     System backups can be easily managed using the &yast; System Backup
     module:
    </para>
    <step>
     <para>
      As &rootuser;, start &yast; and select <menuchoice>
      <guimenu>System</guimenu> <guimenu>System Backup</guimenu>
      </menuchoice>.
     </para>
    </step>
    <step>
     <para>
      Create a backup profile holding all details needed for the backup,
      filename of the archive file, scope, and type of the backup:
     </para>
     <substeps>
      <step>
       <para>
        Select <menuchoice> <guimenu>Profile Management</guimenu>
        <guimenu>Add</guimenu> </menuchoice>.
       </para>
      </step>
      <step>
       <para>
        Enter a name for the archive.
       </para>
      </step>
      <step>
       <para>
        Enter the path to the location of the backup if you want to keep a
        local backup. For your backup to be archived on a network server
        (via NFS), enter the IP address or name of the server and the
        directory that should hold your archive.
       </para>
      </step>
      <step>
       <para>
        Determine the archive type and click <guimenu>Next</guimenu>.
       </para>
      </step>
      <step>
       <para>
        Determine the backup options to use, such as whether files not
        belonging to any package should be backed up and whether a list of
        files should be displayed prior to creating the archive. Also
        determine whether changed files should be identified using the
        time-consuming MD5 mechanism.
       </para>
       <para>
        Use <guimenu>Expert</guimenu> to enter a dialog for the backup of
        entire hard disk areas. Currently, this option only applies to the
        Ext2 file system.
       </para>
      </step>
      <step>
       <para>
        Finally, set the search constraints to exclude certain system areas
        from the backup area that do not need to be backed up, such as lock
        files or cache files. Add, edit, or delete items until your needs
        are met and leave with <guimenu>OK</guimenu>.
       </para>
      </step>
     </substeps>
    </step>
    <step>
     <para>
      Once you have finished the profile settings, you can start the backup
      right away with <guimenu>Create Backup</guimenu> or configure
      automatic backup. It is also possible to create other profiles
      tailored for various other purposes.
     </para>
    </step>
   </procedure>
   <procedure id="proc.trouble.data.profile">
    <para>
     To configure automatic backup for a given profile, proceed as follows:
    </para>
    <step>
     <para>
      Select <guimenu>Automatic Backup</guimenu> from the <guimenu>Profile
      Management</guimenu> menu.
     </para>
    </step>
    <step>
     <para>
      Select <guimenu>Start Backup Automatically</guimenu>.
     </para>
    </step>
    <step>
     <para>
      Determine the backup frequency. Choose <guimenu>daily</guimenu>,
      <guimenu>weekly</guimenu>, or <guimenu>monthly</guimenu>.
     </para>
    </step>
    <step>
     <para>
      Determine the backup start time. These settings depend on the backup
      frequency selected.
     </para>
    </step>
    <step>
     <para>
      Decide whether to keep old backups and how many should be kept. To
      receive an automatically generated status message of the backup
      process, check <guimenu>Send Summary Mail to User root</guimenu>.
     </para>
    </step>
    <step>
     <para>
      Click <guimenu>OK</guimenu> to apply your settings and have the first
      backup start at the time specified.
     </para>
    </step>
   </procedure>
  </sect2>

  <sect2 id="sec.trouble.data.replay">
   <title>Restoring a System Backup</title>
   <para>
    Use the &yast; System Restoration module to restore the system
    configuration from a backup. Restore the entire backup or select
    specific components that were corrupted and need to be reset to their
    old state.
   </para>
<?dbfo-need height="20em"?>
   <procedure id="proc.trouble.data.replay">
    <step>
     <para>
      Start <menuchoice> <guimenu>&yast;</guimenu> <guimenu>System</guimenu>
      <guimenu>System Restoration</guimenu> </menuchoice>.
     </para>
    </step>
    <step>
     <para>
      Enter the location of the backup file. This could be a local file, a
      network mounted file, or a file on a removable device, such as a
      floppy or a DVD. Then click <guimenu>Next</guimenu>.
     </para>
     <para>
      The following dialog displays a summary of the archive properties,
      such as the filename, date of creation, type of backup, and optional
      comments.
     </para>
    </step>
    <step>
     <para>
      Review the archived content by clicking <guimenu>Archive
      Content</guimenu>. Clicking <guimenu>OK</guimenu> returns you to the
      <guimenu>Archive Properties</guimenu> dialog.
     </para>
    </step>
    <step>
     <para>
      <guimenu>Expert Options</guimenu> opens a dialog in which to fine-tune
      the restore process. Return to the <guimenu>Archive
      Properties</guimenu> dialog by clicking <guimenu>OK</guimenu>.
     </para>
    </step>
    <step>
     <para>
      Click <guimenu>Next</guimenu> to open the view of packages to restore.
      Press <guimenu>Accept</guimenu> to restore all files in the archive or
      use the various <guimenu>Select All</guimenu>, <guimenu>Deselect
      All</guimenu>, and <guimenu>Select Files</guimenu> buttons to
      fine-tune your selection. Only use the <guimenu>Restore RPM
      Database</guimenu> option if the RPM database is corrupted or deleted
      and this file is included in the backup.
     </para>
    </step>
    <step>
     <para>
      After you click <guimenu>Accept</guimenu>, the backup is restored.
      Click <guimenu>Finish</guimenu> to leave the module after the restore
      process is completed.
     </para>
    </step>
   </procedure>
  </sect2>

  <xi:include xmlns:xi="http://www.w3.org/2001/XInclude" 
   href="system_repair.xml"/>
 </sect1>
 <xi:include xmlns:xi="http://www.w3.org/2001/XInclude" os="sles" arch="zseries"
  href="zseries_rescue_initrd.xml"/>
</chapter>
