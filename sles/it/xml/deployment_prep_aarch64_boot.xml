<?xml version="1.0" encoding="UTF-8"?>
<sect1 xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:base="deployment_prep_aarch64_boot.xml" version="5.0" xml:id="sec.prep.boot.aarch64">
 <title>Controllo dell'installazione</title>

 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker/>
   <dm:translation>sì</dm:translation>
  </dm:docmanager>
 </info>

 <para>
  Controllare l'installazione seguendo uno dei tanti modi diversi. Il metodo più utilizzato consiste nell'installazione di <phrase role="productname"><phrase os="sles">SUSE® Linux Enterprise Server</phrase></phrase> dalla console del computer. Per situazioni diverse sono disponibili altre opzioni. 

 </para>

 <sect2 xml:id="sec.prep.boot.aarch64.terminal">
  <title>Installazione sulla console del computer</title>
  <para>
   Il modo più semplice per installare <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> è tramite la console del computer. Con questo metodo, un programma di installazione ad interfaccia grafica guida l'utente nel corso dell'operazione. Questo metodo di installazione viene presentato in dettaglio nel <xref linkend="cha.inst"/>.
  </para>
  <para>
   È comunque possibile eseguire l'installazione sulla console senza utilizzare una modalità di funzionamento grafica. Il programma di installazione con interfaccia testuale offre le stesse funzionalità della versione grafica. Informazioni dettagliate sull'utilizzo di questo modulo sono disponibili nel <xref linkend="sec.yast.navi.ncurses_modul"/>.
  </para>
 </sect2>

 <sect2 xml:id="sec.prep.boot.aarch64.serial">
  <title>Installazione tramite una console seriale</title>
  <para>
   Per questo metodo di installazione, è necessario un secondo computer collegato tramite un cavo <emphasis>null modem</emphasis> al computer sul quale si desidera installare <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase>. L'hardware e il firmware di entrambi i computer devono supportare la console seriale. Alcune implementazioni del firmware sono già configurate per inviare l'output della console di avvio a una console seriale (fornendo un albero di dispositivi con il percorso /chosen/stdout-path impostato adeguatamente). In questo caso non è necessaria alcuna configurazione aggiuntiva.
  </para>
  <para>
   Se il firmware non è impostato per utilizzare la console seriale per l'output della console di avvio, è necessario fornire il seguente parametro di avvio al prompt di avvio del sistema di installazione (vedere <xref linkend="sec.grub2.menu_change"/> per i dettagli): <option>console=<replaceable>TTY</replaceable>,<replaceable>BAUDRATE</replaceable></option>
  </para>
  <para>
   <replaceable>BAUDRATE</replaceable> deve essere sostituito dalla velocità di trasmissione per l'interfaccia. I valori validi sono 115200, 38400 o 9600. <replaceable>TTY</replaceable> deve essere sostituito dal nome dell'interfaccia. La maggior parte dei computer dispone di una o più interfacce seriali, i cui nomi variano a seconda dell'hardware:
  </para>
  <itemizedlist>
   <listitem>
    <para>
     <emphasis>ttyS0</emphasis> per APM
    </para>
   </listitem>
   <listitem>
    <para>
     <emphasis>ttyAMA0</emphasis> per SBSA (Server Base System Architecture)
    </para>
   </listitem>
   <listitem>
    <para>
     <emphasis>ttyPS0</emphasis> per Xilinx
    </para>
   </listitem>
  </itemizedlist>
  <para>
   Per l'installazione, occorre un terminale come minicom o screen. Ad esempio, avviare il programma screen in una console locale digitando il seguente comando:
  </para>
<screen>
screen /dev/ttyUSB0 115200
</screen>
  <para>
   Questo significa che il programma screen rimane in ascolto sulla prima porta seriale con una velocità di trasmissione di 115200 baud. Da questo momento in poi l'installazione procede come quella basata su testo, tramite tale terminale.
  </para>
 </sect2>

 <sect2 xml:id="sec.prep.boot.aarch64.ssh">
  <title>Installazione con SSH</title>
  <para>
   Se non dispone di un accesso diretto all'hardware del computer e, ad esempio, l'installazione deve essere avviata da una console di gestione, è necessario controllare l'intero processo di installazione tramite la rete. A tale scopo immettere i parametri <option>ssh=1</option> e <option>ssh.password=<replaceable>SECRET</replaceable></option> al prompt di avvio. Nel sistema viene quindi avviato un daemon SSH ed è possibile eseguire il login come utente <systemitem class="username">root</systemitem> con password <replaceable>SECRET</replaceable>.
  </para>
  <para>
   Per eseguire la connessione, utilizzare <command>ssh -X</command>. Se è disponibile un server X locale, è supportato l'X-Forwarding su SSH. In caso contrario, YaST fornisce un'interfaccia di testo tramite ncurses. YaST guida quindi l'utente in tutta la procedura di installazione. Questo passaggio è descritto dettagliatamente nella <xref linkend="sec.deployment.remoteinst.scenario.sshdyn"/>.
  </para>
  <para>
   Se non si dispone di un server DHCP sulla rete locale, assegnare manualmente un indirizzo IP al sistema di installazione. A tale scopo, immettere l'opzione <option>HostIP=<replaceable>IPADDR</replaceable></option> al prompt di avvio.
  </para>
 </sect2>

 <sect2 xml:id="sec.prep.boot.aarch64.vnc">
  <title>Installazione con VNC</title>
  <para>
   Se non si dispone di accesso diretto al sistema, ma si desidera un'installazione grafica, installare <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> tramite VNC. Questo passaggio è descritto dettagliatamente nella <xref linkend="sec.deployment.remoteinst.monitor.vnc"/>.
  </para>
  <para>
   Dal momento che sono disponibili client VNC appropriati anche per altri sistemi operativi, come Microsoft Windows e MacOS, l'installazione può essere controllata da computer che eseguono tali sistemi operativi.
  </para>
 </sect2>

 <sect2 xml:id="sec.prep.boot.aarch64.autoyast">
  <title>Installazione con AutoYaST</title>
  <para>
   Se è necessario installare <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> in molti computer con hardware simile, è consigliabile eseguire le installazioni tramite AutoYaST. In questo caso, iniziare con l'installazione di un <phrase role="productname"><phrase os="sles">SUSE Linux Enterprise Server</phrase></phrase> e utilizzarlo per creare i file di configurazione AutoYaST necessari.
  </para>
  
  
 </sect2>
</sect1>
